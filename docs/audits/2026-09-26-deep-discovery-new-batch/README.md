# 2026-09-26 凌晨深度发现新增核查

结论：两组重名均有原文支持，未发现这四条姓名错绑；整批数据不能验收通过。凌晨发布使昨晚身份修复失效，703 条空姓名记录仍进入候选库，72 个已删除邮箱被重新导入。

## 范围与方法

用户明确要求 SSH 检查今天凌晨新增记录。服务器时间约 2026-09-26 09:48，查询 discoveredAt 为当天的三层 ES，当前返回记录全部发生在 02:00—03:01。任务 20077 于 02:00 启动、03:01:22 结束，PARTIAL_SUCCESS。

所有 1,010 条现存记录均在服务器内检查姓名完整性、批内重名、跨层同邮箱姓名冲突、新版凭证及删除哈希名单。仅对两组重名共四条逐条对照公开论文原文；其余有姓名记录尚未全部逐条来源核实，不能宣称整批邮箱归属正确。

自动审批拒绝全量明文快照导出，改用服务器端内存处理，只返回汇总；定向复核的四条仅保留姓名、脱敏邮箱、哈希和公开论文定位。未修改线上数据、代码、任务或发送设置。

## 全量检查

| 项目 | 数量 |
|---|---:|
| RAW 现存当日记录 | 1,010 |
| CANDIDATE 同批记录 | 1,010 |
| APPLICATION 同批记录 | 0 |
| 按规范化邮箱去重后的专家档案 | 1,010 |
| 完整姓名 | 307 |
| 姓、名均为空 | 703 |
| 批内规范化完整姓名重复 | 2 组、4 条 |
| 跨层同邮箱姓名冲突 | 0 |
| 有昨晚新版身份凭证 | 0 |
| 命中昨天最终删除的 1,923 邮箱名单 | 72 |

72 条回流中 22 条空姓名、50 条完整姓名，与 703 条空姓名存在重叠。空姓名不等于已证明绑定给另一人，但这些线索不应按昨晚的新规则成为可联系专家。缺少凭证也不等于全部姓名错误。

来源：OPENALEX 287、ARXIV 491、CROSSREF 232。空姓名分别为 221、291、191。02 时段 972 条、03 时段 38 条。

任务统计 indexed/promoted 各 1,015，与当前现存独立档案 1,010 相差 5；本报告以实际 ES 快照为准，未把写入成功次数等同于独立新增人数。该差额原因尚未查实。

## 重名逐条核对

1. **Edward Raff，两条正确：同一作者的两个邮箱。** 原论文署名段明确将 CrowdStrike 邮箱和 UMBC 邮箱同时列在 Edward Raff 名下；两个公开邮箱的 SHA256 与库内两条邮箱分别一致。[原文](https://arxiv.org/html/2609.21306v1)
2. **Hang Zhao，两条各有来源支持。** 港科大广州论文列出 Hang Zhao 及该校邮箱；清华相关论文列出 Hang Zhao、清华 IIIS 机构及清华邮箱。两个公开邮箱的 SHA256 与库内记录一致。不能仅凭同名合并，也不据此断言两条一定属于同一人。[港科大广州论文](https://arxiv.org/html/2609.22795v1)、[清华相关论文](https://arxiv.org/html/2609.21983v1)

## 修复被覆盖的证据

- 昨晚 20:34 身份修复包：`0746909091c6578e279932cfd2dd208178778839c79cc4a77fd4683f69e1c001`。
- 22:37 邮箱发布备份仍包含 `DiscoveryIdentity.class`，没有丢失该修复。
- 当前 WAR 修改时间：2026-09-26 00:34:22；SHA256：`bee031087e6a268bc42d50628165ec384503096b82b4592032bd7f65f3898d95`。
- 当前 WAR 和实际展开运行目录均缺少 `DiscoveryIdentity.class`、删除名单资源。
- 当前 `JatsXmlEmailParser.class` 哈希与昨晚修复前版本完全一致；六个关键类均不匹配昨晚修复版本，其中身份模块完全缺失。

可确认后续部署覆盖了身份修复。尚未确认具体发布操作者或构包步骤，不把 22:37 邮箱补丁误判为覆盖来源。

## 发送影响与建议

- 新增批次邮箱今天关联的 OUTBOUND 邮件记录为 0；有 1 个既有联系人、今日新建联系人为 0。没有读取邮件正文。
- 发送配置 1、3、4、5 的 auto_enabled 均为 0。
- 保持停发。先将身份修复合入当前发布基线，保留其后其他已上线功能，再校验运行代码及资源；不能直接整包回滚覆盖其他功能。
- 随后按名单处理 72 条回流和 703 条空姓名记录，并对剩余档案补充来源核验。此轮仅检查，没有执行删除、隔离或重新发布。

证据：`summary.json`、`impact-summary.json`、`duplicate-groups-masked.json`、`deployment-check.json`、`regression-check.json`。
