## Light Verification: LIGHT_PASS
Child: 04 — docs/plans/2026-08-24/04-expert-rnd-incremental-classification.md (identity commit 3a4162c9c458f899470f59ac6e1a07b9ba748b3a, unamended)
Boundary: bad5a164fa0ea9ed4a41ae5f1871fd083cac932b..cc41036a83d1a6c4c78320caf8292152f4fae02c
Verifier: Ver04

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
|Authorized scope|PASS|Implementation commit `cc41036a` (parent eb37bbb, message `feat(fast-p): implement 04`) touches exactly the 6 authorized files, +520/-1: `ExpertClassificationProperties.kt` (NEW), `RestTemplateConfig.kt` (+1 line), `application.yml` (+6 lines), `ExpertClassificationScheduler.kt` (NEW), `ExpertClassificationSchedulerTest.kt` (NEW), `docs/runbooks/expert-classification-backfill.md` (append-only). `git show --stat cc41036a` lists only these 6. The 5 extra `docs/plans/fast/` files in the range diff belong to controller evidence commit eb37bbb (docs(fast-p): record 03 light verification), which per plan is committed separately and excluded from the implementation commit. No store/schema files touched.|
|Plan and invariants|PASS|I4-1: `@ConditionalOnProperty(prefix="talent-introduction.expert-classification", name=["incremental-enabled"], havingValue="true")` (Scheduler.kt:31-36); yml `incremental-enabled: ${EXPERT_CLASSIFICATION_INCREMENTAL_ENABLED:false}` (application.yml:33); properties default `incrementalEnabled=false` (Properties.kt:26); test `scheduler bean absent by default and when disabled, present when enabled` asserts empty/no-failed context by default and false, 1 bean when true (Test.kt:283-303). I4-2: fixed request CANDIDATE/EXECUTE/rnd-v1-2026/onlyPending=true/confirmation=`EXECUTE_CANDIDATE:rnd-v1-2026` (Scheduler.kt:52-62); request model has no force field (BackfillService.kt:21-31); test asserts every field + initial-progress details, no RAW/APPLICATION (Test.kt:107-132). I4-3: shared `TASK_TYPE="EXPERT_CLASSIFICATION_BACKFILL"` (BackfillService.kt:327) + same `ExpertClassificationBackfillService` + same `@Qualifier("expertClassificationExecutor")`; lock-failure log `incremental classification skipped: task already running` + return, no second task (Scheduler.kt:77-86); tests `skip when lock held by manual backfill, no executor submission`, `executor rejection clears pending token and records FAILED` (Test.kt:158-190, 218-242). I4-4: defaults 500/250/50000, init validation 100..1000/0..5000/1..200000 (Properties.kt:34-38); tests accept boundary + reject out-of-range (Test.kt:243-266); cap `counters.scanned >= request.maxDocs` ends loop normally with no failure marker → normal SUCCESS result path, remaining stays pending for next run (BackfillService.kt:114,120). I4-5: pending filter is pure exists/term on `expertClassification.version` — no script query comparing ES fields (BackfillService.kt:178-191); zero script usage in the 6 changed files; runbook states no same-version recompute. Runbook section 12: enable only after steps 4/5/8 (回填/抽样/发送门禁) acceptance, task-history query before/after, no RAW, no same-version recompute, UNKNOWN stays unsendable.|
|Required commands|PASS|`JAVA_HOME=/Library/Java/JavaVirtualMachines/zulu-11.jdk/Contents/Home mvn test -Dtest=ExpertClassificationSchedulerTest` → surefire `Tests run: 6, Failures: 0, Errors: 0, Skipped: 0`, exit 0. `JAVA_HOME=... mvn test` (full) → `Tests run: 2828, Failures: 0, Errors: 0, Skipped: 4`, `BUILD SUCCESS`, exit 0 (matches child-04-head baseline 2828; 4 skipped are pre-existing @Disabled). `git diff --check` → clean on worktree and on bad5a16..cc41036a (exit 0 both). All run freshly in this verification.|
|Downstream interfaces|PASS|Last child — consumed by runbook/manual acceptance only. Scheduler lifecycle (Scheduler.kt:64-140) matches child 02 controller (ExpertClassificationAdminController.kt:58-135) verbatim: identical initialProgress details map → `tryStartWithToken(TASK_TYPE, initialProgress)` → `if (!started)` skip → `var executionId` → `try { executor.execute { try { runAndRecordWithResult(TASK_TYPE, trigger, request, onStarted { bindExecutionId }) { backfillService.run(request, executionId!!) } } catch { update FAILED } finally { clearExecutionContext(execId ?: pendingToken) } } catch (RejectedExecutionException) { update FAILED; clearExecutionContext(pendingToken) }` — only deltas: trigger `"SCHEDULED"` vs `"MANUAL"`, plan-mandated skip log, no HTTP response. Properties bound under `talent-introduction.expert-classification` with env fallbacks + construction validation. No new shared store fields: boundary diff touches only the 6 authorized files.|
### AUTO_FIX
- N/A

### RECORD_ONLY
- N/A

### Required Action
- COMPLETE_CHILD
