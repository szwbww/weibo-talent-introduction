## Light Verification: LIGHT_PASS
Child: 03 — docs/plans/2026-08-24/03-expert-rnd-send-gate.md (amended A3; plan identity commit:e53d4e745b647acab3c5c418d762a509b6286932, plan content SHA-256 3fcb5ae4… matches on-disk file)
Boundary: ec7226b485dbfff98a33260e68ef289df3fa1169..bad5a164fa0ea9ed4a41ae5f1871fd083cac932b
Verifier: Ver03

### Four Gates

|Gate|Result|Evidence|
|---|---|---|
|Authorized scope|PASS|Boundary contains exactly the 9 authorized files under src/ (git diff --name-only ec7226b..bad5a16 -- src/): f3da97a touches the 7 plan files (ExpertSearchService.kt, InitialOutreachService.kt, BatchExecutionModels.kt, ManualInitialOutreachService.kt, ExpertSearchServiceTest.kt, InitialOutreachServiceTest.kt, ManualInitialOutreachServiceTest.kt); bad5a16 touches exactly the 2 A3 files (OperatorStatusWriteSeamGuardTest.kt, BatchSendTaskRuntimeIntegrationTest.kt, +19/−3). Intermediate commits 583744f/e53d4e7/776a8dc are controller docs evidence (docs/plans/fast/ + plan A3 amendment), not src/. No src/main/resources or config changes; working tree src/ clean. Plan 变更文件清单 lists all 9. |
|Plan and invariants|PASS|I3-1 fail-closed, no bypass/off-switch: memory gate `mailType == INTRODUCTION.name && profile.expertClassification?.sendable != true → false` (BatchExecutionModels.kt:65); last-check in sendInitialBatch (InitialOutreachService.kt:42) and round loop (ManualInitialOutreachService.kt:607); ES parse fail-closed — unknown type ⇒ classification null ⇒ rejected (ExpertSearchService.kt:502-517); `sendable` is a derived getter `type in SENDABLE_TYPES`, ES value cannot spoof (ExpertClassification.kt:36-37). No config key toggles the sendable gate (pre-existing child-02 `gate_filter_enabled` governs I4a template gate-fields only, not in boundary). Tests: `matchesExpert INTRODUCTION sendable gate rejects null false and non-sendable types allows only sendable (I3-1)`, `sendInitialBatch last-chance gate rejects null classification with zero writes (I3-1)`. I3-2 ES/memory parity: `expertSendableFilter()` exact term (ExpertSearchService.kt:55-56); `searchSendableExpertsWithEmail` = exists email AND term, level-sorted (ExpertSearchService.kt:349-388); `searchExpertsWithEmail` untouched (test `searchExpertsWithEmail stays a generic query without the sendable term (I3-2)`); memory/ES parity test `matchesExpert sendable gate agrees with ES sendable term per profile (I3-2)`. I3-3 same-source preview/execution: countEsTargets (ManualInitialOutreachService.kt:1263-1268) and fetchEsPage (:1274-1280) both call buildEsFiltersForLevel; preview (:463-466) and execution (:510-512) both reuse buildRetryableTargets + countEsTargets; buildRetryableTargets applies scope.matchesExpert (:1028); test asserts totalSendable == execution total, sent=1, skipped=1. I3-4 last-check: both delivery services re-check before contact/account/render (InitialOutreachService.kt:42, ManualInitialOutreachService.kt:607); tests assert verifyNoInteractions(composer/delivery) and never().save()/never().selectAccount; reason `EXPERT_NOT_SENDABLE` label `专家非生产/科研可发类型` (BatchExecutionModels.kt:149-161) asserted with count=1 in round-loop test. I3-5 MATERIAL_REMINDER zero impact: gate condition only for INTRODUCTION (BatchExecutionModels.kt:65), filter append only for INTRODUCTION (ManualInitialOutreachService.kt:1318-1322); tests assert MATERIAL_REMINDER query JSON lacks expertClassification term and missing-classification/UNKNOWN fixtures still match+send. |
|Required commands|PASS|All fresh this invocation, JAVA_HOME=…/zulu-11.jdk/Contents/Home: ExpertSearchServiceTest → Tests run: 56, Failures: 0, Errors: 0, Skipped: 0; BUILD SUCCESS; exit 0 (surefire XML 56/0/0/0, fresh run). InitialOutreachServiceTest → 13/0/0/0, BUILD SUCCESS, exit 0. ManualInitialOutreachServiceTest → 92/0/0/0, BUILD SUCCESS, exit 0. BatchSendTaskRuntimeIntegrationTest → 21/0/0/0, BUILD SUCCESS, exit 0. OperatorStatusWriteSeamGuardTest → 1/0/0/0, BUILD SUCCESS, exit 0. Full `mvn test` → Tests run: 2822, Failures: 0, Errors: 0, Skipped: 4; BUILD SUCCESS; exit 0 (matches child 03 head baseline 2822; base 2739 not re-run — regression count exceeds base). `git diff --check` → exit 0, no whitespace errors. |
|Downstream interfaces|PASS|`ExpertSearchService.expertSendableFilter()` exact term `mapOf("term" to mapOf("expertClassification.sendable" to true))` (ExpertSearchService.kt:55-56). `searchSendableExpertsWithEmail(size, level=CANDIDATE)` = exists email AND expertSendableFilter(), honors explicit level + level sort (:349-388). `searchExpertsWithEmail` unchanged (generic query, no term — test evidence). `RecipientScope.matchesExpert`: INTRODUCTION memory gate first, MATERIAL_REMINDER skipped (BatchExecutionModels.kt:65). `buildEsFiltersForLevel`: INTRODUCTION appends term at end of filters, MATERIAL_REMINDER does not (ManualInitialOutreachService.kt:1318-1322). `BatchOutcomeReasonCodes.EXPERT_NOT_SENDABLE` with label `专家非生产/科研可发类型` (BatchExecutionModels.kt:149,161). countBySnapshot/countEsTargets/fetchEsPage/buildRetryableTargets topology unchanged — boundary diff to ManualInitialOutreachService.kt is +23 lines only (round-loop gate + filter append), no seam call-site edits. |

### AUTO_FIX
- N/A (no four-gate violation found; plan A3-authorized refreshes correctly applied: guard pin 445→491 with unchanged context, fixture default sendable classification type=ACADEMIC_RND/version=rnd-v1-2026, both matching A3 exact values)

### RECORD_ONLY
- N/A (no incidental issues observed; pre-existing child-02 `gate_filter_enabled` (V99 migration, commit ccf49e6) is outside this boundary and governs the I4a template gate-fields feature, not the I3-2 sendable gate)

### Required Action
- COMPLETE_CHILD
