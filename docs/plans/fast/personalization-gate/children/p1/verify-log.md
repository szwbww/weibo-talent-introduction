## Light Verification: LIGHT_PASS_WITH_NOTES
Child: p1 · 发送侧硬闸门 — docs/plans/2026-08-09/personalization-gate-p1-send-gate.md
Boundary: ab5dcbb7fbb58f5e8a9b13b7e54022effd270b77..07a77f3e15da0d56317ec413412a5ca15ece913b
Verifier: P1Verifier (fresh; distinct from implementer P1Implementer)
Epoch: 1 · Attempt: 1 · Timestamp: 2026-08-09T12:10:00+08:00

### Four Gates
| Gate | Result | Evidence |
|---|---|---|
| Authorized scope | PASS | `git diff --name-status ab5dcbb..07a77f3` = exactly the brief's 15 authorized files: 10 production (BatchExecutionModels.kt, ManualInitialOutreachService.kt, IntroductionMailComposer.kt, MailPlaceholderService.kt, MailVariableService.kt, ManualExpertMailService.kt, PersonalizationGateService.kt(new), MailComposeTemplate.kt, MailComposeTemplateService.kt, V84__add_required_keys_to_compose_template.sql(new)) + 5 tests (IntroductionMailComposerTest.kt, MailVariableServiceTest.kt, ManualExpertMailServiceGateTest.kt(new), PersonalizationGateServiceTest.kt(new), MailComposeTemplateServiceTest.kt). +960/−16, single commit `07a77f3 feat(fast-p): implement p1`. Worktree otherwise clean; untracked `docs/plans/**` are controller evidence, excluded per brief #6. |
| Plan and invariants | PASS | I-1: ManualExpertMailService.kt:174-181 production path builds variables exclusively via `mailVariableService.buildVariables(account, expert, contact.expertEmail, previewFallbacks = false, contact = contact)` with expert from `resolveExpertProfileFor(contact)`; `mailTemplateVariables` deleted from this file (residual copies exist only in untouched AutoMailReplyService/AutoReplyPreviewService — pre-existing private helpers, out of scope); ManualExpertMailServiceGateTest.kt:192-218 asserts real signed unsubscribe URL + derived primaryResearchField in sent text/body, no `${` residue. I-2: PersonalizationGateService.kt:66-77 `requireNoPlaceholderResidue` regex `\$\{[^}]*\}`, no config switch; invoked before `mailDeliveryService.send` (ManualExpertMailService.kt:60-63) and before return in IntroductionMailComposer.kt:42; PersonalizationGateServiceTest.kt:91-117 + GateTest.kt:239-247 (send never called). I-3: evaluate() (PersonalizationGateService.kt:37-52) runs `detectFallbackKeys` per raw text then intersects requiredKeys; PersonalizationGateServiceTest.kt:17-30 (exact intersection), :36-42 (real value → not blocked), :46-56 (rendered text not misjudged), :62-71 (multi-key), :79-85 (empty required → disabled). I-4: effectiveRequiredKeys (MailComposeTemplateService.kt:140-145) + parseRequiredKeys (:156-170): null/blank/empty-array/parse-fail → `emptyList()`, `log.warn` on parse failure, no throw; MailComposeTemplateServiceTest.kt:1035-1041 parameterized null,"","   ","[]","{}","{不是数组}". I-5: PersonalizationGateService wired in ManualExpertMailService.kt:30 and IntroductionMailComposer.kt:14; IntroductionMailComposerTest.kt:422-447 (byCode path via rendered.templateId=10) and :450-475 (templateId=7 path) assert PersonalizationGateException. I-6: ManualInitialOutreachService.kt:322-327 (MATERIAL_REMINDER) and :598-608 (INTRODUCTION) catch PersonalizationGateException BEFORE the generic/TEMPLATE_RENDER_FAILED catch, `recordSkipped(PERSONALIZATION_INCOMPLETE, "个性化字段缺失…")`, no stopReason/midRoundStop; BatchExecutionModels.kt:94 + :105 const+label "个性化字段缺失"; GateTest.kt:329-341 asserts skippedReasonsMap contains PERSONALIZATION_INCOMPLETE and failure not incremented. I-7: MailPlaceholderService.kt:160 `ES_FIELD_BY_KEY["primaryResearchField"]=="researchFields"`; MailVariableService.kt:146-150 derives first `", "`-segment trimmed; MailVariableServiceTest.kt:539-556. I-8: all four metadata sets (MailPlaceholderService.kt:115, :137, :160, :183); MailVariableServiceTest.kt:560-571 (esField=="researchFields", nullable, example non-blank, label "主要研究方向"). |
| Required commands | PASS | 1) `JAVA_HOME=…zulu-11 mvn test -Dtest='PersonalizationGateServiceTest,ManualExpertMailServiceGateTest,MailVariableServiceTest,IntroductionMailComposerTest,MailComposeTemplateServiceTest'` → exit 0, BUILD SUCCESS; per-class Tests run 9+38+5+9+37 = **98**, Failures 0, Errors 0, Skipped 0 (surefire-reports). 2) `JAVA_HOME=…zulu-11 mvn clean package` → exit 0, BUILD SUCCESS, WAR repackaged; Java **2223** run, 0F 0E, 4S (baseline 2196 0F 0E 4S — +27 new tests, no regression); Node **474 pass / 0 fail** (baseline 474 pass). 3) `git diff --check` → empty, exit 0. |
| Downstream interfaces | PASS | `MailComposeTemplateService.effectiveRequiredKeys(templateId: Long): List<String>` non-null (MailComposeTemplateService.kt:140-145); `requiredEsFields(templateId: Long): List<String>` non-null (:149-154); `ES_FIELD_BY_KEY["primaryResearchField"]=="researchFields"` (MailPlaceholderService.kt:160); V84 `required_keys VARCHAR(500) NULL` + plan-exact COMMENT (V84__add_required_keys_to_compose_template.sql:5-7). Raw gate input via `ComposeTemplateRenderResult.rawTexts` + `templateId`, both defined inside the authorized file (MailComposeTemplateService.kt:653-660); byCode path resolves via `rendered.templateId` (IntroductionMailComposer.kt:26). All four interfaces match the brief verbatim — P2 consumption unaffected. |

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1 (controller deviation 1, test-only fallback): `ManualExpertMailService.composeComposeTemplate` retains a branch when `mailVariableService == null` (legacy 9-arg constructor): `senderVariables(account) + MailVariableService.EXPERT_KEYS.associateWith { "" }` (ManualExpertMailService.kt:183) and `plainTextToHtml` HTML fallback (:220). Adjudication: grep of `ManualExpertMailService(` across src/ finds constructors only in tests — ManualExpertMailServiceGateTest.kt:53 (full 11-arg incl. mailVariableService → production path) and pre-existing, unmodified ManualExpertMailServiceTest.kt:37 (9-arg). The @Service bean is always Spring-DI-injected, so the fallback is unreachable in production; I-1's production contract (buildVariables as sole variable source) holds, and the I-2 residue check (runs on every send) + I-3 evaluate still guard the fallback path, preventing the I-1 violation consequence there. Not AUTO_FIX: the plan-unique correction (delete fallback + legacy constructor) would break pre-existing ManualExpertMailServiceTest.kt, which is NOT in the authorized file list — no correction inside authorized files alone. Controller deviations 2 (rawTexts/templateId gate input) and 3 (MATERIAL_REMINDER catch has no `continue`, matching the plan snippet and the loop's pre-existing generic-catch pattern at base) adjudicated compliant — no record needed.

### Required Action
- COMPLETE_CHILD
