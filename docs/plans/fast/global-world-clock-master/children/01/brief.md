# Fast-P Child Brief 01

- Child ID: 01
- Approved plan: `docs/plans/2026-09-17/global-world-clock-01-component.md` (identity `commit:96a0a411c88eaa447c2c6512f9c70fa8d8aaffb1`)
- Master plan: `docs/plans/2026-09-17/global-world-clock-master.md`
- child_base_sha: `24f5c8205a304d3682e09e02458960bc2caa0463`
- Worktree: `/Users/lukai/IdeaProjects/weibo-talent-introduction-fast-global-world-clock-master`
- Branch: `fast/global-world-clock-master`

The approved plan is the complete contract. Read it in full first, plus the master plan's 关键不变量/样式契约 sections and the audit attachment `docs/plans/2026-09-17/global-world-clock-audit.txt`. Do not reinterpret, re-scope, or redesign it.

## Authorized files (exactly these three; all new)

1. `src/main/resources/static/world-clock.js`
2. `src/main/resources/static/world-clock.css`
3. `src/test/js/worldClock.test.js`

No other file may be created, modified, or deleted. In particular: `index.html`, `app.js`, `styles.css`, `meeting-confirmation.js`, all other `src/test/js/*.test.js`, and any backend/Maven file are out of scope for this child. Registration/activation is child 02 and must not be anticipated.

## Hard constraints

- Global constraints from the master plan: no new backend endpoint/DB field/date library/map/config centre/global nav menu/mail logic/device sync/localStorage preference; read-only catalogue through the existing `api()`; consume the existing `MailboxMeeting.filterZones`; textContent-only rendering.
- S-4 is a byte contract: `world-clock.css` must equal the fenced block between `<!-- WORLD_CLOCK_CSS_BEGIN -->` and `<!-- WORLD_CLOCK_CSS_END -->` exactly (UTF-8/LF, one trailing newline), and the trigger/panel/row templates between the other three marker pairs must be used verbatim (only JS string escaping may differ). The test extracts expectations from those markers in the plan file — do not create a second CSS source.
- Invariants I-1..I-9 as defined in the plan, including: single global instance, insertion before `#logoutBtn` inside `.topnav-side`, panel as direct child of `.topnav`, no `.nav-tab`/`data-view` on the new button, one-line header with icon-only degradation, frozen user selection vs live clock, one-epoch IANA conversion, catalogue reuse without trusting backend offsets, in-memory-only state with request dedup + late-response rejection + auth-hide/destroy cleanup, read-only transport, and the accessibility/lifecycle rules of I-9.
- Downstream interface required by child 02 (keep exactly): files named `world-clock.js` / `world-clock.css`; plain script (not an ES module) that works when loaded after `app.js`; exposes `window.WorldClock` and auto-boots on DOMContentLoaded; reads the host `window.api` and `MailboxMeeting.filterZones`; adds the `world-clock-header` class and its own `#worldClockTrigger`/`#worldClockPanel` nodes at runtime; changes nothing in `index.html`. Node export (`module.exports`) is required for the tests.
- Deterministic conversion table in the plan's 验收标准 must be hard-coded as expected values in the test (never generated by the code under test).
- Test DOM stubs must return `null` for unknown ids (no auto-creating stub). CSS/template tests must read the markers from the plan file.

## Required commands (run all, from the worktree root)

```bash
node --check src/main/resources/static/world-clock.js
node --test src/test/js/worldClock.test.js
TZ=UTC node --test src/test/js/worldClock.test.js
TZ=America/Los_Angeles node --test src/test/js/worldClock.test.js
node --test src/test/js/meetingConfirmation.test.js src/test/js/meetingConfirmationAssets.test.js
```

Do not run the Maven build or the full JS suite: not required for this child, and the full suite is child 02's gate. Do not run formatters or linters.

## Deliverable

- All three files complete and self-contained; no stubs, TODOs, or placeholder behaviour.
- Local commit containing only the three authorized files, message: `feat(fast-p): implement 01`
- Full result written to `docs/plans/fast/global-world-clock-master/children/01/execution.md` (command, exit code, pass/fail counts, files, commit SHA, deviations).
- Explicitly exclude `docs/plans/fast/**` from the implementation commit; the controller commits evidence separately.
- Return only: `READY_FOR_VERIFICATION` | `BLOCKED` | `PLAN_CONFLICT`, the commit SHA, a command summary, and the report path. Never push, merge, rebase, amend, or rewrite history. Never repair unrelated behaviour. If the plan is ambiguous or a requirement needs a new file/behaviour outside the authorized set, stop and return `PLAN_CONFLICT`.
