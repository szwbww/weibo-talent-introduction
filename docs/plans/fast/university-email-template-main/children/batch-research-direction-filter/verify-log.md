# Verification Log — batch-research-direction-filter

## Light Verification: LIGHT_PASS_WITH_NOTES
Child: batch-research-direction-filter (docs/plans/2026-09-18/batch-research-direction-filter.md)
Boundary: 971a21d35a0d5bdffefa4aff9dc6faf93819207d..decdb28dfe6431c9f238b76fa64fc9a1aad7939e
Verifier: ResearchDirectionVerifier

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
|Authorized scope|PASS|`git diff-tree --root --no-commit-id --name-status -r decdb28` = 1×`A` + 9×`M` = exactly the 10 authorized paths (A: `V128__add_research_direction_filter_to_batch_send_task_config.sql`, the next number after `V127__add_outbound_attachments_snapshot.sql`); no CSS file, no second migration, no other file added. `git diff 971a21d..decdb28 --numstat -- src/main/resources/static/styles.css` empty and index.html = `18 0`; `?v=` occurs 0 times in the index.html diff (no cache-key bump). Evidence commits `2bf2cd4`/`24bec04` touch only `docs/plans/fast/**`.|
|Plan and invariants|PASS|I-1: `ResearchDirectionFilters` `ALLOWED={ANY,PRESENT,ABSENT}` + `requireAllowed` (`BatchSendTaskConfig.kt:119-140`); migration `V128:6-7` `VARCHAR(16) NOT NULL DEFAULT 'ANY'`; write `BatchSendTaskConfigService.kt:82,118`, read `:496`, legacy API preservation `:205` (`= existing.researchDirectionFilter`), save-side rejection `:322`, manual-start rejection `BatchSendControlService.kt:434`, snapshot `BatchExecutionModels.kt:342`, scope `:173`; UI round trip `index.html:1288,1501-1503` + `app.js:15993,16763,16905,17129,17149`. I-2: `ManualInitialOutreachService.kt:1317-1330` — no `ANY` branch, `PRESENT` = `ExpertSearchService.fieldPresenceFilter(ResearchDirectionFilters.ES_FIELD)`, `ABSENT` = that filter inside `bool.must_not`; in-memory rule `BatchExecutionModels.kt:107-110` (`isNullOrEmpty`); estimation and execution both go `countBySnapshot`/`runIntroductionFromSnapshot` → `resolveScope` (`:428`, unchanged) → `buildEsFiltersForLevel` (`:1317`, `:1263`, `:1275`, `:1209`). I-3: direction, gate fields and `expertTypesFilter` are flat AND entries in the same filter list; `ABSENT` + `gateEsFields=["researchFields"]` keeps both the presence and the absence filter (no silent drop) and memory returns false for both null and `"AI"`; send-side gate files are not in the commit. S-1: new DOM uses only `bsc-input/bsc-select/batch-config-field/batch-config-field-label/batch-config-diff-badge/batch-config-diff-original`, zero `style="`, CSS diff empty (JS H6 `batchSendTaskConsoleInteraction.test.js:2357-2408`). Tests: Kotlin `ManualInitialOutreachServiceTest.kt:4303,4335,4364,4401,4440,4465,4494,4554,4580,4633`; JS H1–H7 `:2206-2443`; only pre-existing test edit is adding `researchDirectionFilter: "ABSENT"` to the complete-manual-snapshot fixture (`:1124-1125`), no assertion weakened.|
|Required commands|PASS|1) `node --test src/test/js/batchSendTaskConsoleInteraction.test.js` → exit 0, `tests 81 / pass 81 / fail 0` (baseline 74 → +7). 2) `JAVA_HOME=…/zulu-11.jdk/Contents/Home mvn test -Dtest='ManualInitialOutreachServiceTest,BatchSendTaskConfigServiceTest,BatchSendTaskRuntimeIntegrationTest' -DfailIfNoTests=false` → exit 0, `BUILD SUCCESS`; fresh surefire `Tests run: 110 / 64 / 22, Failures: 0, Errors: 0` = 196 (baseline 186 → +10); same run's exec-plugin JS suite `tests 1003 / pass 1003 / fail 0` (baseline 996 → +7). 3) `git diff --check` → exit 0, no output. Worktree product tree clean at `decdb28` (only `docs/plans/fast/**` dirty/untracked).|
|Downstream interfaces|PASS|Child-1 frozen seams unchanged or compatibly extended: `ManualInitialOutreachService.resolveScope` diff line count 0 (`git diff 971a21d..decdb28 -- …/ManualInitialOutreachService.kt` = only an import + the `ANY`-free `when` block in `buildEsFiltersForLevel`); `BatchExecutionModels.kt:77 matchesExpert(profile)`, `:153 fromSnapshot(snapshot)` signatures unchanged, the new scope field is trailing and defaulted (the two in-file positional constructions `:1045,:1185` still compile and fall back to `ANY`); `ExpertSearchService.kt:35 ALLOWED_HAS_FIELDS`, `:55 fieldPresenceFilter` untouched (file not in commit) and now consumed, not reimplemented; `GET /api/compose-templates/{id}/gate-fields` (`template/controller/MailComposeTemplateController.kt:51`) and all template/`PersonalizationGateService` files are outside the commit, so the child-1 template gate contract is neither reimplemented nor weakened.|

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1: The migration/entity mapping clause of I-1 ("existing rows and absent values are ANY") is not exercised by any directed test — all three required Kotlin classes are Mockito-based (no DB/Flyway test is in the authorized command set, and `FlywayMigrationIntegrationTest` is Docker-gated/opt-in), so `V128`'s backfill rests on reading only (`V128:6-7` column `DEFAULT 'ANY'`, entity default `BatchSendTaskConfig.kt:29-30`, `toView` passthrough `BatchSendTaskConfigService.kt:496`, and the existing camelCase→snake_case precedent `gate_filter_enabled`↔`gateFilterEnabled`). Evidence boundary, not a defect; manual A-1/A-3 remain the only end-to-end check.
- O-2: Pre-existing, untouched dead code inside an authorized file: `ManualInitialOutreachService.kt:1151 buildMaterialReminderEsFilters` has no caller anywhere in `src/` (verified present already at `971a21d`), and the live material-reminder path uses `buildEsFiltersForLevel` (`:1209`). Outside the child's four gates and outside its authorized cleanup scope.

### Required Action
- COMPLETE_CHILD
