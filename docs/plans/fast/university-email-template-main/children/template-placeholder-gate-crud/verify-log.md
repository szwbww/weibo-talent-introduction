# Verification Log — template-placeholder-gate-crud

## Light Verification: LIGHT_PASS_WITH_NOTES
Child: template-placeholder-gate-crud (docs/plans/2026-09-18/template-placeholder-gate-crud.md)
Boundary: d51c89105fb5a1f3cf6a1d3a3d71612b8898fc07..971a21d35a0d5bdffefa4aff9dc6faf93819207d
Verifier: TemplateGateVerifier

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
|Authorized scope|PASS|`git diff --name-status d51c891..971a21d` = exactly the 9 authorized paths, all `M` (3 Kotlin main, app.js, index.html, 2 Kotlin tests, 2 JS tests); `git show --name-only 971a21d` same 9, single commit `feat(fast-p): implement template-placeholder-gate-crud`; `git status --porcelain` = only untracked `docs/plans/fast/**`. No `styles.css`, no `db/migration`, no `?v=` cache-key, no new file.|
|Plan and invariants|PASS|I-1: `MailPlaceholderService.kt:88 requiredKeysIn` (gates = `fallback.isNullOrBlank()`, first-occurrence `linkedSetOf`), `:107 templatePlaceholderViolations` (unknown key / blank default / `:134 brokenTokens`) wired at `MailComposeTemplateService.kt:518-527`; zero `required_keys` reads in `src/main` (only `MailComposeTemplate.kt:16` field decl + `V84`). I-2: `PersonalizationGateService.kt:45-60` (`key in keysInText && variables[key].isNullOrBlank()`), residue check untouched; `requiredEsFields:194-211` intersects every possible render and `ManualInitialOutreachService.kt:431-434` still limits to `ExpertSearchService.ALLOWED_HAS_FIELDS:35`; gate render pool (`possibleRenderTexts:246-266`, enabled-only) matches `resolveBlocks:541+` / `ContentVariantService.buildPool:88-106`. I-3: `:71` create ⇒ `INTRODUCTION`, `:96-102` update keeps id/`existing.mailType`/templateCode, `:121-145` delete = row first + reference-only `DataIntegrityViolationException` ⇒ `IllegalArgumentException` (→400), others rethrown; `BatchSendTaskConfigService.resolveMailType:349-369` accepts `INTRODUCTION`. I-4: menu renders `state.variableMeta` wholesale (`app.js:2767-2783`), bare insert only for `isComposeTemplateVarTarget:2727` (`bindVarChipBar:2864-2880`), QA/snippet strictness unchanged (`validatePlaceholderText:2679-2706`, targets `index.html:1839,1883`); `index.html:1953` copy only. M-1/M-3/M-4: gate/prefilter read the live template at every call (`MailComposeTemplateController:54-57`, `ManualInitialOutreachService:427-441`, `IntroductionMailComposer:28-29`, `ManualExpertMailService:236-238`), no cache; diff adds no instance write, no ES write, no send. S-1/S-2: no CSS file in diff; `+` lines contain no `style=`/new class/new DOM node. Tests: `MailComposeTemplateServiceTest` 51/0/0 (legacy-`required_keys`-never-decides, first-occurrence dedupe, blank-default/unknown/broken rejection, variant intersection vs union, CRUD round trip, referenced-delete conflict, non-reference rethrow); `PersonalizationGateServiceTest` 12/0/0; JS 19/0/0 (lenient vs strict, bare insert, full-menu key coverage, index.html copy, block-order reopen).|
|Required commands|PASS|1) `node --test src/test/js/composeTemplatePreview.test.js src/test/js/gateTemplateFilter.test.js` → exit 0, tests 19 / pass 19 / fail 0 (baseline superset of 3 files was 87/0). 2) `JAVA_HOME=…zulu-11… mvn test -Dtest='MailComposeTemplateServiceTest,PersonalizationGateServiceTest' -DfailIfNoTests=false` → exit 0, BUILD SUCCESS; surefire `MailComposeTemplateServiceTest` 51/0/0 + `PersonalizationGateServiceTest` 12/0/0 (= 63 Kotlin tests, reports regenerated 01:17:15-16 this run); exec-plugin JS suite 996 pass / 0 fail (baseline 990 → +6, no regression). 3) `git diff --check` → exit 0.|
|Downstream interfaces|PASS|`git diff --stat base..head -- ManualInitialOutreachService.kt BatchExecutionModels.kt ExpertSearchService.kt MailComposeTemplateController.kt styles.css` = empty (0 hunks). At HEAD: `resolveScope` `ManualInitialOutreachService.kt:427`, `RecipientScope.fromSnapshot:133` / `matchesExpert:65`, `fieldPresenceFilter:55` + `ALLOWED_HAS_FIELDS:35`, gate-fields response `requiredKeys`/`esFields` `MailComposeTemplateController.kt:54-57,69-71`, `ES_FIELD_BY_KEY["primaryResearchField"] = "researchFields"` `MailPlaceholderService.kt:230`.|

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1: A token written with a non-blank default still blocks when the same key is bare in another enabled variant. `evaluate` (`PersonalizationGateService.kt:57-59`) gates on `key in keysInText` (any occurrence in the materialized texts) while `requiredKeys` is the template-wide union (`MailComposeTemplateService.effectiveRequiredKeys:178-186`), and both composers pass that union (`IntroductionMailComposer.kt:28-29`, `ManualExpertMailService.kt:236-238`); so for a snippet whose selected variant renders `${key|default}` while another enabled variant renders `${key}`, a recipient with no value is blocked instead of using the default. Deliberate and test-pinned (`PersonalizationGateServiceTest` "evaluate blocks a listed required key written with a default that has no value" — comment: "Legacy shape kept intact"), already disclosed in execution.md §5.1. Not an AUTO_FIX: the plan itself prescribes the union gate (implementation step 1) plus the presence filter, acceptance I-2 only requires the opposite direction (the variant that *does* require the key is hard-blocked, ES prefilter stays conservative), and a per-render correction would need the composers (unauthorized files). Flagged for human/master review, no repair proposed by this gate.
- O-2: Stale `required_keys` wording survives in a non-authorized file and in one exception message: `ManualInitialOutreachService.kt:421` KDoc ("模板无 required_keys"), `PersonalizationGateException` message "required variables fell back to defaults" (`PersonalizationGateService.kt:29-33`). Cosmetic only; the first file is outside this child's 9-file authorization, so nothing to change here.
- O-3: `composeTemplatePreview.test.js` hardcodes the 21-key metadata list (`VARIABLE_META_KEYS`), so the "every variable-meta key reaches the insert menu" test would not fail if the backend later adds a key; the production menu itself renders `state.variableMeta` unfiltered, so this is a test-fidelity note, not a behavior gap.

### Required Action
- COMPLETE_CHILD
