## Light Verification: LIGHT_PASS_WITH_NOTES
Child: 01 (定时任务新增「执行轮次数」roundsPerRun) — docs/plans/fast/batch-send-rhythm-and-filter/children/01/brief.md
Boundary: a6c27bbbca02a3b018d8a16aeb11822abd905e19..59a99f3e8771c2f08e213b7e36ebbfb3eee7e60c
Verifier: Verifier01

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
|Authorized scope|PASS|`git diff --name-only a6c27bbb..59a99f3` = exactly the brief's 9 authorized files (V91 migration, BatchSendTaskConfig.kt, BatchExecutionModels.kt, BatchSendControlService.kt, BatchSendTaskConfigService.kt, ManualInitialOutreachService.kt + 3 test classes). Forbidden files untouched: BatchSendSettingService.kt / app.js / index.html / BatchSendScheduler.kt / BatchSendTaskRuntimeIntegrationTest.kt / V72 absent from diff. One commit, message `feat(fast-p): implement 01`. |
|Plan and invariants|PASS|I-1: round-budget check is method-local (`roundNumber` local var), reads `snapshot.roundsPerRun`, no repository/`sumSuccessCountToday*` linkage — ManualInitialOutreachService.kt:219-224, :518-523 (both loops); C-2 test `roundsPerRun bounds a single execution at rounds times round size` (sent=40/100 targets). I-2: `stopReason="ROUNDS_PER_RUN_REACHED"`, `finalStatus="COMPLETED"` at both loop breaks (:222-223, :521-522); `applyResultToRuntimeStatus()` unchanged (COMPLETED → else → IDLE, BatchSendControlService.kt:530-531); C-2 asserts COMPLETED. I-3: grep of ManualInitialOutreachService.kt — every `roundsPerRun` read is `snapshot.roundsPerRun` (:193,220,281,310,376,389,399,412,484,519,582,600,669,681,809,823,834,848); zero `config.roundsPerRun`; no `BatchSendSettingService.kt` change. I-4: round-head budget check placed before `roundNumber++` while `oneRoundOnly` break stays at round end (:391-395, :825-830); C-2 test `oneRoundOnly takes precedence over roundsPerRun` (roundsPerRun=5 → 1 round, ONE_ROUND_DONE/PAUSED). I-5: V91 `rounds_per_run INT NOT NULL DEFAULT 1 AFTER round_size`; `require(fields.roundsPerRun >= 1)` (BatchSendTaskConfigService.kt:230), `require(snapshot.roundsPerRun >= 1)` (BatchSendControlService.kt:451); `BatchExecutionSnapshot.roundsPerRun: Int = 1` (BatchExecutionModels.kt:12); all 4 config data classes `= 1`; C-1 tests 1-2 + C-3 test 1 (422, message `roundsPerRun must be >= 1`). I-6: V91 `GREATEST(1, CEIL(daily_cap / round_size))` ≙ `toLegacySnapshot()` `maxOf(1, ceil(dailyCap.toDouble()/roundSize).toInt())` (BatchSendControlService.kt:598) ≙ `toSnapshot()` same formula (ManualInitialOutreachService.kt:1254) — three sites字同构; C-3 test 2 asserts derived 100/10→10. X-2: C-1 test 4 (roundsPerRun-only update keeps oldCron==newCron). X-4: C-1 test 3 (updateLegacyConfig preserves existing.roundsPerRun=7; BatchSendTaskConfigService.kt:166). B-2 symmetry: both loops have all 4 changes; C-2 material test asserts 40 sent / ROUNDS_PER_RUN_REACHED / COMPLETED; sleep-guard test asserts elapsed < 5s with perRoundIntervalMs=120000. Progress: both updateProgress (details map :1069) and updateProgressWithAccumulator (:1341) carry `"roundsPerRun"`; all 14 call sites pass `snapshot.roundsPerRun`. |
|Required commands|PASS|Fresh runs, zulu-11: (1) `mvn test` → exit 0, `Tests run: 2344, Failures: 0, Errors: 0, Skipped: 4`, BUILD SUCCESS (baseline 2344/0 ✓). (2) `mvn test -Dtest=BatchSendTaskConfigServiceTest,ManualInitialOutreachServiceTest,BatchSendControlServiceTest` → exit 0, 25+47+30=102 tests, 0 failures (baseline 102/0 ✓). (3) `mvn test -Dtest=FlywayMigrationIntegrationTest -DmigrationIt=true` → exit 1, failure message exactly `Docker is required for Flyway migration tests` (documented environment baseline — NOT AUTO_FIX). (4) `mvn clean package` → exit 0, BUILD SUCCESS (baseline ✓). (5) `git diff --check a6c27bbb..59a99f3` → exit 0, no output. |
|Downstream interfaces|PASS|Child 02a contract: `BatchExecutionSnapshot.roundsPerRun: Int = 1` placed immediately after `roundSize` (BatchExecutionModels.kt:12) and carried by the single entity conversion point `BatchSendTaskConfig.toExecutionSnapshot()` (:200) covering all 5 entity read paths; legacy KV paths derive via I-6 formula (`toLegacySnapshot` :598, `toSnapshot` :1254) — 02a can consume `snapshot.roundsPerRun` uniformly. stopReason handling: `ROUNDS_PER_RUN_REACHED` added to `stopReasonMessage()` before `ONE_ROUND_DONE` (ManualInitialOutreachService.kt:888, message 「本次调度轮次已用完」); `applyResultToRuntimeStatus()` and `idleSafeOneRoundStopReasons` untouched (COMPLETED else-branch → IDLE, autoEnabled preserved); `BatchSendStatusView.roundsPerRun: Int = 0` + `getStatus()` `details?.asInt("roundsPerRun") ?: 0` (BatchSendControlService.kt:711, :321). |

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1: Legacy snapshot paths (`toSnapshot()` / `toLegacySnapshot()`) now emit `ROUNDS_PER_RUN_REACHED` instead of `DAILY_CAP_REACHED` when the derived budget `ceil(dailyCap/roundSize)` is exhausted at round head — two pre-existing tests updated accordingly (ManualInitialOutreachServiceTest.kt:1980-1983, :2040-2043). Send volume unchanged, `COMPLETED`→IDLE mapping intact, the 6 existing stopReason values/messages/mappings untouched, and the direct-config path (A-4 scenario: roundsPerRun=10, dailyCap=30) still ends mid-round with `DAILY_CAP_REACHED`. This is the direct, plan-mandated consequence of the I-6 formula (budget == cap budget in rounds) plus I-2 semantics, locked by the updated tests — observed behavior shift, not a plan deviation; recorded, no action.
- O-2: `mvn test` / `mvn clean package` additionally execute the project's frontend JS test suite (485 tests, 0 fail) as part of the Maven lifecycle — passes; outside the plan's gate surface (plan gates Java surefire only).

### Required Action
- COMPLETE_CHILD
