## Light Verification: LIGHT_PASS_WITH_NOTES
Child: 02a — docs/plans/fast/batch-send-rhythm-and-filter/children/02a/brief.md
Boundary: 59a99f3e8771c2f08e213b7e36ebbfb3eee7e60c..d5370c6387cc6748b3adadd6bb4ca16a502ead18
Verifier: Verifier02a

### Four Gates
| Gate | Result | Evidence |
|---|---|---|
| 1. Authorized scope | PASS | `git diff --name-only 12ebd6c..d5370c6` (02a commit alone) = exactly the brief's 5 authorized files: BatchSendControlService.kt, ManualInitialOutreachService.kt, BatchSendControlServiceTest.kt, BatchSendTaskRuntimeIntegrationTest.kt, ManualInitialOutreachServiceTest.kt. One commit `d5370c6 feat(fast-p): implement 02a`; its parent is the 01 docs-record commit 12ebd6c (docs/plans artifacts only, not part of 02a's implementation). Forbidden files grep over the whole boundary 59a99f3..d5370c6 (BatchSendTaskConfig.kt, BatchExecutionModels.kt, BatchSendTaskConfigService.kt, BatchSendSettingService.kt, app.js, index.html, any migration): zero matches (exit 1). |
| 2. Plan and invariants | PASS | I-1: `roundQuota = minOf(config.roundSize, estimatedRemaining, remainingAccountCapacity)` at ManualInitialOutreachService.kt:234 (material) and :524 (intro) — 3 args, no daily-cap term; `grep -rn dailyCapRemaining src/` empty. I-2: `grep alreadySent` BatchSendControlService.kt empty; all 5 prechecks removed in diff (startScheduled :63-68, startManual(request) :85-91, startAuto :124-128, startManual(sendType) :154-158, runManualOnce :258-262); `sumSuccessCountTodayByBatchConfigId` in src/main only at TaskExecutionService.kt:46 (definition); `checkRemainingAccountCapacity()` (:461) / `checkRemainingDailyCapacity()` (:585) retained; new reverse tests `startScheduled still accepts when today sum far exceeds dailyCap (I-2)` + `startManual(request) returns 409 with account-capacity message...` (BatchSendControlServiceTest.kt:602, :634) pass. I-3: `grep alreadySentToday src/main` empty; `countSentByMailTypeSince` src/main only MailRecordRepository.kt:403 (definition), src/test zero (6 stubs cleared); `dailySentTotal` in ManualInitialOutreachService.kt exactly 2 lines, both string keys `"dailySentTotal" to sent` (:1046) / `"dailySentTotal" to breakdown.success` (:1318); `runMaterialReminderBatch` dayStart/count call removed. I-4: both `stopReason="DAILY_CAP_REACHED"` production points, `stopReasonMessage()` branch (:862), and `idleSafeOneRoundStopReasons` entry (BatchSendControlService.kt:667) removed; only remaining `DAILY_CAP_REACHED` hit is BatchExecutionModels.kt:151 `annotateTerminalRemaining` historical-log mapping — pre-existing at base 59a99f3 (git show identical), file has 0 diff across boundary (forbidden, untouched). `DAILY_CAP_EXCEEDED` intact at BatchExecutionModels.kt:93 (const) + :104 (LABELS) + :151 (annotator), all identical to base. I-5: `classifyNoSendableOutcome()` (:831) / `classifyLimitReachedOutcome()` (:844) / `stopReasonMessage()` WARMUP_LIMIT_REACHED/DAILY_LIMIT_REACHED/NO_AVAILABLE_ACCOUNT branches byte-identical to base (restricted diff shows zero changes; only `"DAILY_CAP_REACHED"` branch line and a doc comment removed); `remainingAccountCapacity <= 0` when-branch retained in both loops (:239, :531); new I-5 test `runScheduledBatch stops at account capacity with DAILY_LIMIT_REACHED (I-5)` (ManualInitialOutreachServiceTest.kt:618) passes. Symmetry: diff shows both loops received all A-2 items 1–4 (dailyCapRemaining delete, 3-arg roundQuota, dailyCapRemaining when-branch delete, dailySentTotal var + 2 increments delete). Details keys preserved: `"dailyCap" to config.dailyCap` (:1045/:1317), `"dailySentTotal"` (:1046/:1318); `require(snapshot.dailyCap > 0)` preserved (BatchSendControlService.kt:419); `assertEquals(1000, status.dailyCap)` retained (BatchSendControlServiceTest.kt:505); KV-layer BatchSendSettingService cases untouched. Test rewrites match A-3: 5 precheck-409 cases rewritten, 2 new reverse cases, 4 runMaterialReminderBatch cases rewritten to assert no DAILY_CAP_REACHED truncation, 3 runtime-integration cases rewritten to assert 202 + `sumSuccessCountTodayByBatchConfigId` never invoked, helpers `dailyCapContextWithTodaySum`/`dailyCapContext` retained. |
| 3. Required commands | PASS (with note) | Fresh runs, zulu-11: (1) `mvn test` → exit 0, BUILD SUCCESS, surefire aggregate `Tests run: 2347, Failures: 0, Errors: 0, Skipped: 4` (baseline 2344/0; +3 = the 3 new 02a tests). (2) `mvn test -Dtest=BatchSendControlServiceTest,ManualInitialOutreachServiceTest,BatchSendTaskRuntimeIntegrationTest` → exit 0, 32+48+21=101 tests, 0 failures/errors. (3) `mvn clean package` → exit 0, BUILD SUCCESS, 2347/0/0/4. (4) `git diff --check 59a99f3..d5370c6` → exit 2, exactly 1 finding: `docs/plans/fast/batch-send-rhythm-and-filter/children/01/brief.md:466: new blank line at EOF` — a fast-p docs artifact added by the 01 verification-record commit 12ebd6c, NOT part of the 02a implementation; `git diff --check 12ebd6c..d5370c6` (02a commit only) → exit 0, no output. Docker Flyway IT not re-run (documented env baseline, not required by this child). |
| 4. Downstream interfaces | PASS | Nothing 02b consumes was deleted. `dailyCap` field/column intact: BatchSendTaskConfig.dailyCap (BatchSendTaskConfig.kt untouched), `batch_send_task_config.daily_cap` (no migration in boundary), `BatchExecutionSnapshot.dailyCap` (BatchExecutionModels.kt untouched). Details string keys `"dailyCap"`/`"dailySentTotal"` intact. 02b residual-audit items all present at HEAD: BatchSendControlService.kt `getStatus` `dailyCap = details?.asInt("dailyCap")` (:294), `require(snapshot.dailyCap > 0)` (:419), `BatchSendStatusView.dailyCap` (:680), `toLegacySnapshot` + `roundsPerRun = maxOf(1, ceil(...))` (:562/:567); ManualInitialOutreachService.kt `toSnapshot`/`toBatchSendConfig` + derivation (:1229); KV layer BatchSendSettingService.kt untouched. Methods retained: `TaskExecutionService.sumSuccessCountTodayByBatchConfigId` (TaskExecutionService.kt:46), `MailRecordRepository.countSentByMailTypeSince` (:403), `checkRemainingAccountCapacity`. |

### AUTO_FIX
- N/A

### RECORD_ONLY
- R-1: `git diff --check` over the full boundary exits 2 with a single "new blank line at EOF" in `docs/plans/fast/batch-send-rhythm-and-filter/children/01/brief.md:466`. The file was added by commit 12ebd6c (`docs(fast-p): record 01 light verification`), which sits between base and head and is the 01 child's record, not the 02a implementation. The 02a-only range `12ebd6c..d5370c6` passes `git diff --check` (exit 0). 02a is contractually forbidden from modifying docs/plans artifacts, so no correction is authorized in this child; surfaced for the ledger / 01 record to fix if desired.
- R-2: I-4 acceptance criterion "grep DAILY_CAP_REACHED in src/main is empty" is not literally satisfiable: a pre-existing `"DAILY_CAP_REACHED" -> BatchOutcomeReasonCodes.DAILY_CAP_EXCEEDED` mapping in `BatchExecutionModels.kt:151` (`annotateTerminalRemaining`, historical-log reverse lookup) exists at base and is byte-identical at HEAD (0 diff; file is on the forbidden list and untouched). The 4 named removal points of I-4 are all removed; the criterion's blanket grep overstates the required end state relative to the forbidden-file constraint. Not an implementation defect.
- R-3: I-4/02b criterion "DAILY_CAP_EXCEEDED hits 2 处 (const + LABELS)" — actual grep yields 3 hits in BatchExecutionModels.kt (:93 const, :104 LABELS, :151 annotator mapping); all 3 identical to base, nothing added/removed. Criterion omitted the pre-existing annotator hit; intent (constant and LABELS preserved) holds.

### Required Action
- COMPLETE_CHILD
