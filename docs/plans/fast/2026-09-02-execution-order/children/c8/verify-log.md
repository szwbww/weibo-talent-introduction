# c8 Light Verification Log

## Light Verification: LIGHT_PASS

Child: c8 — `docs/plans/2026-09-02/07-legacy-entry-retire.md` (plan identity `commit:46cc5c46395814b1ef03e52ab8b8bfb5197f372c`; `git diff 46cc5c46 -- <plan>` EMPTY, re-verified)
Master: `docs/plans/2026-09-02/00-execution-order.md` (identity `commit:92b0519a18a3a46989f8733259af4649f7748a72`; diff EMPTY, re-verified; G-1..G-9 read)
Boundary: `e76b5a9f49c866afffbccdffcae78443cb16cab3` (c7 code head) .. `6d934ca43e6e74c35917ef841fae92056e2d66f4` (implementation commits `1706d8be` backend stop-writes + `6d934ca` endpoint/frontend cleanup); docs-only `56e74a4` (c7 evidence) inside boundary, not product; SPLIT executed per plan T1 >3-hit rule (state in execution.md)
Verifier: VerifierC8
Date: 2026-09-03 (JDK 11 zulu-11, worktree root; worktree clean except `?? …/children/c8/`)

### Four Gates

| Gate | Result | Evidence |
|---|---|---|
| **1. Authorized scope** | PASS | Product boundary (56e74a4..6d934ca, i.e. e76b5a9.. minus 4 c7-docs/ledger paths) = **17 files** = commit 1706d8b (3: QaRuleManagementController M, QaRuleReadOnlyTest A, QaRuleManagementControllerTest D) + 6d934ca (14). Maps to plan rows 1-7 (QaRuleManagementController, TrustReplyWorkbenchController D, TrustReplyWorkbenchControllerTest D, app.js, index.html, batchSendTaskConsoleVisualFix, QaRuleReadOnlyTest NEW) + T1-grep rows 8-10 (4 hits → SPLIT authorized by plan/brief: QaRuleManagementControllerTest D — whole-file old-write contract, replaced by QaRuleReadOnlyTest; aiTrainingUnsupportedAnswers + ragKnowledgeBasePage rewritten; qaFactCardEditor hit but **kept zero-change** — asserts live mail-templates read surface, GET /rules retained per I-36, JS suite green) + master-G-5 re-grep extras (6 pin files beyond row 6, each diff = single-string key sync only). Plan-gap deviations (execution.md 1-2): **AiTrainingController.kt +43 lines** — the 6 shared HTTP shapes (TrustReplySource/RequestFactSelection/FrameSelection/FrameSnapshot/LockedItem/ErrorResponse) relocated from the deleted workbench controller, verified **byte-identical** per class against base file via scripted comparison, pure declarations appended at tail (0 deletions, 0 logic changes), confined to what the live `/simulate/evaluations` consumer needs; **RagReplyController.kt** — diff is exactly 2 KDoc comment lines (I-38 grep vs c3-file clash), zero code. Nothing else changed. |
| **2. Plan requirements & invariants (I-35..I-38, G-1..G-8, S-1)** | PASS | **I-35**: delete-SQL grep 0 hits; 0 migration files changed; `qa/domain` + `db/migration` + styles.css + mail_record_qa_rule/inbound_mail_tag/MailComposeTemplate/MailMonitoring/TrustReplyWorkbenchService/AiReplyGenerationCoordinator paths all zero diff. **T2/I-36**: seven write endpoints (categories POST/enable/disable + rules POST/PUT/enable/disable) each throw `ResponseStatusException(FORBIDDEN, "QA_RULE_READ_ONLY")` at top (private `readOnly()` helper); signatures/routes kept; `GET /rules` and all read endpoints byte-untouched; `QaRuleManagementService` untouched. Local `@ExceptionHandler(ResponseStatusException)` is a class member (scope = this controller only) and is **required**: `GlobalExceptionHandler`'s `@ExceptionHandler(Exception)` catch-all would otherwise render the bare RSE as 500 INTERNAL_ERROR (implementer probe demonstrated; repo precedent = controller-local handlers). Read-error semantics unchanged — no read endpoint in this controller throws RSE (qa-package grep: RSE only in `readOnly()`); read failures still flow to the same global handlers; GET /rules 200 non-empty asserted in QaRuleReadOnlyTest. **I-37**: `loadAiTrainingQa\|renderAiTrainingQaTable\|aiTabQa` grep on worktree static+test/js (absolute paths) = zero; ragKnowledgeBasePage negative assertions preserved via `"aiTab"+"Qa"` concatenation. **I-38**: `trust-reply/workbench` grep over worktree src = zero (incl. comments); TrustReplyWorkbenchControllerTest deleted (25 tests, all endpoint contracts of the 9 removed endpoints); services + 4 service tests untouched. **T4/G-8**: app.js diff = pure deletion (0 added / 163 deleted: 8 functions + 1 const + 6 state fields + 11 bindEvents bindings + trailing comment block); dangling-ref grep over all identifiers = zero in static+test. index.html = key bumps (3) + orphan `#aiTrainingQaModal` DOM block only; no `aiTabQa`/`data-tab="qa"` residue. **G-5**: exactly 3 keys in index.html, all `20260902-legacy-retire` single value; 7 pin files single-string sync; old value `20260902-rag-prompt-console` zero hits in src. **S-1**: styles.css diff EMPTY. Deleted-test arithmetic: 3114 − 8 − 25 + 8 = 3089 ✓. |
| **3. Required commands (fresh, worktree root, JDK 11)** | PASS | `mvn test -Dtest=QaRuleReadOnlyTest` rc 0 — Tests run: 8, Failures: 0, Errors: 0 (fresh surefire report). `mvn test -Dtest=InboundMailTagServiceTest,MailComposeTemplateServiceTest` rc 0 — 9/0/0/0 (read path 9) + 40/0/0/0 (read path 10); actual class names `mail/service/InboundMailTagServiceTest`, `template/service/MailComposeTemplateServiceTest` (glob-verified). `node --test src/test/js/*.test.js` rc 0 — tests 648, pass 648, fail 0. `node --check src/main/resources/static/app.js` — SYNTAX_OK (also trust-reply-workbench.js, task-modal-runtime.js). `mvn test` rc 0 — Tests run: 3089, Failures: 0, Errors: 0, Skipped: 8, BUILD SUCCESS. `mvn clean package` rc 0 — Tests run: 3089, Failures: 0, Errors: 0, Skipped: 8, BUILD SUCCESS, WAR produced (clean target re-proof ⇒ stale-.class issue confirmed environmental, non-defect). Post-greps: styles.css diff empty; delete-SQL grep 0; I-37 grep empty (absolute worktree paths); I-38 grep empty; `20260902-rag-prompt-console` grep empty in src; `git diff --check` clean. |
| **4. Downstream interfaces** | PASS | `/api/qa` write endpoints → 403 with body code `QA_RULE_READ_ONLY` (routes alive — 7 QaRuleReadOnlyTest assertions; local handler verified); GET /rules 200 non-empty. `/api/trust-reply/workbench/*` → 404 (controller file gone; zero frontend callers). New chain (rag tables, compose, send bridge, KB page, prompt console, workbench) files untouched and suites green. No migrations; qa_rule/qa_category data intact (I-35). mail_record_qa_rule / inbound_mail_tag / template QA_RULE block / monitoring display functional per untouched code + read-path test greens. X-2/X-3/X-4/D-14/X-5 deferred items documented in plan (re-read) and still open for future plans; D-14 FREE_FORM/prompt-config untouched (UnmatchedInboundMailController zero diff; AiTrainingController +43 tail-append only). |

### AUTO_FIX
N/A — no four-gate violation found; no plan/master-rule-unique repair needed.

### RECORD_ONLY
- O-1 (execution deviation #1): 6 shared HTTP shapes relocated from deleted TrustReplyWorkbenchController.kt into AiTrainingController.kt (their live consumer). Verified byte-identical class bodies; zero logic change; co-located for X-4 batch removal when the DTOs die.
- O-2 (execution deviation #2): c3's RagReplyController.kt KDoc reworded (2 comment lines, `/api/trust-reply/workbench` literal removed) — required for I-38's src/main-wide final grep to be empty; zero behavior change.
- O-3 (execution deviation #3): controller-local `@ExceptionHandler(ResponseStatusException)` added — plan T2 mechanism gap (global catch-all → 500). Necessary to satisfy T7/A-1 (403 + code, explicitly "not 500"); scoped to this controller; no read path throws RSE so read semantics preserved.
- O-4 (execution deviation #4): frontend deletion surface wider than T4's three named functions (5 helpers + const + 6 state fields + 11 dead bindings + orphan `#aiTrainingQaModal` DOM), each grep-proven dead before deletion. Legacy `ai_training_qa` CRUD backend (`/api/ai-training/qa` in AiTrainingController) and its AiTrainingSimulateTest coverage remain — orphaned (only caller was the deleted frontend); D-10/X-4 physical-deletion backlog candidate, consistent with plan 07 "只摘入口".
- O-5 (execution deviation #5): stale `.class` of git-rm'd tests polluted one incremental full run; independent `mvn clean package` re-run from zero reproduces 3089/0/0/8 — environmental, not an implementation defect.
- O-6 (plan-scope observation, no c8 action): two legacy QA-edit UI surfaces remain in product and were outside plan 07's authorized file list/grep patterns: (a) mail-templates view "QA 规则" admin (`#newQaRuleBtn`/edit/toggle → `saveQaRule`/`handleQaAction` → POST/PUT/enable/disable `/api/qa/rules*`), (b) AI-训练 无依据回答索引 待转事实 channel-B conversion (2026-08-28 plan 16 flow) which opens the same `#qaRuleModal` and saves via `/api/qa/rules`. Both now receive 403 (per plan T2/A-1 "no entry can edit" — satisfied); leftover buttons surface error toasts. Deferred surface-removal candidate for a follow-up plan (X-4/UI batch); zero product change required at c8. JS suite 648/648 green confirms no test churn.
- O-7 (verifier process note): grep tool with semicolon-mixed absolute+relative path lists resolves the relative entry against the main checkout (which correctly retains pre-c8 content) — all decisive greps in this log re-run with fully absolute worktree paths.

### Required Action
- COMPLETE_CHILD
