## Light Verification: LIGHT_PASS_WITH_NOTES

Child: c4 — docs/plans/2026-09-02/03b-rag-send-bridge.md (RAG 草稿采用 → 人工发送桥接；identity `commit:f63153a1e7bc03fa0455b94453787990c6c09a23` = amended A3/A4 — `git diff f63153a -- <plan>` empty, verified, 0 output)
Master plan: docs/plans/2026-09-02/00-execution-order.md (identity `commit:92b0519a18a3a46989f8733259af4649f7748a72`; G-1..G-9 read in full)
Boundary: 10a38bb6457280f7104a333faa46fad6f7cb078f..ae755c417d2be4cecda52c5adf20a7f52227a072 (`feat(fast-p): implement c4`; interleaved docs-only 2987af4 c3-evidence / 5a42058 A3 / f63153a1 A4 amend = plan+ledger files only, not product)
Verifier: VerifierC4
Worktree: /Users/lukai/IdeaProjects/weibo-talent-introduction-fast-2026-09-02-execution-order (branch `fast/2026-09-02-execution-order`)
Product read-only honored; only file written: this report. Working tree after runs: only `docs/plans/fast/**` ledger.md modified + children/c4/ untracked (implementer brief/execution + this report).

### Four Gates

| Gate | Result | Evidence |
|---|---|---|
| 1. Authorized scope | PASS | `git show ae755c4 --name-only` = exactly the plan's 10-file `## 变更文件清单` (V113 sql, MailRecordRagFact.kt, MailRecordRagFactRepository.kt, PendingMailOperationService.kt, UnmatchedInboundMailController.kt, app.js, RagSendBridgeTest.kt, ragAdoptSendBridge.test.js, UnmatchedInboundTrustWorkbenchTest.kt [A3], OperatorStatusWriteSeamGuardTest.kt [A4]). Boundary product diff (10a38bb..HEAD minus docs-only interleaved commits) restricted to those 10. No index.html/styles.css (diff vs HEAD and across boundary both EMPTY), no migration V1..V112 (only V113 added), no docs/plans/fast in commit. No c1-c3 rag/mail file modified (RagKnowledgeBase/TrustReplyWorkbenchService/ManualReplySendAttemptService/MailRecordQaRuleRepository all untouched — not in commit). |
| 2. Plan requirements & invariants | PASS | Direct code + executed-test evidence below for I-39..I-44, I-47, G-1..G-8 relevant, T1-T6 mechanics, old-path byte preservation, endpoint/DTO wiring, error mapping. |
| 3. Required commands | PASS | All 12 required commands freshly run by me (JDK 11 zulu-11, worktree root, final committed state); exit codes + counts in table below. 3107/0/0/7 reproduces implementer claim exactly (c3 3097 + 10 = RagSendBridgeTest). Plain suite Docker-free green (7 skips = docker/IT migrationIt-gated + I-46 @Disabled, pre-existing). Flyway IT env-blocked with base reproduction + V113 scratch-chain evidence recorded in execution.md — verified, not failed. |
| 4. Downstream interfaces | PASS | `mail_record_rag_fact` (V113): id/mail_record_id/fact_code VARCHAR(32)/ordinal/corpus_fingerprint VARCHAR(64)/created_at; `UNIQUE uk_mail_record_rag_fact (mail_record_id, ordinal)`; indexes on record + code; **no FK** to rag_fact/mail_record. `MailRecordRagFactRepository.findByMailRecordIdOrderByOrdinalAsc` (same shape as MailRecordQaRuleRepository). `sendManualRichReply` RAG-branch semantics (I-39 400 / I-41 400+409 / I-40 422 pre-claim; rag_fact rows written only post-success). `collectSafetyFindings(..., ragSend: Boolean = false)` trailing defaulted param. Frontend context shapes `{ragFactCodes, ragCorpusFingerprint, baselineText}` vs legacy `{qaRuleIds, baselineText}` — c4 `adoptTrustReplyAssembly` shape split is exactly the c6-consumed contract. |

Required commands (fresh, JDK 11 zulu-11, final committed state, worktree root — all executed by verifier):

| # | Command | Exit | Result |
|---|---|---|---|
| 1 | `mvn test -Dtest=RagSendBridgeTest` | 0 | **Tests run: 10, Failures: 0, Errors: 0, Skipped: 0** — BUILD SUCCESS |
| 2 | `node --test src/test/js/ragAdoptSendBridge.test.js` | 0 | **# pass 5 / # fail 0** |
| 3 | `mvn test -Dtest=PendingMailOperationServiceTrustWorkbenchTest` | 0 | **Tests run: 59, Failures: 0, Errors: 0, Skipped: 0** — BUILD SUCCESS |
| 4 | `mvn test -Dtest=UnmatchedInboundTrustWorkbenchTest` | 0 | **Tests run: 12, Failures: 0, Errors: 0, Skipped: 0** (A3 gate) — BUILD SUCCESS |
| 5 | `mvn test -Dtest=OperatorStatusWriteSeamGuardTest` | 0 | **Tests run: 1, Failures: 0, Errors: 0, Skipped: 0** (A4 gate) — BUILD SUCCESS |
| 6 | `node --test src/test/js/*.test.js` | 0 | **tests 809, pass 809, fail 0** |
| 7 | `node --check src/main/resources/static/app.js` | 0 | SYNTAX_OK |
| 8 | `mvn clean package` | 0 | **Tests run: 3107, Failures: 0, Errors: 0, Skipped: 7** — BUILD SUCCESS, WAR built |
| 9 | `mvn test` (full regression) | 0 | **Tests run: 3107, Failures: 0, Errors: 0, Skipped: 7** — BUILD SUCCESS |
| 10 | `git diff --stat HEAD -- styles.css index.html` + boundary variant | — | **EMPTY output** (both vs HEAD and 10a38bb..HEAD) — no cache-key bump, c6 owns |
| 11 | `git diff --check` | 0 | No output |
| 12 | `git diff ae755c4^ ae755c4 -- PendingMailOperationService.kt` | — | Adds branches only (+109/−8): new ragMode gates + `} else if (ragMode)` branches; `:181-215` assembly/element-equal/canonicalize block not rewritten (byte-compare of base vs head region confirms old lines verbatim) |

Count arithmetic: c3 terminal 3097/0/0/7 + 10 new RagSendBridgeTest = 3107; Skipped 7 unchanged (docker/IT migrationIt-gated + I-46 @Disabled — pre-existing, none from c4). Implementer's claimed numbers reproduced byte-for-byte. FlywayMigrationIntegrationTest `-DmigrationIt=true`: not rerun (env-blocked, base-reproduced — docker-java client API 1.32 vs OrbStack daemon min 1.40 confirmed live via `docker version`; V82 fresh-chain defect pre-existing; V113 verified on scratch patched chain per execution.md §迁移验证 with full shape/UNIQUE/no-FK/1062 assertions). Working tree after all runs: only ledger.md modified + untracked children/c4/ docs — no product dirt.

Gate-2 detail evidence (direct code inspection + executed tests):
- **I-39 three-path mutual exclusion**: `PendingMailOperationService.kt:184-189` — `ragMode = ragFactCodes != null`; `if (trustReplyAssembly != null && ragMode) throw 400 "SEND_EVIDENCE_SOURCE_CONFLICT"` placed **before** the assembly re-verification block (i.e., before `:181`-region verifyAssembly) and before any suppression/claim/SMTP side effect. Executed `assembly plus ragFactCodes conflicts...` asserts 400 SEND_EVIDENCE_SOURCE_CONFLICT + `verifyNoInteractions` on mailDeliveryService/manualReplySendAttemptService/qaFactSelectionService/mailRecordRagFactRepository/mailRecordQaRuleRepository/**trustReplyWorkbenchService** (conflict fires before server-side assembly recompute). Old paths byte-identical: base-vs-head region diff shows only inserted `} else if (ragMode) {...}` branches; the `verifyAssembly` block, element-equal 422 (`qaRuleIds must equal the server-verified canonical fact ids`), `carriesQa` legacy else (`!qaRuleIds.isNullOrEmpty()`), `canonicalizeFactRuleIds(inboundText, qaRuleIds!!, researchProfileSufficient)` (:208-equivalent), and legacy `serverSuggestedFactIds = if (ragMode) { emptyList() } else if (carriesQa) {...}` all preserved — ragMode=false path behavior identical. Executed `legacy qaRuleIds path still sends...` + `trust assembly path still sends...` smoke tests green, both asserting `verifyNoInteractions(mailRecordRagFactRepository)` (old paths never write rag_fact).
- **I-40 snapshot-only validation**: RAG gate (`:193-210`) reads only `ragKnowledgeBase.snapshot().facts` filter `enabled` → 422 `RAG_FACT_CODE_UNKNOWN` for unknown or disabled code; never touches qa_rule / legacy_rule_id. RAG branch `factResolution = CanonicalFactResolution(emptyList(), emptyList())`, `serverSuggestedFactIds = emptyList()` (no `qaFactSelectionService.select()`), `carriesQa` audit var = codes non-empty. Executed `unknown fact code` + `disabled fact code` tests assert 422 RAG_FACT_CODE_UNKNOWN + zero delivery/attempt interaction; success test asserts `verifyNoInteractions(qaRuleRepository)` + `verifyNoInteractions(qaFactSelectionService)`.
- **I-41 fingerprint freshness gate**: `:196-199` — blank/null → 400 `RAG_FINGERPRINT_REQUIRED`; `!= ragKnowledgeBase.fingerprint()` → 409 `RAG_CORPUS_STALE`; no auto-regenerate, no silent pass. Executed both tests assert no delivery/attempt/rag-repo interaction (no send attempted). Gate sits before claim/SMTP.
- **I-42 ordered no-sort no-dedupe persistence**: success block (`:437-454`) `ragEvidenceRepo.saveAll(ragFactCodes.mapIndexed { ordinal, factCode -> MailRecordRagFact(...) })` — ordinal = request index verbatim, no sort/dedupe, corpusFingerprint captured at gate. Executed success test asserts rows `[(KB-FUND-033,0),(KB-COMP-007,1),(KB-PROG-002,2)]` with mailRecordId 500 + fingerprint; executed duplicate test asserts 4 rows ord 0..3 including repeated KB-FUND-033. Repository/entity shapes match plan T2. Write guarded to RAG path only — legacy/assembly paths never touch it (smoke tests assert zero interaction).
- **I-43 matched_qa_rule_id null on RAG path**: `:274-275` `primaryRuleId = if (ragMode) null else canonicalFactIds.firstOrNull()` — no legacy_rule_id fallback. Executed success test asserts `payload.primaryRuleId == null` and `payload.canonicalQaRuleIds == emptyList()`; `ManualReplySendAttemptService.kt:236` writes `matchedQaRuleId = payload.primaryRuleId` → null. `mail_record_qa_rule` writes are guarded by `payload.canonicalQaRuleIds.isNotEmpty()` (`ManualReplySendAttemptService.kt:250-260`) → RAG path zero qa_rule rows; success test asserts `verifyNoInteractions(mailRecordQaRuleRepository)`.
- **I-47 collectSafetyFindings ragSend short-circuit**: trailing defaulted `ragSend: Boolean = false` (`:906-909`); RAG call site passes `carriesQa = if (ragMode) false else carriesQa` + `ragSend = ragMode` (old paths: ragSend=false → logic byte-identical, verified in diff). Guard `if (ragSend) return findings` (`:943-957`) placed **after** the three pure-text checks (hallucinated number/url, unbacked high-risk, trust rhetoric — all in the `carriesQa=false` else branch + trust-rhetoric block, no qa_rule reads) and **before** `val selection = verifiedSelection ?: qaFactSelectionService.select(...)`, trust-gap evaluation, intent traversal, and AiReplyActionPolicy — whole QA chain short-circuited. carriesQa=false on RAG makes `QA_FACTS_ALL_INVALID` unreachable (`:921-927` structure). Executed `rag send with hallucinated number still requires confirmation...` asserts findings contain `AI_REPLY_CLAIM_HALLUCINATED_FACT` (pure-text check still fires), **not** `QA_FACTS_ALL_INVALID`, zero interactions with qaRuleRepository/qaFactSelectionService/aiReplyDraftService, blocked first (ManualSendSafetyBlockedException) then succeeds with `safetyWarningConfirmed=true` — second confirmation intact (D-1). Second-confirmation blocks for both old paths (`findings.isNotEmpty() && !safetyWarningConfirmed` + `requiresStrong && strongConfirmationText != "确认发送"`) unchanged.
- **I-44 / frontend shape split (app.js)**: `adoptTrustReplyAssembly` (`:10251-10302`) branches on `ragShape = Array.isArray(assembly.usedFactCodes)`; RAG shape → `manualReplyQaContext = { ragFactCodes, ragCorpusFingerprint, baselineText }` + `trustReplyAssembly = null` + SKIP `schedulePreflightCheck()` (calls pre-existing `resetPreflightState()` instead — function present in base too, count 1 in both); canonicalFactIds shape → unchanged `{qaRuleIds, baselineText}` + snapshot + preflight. Send assembly (`handleUnmatchedAction` `send-manual-rich-reply`) picks ragFactCodes+ragCorpusFingerprint vs qaRuleIds by context shape; legacy qaRuleIds branch verbatim in source. `assembly.text` fallback (05 payload compat) only affects shapes lacking renderedDraftText/rawDraftText. Executed `ragAdoptSendBridge.test.js` 5/5: RAG context has ragFactCodes+fingerprint, no qaRuleIds, preflight count 0, snapshot count 0, reset count 1; empty usedFactCodes still RAG-shape; canonicalFactIds shape behaves exactly as before (qaRuleIds + snapshot 1 + preflight 1); send-block source assertions both branches; G-8 anchor tests (`manualRichReplyEditor`/`manualReplyPreflight` ids still produced by app.js templates).
- **G-1..G-8 relevant**: G-1 fact_code string only business key (V113 comment, entity doc, DTO `ragFactCodes: List<String>`); G-2 fingerprint flow (client-supplied vs `RagKnowledgeBase.fingerprint()` live compare, no constant hardcode in service); G-4 no new rag→qa_rule coupling — grep over service RAG-branch lines shows no canonicalize/qaFactSelection/qaRuleRepository reference on ragMode paths; `MailRecordRagFact` refs confined to authorized files; G-5 no cache-key bump (styles/index.html untouched — c6 owns); G-8 honored by the new JS test's template-anchor assertions; G-9 V113 assigned per deployment order.
- **Controller/DTO**: `PendingManualRichReplyRequest` (`PendingMailOperationService.kt:1322-1342`) gains `ragFactCodes: List<String>? = null` + `ragCorpusFingerprint: String? = null` — existing `qaRuleIds: List<Long>? = null` untouched; sole live endpoint `UnmatchedInboundMailController.kt:247-269` forwards both new fields (2 added lines, matching A4's 1116→1118 pin rationale; line 1118 = `operatorStatus = operatorStatus` verified). Constructor gains trailing nullable `mailRecordRagFactRepository`/`ragKnowledgeBase` (defaulted null + `requireNotNull` at RAG entry) — direct-construction unit tests unaffected.

### AUTO_FIX
N/A — no proven four-gate violation (scope exact; plan identity binding verified; every acceptance criterion machine-verified; no unauthorized modification; no drift in downstream shapes).

### RECORD_ONLY
- O-1 (SHA transcription mismatch in orchestration + execution.md, not a product defect): assignment and c4 execution.md cite implementation commit `ae755c4b8b851f17cbf82db081d631332cee83e4`; that object **does not exist** in the repo (`git cat-file` fails; reflog shows a single c4 commit `ae755c417d2be4cecda52c5adf20a7f52227a072` created 2026-09-03 09:41:51). Actual HEAD = `ae755c417d2b...` with exact expected subject `feat(fast-p): implement c4`, exact expected parent chain (10a38bb → 2987af4 → 5a42058 → f63153a → HEAD), and exact authorized 10-file scope — content fully verified on all four gates. Likely the recorded hex was corrupted/truncated-copied (first 7 chars match). No evidence of wrong content or amended commit; branch tip and identity chain unambiguous. Future ledger/evidence should cite `ae755c417d2be4cecda52c5adf20a7f52227a072`.
- O-2 (one-line): success-path audit `recordSendAudit(carriesQa = <audit var>)` receives the RAG-computed carriesQa (true when codes present, distinguishing action type), while `collectSafetyFindings` explicitly gets false — plan I-47 ① only constrains the safety call; audit-var behavior is the plan's own T3 step-4 value (`ragFactCodes.isNotEmpty()`). No deviation.
- O-3 (one-line): brief.md header still states plan identity `commit:46cc5c4...` (pre-amendment seed); the A3/A4-amended identity f63153a is authoritative per ledger Amendments table and verified binding — brief was authored before A3/A4.

### Required Action
- COMPLETE_CHILD
