## Light Verification: LIGHT_PASS_WITH_NOTES

Child: c3 — docs/plans/2026-09-02/03-rag-letter-composer.md (整封生成：两次 LLM 调用 + 令牌逐字替换 + 未识别提问)
Plan identity: commit 46cc5c46395814b1ef03e52ab8b8bfb5197f372c (`git diff 46cc5c4 -- docs/plans/2026-09-02/03-rag-letter-composer.md` empty — verified, 0 output)
Master plan: docs/plans/2026-09-02/00-execution-order.md (identity 92b0519a18a3a46989f8733259af4649f7748a72 — diff empty, verified; G-1..G-4 read in full)
Boundary: af8fb5fad2bb28ebf18324242e2959d11d297aad..10a38bb6457280f7104a333faa46fad6f7cb078f (implementation commit `feat(fast-p): implement c3`; interleaved docs-only 163bd43 = c2 evidence under docs/plans/fast/**, not product)
Verifier: VerifierC3
Worktree: /Users/lukai/IdeaProjects/weibo-talent-introduction-fast-2026-09-02-execution-order (branch `fast/2026-09-02-execution-order`, HEAD == 10a38bb6457280f7104a333faa46fad6f7cb078f — confirmed before and after runs)
Product read-only honored; only file written: this report. `git status` shows only untracked `docs/plans/fast/2026-09-02-execution-order/children/c3/` (implementer brief/execution + this report).

### Four Gates

| Gate | Result | Evidence |
|---|---|---|
| 1. Authorized scope | PASS | `git show 10a38bb --name-only` = exactly the plan's 10 `## 变更文件清单` files: HttpLlmDraftClient.kt (mod) + RagPromptConstraints/RagPromptBuilder/RagProcessContextResolver/RagVerbatimRenderer/RagLetterComposer/RagReplyController + RagLetterComposerTest/RagVerbatimRendererTest/RagProcessContextResolverTest. Boundary product diff base..HEAD restricted to those 10 (grep over name-only minus docs: nothing else). No c1/c2 rag file modified (RagKnowledgeBase/RagProperties/V112/domain/repositories/c2 four services all untouched — not in commit; c3 consumes `snapshot()`, `RagProperties`, `RagPrefilterService.prefilter/requestedCoverageKeys`, `RagMandatoryResolver.resolve`, shared `RagProcessContext` data class defined in c2's RagPrefilterService.kt:31 read-only). No migration, no `qa_rule`/`qa_category`, no ReplySnippetService/TrustReplyWorkbenchService change (ReplySnippetService not in commit; `resolveDefaultSelectableFrame():83` / `resolveSelectableFrame():100` pre-existing read APIs used — 4th consumer read-only). No docs/plans/fast in commit. |
| 2. Plan requirements & invariants | PASS (3 registered deviations consistent with plan acceptance — see RECORD_ONLY O-1..O-3) | Code + direct test evidence below for I-13..I-19, I-45, I-46, I-10, G-1..G-4, endpoint/error mapping, cache key, T0 call-path trace. |
| 3. Required commands | PASS | All 9 required commands freshly run by me (JDK 11 zulu-11, worktree root, final committed state); exit codes + counts in table below. 3097/0/0/7 reproduces the implementer claim exactly (3073 + 24 new tests; skip 6 → 7 = I-46 `@Disabled` registration). Plain suite Docker-free green: 5 docker/IT classes are migrationIt-gated (skipped message `System property [migrationIt]...`), RagLetterComposerTest skip = I-46 registration, EuropePmcDataSourceTest network-gated (pre-existing). |
| 4. Downstream interfaces | PASS | `RagComposeResult(frame: ResolvedReplyFrame, bodyParagraphs, usedFacts, unaddressed, modelCoverage, warnings, corpusFingerprint, retrievalUsage, generationUsage)` (RagLetterComposer.kt:73-87); `usedFacts` items carry `factCode/title/renderMode/riskLevel/status/origin(MANDATORY\|MODEL)` (:48-59); `unaddressed` items `quote/reason` (:63-68); DTOs hold fact_code strings only (G-1). T0 4-arg overload `chatWithModelObservedJson(messages, temperature, providerModel, maxTokens)` defaulted on the interface (HttpLlmDraftClient.kt:145-153), sole override in HttpLlmDraftClient (:223-227). Service APIs all public per plan T1-T6; `RagPromptConstraints.DERIVED_GENERATION_RULE_INDICES = {17,18,20}` exported for 06's derived-rules UI; `LlmTokenUsage` added (authorized-file internal; nuance (c) resolved — defaulted nullable field, every existing callsite uses ≤2 positional args, zero other-caller breakage, full suite green). |

Gate-2 detail evidence (direct code inspection + executed tests):
- **T0 path trace (I-45, nuance (b), file:line)**: RAG chain calls `chatWithModelObservedJson(...)` with all 4 args from the single shared channel `RagLetterComposer.callLlm` (RagLetterComposer.kt:317-327 `maxTokens = maxTokens`); both retrieval (`:184-189` temp=retrievalTemperature 0.0, maxTokens=retrievalMaxTokens 900) and generation (`:237-242` 0.2 / 2600) go through it. Rag package contains **zero** `chatWithModelObservedStream` / `chatWithModelObserved` / `chat(` references (grep) — the chain never streams. `HttpLlmDraftClient` 4-arg override (:223-227) → `executeChatObserved(..., jsonOutput=true, maxTokens)` (non-streaming restTemplate `postForEntity`, :443-484) appends `body["max_tokens"] = maxTokens` only when non-null (:462-465). 3-arg path: `maxTokens` defaults null → key never added → request byte-identical to pre-change (verified in diff: only hunk touching success-path response is the additive `usageOf` — content/failureType semantics unchanged). Signatures of `chatWithModelObserved`/`chat`/`chatWithModel` have **no diff hunks** (grep over +/- lines: only the two new 4-arg declarations). Override count `grep -rc 'override fun chatWithModelObserved\b'` = **22** (unchanged). Test `four arg overload appends max_tokens only when provided` asserts real `HttpLlmDraftClient` bodies via captor: maxTokens=900 → `"max_tokens":900`; maxTokens=null → key absent; 3-arg call → key absent — all green.
- **I-13 / G-3**: `RagPromptBuilder.generationRecord` (RagPromptBuilder.kt:211-235) drops `answer` for VERBATIM, appends `render_token = {{FACT:<fact_code>}}` + `render_instruction`; COMPOSE records keep answer. Generation record has no `title`/`retrieval_text`; retrieval record (retrievalRecord :189-208) carries `title` + `retrieval_text` per script `retrieval_record()` (G-3 layered — title on retrieval side only). `generationPromptHidesVerbatimAnswers` (executed, 1/0/0/0) asserts: `After a successful application,` prefix absent, full KB-FUND-033 answer absent, `{{FACT:KB-FUND-033}}` present, **all 7** VERBATIM answers absent from user prompt, and no CJK characters in system+user generation prompt (Chinese titles never leak). Compose test asserts KB-NOPE-999 (invalid retrieval id) also never reaches `retrieved_chunks`.
- **I-15**: `RagVerbatimRenderer.render` (RagVerbatimRenderer.kt:33-77) implements dedupe-keep-first → 3-level fallback (nearest previous in-body token after / nearest following in-body token before / after first paragraph, `\n\n` boundaries; no blank line → very front) → verbatim replace, final trim. `RagVerbatimRendererTest` (6/0/0/0, all green): 3 fallback cases each asserting exact expected text incl. `\n\n` padding on both sides, 4th front-insert (single unbroken paragraph) case, duplicate-token keep-first (answer occurs exactly once), and no-verbatim returns draft unchanged. Composer-level `missing single token is inserted and the final answer appears` (KB-COMP-007 omitted from a 6/7-token draft → inserted after KB-GOV-004 with `\n\n`) green.
- **I-14**: two-layer: pre-render guard in composer (`verbatimFacts.isNotEmpty() && missingInDraft.size == verbatimFacts.size` → `RagComposeException(422, "RAG_VERBATIM_MISSING")` listing all codes, RagLetterComposer.kt:266-275) then `render()` + post-render `violations()` substring audit (:278-285). `verbatimMissingFailsWholeCompose` (executed, 1/0/0/0) — stub returns zero-token paraphrased draft → asserts status 422, code RAG_VERBATIM_MISSING, message lists KB-PROG-002 + KB-FUND-033, and (by exception throw) no draft object is returned. Registered semantics deviation O-1 below.
- **I-16**: code (RagLetterComposer.kt:196-230): invalid model fact_ids dropped + `log.warn`; mandatory (resolver + request-forced, LinkedHashSet keep-first) front-merged; coverage-key intersect with `requestedCoverageKeys` appends unselected candidates at tail; truncation `take(properties.retrievalLimit)` (14); empty/all-invalid + no coverage hit → `candidateFacts.take(12)` fallback. Tests green: invalid codes dropped & mandatory front (usedFacts == japanesePrefilter 14, take(7)==mandatory, origin split MANDATORY/MODEL, fingerprint e62421a42c432cf3 asserted); empty-retrieval gibberish → exactly first 12 prefilter candidates, all origin MODEL.
- **I-17**: `parseUnaddressed` (RagLetterComposer.kt:356-386) reuses InboundAskEnumerator shape: `foldWhitespace` both sides (no lowercasing), folded length < 8 dropped, `foldedInbound.indexOf(foldedQuote) < 0` dropped silently, `seenQuotes` first-occurrence dedupe. Tests green: invented quote dropped, real quote kept with reason, short quote dropped, duplicate collapsed; whitespace-folded multiline quote still matches and dedupes against its single-line twin.
- **I-18 / D-5**: `GENERATION_RULES[11]` (12th) = "Do not write a salutation, greeting, thank-you, or signature; the reply frame supplies them." — no `Sign as` anywhere in the file (test `generation rules index eleven has no Sign as` asserts 22 rules + no `Sign as` + contains "salutation"). Frame resolved via ReplySnippetService, model body and frame returned separately (`RagComposeResult.frame`/`bodyParagraphs`). Test `model body never contains Wu Wei while the frame closing does` green: bodyParagraphs concat has no `Wu Wei`/`Dear Professor`, `frame.closing` contains `Wu Wei, Customer Care Officer`.
- **I-19**: `RagProcessContextResolver.resolve` (RagProcessContextResolver.kt:36-62): CV row `PROVIDED→RECEIVED`, `DECLINED→UNKNOWN`, null row→`MISSING`, defensive `PENDING→MISSING`, other→`UNKNOWN`+warn; `expertReplyCount` = `mail_record` count with `direction == INBOUND` (app-layer filter over existing `findAllByExpertContactIdOrderByCreatedAtAsc`); `expertTags` empty. Resolver tests (4/0/0/0): three mapping cases + 3-INBOUND-of-5 count, all green, mocked repositories, zero DB.
- **I-46**: class-level comment in RagLetterComposerTest + `@Disabled("I-46 已登记偏离…")` placeholder test `registeredDeviationsThinkingAndStreamAreNotImplemented` — registered, skipped (counted in Skipped: 7), not a failure. Note: the RAG chain's own requests are non-streaming `executeChatObserved` bodies that never carry `stream`/`thinking` keys at all; the deviation text covers production's streaming workbench path.
- **I-10**: `modelCoverage` appears only as DTO field declaration (:82), doc comment, and constructor assignment `modelCoverage = parseCoverage(generationJson)` (:296) — never read by any branch (grep: 3 hits, no `if`). `unaddressed` likewise only filtered then passed through. Test `model coverage and warnings pass through without driving decisions` green (coverage array echoed verbatim; not consulted).
- **Endpoint / error mapping / validation**: `POST /api/rag-reply/compose` (RagReplyController.kt:55-79); source resolution via two repo paths (`resolveTrainingMail` mail_record INBOUND-only; `resolveLiveInbound` inbound_mail_processing w/ contact required) + expert_contact — never `TrustReplyWorkbenchService.resolveSource` (grep: comment-only references). `RagComposeException` `@ExceptionHandler` ( :141-146) maps `ex.status` → HTTP (422 RAG_VERBATIM_MISSING / 502 RAG_LLM_UNAVAILABLE from composer; 400 RAG_FACT_CODE_INVALID from `validateFactCodes` against `knowledgeBase.enabledFacts()` :122-139). G-1: forced/excluded are `fact_code` strings; no rag auto-increment id in any DTO/response (request DTO `RagReplyComposeRequest` :66-74 — sourceId is the mail/inbound id, not a rag id). Cache key `sha256(inboundText) + ":" + snapshot.fingerprint` (RagLetterComposer.kt:175, reused G-2 fingerprint e62421a42c432cf3). No draft persistence, no send-path call anywhere in rag main code (grep: composer/controller have no repository writes).
- **G-1..G-4**: G-1 above; G-2 fingerprint constant asserted in test (`EXPECTED_FINGERPRINT`); G-3 evidence under I-13; G-4 grep `qa_rule|QaRule` under rag/ = no matches (only legacyRuleId semantics untouched in c1 domain).

Required commands (fresh, JDK 11, final committed state, worktree root — all executed by verifier):

| # | Command | Exit | Result |
|---|---|---|---|
| 1 | `mvn test -Dtest=RagLetterComposerTest` | 0 | Tests run: 14, Failures: 0, Errors: 0, Skipped: 1 (I-46 registration) — BUILD SUCCESS |
| 2 | `mvn test -Dtest=RagVerbatimRendererTest` | 0 | Tests run: 6, Failures: 0, Errors: 0, Skipped: 0 — BUILD SUCCESS |
| 3 | `mvn test -Dtest=RagProcessContextResolverTest` | 0 | Tests run: 4, Failures: 0, Errors: 0, Skipped: 0 — BUILD SUCCESS |
| 4 | `mvn test -Dtest=RagLetterComposerTest#verbatimMissingFailsWholeCompose` | 0 | Tests run: 1, Failures: 0, Errors: 0, Skipped: 0 — BUILD SUCCESS |
| 5 | `mvn test -Dtest=RagLetterComposerTest#generationPromptHidesVerbatimAnswers` | 0 | Tests run: 1, Failures: 0, Errors: 0, Skipped: 0 — BUILD SUCCESS |
| 6 | `grep -rc "override fun chatWithModelObserved\b" src/main src/test \| awk -F: '{s+=$2} END {print s}'` | 0 | **22** — I-45 count unchanged |
| 7 | `mvn clean package` | 0 | **Tests run: 3097, Failures: 0, Errors: 0, Skipped: 7** — BUILD SUCCESS, WAR built |
| 8 | `mvn test` (full regression) | 0 | **Tests run: 3097, Failures: 0, Errors: 0, Skipped: 7** — BUILD SUCCESS |
| 9 | `git diff --check` | 0 | No output |

Count arithmetic: c2 terminal 3073/0/0/6 + 24 new tests (14 composer incl. 1 disabled + 6 renderer + 4 resolver) = 3097; Skipped 6 → 7 = the single I-46 `@Disabled`. Implementer's claimed numbers reproduced byte-for-byte. Worktree clean except untracked c3 docs; HEAD verified at 10a38bb before and after all runs.

### AUTO_FIX
N/A — no proven four-gate violation (scope exact; plan identity binding verified; every acceptance criterion machine-verified; no unauthorized modification).

### RECORD_ONLY
- O-1 (registered deviation (a), consistent with plan acceptance): I-14 trigger semantics — composer fails whole-compose 422 when the **raw model draft contains zero** VERBATIM render tokens (overall-rewrite case, RagLetterComposer.kt:266-275), while drafts retaining ≥1 token get missing tokens rescued by I-15 insertion. Pure script semantics (`render_verbatim_facts` auto-inserts everything) would make post-render I-14 violations unreachable — the plan's own I-15 risk note says exactly that — so the plan's acceptance `verbatimMissingFailsWholeCompose` ("既无令牌、又把原文改写 → 422 且无草稿返回") is only satisfiable via a pre-render guard. Implementation matches the plan acceptance test verbatim; post-render `violations()` audit still runs as final net. Classified RECORD_ONLY (implementer-registered; no plan text violated; no AUTO_FIX repair exists or is needed).
- O-2 (registered deviation 3; authorized-file-internal): T0 file additionally adds `LlmTokenUsage` data class + `LlmChatResult.usage: LlmTokenUsage? = null` (defaulted) and parses DeepSeek `usage` on the **non-streaming** success path only (streaming stays null). Justification: the plan-mandated `RagComposeResult(retrievalUsage, generationUsage)` fields have no other data source. Nuance (c) resolved: defaulted tail field; grep of every `LlmChatResult(` callsite in src/main+test shows all use ≤2 positional args (or named) — no caller breaks; failure/content semantics of all pre-existing paths byte-identical (only hunk: `LlmChatResult(content)` → `LlmChatResult(content, SUCCESS, usageOf(body))`); full 3097 suite green.
- O-3 (registered deviation 1/2 combined with plan acceptance): streaming path (`chatWithModelObservedStream`, 18 test-stub overrides frozen like I-45's 22) does not accept maxTokens — RAG chain never uses it (non-streaming 4-arg JSON overload only, evidenced above), so `max_tokens` reaches the only HTTP body the rag chain actually sends. 3-arg path byte-identical (no key). Both asserted in `four arg overload appends max_tokens only when provided`.
- O-4 (one-line): retrieval cache keyed on `sha256(inbound)+":"+fingerprint` per plan; a same-inbound call from a different contact/context reuses model-selected ids but re-runs the full I-16 validation + coverage/回补 per request (documented in code :115-125) — no plan deviation, minor design note only; no direct cache unit test (behavior exercised implicitly, retrieval invoked once per compose).
- O-5 (one-line): controller adds supplementary source-error codes 404 `RAG_REPLY_SOURCE_NOT_FOUND`/`RAG_REPLY_SOURCE_CONTACT_NOT_FOUND`, 422 `RAG_REPLY_SOURCE_NOT_INBOUND`/`RAG_REPLY_SOURCE_CONTACT_REQUIRED`, 400 `RAG_REPLY_SOURCE_INVALID` through the same `RagComposeException` channel; plan pins only the three named 400/422/502 codes — additive, no conflict.

### Required Action
- COMPLETE_CHILD
