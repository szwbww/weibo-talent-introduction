## Light Verification: LIGHT_PASS_WITH_NOTES

Child: c2 — docs/plans/2026-09-02/02-rag-deterministic-retrieval.md (确定性层：归一化 / 短语组 / 覆盖键 / 强制 / 剔除 / 预筛)
Plan identity: commit 46cc5c46395814b1ef03e52ab8b8bfb5197f372c (`git diff 46cc5c4 -- docs/plans/2026-09-02/02-rag-deterministic-retrieval.md` empty — verified, 0 lines)
Master plan: docs/plans/2026-09-02/00-execution-order.md (identity 92b0519a18a3a46989f8733259af4649f7748a72 — diff empty, verified; G-1..G-4)
Boundary: acb88c1e77d172a7f252690b1da1203f08c01817..af8fb5fad2bb28ebf18324242e2959d11d297aad (implementation commit `feat(fast-p): implement c2`; interleaved docs-only cd2c363 = c1 evidence under docs/plans/fast/**, not product)
Verifier: VerifierC2
Worktree: /Users/lukai/IdeaProjects/weibo-talent-introduction-fast-2026-09-02-execution-order (branch `fast/2026-09-02-execution-order`, HEAD == af8fb5fad2bb28ebf18324242e2959d11d297aad — confirmed)
Product read-only honored; only file written: this report.

### Four Gates

| Gate | Result | Evidence |
|---|---|---|
| 1. Authorized scope | PASS | `git show af8fb5f --name-only` = exactly the plan's 8 `## 变更文件清单` files: RagTextNormalizer.kt, RagPhraseMatcher.kt, RagMandatoryResolver.kt, RagPrefilterService.kt, scripts/dump_rag_parity_fixtures.py, src/test/resources/rag-parity/fixtures.json, RagPrefilterParityTest.kt, RagTextNormalizerTest.kt. Boundary diff acb88c1..af8fb5f adds only those 8 product files + 4 docs/plans/fast evidence files from docs-only cd2c363 (c1 brief/execution/verify-log/ledger). Nothing else touched: no c1 rag file modified (RagFact/RagRetrievalRule/repositories/RagKnowledgeBase/RagProperties/V112/export_rag_kb_sql.py/FlywayMigrationIntegrationTest all untouched), no migration, no qa_rule, no other source/test. `git status`: only untracked `docs/plans/fast/2026-09-02-execution-order/children/c2/` (evidence dir incl. this report). |
| 2. Plan requirements & invariants | PASS (2 human-approved/registered deviations, see RECORD_ONLY) | Code + direct test/fixture evidence below: I-7..I-12, G-1..G-4 (c2 consumption), D-3 registration, GOV alias nuance, parity corpus (10 cases, deterministic). |
| 3. Required commands | PASS | All freshly run JDK 11 (zulu-11) from worktree root, exit codes + counts in the table below; 3073/0/0/6 matches implementer claim and c1 3063 + 10 new tests; plain suite docker-free green (RagKnowledgeBaseTest + FlywayMigrationIntegrationTest each `Tests run: 1, Skipped: 1` migrationIt-gated). |
| 4. Downstream interfaces | PASS | RagTextNormalizer.normalize/tokens; RagPhraseMatcher.containsAny/matchedGroups (group_code ascending, distinct, sorted — code); RagMandatoryResolver.resolve (ordered by sort_order, LinkedHashSet first-occurrence dedupe, enabled-only filter); RagPrefilterService.requestedCoverageKeys/shouldRequestCv/lexicalScore/prefilter (≤18 via take(prefilterLimit=18)); all four services hold only optional `RagKnowledgeBase` + `RagProperties` and read exclusively `snapshot()` (no repository/DB/LLM in c2 main code); extra `RagProcessContext` data class (D-7 mapping target) inside the authorized new service file; zero production callers (grep under src/main: only self-references inside rag/service). |

Gate-2 detail evidence (direct code/test/fixture inspection):
- **I-7**: `RagTextNormalizer` hardcodes `Regex("[a-z0-9]+")` (ASCII, documented never-Unicode); `normalize` = lowercase → tokens joined `" "` → padded one space each side; `tokens` = same-regex set. `RagPhraseMatcher.containsAny` = `normalize(phrase) in normalize(text)` substring. Test literals: `normalize("Compensation, please?") == " compensation please "`; `containsAny("I am unpaid", ["paid"]) == false`; `("I am paid") == true`; `"" → "  "`; é/× → tokens cut at ASCII boundary (`setOf("caf","2","r","sum")`); empty phrase list → false. Script `_TOKEN_RE`/`_normalized` comparison verbatim (spike :418, :506-512).
- **I-8**: `prefilter` implements the five steps in order: ① sort `-score, fact_code` over ALL enabled facts; ② requested non-empty → coverage-intersect else `score >= minLexicalScore(2)`; ③ exclusions (`whenGroups` all hit && `unlessGroups` none hit → drop FACT_CODE / COVERAGE_KEY target); ④ mandatory front-merge `mandatory + surviving.filterNot { in mandatoryIds }` where mandatory is resolved from the **full enabled corpus** (`enabledById` built from all enabled facts, never from `selected`); ⑤ `take(prefilterLimit=18)`. Step order KDoc + white-box test `prefilter step order is not commutable` proves 033 is a step-③ exclusion target in the `more details + compensation` scenario and that its only re-entry path is step ④ from the full corpus; scenario-4 asserts final candidates contain 033 and not KB-FUND-034. Fixtures: no prefilter list > 18 (all 10 cases ≤ 18; max 14).
- **I-9**: LinkedHashSet first-occurrence dedupe in `RagMandatoryResolver.resolve` and `requestedCoverageKeys`; Japanese-professor full sample asserted exact `[KB-PROG-002, KB-FUND-033, KB-PROG-003, KB-GOV-004, KB-COMP-007, KB-IP-039, KB-CONF-036]` in both fixture and Kotlin (parity + dedicated I-9 test), `indexOf("KB-FUND-033") == 1` (DETAIL_INQUIRY(10) + COMPENSATION(15) dual hit keeps 033 after PROG-002).
- **I-10**: grep gate `coverage\[` under `src/main/kotlin/com/weibo/talentintroduction/rag/` → no matches; none of the four services reference any LLM output; requested keys derive only from `snapshot.intentCoverage` + matched groups + I-12 CV append.
- **I-11**: exclusions kept verbatim — the 4 `rag_prefilter_exclusion` rows (fixture corpus mirrors V112) encode exactly the script's two hardcoded prefilter blocks (block1 ≡ rows 1-2 with COMPENSATION_MENTION/GOVERNMENT_FUNDING_MENTION single-phrase groups; block2 ≡ rows 3-4 with DETAIL_INQUIRY 18 phrases / COMPENSATION_STRUCTURE 6 phrases). KB-FUND-036 stays suppressed when only compensation is asked (036 absent from all 8 plan rows incl. 033-hit rows; verified in fixture data + D-3 test). Direct corpus-level check I ran: for all 10 fixture emails, comparing raw `spike.prefilter_facts`/`spike.mandatory_fact_ids` against the D-3-patched fixture outputs, the differences are **exactly** the D-3 COMPENSATION-row consequences (KB-FUND-033 added as mandatory and front-merged) on the 3 compensation-only letters (real-mockup-compensation-only, scenario-1, scenario-6); mandatory/prefilter identical on the other 7; requested-key sets identical on all 10 → D-3 is the only deliberate behavioral deviation.
- **I-12**: code = `expertReplyCount >= 2 && cvStatus == MISSING && positiveIntent && asksNextStep` (AND), positiveIntent = `WILLING_TO_CONTINUE` tag (trim/uppercase) or POSITIVE_INTENT group hit, asksNextStep = NEXT_STEP group hit; test has 4 single-condition-false cases + full-true case + `application.required_materials` appended at end of requested on hit + tag path.
- **I-2 / A-5**: exactly 1 disabled fact (KB-APP-017, `enabled=false`); resolve() filters enabled-only; KB-APP-017 absent from every fixture expected output (JSON scan) and asserted never in Kotlin mandatory/requested/prefilter for all cases.
- **GOVERNMENT_ORG/GOVERNMENT_ORGANIZATION alias nuance (verified per brief)**: (a) the canonical phrase/intent group code in the machine corpus is `GOVERNMENT_ORGANIZATION` (rag_phrase_group 5 rows, rag_intent_coverage 2 rows; fixture corpus and V112 agree; script side has no group codes — constant `_GOVERNMENT_ORGANIZATION_PHRASES`, export `_INTENT_GROUP_NAMES` pairs it to code GOVERNMENT_ORGANIZATION), while `rag_mandatory_rule` rows 30/40 carry raw `GOVERNMENT_ORG` (data typo inherited from plan-01 T2/`export_rag_kb_sql._MANDATORY_ROWS` — NOT modified by c2, fixture corpus serializes the same raw strings byte-identically to the DB rows). Fixtures are machine-generated from the script sources: every expected value computed by `dump_rag_parity_fixtures.py` from spike functions/constants (`_normalized`, `_lexical_score`, `should_request_cv`, `_INTENT_COVERAGE`, `_POSITIVE_INTENT_PHRASES`, `_NEXT_STEP_PHRASES`, `SAMPLE_INBOUND_EMAIL`) + export constants (`_MANDATORY_ROWS`, `_EXCLUSION_ROWS`, `_INTENT_GROUP_NAMES`, `_EXTRA_PHRASE_GROUPS`) with a hard fingerprint gate. (b) the alias (`GOVERNMENT_ORG → GOVERNMENT_ORGANIZATION`, identity otherwise) exists identically in Kotlin `RagMandatoryResolver.normalizeGroupCode` and python `mandatory_ids_d3._normalize_group_code`, so org-asking letters force KB-GOV-004/KB-COMP-007 in both; professor full sample (asks org) yields the plan-table 7-code list in fixture AND Kotlin (parity 7/7 green) — outputs match. (c) D-3 remains the only deliberate behavioral deviation — corpus-level spike-vs-patched comparison above. (d) no authorized-file violation (Gate 1).
- **Parity corpus**: fixtures.json has 10 cases = 3 real (real-spike-sample = verbatim spike `SAMPLE_INBOUND_EMAIL` real letter; real-mockup-compensation-only = mockup sample A; real-mockup-japanese-professor-full = de-identified mockup sample B) + 7 constructed (SCENARIOS rows 1-7 verbatim from the plan table; row 8's letter is real-mockup-japanese-professor-full per execution §Deviations 2) → the full 8-row plan table is reproduced by fixture id. `compensationMandatoryIsTheOnlyDeliberateDeviation` asserts plan-table constants == fixture output == Kotlin output for all 8 rows (mandatory lists + 033-hit/036-hit columns) + row-7 no-033 + A-1 three wordings. Fixture corpus section mirrors V112 byte-identically in row semantics: 45 facts / 87 phraseGroups / 21 intentCoverage / 6 mandatoryRules / 4 exclusions / fingerprint e62421a42c432cf3 (test asserts fingerprint + 45). Deterministic regeneration: two consecutive `python3 scripts/dump_rag_parity_fixtures.py` runs then `git diff --stat src/test/resources/rag-parity/fixtures.json` → empty (verified). Kotlin parity: all 10 cases × 3 outputs (mandatory/requested/prefilter ordered lists) == fixture, zero diff (RagPrefilterParityTest 7/7 green; also my fresh recompute of generator outputs == committed fixtures for all 10 cases).
- **G-1..G-4 (c2 consumption)**: fact_code is the only surfaced key (rules/exclusions/fixtures reference codes only; no id in any outward path); G-2 fingerprint constant e62421a42c432cf3 asserted at fixture load and gate in generator; G-3 nothing outward in this child (no prompts/callers exist); G-4 grep `qa_rule|QaRule` under src/main/kotlin/com/weibo/talentintroduction/rag/ → no matches (only c1's legacyRuleId comment in domain, not in c2 files).

Required commands (fresh, JDK 11, final committed state, worktree root):

| # | Command | Exit | Result |
|---|---|---|---|
| 1 | `mvn test -Dtest=RagTextNormalizerTest` | 0 | Tests run: 3, Failures: 0, Errors: 0, Skipped: 0 — BUILD SUCCESS |
| 2 | `mvn test -Dtest=RagPrefilterParityTest` | 0 | Tests run: 7, Failures: 0, Errors: 0, Skipped: 0 — BUILD SUCCESS |
| 3 | `mvn test -Dtest=RagPrefilterParityTest#compensationMandatoryIsTheOnlyDeliberateDeviation` | 0 | Tests run: 1, Failures: 0, Errors: 0, Skipped: 0 — BUILD SUCCESS |
| 4 | `python3 scripts/dump_rag_parity_fixtures.py` ×2 + `git diff --stat src/test/resources/rag-parity/fixtures.json` | 0 | Deterministic: rerun left fixtures.json byte-identical (diff empty); script prints `10 cases (3 real / 7 constructed)` |
| 5 | `mvn clean package` | 0 | **Tests run: 3073, Failures: 0, Errors: 0, Skipped: 6** — BUILD SUCCESS |
| 6 | `mvn test` (full gate) | 0 | **Tests run: 3073, Failures: 0, Errors: 0, Skipped: 6** — BUILD SUCCESS; docker classes migrationIt-gated (RagKnowledgeBaseTest/FlywayMigrationIntegrationTest: Tests run 1, Skipped 1 each) — plain suite Docker-free green |
| 7 | `git diff --check` (+ `git diff acb88c1..af8fb5f --check`) | 0 | No output, both forms clean |

Counts: baseline @seed 46cc5c4 3062/0/0/5 → c1 3063/0/0/6 → c2 3073/0/0/6 = +10 new tests (7 parity + 3 normalizer), exactly the implementer's reported 3073/0/0/6.

### AUTO_FIX
N/A — no proven four-gate violation.

### RECORD_ONLY
- O-1 (human-approved env/data block, do-not-FAIL): parity corpus has **10 cases (3 real / 7 constructed)** vs plan T5's "≥28 条 (≥20 封真实来信 + 8 构造场景)". Historical `mail_record` INBOUND bodies are runtime user data — no migration seeds any mail_record row, and no reachable DB in this environment holds ≥1 INBOUND body (execution §Deviations 1 evidence a-f: localhost:3306 down; mailagg-mysql 5 rows/1 INBOUND body NULL; qingfei-phase1-mysql no mail tables; brew mysql@5.7 no talent_introduction DB; scratch chain rebuilds V1..V112 schema only; repo/K-docs contain no real archived bodies). Per controller ruling fixtures = all available real/realistic letters (spike SAMPLE_INBOUND_EMAIL verbatim + mockup sample A + de-identified mockup sample B) + the 8 constructed table rows; **no fabricated emails**. Test pins existence (8 plan rows + ≥1 real kind + 10 cases) not the unreachable 28.
- O-2 (registered, machine authority): plan 实测基线 row-8 prose is stale on two cells — the spike `SAMPLE_INBOUND_EMAIL` actually machine-runs to 2 mandatory / 9 requested / 8 candidates, while the de-identified mockup `rag-knowledge-base.html` sample-B letter (the plan author's own tracked letter, fixture `real-mockup-japanese-professor-full`) reproduces the plan row-8 values exactly: 7 mandatory incl. KB-GOV-004/KB-COMP-007, requested 16, detail=是, 033 hit, 036 miss; and the "预筛候选 18 条" cell is unreachable (this profile maxes at 14 qualifying facts; fixture + Kotlin assert 14). D-3 test asserts the plan table's mandatory/033/036/16-keys cells verbatim against fixture and Kotlin; only the 18-candidate prose differs (execution §Deviations 2).
- O-3 (registered): requested-coverage-keys list order = DB load order (`rag_intent_coverage` ORDER BY group_code, sort_order — plan-01 repository authority) instead of spike's private `_INTENT_COVERAGE` constant order. Key **sets** identical on every input; the only ordered-list corner is an email hitting both IP and CONFIDENTIALITY groups (shared key `confidentiality.materials` changes keep-first position). No downstream effect — prefilter intersects key sets only. Generator + Kotlin share the same order → parity exact (execution §Deviations 4; fixture `note` field).
- O-4 (registered): fixture expected values are computed by D-3-patched mirrors over spike functions (`prefilter_d3`/`mandatory_ids_d3` reimplement spike's five-step pipeline calling `spike._lexical_score`/`_normalized`/`should_request_cv`, with mandatory via the 6 DB rules) rather than literally invoking `spike.prefilter_facts`/`mandatory_fact_ids` — the D-3 row and DB-order representation cannot be injected into the not-authorized spike file. Anchor: my raw-vs-patched comparison proves divergence from the actual script functions is exactly the D-3 033 consequences on 3/10 cases and zero elsewhere (see I-11 evidence). Plan-row constants additionally pinned by the D-3 registration test.
- O-5 (one-line): D-3's COMPENSATION matcher group is the shared intent group including the script's own phrase `paid` (spike intent row 8 tuple `compensation/remuneration/salary/paid`, unchanged c1 seed) — so a letter containing only the word "paid" also triggers the D-3 mandatory 033; consistent across fixture python and Kotlin, within approved D-3 rule semantics (c1 domain), no divergence vs script possible (script has no D-3).
- O-6 (one-line): environment accommodation required for container runs is NOT needed for c2 — all required commands (incl. full gate) ran Docker-free green on plain `mvn test`; no V82 involvement in this child.

### Required Action
- COMPLETE_CHILD
