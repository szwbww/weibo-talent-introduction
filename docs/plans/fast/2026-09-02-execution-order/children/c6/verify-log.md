## Light Verification: LIGHT_PASS

Child: c6 — `docs/plans/2026-09-02/05-workbench-frontend-replace.md` (plan identity `commit:46cc5c46395814b1ef03e52ab8b8bfb5197f372c`; worktree diff vs identity EMPTY)
Boundary: `db89054f32a51f79f4cc86f5b21a9871a8dac729` (c5 code head) .. `60efcbaaa14e919ff8b4cfa9539cca41fd6a6d62` (`feat(fast-p): implement c6`); docs-only `4f11bc6` (c5 evidence) inside boundary, not product
Verifier: VerifierC6
Date: 2026-09-03 (JDK 11 zulu-11, worktree root)

### Four Gates

| Gate | Result | Evidence |
|---|---|---|
| **1. Authorized scope** | PASS | `git show 60efcba --name-only` = 14 files, all in expected set: rewrite `trust-reply-workbench.js` (3122 → 587 lines), `index.html` (3 key lines only), `styles.css`, `ragWorkbenchRender.test.js` (NEW), rewrite `trustReplyWorkbenchSharedMount.test.js` (3062 → 269), DELETE `trustReplyWorkbench.test.js` + `trustReplyWorkbenchThreeStep.test.js`, key sync `batchSendTaskConsoleVisualFix.test.js:49-51`; controller/G-5/G-7-authorized: DELETE `autoRunOrchestration.test.js` + `aiReplyLoadingFeedback.test.js`, surgical rewrite `overlayAndDialogContrast.test.js` (5 surviving cases: I-7/I-8/S-1/S-2/S-3), single-string syncs in `checkRepliesRelocation.test.js:11`, `manualReplySubjectPrefill.test.js:13`, `ragKnowledgeBasePage.test.js:332-338`. `autoPreviewWorkbenchHost.test.js` **zero-diff confirmed** (`git diff … -- it` = 0 bytes; content asserts AUTO_PREVIEW-retirement tokens + LIVE host existence, orthogonal to c6). **`git diff db89054..60efcba -- app.js` = EMPTY** (0 bytes). No backend files in boundary (`git diff --stat` lists static/test/docs only). No unexpected file. |
| **2. Plan requirements & invariants (I-24..I-29, G-1..G-8, S-1..S-5)** | PASS | I-24: idempotent IIFE + `window.TrustReplyWorkbench.mount(host, options)` → `{unmount}`; options keys mode/source/contextPath/autoBootstrap/onUnauthorized/onChange/onComplete consumed (`options.<key>` each); app.js mount sites/unmount/`requireTrustReplyWorkbenchRuntime` untouched (app.js diff empty); SharedMount asserts mount-site option-key union == contract set and no extra keys. I-25: per-instance `requestSeq` + `AbortController` Set; compose seq-guards + aborts old; `unmount()` aborts all + unbinds listeners (tests: signal.aborted, listenerCount 0, late response never rewrites host). I-26: `renderParagraph` emits `verbatim` class solely from `renderMode==="VERBATIM"`; `grep 'answer ==='` EMPTY in js + test. I-27: `dirty` visible marker (`data-role="dirty-flag"` 「已手工编辑」); dirty regenerate → `global.confirm` first; cancel keeps edits, confirm fires compose (test covers both). I-28: `applyAddFact/applyRemoveFact` only mutate `forcedFactCodes`/`excludedFactCodes` then re-compose; paragraph array untouched (pure-fn + flow tests assert length stable). I-29: send disabled only by `!hasDraft || busy`; unaddressed=3 keeps button enabled + 旁注 (test). Payload: `buildAssembly` = `{ text, usedFactCodes, ragCorpusFingerprint, unaddressed }`; matches c4's adopt split `ragShape = Array.isArray(assembly.usedFactCodes)` (app.js:10523, branch consumes ragFactCodes/ragCorpusFingerprint, skips preflight) — app.js unchanged so c4 chain auto-engages. Requests: only `COMPOSE_PATH=/api/rag-reply/compose` (+ read-only GET `/api/reply-snippets` for frame selects, non-generating); `grep 'trust-reply/workbench'` EMPTY in new js AND `src/test/js/`. S-1: added CSS only *uses* `var(--verbatim*)`; zero redefinition of tokens/`.rag-badge` in diff. S-2..S-4: python compare of plan CSS fences (S-2+S-3+S-4) vs styles.css appended block = EQUAL whitespace-normalized (2 whitespace bytes delta = inter-block blank lines); `.trust-reply-send` contains literal `rgba(255, 255, 255, .96)` + `backdrop-filter: blur(8px)`, NO `var(--panel-bg)` (test asserts too). S-5: diff hunks = exactly 4 deletions (autorun/-hint/preanalysis/autofilled @5516; gate-list/-item/:empty @5598; toolbar `.ai-reply-model-row` 4 descendant rules @7465, container rule byte-identical pre/post; factset* @7542) + EOF append of new classes; preserved blocks `.trust-reply-workbench`, `.trust-reply-busy-*`, `.trust-reply-readonly*`, `.trust-reply-mode-note{::before}` have no hunk (pre/post counts: 22 base lines → 17 final = minus the 5 toolbar model-row selector lines only); residue greps `trust-reply-factset/-autorun/-gate-list/-preanalysis/-autofilled` in static+test EMPTY. G-5: index.html keys (lines 11/2093/2094) all `20260902-rag-workbench`; all 7 pinning test files carry the single value, no `20260902-rag-knowledge-base`/`-monitoring-window` residue in src/. G-7: spot-checked deleted files assert removed surface only — autoRunOrchestration: `/assemble`+`/state` orchestration, `data-action="auto-run"`, `.trust-reply-autorun{`/`-autofilled` CSS; aiReplyLoadingFeedback: `attempt-timeout`/`total-timeout`/`raw-preview` data-roles; trustReplyWorkbench: 逐问处理 tabs / 处理方式; ThreeStep: `/rearrange` + item locks; overlayAndDialogContrast rewritten to keep only CSS-contract cases on blocks outside the S-5 disposal table (busy rules are 保留不改). G-8: ragWorkbenchRender test #1 asserts `trust-reply-workbench.js?v=` present in index.html source + 3 keys one value. Old endpoint greps empty; all compose hits in repo = RagReplyController `/api/rag-reply` + `@PostMapping("/compose")`. |
| **3. Required commands (fresh, worktree root, JDK 11)** | PASS | `node --test ragWorkbenchRender.test.js` rc0 tests 7 pass 7 fail 0; `node --test trustReplyWorkbenchSharedMount.test.js` rc0 tests 5 pass 5 fail 0; `node --test autoPreviewWorkbenchHost.test.js` rc0 tests 5 pass 5 fail 0; `node --test overlayAndDialogContrast.test.js` rc0 tests 5 pass 5 fail 0; `node --test src/test/js/*.test.js` rc0 **tests 641 pass 641 fail 0**; `node --check trust-reply-workbench.js` + `app.js` clean; `mvn test` rc0 **Tests run: 3108, Failures: 0, Errors: 0, Skipped: 8** (fresh surefire aggregate), BUILD SUCCESS; `mvn clean package` rc0, BUILD SUCCESS, WAR `target/weibo-talent-introduction-1.0.0-SNAPSHOT.war` (46.9 MB), 3108/0/0/8; `git diff db89054..60efcba -- app.js` EMPTY; `git diff --check db89054..60efcba` clean (rc0); `grep trust-reply/workbench src/test/js/` empty; residue greps empty. Implementer's claimed numbers reproduced exactly. |
| **4. Downstream interfaces (c8)** | PASS | `window.TrustReplyWorkbench.mount/unmount` contract intact (app.js hosts unchanged, diff empty). `onComplete` payload shape matches c4 adopt split exactly (usedFactCodes array → rag branch; no `canonicalFactIds` produced). Old server endpoints NOT retired: `TrustReplyWorkbenchController.kt:45 @RequestMapping("/api/trust-reply/workbench")` still present (c8 scope); `RagReplyController.kt` `/api/rag-reply` + `/compose` = the only compose target. Frame-snippet data path untouched (backend zero diff). |

### AUTO_FIX
N/A — no four-gate violation found.

### RECORD_ONLY
- O-1: Plan observable-outcome #2 mentions a `fact_code` 角标 on verbatim paragraphs; implementation renders a 「逐字」tag via `.trust-reply-para-tag` instead (execution.md deviation #4: `RagBodyParagraph` carries text+renderMode only, and I-26 forbids text-guessing). No acceptance criterion or invariant (I-26 rule text, A-1..A-9) asserts the per-paragraph fact code; CSS contract + I-26 fully satisfied. No action.
- O-2: styles.css appended S-2..S-4 block vs plan code fences: content-equal whitespace-normalized; only inter-block blank-line bytes differ (2 bytes). Test's byte-identical CSS constants pass against styles.css. No action.
- O-3 (verifier process note): two early `mvn` invocations lacked explicit `cwd` and landed in the sibling main checkout (`/Users/lukai/IdeaProjects/weibo-talent-introduction`, HEAD 8e71405 — a different tree without c6); results discarded. All tabulated gate commands above were re-run fresh in the worktree root (pwd verified); only those results count.

### Required Action
- COMPLETE_CHILD
