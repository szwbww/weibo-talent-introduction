## Light Verification: LIGHT_PASS_WITH_NOTES

Child: c1 — docs/plans/2026-09-02/01-rag-knowledge-base-schema.md (V112 五张表 + 45 条种子 + 快照与指纹)
Plan identity: commit 92b0519a18a3a46989f8733259af4649f7748a72 (A1/A2 amended; `git diff 92b0519 -- <plan>` empty — verified)
Master plan: docs/plans/2026-09-02/00-execution-order.md (G-1..G-4, G-9; D-1..D-4 decisions table)
Boundary: bbf08287d91bd7a540401bfe71c8dc8baecd34f3..acb88c1e77d172a7f252690b1da1203f08c01817 (implementation commit `feat(fast-p): implement c1`; interleaved docs-only 46cc5c4/92b0519 excluded)
Verifier: VerifierC1
Worktree: /Users/lukai/IdeaProjects/weibo-talent-introduction-fast-2026-09-02-execution-order (branch `fast/2026-09-02-execution-order`, HEAD == acb88c1e77d172a7f252690b1da1203f08c01817, bbf0828 ancestor — all confirmed)

### Four Gates

| Gate | Result | Evidence |
|---|---|---|
| 1. Authorized scope | PASS | `git show acb88c1 --name-only` = exactly the plan's 11 authorized files (10 of `## 变更文件清单` + A2 file #11 `FlywayMigrationIntegrationTest.kt`): export_rag_kb_sql.py, V112__create_rag_knowledge_base.sql, RagFact.kt, RagRetrievalRule.kt, RagFactRepository.kt, RagRetrievalRuleRepository.kt, RagKnowledgeBase.kt, RagProperties.kt, application.yml, RagKnowledgeBaseTest.kt, FlywayMigrationIntegrationTest.kt. `git diff bbf0828..acb88c1 --name-only` adds only seed/plan content from 46cc5c4/92b0519 (docs/mockups ×3, docs/plans/2026-09-02 ×8, scripts/spike_deepseek_reply.py). Nothing else touched: no qa_rule/qa_category, no V1..V111 migration, no other source/test. `git status`: only untracked `docs/plans/fast/**` evidence dir. Plan identity diff empty; HEAD == acb88c1; V112 is top of chain (V1..V112). |
| 2. Plan requirements & invariants | PASS | See evidence below (I-1..I-6, G-1..G-4, G-9, D-3, D-4, 4 exclusion rows, fingerprint dual-implementation, acceptance test coverage, G-4 grep). |
| 3. Required commands | PASS (1 env-blocked, pre-classified) | Fresh JDK-11 runs: `mvn test` exit 0 **3063/0/0/6** BUILD SUCCESS (baseline @seed 46cc5c4: 3062/0/0/5 → +1 gated class); `mvn test -Dtest=RagKnowledgeBaseTest` (no property) exit 0, class skipped (Tests run 1, Skipped 1 — `@EnabledIfSystemProperty(migrationIt)` per repo convention); `mvn clean package` exit 0 **3063/0/0/6**, WAR produced; `python3 spike_deepseek_reply.py --dump-kb \| python3 -c …` → **45**; `python3 export_rag_kb_sql.py \| grep -o "fingerprint[^;]*"` → **fingerprint e62421a42c432cf3** (script exit 0); fresh export output byte-identical to V112 machine seed section (33988/33988 bytes); `git diff --check` clean exit 0. `mvn test -Dtest=FlywayMigrationIntegrationTest -DmigrationIt=true` exit 1, **Tests run: 11, Failures: 0, Errors: 10, Skipped: 0** — all 10 = `V82 baseline drift: audited legacy QA rules changed` (SQLSTATE 45000 gate in pre-existing V82, fails before V112 executes). EXACTLY matches execution.md command #3 and the base reproduction at 92b0519 without V112 (execution.md §偏离 2). Per controller ruling this is the accepted pre-existing environment defect, NOT an implementation failure; genuine V112 verification was performed on the scratch patched chain (V1..V81 + row alignment + V82..V112): execution.md §偏离 3 records RagKnowledgeBaseTest **10/10 green** + full row assertions (45 facts, fingerprint e62421a42c432cf3, fact_count 45, phrase 87 / coverage 21 / mandatory 6 / exclusion 4, enabled=0 exactly 1, VERBATIM 7); containers/worktrees since cleaned. node --test baseline 804 pass / 0 fail unchanged scope (no JS files in c1). |
| 4. Downstream interfaces | PASS | V112 defines all five `rag_*` tables + `rag_kb_meta` singleton (`CHECK (id=1)`) seeded `(1, 'e62421a42c432cf3', 45, NOW())`; IT/scratch-chain asserts single meta row fingerprint=e62421a42c432cf3 fact_count=45. `RagKnowledgeBase` API: `snapshot()` (immutable `RagCorpusSnapshot`, @Volatile), `verifyAndPublish()` (@PostConstruct), `@Transactional republish(writeInTx): String`, `enabledFacts()` 44 rows excluding KB-APP-017 (asserted in test); extra `fingerprint()` (03b I-41 consumer). `RagProperties` defaults + application.yml `talent-introduction.rag:` block all 10 values exactly as downstream spec: prefilter-limit 18, retrieval-limit 14, min-lexical-score 2, coverage-weight 100.0, phrase-weight 12.0, overlap-weight 1.0, retrieval-temperature 0.0, generation-temperature 0.2, retrieval-max-tokens 900, generation-max-tokens 2600 (env-overridable; bound via kebab-case). Phrase/intent counts self-consistent 87/21 across V112 seed, export script, IT assertions and test. |

Gate-2 detail evidence (direct code/test inspection):
- **I-1**: `RagKnowledgeBase.validateFactCodes` on every load — regex `^KB-([A-Z]+)-(\d{3})$` + area/seq self-consistency + uniqueness; V112 `UNIQUE KEY uk_rag_fact_code`; test `45 fact codes are unique and self-consistent with area and seq - I-1`.
- **I-2**: `RagFact.effectiveStatus()` = `if (enabled) status else "DISABLED"`; `enabledFacts()` filters enabled+non-DISABLED; seed has exactly 1 disabled fact (KB-APP-017, enabled='0'); test asserts 1 disabled, KB-APP-017, DISABLED, enabledFacts == 44 and no KB-APP-017.
- **I-3 / P0-3**: two distinct entry points, never merged — `verifyAndPublish()` @PostConstruct, read-only (load → compare vs `rag_kb_meta` → ISE with expected+actual fingerprints in message → publish; no meta write); `republish(writeInTx)` @Transactional: writeInTx → load → `UPDATE rag_kb_meta SET fingerprint/fact_count` → afterCommit publish, **no old-fingerprint comparison**; returns new fingerprint. Tests: drift → verifyAndPublish throws with both hex values in message (`contains("expected e62421a42c432cf3")` + `contains("computed <other>")`); **P0-3 case** — republish { change answer } → no throw, meta updated to new fingerprint, then `verifyAndPublish()` passes.
- **I-3b**: `TransactionSynchronizationManager.registerSynchronization(afterCommit)` publishes only after commit; test forces writeInTx RuntimeException → rollback → `assertSame(before, snapshot())`, meta fingerprint/fact_count unchanged, verifyAndPublish passes.
- **I-4/I-5**: `splitList` per separator (`|` variants/keywords, `,` coverage_keys/source_refs, trim + drop empty — isomorph of script `item.strip()`); `retrievalText` = title | variants | keywords | coverage_keys | answer joined `" | "` (script order, duplicates kept). KB-GOV-004 test asserts 6 variants / 4 coverage keys / 3 source refs, first variant literal `responsible government organization`, retrievalText starts `项目组织层级 | responsible government organization | `, ends with answer, phrase occurs twice.
- **I-6**: `@Volatile` immutable `RagCorpusSnapshot`, whole-replacement publish; test `snapshot is stable until a successful republish - I-6` assertSame twice then assertNotSame after republish.
- **G-1**: fact_code is the only surfaced business key; auto-increment id appears only as repository `@Id`/PK — never in any outward path (no consumers exist yet by design).
- **G-2 (A1)**: constant `e62421a42c432cf3` everywhere (V112 meta seed, export FINGERPRINT + runtime guard `!= constant → fatal`, test constant). Canonical algorithm identical in Kotlin `fingerprintOf` and Python `_corpus_fingerprint`: fact_code-ascending rows; 17 V112 data columns (CREATE TABLE order, excluding id/created_at/updated_at/updated_by) joined `|` (legacy_rule_id NULL → empty string, enabled → 1/0, seq/sort_order plain ints, strings unquoted); rows joined `\n`; SHA-256 hex[:16]. Column lists byte-for-byte congruent (verified by inspection of both implementations); cross-validated on real MySQL (scratch chain): computed == e62421a42c432cf3 == V112 constant, and export script independently reproduces the constant (exit 0).
- **G-3**: only `answer` can be outward text; `title` never enters any prompt/outward path in c1 (no consumers/prompts exist in this child); retrievalText includes title by design per master G-3 layered rule.
- **G-4**: exact gate grep `qa_rule|QaRule` under `src/main/kotlin/com/weibo/talentintroduction/rag/` → **no matches**. Only `legacyRuleId` field/comment (read-only reconciliation data, allowed) and table-column comments. No rag_* callers anywhere in main.
- **G-9**: migration file is `V112__create_rag_knowledge_base.sql`, first of the four deploy-ordered migrations (V113..V115 not yet present); FlywayMigrationIntegrationTest pins targetSchemaVersion **112** in 5 spots (A2-authorized); header comment records deploy order.
- **D-3**: `rag_mandatory_rule` 6 rows (10/15/20/30/40/50); sort_order 15 = COMPENSATION → KB-FUND-033 in V112 seed, `_MANDATORY_ROWS`, repository `loadAll` ORDER BY sort_order, `RagRetrievalRuleData.init` require (6 rows + row-15 COMPENSATION), and test `mandatory rules hold 6 rows with D-3 COMPENSATION at sort order 15` (also asserts every referenced fact_code exists in corpus).
- **D-4**: exactly 45 facts, machine-generated exclusively from `spike.RAG_KNOWLEDGE_BASE` (dump-kb len 45; seed == export byte-identical; 33 non-null legacy_rule_id values match plan audit set — no fact carries legacy 14/43/45/46; no `confidentiality.research` addition). Seed script main() hard-fails if `len(fact_rows) != 45`.
- **rag_prefilter_exclusion**: 4 rows (T2 heading "（3 行）" is the documented typo; brief/A-1 = 4), targets finance.government_funding / finance.additional_support (COVERAGE_KEY) and KB-FUND-034 / KB-FUND-035 (FACT_CODE); asserted by IT and consistent in export.
- **Acceptance test coverage (T6)**: 10 test methods covering every acceptance bullet — 45 rows + fingerprint, I-1 uniqueness/format, VERBATIM==7, disabled==1 + effectiveStatus DISABLED + enabledFacts 44, drift ISE w/ both fingerprints (A-2 step-3 semantics), P0-3 republish-then-verify, I-3b rollback, I-6 identity (assertSame/assertNotSame), I-4 KB-GOV-004 parse, I-5 retrievalText layout, D-3 row-15. Each case restores seed state in finally (order-independent); genuine run 10/10 green on real MySQL 8.0.36 scratch patched chain (V1..V112) per execution.md.

### AUTO_FIX
N/A — no proven four-gate violation; the docker IT outcome is classified per controller ruling as the pre-existing environment defect (base reproduction recorded), not an implementation failure.

### RECORD_ONLY
- O-1: `rag_phrase_group` seeds **87 rows** vs plan wording "约 120" (T2/A-1, brief "~120"). Machine-derived count from the actual script sources (`_XXX_PHRASES` + 12 `_INTENT_COVERAGE` groups incl. inline named + COMPENSATION + POSITIVE_INTENT/NEXT_STEP/COMPENSATION_MENTION/GOVERNMENT_FUNDING_MENTION matcher groups); plan "约" is approximate; consistent across V112/export/IT/test; documented in execution.md §偏离 5. No hand-copied content — machine-generation invariant intact.
- O-2: `RagKnowledgeBaseTest` class-level `@EnabledIfSystemProperty(migrationIt)` + external-DB mode (`RAG_KB_TEST_DB_URL`) go beyond plan T6 text; controller-ruled implementation detail (execution.md §偏离 1) so plain `mvn test`/`clean package` stay Docker-free per repo convention; genuine semantic run performed on scratch patched chain.
- O-3: V112 `rag_fact.source_refs` TEXT has no `DEFAULT ''` (MySQL 8 forbids TEXT literal defaults); documented in column comment + execution.md §偏离 4; seed always supplies values — no behavior gap vs T2 intent.
- O-4: `rag_fact.sort_order` seeded as fact_code-ascending ordinal 1..45 (makes Kotlin DB-read order equal Python enumeration; fingerprintOf sorts by fact_code regardless). Plan specified the column but not seed semantics; no observable divergence.
- O-5: environment note (not repo): verification attempts 1-3 of the Flyway IT could not start containers — docker-java (testcontainers 1.19.8 / docker-java 3.3.6) pins API 1.32 while the OrbStack daemon (29.4.0) enforces MinAPIVersion 1.40; worked around with `-Dapi.version=1.40 -Ddocker.client.strategy=…EnvironmentAndSystemPropertyClientProviderStrategy -Ddocker.host=unix://…/.orbstack/run/docker.sock` (no repo change; `~/.testcontainers.properties` backed up and restored). With containers reachable the defect reproduces verbatim. Explains why implementer runs needed the same kind of environment accommodation.
- O-6: `COMPENSATION_MENTION`/`GOVERNMENT_FUNDING_MENTION` single-phrase groups (+ POSITIVE/NEXT_STEP) added as phrase groups beyond T2's literal phrase-source list; they back this plan's own exclusion-rule group codes for downstream 02 (I-8③/I-12) — execution.md §偏离 5; consistent across all four representations.
- O-7: `republish()` also exposes `fingerprint()` and returns the new fingerprint (plan T4 requires return; fingerprint() serves 03b I-41 freshness compare) — additive, within authorized file.

### Required Action
- COMPLETE_CHILD
