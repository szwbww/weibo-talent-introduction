## Light Verification: LIGHT_PASS_WITH_NOTES
Child: 03b-source-version-split (docs/plans/2026-08-19/workbench-repair-03b-source-version-split.md)
Boundary: e2ad440157017fb6ced066fe63ad2d5e104a8296..8ee03a9b207227890bca01da272207ff9a22f943
Verifier: Ver03bSourceVersionSplit

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
|1 Authorized scope|PASS|git show 8ee03a9 touches exactly the 8 authorized files (AiReplyContextService.kt, TrustReplyWorkbenchService.kt, trust-reply-workbench.js, AiReplyContextServiceTest.kt, TrustReplyWorkbenchServiceTest.kt, TrustReplyWorkbenchItemFlowTest.kt, PendingMailOperationServiceTrustWorkbenchTest.kt, trustReplyWorkbenchSharedMount.test.js); no new files; zero diff on all prohibited files (AiReplyContextBuilder.kt, AiTrainingQaService.kt, AiReplyIntentCatalog.kt, GroundedAutoReplyDecisionService.kt, AiTrainingController.kt, UnmatchedInboundMailController.kt, PendingMailOperationService.kt, app.js, db/migration/*, styles.css, index.html). Note: boundary e2ad440..8ee03a9 also contains intermediate docs-only commit d290a1d (03a child records + ledger.md, no product code) — expected fast-p process, not part of the implementation commit. Working tree product files clean (only docs/plans untracked/modified).|
|2 Plan requirements and invariants|PASS|I-1: sourceVersion() (TrustReplyWorkbenchService.kt:1564-1580) has exactly 7 identity components (sourceType.name, sourceId, contactId, messageId, subject, senderAccountCode, sha256Hex(inboundText)); sha256Hex(mailHistory)/sha256Hex(profileText) appear nowhere in sourceVersion() — the only file hit is line 1589 inside the new contextVersion(), which is verbatim plan-mandated T3 text; contextVersion(trainingKnowledgeText, mailHistory) exists (1588-1589). I-2: researchEvidence (sha256Hex(expertProfileText) + " " + researchProfileSufficient) passed ONLY for requiresResearchContext==true items in resolveCanonicalSelection (:1650-1660), fullDraft branch (:995-1008) and materializeVersions (:1774-1780); null keeps the exact 03a 3-component hash input, asserted byte-identical by test. I-3: both content hash and boolean mixed (unit test: boolean constant true, content change still moves research item's version). I-4: JS renders verbatim per-item `<span class="muted" data-role="item-context-stale">` and status-area verbatim button `<button ... data-action="regenerate-context-stale">` only when >=1 stale; click handler regenerates via shared runItemSequence (skipResolved:false), no bootstrap/resetVersions/handleStaleGeneration in the regeneration path — JS test asserts stream count == stale count, 0 re-bootstrap. I-5: AiReplyContext gains expertProfileText/trainingKnowledgeText with defaults; profileText construction semantically unchanged (extract-to-local only, verified in AiReplyContextService.build:44-49); AiReplyContextBuilder untouched. I-6: contextVersion absent from bodies of requestKey (2019-2031), versionId (2034-2051), requestEvidenceVersion (2070-2093), aggregateEvidenceVersion (2098-2099) — all hits are data-class fields/response wiring/observational. must-NOT-change: AiReplyIntentCatalog.kt:567 and :705 MISSING gates intact (file diff 0 lines; AiReplyIntentCatalogTest 35/35 green); identity changes still flip all requestKeys (7-component sourceVersion + regression test asserting STALE whole-state); 03a per-request semantics hold (03a tests green in full run).|
|3 Required commands|PASS|All run freshly (JDK 11 zulu-11; surefire 2.22.2 requires comma-form -Dtest, plan's '+' syntax rejected — recorded deviation): (1) mvn test -Dtest='AiReplyContextServiceTest,TrustReplyWorkbenchServiceTest,TrustReplyWorkbenchItemFlowTest,PendingMailOperationServiceTrustWorkbenchTest' → 36+54+35+30=155 tests, 0 failures; (2) guard tests -Dtest='AiReplyIntentCatalogTest,GroundedAutoReplyDecisionServiceTest,AiTrainingQaServiceTest' (all 3 classes exist; GroundedAutoReplyDecisionServiceTest found at mail/service/) → 35+17+5=57 tests, 0 failures; (3) node --test src/test/js/trustReplyWorkbenchSharedMount.test.js → 55 pass, fail 0; (4) node --test src/test/js/*.test.js → 663 pass, fail 0; (5) node --check trust-reply-workbench.js → OK; (6) full mvn test → Tests run 2611, Failures 0, Errors 0, Skipped 4 (all 4 skipped are pre-existing @Disabled in untouched integration classes: OperatorActionLogRepositoryTest, AuthFlowIntegrationTest, FlywayMigrationIntegrationTest, EuropePmcDataSourceTest), BUILD SUCCESS; baseline 2603 → +8 exactly matches 03b's added tests; (7) mvn clean package → BUILD SUCCESS, WAR produced; (8) git diff --check → exit 0, no output.|
|4 Downstream interfaces|PASS|03a framework fully functional with new researchEvidence param: requireCurrentEvidenceVersion exactly 3 lines (495 saveState, 1069 adjustItem, 1743 definition; no assemble call); evidenceSetVersionWithMapping gone (repo-wide grep empty); resetVersions() functional call/definition sites exactly 3 (377 handleStaleGeneration, 404 definition, 659 bootstrap non-preserve branch) and changeRequestFacts contains no call. 03b acceptance tests for I-1a/b/c, I-2/I-3, must-NOT-change 1, T5/I-6 (PendingMailOperationServiceTrustWorkbenchTest older-context assembly still sends) all present and green.|

### AUTO_FIX
- N/A (no proven gate violation; no plan-uniquely-determined repair required)

### RECORD_ONLY
- O-1: 03b plan's I-1 acceptance grep "sha256Hex(mailHistory)|sha256Hex(profileText) 无输出" textually has 1 hit at TrustReplyWorkbenchService.kt:1589, inside the new contextVersion() — this is verbatim plan T3-mandated code (contextVersion must hash trainingKnowledgeText + mailHistory), so the substance of I-1 (both moved out of sourceVersion()) holds; acceptance-criterion wording is stale relative to T3, not an implementation defect.
- O-2: 03a acceptance grep "resetVersions() 恰 3 行" now returns 4 textual lines because 03b's comment at trust-reply-workbench.js:1039 mentions "resetVersions()/handleStaleGeneration are never invoked" — comment-only hit; the 3 functional sites and the no-call-in-changeRequestFacts invariant are unchanged, so this is cosmetic grep drift, not a functional regression.

### Required Action
- COMPLETE_CHILD
