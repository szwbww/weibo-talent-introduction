## Light Verification: LIGHT_PASS_WITH_NOTES
Child: 03a-per-request-evidence-version — docs/plans/2026-08-19/workbench-repair-03a-per-request-evidence-version.md
Boundary: 055d313d250053d7cbd917884745571b9580b9b4..e2ad440157017fb6ced066fe63ad2d5e104a8296
Verifier: Ver03aPerRequestEvidenceVersion

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
|1 Authorized scope|PASS|`git show e2ad440 --stat` changes exactly the 9 authorized files (266/19/181/181/42/399/211/37/44 lines; 1196+/184-; no new files). Boundary `git diff --name-only 055d313..e2ad440` shows only those 9 plus 5 docs files from the prior child-02 docs commit 0f73945 (not this implementation). Prohibited set (TrustReplyWorkbenchController.kt, UnmatchedInboundMailController.kt, app.js, AiReplyDraftService.kt, AiReplyReviewAuditService.kt, AiTrainingEvaluationService.kt, UnsupportedAnswerIndexService.kt, AiReplyHighRiskClaimValidator.kt, AiReplyPointByPointComposer.kt, db/migration/*, styles.css, index.html): zero hits in boundary diff (`git diff --stat -- '*styles.css' '*index.html' ... '*.sql'` empty; db/migration count = 0). Working tree `src/` clean vs e2ad440 (`git diff e2ad440 -- src/` empty); only unstaged/untracked items are docs/plans/fast machinery (ledger.md M, 03a/03b child dirs), outside the boundary.|
|2 Plan requirements and invariants|PASS|I-1: `requestEvidenceVersion(requestKey, factRuleIds, baseSnapshotOf)` at service :1980-1997 — inputs exactly requestKey + ordered factRuleIds + subset-only snapshot (per-request `baseSnapshotOf(factRuleIds)` via C-1 per-subset calls; never sendQaRuleIds union, no observation time). I-2: identity includes requestKey and order (joinToString(",")); test `rebinding a fact invalidates exactly both ends...` asserts ends 1+2 change, end 3 identical, stale lock rejected TRUST_REPLY_EVIDENCE_STALE. I-3: `grep requireCurrentEvidenceVersion` = exactly 3 lines (:473 saveState aggregate, :1025 adjustItem per-request, :1678 def); assemble pre-check deleted server-side (per-item validation at :1127/:1153); `aggregateEvidenceVersion` derived; `grep evidenceSetVersionWithMapping src/` = 0 matches (deleted). I-4: `validateLockedSubset` returns kept/droppedCount; status PARTIALLY_RESTORED when 0<dropped<kept, STALE when all dropped; droppedItemCount in TrustReplySavedState; tests assert PARTIALLY_RESTORED/1-drop and all-dropped STALE. I-5: `bootstrap({preserveVersions})` + capture/reconcile per-item; changeRequestFacts calls `bootstrap({preserveVersions:true})` and no resetVersions(); `grep resetVersions()` = exactly 3 hit lines (:373 handleStaleGeneration, :400 def, :644 non-preserve branch); JS test asserts exactly 1 new /bootstrap and untouched item keeps lock. I-6: SCHEMA_VERSION v4 / PREVIOUS v3 / LEGACY v1 / ACCEPTED {v4,v3,v1} (v2 removed); v3 decodes verbatim and restoreSavedStateWithFrame judges whole STALE (never per-item); v2 decodes null → INVALID; tests cover. I-7: db/migration diff = 0 files. S-1: verbatim `<span class="muted" data-role="item-evidence-stale">事实已变化，本条回答需重新生成</span>` at JS :1732, output only when `request.evidenceStale === true`; no new class, no inline style, no styles.css/index.html change. must-NOT-change: TRUST_REPLY_FACT_ALREADY_ASSIGNED preserved (:1581); evidenceSetVersion still aggregate fingerprint in bootstrap/assemble/saveState payloads and audit/evaluation path files untouched (git diff empty for those files); fact order participates (I-1 test (b)); FULL_DRAFT buildInitialItemVersions evidence source unchanged (still buildEvidenceSnapshotForSelection; per-request partitioning is plan T3's own mandate, C-6 kept as observation). Research checkpoint grep `evidenceSetVersionWithMapping --include=*.kt src/main src/test` = no callers (function deleted per T1).|
|3 Required commands (fresh)|PASS|All run fresh on this worktree, JDK 11 zulu-11, node v22.23.2: (1) targeted Kotlin (comma form, see deviation note): exit 0, `Tests run: 147, Failures: 0, Errors: 0`, BUILD SUCCESS; (2) `node --test src/test/js/trustReplyWorkbenchSharedMount.test.js`: exit 0, 54 pass / 0 fail; (3) `node --test src/test/js/*.test.js`: exit 0, 662 pass / 0 fail; (4) `node --check src/main/resources/static/trust-reply-workbench.js`: exit 0; (5) full `mvn test`: exit 0, `Tests run: 2603, Failures: 0, Errors: 0, Skipped: 4` (baseline 2594/0 → +9 new tests, 0 failures), node-test execution 662 pass / 0 fail, BUILD SUCCESS; (6) `mvn clean package`: exit 0, BUILD SUCCESS; (7) `git diff --check 055d313..e2ad440`: exit 0, no output. Infra deviation recorded: surefire 2.22.2 rejects the plan's literal `+`-joined class list ('No tests were executed!'); the comma-separated equivalent `-Dtest='TrustReplyWorkbenchServiceTest,TrustReplyWorkbenchItemFlowTest,TrustReplyWorkbenchStateStoreTest,TrustReplyWorkbenchControllerTest,PendingMailOperationServiceTrustWorkbenchTest'` selects the identical class set and was used.|
|4 03b downstream interface|PASS|`requestEvidenceVersion(requestKey: String, factRuleIds: List<Long>, baseSnapshotOf: (List<Long>) -> String): String` and `aggregateEvidenceVersion(perRequestByIndex: List<Pair<Int, String>>): String` exist exactly per plan T1 (service companion). `TrustReplyRequestCoverage.evidenceSetVersion: String = ""` per-item field appended (T3). Bootstrap coverage carries per-item values: `toCoverage(sourceVersion, requestEvidenceVersions)` fills each item's value from the per-request map; controller returns service bootstrap directly (no DTO conversion, C-4). `data-role="item-evidence-stale"` span present verbatim in JS.|

### AUTO_FIX
- N/A — no gate violation proven; every enumerated invariant/acceptance check passes and all required commands exit 0.

### RECORD_ONLY
- O-1: Plan T6 (and change-list description "assemble 不再收 expectedEvidenceSetVersion" for TrustReplyWorkbenchControllerTest) requires deleting `expectedEvidenceSetVersion` from the JS assemble payload; the implementation keeps the field on the wire with an explanatory comment (JS :1133-1141) and its controller test instead asserts "assemble forwards arbitrary expected evidence version without validating it" (pass-through). Root cause: the plan contradicts itself — C-4 freezes the controller DTO, and `TrustReplyAssembleHttpRequest.expectedEvidenceSetVersion` is non-null without a default at base 055d313 (and still is), so literal T6 execution would make Jackson fail to deserialize every assemble request (400) unless a prohibited file is touched. The implementation is the only executable reading and fully satisfies I-3's observable intent (server-side whole-draft pre-check deleted — grep shows no assemble caller; per-item validation; `changing one request facts does not block assembling the unchanged requests` test passes; stale expectedEvidenceSetVersion is ignored, covered by ItemFlow + controller + mail-service tests). Not AUTO_FIX: the plan does not uniquely determine the repair for this item.
- O-2: v1 saved states carrying locked items now restore as whole STALE (pre-change: RESTORED when the aggregate fingerprint matched), because the legacy aggregate value can never equal a fresh per-request value, so every lock is dropped. The implementation documents this in tests (`bootstrap marks v1 saved locks stale under per request semantics`) and it is consistent with I-6's violation-consequence ("不得静默错配…locked item 可能被错误接受") and manual acceptance A-7 (pre-deployment drafts explicitly invalidated, no silent accept); noted because the plan's "v1 保持既有 legacy 处理" phrasing could be read as preserving v1 restores.

### Required Action
- COMPLETE_CHILD
