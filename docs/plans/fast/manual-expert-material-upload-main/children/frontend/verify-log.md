## Light Verification: LIGHT_PASS_WITH_NOTES
Child: frontend (docs/plans/2026-09-20/manual-expert-material-upload-frontend.md)
Boundary: 80beb2bddfc77f8f65f8c51446c9a6c14f2e10df..5f4967b8d5f94663266c095e6a2e9ec69f570505
Verifier: FrontendLightVerifier
Epoch: 1  Attempt: 1  Timestamp: 2026-09-20T07:38:27Z

### Four Gates
| Gate | Result | Evidence |
|---|---|---|
| Authorized scope | PASS | `git show --stat 5f4967b` = exactly the brief's 6 paths and nothing else: `expert-materials.js` +389/-18, `index.html` 22, `styles.css` +232/-0, `expertMaterialsShared.test.js` +484, `expertMaterialsStyle.test.js` +340/-2, `sharepointFileCardDisplay.test.js` 12; no `docs/plans/**`, no Kotlin/backend path, no new file. Boundary range also contains only the controller's `c591668 docs(fast-p)…` commit (backend child evidence), not part of the implementation commit. `git status --short` = clean worktree + untracked child evidence dir only. |
| Plan and invariants | PASS | I-1 `expert-materials.js:491-498` (FormData 单 `file` 段 + `headers:{}`) / `:634-658` `for…of+await` 串行; I-2 `:53` `MAX_MANUAL_MATERIAL_BYTES=100*1024*1024`, `:463-465` `size > MAX` → `blocked` + `file=null`, 上传目标仅 `pending/failed` (`:620`); I-3 `:666-667` `store.page=0` + `await fetchPage(store,{page:0,reason:"manual-upload"})`, 无 `store.items`/`pageOrder` 写入（`grep -n "store.items\s*=\|items.unshift\|items.push"` = 无命中）; I-4 状态机 `:463-476`、上传中 `input/cancel/close/start/remove` disabled + `:728` `cancel` 事件 `preventDefault()`; I-5 `:945,952` 全部来源/单一“手动上传”选项, `:987` 行来源三段式; I-6 `:782-800` 动作组仅 `mode!=='selectionOnly'`, inline 保留 AI、drawer 保留关闭, `:1391-1393` `manual-upload` 由组件消费; I-7 `index.html` 11 处 `?v=` 全部 = `20260920-manual-material-upload`（`grep -o '?v=…' | uniq -c` = 11 同一值），5 CSS + 6 JS 顺序与 base 逐一对照不变。S-1/S-2 CSS：自计划 `\`\`\`css` 围栏抽取的 S-1(282B)/S-2(4216B) 两段在 `styles.css` 中逐字存在（字节级比对：`head_css == head_css[:mi]+head_css[nj:]` 之外零差异，即除插入段外与 base 字节相同），S-2 紧随 S-1；`styles.css` diff 232 插入 / 0 删除（`grep -c '^-[^-]'` = 0），`.button/.button.primary/.button.secondary/.button.small/.modal-close-btn` 既有规则逐一仍在→共享规则未被就地修改；S-3 policy 文案、`aria-label="材料来源"`、`全部来源` 在 `:833,945` 与计划逐字一致；`src/main/resources/static/expert-materials.css` 在边界内 numstat 为空（字节未变）。验收/定向命令证据：shared 新 5 suite（串行 `maxConcurrentUploads===1`/`partNames===["file"]`/`headers` 深等 `{}`、104857600 提交 vs 104857601 零请求、部分失败继续+仅重试 failed、page=0 单次 GET+保留 q/state/selection、MANUAL_UPLOAD 行与成对 query），style 新 suite（计划 CSS 逐字、入口/selectionOnly、dialog 结构/无 id/无 inline style、S-3），全部通过。 |
| Required commands | PASS | ① `node --check src/main/resources/static/expert-materials.js` → exit 0。② `node --test …expertMaterialsShared.test.js …expertMaterialsStyle.test.js …sharepointFileCardDisplay.test.js` → exit 0，tests 39 / suites 16 / pass 39 / fail 0。③ `node --test …meetingConfirmationAssets …trustReplyWorkbenchSharedMount …ragKnowledgeBasePage …checkRepliesRelocation …overlayAndDialogContrast` → exit 0，tests 49 / suites 7 / pass 49 / fail 0。④ `git diff --check` → exit 0（边界 diff 亦 exit 0）。⑤ `node --test src/test/js/*.test.js` → exit 1；tests 1035 / suites 207 / pass 1034 / **fail 1** = `meetingCalendar.test.js:188`「styles.css 逐字追加 S-2 CSS 块」，与报告一致。⑥ `JAVA_HOME=…zulu-11… mvn test` → exit 1，唯一失败步骤 `exec-maven-plugin:exec (node-test)`（即上一条），Java surefire 汇总 `target/surefire-reports/*.txt`（250 文件）= **3503 tests / 0 failures / 0 errors / 13 skipped**（绿）。基线独立重放（`git archive 80beb2b | tar -x -C /tmp/fp-base-verify`，在仓库外）→ exit 1，tests 1023 / pass 1006 / **fail 17**，12 个失败 suite；同一 subtest「styles.css 逐字追加 S-2 CSS 块」在基线即以同一 error（`'S-2 CSS 块必须与计划逐字一致'`）失败 → 17→1：16 个缓存键红灯全部消失，剩余 1 个是同一预存红灯。 |
| Downstream interfaces | PASS | `expert-materials.js:492` 路径逐字 `POST /api/expert-contacts/${store.contactId}/materials/uploads`；`:495-496` `method:"POST"` + `headers:{}`（宿主 `app.js:1541-1543` 的 `{headers:{json}, ...options}` 展开次序使 `{}` 整体覆盖 Content-Type，浏览器生成 boundary）；单 `file` 段；失败行渲染 `err.message`（`app.js:1549-1554` 保留服务端 message/status，413 PAYLOAD_TOO_LARGE 文案落 `.em-upload-state`，`:474-476`）；列表仅由共享 store GET 提供（`:667`），无本地插行；后端/契约文件在提交中零改动（gate 1 证据）。 |

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1 新增 CSS 位置（实现者偏差 a）：S-1/S-2 被插在既有末尾块 `/* meeting-mail-07: outbound files */` **之前**，而计划 S-1/S-2 字面写“追加到 `styles.css`”。这不是可自动修复项：把新块放到文件物理末尾会让两条既有绿灯契约转红——`src/test/js/mailboxOutboundAttachments.test.js:2193-2195`（`stylesSource.endsWith(S2_BLOCK)`）与 `src/test/js/materialRequestIntegration.test.js:839`（`assert.ok(!stylesSource.endsWith(s2), "S-2 不得追加到 styles.css 末尾（meeting-mail-07 的块必须仍在最后）")`），二者均不在本子计划 6 文件授权内，且计划 S-1/S-2 自身验收标准只要求“与 `styles.css` 连续块逐字一致”+ 不改既有 selector/不覆盖并行工作，故计划未唯一确定一个授权范围内的修法 → 记为偏差而非违规。
- O-2 余下 1 个红灯（`src/test/js/meetingCalendar.test.js:188/189`）为**基线既有、非本子计划引入**：对 `git show 80beb2b:src/main/resources/static/styles.css` 求值 `includes(briefCss) === false`，漂移点是 `styles.css:11195` 的 `.calendar-dialog{inset:0;margin:auto;…}` 相对 `docs/plans/fast/meeting-mail-master/children/03-calendar-ui/brief.md` 围栏多出 `inset/margin`（并行 meeting-mail 工作），与手动上传无关；`node --test src/test/js/*.test.js` 与 `mvn test` 的退出码 1 均只源于此，Java 侧全绿。
- O-3 偏差 b（测试 stub 增强）与围栏缩进：`expertMaterialsStyle.test.js` 仅新增 `matches/closest/preventDefault/stopPropagation`、FormData stub 与 POST 记录，全部 diff 删除行仅 2 行（`api(url)` 改 `api(url, options)`、sandbox 增加 `FormData`），无既有断言被削弱或改写；S-1/S-2 复制时保留计划围栏的 2 空格列表缩进（去除的仅是闭合围栏行自身的缩进空白），与仓库既有抽取写法 `/```css\n([\s\S]*?)```/` 及本子计划 style test 的抽取口径一致，CSS 语义无差异。

### Required Action
- COMPLETE_CHILD
