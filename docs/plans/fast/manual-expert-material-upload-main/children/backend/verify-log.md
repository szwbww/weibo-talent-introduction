## Light Verification: LIGHT_PASS_WITH_NOTES
Child: backend (docs/plans/2026-09-20/manual-expert-material-upload-backend.md)
Boundary: d2a7f65ecbc46b5165863dfcab94ae5972f50605..80beb2bddfc77f8f65f8c51446c9a6c14f2e10df
Verifier: BackendLightVerifier
Epoch: 1  Attempt: 1  Timestamp: 2026-09-20T07:11:17Z

### Four Gates
| Gate | Result | Evidence |
|---|---|---|
| Authorized scope | PASS | `git diff --name-status d2a7f65..80beb2b` = exactly the brief's 10 paths (5 M + 5 A, +1646/-32); `git show --stat 80beb2b` lists no `docs/plans/**`; `git status --short` = only untracked `?? docs/plans/fast/manual-expert-material-upload-main/` (controller evidence, uncommitted). HEAD=80beb2b, parent=d2a7f65. |
| Plan and invariants | PASS | I-1 V130 three-way owner (`V130...sql:24-42,44-68`) + IT zero/double/triple-owner rejection (`FlywayMigrationIntegrationTest.kt:374-397`); I-2 stream count + atomic move + single `TransactionTemplate` + final cleanup (`ManualExpertMaterialUploadService.kt:78-197`); I-3 `100L*1024*1024` counted per read (`ManualExpertMaterialUploadService.kt:184-186,225`) with parser ceiling 100MB/101MB (`application.yml:24-25`); I-4 no transfer row, `PENDING_REVIEW`, `storageStateOf` real-file STORED (`ExpertMaterialService.kt:524-547`); I-5 left-join + fixed source (`ExpertMaterialService.kt:429-444,810-814,849-857,913-919`) with sortTime=receivedAt (`:403`); I-6 username only from `AuthSessionKeys.USERNAME` (`ExpertMaterialController.kt:82-98`), doc+owner contact both checked (`ExpertMaterialService.kt:107-120`), `MAX_CREATED_BY_CHARS=100` reused (`OutboundAttachmentModels.kt:37`); I-7 no mailbox/status/transfer write touched (grep `MANUAL_UPLOAD\|manualUploadId` outside document/ = only `MailAttachment.kt:30` + V130); I-8 `information_schema.TABLE_CONSTRAINTS + PREPARE` guard (`V130...sql:44-61`). Flow test 13 cases cover 阶段 1-4/3-3 items incl. 0B, exact 104857600, stream/2nd/3rd-write/commit failures, cross-expert refusal, source filter, yml regex, reconcile fixture. Deviations adjudicated (none violate a written requirement): D1 nullable DI (`ExpertMaterialController.kt:37`, `ExpertMaterialService.kt:63`) is the only way to honour I-6's hard file boundary without breaking existing 9-arg/`@WebMvcTest` constructions; D2 400 via `OutboundAttachmentException.badRequest` is required because GEH has only `@ExceptionHandler(Exception)` (`GlobalExceptionHandler.kt:82-83`, no MissingServletRequestPartException handler) which would turn `required=true` into 500, breaking the frozen 400; D3 reusing `attachmentTransferTransactionTemplate` (`AttachmentTransferWorker.kt:730`, REQUIRES_NEW) still satisfies "一个 TransactionTemplate", no outer tx at the call site; D4 defaulted appended `uploadedBy`/4 row fields matches the plan's own "末尾追加 nullable …/null 默认" wording; D5/D6 are environment/follow-up, not plan requirements. |
| Required commands | PASS | 1) `mvn -Dtest=ManualExpertMaterialUploadFlowTest,ExpertMaterialServiceTest,ExpertDocumentBrowseServiceTest,DocumentTextExtractorTest,OperatorStatusReconcileServiceTest,OutboundAttachmentServiceTest test` → exit 1, surefire `Tests run: 107, Failures: 0, Errors: 0, Skipped: 0` (Outbound 28/Flow 13/EMS 33/Extractor 6/Browse 15/Reconcile 12); exit 1 only from the `node-test` exec step. 2) `mvn -DmigrationIt=true -Dtest=FlywayMigrationIntegrationTest test` → exit 1, `Tests run: 1, Errors: 1` = `IllegalStateException: Docker is required for Flyway migration tests` (docker-java API 1.32 < engine min 1.40) — environment blocker, not silently skipped. 2b) workaround `mvn -DmigrationIt=true -DargLine="-Dapi.version=1.43" -DskipNodeTests=true -Dtest=FlywayMigrationIntegrationTest test` → exit 1, IT `Tests run: 25, Failures: 0, Errors: 1`; the single error is pre-existing `V124 allows material attached promotion audit trigger` (FK `fk_eap_contact`), `V130 adds the manual upload owner table switching the owner check to three-way` ran and passed. 3) `git diff --check` → exit 0. 4) `mvn test` → exit 1, surefire aggregate `Tests run: 3503, Failures: 0, Errors: 0, Skipped: 13` (= baseline 3490 + 13 new flow tests), exit 1 only from `node-test` (JS `tests 1023 / pass 1006 / fail 17`, identical to baseline). |
| Downstream interfaces | PASS | Single mapping repo-wide: `@RequestMapping("/api/expert-contacts/{contactId}/materials")` + `@PostMapping("/uploads")` (`ExpertMaterialController.kt:31,81`), part `file` (`:84`), missing-file 400 (`:93`), 201 (`:97`). Fresh run stdout: `FROZEN_201={"attachmentId":2,"documentId":3,"fileName":"cv.pdf","contentType":"application/pdf","fileSize":6,"documentType":"CV","documentStatus":"PENDING_REVIEW","storageState":"STORED"}`; `FROZEN_GET_ITEM={... "source":{"type":"MANUAL_UPLOAD","id":1,"subject":"手动上传","receivedAt":"2026-09-20T14:59:36.293813","accountCode":null,"uploadedBy":"op1"},… "actualSize":6,… "storageState":"STORED",… "canFetch":false,"canDownload":true,…}`; `FROZEN_413={"code":"PAYLOAD_TOO_LARGE","message":"单个材料不能超过 104857600 字节","detail":"Payload Too Large"}` — field-for-field identical to the execution report (only `receivedAt` differs per run). No `storagePath`/absolute root/`.tmp-` in any body (in-test asserts + observed output); upload DTO carries no path field (`ManualExpertMaterialUploadService.kt:32-41`). |

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1 Pre-existing JS red reproduced fresh: `node --test src/test/js/*.test.js` via the `node-test` exec step = 1023 tests / 1006 pass / 17 fail, all `?v=` static-asset cache-key cases; this child changed no frontend file, so it is the baseline red, not a new failure.
- O-2 Pre-existing opt-in IT error: `FlywayMigrationIntegrationTest.V124` fails with FK `fk_eap_contact` (no `expert_contact` row after `clean()`+migrate; `grep "INSERT INTO expert_contact" db/migration` = 0 hits). Its body is unmodified by this diff (only the `"129"→"130"` literal), and baseline never runs it (`migrationIt=false`).
- O-3 The exact documented migration IT command cannot reach Docker on this machine (testcontainers 1.19.8/docker-java client API 1.32 vs engine minimum 1.40). Real V130 IT evidence therefore comes only from the `-DargLine="-Dapi.version=1.43"` workaround; MySQL 5.7 (A-8) was not exercised at all.
- O-4 No live/full-context smoke: every `@SpringBootTest` class is gated behind `migrationIt`/`mysqlIt` (`AuthFlowIntegrationTest.kt:46`, `RagFactAdminServiceTest.kt:45`, `RagKnowledgeBaseTest.kt:45`), and `MailboxConversationControllerTest.kt:99` documents a pre-existing full-context mapping conflict; so production wiring of `ManualExpertMaterialUploadService` + `TransactionTemplate` is exercised by no test — residual risk to be covered by human A-1.
- O-5 Plan 阶段 3-3 also names "移动失败", but `ManualExpertMaterialUploadFlowTest` injects only the stream (write) failure; move failure is safe by construction (`writeStreamedFile` finally deletes `.tmp-*`, DB write happens after the move) yet untested — the enumerated injections in 验收标准 I-2 do not require it.

### Required Action
- COMPLETE_CHILD
