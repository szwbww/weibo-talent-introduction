## Light Verification: LIGHT_PASS_WITH_NOTES
Child: 03-ui — `docs/plans/2026-09-17/03-material-request-ui.md` (brief `docs/plans/fast/material-request/children/03-ui/brief.md`, amended by A2 to 8 authorized files; plan SHA-256 `4fc501af859b17476e80a50505f88f026710bb9ab19e50e329885f321d7db251`, brief `050aedf7b76600ab89dea3777e3bcaef0b3ff8c28c6e795e160827a93451ac48` — both re-read from disk in this round and equal to the epoch-2 record)
Boundary: 4a91a6135c031cc60fd8091b1fe8ae61f77c4a2b..75628fb9ae6201e7aa2c26f3dcb880dde8faa9ab
Verifier: LightVerifier03

### Four Gates
| Gate | Result | Evidence |
|---|---|---|
| 1 — changed product/test files ⊆ authorized 8 | PASS | `git diff --name-status 4a91a61..75628fb -- src` = exactly the brief's eight paths: `static/app.js` (M), `static/index.html` (M), `static/mailbox-chat.js` (M), `static/styles.css` (M), `test/js/contactHeadLayout.test.js` (M), `test/js/mailboxOutboundAttachments.test.js` (M, A2 #7), `test/js/materialRequestIntegration.test.js` (A), `test/js/meetingConfirmationIntegration.test.js` (M, A2 #8). No other `src/` path in the range: `git diff --name-only 4a91a61..75628fb -- src/main/kotlin src/test/kotlin src/main/resources/db src/main/resources/application.yml pom.xml` → 0 lines; `mailbox-chat.css`, `meeting-confirmation.js`, `expert-materials.js` and every other static asset are outside the range (`git diff --name-only 4a91a61..75628fb -- src/main/resources/static/mailbox-chat.css` → 0). Per commit: `ee9cc36` = the six epoch-1 files (`6 files changed, 1474 insertions(+), 62 deletions(-)`), `75628fb` = the two A2 test files (`2 files changed, 6 insertions(+), 5 deletions(-)`; `git diff --numstat dcd5a20..75628fb` = `2/2` and `4/3`), `4f4eaf7` = the plan's `## 修正记录` A2 entry only (7+/1-), `dcd5a20` = pause evidence (brief/execution/ledger). The remaining 6 changed paths in the range are `docs/plans/**` evidence/plan artifacts owned by the controller, not product/test files. |
| 2 — plan requirements + I-1…I-5, S-1/S-2 have direct code/test evidence | PASS | I-1: `app.js:8063` GET `/api/expert-contacts/${contactId}/material-requests` inside `loadContactDetail`; `app.js:8045` PUT `/api/expert-contacts/${contactId}/material-requests/${materialCode}` in `saveExpertMaterialStatus`, row re-rendered from the response at `:8049`; `grep -n '/materials' src/main/resources/static/app.js` → **0 hits** (the only remaining `/materials` consumer in `static/` is the untouched `expert-materials.js` upload component), so the `/materials` paging object can no longer be treated as the five-item array; the row still renders only under `Array.isArray(materials)` (`:8124`) with its own isolated catch and one `材料状态加载失败` status, and successful saves still show no toast (test asserts `showStatus` called 0 times). `renderExpertMaterialRow` (`:7978-8009`) is byte-identical to base (total app.js diff = 2 changed lines). I-2: one GET per open, no cache (`mailbox-chat.js:4405` is the only `hostApi()` call inside the whole material-request block); `renderMaterialRequestOptions:4301-4324` sets `selectable = status === "PENDING"` with `input.checked = selectable` (`:4317`) and `input.disabled = !selectable` (`:4318`) and appends a `<small>` Chinese status label; the preview is the fixed lead (`MATERIAL_REQUEST_LEAD:4218`, `escapeText`ed once in the skeleton) plus an unnumbered `<ul>` whose `<li>`s are the response `requestText` in catalogue order (`renderMaterialRequestPreview:4343-4355`, DOM order = API order); apply is disabled while `selected.length === 0` (`:4359-4360`); cancel (`mc-close-material-request` → `closeMaterialRequestDialog:4457`) issues no request, and confirm (`mc-apply-material-request` → `applyMaterialRequestFromDialog:4426`) appends `<p>` + `<ul><li>` built with `createElement`/`textContent` (`:4438-4448`) and calls only `handleManualComposeInput(inputs.editor)` (`:4450`) — zero PUT, zero send, no subject/QA/meeting/attachment write. I-3: `materialRequestIdentity:4268` captures `ownerKey` (`conversationCacheKey(user, accountScope, contactId)`), `contactId`, `targetKey` (`currentTargetKey()`), `editorRevision` (`instance.meeting.editorRevision`); a late GET is dropped unless `open && seq === seq && materialRequestIdentityMatches(...)` (`:4410-4414`), confirm re-checks the same identity (`:4428-4432`) so an editor revision bump while the dialog is open makes confirm a no-op; the dialog is closed on teardown (`:1610`), manage overlay (`:3070`), follow-up open (`:4074`) and target change (`:4689`). I-4: `label`/`requestText` reach the DOM only through `textContent`/text nodes; the sanitizer round-trip works because `meeting-confirmation.js:128-133` whitelists `p`, `ul`, `li`; no new draft field (`saveDraftFromInputs:3801` untouched — the mailbox-chat.js hunks are only at 505, 1610, 2790, 2861/2885, 3068/3070, 4074, 4212, 4689, 5499, 5593, 5605, 5877). I-5: `grep -rn` over `src/main/resources/static/*.js` finds 0 of the five English `requestText` strings (only the one fixed lead literal at `mailbox-chat.js:4218`); `index.html` carries exactly 11 `?v=` refs, all `?v=20260918-material-request-ui` (`grep -oE '\?v=[0-9a-z._-]+' … \| sort \| uniq -c` → `11 ?v=20260918-material-request-ui`), key matches `[0-9a-z-]+`, and the resource list/order is unchanged as 5 CSS (`styles.css, expert-materials.css, mailbox-chat.css, meeting-confirmation.css, world-clock.css`) then 6 JS (`trust-reply-workbench.js, expert-materials.js, meeting-confirmation.js, mailbox-chat.js, app.js, world-clock.js`); `grep -rF '20260917-meeting-mail-global-world-clock' src/` → 0 hits. S-1: `styles.css` numstat in the range is `34 0` (insertions only, all inside the S-2 block), so there is no action-row CSS change and no new class; the row keeps `contact-head-status-row`/`contact-head-label`/`expert-material-tags`/`expert-material-tag`+tri-state/`dropdown-menu`, with no inline style and no edit/save button (`contactHeadLayout.test.js`: 5 ordered tags, `!includes(item.requestText)`, `!includes("requestText")`, `!includes("style=")`, `!includes("data-action=")`, `编辑材料`/`保存材料` absent). S-2: the plan's block (3553 bytes, sha256 `bd1ec48e26bab1528ef58cfe1d70ae161ff5918d02596ff1a34a1d4ac75e3d24`) is contained byte-for-byte in `styles.css` at offset 248073 and is immediately followed by `'\n/* meeting-mail-07: outbound files */\n'`; `styles.rstrip().endswith(block)` is False; all 32 `material-request` tokens in `styles.css` are the 32 lines of the block (`styles.css:11198-11230`), i.e. no class declared outside it; `grep -c material-request src/main/resources/static/mailbox-chat.css` → 0 and that file is not in the boundary; `grep -c 'style=' src/main/resources/static/mailbox-chat.js` → 0; the trigger sits in `${meetingTrigger}${materialTrigger}${followUpButton}` (`mailbox-chat.js:2885`) with `materialTrigger = ui ? materialRequestTriggerHtml() : ""` (`:2862`) under the same `ui = meetingEnabled() && !isOutbound` gate as the meeting trigger (`:2853`), i.e. exactly between meeting and follow-up; the dialog is the plan's native `<dialog class="material-request-dialog" aria-labelledby="materialRequestTitle">` skeleton mounted in the single portal root (`ensurePortalRoot()` at `:4375`, the same host as the follow-up dialog, listener bound right after the root is created at `:5875-5877`). |
| 3 — every required command fresh vs baseline | PASS | All commands re-run this round in the worktree with `node v25.7.0`. `node --check src/main/resources/static/app.js` → exit 0, no output; `node --check src/main/resources/static/mailbox-chat.js` → exit 0, no output. `node --test src/test/js/contactHeadLayout.test.js src/test/js/materialRequestIntegration.test.js src/test/js/mailboxChatStyle.test.js src/test/js/meetingConfirmationIntegration.test.js src/test/js/mailboxOutboundAttachments.test.js` → exit 0, `tests 104 / suites 26 / pass 104 / fail 0 / cancelled 0 / skipped 0 / todo 0` (epoch 1 was 104/102/2; the two changed assertions are exactly the two recorded reds). `node --test src/test/js/*.test.js` → exit 0, `tests 1003 / suites 196 / pass 1003 / fail 0 / cancelled 0 / skipped 0 / todo 0 / duration_ms 2407` = the recorded seed-commit baseline **990/990 exit 0** plus the 13 new `materialRequestIntegration` cases (and their 4 suites); no baseline test regressed. `git diff --check` → exit 0, no output (also `git diff --cached --check` → exit 0, and `git log --check -1 75628fb` → exit 0). |
| 4 — downstream interfaces | N/A | This is the last child of the fast-p master plan (`docs/plans/fast/material-request/ledger.md` — 03-ui is the final row; children 01/02 closed), so there is no remaining consumer contract to re-check. Deferred human-acceptance items carried, not gates: plan 03's A-1…A-5 need a deployed environment the repo cannot currently provide (`flyway-mysql` is test-scoped, so `mvn spring-boot:run` cannot serve MySQL 8 — child 02 concern 3); the opt-in migration group stays red on the pre-existing `V124 allows material attached promotion audit trigger` `fk_eap_contact` error (child 02 O-1, reproduced at base `535f76f`); `OperatorStatusWriteSeamGuardTest.kt:67-68` still says `使 :549 偏移至 :564` while the A1 pin is 578 (child 02 O-3); child 01 O-2's stale plan-prose key (`20260917-calendar-layout-align`) is superseded in fact by the live `20260918-material-request-ui` now in `index.html`. |

### AUTO_FIX
- N/A — no proven four-gate violation exists in this round: all eight changed product/test files are authorized (A2), the two A2 edits are expectation-only inside their pinned toolbar enumerations, and every invariant has direct code + fresh-test evidence. Nothing the plan uniquely determines is left unfixed inside an authorized file.

### RECORD_ONLY
- O-1 `materialRequestTriggerHtml()` is declared twice in the same `mailbox-chat.js` host scope, byte-identically: `mailbox-chat.js:2791-2793` (next to `meetingTriggerHtml()`) and `:4227-4229` (inside the material-request block). `sed -n '2791,2793p' | md5sum` = `sed -n '4227,4229p' | md5sum` = `112c3f6332730d3bff4df9c04383084c`, so the later declaration wins and the earlier one is dead code; behaviour and every asserted contract are unaffected, and no gate or invariant forbids it — this is weight/hygiene, which the light gate deliberately does not judge, hence not AUTO_FIX. Removing the `:2791-2793` copy (a 3-line deletion in the already-authorized `mailbox-chat.js`) is the smallest correction if the controller wants it.
- O-2 (carried forward, not this child's) The four items in Gate 4 remain open for the controller: the unreachable deployed-environment acceptance run, the pre-existing `V124` migration-group error, the child-02 guard-comment drift, and child 01's stale plan-prose cache key. None is caused by 03-ui (`git diff 4a91a61..75628fb` touches no Kotlin, migration, pom or `application.yml`) and none blocks the child.

### Required Action
- COMPLETE_CHILD — all four light gates PASS on boundary `4a91a61..75628fb`. Gate 1: exactly the eight A2-authorized product/test files (`ee9cc36` six + `75628fb` two A2 test files), nothing else under `src/`. Gate 2: the action row reads only the five-item GET/PUT and never `/materials`; the dialog re-reads per open, selectable/checked = `PENDING` only with `PROVIDED`/`DECLINED` shown-but-disabled, cancel zero-write, confirm zero PUT / zero send / DOM-append only through the existing input seam; the fixed lead + API-order unnumbered `<ul>` come from the response `requestText` (no English hardcoded in `static/*.js`); owner/contact/target/revision capture guards the late GET and confirm; appended nodes are `textContent`-only and survive the untouched sanitizer; S-1 adds no CSS/class/inline style, and the S-2 block is byte-identical (sha256 `bd1ec48e…`) immediately before the `/* meeting-mail-07: outbound files */` marker with `mailbox-chat.css` unchanged and the trigger between the meeting trigger and the follow-up button; the 11 `?v=` refs share one `[0-9a-z-]` key over the unchanged 5 CSS + 6 JS list. Gate 3: `node --check` ×2, the five named files (104/104), the full suite (1003/1003, exit 0; baseline 990/990) and `git diff --check` are all fresh and green. Gate 4 N/A (last child). Only note O-1 (duplicate dead helper) stays open and does not block completion.

## Light Verification: LIGHT_PASS_WITH_NOTES
Child: 03-ui — `docs/plans/2026-09-17/03-material-request-ui.md` (sha256 `4fc501af859b17476e80a50505f88f026710bb9ab19e50e329885f321d7db251`, re-read from disk this round and equal to the epoch-2 record) together with the brief `docs/plans/fast/material-request/children/03-ui/brief.md` (sha256 `050aedf7b76600ab89dea3777e3bcaef0b3ff8c28c6e795e160827a93451ac48`), amended by A2 — the final `## 修正记录` bullet (HUMAN:2026-09-18T09:40+08:00) authorizes the two toolbar-enumerating tests, so the authorized set is the eight files listed below. This is an independent third pass over the same code head: every command was re-run and every code fact re-derived in this round, not carried over.
Boundary: 4a91a6135c031cc60fd8091b1fe8ae61f77c4a2b..75628fb9ae6201e7aa2c26f3dcb880dde8faa9ab (worktree HEAD `4a91087`, branch `fast/material-request`)
Verifier: LightVerifier03b

### Four Gates
| Gate | Result | Evidence |
|---|---|---|
| 1 — changed product/test files ⊆ authorized 8 | PASS | `git diff --name-status 4a91a61..75628fb` → 15 paths; exactly 8 lie under `src/` and all 8 are brief-authorized: `src/main/resources/static/app.js` (M), `static/index.html` (M), `static/mailbox-chat.js` (M), `static/styles.css` (M), `src/test/js/contactHeadLayout.test.js` (M, #5), `src/test/js/materialRequestIntegration.test.js` (A, #6), `src/test/js/mailboxOutboundAttachments.test.js` (M, A2 #7), `src/test/js/meetingConfirmationIntegration.test.js` (M, A2 #8). The other 7 paths are `docs/plans/**` evidence only (the A2 record in the child plan, child-02 evidence, the 03 brief/execution, the ledger, `human-review-handoff.md`). Negative checks: `git diff --name-only 4a91a61..75628fb -- src/main/kotlin src/test/kotlin src/main/resources/db src/main/resources/application.yml pom.xml` → 0 lines; `git diff --name-only 4a91a61..75628fb -- src/main/resources/static/` → only the 4 static files above, so `mailbox-chat.css`, `meeting-confirmation.*`, `expert-materials.*`, `world-clock.*` are untouched; `git status --porcelain` in the worktree → only `M docs/plans/fast/material-request/ledger.md` + untracked `docs/plans/fast/material-request/human-review-handoff.md` (controller evidence), i.e. no uncommitted product/test residue that could invalidate the commit-range scope check. The two A2 edits are assertion-only: `mailboxOutboundAttachments.test.js:1559-1560` inserts `"mc-open-material-request"` between `"mc-open-meeting"` and `"mc-open-followup"` in the enumerated `.mc-editor-tools` list plus its message, and `meetingConfirmationIntegration.test.js:1421-1425` changes `tools.length` 7→8, `tools[6]` → `mc-open-material-request` and shifts the follow-up index; `git diff --stat` for those two files is 6 insertions / 5 deletions with no other hunk. |
| 2 — plan requirements + I-1…I-5, S-1/S-2 have direct code/test evidence | PASS | I-1: `app.js:8045` PUT `/api/expert-contacts/${contactId}/material-requests/${materialCode}` and `app.js:8063` GET `/api/expert-contacts/${contactId}/material-requests`; `grep -n '/materials' src/main/resources/static/app.js` → 0 hits (the `/materials` paging object can no longer be read as the item array); the row is re-rendered only from the response at `app.js:8049` (`row.outerHTML = renderExpertMaterialRow(materials, contactId)`) and the `finally` at `:8050-8053` restores only dropdown-item disabled state, so a failed PUT leaves the old DOM and the function emits no success toast; GET failure is isolated at `:8064` (`showStatus("材料状态加载失败: " + error.message, "error")` → `null`) and the row is skipped entirely unless `Array.isArray(materials)` (`app.js:8124`). I-2: each open re-reads for the current contact — `mailbox-chat.js:4404-4421` issues `hostApi()('/api/expert-contacts/${identity.contactId}/material-requests')` inside `openMaterialRequestDialog` under 「每次打开都为当前 contact 重读五项；不做任何本地缓存复用」; only `PENDING` is selectable and default-checked — `:4309-4313` (`selectable = status === "PENDING"`, `input.checked = selectable`, `input.disabled = !selectable`), with `PROVIDED`/`DECLINED` shown under the Chinese short labels `:4222-4224`; cancel performs zero state write — `closeMaterialRequestDialog` (`:4459-4481`) only flips `open`/`seq`/`identity`/`items`, closes the `<dialog>` and clears the portal root, no fetch; confirm performs zero PUT — `applyMaterialRequestFromDialog` (`:4425-4449`) appends nodes and calls the existing `handleManualComposeInput(inputs.editor)`, no request; English text comes only from the response `requestText` — the lead is the single literal `MATERIAL_REQUEST_LEAD` (`:4218`) and every list item is `li.textContent = String(item.requestText == null ? "" : item.requestText)` (`:4355` preview, `:4444` append), while `grep -rn 'representative publications\|Supporting documents for research\|Patent certificates\|Honor and award\|degree certificates' src/main/resources/static/` → 0 hits (no hardcoded English request text ships); the appended list is an unnumbered `<ul>` in API catalogue order — selection maps `dataset.materialIndex` back into `instance.materialRequest.items` (`:4332-4340`) and both the preview and the append loop iterate that array in order. I-3: `materialRequestIdentity()` (`:4268-4276`) captures `ownerKey`/`contactId`/`targetKey`/`editorRevision` before the GET (`:4369-4385`); a late GET is voided by the `disposed \|\| !open \|\| seq` guard (`:4410`) and by `materialRequestIdentityMatches` (`:4411-4414` → close without writing); confirm re-checks the same identity (`:4428-4432`); confirm is disabled at zero selection (`:4357-4359`, `apply.disabled = selected.length === 0`); the append path touches neither send, subject, QA, meeting snapshot nor attachments — `grep -n 'materialRequest' src/main/resources/static/mailbox-chat.js` yields only the in-memory `instance.materialRequest` state (`:505`) and dialog helpers, with no draft field and no send/attachment call site. I-4: `label`/`requestText` are text-only (`name.textContent`/`state.textContent` `:4315-4321`, preview `li.textContent` `:4355`, appended `lead.textContent`/`li.textContent` `:4434-4443`), and the sanitizer round-trip is proven by the shipped integration cases: `materialRequestIntegration.test.js` "确认：…重挂载后仍在" re-mounts expert 2 → expert 1 and asserts the same three `<li>` texts in order survive, while "已有会议块：追加材料不改写会议块，且会议块与材料段一起经草稿恢复" asserts the single `[data-meeting-block="true"]` node is preserved with the material paragraph appended after it. I-5: key read live from `index.html` first → `grep -o '?v=[^"]*' src/main/resources/static/index.html \| sort \| uniq -c` returns `11 ?v=20260918-material-request-ui` (charset `[0-9a-z-]` only, satisfying child 01's extractor contract); `diff` of the `href=`/`src=` list with the query stripped against `git show 4a91a61:…/index.html` → empty, i.e. the same 5 CSS + 6 JS resources in the same order, with `task-modal-runtime.js` still keyless; the base key `20260917-meeting-mail-global-world-clock` → `grep -rn` over `src/` → nothing, and `grep -rn '20260918-material-request-ui' src/test/js` → 0 hits, so no fixture is pinned to the new value. S-1: `renderExpertMaterialRow` (`app.js:7978-8007`) reuses `contact-head-status-row`/`contact-head-label`/`expert-material-tags`/`expert-material-tag` with `is-pending`/`is-provided`/`is-declined`/`dropdown`/`dropdown-menu`/`dropdown-item` and renders Chinese-only labels (`待提供`/`✓ 已提供`/`⊘ 暂不愿提供`), no English request text and no edit/save button; the `styles.css` diff is one hunk whose added lines are exclusively the S-2 block, so every `.expert-material-*` rule is untouched (`expert-material-tag-mark` present in the base file, 2 hits); `contactHeadLayout.test.js` asserts exactly 5 tags, the tri-state class/mark/aria-label per state, `data-material-action="set-status"` ×15 and `!html.includes(item.requestText)`. S-2: the plan's CSS block (plan lines 63-95) vs `styles.css:11198-11230` → `diff` empty, both md5 `02121aba09a74a58f8b5dd5282c3d5c1` (33 lines), inserted immediately before the existing `/* meeting-mail-07: outbound files */` (`styles.css:11232`) with the file tail untouched; the toolbar is `${meetingTrigger}${materialTrigger}${followUpButton}` inside `.mc-editor-tools` (`mailbox-chat.js:2860-2867`, template `:2887`), the trigger class is exactly `button material-request-trigger` with `data-action="mc-open-material-request"` (`:4227-4229`), rendered only on the `ui = meetingEnabled() && !isOutbound` branch (`:2852`), and `git diff --name-only 4a91a61..75628fb -- src/main/resources/static/mailbox-chat.css` → 0 lines; `git diff 4a91a61..75628fb -- src/main/resources/static/mailbox-chat.js \| grep '^+' \| grep -c 'style="'` → 0 (no inline styles), consistent with the green `mailboxChatStyle.test.js`. |
| 3 — every required command fresh vs baseline | PASS | All commands re-run this round in the worktree on `node v25.7.0` (no JDK, no browser, no server available). `node --check src/main/resources/static/app.js` → exit 0, no output; `node --check src/main/resources/static/mailbox-chat.js` → exit 0, no output. `node --test src/test/js/contactHeadLayout.test.js src/test/js/materialRequestIntegration.test.js src/test/js/mailboxChatStyle.test.js src/test/js/meetingConfirmationIntegration.test.js src/test/js/mailboxOutboundAttachments.test.js` → exit 0, `tests 104 / suites 26 / pass 104 / fail 0 / cancelled 0 / skipped 0 / todo 0` (duration 1335.7 ms). `node --test src/test/js/*.test.js` → exit 0, `tests 1003 / suites 196 / pass 1003 / fail 0 / cancelled 0 / skipped 0 / todo 0` (duration 2799.1 ms; the brief's seed-commit baseline was 990/990, +13 from the new integration file) with every suite line `✔` and no `not ok`. `git diff --check` → exit 0, no output. |
| 4 — downstream interfaces | N/A | This is the last child: the master plan (`docs/plans/2026-09-17/00-material-request-master.md:9`) orders 01 → 02 → 03, the ledger's child table ends at the `03-ui` row (children 01 and 02 closed), and the ledger already reads `Status: READY_FOR_HUMAN_REVIEW` with `Current child: N/A`, so no remaining consumer contract exists to re-check. Deferred human-acceptance items are recorded, not gated: plan 03's A-1…A-5 need a deployed environment this repo cannot currently provide (`flyway-mysql` is test-scoped, so `mvn spring-boot:run` cannot serve MySQL 8), and the Java side is unrunnable in this verification environment (no JDK); the opt-in migration group's pre-existing `V124 allows material attached promotion audit trigger` `fk_eap_contact` error (child 02 O-1) lies outside this child's diff. |

### AUTO_FIX
- N/A — no proven four-gate violation exists in this round. Gate 1: the changed `src/` set is exactly the eight A2-authorized files. Gate 2: every I-1…I-5 and S-1/S-2 requirement has direct code evidence plus a fresh green test. Gate 3: all six required commands exit 0 at the recorded-baseline-or-better counts. Nothing the plan uniquely determines is left unfixed inside an authorized file, so no mechanical fix is warranted.

### RECORD_ONLY
- O-1 `materialRequestTriggerHtml()` is still declared twice in the same host-factory scope, byte-identically: `mailbox-chat.js:2791-2793` (directly after `meetingTriggerHtml()`) and `:4227-4229` (inside the material-request block). `diff` of the two 3-line ranges → identical (no output), and both declarations sit at the same nesting depth inside the mount factory, so function-declaration instantiation order makes the later one win and the earlier copy is dead code. Behaviour, DOM order, class list and every asserted contract are unaffected, and no invariant or style contract forbids a duplicate declaration, so this is weight/hygiene rather than a gate failure. Smallest correction if the controller wants it: delete the 3 lines at `mailbox-chat.js:2791-2793` inside the already-authorized file.
- O-2 (carried forward; not caused by 03-ui and not gating) Four items stay open for the controller: (1) plan 03 A-1…A-5 human acceptance cannot be executed here (no deployed environment, and this verification round has no JDK/browser/server); (2) the opt-in Flyway migration group stays red on the pre-existing `V124 allows material attached promotion audit trigger` `fk_eap_contact` error reproduced at base `535f76f` (child 02 O-1); (3) child-02 guard-comment drift is still present — `src/test/kotlin/com/weibo/talentintroduction/campaign/OperatorStatusWriteSeamGuardTest.kt:68` still reads 「使 :549 偏移至 :564」 while the A1 pin on line 69 is `578` (that is a Kotlin test file and `git diff --name-only 4a91a61..75628fb -- src/test/kotlin` → 0 lines, so 03-ui neither caused nor touched it); (4) child 01's plan prose still names the stale key `20260917-calendar-layout-align` (`docs/plans/2026-09-17/01-material-request-cache-fixtures.md:30,68`) while the live key both before and after this bump is the value derived from `index.html`. None of the four is a 03-ui defect.

### Required Action
- COMPLETE_CHILD

## Finalization Note (controller)

- Appended for the finalization evidence boundary only; the verdict above is unchanged and remains the terminal light result for this child.
