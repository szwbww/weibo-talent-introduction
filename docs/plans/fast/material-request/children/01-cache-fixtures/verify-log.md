## Light Verification: LIGHT_PASS_WITH_NOTES
Child: 01-cache-fixtures — `docs/plans/2026-09-17/01-material-request-cache-fixtures.md` (brief: `docs/plans/fast/material-request/children/01-cache-fixtures/brief.md`)
Boundary: 44136f5dc7bbf94f429afa72c55242c5137d7f4f..535f76f3bcbee243ae5bef09686bbe57921075e7
Verifier: LightVerifier01

Method note: every command ran in the worktree `/Users/lukai/IdeaProjects/weibo-talent-introduction-fast-material-request` (Node v25.7.0). The destructive part of the mandatory negative experiment was reproduced **without mutating the worktree**: `git archive 535f76f | tar -x -C /tmp/probe` made an isolated copy; all probes ran there and the repository was never written outside this report (final `git status --porcelain` = only `?? docs/plans/fast/material-request/`, `git diff --check` exit 0).

### Four Gates
| Gate | Result | Evidence |
|---|---|---|
| 1. Changed files inside the brief's Authorized Files | PASS | `git diff --numstat 44136f5..535f76f` = exactly the nine authorized `src/test/js/*.test.js`; `git diff 44136f5..535f76f -- src/main/resources/static src/main/kotlin src/test/kotlin \| wc -l` → 0; `git log --oneline 44136f5..535f76f` → single commit `535f76f feat(fast-p): implement 01-cache-fixtures`, `9 files changed, 72 insertions(+), 19 deletions(-)`; `index.html` byte-identical between base and head (11 refs of the same key at both revs). |
| 2. Plan requirements + I-1/I-2 have direct code/test evidence | PASS | Derivation `const CACHE_KEY = (() => { const match = html.match(/styles\.css\?v=([^"'&<>]+)/); … })()` present exactly once per file (`rg -c -F 'styles\.css\?v=' <nine>` → 1 each) and consumed by each file's own version assertion (`batchSendTaskConsoleVisualFix:60,67`; `checkRepliesRelocation:71-84`; `mailboxChatStyle:103`; `manualReplySubjectPrefill:97-108`; `meetingConfirmationAssets:39-41,67-68`; `overlayAndDialogContrast:64-76`; `ragKnowledgeBasePage:343-358`; `ragWorkbenchRender:396-408`; `trustReplyWorkbenchSharedMount:184-194`). Escape-before-regex honoured where a `RegExp` is built from the key (`mailboxChatStyle:33,103`; `meetingConfirmationAssets:30,67,68`). I-2: structure-line diff of the two set-heavy files (`git show <rev>:<file> \| rg 'CSS_ORDER\|JS_ORDER\|ALL_ASSETS\|keys\.length\|matchAll\|indexOf\|ordered'`) is identical apart from the version-literal source; count-11 assertions (`meetingConfirmationAssets:40`, `ragKnowledgeBasePage:350`), ordered sets, single-resource presence, `mailbox-chat.css` key sharing and syntax checks all unchanged. I-1 negative detection independently reproduced in `/tmp/probe`: control 990/990; probe A (one `?v=` at line 13 → `20260101-negative-probe`) → exit 1, 990/980/10, **all nine files failing** — identical to the implementer's recorded counts; a test passing for any key could not have failed probe A. |
| 3. Required commands fresh vs baseline | PASS | `node --test <nine files>` → exit 0, `tests 100 / pass 100 / fail 0`; `node --test src/test/js/*.test.js` → exit 0, `tests 990 / pass 990 / fail 0` (= recorded baseline 990/990/0); `git diff --check` → exit 0, no output. |
| 4. Downstream interface for child 03 | PASS | `rg -F '20260917-meeting-mail-global-world-clock' src/test/js` → no output, exit 1; no version literal remains in the nine (`rg 'v=20' <nine>` → no matches); repo-wide the key literal survives only in `index.html`. Extraction survives a bump: probe C in `/tmp/probe` (all 11 refs set to `20260918-material-request-ui`) → 990/990 green. Pre-flight set at `44136f5`: `git grep -l -F '<live key>' 44136f5 -- src/test/js` = exactly the nine authorized files → plan change list needed no amendment. |

Governing invariant: I-1 (single shared key sourced from `index.html`, active assertion) and I-2 (assertion intents preserved) both hold with direct code and reproduced test evidence.

### AUTO_FIX
- N/A — no four-gate violation proven; both notes below are explicitly out of the light gate.

### RECORD_ONLY
- O-1 (charset-narrow key extractor, already recorded by the implementer): four files re-extract the eleven keys with `/\?v=([0-9a-z-]+)/g` (`meetingConfirmationAssets:40,49,50,81,85`; `ragKnowledgeBasePage:349`; `ragWorkbenchRender:396`; `trustReplyWorkbenchSharedMount:181`) while the derived key comes from `[^"'&<>]+`. Probe D in `/tmp/probe` (all 11 refs = `20260918_MaterialRequest`, identical everywhere) → exit 1, 990/983/7, failures confined to those four files and caused by charset truncation, not by any real key divergence. Assessed as a **constraint on child 03's bump value** (`[0-9a-z-]` only, e.g. the existing `20260918-…` style), not a defect of this child: plan T1 explicitly forbids widening the match range (「不能放宽资源名或匹配范围」), so no uniquely determined in-plan correction exists. Outside the light gate; contract states the implementer's concern is evidence to assess, not a finding to fix.
- O-2 (stale plan prose, human gate): the plan's 现状审计 and acceptance item A-1 name the key `20260917-calendar-layout-align`, which exists nowhere in this worktree (`rg -F 'calendar-layout-align' src` → no matches, exit 1); the live key is `20260917-meeting-mail-global-world-clock`. A-1 as written would look for a nonexistent value. Amending the plan is outside verifier authority.

Corroboration note: the implementer's execution report claims were re-derived independently — boundary commit content, nine-file scope, `index.html` untouched, `rg -F` empty receipt, 990/990 suite, `git diff --check` 0, and probe-A failure profile (990/980/10 with 9/9 files failing) all reproduce exactly. The `20260917-calendar-layout-align` literal seen in the main worktree (`~/IdeaProjects/weibo-talent-introduction`, branch `main`) is not present in this child's worktree and did not affect execution.

### Required Action
- COMPLETE_CHILD
