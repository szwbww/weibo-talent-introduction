# 02-status-api — fix-log.md

## Epoch 3 — Round 1/3

- Findings: verifier `LightVerifier02` returned **PAUSE** on gate 3 (`verify-log.md`, `### Required Action`): the only in-scope red was child 02's own `V128 widens the material code check to twelve codes without touching stored rows` assertion `clause.contains("'$code'")` in authorized file #6 — MySQL 8.0.36 renders `information_schema.CHECK_CONSTRAINTS.CHECK_CLAUSE` with a charset introducer and **backslash-escaped literals** (`_utf8mb4\'CV\'`), so the substring `'CV'` never exists even though `CV` is in the domain. The verifier's `AUTO_FIX` section declared the repair not uniquely forced and offered two forms: (a) normalize `clause.replace("\\'", "'")` and keep the text check, or (b) delete the text check and rely on the behavioural probes already present in the test. Human arbitration (2026-09-18, recorded in this session) selected **(b)**. The other red in the same run, `V124 allows material attached promotion audit trigger` `SQLIntegrityConstraintViolationException` on `fk_eap_contact`, is **pre-existing at `child_base_sha` 535f76f3bcbee243ae5bef09686bbe57921075e7** (base: 23 tests, 0 failures, 1 error, same test/FK; also reproduced by `LightVerifier02`) — recorded here, never repaired, never attributed to child 02.

- Before: before this round the migration-IT group was `Tests run: 24, Failures: 1, Errors: 1, Skipped: 0` (epoch 2 `/tmp/e2_migration_it.log`), the failure being `V128 … :196 clause dropped legacy CV ==> expected: <true> but was: <false>`. Because assertions run in order, the behavioural oracle that follows it (12 accepted inserts, `REQ_UNKNOWN` / `PENDING` / duplicate rejections, `COUNT(*) = 12`) never executed. The six lines removed from the test body (verbatim, `git diff` of this round):

  ```diff
  -            val clause = connection.queryString(
  -                "SELECT CHECK_CLAUSE FROM information_schema.check_constraints " +
  -                    "WHERE constraint_schema = DATABASE() AND constraint_name = 'chk_expert_material_code'"
  -            )
  -            legacyCodes.forEach { code -> assertTrue(clause.contains("'$code'"), "clause dropped legacy $code") }
  -            requestCodes.forEach { code -> assertTrue(clause.contains("'$code'"), "clause missing $code") }
  ```

  `legacyCodes` / `requestCodes` declarations are **retained** (still consumed by the insert loops); the `Connection.queryString` helper is **retained** (used by ~30 other assertions in the same file, e.g. `:72`). Every other assertion of the test is byte-identical: `migrateToV23AndSeedBase()`, `targetSchemaVersion == "128"`, `COUNT(*) == 0` before, the `chk_expert_material_code` / `chk_expert_material_status` / `uk_expert_material_contact_code` existence checks, the 7 legacy + 5 `REQ_*` inserts, `COUNT(*) == 12`, the `REQ_UNKNOWN` / `PENDING` / duplicate-row rejections, and the closing `COUNT(*) == 12`.

- Fix commit: `4a91a6135c031cc60fd8091b1fe8ae61f77c4a2b` — `fix(fast-p): repair 02-status-api round 1` (created with `git -c user.name=omp -c user.email=omp@local commit`), 1 file changed, 6 deletions(-). Parent: pause commit `24f2dde2df36ea35e2f14630ab83386f50ec479d`. `docs/plans/fast/**` excluded from the commit (controller commits evidence separately); no history rewritten, nothing pushed/merged.

- Authorized files changed: exactly **one** of the seven — `src/test/kotlin/com/weibo/talentintroduction/campaign/repository/FlywayMigrationIntegrationTest.kt` (authorized file #6; six deleted lines, no insertion). The other six authorized files are untouched (`git diff --name-only 535f76f..4a91a61 -- src` is still the authorized 7-path set, split across `5aaa801` + `71e5874` + `4a91a61`). No plan, ledger, other child's artifact, migration or production source was modified.

- Commands: all with `JAVA_HOME=/Library/Java/JavaVirtualMachines/zulu-11.jdk/Contents/Home` → `openjdk 11.0.15 (Zulu11.56+19-CA)`, run in the target worktree `/Users/lukai/IdeaProjects/weibo-talent-introduction-fast-material-request` @ `fast/material-request`.

  1. `MAVEN_OPTS=-Dapi.version=1.44 mvn test -Dtest=FlywayMigrationIntegrationTest -DmigrationIt=true` — **environment red, group did NOT start**: exit 1, `Tests run: 1, Failures: 0, Errors: 1`, `IllegalStateException: Docker is required for Flyway migration tests`; all three testcontainers strategies `Status 400: client version 1.32 is too old. Minimum supported API version is 1.40`. `MAVEN_OPTS` populates the Maven JVM only and is not forwarded to the surefire fork, so the shaded docker-java (testcontainers 1.19.8) never saw `api.version`; this matches verifier record **O-2** (the runbook's env-var form cannot start the group).
  1b. Same commands **+ `-Dapi.version=1.44`** on the Maven command line (the epoch-2/verified form, required for surefire to forward the shaded docker-java system property): `MAVEN_OPTS=-Dapi.version=1.44 mvn test -Dtest=FlywayMigrationIntegrationTest -DmigrationIt=true -Dapi.version=1.44` — **exit 1, `Tests run: 24, Failures: 0, Errors: 1, Skipped: 0`, 202.23 s, `BUILD FAILURE`**. The container started (`mysql:8.0.36`, `testcontainers/ryuk:0.7.0`) and Flyway applied 104 migrations to `v128`. `Failures: 0` ⇒ **the `V128 …` test now passes** (surefire XML: `<testcase name="V128 widens the material code check to twelve codes without touching stored rows" … time="9.155"/>` with no `<failure>`/`<error>` child). The sole remaining red is the pre-existing, out-of-scope `V124 allows material attached promotion audit trigger:291->execute:1274 » SQLIntegrityConstraintViolationException … CONSTRAINT fk_eap_contact FOREIGN KEY (expert_contact_id) REFERENCES expert_contact (id)` — byte-identical in kind to the `child_base_sha` run (23 tests, 0 failures, 1 error, same test/FK). Log: `/tmp/e3_migration_it_retry.log`.
  2. `mvn test` (full suite) — **exit 0, `BUILD SUCCESS`**: `Tests run: 3465, Failures: 0, Errors: 0, Skipped: 13` (the 13 skips include the opt-in `FlywayMigrationIntegrationTest`), and the pom-bound JS gate in the same lifecycle `node --test src/test/js/*.test.js` → `tests 990 / pass 990 / fail 0`. `OperatorStatusWriteSeamGuardTest` 1/1 green (A1 pin intact). Log: `/tmp/e3_full.log`.
  3. `git diff --check` — **exit 0**, no output (no whitespace/conflict errors).

- Result: **FIXED**. Round-1 acceptance met: migration-IT reports `Failures: 0` with only the pre-existing V124 `fk_eap_contact` error left; full suite BUILD SUCCESS; `git diff --check` clean; exactly one new local commit containing only the one authorized file.

- Notes:
  - **Why deleting the text check is sound (the arbitrated rationale).** The `CHECK_CLAUSE` string is a MySQL *rendering* detail, not part of the constraint contract: MySQL 8.0.36 emits a charset introducer plus backslash-escaped literals (`_utf8mb4\'CV\'`, and `_latin1\'…\'` for a latin1 session), so `contains("'CV'")` is false for a constraint that does contain `CV` — the assertion tested the renderer, not the domain, which is exactly the class of red the verifier flagged as not AUTO_FIX. The behavioural probes that remain in the same test prove the *same* CHECK domain strictly better, because they are executed against the real MySQL 8.0.36 instance: all 7 legacy + all 5 `REQ_*` codes are **accepted** (⇒ domain ⊇ the 12 required codes) and `REQ_UNKNOWN` is **rejected** with `Check constraint 'chk_expert_material_code' is violated` (⇒ domain ⊅ anything outside the 12), while `COUNT(*) == 12` before and after the three rejections pins that nothing else was admitted or silently rewritten. The still-passing `chk_expert_material_status` / `uk_expert_material_contact_code` existence checks plus the `PENDING` and duplicate-row rejections keep I-2's two-valued status and the unique key asserted independently of any clause text; `COUNT(*) == 0` before the inserts and the unchanged V111 rows keep I-1's zero-data-rewrite property. The repair therefore removes a renderer-dependent, weaker oracle in favour of the stronger behavioural one — it does not weaken the test.
  - No new assertion was added, no migration was touched, no other test was touched, and no production file was touched; the test body's only change is the six-line deletion shown under **Before**.
  - Housekeeping disclosure: the fix-log template placeholder line `(no entries yet)` was replaced by this first round entry — the file's header is otherwise unchanged and no prior entry existed, so nothing was rewritten.
  - Scratch artifacts live outside the repo: logs `/tmp/e3_migration_it.log` (env-only attempt), `/tmp/e3_migration_it_retry.log`, `/tmp/e3_full.log`; testcontainers MySQL/ryuk containers are reaped by ryuk; epoch-1's `fast02-mysql` container remains as documented in `execution.md`.
