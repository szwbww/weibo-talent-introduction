## Light Verification: PAUSE
Child: 02-status-api — `docs/plans/2026-09-17/02-material-request-status-api.md` (brief: `docs/plans/fast/material-request/children/02-status-api/brief.md`, amended by A1 to 7 files)
Boundary: 535f76f3bcbee243ae5bef09686bbe57921075e7..71e587477e0b2defb9775042ae8e9cee03365321
Verifier: LightVerifier02

### Four Gates
| Gate | Result | Evidence |
|---|---|---|
| 1 — changed product/test files ⊆ authorized 7 | PASS | `git diff --name-status 535f76f..71e5874 -- src` = exactly 7 paths, matching the brief's list: `V128__add_material_request_codes.sql` (A), `ExpertMaterialService.kt`, `ExpertContactManagementController.kt`, `ExpertMaterialRequestServiceTest.kt` (A), `ExpertContactManagementControllerTest.kt`, `FlywayMigrationIntegrationTest.kt`, `OperatorStatusWriteSeamGuardTest.kt`. `git show --stat 5aaa801` = the six files; `git show --stat 71e5874` = the guard file only (1+/1-). No other `src/` path (no `MailVariableService.kt`, `RagProcessContextResolver.kt`, `document/…/ExpertMaterialController.kt`, static or `src/test/js`) appears in the range. Worktree clean apart from the three `docs/plans/fast/**` evidence files the controller owns. |
| 2 — plan requirements + I-1..I-4 have direct evidence | PASS | I-1: `ExpertMaterialRequestCode` (`ExpertMaterialService.kt:50-72`) = `REQ_PUBLICATIONS, REQ_PROJECTS, REQ_PATENTS, REQ_AWARDS, REQ_DEGREES` in that order; the 7 legacy entries are untouched, and `listMaterials:109` / `renderPendingMaterials:178` bodies are byte-identical (diff is a pure relocation of the latter). Legacy/RAG/uploads diff empty. I-2: one shared `persistStatus:191` (PENDING→`deleteById`, PROVIDED/DECLINED→`save`) reused by `updateStatus` and the new PUT; V128 (23 lines) only `DROP CHECK`/`ADD CONSTRAINT chk_expert_material_code` — no DML, status CHECK and `uk_expert_material_contact_code` untouched. I-3: the five English strings exist only in the server enum (`ExpertMaterialService.kt:53-69`, `#5` uses U+2019, verified 0x2019); `listMaterialRequests:140` performs only `requireContact` + `findAllByExpertContactId` (no write/scan/mail) and the service test asserts zero `save`/`deleteById`/`delete`. I-4: `GET`/`PUT /{contactId}/material-requests[/{code}]` (`ExpertContactManagementController.kt:258,262` under `@RequestMapping("/api/expert-contacts")`); the legacy `listMaterials:246` carries no mapping annotation; `material-requests` occurs nowhere else in `src/`; live context started with no Ambiguous mapping and `GET /{id}/materials` still served by `document/controller/ExpertMaterialController.kt` (`@RequestMapping("/api/expert-contacts/{contactId}/materials")`). A1: the only change to `OperatorStatusWriteSeamGuardTest.kt` is line 69 `564`→`578`, path string and fragment `operatorStatus = operatorStatus` identical. |
| 3 — required commands re-run fresh vs baseline | FAIL | `mvn test -Dtest=ExpertMaterialRequestServiceTest,ExpertContactManagementControllerTest` → exit 0, `Tests run: 20, Failures: 0, Errors: 0, Skipped: 0` (10+10), BUILD SUCCESS, and the pom-bound JS gate in the same lifecycle `node --test src/test/js/*.test.js` → `tests 990 / pass 990 / fail 0` (baseline 990/990). `mvn test` → exit 0, `Tests run: 3465, Failures: 0, Errors: 0, Skipped: 13` (incl. `FlywayMigrationIntegrationTest` skipped as opt-in), JS 990/0, `OperatorStatusWriteSeamGuardTest` 1/1 and both new classes green. `git diff --check` → exit 0, no output (also over the boundary range). `MAVEN_OPTS=-Dapi.version=1.44 mvn test -Dtest=FlywayMigrationIntegrationTest -DmigrationIt=true -Dapi.version=1.44` → **exit 1**; the group did run (mysql:8.0.36 testcontainer started, `Total time: 06:01 min`): `Tests run: 24, Failures: 1, Errors: 1, Skipped: 0` — (a) child-caused failure `V128 widens the material code check to twelve codes without touching stored rows:196 clause dropped legacy CV ==> expected: <true> but was: <false>`: `CHECK_CLAUSE` is rendered by MySQL 8.0.36 with a charset introducer and backslash escapes, independently reproduced on the live v128 schema (`SELECT CHECK_CLAUSE …` → `(\`material_code\` in (_utf8mb4\'CV\',…,_utf8mb4\'REQ_DEGREES\'))`), so `contains("'CV'")` is false; V128 itself is correct (clause applied to v128, legacy+new rows coexist) but the plan-required 12-code / invalid-rejection probes at lines 199-243 never execute; (b) pre-existing `V124` error → O-1. |
| 4 — interface for child 03 matches the producer | PASS | Fresh live run (JDK 11, app on 18080 against MySQL 8.0.36 at `jdbc:mysql://127.0.0.1:13306/talent_introduction`, Flyway at v128): `GET /api/expert-contacts/1/material-requests` → 200, 5-element array field order `code,label,status,requestText` (DTO declaration order, `ExpertMaterialService.kt:89-94`); codes/order `REQ_PUBLICATIONS, REQ_PROJECTS, REQ_PATENTS, REQ_AWARDS, REQ_DEGREES`; labels `代表性论文/科研项目/专利/荣誉奖项/学位`; the five English `requestText` verbatim incl. U+2019 in #5 — identical to the record in `execution.md`. `PUT …/material-requests/REQ_AWARDS {"status":"DECLINED"}` → 200 with the same array (DECLINED), `{"status":"PENDING"}` → 200 back to PENDING, and `expert_material_status` then held only `CV PROVIDED` + `REQ_DEGREES PROVIDED` (no PENDING row). `GET …/materials?page=0&size=10` → the `ExpertMaterialPage` object `{"items":[],"total":0,…}`; legacy `PUT …/materials/CV` → 7-item array with `CV=PROVIDED`; `PUT REQ_UNKNOWN` / `{"status":"DONE"}` → 400 `Unknown material request code…` / `Unknown material status: DONE`. All routes produced by `ExpertContactManagementController.kt:258,262` + the enum/DTO above. |

### AUTO_FIX
- N/A — the single gate-3 red is not AUTO_FIX-class. The governing requirement is T3/验收标准 ("V128 CHECK 实施" via `-DmigrationIt=true`), but the plan never specifies the `information_schema.CHECK_CLAUSE` text assertion at `FlywayMigrationIntegrationTest.kt:196-197`, so it does not uniquely determine the repair: both candidate corrections live in already-authorized file #6 yet neither is forced — normalize the escaping (`clause.replace("\\'", "'")`) and keep the text check, or delete the text check and rely on the behavioural probes already present (12 inserts accepted, `REQ_UNKNOWN` / `PENDING` / duplicate rejected, `COUNT(*) = 12`), which are the stronger oracle and exactly what T3 demands. Choosing between them is a human/plan decision, so the failure is recorded for authorization instead of being auto-fixed.

### RECORD_ONLY
- O-1 `V124 allows material attached promotion audit trigger:297->execute:1280 » SQLIntegrityConstraintViolation` (`fk_eap_contact` → `expert_contact(id)`, because the test does `flyway.clean()` + `migrate()` with no base seed and then inserts `expert_contact_id = 1`) is **not caused by this child**. Own base-run evidence: a scratch clone of this worktree at `child_base_sha` 535f76f (`/tmp/lv02-base`, `git checkout --detach` in the clone only) run with the same pinned command yields `Tests run: 23, Failures: 0, Errors: 1, Skipped: 0`, the same single error, same test, same FK constraint (only the in-file line numbers differ: `:233->:1216` at base vs `:297->:1280` at HEAD). The opt-in migration group had never been run on this machine before child 02, so this latent repo-level defect keeps the group red on any branch and needs an owner/scope decision (seed the base contact before the audit insert). Outside the authorized 7 files and outside the four gates.
- O-2 The brief's runbook line ("If the bundled docker-java client fails API negotiation, retry with `DOCKER_API_VERSION=1.44`") cannot make the group start: epoch 1's env-var-only attempts returned `Status 400 … client version 1.32 is too old`, and my run started the container only with the shaded docker-java system property (`MAVEN_OPTS=-Dapi.version=1.44` and/or `-Dapi.version=1.44`), matching the ledger's note. Environment/runbook inconsistency, not a child violation.
- O-3 A1's line-number-only correction leaves the adjacent comment stale (`// （plan 01 新增 materials GET/PUT 端点与 UpdateExpertMaterialStatusRequest DTO 使 :549 偏移至 :564）`, `OperatorStatusWriteSeamGuardTest.kt:67-68`, now the pin is 578). Documentation drift only; A1 authorized exactly the number and explicitly no other edit. Not a gate violation.
- O-4 The five `requestText` values must reach child 03 from the API (U+2019 in #5); the controller test and the service test re-spell them as assertions, which is why a plain repo-wide grep finds them in three files. No consequence for I-3 (single server-side producer) — noted so a later grep-based audit is not misread.

### Required Action
- PAUSE — human approval is needed before this child can close: (1) authorize the bounded repair of the child's own `FlywayMigrationIntegrationTest.kt:196-197` clause assertion (one of the two forms in AUTO_FIX) and then re-run `mvn test -Dtest=FlywayMigrationIntegrationTest -DmigrationIt=true -Dapi.version=1.44` (expect `Failures: 0` with only the pre-existing V124 error) plus `mvn test -Dtest=OperatorStatusWriteSeamGuardTest` and the scoped pair; (2) decide the disposition of O-1 (pre-existing V124 FK error, which keeps the opt-in group red repo-wide); (3) optionally fold O-3's stale comment into the same amendment. Gates 1, 2 and 4 are PASS: the implementation, the routes, V128, the A1 pin and the live API contract are sound; only the migration-IT red — and the authority to repair it — blocks completion.

---

## Light Verification: LIGHT_PASS_WITH_NOTES
Child: 02-status-api — `docs/plans/2026-09-17/02-material-request-status-api.md` (brief `docs/plans/fast/material-request/children/02-status-api/brief.md`, amended by A1 to 7 authorized files)
Boundary: 535f76f3bcbee243ae5bef09686bbe57921075e7..4a91a6135c031cc60fd8091b1fe8ae61f77c4a2b
Verifier: LightVerifier02b

### Four Gates
| Gate | Result | Evidence |
|---|---|---|
| 1 — changed product/test files ⊆ authorized 7 | PASS | `git diff --name-status 535f76f..4a91a61 -- src` = exactly the authorized set: `V128__add_material_request_codes.sql` (A), `ExpertMaterialService.kt` (M), `ExpertContactManagementController.kt` (M), `OperatorStatusWriteSeamGuardTest.kt` (M, A1), `ExpertContactManagementControllerTest.kt` (M), `FlywayMigrationIntegrationTest.kt` (M), `ExpertMaterialRequestServiceTest.kt` (A). Per commit: `5aaa801` 6 files (550+/32-), `71e5874` guard file only (1+/1-), `4a91a61` 1 file (6 deletions, `git show --stat`). `git diff --name-only 535f76f..4a91a61` non-`docs/plans` paths are those same 7; `git diff --stat 535f76f..4a91a61 -- src/main/resources/static src/test/js pom.xml` is empty, so `MailVariableService.kt`, `RagProcessContextResolver.kt`, `document/controller/ExpertMaterialController.kt`, static assets, JS tests and the pom are untouched. Repo state: `git status --porcelain` lists only the two `docs/plans/fast/**` evidence files the controller is writing (EXECUTION/FIX-LOG); `git diff --cached --stat` empty (index untouched by me). |
| 2 — plan requirements + I-1..I-4 hold | PASS | I-1: `ExpertMaterialRequestCode` (`ExpertMaterialService.kt:50-71`) = `REQ_PUBLICATIONS, REQ_PROJECTS, REQ_PATENTS, REQ_AWARDS, REQ_DEGREES` in that order; raw `diff -u` of base vs head shows only an insertion after the legacy `ExpertMaterialCode` enum, so the 7 legacy entries are byte-identical; `listMaterials` is absent from that diff and `renderPendingMaterials` is relocated byte-identically; V128 keeps all 7 legacy codes in the CHECK (`V128__add_material_request_codes.sql:13-23`), so legacy rows stay legal. I-2: one shared `persistStatus` (`:191`) — `PENDING`→`deleteById`, `PROVIDED/DECLINED`→`save` — used by both `updateStatus` (`:128`) and `updateMaterialRequestStatus` (`:161`); V128 contains only `DROP CHECK` + `ADD CONSTRAINT chk_expert_material_code` (no DML), leaving `chk_expert_material_status` and `uk_expert_material_contact_code` untouched; the migration-IT run on a real MySQL 8.0.36 proves the domain behaviourally (7+5 inserts accepted, `COUNT(*)` 0→12, `REQ_UNKNOWN` rejected, `PENDING` rejected, duplicate `(1,'REQ_DEGREES')` rejected, `COUNT(*)`=12 after). I-3: the five English strings exist only in the server enum (`:52-71`); `listMaterialRequests` (`:140-159`) performs no write (service test asserts `verifyNoInteractions` on rejects and `never` on `save`/`deleteById`/`delete` for the read path). I-4: new routes `@GetMapping("/{contactId}/material-requests")` / `@PutMapping("/{contactId}/material-requests/{code}")` (`ExpertContactManagementController.kt:258-268`) under `@RequestMapping("/api/expert-contacts")` (`:46`); `material-requests` appears nowhere else in `src/main`; `GET /materials` is mapped only by the untouched document controller (`document/controller/ExpertMaterialController.kt:27,34`) and is unrouted here — `mockMvc.perform(get("…/materials"))` expects 404 (`ExpertContactManagementControllerTest.kt:159-162`). Legacy preservation: legacy `PUT /{contactId}/materials/{materialCode}` still present (`:249`) plus the 7-item array; `${pendingExpertMaterials}` still fed by `MailVariableService.kt:158 → :274-277`; RAG `CV` read intact at `RagProcessContextResolver.kt:31-44`. A1: `git show 71e5874` is a single line, 564→578, path string and fragment text unchanged; pin `OperatorStatusWriteSeamGuardTest.kt:69` equals the observed site `ExpertContactManagementController.kt:578` and the guard test is green. I-4's "context starts with no Ambiguous mapping" is not re-booted in this round (see O-5); `git diff --stat 71e5874..4a91a61 -- src/main` is empty, so the epoch-1 live boot observation applies to byte-identical main sources. |
| 3 — required commands fresh vs baseline | PASS | All fresh this round, JDK 11 (`zulu-11`). (a) `mvn test` → exit 0, `BUILD SUCCESS`, `Tests run: 3465, Failures: 0, Errors: 0, Skipped: 13`, `Total time: 02:54 min`, and the pom-bound JS gate in the same lifecycle `node --test src/test/js/*.test.js` → `tests 990 / pass 990 / fail 0` (baseline 990/990) — `/tmp/lv02b_full.log`; in it `ExpertMaterialRequestServiceTest` 10/10, `ExpertContactManagementControllerTest` 10/10, `OperatorStatusWriteSeamGuardTest` 1/1 green; the 13 skips are the pre-existing env/opt-in classes. (b) `git diff --check` → exit 0, no output; `git diff --check 535f76f..4a91a61` → exit 0, no output. (c) `mvn test -Dtest=FlywayMigrationIntegrationTest -DmigrationIt=true -Dapi.version=1.44` → the group **ran** (container `mysql:8.0.36`, `Successfully applied 127 migrations … now at version v128`): `Tests run: 24, Failures: 0, Errors: 1, Skipped: 0`, `337.59 s` — `/tmp/lv02b_migration_it.log`. `Failures: 0` ⇒ the repaired `V128 widens the material code check to twelve codes without touching stored rows` testcase is present and self-closing in the surefire XML (`time="8.924"`, no failure/error child). Sole red = `V124 allows material attached promotion audit trigger:291->execute:1274 » SQLIntegrityConstraintViolation … CONSTRAINT fk_eap_contact FOREIGN KEY (expert_contact_id) REFERENCES expert_contact (id)` — the arbitrated pre-existing red, identical (same test, same FK, 0 failures/1 error) to the base `535f76f` run, so per the light gate's baseline rule it is not a gate-3 violation of this child. |
| 4 — child 03 interface matches the producer | PASS | Byte-level comparison of the `execution.md` record against the code: five codes in order, five labels `代表性论文/科研项目/专利/荣誉奖项/学位`, five English `requestText` values — all equal, including U+2019 in #5 (`Bachelor’s, master’s, and doctoral degree certificates`, verified at byte level in `ExpertMaterialService.kt:65` and in the recorded table). The recorded JSON blob equals the catalogue and its key order equals the DTO declaration order `code,label,status,requestText` (`ExpertMaterialService.kt:89-94`). Routes/verbs: recorded `GET /api/expert-contacts/{contactId}/material-requests` and `PUT …/{code}` match `ExpertContactManagementController.kt:258` and `:262` (class-level `:46`). The controller test additionally pins the wire contract with a strict MockMvc JSON assertion (`ExpertContactManagementControllerTest.kt:131-157`), and no `src/main` byte differs between the live-observed revision `71e5874` and `4a91a61`. |

### AUTO_FIX
- N/A — no proven four-gate violation exists in this round; the repaired failure is gone and both remaining observations (V124, runbook) are outside the child's authority/scope.

### RECORD_ONLY
- O-1 (carried forward, re-observed) `V124 allows material attached promotion audit trigger` still reds the opt-in migration group with the `fk_eap_contact` violation (fresh: `:291->execute:1274`, `Tests run: 24, Failures: 0, Errors: 1`). It reproduces on the untouched base `535f76f` (0 failures, 1 error, same test) and is arbitrated out of scope (RECORD_ONLY), so it is not attributable to child 02.
- O-2 (carried forward) Runbook/brief gap: the migration group is startable on this machine only with `-Dapi.version=1.44` as a Maven command-line system property; `DOCKER_API_VERSION`/`MAVEN_OPTS` alone never reach the surefire fork and leave the group unstartable (`Status 400 … client version 1.32 is too old`). Environment/runbook inconsistency, not a child violation.
- O-3 (carried forward, unchanged) The comment adjacent to the A1 pin is still stale — `OperatorStatusWriteSeamGuardTest.kt:67-68` says `使 :549 偏移至 :564` while the pin is 578. A1 authorized exactly the number (`git show 71e5874` = one line); documentation drift only, not a gate violation.
- O-4 (carried forward) The five `requestText` values are re-spelled as assertions in the service and controller tests as well as in the server catalogue, which is why a repo-wide grep finds them in three places. No I-3 consequence (single server-side producer).
- O-5 (new, evidence provenance) The "no Ambiguous mapping at startup" claim was not re-booted in this round; it rests on static route analysis (distinct literal segment `material-requests`, unique in `src/main`, plus the untouched document controller's `/materials`) and on the epoch-1 live boot of `71e5874`, whose `src/main` is byte-identical to `4a91a61` (`git diff --stat 71e5874..4a91a61 -- src/main` empty). Recorded for transparency, not a gate finding.

### Required Action
- COMPLETE_CHILD — all four light gates PASS on boundary `535f76f..4a91a61`: the A1 pin is the only change to the guard file (line number only), the repair round removed only the defective `CHECK_CLAUSE` text assertion from child 02's own test, V128 and the five-item API are behaviourally proven, `mvn test` (3465/0/0/13, JS 990/990) and `git diff --check` are green, and the sole migration-group red is the pre-existing, human-arbitrated V124 `fk_eap_contact` error carried as O-1. Notes O-2…O-5 remain open for the controller and do not block child 02.
