## Light Verification: LIGHT_PASS
Child: unsubscribe-07-opaque-token (docs/plans/2026-08-12/unsubscribe-07-opaque-token.md)
Boundary: 04f88337da5824389767a3ef504eb92e6de083f4..d2c5bda11fb7df7052d8f25134b336481d3268dd
Verifier: Ver07

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
|Authorized scope|PASS|`git diff --name-only 04f8833..d2c5bda` = 13 files: exactly the 6 authorized product files (`src/main/kotlin/.../mail/domain/UnsubscribeToken.kt`, `mail/repository/UnsubscribeTokenRepository.kt`, `mail/service/UnsubscribeTokenService.kt`, `src/main/resources/db/migration/V89__create_unsubscribe_token.sql`, `src/test/.../UnsubscribeTokenMigrationTest.kt`, `UnsubscribeTokenServiceTest.kt`) + 7 docs files from docs-only commits verified per-commit: 5a028eb (child-06 verify records: 5 docs), e8d4d88 (pause: execution.md + ledger.md), 69c9fa2 (A2 amendment: plan doc). No docs/plans/* in the product diff; no product file touched outside the range. `git diff --check` clean. Working tree dirty only with docs (brief/execution/ledger modified, 2 untracked master docs) — controller-owned, not product code.|
|Plan and invariants|PASS|All T-1..T-6 present matching plan forms (domain/repo/V89/service verbatim). I-1: A2 deterministic byte-subsequence assertion present (UnsubscribeTokenServiceTest `sign token carries no dot and decodes without email address`); `enc(n)` grep = 0 hits (legacySign uses `enc(normalizedEmail)` — encoding confined to legacy path). I-2: idempotent-save + DuplicateKeyException re-read tests; T-6 asserts uk_email. I-3: `verify()` first line is `repository?.findByToken(token)` (UnsubscribeTokenService.kt:28); dual-channel tests (table + legacy fallback). I-4: `SecureRandom` import :9 / field :20; `UUID\|java.util.Random` = 0 hits; 32-byte decode assertion. I-5: `enabled()` split + blank-secret legacy→null tests; original `enabled requires base url and secret` :63-67 3 assertions intact. I-6: 10 non-null-repository cases ≥ 8; `grep -c "UnsubscribeTokenService(properties, "` = 1. I-7: T-6 "no INSERT" assertion. 8 original tests preserved byte-for-byte (`git diff` shows 0 removed lines — sole `^-` line is the diff header). A2 deviation adjudicated YES (see below) → gate 2 PASS.|
|Required commands|PASS|All fresh, JDK 11 (JAVA_HOME=zulu-11). `mvn test -Dtest=UnsubscribeTokenServiceTest` exit 0, 18/0/0. `mvn test -Dtest=UnsubscribeTokenMigrationTest` exit 0, 3/0/0. 5 unchanged classes `-Dtest=UnsubscribeControllerTest,UnsubscribeControllerIllegalTokenTest,MailVariableServiceTest,SmtpMailDeliveryServiceTest,ManualExpertMailServiceGateTest` exit 0, 77/0 (5+3+40+24+5). Full `mvn test` exit 0, `Tests run: 2322, Failures: 0, Errors: 0, Skipped: 4`, BUILD SUCCESS, JS 485/485 — matches implementer's recorded ×2 full-gate runs. `mvn clean package` exit 0, BUILD SUCCESS, 2322/0/4, JS 485/485 (freshly run: the plan's 验证命令 lists it without a default-skip annotation, so the implementer's NOT-RUN gap is closed by direct evidence; the two full-gate runs would have covered the regression gate but not the packaging phase — fresh run makes this moot). `git diff --check` exit 0, no output. FlywayMigrationIntegrationTest NOT RUN — Docker unavailable (freshly confirmed `docker info` fails); plan marks it "需本机 Docker；默认跳过" → plan-consistent default skip, matches implementer record. Baseline comparison: 2309 tests at 0482bcd; 2322 = 2309 + 10 new service + 3 new migration tests (implementer arithmetic confirmed by my fresh run counts).|
|Downstream interfaces|PASS|UnsubscribeTokenService public surface unchanged in shape: `enabled()/sign()/verify()/unsubscribeUrl()` same names/signatures; ctor gained defaulted `repository: UnsubscribeTokenRepository? = null` → zero call-site changes. UnsubscribeController.kt, MailVariableService.kt, SmtpMailDeliveryService.kt untouched (empty diff base..head). Controller verify() contract unchanged: `verify(token) ?: 400` "invalid"/"invalid link", 200 "unsubscribed" (UnsubscribeController.kt:23/:31/:37). Child 08 does not assume any token shape (brief).|

### AUTO_FIX
- N/A

### RECORD_ONLY
- N/A (implementer's `mvn clean package` NOT-RUN deviation is resolved by my fresh run: BUILD SUCCESS; Flyway IT skip is plan-marked default-skip. No gate violation remains.)

### A2 Deviation Adjudication (gate 2)
Plan's A2 code form `decoded.toList().indexOfSlice(emailBytes.toList()) >= 0` cannot compile: `indexOfSlice` is absent from Kotlin stdlib — verified by scanning `kotlin-stdlib-1.9.25.jar` (project pin, pom.xml:21) for the identifier: 0 occurrences; implementer recorded compiler `Unresolved reference: indexOfSlice` at :120,30. Implemented form: `decoded.toList().windowed(emailBytes.size).any { it == emailBytes.toList() }` — semantically identical contiguous-byte-subsequence containment check over the same fixed inputs (15-byte emailBytes vs 32 decoded bytes; windowed(15) enumerates exactly the subsequences indexOfSlice would scan; empty-window edge cases cannot occur). Test name, failure message, and I-1 purpose (deterministic proof the token does not encode the normalized email) unchanged. Adjudication: YES — the implemented assertion satisfies the plan's T-5/A2 requirement → gate 2 PASS; the translation is recorded as evidence (per brief instructions).

### Required Action
- COMPLETE_CHILD
