## Light Verification: LIGHT_PASS_WITH_NOTES
Child: unsubscribe-08-branded-page (docs/plans/2026-08-12/unsubscribe-08-branded-page.md, amended plan identity commit b893912f380a6a6ed47aa31557551c9d5c43897a)
Boundary: d2c5bda11fb7df7052d8f25134b336481d3268dd..44d6aa23ebdb43581af427708f8348423e2c33a7
Verifier: Ver08

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
|Authorized scope|PASS|`git diff --name-only d2c5bda..44d6aa2 -- src/` = exactly the 7 authorized files (count 7): UnsubscribeProperties.kt, UnsubscribeController.kt, UnsubscribePageRenderer.kt, application.yml, UnsubscribeControllerIllegalTokenTest.kt, UnsubscribeControllerTest.kt, UnsubscribePageRendererTest.kt. Implementation commit 44d6aa2 (`git show --name-only`) touches exactly those 7; docs-only commits between base and head (1e8237d, 854b4ce, b893912) touch only docs/plans/* (verified per-commit). No product file touched outside the boundary range. Full-range diff's docs entries all originate from the docs-only commits. Working-tree modifications (brief.md, execution.md, ledger.md modified; master.md untracked) are docs-only.|
|Plan and invariants|PASS|I-1: `suppressionService` grep in UnsubscribeController.kt hits only :19 (field), :26 (oneClick), :40 (confirm) — 0 in `page()` body; T-6 asserts Keep me subscribed is `<a`, exactly 1 `<form`/1 `<button`. I-2: escapeHtml replaces `& < > " '` with `&` first; `value="$token"` grep = 0 lines; T-6 escape test asserts `&quot;`/`&lt;script&gt;` and no `<script>`. I-3: verbatim `action="unsubscribe/confirm"`, `action="/u/` grep = 0; T-6 + T-5 assertions. I-4: renderer grep for `styles.css\|cdn\.\|fonts.googleapis\|<script` = 0; wordmark degrade (no `<img>`, `class="qf-wordmark"`) and logo branch (`<img class="qf-logo" src=... alt=`) both tested. I-5: maskEmail = last-`@` split; T-6 boundary tests (normal, single-char local, no-@, multiple-@) pass; adjudicated interpretation 1 confirmed (`a@b@c.com` -> `•••@c.com` via `local.contains('@')`). I-6: single `renderShell`; T-6 asserts both states have exactly 1 `<style` each and byte-identical style content. S-1: `<style>` block byte-identical vs plan text at b893912 (extracted plan lines 84-110 vs renderer STYLE_BLOCK lines 107-133; `diff` exit 0; 4286/4286 bytes); 13 positive baseline greps all >=1, 5 negative (obsolete palette) all 0; `<!DOCTYPE` count = 1; `style="` count = 1 (only `style="margin:0"`). S-2/S-3/S-4/S-5 DOM fragments verbatim with the 3 adjudicated interpretations (brandShortName -> brandName; T-6 `style contract fragments` + `success page renders contract fragments` assert the literal fragments). T-1..T-7 all present as amended (T-7 `@MockBean renderer` per A3; 3 cases + TokenTestConfig preserved verbatim; T-5 nested @TestConfiguration real-renderer wiring per interpretation 3; `You&#39;ve been unsubscribed` replaces old success string; 3-arg suppress verify verbatim).|
|Required commands|PASS|All run freshly with JDK 11 (zulu-11), exit 0: `mvn test -Dtest=UnsubscribePageRendererTest` -> Tests run: 11, Failures: 0, Errors: 0; `mvn test -Dtest=UnsubscribeControllerTest` -> 5/0/0; `mvn test -Dtest=UnsubscribeControllerIllegalTokenTest` -> 3/0/0; single-method `-Dtest=UnsubscribeControllerTest#GET valid token returns confirm html with context-path-safe action` -> 1/0/0; full `mvn test` -> Tests run: 2333, Failures: 0, Errors: 0, Skipped: 4, BUILD SUCCESS (02:20 min); `mvn clean package` -> BUILD SUCCESS, 2333 tests; `git diff --check` -> exit 0, no output. Every recorded implementer claim reproduced exactly (11/0, 5/0, 3/0, 1/0, 2333/0/4).|
|Downstream interfaces|PASS|Last child — interface consumption N/A. must-NOT-change items re-verified: `oneClick()` zero-change (controller diff shows only constructor param added; body byte-identical), POST /u/unsubscribe returns exactly `"unsubscribed"`/`"invalid"` (UnsubscribeController.kt:26,28 + tests), GET invalid token -> 400 `"invalid link"` verbatim (:32 + `GET invalid token returns 400` test), confirm suppress 3 args verbatim (`ONE_CLICK`, `"web confirm unsubscribe"`), form action relative `unsubscribe/confirm`.|

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1: I-5 invariant prose lists maskEmail boundary coverage as 无`@`/多个`@`/空 local/空 domain, but T-6's test `mask email handles boundary shapes` asserts only the three T-6-enumerated inputs plus the normal case (no-@ `noatsign`, multiple-@ `a@b@c.com`, single-char local `a@b.com`); 空 local (`@b.com`) and 空 domain (`a@`) are implemented correctly in UnsubscribePageRenderer.kt (`domain.isEmpty()` -> `"•••"`; `local.length <= 1` -> `"•••@"+domain`) but not directly asserted. Outside the light gate because the plan's own T-6 case list and the 验收标准's "四条" cases all pass.

### Required Action
- COMPLETE_CHILD
