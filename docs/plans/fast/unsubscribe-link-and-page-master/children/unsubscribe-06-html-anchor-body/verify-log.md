# Verify Log — unsubscribe-06-html-anchor-body

## Light Verification: LIGHT_PASS_WITH_NOTES
Child: unsubscribe-06-html-anchor-body (docs/plans/2026-08-12/unsubscribe-06-html-anchor-body.md)
Boundary: 0482bcd497eefba9ce4f44f61a5624ae25d0efe1..04f88337da5824389767a3ef504eb92e6de083f4
Verifier: Ver06
Date: 2026-08-12 (re-verification after resumed A1 implementation commit)

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
|Authorized scope|PASS|`git diff --name-only 0482bcd..04f8833` = 25 files: 15 under `docs/plans/*` only (incl. `docs/plans/2026-08-12/unsubscribe-06-html-anchor-body.md`) + exactly the 10 files from the amended plan's 变更文件清单 (MailContentService.kt, IntroductionMailComposer.kt, InitialOutreachService.kt, ManualInitialOutreachService.kt, ManualExpertMailService.kt, V88__rewrite_unsubscribe_line_wording.sql, MailContentServiceTest.kt, IntroductionMailComposerTest.kt, UnsubscribeWordingMigrationTest.kt, ManualExpertMailServiceGateTest.kt). No `docs/plans/2026-08-11/**`, no `knowledge/CLAUDE.md`. Per-commit: 72dbc9a (pause), 249d3dd (stub drop), 8941887 (A1 amendment) all docs-only; 04f8833 (implementation) touches exactly the 10 authorized files. |
|Plan and invariants|PASS|I-1: IntroductionMailComposer.kt:36-44 — `val plain = rendered.body` is the single content source: `body = plainTextToHtml(plain, …)`, `text = plain` (same local var). I-2: exactly 7× `body = mail.text ?: mail.body` at InitialOutreachService.kt:95,105 and ManualInitialOutreachService.kt:695,709,723,747,763; boundary diff adds no `body = mail.body` (only pre-existing out-of-scope hit, see O-1). I-3: MailContentService.kt:9-17 — `escapeHtml` before URL→anchor replace; exact-string `replace(url, …)`; no `Regex("https?://")` and no `href=""` in src/main/kotlin (grep 0 hits). I-4: `filter { it.isNotBlank() }` + `distinct().sortedByDescending`; single-arg overload delegates `plainTextToHtml(plain, emptyList())`; empty-URL test passes. I-5: IntroductionMailComposer.kt:45 `requireNoPlaceholderResidue(mail.subject, plain)` — second arg is `plain`, not `mail.body`; `evaluate(rendered.rawTexts, …)` untouched (line 28). I-6: V88 uses `SET b.custom_text = REPLACE(…)` with `LIKE '%you can unsubscribe here: ${unsubscribeUrl}%'` guard, targets INTRODUCTION/MATERIAL_REMINDER CUSTOM_TEXT only, no `mail_template` write. T-1..T-11 all present: two-arg overload + UNSUBSCRIBE_ANCHOR_TEXT (T-1), MailContentService injection with default (T-2), html/text ComposedMail + plain gate arg (T-3), 7 write-point fixes (T-4/T-5), ManualExpertMailService.kt:243 two-arg call (T-6), V88 migration (T-7), MailContentServiceTest 5 new cases (T-8), IntroductionMailComposerTest 4 new cases incl. messageId shape/thread headers (T-9), UnsubscribeWordingMigrationTest 5 cases asserting V88 source is substring of V87 sentence (T-10), ManualExpertMailServiceGateTest:219 anchored-prefix assertion (T-11). SmtpMailDeliveryServiceTest:133/159/183 header cases + :205/:226/:252 multipart/text cases present unchanged (must-NOT-change 4 and explicit-text-part assertion). |
|Required commands|PASS|All run freshly under JAVA_HOME=zulu-11 (openjdk 11.0.15): (1) focused 6-class `mvn test -Dtest=MailContentServiceTest,IntroductionMailComposerTest,UnsubscribeWordingMigrationTest,UnsubscribeBodyLinkMigrationTest,SmtpMailDeliveryServiceTest,ManualExpertMailServiceGateTest` exit 0 — surefire: 13+12+5+6+24+5 = 65 tests, 0 failures/errors, JS 485/485; (2) full `mvn test` exit 0 — `Tests run: 2309, Failures: 0, Errors: 0, Skipped: 4`, BUILD SUCCESS, 02:12 min; (3) `mvn clean package` exit 0 — BUILD SUCCESS, war built, 02:15 min; (4) `git diff --check` exit 0 (both working tree and boundary range). FlywayMigrationIntegrationTest not run — Docker unavailable, plan-marked default skip (matches implementer's recorded baseline; no divergence). Implementer claims (65 tests exit 0; full 2309/0/0/4 exit 0; BUILD SUCCESS; diff-check clean) reproduced exactly. |
|Downstream interfaces|PASS|Children 07/08 have no code dependency on this child (N/A per brief). `ComposedMail` data class shape untouched in boundary (only constructor default param `mailContentService` appended; `html`/`text` fields pre-existed). `plainTextToHtml` single-arg overload preserved and delegates to `emptyList()` — AutoMailReplyService.kt:570 and PendingMailOperationService.kt:204 remain single-arg callers with 0-line diff, so behavior is byte-identical (I-4 back-compat). |

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1: Pre-existing `body = mail.body` at AutoMailReplyService.kt:977 (sendMeetingInvitation, MEETING_INVITATION path) — plan's I-2 acceptance grep (`grep -rn "body = mail.body" src/main/kotlin` must be 0 lines) finds this 1 hit, but the line is unchanged from base 0482bcd (0-line diff in boundary), the MEETING family is explicitly out of scope in the plan (must-NOT-change 3, 前序 Plan 04 状态不变), and at that call site `ComposedMail` is constructed without `html=true` so `body` is plain text (no HTML-drift risk). Plan wording of the acceptance grep overstates the invariant's scope; not a boundary violation, not authorized for correction.

### Required Action
- COMPLETE_CHILD
