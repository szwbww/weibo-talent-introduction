# Light Verification — child 03

## Light Verification: LIGHT_PASS_WITH_NOTES
Child: 03 and plan path
Boundary: 77f3049..73a7d30
Verifier: Ver03

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
|Authorized scope|PASS|Implementation commit 73a7d30 touches exactly 11 files = amended plan's 11-file list: 5 NEW (V104__create_auto_reply_confidence_log.sql, AutoReplyConfidenceLog.kt, AutoReplyConfidenceLogRepository.kt, AutoReplyConfidenceScorer.kt, AutoReplyConfidenceScorerTest.kt — 4 main + 1 test as the brief expects) and 6 modified (LlmProperties.kt, application.yml, GroundedAutoReplyDecisionService.kt, AutoMailReplyService.kt, GroundedAutoReplyDecisionServiceTest.kt, AutoMailReplyServiceTest.kt). No other files. The 3 remaining files in the 77f3049..73a7d30 range are fast-p docs commits, not implementation: 421ae51 (children/02 fix-log.md + verify-log.md) and 6f2ec3c (03 plan amendment itself); 73a7d30 excludes docs/plans/fast/** as required.|
|Plan and invariants|PASS|I-1: resolveReason()/passesSendGate() bodies zero-diff (no +/− lines touch them; changed-line grep count 0); readyToSend decided ONLY by resolveReason()+passesSendGate() at GroundedAutoReplyDecisionService.kt:74-75; crs/confidence/tier appear in neither method; shadow degradation only at return construction :78-81. I-2: kill-switch `if (!llmProperties.autoReplyEnabled && !llmProperties.shadowScoringEnabled)` :58-59; shadowOnly forces readyToSend=false, reason=AI_AUTO_REPLY_DISABLED :78-81; tests `shadow mode scores but never sends` (asserts false + AI_AUTO_REPLY_DISABLED + confidence!=null) and `both flags off skips generation entirely` (verify(generate, never()) + confidence==null). I-3: AutoMailReplyService.kt:514-537 — decision.confidence?.let { runCatching { save(...) }.onFailure { log.warn("Failed to persist auto-reply confidence log: {}", it.message) } }, placed after decide() :508 and before the readyToSend branch; never throws; A2 regression test `confidence log write failure does not block inbound processing` stubs save() to throw RuntimeException, asserts processSingle returns SinglePipelineOutcome.QA_REPLIED, recorded=true, delivery attempted, save attempted; Mockito.reset(..) at end — sound: no other test stubs this repo's save and the default null return is unused. I-4: entity `createdAt: LocalDateTime` non-nullable (AutoReplyConfidenceLog.kt:37); write path passes `createdAt = LocalDateTime.now()` explicitly (AutoMailReplyService.kt:535). I-5: grep "Repository\|DraftService\|LlmClient" AutoReplyConfidenceScorer.kt → empty; components derive only from AiReplyDraftResult + verifiedRuleIds (pure @Service, constants centralized in companion: 40/25/20/15 max, HISTORY_COLD_START 7.0, weights 1.0/0.6/0.35, factors 1.0/0.75/0.0, MIN/MAX 0/100). I-6: AutoReplyPreviewService not in git diff --stat; grep ConfidenceLog there → empty. V104 DDL matches plan verbatim (all 23 columns, KEY idx_arcl_contact + idx_arcl_created, ENGINE=InnoDB DEFAULT CHARSET=utf8mb4, operator_edited/operator_edit_distance NULL); grep '\${' → empty; migration max 103→104. tier constant "SHADOW" (AutoMailReplyService.kt:533). LlmProperties.shadowScoringEnabled appended at END with default false (LlmProperties.kt:19); application.yml:106 shadow-scoring-enabled immediately after auto-reply-enabled (:105). X-1: exactly 2 production .decide( callers (AutoMailReplyService.kt:508, AutoReplyPreviewService.kt:112). 8 required tests present: scorer 6 (full-grounded 92.0, mixed 25.5, empty requestFacts→0 + finite non-NaN, BLOCKED→consistency 0, empty verifiedRuleIds→evidence 0, clamp [0,100]) + shadow-never-sends + both-off-skips-generation + I-3 save-throws. Declared deviations judged acceptable: (a) default constructor param `= AutoReplyConfidenceScorer()` keeps unlisted AiReplyDraftServiceTest.kt:928-934 positional construction compiling — that file unchanged in range (grep count 0), Spring injects in production; (b) 1-decimal component rounding consistent with DECIMAL(5,2) — see O-1; (c) requestCount==0 guards give 0.0 not NaN, required by test case 3; (d) I-3 test isolation sound as reasoned above.|
|Required commands|PASS|All run fresh with JAVA_HOME=/Library/Java/JavaVirtualMachines/zulu-11.jdk/Contents/Home. mvn test -Dtest=AutoReplyConfidenceScorerTest: exit 0, Tests run: 6, Failures: 0, Errors: 0, Skipped: 0. mvn test -Dtest=GroundedAutoReplyDecisionServiceTest: exit 0, Tests run: 17, Failures: 0, Errors: 0. mvn test -Dtest=AutoMailReplyServiceTest: exit 0, Tests run: 42, Failures: 0, Errors: 0. grep -n '\${' src/main/resources/db/migration/V104__create_auto_reply_confidence_log.sql: no output (exit 1, expected). mvn test: exit 0, BUILD SUCCESS, Tests run: 2583, Failures: 0, Errors: 0, Skipped: 4 (permitted: migrationIt-gated + permanent @Disabled). mvn clean package: exit 0, BUILD SUCCESS (2583/0/0/4). git diff --check (boundary and working tree): clean, exit 0.|
|Downstream interfaces|PASS|Last child: decide() signature unchanged (inboundText, inboundSubject, contact, currentInboundMessageId=null — zero diff on signature); 2-caller invariant intact; `confidence` nullable with default null and disabledDecision passes confidence=null; preview untouched and still counterfactual — single log write point is AutoMailReplyService.kt:516 only, preview path writes nothing. Ledger consistent: row 35 records 03 base 77f3049 head 73a7d30 status LIGHT_VERIFYING, A2 row documents the +1 file amendment; this report completes children/03 evidence.|

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1, deviation (b): components rounded to 1 decimal via oneDecimal() besides CRS. No invariant violated and consistent with DECIMAL(5,2) storage, but in rare n=3 drafts with non-terminating raw components (e.g. coverage 40×1.3/3 = 17.333→17.3, evidence 25×1/3 = 8.333→8.3) the sum of rounded components can differ from the rounded-sum CRS by 0.1 (42.6 vs 42.7), which could trip A-1's manual ±0.05 sum tolerance. Suggest human acceptance allow ±0.1 or recompute from unrounded values. Gate verdict unaffected.

### Required Action
- COMPLETE_CHILD
