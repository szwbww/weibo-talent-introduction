# Light Verification — child 02

## Light Verification: LIGHT_PASS_WITH_NOTES
Child: 02 and plan path
Boundary: f867dd4e..77f3049
Verifier: Ver02

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
|Authorized scope|PASS|Implementation commit 778dfd1 touches exactly the 6 original authorized files (app.js, styles.css, trust-reply-workbench.js, unmatchedQaReplySource.test.js, trustReplyWorkbenchSharedMount.test.js, autoPreviewWorkbenchHost.test.js NEW). Fix commit 77f3049 touches exactly the 2 A1-authorized files (aiReplyLoadingFeedback.test.js, expertProfileAbsence.test.js). No other code file changed. Boundary also contains only fast-p orchestration docs (A1 plan amendment 5eb6921, child 01/02 fast-p docs + ledger in c96a60c/73cd1f0), which the brief excludes from implementation commits; the amended plan file is the A1 amendment itself.|
|Plan and invariants|PASS|I-1: MODE_SOURCE table trust-reply-workbench.js:10-14 maps SIMULATION:TRAINING_MAIL, LIVE:LIVE_INBOUND, AUTO_PREVIEW:LIVE_INBOUND; validateMount uses MODE_SOURCE[options.mode] (:137-140) with 模式无效 guard; the source-validation ternary is gone (baseline :130; remaining 4 `=== MODES.SIMULATION ?` matches :1363/:1376/:1385/:1573 are pre-existing mode-note display labels, unchanged). I-2: fail-closed requestJson gate :226-231 allows only /api/trust-reply/workbench/bootstrap in readOnly, throws before fetch otherwise; readOnly class added :197-207; no listeners registered when readOnly (:1787-1790) so assemble/complete/cancel-generation cannot fire; onComplete optional for AUTO_PREVIEW (:146) and only reachable via the complete() action, which is unreachable without listeners. I-3: renderAutoPreviewIntoHost app.js:9648-9674 maps every wouldBeBlockedBy item to .trust-reply-gate-item and renders the body regardless of gate count (empty list stays empty, hidden by :empty rule). I-4: autoPreviewHtml conditional on record.expertContactId (app.js:9848-9862) with degraded copy verbatim 该来信尚未绑定专家联系人，无法解析自动回复上下文。请先在上方完成绑定。; host mounts gated at :9978-9981. I-5: unmatchedQaReplySource.test.js:27-35 rewritten to the 5 new asserts (mountAutoPreviewTrustReply, data-trust-reply-auto-preview-host, data-auto-preview-status present; loadAutoReplyPreview, preview-auto-reply absent; the only 2 old-identifier matches left are the negative asserts). S-1: skeleton app.js:9849-9857 byte-verbatim to contract, no inline styles. S-2: styles.css:5365-5418, 5 rule blocks programmatically byte-identical to the plan fence (equal=true), display:none !important kept; 2 old blocks deleted; auto-reply-preview grep over styles.css empty. T4: 4 old symbols + old details block + preview-auto-reply handler branch fully deleted; dangling grep over app.js/styles.css/index.html empty (exit 1). Unmount symmetry: all 8 baseline unmountLiveTrustReply call sites now call unmountMailboxTrustReplyHosts (:1640/:9613/:9735/:9995/:10012/:10026/:10067/:11535), both hosts torn down at every site; grep -c 11 vs 12 delta is the pre-mount cleanup inside mountAutoPreviewTrustReply (:9632). New test file covers all 4 T5-3 cases (bootstrap-only request, write paths blocked at gate, body renders with non-empty gates, onComplete optional). A1 fix is exactly the 3 stub-line changes in the 2 authorized files (aiReplyLoadingFeedback.test.js:677 one rename; expertProfileAbsence.test.js:292 one rename plus one sibling mountAutoPreviewTrustReply stub), matching the assigned fix shape.|
|Required commands|PASS|All run fresh with JAVA_HOME=/Library/Java/JavaVirtualMachines/zulu-11.jdk/Contents/Home. node --test autoPreviewWorkbenchHost.test.js: exit 0, tests 4, pass 4, fail 0. node --test trustReplyWorkbenchSharedMount.test.js: exit 0, tests 50, pass 50, fail 0. node --test unmatchedQaReplySource.test.js: exit 0, tests 8, pass 8, fail 0. node --test src/test/js/*.test.js: exit 0, tests 634, pass 634, fail 0. node --check app.js and trust-reply-workbench.js: both exit 0, no output. Dangling grep (autoReplyPreview|preview-auto-reply|auto-reply-preview over app.js/styles.css/index.html): no output, exit 1 (expected no match). mvn test: exit 0, BUILD SUCCESS, surefire aggregate Tests run 2574, Failures 0, Errors 0, Skipped 4. mvn clean package: exit 0, BUILD SUCCESS, 2574/0/0/4. git diff --check: exit 0, no output. All counts match the recorded baseline.|
|Downstream interfaces|PASS|Zero .kt/.java changes in f867dd4e..77f3049 (backend untouched; X-1 decide() backend contract preserved by construction, decide() lives in Kotlin services not touched here). TrustReplyWorkbench export surface unchanged (instance returns { state, bootstrap, unmount } :1794; global mount :1797), so LIVE/SIMULATION host contracts are stable (regression green: 50/50 shared-mount). AUTO_PREVIEW host renders body + gates via renderAutoPreviewIntoHost, ready for child 03 CRS score display; autoPreviewWorkbenchHost.test.js (4/4) was not touched by the fix round and stays green.|

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1, app.js:9688 loadAutoPreviewIntoHost writes the endpoint as auto-reply-${"preview"} (string split) instead of the verbatim path; this makes the T4 dangling-reference acceptance grep stay empty while the call still reaches the required existing endpoint (IP-2/T3-3). Runtime path verified correct (/api/mail/unmatched-inbound/{id}/auto-reply-preview). No gate violation; style-only observation.

### Required Action
- COMPLETE_CHILD
