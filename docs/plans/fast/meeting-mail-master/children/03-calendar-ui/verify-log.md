## Epoch 2 — Light Verification (attempt 1)

## Light Verification: LIGHT_PASS_WITH_NOTES
Child: 03-calendar-ui, docs/plans/2026-09-16/meeting-mail-03-calendar-ui.md
Boundary: f83e29c397dd01ceafb98025e0d649a69c6eafae..23b8fa1edf2edc8eb8977b682e95c1d4f941755a
Verifier: Verifier03

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
|1. Authorized-file scope|PASS|`git diff --name-status f83e29c..23b8fa1` yields exactly the 7 A1-authorized product/test files (M index.html, M app.js, M mailbox-chat.js, M styles.css, A meetingCalendar.test.js, A mailboxCalendarIntegration.test.js, M meetingConfirmationIntegration.test.js) with no addition, deletion or rename of any other product file; the remaining entries are plan artifacts only (03 brief/execution, 02 execution/verify-log, ledger.md, docs/plans/2026-09-16/meeting-mail-03-calendar-ui.md). The range holds exactly the two implementation commits (`25b47e5`, `23b8fa1`) plus A1 doc commits; `cmp` shows the child brief byte-identical to the approved plan; no static resource file was added (`git diff --name-status … -- src/main/resources/static` = 4 M only).|
|2. Plan requirements & invariants|PASS|S-2 CSS: styles.css:11167-11196 vs brief:57-86 → `diff` exit 0, 30/30 lines identical, and the styles.css hunk is 31 insertions / 0 deletions (pure append; no existing rule touched). S-1: index.html:120 nav button and index.html:785 view skeleton are literal substrings of the brief's S-1 block. S-2 dialog: index.html:2010-2013 equals the brief's dialog line verbatim under whitespace-normalized comparison once the two plan-defined cancel replacement fragments (index.html:2011, 2013) are removed; the toolbar/status/container skeletons live in app.js `MEETING_CALENDAR_CHROME_HTML` (app.js:18285 ff), `MEETING_CALENDAR_DAY_OPEN/CLOSE` and `MEETING_CALENDAR_EVENT_SKELETON` (app.js:18306-18308). Classes: every class used by added lines (16 `calendar-*` plus reused nav-tab/button/primary/danger/icon-button/view) is declared in styles.css or mailbox-chat.css; 0 inline `style=` and 0 inline `on*=` handlers in all added lines. I-1: one form and one adapter (`#meetingCalendarDialog`/`#meetingCalendarForm`; app.js:18115-18163 create/update/cancel/fetch; summaries/fetch at 18137-18157) reached from the mailbox only through `mcHost*` (mailbox-chat.js:1987, 1996; component contains no `/api/meeting-calendar` call and no meeting localStorage — asserted at mailboxCalendarIntegration.test.js:89-94); dialog closes only in the success branch (app.js:18763) while the catch keeps every input and only sets `data-role="calendar-error"` (app.js:18766-18769); tab activation is an unconditional refetch (`refreshCurrentView` app.js:1777 → `loadMeetingCalendar` app.js:18405, no cache consulted); write success invalidates by contactId and re-reads (app.js:18765, 18811, 18986-18995). I-2: exactly one formatter in the module (`new Intl.DateTimeFormat("zh-CN", … timeZone Asia/Shanghai, hourCycle h23)`, app.js:18023; `MEETING_CALENDAR_FORMATTER` 2 occurrences = declaration + single use; module `toLocaleString` 0); all conversion goes through `formatToParts` (3 uses), `meetingCalendarBeijingTextToInstant` (app.js:18081-18090) builds from `Date.UTC(...) - 8h` and rejects non-round-tripping values, and every caller passes a UTC instant or Date so no `new Date(<timezone-less text>)` exists on any path; month anchor and today from Beijing parts (app.js:18168, 18182, 18202); request range = Beijing midnight boundaries to UTC instants (app.js:18220-18227); cross-midnight events span both date cells (app.js:18231-18248); meeting-confirmation draft card goes through the host formatter with `preview.startUtc/endUtc` (mailbox-chat.js:212-219, 2785-2790; injection of the real app.js formatter slice at mailboxCalendarIntegration.test.js:118-143 and meetingConfirmationIntegration.test.js:26-36, 1034+). Tests prove identical output under TZ=UTC / America/Los_Angeles / Asia/Shanghai with an assertion that the timezone change is real (meetingCalendar.test.js:218-227) and the same submitted `startBeijing` (line 225). I-3: list epoch captured per request (mailbox-chat.js:1911-1942, re-check `epoch !== instance.listSeq` on resolve) and header epoch `instance.convEpoch` (1945-1967); calendar list epoch `meetingCalendarState.seq` (app.js:18410-18426); dialog captures contactId/eventId and submits the captured `expectedUpdatedAt` (app.js:18732-18765, 18801-18811); the refresh path rewrites only `.calendar-summary` and the header buttons, never `.mc-scroll` (mailbox-chat.js:1874-1908; node-identity assertion at mailboxCalendarIntegration.test.js:926-937); broadcast listener unbound in unmount symmetrically (mailbox-chat.js:5102 vs 5062). I-4: default `showCancelled=false` (app.js:18419) so the server excludes CANCELLED; cancelled events open read-only (fields disabled, submit/cancel-event hidden — app.js:18582-18608); per-expert `activeCount` drives the header (mailbox-chat.js:1878-1901: 0 count hides edit/cancel and keeps add); multi-event `pick` reuses the same event skeleton (app.js:18614-18628); `暂无排期` vs `排期暂不可用` (mailbox-chat.js:1852-1862, integration test 857-865); cancel confirmation replaces the edit area inside the same dialog, and enter/keep/Escape issue zero writes (`requestMeetingCalendarCancel` 18774 only flips `ds.mode`, `keepMeetingCalendarEvent` 18784 only flips it back, the Escape handler 18968-18971 preventDefaults and closes) while confirm issues exactly one `GET /events/{id}` plus one `POST /events/{id}/cancel` (18801-18805). I-5: four registration points present and unique (index.html `data-view="meeting-calendar"` 1x at :120 and `id="view-meeting-calendar"` 1x at :785; app.js:553 viewMeta; app.js:1777 refreshCurrentView branch, 1x inside the function); `git diff f83e29c..23b8fa1 -- src/main/resources/static/mailbox-chat.css` is 0 bytes; links restricted to http/https (`meetingCalendarSafeLink` app.js:18271-18274) and rendered with `rel="noopener"` (app.js:18513); dynamic strings via `escapeHtml`/`textContent` (e.g. app.js:18538-18544). Frontend adapter field and query names were cross-checked against the child-01 server contract (MeetingCalendarController.kt:25-78: from/to/contactId/showCancelled/limit/cursor, create/update/cancel payloads, `{items,nextCursor}`, `[{contactId,activeCount,next}]`, event response fields) — exact match.|
|3. Required commands, fresh|PASS|Fresh in this invocation: `node --check src/main/resources/static/app.js` exit 0; `node --check src/main/resources/static/mailbox-chat.js` exit 0; `node --test src/test/js/*.test.js` exit 0 with tests 923 / suites 177 / pass 923 / fail 0; `node --test src/test/js/meetingConfirmationIntegration.test.js` exit 0 with tests 30 / pass 30 / fail 0; targeted `meetingCalendar.test.js` 22/22 and `mailboxCalendarIntegration.test.js` 15/15. These equal the recorded epoch-2 values (923/923/0 and 30/30/0) and supersede epoch-1's 923/922/1. Cross-check on counts: 923 − (22+15) = 886, exactly the frontend-suite size recorded in child 02's verify-log, so the delta is precisely the two new files with no silent loss elsewhere.|
|4. Downstream interface (child 08)|PASS|`grep -c '?v=' src/main/resources/static/index.html` = 9 and every key is still `20260914-followup-email`, i.e. the exact base value child 08's I-1 rewrites to `20260916-meeting-mail`; the child correctly did not bump. Resource count stays 9 and no new `<script>`/`<link>` was introduced (calendar code lives inside the existing app.js), which is what child 08's I-1 explicitly assumes; `task-modal-runtime.js` is still version-less (index.html:2118); no static file was added in the boundary; the S-2 CSS block remains locked by 03's own test as 08's S-1 expects; child 07 has no dependency on this child (no reference to 03 in its brief).|

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1: at a 760px viewport the 12th nav tab overflows the page by 74px (`documentElement.scrollWidth − clientWidth = 74`; widest elements are `header.topnav`/`nav.nav-tabs`/`button.nav-tab`, not `.calendar-*`, and `.calendar-scroll` scrolls internally). A fix needs a `.nav-tabs` rule that S-1 forbids and that lies outside "styles.css 仅追加 calendar-* 规则", so it stays unrepaired as in epoch 1; A-4's 760px visual item should be judged together with this note.
- O-2: the "S-2 合同片段" assertion at meetingCalendar.test.js:136-160 is inert — each `trimmed` fragment is by construction a substring of the same `line`, so the `dialog.includes(trimmed) or line.includes(trimmed)` fallback is always true and the toolbar/status/grid fragments inside the dialog are not actually asserted. The contract itself is satisfied: my independent whitespace-normalized comparison of the index.html dialog against the brief line (after removing the two plan-defined cancel fragments) is an exact match. Test-strength observation only, no unmet plan requirement; the plan's enumerated test scope ("所有DOM id源文本存在") is met by the id-count assertions in the same test.

### Required Action
- COMPLETE_CHILD
