## Epoch 1 — Light Verification (attempt 1)

## Light Verification: LIGHT_PASS_WITH_NOTES
Child: 04-attachment-storage, docs/plans/2026-09-16/meeting-mail-04-attachment-storage.md
Boundary: 23b8fa1edf2edc8eb8977b682e95c1d4f941755a..2540a0665cd1eff406bec460e56b75fced929cbf
Verifier: Verifier04

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
|1. Authorized-file scope|PASS|`git show --stat 2540a06` = exactly the brief's 10 authorized files (A V126__create_outbound_mail_attachment.sql, A OutboundMailAttachment.kt, A OutboundMailAttachmentRepository.kt, A OutboundAttachmentModels.kt, A OutboundAttachmentService.kt, A OutboundAttachmentController.kt, M application.yml, A OutboundAttachmentServiceTest.kt, M FlywayMigrationIntegrationTest.kt, M GlobalExceptionHandler.kt), 1639 insertions / 14 deletions, no rename or deletion. The range's only other commit a7aaefb touches 3 plan artifacts (03-calendar-ui/execution.md, 03-calendar-ui/verify-log.md, ledger.md) and zero product/test files, so no product or test file outside the authorized list is modified. `cmp` brief vs docs/plans/2026-09-16/meeting-mail-04-attachment-storage.md = exit 0 (byte-identical). V126 is the only new migration and the highest version present (V119..V126).|
|2. Plan requirements & invariants|PASS|I-1 repository exposes only INSERT + SELECT (OutboundMailAttachmentRepository.kt:27,53-70; no UPDATE/DELETE statement); all file writes confined to `basePath/outbound` (OutboundAttachmentService.kt:60,239-243); grep over the 5 new main files finds no mail_attachment / expert_document / SMTP / scheduling / conversation-state write outside comments. I-2 `existsById` gate (Service.kt:56-58); identity read only from `AuthSessionKeys.USERNAME` (Controller.kt:80-82) and no operatorName parameter exists in any signature; same-contact + same-uploader enforced on draft download (Service.kt:110-112), send resolution (Service.kt:141-143) and loadSnapshots (Service.kt:160-163); client submits ids only (Service.kt:131-135). I-3 constants models:15,18,21 = 10MiB/20MiB/10; streamed counter with immediate 413 before the offending write plus `finally` temp delete (Service.kt:81-99); duplicate ids and >10 ids -> 400 (Service.kt:200-208); total >20MiB -> 413 (Service.kt:210-212); filename last path segment + control-char strip + 255-char cap keeping the extension (models:216-228,258-264); MIME restricted to CRLF-free `type/subtype` else application/octet-stream (models:236-248); sha256/byteLength over the original bytes (Service.kt:88-96,176). I-4 temp file -> ATOMIC_MOVE to `outbound/<UUID>` -> metadata insert, DB failure deletes the file (Service.kt:82-124); read re-verifies realpath inside the outbound root, regular file, size, hash (Service.kt:227-247) with 404 for missing and 409 for corrupt; response forced `attachment` + UTF-8 filename + nosniff + `private,no-store` (Controller.kt:63-76); yml sets multipart max-file-size 10MB / max-request-size 11MB (application.yml:15-22). I-5 snapshot = schemaVersion,id,filename,contentType,byteLength,sha256 with no path/bytes/username (models:76-83); strict codec rejects empty list, unknown schema, unknown field, trailing tokens, illegal id/name/type/sha, over-count and over-total (models:141-197); test asserts the JSON contains neither storageRoot nor OWNER. GlobalExceptionHandler diff adds only OutboundAttachmentException -> its fixed 400/404/409/413 and MaxUploadSizeExceededException -> 413 (plus 2 imports and 2 private constants); ResponseStatusException is not generalized and no other handler changed. Test anchors: OutboundAttachmentServiceTest.kt (28 tests) covers each of these with a real temp dir, real advice and session-bearing MockMvc.|
|3. Required commands, fresh|PASS|Fresh `JAVA_HOME=/Library/Java/JavaVirtualMachines/zulu-11.jdk/Contents/Home mvn test -Dtest=OutboundAttachmentServiceTest` -> EXIT=0; freshly written surefire report (10:12:18) `Tests run: 28, Failures: 0, Errors: 0, Skipped: 0` - identical to the recorded 28 run / 0 fail / 0 error / 0 skipped. Fresh `... mvn test -Dtest=FlywayMigrationIntegrationTest -DmigrationIt=true` -> EXIT=1, `Tests run: 1, Failures: 0, Errors: 1`, `DockerClientProviderStrategy ... Could not find a valid Docker environment ... NoSuchFileException (/var/run/docker.sock)`, `java.lang.IllegalStateException: Docker is required for Flyway migration tests`; independent `docker info` reproduces `dial unix /Users/lukai/.orbstack/run/docker.sock: no such file or directory`. This is the same environment blocker the implementer recorded as untested, so the V126 version/DDL assertions in FlywayMigrationIntegrationTest remain unexecuted; no migration assertion, historical target or test body was altered beyond the 125->126 sweep (13 assertions at lines 62..1090, all "126", no stray "125" left in src/main or src/test).|
|4. Downstream interfaces 05/06/07|PASS|`POST /api/mail/conversations/{contactId}/outbound-attachments` (multipart `file`, 201 body = id,filename,contentType,byteLength,sha256,downloadUrl) and `GET /api/mail/conversations/{contactId}/outbound-attachments/{id}/download` implemented verbatim (Controller.kt:40-76; response class models:98-105); `resolveForSend(contactId, attachmentIds, authenticatedUsername): OutboundAttachmentFileSet` returns immutable bounded `files` + order-preserving `snapshots` with all bounds enforced before bytes are read (Service.kt:131-146,199-216); `MAX_FILE_BYTES=10MiB`, `MAX_TOTAL_BYTES=20MiB`, `MAX_FILES=10` (models:15-23) and `OutboundAttachmentSnapshotCodec` in OutboundAttachmentModels.kt (models:114-199) are exactly the names/values the brief fixes. Cross-checked against the 05/06/07 briefs: 05 I-3's "04 snapshot + ByteArray" payload is `OutboundMailFile(snapshot, bytes)` (models:86-89) and 05 I-1/I-2 call the codec; 06 I-2's metadata-only ownership-checked batch compare (not requiring originals online) is `loadSnapshots` (Service.kt:155-166) and 06 I-4's sent download "does not require the original uploader" is `resolveForMessageDownload` (Service.kt:172-173) - 06's change list does not include OutboundAttachmentService.kt, so this surface must exist here, and 06 I-2 reuses the 409 mapping of `OutboundAttachmentException.conflict` (models:56). 07 references no 04 interface beyond a new JS test file.|

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1: `OutboundAttachmentSnapshotCodec.parseOrThrow` treats blank (`""`/whitespace) exactly like SQL NULL (`json.isNullOrBlank()` -> null, OutboundAttachmentModels.kt:152-154; pinned by `assertNull(...parseOrThrow(""))` in OutboundAttachmentServiceTest). The brief's I-5 and child 05's I-1 name SQL NULL as the only "no generic attachments" form, so a future 05 regression writing `""` would read back as "no attachments" instead of failing closed with 409. No producer in this child can write that column (it is added by 05) and no 04 acceptance item requires blank to be rejected, so this stays outside the light gate with no fix.
- O-2: an over-wide login identity is rejected with `IllegalStateException` (OutboundAttachmentService.kt:250-255), which the pre-existing `handleIllegalState` maps to HTTP 400 BAD_REQUEST, whereas plan T1 calls it a configuration error; the required substance (reject, never truncate into another user, no file, no metadata - asserted by `upload rejects unknown expert and over wide operator identity without writing`) is implemented and the plan fixes no status code for it.

### Required Action
- COMPLETE_CHILD
