## Epoch 3 — Light Verification (attempt 1)

## Light Verification: LIGHT_PASS_WITH_NOTES
Child: 01-calendar-api, docs/plans/2026-09-16/meeting-mail-01-calendar-api.md
Boundary: 24f5c8205a304d3682e09e02458960bc2caa0463..906f241cf685145d70c7917bb8b950f69c711835
Verifier: Verifier01

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
|1 — Changed product/test files inside Authorized Files|PASS|`git diff --name-only 24f5c82..906f241 -- src/` returns exactly the 8 authorized paths: `V125__create_meeting_calendar_event.sql`, `MeetingCalendarEvent.kt`, `MeetingCalendarEventRepository.kt`, `MeetingCalendarService.kt`, `MeetingCalendarController.kt`, `MeetingCalendarServiceTest.kt`, `MeetingCalendarControllerTest.kt`, `FlywayMigrationIntegrationTest.kt`. `git show --stat 906f241` = 8 files changed, 2210 insertions(+), 14 deletions(-); the implementation commit carries no `docs/plans/**` path. No 9th src file touched anywhere in the boundary.|
|2 — Child-plan requirements and key invariants have direct code/test evidence|PASS|I-1: `V125__...sql:13` `UNIQUE KEY uk_meeting_calendar_source_mail (source_mail_record_id)` plus `fk_meeting_calendar_contact`/`fk_meeting_calendar_source_mail` RESTRICT; `MeetingCalendarService.kt:33` createManual leaves `sourceMailRecordId` null; `:64` createFromSentMail returns the pre-existing row via `existingSourceFor` instead of overwriting; evidence tests `createFromSentMail is idempotent per source mail and never overwrites the original row`, `createFromSentMail refuses a source mail already linked to another expert`, mysqlIt `the same source mail keeps exactly one row even under concurrency`. I-2: repository `getUtcInstant` parses the DATETIME text with `ZoneOffset.UTC` (JVM-default-tz independent) and `Instant.toUtcLocalDateTime()` is used for writes; service `parseBeijing` = `Asia/Shanghai` over a STRICT `uuuu-MM-dd'T'HH:mm` formatter; `validateInput` enforces `endUtc > startUtc`; controller emits `Instant.toString()` (Z-suffixed) for startUtc/endUtc/createdAt/updatedAt and exposes `updatedAt` as the opaque version; evidence `UTC conversion does not depend on the JVM default timezone`, mysqlIt `Beijing schedule is stored as UTC and read back with Z regardless of the JVM timezone` (asserts raw column text `2026-09-18 02:00:00`). I-3: `cancel` at `:200` returns the stored row for an already-CANCELLED event before touching version or reason; `update` at `:170` rejects CANCELLED with 409 after the version compare; `nextVersion` = `max(now, previous+1µs)`; an unchanged reschedule returns the row without issuing UPDATE; V125 ships no delete path (row retention); evidence `repeated cancellation returns the current row without overwriting the original reason`, `rescheduling bumps the version strictly above the previous one`, mysqlIt `stale version conflicts while cancel is terminal and keeps the original reason`. I-4: `MeetingCalendarService` depends only on the calendar repository and `ExpertContactRepository.existsById`; no SMTP / `ConversationStateService` / operator_status write anywhere in the child (grep for `SmtpMailDelivery|ConversationStateService|MailDeliveryService` over the 3 new main files = no hits); the `updateMutable` and `cancel` SQL never touch `expert_contact_id`/`source_mail_record_id`; evidence `calendar writes reach only the calendar repository and never expert state` (`verifyNoMoreInteractions`), mysqlIt `rescheduling and cancelling never touch the source mail or expert rows` (body cal pipe calendar_attachment_json snapshot, current_status, operator_status, status-history count). I-5: repository `list` uses `e.starts_at_utc < :toUtc AND e.ends_at_utc > :fromUtc`, `ORDER BY e.starts_at_utc ASC, e.id ASC LIMIT :limit` with `limit+1`; service `MAX_RANGE = 62 days`, `limit in 1..200`, cursor filter equality (`cursor does not match filters`), `summaries` at most 100 ids / zero-fill / nearest-future-else-nearest-past / request order; controller `limit` default 100; evidence mysqlIt `a cross-midnight event intersects both Beijing days and nothing else`, `201 rows are read completely in two cursor pages and limit above 200 is rejected`, controller-mysqlIt `calendar range intersects Beijing days and paginates through the cursor`.|
|3 — Required commands run freshly, counts vs recorded baseline|PASS (1 sub-check unverified, environment)|cmd1 `mvn test -Dtest=MeetingCalendarServiceTest,MeetingCalendarControllerTest` → exit 0, `BUILD SUCCESS`, surefire `Tests run: 18` + `Tests run: 6` = 24 passed, 0 failures/errors/skips; baseline was exit 1 (no matching tests existed), so the new coverage is confirmed. cmd2 `mvn test -DmysqlIt=true -Dtest=MeetingCalendarServiceMysqlTest,MeetingCalendarControllerMysqlTest` → exit 0, `BUILD SUCCESS`, surefire `Tests run: 7` + `Tests run: 3` = 10 passed, 0 failures/errors/skips (matches the implementer report). cmd3 `mvn test -Dtest=FlywayMigrationIntegrationTest -DmigrationIt=true` → exit 1, `Tests run: 1, Errors: 1` at `FlywayMigrationIntegrationTest.startMysql` with `IllegalStateException: Docker is required for Flyway migration tests`; root cause is the pre-declared environment fact `BadRequestException (Status 400: {"message":"client version 1.32 is too old. Minimum supported API version is 1.40, ..."})` from testcontainers' bundled docker-java. Recorded as unverified, not reclassified as a code defect (see O-1).|
|4 — Downstream interfaces for later children match the brief|PASS|`MeetingCalendarService.kt:63-64` `@Transactional(propagation = Propagation.MANDATORY)` on `fun createFromSentMail(record: MailRecord, input: MeetingCalendarInput): EventRow`. `MeetingCalendarEvent.kt:26-30` `data class MeetingCalendarInput(val startUtc: Instant, val endUtc: Instant, val meetingLink: String? = null)` is declared in the domain file as required. There is no public mark-as-sent endpoint: the controller exposes exactly GET /events, GET /events/{id}, GET /summaries, POST /events, PUT /events/{id}, POST /events/{id}/cancel. `MeetingCalendarService.kt:310` `class MeetingCalendarConflictException` is handled by `MeetingCalendarController.kt:80-84` `@RestControllerAdvice(assignableTypes = [MeetingCalendarController::class])` + `@Order(Ordered.HIGHEST_PRECEDENCE)` `@ExceptionHandler` returning 409 `ApiErrorResponse("CONFLICT", ...)`; `ResponseStatusException` has zero occurrences in the child files, so the 500 mapping in `GlobalExceptionHandler` is never reached for calendar conflicts. 02's repeat-send expectations hold: a repeated `createFromSentMail` for the same source mail returns the stored row unchanged, including a CANCELLED one, so a manual reschedule or a cancel is neither overwritten nor revived.|

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1 — `mvn test -Dtest=FlywayMigrationIntegrationTest -DmigrationIt=true` cannot execute on this machine: testcontainers' bundled docker-java negotiates Docker API 1.32 while the local OrbStack daemon requires at least 1.40 (`BadRequestException (Status 400: {"message":"client version 1.32 is too old. Minimum supported API version is 1.40, please upgrade your client to a newer version"})`), aborting in `@BeforeAll startMysql()` before any migration assertion. The new `V125 creates the structured calendar table and constraints` assertions therefore remain unverified by their own command. This is the pre-declared environment limitation, not a four-gate violation; the same V125 fact was confirmed independently against the live MySQL at 127.0.0.1:3306 (`flyway_schema_history` row installed_rank 124 / version `125` / description `create meeting calendar event` / success 1, and `SHOW CREATE TABLE meeting_calendar_event` returns exactly the brief's column/index/FK spec: `starts_at_utc`/`ends_at_utc`/`created_at`/`updated_at` datetime(6), `meeting_link` varchar(1024), `note`/`cancel_reason` varchar(200), `status` varchar(16) NOT NULL DEFAULT 'ACTIVE', `uk_meeting_calendar_source_mail`, both `idx_meeting_calendar_*` indexes, both RESTRICT foreign keys). Fixing the Docker client version is not a plan-authorized change.
- O-2 — `git diff --check` over the whole review boundary exits 2, against the recorded pre-child baseline of exit 0. Both whitespace hits are `new blank line at EOF` in seeded plan documents outside this child — `docs/plans/2026-09-16/meeting-mail-05-attachment-delivery.md:91` and `docs/plans/2026-09-16/meeting-mail-07-attachment-ui.md:155` — both introduced by `59e9090 docs(fast-p): seed meeting-mail plans`, which precedes the child-01 implementation commit. Child 01's own range `0f5b75f..906f241` is clean (`git diff --check` exit 0). Documentation whitespace is outside the four light gates and neither file is in the child's authorized list.

### Required Action
- COMPLETE_CHILD
