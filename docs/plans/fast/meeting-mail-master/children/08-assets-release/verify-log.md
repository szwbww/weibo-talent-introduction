## Epoch 1 — Light Verification (attempt 1)

## Light Verification: PAUSE
Child: 08-assets-release, docs/plans/2026-09-16/meeting-mail-08-assets-release.md
Boundary: ef836c3e13f57bc14696318ec0f8a5c89ab06874..8c91c656d6530972ff8fc242705c928715c5b4d5
Verifier: Verifier08

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
|1 — Authorized files only + key-only diff|PASS|`git show --stat 8c91c65` = **exactly the 10 Authorized Files** (`src/main/resources/static/index.html` + the 9 `src/test/js/*.test.js` named in the brief; 28 insertions / 28 deletions, no new file). Range `git log --oneline ef836c3..8c91c65` = `8c91c65` (child 08 implementation) + `da60feb` (child 07 light-verification records, docs-only). Key-only proof per file: `git show ef836c3:<f> \| sed 's/20260914-followup-email/@@K@@/g' \| md5sum` == `git show 8c91c65:<f> \| sed 's/20260916-meeting-mail/@@K@@/g' \| md5sum` → **10/10 `KEY-ONLY-OK`** (index.html 2125 lines at both ends; no line added/removed). `git grep -l '20260914-followup-email' 8c91c65 -- src` → 0 files; `git grep -l '20260916-meeting-mail' 8c91c65 -- src` → exactly those same 10 files; at run base `24f5c82` the old key hit exactly index.html + those 9 tests (no other src carrier existed, so the plan's file list is complete). Brief ≡ approved plan (`shasum -a 256` both `3a1aa8fcfef47ea9e293a9ce6ffa0e80a124aeab5001c6654e020734cdf3472b`).|
|2 — I-1 / I-2 / S-1 direct evidence|PASS|**I-1**: `grep -o '?v=[A-Za-z0-9._-]*' index.html \| sort \| uniq -c` → `9 ?v=20260916-meeting-mail` (count 9, single key value); `task-modal-runtime.js` still version-less at `index.html:2118` and is the **only** unversioned `<link>/<script>` static reference; the 9 fixed-key tests contain 0 old-key hits; fresh `node --test src/test/js/*.test.js` → exit 0, tests 944 / suites 183 / pass 944 / fail 0 / skipped 0. **I-2**: the md5-normalization proof above shows the stage diff is key-only (no DOM, style, script order, resource-count or business change); the child commit contains no `.kt`, `.sql`, `.css` or JS source change; the S-1 verbatim 9-line block is byte-equal (after stripping the markdown indent of the plan block) to the plan's target, in the same relative order (links `index.html:11-14`, scripts `:2119-2123`, `indexOf` monotonic; `task-modal-runtime.js` still precedes the 5 scripts, as at base `ef836c3`). **S-1**: same block + all 9 referenced assets plus `task-modal-runtime.js` exist under `src/main/resources/static/`; zero diff outside index.html and the 9 tests; no inline style/class/DOM change.|
|3 — Required commands freshly re-run + attribution|FAIL|All four commands re-run now (JDK 11 zulu-11; see table below) and all four counts reproduce the implementer's record exactly. `node --test` exit 0 and the mysqlIt group exit 0. **`mvn test` exit 1 (3452/1/4/13) and Flyway IT exit 1 (23/0/1/0).** The 5 `mvn test` failures are a **regression from this run's commit `82a46dc32d50cbc352165656842417bc0a569f2` (child 06)** — the guard pins of `OperatorStatusWriteSeamGuardTest.kt` matched exactly at the run base and were invalidated only by that commit; the Flyway error is **pre-existing** (base commit `6ab8eb3`, 2026-09-11). Per the rubric an earlier-children regression in a required command is a gate-3 FAIL, and neither correction fits inside child 08's 10-file authorization → PAUSE.|
|4 — Downstream interfaces (terminal child)|PASS|No product contract was altered by the key swap: child 08's entire commit is index.html + 9 JS test files and is provably key-only (gate 1/2 evidence); no `<link>`/`<script>` was added, removed or reordered; all 9 versioned assets and `task-modal-runtime.js` still resolve to existing files; no Kotlin/API/DTO/DOM/CSS change exists anywhere in `8c91c65`; the fresh 944-case JS suite — which asserts the trust-reply workbench mount contract, mailbox-chat behavior, RAG page registration order and the 9-key/9-resource contract — passes. This is the terminal child (no downstream consumer), so the only downstream surface is the browser's 9 asset URLs, whose paths and relative order are unchanged.|

#### Required commands (fresh, JDK 11; Flyway/Docker with `DOCKER_API_VERSION=1.44`)

|#|Command|Exit|Fresh counts|Recorded|Match|
|---|---|---|---|---|---|
|1|`node --test src/test/js/*.test.js`|0|tests 944 / pass 944 / fail 0 / skipped 0 (suites 183)|944/944/0|yes|
|2|`JAVA_HOME=…/zulu-11… mvn test`|1|Tests run: 3452 / Failures: 1 / Errors: 4 / Skipped: 13|3452/1/4/13|yes|
|3|`DOCKER_API_VERSION=1.44 … mvn test -Dtest=FlywayMigrationIntegrationTest -DmigrationIt=true -Dapi.version=1.44`|1|Tests run: 23 / Failures: 0 / Errors: 1 / Skipped: 0|23/0/1/0|yes|
|4|`… mvn test -Dtest=MeetingCalendarServiceTest,MeetingCalendarControllerTest,MailboxConversationControllerTest,MailboxConversationRepositoryIT,AttachmentTransferWorkerIT,ImapMetadataFetchIT,MeetingCalendarSendIntegrationTest -DmysqlIt=true -DfailIfNoTests=false`|0|Tests run: 105 / Failures: 0 / Errors: 0 / Skipped: 0 (18+6+27+21+17+9+7)|105/0/0/0|yes|

- Command 2's 13 skipped are the environment-gated `mysqlIt`/`migrationIt` classes (verified carriers: 7 classes / 8 `@EnabledIfSystemProperty(named = "mysqlIt"…)` annotations + 10 `migrationIt` carriers); they are **not** "passed" by command 2 and are exercised by commands 3–4.
- Command 4 confirms the 7 classes are the complete `mysqlIt` carrier set (`grep -rn 'EnabledIfSystemProperty(named = "mysqlIt"' src/test`), matching the implementer's enumeration.

#### Failure attribution (Git evidence, no baseline re-run)

**F-1 `campaign.OperatorStatusWriteSeamGuardTest` — caused by this run's `82a46dc` (child 06).**
- Fresh symptom: `OperatorStatusWriteSeamGuardTest.operator_status write sites exactly match whitelist:149` → `expected: <[ExpertOperatorStatusService.kt, ManualInitialOutreachService.kt]> but was: <[…, UnmatchedInboundMailController.kt]>`, with violations `UnmatchedInboundMailController.kt:221: operatorStatus = request.operatorStatus,` and `:1137: operatorStatus = operatorStatus,`.
- Base check: at run base `24f5c82` **all 10 `EXCLUDED_NOISE_SITES` pins matched exactly** (mechanically re-evaluated `path`+`line`+`context` against `git show 24f5c82:src/main/kotlin/…`); at HEAD exactly 2 are stale (`219→221`, `1125→1137`). The guard test file itself has zero commits in `24f5c82..8c91c65`.
- Cause: `82a46dc32d50cbc352165656842417bc0a569f2` is the **only** commit in `24f5c82..8c91c65` touching `UnmatchedInboundMailController.kt` (`git log --oneline 24f5c82..8c91c65 -- …/UnmatchedInboundMailController.kt` → `82a46dc` only); its hunks `@@ -250,7 +252,10 @@` and `@@ -274,9 +279,16 @@` insert the `servletRequest` parameter plus the `attachmentIds`/`authenticatedUsername` forwarding arguments, shifting the two pinned lines. The drift removes those two lines from the exclusion set, so they enter `violations` → `hitFiles` gains `UnmatchedInboundMailController.kt` → the whitelist assertion at `:149` fails (the stale-exclusion self-check at `:152` would fail next). Child 08's `8c91c65` touches neither the controller nor the guard, so it cannot be the cause.
- Smallest correction: refresh only the two pins `219 → 221` and `1125 → 1137` (line numbers in the `NoiseSite(…)` rows plus their comment) in `src/test/kotlin/com/weibo/talentintroduction/campaign/OperatorStatusWriteSeamGuardTest.kt` — precedent A5/A6/A7 and `K-line-number-guard-breaks-on-any-insertion`. **That file is NOT inside child 08's 10-file authorization → plan amendment required.**

**F-2 `mail.controller.UnmatchedInboundTrustWorkbenchTest` — caused by this run's `82a46dc` (child 06).**
- Fresh symptom: 4 errors `java.lang.InvalidUseOfMatchersException: 23 matchers expected, 21 recorded` in the 4 cases (`…propagates 422 for blocking content` / `…409 for delivery unknown` / `…503 for safe retry` / `…with dedup returns SENT…`, stub sites `UnmatchedInboundTrustWorkbenchTest.kt:227/288/…`).
- Cause: the same `82a46dc` hunk `@@ -171,7 +182,12 @@` on `PendingMailOperationService.kt` appends exactly 2 trailing parameters to `sendManualRichReply` (`attachmentIds: List<String> = emptyList()`, `authenticatedUsername: String? = null`; base signature ends at `previewAttachmentSha256`, head has those 2 extra), while `UnmatchedInboundTrustWorkbenchTest.kt` has **zero commits** in `24f5c82..8c91c65` → its positional Mockito stubs still record 21 matchers for a 23-parameter call. Child 08's `8c91c65` cannot be the cause (no `.kt` in the commit).
- Smallest correction: append 2 trailing `Mockito.any()` matchers to each of the 4 `sendManualRichReply` stubs in `src/test/kotlin/com/weibo/talentintroduction/mail/controller/UnmatchedInboundTrustWorkbenchTest.kt` — same mechanical shape as HUMAN-authorized amendments A3 (`docs/plans/fast/2026-09-02-execution-order`) and A1 (`docs/plans/fast/meeting-confirmation`). **That file is NOT inside child 08's 10-file authorization → plan amendment required.**

**F-3 `campaign.repository.FlywayMigrationIntegrationTest` — pre-existing base defect (`6ab8eb3`, before the run), not caused by this run.**
- Fresh symptom: exit 1, 23/0/1/0; `V124 allows material attached promotion audit trigger:233->execute:1216` → `java.sql.SQLIntegrityConstraintViolationException: Cannot add or update a child row: a foreign key constraint fails (talent_introduction.expert_application_promotion, CONSTRAINT fk_eap_contact FOREIGN KEY (expert_contact_id) REFERENCES expert_contact (id))`. The version assertion on the preceding line (`:226`, now `"127"`) passed, so the DB reached v127 and only the INSERT failed.
- Provenance: the failing method — including the `INSERT … VALUES (1, '0000-0000-0000-0001', 'MATERIAL_ATTACHED', 'PENDING')` with no seeded `expert_contact` — was introduced by base commit `6ab8eb318bad141e6927f234e95eb4f2df17a2eb` (`fix: preserve inbound replies when promotion audit fails`, 2026-09-11); `git log -S'V124 allows material attached promotion audit trigger'` returns only that commit, and `git merge-base --is-ancestor 6ab8eb3 24f5c82` → YES (it predates the run base).
- The run did not touch the chain: no commit in `24f5c82..8c91c65` matches `-S'fk_eap_contact'`, `-S'expert_application_promotion'`, or the `V15__…promotion_audit.sql` / `V124__widen_…triggered_by.sql` paths (all empty); `fk_eap_contact` is defined only in `V15:43`; the run's only new migrations are `V125` (meeting_calendar_event), `V126` (outbound_mail_attachment) and `V127` (mail_record column), none of which touch `expert_contact`. No migration seeds that table (`grep -rin 'into .*expert_contact' src/main/resources/db/migration/` → 0 relevant hits), so the FK violation is deterministic at the run base too; the run's *only* edit to that method is the expected version literal `124 → 127` (plus 3 new V125–V127 test methods and one private helper elsewhere in the file).
- Smallest correction: seed an `expert_contact` row (or bind the INSERT to an existing seeded contact id) before the INSERT at `FlywayMigrationIntegrationTest.kt:233`; needs `src/test/kotlin/com/weibo/talentintroduction/campaign/repository/FlywayMigrationIntegrationTest.kt` — **outside child 08's 10-file authorization → separate authorized repair required.**

### AUTO_FIX
- N/A — no correction exists inside child 08's 10 authorized files. The child's own diff is key-only and all its scope-owned commands/assertions pass; the two gate-3 regressions (F-1, F-2) belong to earlier child 06 (`82a46dc`) and their smallest corrections both live in files outside the 10-file authorization (`OperatorStatusWriteSeamGuardTest.kt`, `UnmatchedInboundTrustWorkbenchTest.kt`), so they cannot be applied as an AUTO_FIX and require a plan amendment.

### RECORD_ONLY
- O-1 (F-3, pre-existing) — `FlywayMigrationIntegrationTest.V124 allows material attached promotion audit trigger` fails on `fk_eap_contact`; introduced by base commit `6ab8eb3` (ancestor of run base `24f5c82`), the run only bumped the expected version `124→127` in that method. Recorded rather than treated as a child-08 gate failure because it is not an earlier-children regression, but it still blocks a green `mvn test`-family release gate and its fix needs a file outside child 08's authorization.
- O-2 — evidence citation only: child `execution.md` cites the script block as `index.html:2109` / `:2110-2114`, while at both the child base `ef836c3` and HEAD the block sits at `:2118` (`task-modal-runtime.js`) and `:2119-2123` (5 scripts); index.html is 2125 lines and this child did not change its line count. The referenced content and relative order are correct (the approved brief hedges with ":2109-2114附近"); no product or test impact.
- O-3 — the review boundary `ef836c3..8c91c65` also contains `da60feb` (child 07 light-verification records: `children/07-attachment-ui/execution.md`, `verify-log.md`, docs-only). Child 08's own implementation commit is exactly the 10 authorized files; the two doc files are not child-08 product/test changes.
- O-4 — beyond-gate observation: `mvn test`'s 13 skipped are the flag-gated `mysqlIt`/`migrationIt` classes (not "passed"); the implementer's supplementary `-DmigrationIt=true` group beyond the 4 required commands (67 run / 0 failures / 37 errors after the API pin, all argued pre-existing environment/context conditions) was **not** re-run by this verifier and stays outside the light gate.

### Required Action
- PAUSE

## Epoch 1 — Light Verification (attempt 2)

## Light Verification: LIGHT_PASS_WITH_NOTES
Child: 08-assets-release, docs/plans/2026-09-16/meeting-mail-08-assets-release.md
Boundary: ef836c3e13f57bc14696318ec0f8a5c89ab06874..ae5d947b7257bf714e1d70e93dafbaf1894cbff6
Verifier: Verifier08b

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
|1 — Authorized files only + key-only diff|PASS|Boundary file set = exactly the **12 A3-authorized paths**. `git show --stat 8c91c65` = the 10 cache-key files (28 insertions / 28 deletions); `git show --stat ae5d947` = the 2 A3 files (`OperatorStatusWriteSeamGuardTest.kt`, `UnmatchedInboundTrustWorkbenchTest.kt`, 30 insertions / 6 deletions); no other `src/**` file appears in `git diff --name-status ef836c3..ae5d947` (the remaining entries are docs-only fast-p bookkeeping, see O-3). Key-only proof per file: `git show ef836c3:<f> \| sed 's/20260914-followup-email/@@K@@/g' \| md5sum` == `git show ae5d947:<f> \| sed 's/20260916-meeting-mail/@@K@@/g' \| md5sum` → **10/10 `KEY-ONLY-OK`** (index.html 2125 lines at both ends). `git grep -l '20260914-followup-email' HEAD -- src` → 0 files. Boundary contains no `src/main/kotlin`, no `.sql`, no `.css` change, and `index.html` is the only static source file touched. See "Repair round 1" below for the F-1/F-2 scope proof.|
|2 — I-1 / I-2 / S-1 direct evidence|PASS|**I-1**: `grep -o '?v=[A-Za-z0-9._-]*' index.html \| sort \| uniq -c` → `9 ?v=20260916-meeting-mail` (single key, count 9); old key 0 hits in `src/`; exactly one unversioned static reference remains — `index.html:2118 <script src="task-modal-runtime.js"></script>`. **S-1**: the 9 versioned resources sit at `:11-14` (4 links: styles.css, expert-materials.css, mailbox-chat.css, meeting-confirmation.css) and `:2119-2123` (5 scripts: trust-reply-workbench.js, expert-materials.js, meeting-confirmation.js, mailbox-chat.js, app.js) — same nodes, same relative order as the plan's verbatim block, no node added/removed/moved. **I-2**: `mailbox-chat.css` md5 identical base↔head (`7706e83b1751a209a5fc909596bd7511`); boundary diff has no business/DOM/style change (md5 normalization above proves the cache-key files are key-only); no `.kt`/`.sql`/`.css` in the boundary; fresh `node --test src/test/js/*.test.js` → exit 0, tests 944 / suites 183 / pass 944 / fail 0 / skipped 0.|
|3 — Required commands freshly re-run + attribution|PASS|Five commands re-run now under JDK 11 (zulu-11) with `DOCKER_API_VERSION=1.44` (see table). **The focused repair command is green: exit 0, 13 run / 0 failure / 0 error** (`OperatorStatusWriteSeamGuardTest` 1, `UnmatchedInboundTrustWorkbenchTest` 12), and the **full `mvn test` is green: 3452 run / 0 failure / 0 error / 13 skipped, BUILD SUCCESS** — the 1 failure + 4 errors of attempt 1 are gone. The `mysqlIt` group is green (105/0/0/0). The only residual non-green group is `-DmigrationIt=true` (23/0/1/0, exit 1), whose sole error is attributed to base commit `6ab8eb3` and recorded as **O-1** rather than a gate failure. All counts reproduce the implementer's record exactly.|
|4 — Downstream interfaces (terminal child)|PASS|The key swap altered no product contract: the boundary is index.html + 9 JS test files (provably key-only) + 2 Kotlin test files; no `<link>`/`<script>` added, removed or reordered; all 9 versioned assets and `task-modal-runtime.js` still resolve to existing files; no Kotlin main-source / API / DTO / DOM / CSS change exists anywhere in the boundary; the fresh 944-case JS suite (asserting the workbench shared-mount contract, mailbox-chat behavior, RAG page order and the 9-key/9-resource contract) passes. Terminal child — its only downstream surface is the browser's 9 asset URLs, whose paths and relative order are unchanged.|

#### Repair Round 1 (F-1 line-pin shift, F-2 Mockito matcher arity) — both correctly applied

- **F-1 (line pins)**: `diff` of the guard test's **non-comment lines** between `8c91c65` and `ae5d947` yields exactly the two `NoiseSite` rows — nothing else — with line numbers `219 → 221` and `1125 → 1137`; the `context` strings are byte-identical (`operatorStatus = request.operatorStatus`, `operatorStatus = operatorStatus`) and the other 8 `NoiseSite` rows are byte-identical. Pins match reality at HEAD: `UnmatchedInboundMailController.kt:221` = `            operatorStatus = request.operatorStatus,`, `:1137` = `    operatorStatus = operatorStatus,`; the guard's own anti-staleness self-check (`staleExclusions` assertion) passes in the fresh focused run. Only line numbers and explanatory comments changed.
- **F-2 (matcher arity)**: the diff adds exactly 2 trailing matchers (`anyValue(emptyList()), Mockito.isNull()`) to each of the **4 matcher-based** stubs (stub calls at `:226/:261/:293/:396`; matcher lines now at `:240/:275/:307/:410`) plus one private helper (`anyValue`, `:432`, inside the class which closes at `:433`). No matcher, assertion or test case was deleted or weakened (diff has no other deletions). Arity is correct: the real signature `PendingMailOperationService.sendManualRichReply` (`:159-190`) has 23 parameters (21 up to `previewAttachmentSha256` + `attachmentIds: List<String> = emptyList()` + `authenticatedUsername: String? = null`), and each patched stub records exactly 23 matchers. The 2 remaining `sendManualRichReply` stubs (`:174`, `:332`) use **named arguments**, so the 2 new defaulted parameters require no change — correct, not an omission.

#### Required commands (fresh, JDK 11 zulu-11; Docker OrbStack `ti-mysql-it` with `DOCKER_API_VERSION=1.44`/`-Dapi.version=1.44`)

|#|Command|Exit|Fresh counts|Recorded|Match|
|---|---|---|---|---|---|
|1|`node --test src/test/js/*.test.js`|0|tests 944 / suites 183 / pass 944 / fail 0 / skipped 0|944/944/0|yes|
|2|`JAVA_HOME=…/zulu-11… mvn test -Dtest=OperatorStatusWriteSeamGuardTest,UnmatchedInboundTrustWorkbenchTest`|0|Tests run: 13 / Failures: 0 / Errors: 0 / Skipped: 0 (1 + 12), BUILD SUCCESS|13/0/0|yes|
|3|`JAVA_HOME=…/zulu-11… DOCKER_API_VERSION=1.44 mvn test`|0|Tests run: 3452 / Failures: 0 / Errors: 0 / Skipped: 13, BUILD SUCCESS (03:30)|3452/0/0/13|yes|
|4|`… DOCKER_API_VERSION=1.44 mvn test -Dtest=FlywayMigrationIntegrationTest -DmigrationIt=true -Dapi.version=1.44`|1|Tests run: 23 / Failures: 0 / Errors: 1 / Skipped: 0, BUILD FAILURE — sole error `V124 allows material attached promotion audit trigger:233->execute:1216 » SQLIntegrityConstraintViolation`|23/0/1/0|yes|
|5|`… mvn test -Dtest=MeetingCalendarServiceTest,MeetingCalendarControllerTest,MailboxConversationControllerTest,MailboxConversationRepositoryIT,AttachmentTransferWorkerIT,ImapMetadataFetchIT,MeetingCalendarSendIntegrationTest -DmysqlIt=true -Dapi.version=1.44 -DfailIfNoTests=false`|0|Tests run: 105 / Failures: 0 / Errors: 0 / Skipped: 0 (18+6+27+21+17+9+7), BUILD SUCCESS|105/0/0/0|yes|

- Command 3's 13 skipped are the flag-gated `migrationIt`/`mysqlIt` classes (10 `migrationIt` carriers + 6 `mysqlIt` annotations across 4 files); they are **not** "passed" by command 3 and are exercised by commands 4–5.

### AUTO_FIX
- N/A — both authorized repairs (F-1, F-2) are verified applied exactly as authorized, and the only residual failure in the required-command set (O-1) lies outside the 12 authorized files, so no correction is available inside this child's authorization.

### RECORD_ONLY
- **O-1 (pre-existing; outside the change boundary)** — `mvn test -Dtest=FlywayMigrationIntegrationTest -DmigrationIt=true` is **not green**: exit 1, 23 run / 0 failure / 1 error / 0 skipped, sole error `V124 allows material attached promotion audit trigger:233->execute:1216 » SQLIntegrityConstraintViolation` (`CONSTRAINT fk_eap_contact` → `expert_contact(id)`). Attribution: the failing method was introduced by base commit `6ab8eb318bad141e6927f234e95eb4f2df17a2eb` (2026-09-11, `fix: preserve inbound replies when promotion audit fails`; `git log -S'V124 allows material attached promotion audit trigger'` returns only that commit, and `git merge-base --is-ancestor 6ab8eb3 ef836c3` → YES, i.e. it predates the boundary base). The boundary touches neither that test file (`git log ef836c3..ae5d947 -- …/FlywayMigrationIntegrationTest.kt` → empty) nor the constraint (`fk_eap_contact` is defined only at `V15__…:43`; no migration seeds `expert_contact` — `grep -rlnE 'insert into expert_contact' src/main/resources/db/migration/` → 0 files), so the violation is deterministic at the base too. Recorded instead of treated as a gate failure per the assignment; the `-DmigrationIt=true` group therefore remains not green and its fix needs a file outside the 12 authorized paths.
- **O-2 (attribution of the repaired defects)** — At the boundary base `ef836c3` both repaired defects were **already red**: the guard pins (`219`/`1125`) did not match the controller, whose pinned lines already sat at `221`/`1137` (controller byte-identical to `82a46dc`, itself an ancestor of `ef836c3`), and the workbench stubs recorded 21 matchers against the already-23-parameter `sendManualRichReply` (service byte-identical base↔HEAD; guard/workbench test files byte-identical `ef836c3`↔`8c91c65`). F-1/F-2 are therefore base-level breakage repaired under the A3 authorization, not regressions introduced inside this boundary; nothing residual remains.
- **O-3 (docs-only boundary entries)** — besides the 12 authorized paths, the boundary diff contains only fast-p bookkeeping docs: `2f0d697` (pause records: child-08 `execution.md`/`verify-log.md` attempt 1 + `ledger.md`), `2d72662` (A3 amendment: the amended plan + the byte-identical child `brief.md` — `diff -q` → IDENTICAL) and `da60feb` (child-07 light-verification records). No product/test change outside the 12.
- **O-4 (uncommitted repair bookkeeping)** — `git status --porcelain` shows the working tree with `children/08-assets-release/fix-log.md` (new, 11 lines) and `ledger.md` modified but uncommitted; these are documentation/ledger only, contain no product or test change, and are not part of the boundary diff. No product impact.

### Required Action
- COMPLETE_CHILD
