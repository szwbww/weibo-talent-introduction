## Epoch 1 — Light Verification (attempt 1)

## Light Verification: LIGHT_PASS_WITH_NOTES
Child: 05-attachment-delivery, docs/plans/2026-09-16/meeting-mail-05-attachment-delivery.md
Boundary: 2540a0665cd1eff406bec460e56b75fced929cbf..c75693a2e9dc6cc2f5b1a90b57eb84b072e43908
Verifier: Verifier05

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
|1. Authorized-file scope|PASS|`git log --oneline <base>..<head>` = 2 commits: `c75693a feat(fast-p): implement 05-attachment-delivery` and `d6d735f docs(fast-p): record 04-attachment-storage light verification`. `git diff --stat <base>..<head> -- src/` = exactly the 8 authorized paths (A `src/main/resources/db/migration/V127__add_outbound_attachments_snapshot.sql`, M `mail/domain/MailRecord.kt`, M `mail/service/ManualReplySendAttemptService.kt`, M `mail/service/IntroductionMailComposer.kt`, M `mail/service/SmtpMailDeliveryService.kt`, M `mail/service/ManualReplySendAttemptServiceTest.kt`, M `mail/service/SmtpMailDeliveryServiceTest.kt`, M `campaign/repository/FlywayMigrationIntegrationTest.kt`), 607 insertions / 52 deletions, no rename or deletion, no other product or test file. The range's only other commit d6d735f touches 2 plan artifacts (`children/04-attachment-storage/execution.md`, `verify-log.md`) and zero `src/` files. `cmp` brief.md vs `docs/plans/2026-09-16/meeting-mail-05-attachment-delivery.md` = identical (sha256 `e2a5c6e4c698398c9eee8aa643d2733eae75b1b24dc21b89c124baf9c8eb56ea`, matching execution.md's recorded plan identity). Worktree HEAD = `c75693a` on `fast/meeting-mail-master`; the only dirty paths are `docs/plans/**` (execution.md, ledger.md).|
|2. Plan requirements & invariants|PASS|I-1: V127:14 is the single statement `ALTER TABLE mail_record ADD COLUMN outbound_attachments_json LONGTEXT NULL` (whole file 14 lines: no backfill, no DEFAULT, no index); `MailRecord.kt:41` appends `outboundAttachmentsJson: String? = null`; `ManualReplySendAttemptService.kt:92` declares `SendPayload.outboundAttachments` default `emptyList()`; all four finalize branches assign the field from one shared local `outboundSnapshotJson` computed as `takeIf { it.isNotEmpty() }?.let { serialize }` (success-copy :373 and success-new :394 from :356-358; failure-copy :471 and failure-new :493 from :454-456), so empty input is an explicit SQL NULL, `[]`/`""` are impossible, and a previous attempt's snapshot is always overwritten or cleared; `grep -rn outboundAttachmentsJson src/` finds no other producer (only MailRecord, the four assignments and test helpers). I-2: the pre-existing 11 length-prefixed segments (:154-168) and child 02's `meeting-calendar-v1` + semantic sha segment (:172-175) are byte-identical; the new `outbound-attachments-v1` segment is appended only when non-empty (:177-186) as marker + count + per-item filename/contentType/byteLength/sha256, all through the existing big-endian length-prefix writer (:644-652), with no upload UUID/path/time; the deployed golden `fa838dbc...d38becda` is still asserted unchanged at `ManualReplySendAttemptServiceTest.kt:90` and :1252-1253 and re-asserted for the explicit-empty-list case at :310; new tests :295 (upload-id independent, empty list = golden), :317 (bytes/filename/mime/order each change the hash), :342 (calendar segment independent of the attachment segment, calendar still ordered by `semanticSha256`). I-3: the head no-attachment condition `calendar == null && outboundAttachments.isEmpty()` keeps the pre-05 branch byte-identical (`git show <base>:.../SmtpMailDeliveryService.kt:49-101` vs head :51-63); the mixed branch is part1 = original plain/alternative body (old inline construction moved verbatim into `originalBodyPart`, :131-148), then the existing ICS, then generic attachments in selection order, each with `Disposition: ATTACHMENT`, snapshot filename/contentType and untouched bytes (:75-99); the three new tests re-parse real `MimeMessage.writeTo` output (`SmtpMailDeliveryServiceTest.kt:602` generic-only, :649 ICS+two generic, :684 plain body+generic) asserting outer `multipart/mixed`, part counts, ICS-before-generic order, exactly one `text/calendar` part, decoded Chinese filenames and `arrayEquals` on the original bytes (zip not expanded); 0-attachment and ICS-only shapes stay pinned by the pre-existing tests in the same class. I-4: `prepareAndClaim`/`finalizeSuccess`/`finalizeFailure` remain `@Transactional(REQUIRES_NEW)` with the claim committed before SMTP, `MailSendAttemptStatus`/`ClaimResult` gain no state, the success `mail_record` and `meetingCalendarService.createFromSentMail` still run in the one finalize transaction (:407-412), and the new `attachment only send never touches the scheduling service` (:879) verifies an attachment-only send never creates a meeting while the failure tests keep `sendStatus = "FAILED"` / `sentAt = null`.|
|3. Required commands, fresh|PASS|Fresh `JAVA_HOME=/Library/Java/JavaVirtualMachines/zulu-11.jdk/Contents/Home mvn test -Dtest=ManualReplySendAttemptServiceTest,SmtpMailDeliveryServiceTest` -> EXIT=0, `BUILD SUCCESS` (total 03:08 min); freshly written surefire reports (10:44) `ManualReplySendAttemptServiceTest: Tests run: 56, Failures: 0, Errors: 0, Skipped: 0` and `SmtpMailDeliveryServiceTest: Tests run: 32, Failures: 0, Errors: 0, Skipped: 0` = 88 run / 0 fail / 0 error / 0 skipped, identical to the recorded evidence. Fresh `JAVA_HOME=<zulu-11> mvn test -Dtest=FlywayMigrationIntegrationTest -DmigrationIt=true` -> EXIT=1, `Tests run: 1, Failures: 0, Errors: 1, Skipped: 0`: `o.t.d.DockerClientProviderStrategy - Could not find a valid Docker environment ... UnixSocketClientProviderStrategy: failed with exception InvalidConfigurationException (Could not find unix domain socket). Root cause NoSuchFileException (/var/run/docker.sock)` then `java.lang.IllegalStateException: Docker is required for Flyway migration tests at FlywayMigrationIntegrationTest.startMysql` - identical to the recorded unverified blocker, and `docker info` reproduces `dial unix /Users/lukai/.orbstack/run/docker.sock: connect: no such file or directory` (`/var/run/docker.sock` is a dangling symlink to the absent OrbStack socket). Environment limitation, not repaired and not reclassified as a code defect; the DB-level V127 assertions remain unexecuted, exactly as in the recorded evidence.|
|4. Downstream interfaces 06/07|PASS|Child 06 needs one 04 file set feeding both payload shapes: `SendPayload.outboundAttachments: List<OutboundAttachmentSnapshot>` (ManualReplySendAttemptService.kt:92) and `ComposedMail.outboundAttachments: List<OutboundMailFile>` (IntroductionMailComposer.kt:90), both default-empty so every existing call site keeps the old behaviour and 06 only adds the two arguments it plans to add. 06's SENT-window projection reads `mail_record.outbound_attachments_json` (MailRecord.kt:41) with the 04 codec `OutboundAttachmentSnapshotCodec.parseOrThrow` and snapshot shape `OutboundAttachmentSnapshot(schemaVersion,id,filename,contentType,byteLength,sha256)` (OutboundAttachmentModels.kt:83-92,135) - unchanged by this child - and the archived JSON is exactly that array, so 06 can validate membership by attachment id and compare filename/type/size/hash in selection order. 06's requirement that `MailDeliveryService.send()` keeps its signature holds: `fun send(account: MailSenderAccount, mail: ComposedMail): DeliveredMail` (MailDeliveryService.kt:7) is untouched, so the 9 dependent test files and 06's seam expectations are unaffected. 07 consumes only 06's message DTO (`outboundAttachments`) and references no 05 symbol, and the 05 API surface is 04-owned (`OutboundMailFile`, `OutboundAttachmentSnapshot`, `OUTBOUND_ATTACHMENT_*` constants: none edited in this boundary).|

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1: the I-1 acceptance bullet "four branches each carry a two-attachment and an empty-list verification" is not cell-complete in the test suite: the two unasserted matrix cells are finalizeFailure new-branch with an empty list and finalizeFailure copy-branch with a non-empty list. The pre-existing `finalizeFailure creates failed mail record with error summary` (ManualReplySendAttemptServiceTest.kt:538) already exercises the failure-new-empty scenario but asserts only `calendarAttachmentJson`/`sentAt`/`sendStatus`, and the new tests cover failure-new-with (:824), failure-copy-empty (:846), success-new-empty (:865), success-copy-with/empty (:787/:806) - the same cell pattern child 02 accepted for the calendar column. Every branch still has direct code evidence (all four assign the single shared `outboundSnapshotJson` local computed at :356-358 / :454-456, so the value cannot differ per branch), so this is a verification-completeness note, not a four-gate violation, and no fix is authorized.

### Required Action
- COMPLETE_CHILD
