## Epoch 1 — Light Verification (attempt 1)

## Light Verification: LIGHT_PASS
Child: 02-calendar-send, docs/plans/2026-09-16/meeting-mail-02-calendar-send.md
Boundary: 906f241cf685145d70c7917bb8b950f69c711835..f83e29c397dd01ceafb98025e0d649a69c6eafae
Verifier: Verifier02

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
|1. Authorized-file scope|PASS|`git diff --name-status 906f241c..f83e29c` = exactly the 5 Authorized Files (`M` PendingMailOperationService.kt, `M` ManualReplySendAttemptService.kt, `M` PendingMailOperationServiceTest.kt, `M` ManualReplySendAttemptServiceTest.kt, `A` MeetingCalendarSendIntegrationTest.kt) + 2 child-01 plan docs (`01-calendar-api/execution.md`, `verify-log.md`), which are plan artifacts, not product/test. No controller/migration/other-product file touched; no deletion or rename; the new test's support class + failure toggle live inside the single authorized new file (lines 655/683).|
|2. Plan requirements & invariants|PASS|I-1: `PendingMailOperationService.kt:541,567-597` keeps the one `validateAndBuild` product in `rebuiltMeeting` and derives both `calendarSnapshot` and `MeetingCalendarInput(Instant.parse(rebuilt.startUtc), Instant.parse(rebuilt.endUtc), rebuilt.meeting.zoomUrl)` from it (no second ICS, no re-read of `now()`; startUtc/endUtc are `Instant.toString()` from `MeetingConfirmationService.kt:177-178`); `SendPayload.meetingEvent` default null (`ManualReplySendAttemptService.kt:82`), fed at `PendingMailOperationService.kt:637-648`. Test: PendingMailOperationServiceTest `meeting send carries the one real snapshot…` asserts field equality with the preview, and the no-meeting path asserts `assertNull(payload.meetingEvent)`. I-2: `finalizeSuccess` is `REQUIRES_NEW`; `createFromSentMail(savedRecord, event)` sits after `requireNotNull(savedRecord.id)` on both success branches (new + copy) and before `updateStatusAndError(SENT)` (`ManualReplySendAttemptService.kt:377-386`); nothing swallows it; child 01's method is `@Transactional(MANDATORY)` so it joins that tx; `finalizeFailure` never calls it. Test: InOrder(save → createFromSentMail → updateStatusAndError), `finalizeFailure never creates a schedule even when the payload carries a meeting`, `finalizeSuccess propagates a schedule write failure instead of marking the attempt sent`, plus the MySQL IT rollback case. I-3: failure/unknown paths untouched (`PendingMailOperationService.kt:753-771` DELIVERY_UNKNOWN + 409「发送状态未知，请勿重复发送」; `:827-855` unknown/safe-retry); DEDUP_SENT branch returns the existing result without touching the calendar; the conversation short-circuit `findCompletedByRequestId` (`:377-388`) lies outside every diff hunk. Test: unit dedup/unknown SMTP-total assertions + IT `resending a completed request…` (real SMTP count 1; reschedule 14:00 kept; CANCELLED kept; source mail row bytes unchanged) and the IT rollback case (UNKNOWN retry does not re-SMTP). I-4: `computeFingerprint` appends only `calendarAttachment.semanticSha256` (`ManualReplySendAttemptService.kt:155-161`) — `meetingEvent` never enters it; both golden `fullHex` assertions are byte-identical base↔head (`fa838dbceec46871ec1eae26e9ba4666d6f1ac0fda1bd36f872110efd38becda`), verified with a line diff; IT `an ordinary rich reply without a meeting…` asserts 0 calendar rows and null `calendar_attachment_json`; child 01's `createFromSentMail` only reads (`requireContact` → `existsById`).|
|3. Required commands, fresh|PASS|No Docker needed. `mvn test -Dtest=ManualReplySendAttemptServiceTest,PendingMailOperationServiceTest,MeetingCalendarSendIntegrationTest` (exact form, **no** `-DskipNodeTests=true`): exit 0, `Tests run: 75, Failures: 0, Errors: 0, Skipped: 1` (Pending 29/0, ManualReply 45/0, IT class 1 skipped = `mysqlIt` gate); the node JS suite also ran (886 tests, 0 fail). Same command + `-DmysqlIt=true`: exit 0, `Tests run: 81, Failures: 0, Errors: 0, Skipped: 0` (Pending 29, ManualReply 45, MeetingCalendarSendIntegrationTest 7/7 — real MySQL 8.0 `jdbc:mysql://localhost:3306/talent_introduction`, Flyway at V125; no docker/testcontainers). Both counts match the implementer-reported evidence exactly (75/0/1 and 81/0/0). Recorded baseline (these three classes were not a covered target before; the run-level baseline was child 01's targets → exit 1 / no matching tests) is superseded by exit 0 with 0 failures for all three classes, including the two pre-existing ones.|
|4. Downstream interface (child 03)|PASS|No new endpoint and no HTTP-surface change: no `controller` file appears in the boundary. Child 01's contract is unchanged — `campaign/service/MeetingCalendarService.kt` and `campaign/domain/MeetingCalendarEvent.kt` are not in the boundary diff, so `createFromSentMail(record: MailRecord, input: MeetingCalendarInput): EventRow` (MANDATORY) and `MeetingCalendarInput(startUtc, endUtc, meetingLink)` are byte-identical; this child only consumes them. Child 03 consumes only the existing mailbox/calendar HTTP endpoints.|

### AUTO_FIX
- N/A

### RECORD_ONLY
- N/A

### Required Action
- COMPLETE_CHILD
