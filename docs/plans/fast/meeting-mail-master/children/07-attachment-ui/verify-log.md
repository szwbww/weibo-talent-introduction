## Epoch 2 — Light Verification (attempt 1)

## Light Verification: LIGHT_PASS_WITH_NOTES
Child: 07-attachment-ui, docs/plans/2026-09-16/meeting-mail-07-attachment-ui.md
Boundary: 82a46dcc32d50cbc352165656842417bc0a569f2..ef836c3e13f57bc14696318ec0f8a5c89ab06874
Verifier: Verifier07

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
|1 · Authorized files|PASS|`git diff --name-only 82a46dc..ef836c3` product/test entries = exactly the 5 authorized files: `mailbox-chat.js`, `styles.css`, `test/js/mailboxOutboundAttachments.test.js` (new), `test/js/meetingConfirmationIntegration.test.js`, `test/js/mailboxChatBehavior.test.js`. Remaining entries are `docs/plans/**` plan/ledger/child-log artifacts; no other `src/` path. A2 minimality confirmed by `--numstat src/test/js/mailboxChatBehavior.test.js` = 3+/1- (single hunk at `:3528-3536`): only `:3531` sibling-adjacency pin relaxed to an ordering assertion; `:3530 kids[kids.indexOf(editorEl) + 1] === note` and all other assertions unchanged. `git status --porcelain -- src/` empty at boundary HEAD; `mailbox-chat.css` sha256 `0fd354027e54ae69f0a2ba76451801b85c6b74cd2792e98a3852cbb14bade17d` = base `82a46dc`.|
|2 · Requirements, invariants, verbatim style contracts|PASS|**S-1**: brief lines 48/49 (`grep -F` against brief = 1 hit each in the source) appear byte-verbatim at `mailbox-chat.js:2874-2875`, after the `createLink` button and before `${meetingTrigger}`; `title`/`aria-label` 上传附件, no visible text, `multiple` without `accept` (`mailboxOutboundAttachments.test.js:1539`, `:1587`); cancel = zero requests, zero draft change. **S-2**: `tail -18 styles.css` diffs empty against the plan's css fence (byte-identical), plus the `endsWith` lock at `:2193`; all 9 classes introduced by the patch (`outbound-upload/-files/-file/-file-icon/-file-main/-file-name/-file-meta/-file-actions/-file-link`) are declared in that block; no `style="` in any added line; draft container between the meeting card and the footer (`:2865-2882`), sent container after body/ICS and before the inbound-material summary (`:2102-2132`). **I-1** `:3608-3630` queues only uploads. **I-2** single truth `outboundAttachmentDraft` with owner capture before each async call (`captureOutboundOwner`), write-back only to the captured map + targetKey (`setOutboundAttachmentItems`), late/removed/LRU responses no-op (`applyOutboundItemChange` returns false when the key is gone; tests `:1662`, `:1687`, `:1708`), and every rebuild write point preserves the field — `saveDraftFromInputs:3840`, `writeDraftWithMeeting:4506`, `ensureOutboundRequestId:3891`, `removeMeetingFromDraft` / `retargetManual:4973` / meeting-stale `:4841` via `Object.assign` (tests `:2054`, `:2094`, `:2137`). **I-3** only ordered `ready` ids (`:3423`), any `uploading`/`failed` blocks send (`:4733-4738`), `requestId` invalidated only on pick/remove (`:3644`, `:3725`) and stable across upload settle / programmatic save / failure retry (`:1790`), success clears only the captured snapshot (`:4853-4860`, tests `:1867`, `:1898`). **I-4** sent cards consume only `message.outboundAttachments` (`:3478-3491`), `href = contextPathValue() + downloadUrl` (`/talent` asserted at `:1985`), `escapeText` names, `download` attr, no remove button, no Blob (`:1964`, `:1995`, `:2039`). **I-5** ICS and generic cards independent (`:2054`), `SESSION_CACHE_LIMIT = 10` / `MESSAGE_CACHE_LIMIT = 500` (`:45-46`) not in the diff, no `localStorage`/`IndexedDB` in added code, toolbar order B/I/列表/链接/回形针/会议确认/跟进 with conditional inclusion unchanged (`meetingConfirmationIntegration.test.js` 30/30).|
|3 · Fresh commands vs recorded evidence|PASS|Re-run at boundary HEAD `ef836c3` in the worktree: `node --check src/main/resources/static/mailbox-chat.js` → exit 0; `node --test src/test/js/*.test.js` → exit 0, tests 944 / suites 183 / pass 944 / fail 0; `mailboxChatBehavior.test.js` → exit 0, 80/80; `mailboxOutboundAttachments.test.js` → exit 0, 21/21; `meetingConfirmationIntegration.test.js` → exit 0, 30/30; `shasum -a 256 mailbox-chat.css` matches base. All counts equal the recorded epoch-2 evidence (epoch-1's 944/943/1 → 80/79 transition is reproduced by the A2 diff).|
|4 · Interfaces required by child 08|PASS|`index.html` byte-unchanged in the boundary (`git diff … -- src/main/resources/static/index.html` = 0 lines): exactly 9 `?v=20260914-followup-email` references (4 links + 5 scripts), so child 08's baseline key and resource count/order are intact; none of the 9 fixed-key test files (`manualReplySubjectPrefill`, `ragKnowledgeBasePage`, `overlayAndDialogContrast`, `mailboxChatStyle`, `meetingConfirmationAssets`, `batchSendTaskConsoleVisualFix`, `trustReplyWorkbenchSharedMount`, `checkRepliesRelocation`, `ragWorkbenchRender`) was touched by 07. The DOM/CSS contract 08 must preserve is landed by 07 with no index.html change (CSS appended to `styles.css`, DOM generated in `mailbox-chat.js`), which is what 08's I-2/S-1 assume.|

### AUTO_FIX
- N/A — no proven four-gate violation; no correction is uniquely determined by the plan.

### RECORD_ONLY
- O-1: an upload still in `uploading` when the same expert's reply target is retargeted survives the migration as a permanently `uploading` item. `retargetManual` migrates the whole draft field (`mailbox-chat.js:4973-4980`, `Object.assign` + `deleteDraft(oldKey)`), while the late response writes back through the captured old `targetKey` (`outboundItemsIn:3509-3512`) and `applyOutboundItemChange:3691-3694` returns false, so the item never settles and (together with its retained `File`) blocks sending until the user removes it manually. The no-op-on-vanished-key mechanism itself is empirically verified by the LRU case (`mailboxOutboundAttachments.test.js:1708`). Impact is one manual removal step in a same-expert corner case; the plan only specifies "已 ready 附件随草稿迁移" for this transition and does not define in-flight behaviour, so no single authorized correction follows — outside the light gate (I-2 wording is arguably honoured, since the captured key no longer exists).

### Required Action
- COMPLETE_CHILD
