## Epoch 1 — Light Verification (attempt 1)

## Light Verification: LIGHT_PASS_WITH_NOTES
Child: 06-attachment-flow, docs/plans/2026-09-16/meeting-mail-06-attachment-flow.md
Boundary: c75693a2e9dc6cc2f5b1a90b57eb84b072e43908..82a46dcc32d50cbc352165656842417bc0a569f2
Verifier: Verifier06

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
|1 — Authorized files only|PASS|`git show --stat 82a46dc` = exactly the 10 Authorized Files (`git diff --name-only 82a46dc~1 82a46dc | wc -l` → 10): 5 product (.kt under `mail/service`, `mail/controller`) + 5 test (4 modify, `controller/OutboundAttachmentFlowTest.kt` new). Commit subject exactly `feat(fast-p): implement 06-attachment-flow`. The wider review boundary also spans the intermediate evidence commit `dcc1b91` (child 05 `execution.md`/`verify-log.md`), which contains no product/test file. Plan identity: `shasum -a 256` of `children/06-attachment-flow/brief.md` and `docs/plans/2026-09-16/meeting-mail-06-attachment-flow.md` both `04e4500920152dc7ee3ddc2afd865048506155be42933dfb659453e79151c4ff`.|
|2 — Plan requirements / invariants have direct code+test evidence|PASS|I-1: both entries (`sendManualRichReply` 185-190, `sendConversationManualRichReply` 382-384) forward `attachmentIds` + `authenticatedUsername` into the single `executeManualRichSend` (516-533, called at 328-358 and 476-487); `resolveOutboundAttachmentFileSet` (960-972) → 04 `resolveForSend` runs at 667, **before** `prepareAndClaim` at 717 and outside the delivery `try` (742-925), so failures are 400/404/409/413 and never `finalizeFailure`/SMTP UNKNOWN; `attachmentFileSet.snapshots` → `SendPayload.outboundAttachments` (690-697) and `attachmentFileSet.files` → `ComposedMail.outboundAttachments` (751-753) from the same set; identity is `sessionUsername(...)`/`sessionUsernameOrNull()` (AuthSessionKeys.USERNAME), never body `operatorName` (flow test passes `operatorName="display-op"` with an `OWNER` session and asserts resolution under `OWNER`). I-2: completed-`requestId` branch (403-425) reads the record first, calls only 04 `loadSnapshots` (no bytes), never re-queries the anchor (`findById`/`findLatestSentOutboundAnchor` asserted never) and returns the original SENT; ordered `sameOutboundAttachmentSet` (997-1009, filename/contentType/byteLength/sha256) → identical SENT, different → `OutboundAttachmentException.conflict` (409, message `该请求已发送，附件与原请求不同，请发起新回复` literally asserted); empty/empty short circuit untouched and `verifyNoInteractions(outboundAttachmentService)`; UNKNOWN/IN_PROGRESS still 409 without resend. I-3: `outboundAttachments: List<ConversationOutboundAttachment> = emptyList()` in the message DTO (129-141, 157-172) with id/filename/contentType/byteLength/downloadUrl; `outboundAttachmentsOf` (410-450) consumes only the existing single `findAllById` window (242-253 pre-filters `MAIL_RECORD + OUTBOUND + SENT`, helper adds `MANUAL_RICH_REPLY`, same contact, same accountCode), parses via `parseOrNull`, corrupt/NULL → `[]` + body/path-free `log.warn` and page stays 200; `attachmentCount`/`firstAttachmentNames`/`calendarAttachment` asserted unchanged on an attachment row. I-4: new `GET /{contactId}/messages/{mailRecordId}/outbound-attachments/{attachmentId}/download` (121-146) checks session (anonymous 401), `record.expertContactId == path`, OUTBOUND + MANUAL_RICH_REPLY + SENT, `findAllByAccountCodeNot(SIMULATOR_ACCOUNT_CODE)` membership (disabled accounts included by construction), snapshot membership, then 04 verified read + `sameAttachmentMetadata` id/name/type/size/hash; cross-operator 200, wrong contact / wrong record / unreferenced id / simulator / FAILED / INTRODUCTION / corrupt snapshot → 404, draft download stays uploader-only. I-5: no semantic line was removed anywhere in the 5 product files (`git diff … | grep '^-'` yields only signature/parameter lines); RAG/QA evidence, thread anchors (`MAIL_RECORD:$ANCHOR_RECORD_ID`, inReplyTo/references), unsubscribe pre-check and two-stage safety confirmation tests all green; two-stage resubmit re-resolves the same ids (`Mockito.verify(outboundAttachmentService).resolveForSend(1L, listOf(ATTACHMENT_ID), "op")` exactly once, i.e. blocked attempt has zero delivery side effects).|
|3 — Required commands re-run freshly|PASS|Run now, JDK 11, fresh: (a) `mvn test -Dtest=PendingMailOperationServiceTest,PendingMailOperationServiceTrustWorkbenchTest,RagSendBridgeTest,OutboundAttachmentFlowTest` → **exit 0**, BUILD SUCCESS, `Tests run: 115, Failures: 0, Errors: 0` (surefire: Pending 35, TrustWorkbench 60, RagSendBridge 11, OutboundAttachmentFlow 9) and the bound node suite 923 pass / 0 fail — identical to the recorded evidence. (b) `mvn test -Dtest=MailboxConversationControllerTest -DmysqlIt=true` → **exit 0**, BUILD SUCCESS, `Tests run: 27, Failures: 0, Errors: 0, Skipped: 0` (7.925 s, real MySQL 8.0.46 @127.0.0.1:3306/talent_introduction, `nc -z` open). This replaces the implementer's environment-only blocker (27/27 `Failed to load ApplicationContext` → `Communications link failure`); the recorded blocker was purely the datasource and is now disproved by a green run of the same 27 tests, including the new real-SQL I-3 projection/batch-read test.|
|4 — Downstream interfaces required by child 07|PASS|07 I-4 consumes the message DTO field `outboundAttachments` (present, default `[]`) whose items expose `id`, `filename`, `contentType`, `byteLength` (Long) and a relative server-built `downloadUrl` `/api/mail/conversations/{contactId}/messages/{mailRecordId}/outbound-attachments/{attachmentId}/download` — exactly the path in 06 实现方案 4, so 07's `instance.options.contextPath + downloadUrl` prefixing works unchanged. 07 I-3 sends `attachmentIds` on both send entries: `ConversationManualRichReplyRequest.attachmentIds: List<String> = emptyList()` and `PendingManualRichReplyRequest.attachmentIds: List<String> = emptyList()` (absent ⇒ empty, old payload shape preserved). Upload/draft-download endpoints and the upload response DTO that 07's draft card consumes are untouched (child 04 surface, no diff in those methods).|

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1 (declared deviation, judged not a gate violation): `PendingMailOperationService` (`outboundAttachmentService`, line 96) and `OutboundAttachmentController` (`mailRecordRepository`, `mailSenderAccountRepository`, lines 49-53) use nullable defaults with `requireNotNull` at the attachment/endpoint seams, while the brief's 现状审计/实现方案 called the attachment service a 必需构造依赖. Verified rationale: `MeetingCalendarSendIntegrationTest.kt:179` builds the service with 25 positional args and `OutboundAttachmentServiceTest.kt:102` builds `OutboundAttachmentController(service)`; both are outside the 10 Authorized Files, so required non-null parameters would break the module compile and the required commands. Production wiring is unaffected — both are Spring beans (`@Service`/`@RestController`, single primary constructor, no manual instantiation in `src/main`), so the dependencies are injected; the seams fail closed rather than silently skipping. No correction is uniquely determined inside the authorized file set, hence a note for the human reviewer, not an AUTO_FIX.
- O-2 (evidence gap outside the 验收标准): 实现方案 5 enumerates "一封多附件＋ICS" and "跨消息下载" for the new 贯通 test, but all 9 `OutboundAttachmentFlowTest` cases use a single attachment id, and the "wrong message" case uses a non-existent record id (`recordId + 999`) plus an unreferenced upload id instead of a second real sent record; no test in the tree passes two attachment ids or a meeting+attachment combination. The 验收标准 (I-1..I-5) themselves are all covered by direct evidence, and multi-file ordering/bounds and ICS+file MIME merging are owned by child 04 `resolveForSend` and child 05 SMTP tests, so this is recorded for the human checklist (A-2 exercises ICS + multiple files manually) rather than treated as a gate failure.

### Required Action
- COMPLETE_CHILD
