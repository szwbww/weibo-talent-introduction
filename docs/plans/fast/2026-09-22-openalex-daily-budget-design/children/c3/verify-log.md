## Light Verification: LIGHT_PASS_WITH_NOTES
Child: c3 (docs/plans/2026-09-22/03-discovery-continuous-run.md)
Boundary: ee7a45fa23c166c4e235cb3121c4fc10b402f265..13b82fde5d74836977d12e97b7f794a98803429d
Verifier: C3Verifier

### Four Gates
| Gate | Result | Evidence |
|---|---|---|
| Authorized scope | PASS | `git diff --stat ee7a45fa..13b82fde` lists exactly the 10 authorized paths (2225 insertions / 93 deletions) and a single commit `13b82fd feat(fast-p): implement c3` whose parent is `ee7a45fa` (docs-only) and which is the branch HEAD. `git diff --stat` for the five unauthorized paths is empty (no output, exit 0): `src/main/resources/static/styles.css`, `ExpertDiscoveryControllerTest.kt`, `TaskProgressControllerExecutionsTest.kt`, `TaskProgressStore.kt`, `TaskExecutionRepository.kt`. `git diff ee7a45fa..13b82fde -- index.html` is 11 insertions / 11 deletions, all of them `?v=` key substitutions (no DOM / inline-style change). All three Kotlin test files are pure additions (368+/0-, 129+/0-, 81+/0-); `docs/plans/fast/**` is not part of the commit (working tree only). |
| Plan and invariants | PASS | I-1: `ExpertDiscoveryController.kt:101-110` and `:180-196` route both POSTs through `continuousPipeline()` + `normalizeDiscoveryCriteria`; `:407-409` forces `subjectScope=RND_TARGET` and clears `cursor`; `:94` disables scope-excluded sources with the same predicate as `ExpertDiscoveryService.kt:315`; `ExpertDiscoveryScheduler.kt:56-63` new-mode cron only ticks (no once-per-day gate). Diff proof that the legacy path is untouched: the only deleted lines in the controller are :66 (constructor tail) and :90 (the `/sources` mapOf now carrying the scope rule) — every other removed line was verified present verbatim in the head file; likewise the scheduler/task-controller deletions are constructor/signature lines only. Tests: MVC `both manual entries are accepted with the same normalized RND_TARGET query and never run legacy sync` asserts `captured[0] == captured[1]`, `subjectScope == RND_TARGET` and that `discover` / `tryStartWithToken` are never called; scheduler `continuous cron only dispatches the tick and ignores the once-per-day gate` (with a same-day SCHEDULED row stubbed) asserts 2 ticks and no progress-slot use; `a manually paused pipeline is never resumed by cron or the recovery tick` asserts no `launch`/`resume`. Unmodified `ExpertDiscoveryControllerTest` 17/17 and `TaskProgressControllerExecutionsTest` 26/26 stay green (gate 1). I-2: `:412-431` maps a persist exception to 503, `:432-461` maps `applied=false` to 409 (reason + message) and success to 202 with `mode/pipelineId/state/phase/desiredState/queryHash/executionId/resumed`; `:278-300` returns `configured = (queryHash != null)` and no historical task, `mode=LEGACY`/`state=null` when unassembled. Front end: `app.js:982-1004` renders acceptance only, `app.js:975-977` binds only a non-null `status.currentExecutionId`, and `task-modal-runtime.js:90-104` cannot adopt a historical run because `terminalObserved` is set only by the legacy progress path (`:176`) which continuous mode never runs. Tests: `a different query is 409 while a repeated identical query is idempotent`, `a configuration that cannot be persisted is 503`, `pipeline status without a saved query is PAUSED and never impersonates a historical task`, JS `binds only a non-null currentExecutionId from the status snapshot`. I-3: `TaskProgressController.kt:57-73` diverts `EXPERT_DISCOVERY` cancel to `:75-93` (pause() first, then `requestCancel`; gap → 200 PAUSED; pause failure → 503 without cancelling a window); `ExpertDiscoveryController.kt:304-318` explicit resume; `app.js:1421-1466` one button toggles 暂停发现/恢复发现 with `disabled` while pending. Tests: `cancelTask persists the pause first and then requests the window stop` (Mockito `InOrder` pause→requestCancel), `cancelTask during a window gap answers 200 PAUSED instead of 409`, `cancelTask returns 503 without cancelling a window when the pause is not persisted`, `cancelTask of another task type keeps the in-process semantics`, JS pause/resume/pending-disabled cases. I-4: `ExpertDiscoveryScheduler.kt:47-53` registers the recovery tick only when `pipelineEnabled` and uses `pipelineTick.toMillis()`; `:115-127` tick only, exceptions swallowed; `:129-137` `check(!pipelineEnabled)` fails loudly; no sleep anywhere. Test `the recovery tick is registered with the configured interval exactly when the pipeline is enabled` asserts interval 20000 ms, zero fixed-delay tasks when disabled, and tick-only dispatch; `a tick failure never kills the scheduler thread and never falls back to legacy`; `an enabled switch without the coordinator fails loudly`. I-5: `:463-476` state copy; `:478-501` maps all ten wait reasons with `else -> reason` passthrough for raw source errors; `DAILY_BUDGET` formats the real `resetAt`/`retryAt` in Asia/Shanghai; front end `app.js:859-880` (待同步 for unsynced budget), `:871-901` (no daily paper total), `:902-940` (per-source detail, all values `escapeHtml`). Tests: MVC `pipeline status exposes the 02 snapshot with honest texts and no legacy binding` (`waitTexts[0]` formatted reset, `waitTexts[1] == SEARCH_FAILED`, `waitTexts[2]` queue-full), JS `maps every state onto the existing status classes and hides the daily percentage`, `shows real numbers, 待同步 for unsynced budget and tolerates unknown wait reasons`. I-6: `app.js:803` `COEXISTING_TASK_TYPES`, `:1207-1229` per-request-type lock that throws on query failure, `:1231-1237`, callers `:6302-6310` and `:7083-7092`; JS `allows deep discovery and check replies to coexist in both directions`, `still blocks the same type and every other combination` (RAW_PROMOTION_SCAN / EXPERT_ENRICHMENT), `surfaces a failed state query instead of reporting an idle system`, `keeps legacy no-argument callers tolerant of a failed query`. I-7: timeouts `app.js:797-801` (status 10 s, launch 15 s, sources 10 s) enforced by `api(..., {timeoutMs})` at `:1950-1978`; selections frozen before the view switch `:7079-7085`; source-ready gate `:7086-7090`; retryable config restore `:1054-1078`; generation checks `:950`, `:1019-1052`, `:1098-1124`; close-is-not-pause `:1383-1396`. JS cases `freezes the operator selections before switching views and posts with a finite timeout`, `restores the retryable configuration when a different query is refused`, `refuses to start when the source list never loaded`, `never renders into a closed or switched dialog`. One I-7 gap on the modal-open path is recorded as O-1 below (not a gate blocker). S-1: `index.html:1085-1105` nodes and inline styles byte-identical (the file's diff is keys only); `styles.css` zero diff; `app.js:829-845` maps QUEUED/RUNNING/WAITING→running, PAUSED→cancelled, FAULTED→failed, DRAINED→completed (failed + 已排空，存在失败 when `failedItems > 0`); `:950-980` writes only `textContent`/`hidden`/existing classes; `:1303-1315` hides the existing track in continuous mode and restores it for legacy; the only classes used are pre-existing (`task-modal-status`, `task-progress-detail`, `text-muted`, `source-cb` — `source-cb` already existed at base app.js:6614 with zero styles.css rules). S-2: single `#taskModalCancelBtn` (`app.js:1441-1466`), `#taskLaunchDesc` carries `CONTINUOUS_DISCOVERY_DESC` (`:800`, `:6580-6590`) or the source-load error with `canRun:false` and no retry widget, `#taskLaunchRunBtn` re-enabled by `restoreLaunchConfigAfterFailure`; no new button, inline style in index.html, or CSS. |
| Required commands | PASS | (1) `JAVA_HOME=/Library/Java/JavaVirtualMachines/zulu-11.jdk/Contents/Home mvn -B -Dtest=ExpertDiscoveryControllerMvcTest,ExpertDiscoveryControllerTest,ExpertDiscoverySchedulerTest,TaskProgressControllerTest,TaskProgressControllerExecutionsTest test` → exit 0, BUILD SUCCESS, `Tests run: 72, Failures: 0, Errors: 0, Skipped: 0` (MvcTest 4, ControllerTest 17, SchedulerTest 13, TaskProgressControllerTest 12, ExecutionsTest 26); c2 baseline was MvcTest 3 / SchedulerTest 8 / TaskProgressControllerTest 8, and the deltas are exactly this child's new cases. (2) `mvn -B -Dtest=DiscoveryPipelineServiceTest,OpenAlexRequestPolicyTest,ExpertDiscoveryServiceTest test` → exit 0, BUILD SUCCESS, `Tests run: 204, Failures: 0, Errors: 0, Skipped: 0` (41 / 121 / 42), identical to the c2 verification split. (3) `node --check src/main/resources/static/app.js` → exit 0, no output. (4) `node --test src/test/js/*.test.js` → exit 0, `tests 1063 / suites 215 / pass 1063 / fail 0 / skipped 0`, i.e. baseline 1037 + 26 new cases (20 discoveryContinuousRun + 6 taskActivityCenter). (5) `mvn -B -DskipTests clean package` → exit 0, BUILD SUCCESS, `Building war: target/weibo-talent-introduction-1.0.0-SNAPSHOT.war`. Note: command (1) does not execute the two new-mode MVC classes (surefire `-Dtest` without a wildcard matches exact class names), so its 4 MvcTest cases are the legacy ones; see O-3. |
| Downstream interfaces | PASS | Cache-key contract: `index.html:11-15` and `:2128-2133` carry exactly 11 versioned resources, all on `20260922-discovery-continuous` (`task-modal-runtime.js` stays unversioned at `:2127`); `taskActivityCenter.test.js:18-29` derives the live key from the shipped `index.html` (`styles.css?v=…`) instead of pinning a literal, `:48-52` asserts 11 resources and a single unique key, and `:83-90` asserts the key appears in no other shipped/test file. The test has real teeth (checked by in-memory mutation, no file touched): changing one resource's key yields 2 unique keys → the `deepStrictEqual` assertion fails; removing one resource yields 10 → the count assertion fails. Front end ↔ back end field mapping crosses correctly: `app.js` reads `mode`, `status.queryHash`, `status.state/phase/desiredState/currentExecutionId`, `status.waitReasons`, `status.sources[].{source,cursorState,queuedItems,processedItems,activeJobs,failedJobs,indexedExperts,sourceError}`, the counters, `status.budget.{lastSyncedAt,confirmedSpentCredits,officialLimitCredits,reservedCredits,effectiveRemainingCredits,resetAt,retryAt}`, the envelope `waitTexts`, and the launch `state/phase/executionId/message`; every one of these exists in `DiscoveryPipelineStatus` / `DiscoverySourceStatus` / `OpenAlexBudgetSnapshot` and in the controller response maps, and no produced field is silently dropped except values the UI intentionally ignores (the cancel body, whose widened `Map<String, Any>` type does not disturb the unmodified ExecutionsTest 26/26). Continuity contract: reopening the dialog with a saved query shows the status area, `PAUSED` shows 恢复发现, and no code path writes RUNNING on open or on close (`:1383-1396`), so the first deployment stays `PAUSED` until an explicit start/resume. |

### Declared Deviations
| # | Verdict | Evidence |
|---|---|---|
| 1 | CONFORMS | `RND_TARGET` forcing exists only in the new-mode branch (`ExpertDiscoveryController.kt:103-110`, `:184-196`, `:407-409`), which is what I-1 scopes to `pipeline-enabled=true` and what the brief requires for the flag-off path (byte-for-byte legacy). Verified mechanically: the controller's only deletions are 2 lines (constructor tail, `/sources` mapOf) and all other removed lines are still present verbatim in the head file; both legacy `tryStartWithToken` bodies and the legacy cron body are unchanged. |
| 2 | CONFORMS | `/sources` (`ExpertDiscoveryController.kt:90-95`) uses `SubjectScopeCatalog.excludedSources(RND_TARGET)`, exactly the predicate the server applies in `ExpertDiscoveryService.kt:315` / `:1395`, so the front end can no longer submit EUROPE_PMC/PMC_OA as effective sources; the unmodified `ExpertDiscoveryControllerTest` (17/17 in gate 1) still passes. |
| 3 | CONFORMS | Exactly the 11 keys bumped to `20260922-discovery-continuous` in `index.html` with no other change, and `taskActivityCenter.test.js` is a derived-key contract test rather than a pinned literal (see the Downstream interfaces row). Matches brief requirement and plan T-3. |
| 4 | CONFORMS | `pipeline-tick` is a Spring `Duration` (`application.yml:208` = `30s`, `ExpertDiscoveryProperties.kt:60` default 30 s), so `fixedDelayString` would fail at startup; `ExpertDiscoveryScheduler.kt:47-53` instead uses `SchedulingConfigurer.addFixedDelayTask(..., pipelineTick.toMillis())`, the same idiom as `BounceCollectionScheduler` / `DailyCountResetScheduler`, and `@EnableScheduling` exists (`TalentIntroductionApplication.kt:9`). No new config key; the 30 s default is preserved, and the test pins the derived interval. |
| 5 | CONFORMS | Trailing optional dependency with a default (`ExpertDiscoveryController.kt:66-72`, `TaskProgressController.kt:30-35`, `ExpertDiscoveryScheduler.kt:30-38`) satisfies the plan's allowance ("新增末尾可选参数") and keeps every existing constructor call site compiling — gate 1 proves this on both unmodified tests (17/17, 26/26) plus the modified ones. The choice of a nullable service over `ObjectProvider` is a test-double constraint, not a contract change. |
| 6 | CONFORMS | The new per-source table stays inside the existing `#taskModalBySource` / `#taskModalBySourceContent`, adds no DOM node, CSS rule, colour or percentage, and escapes every value (`app.js:902-940`); its inline-style idiom is copied from the pre-existing `renderBySourceTable` (`app.js:1819-1860`). Reusing that renderer verbatim would have stamped discovery counters with the wrong column names (论文/邮箱/有效/收录/晋升), which conflicts with I-5, so the deviation serves the plan rather than contradicting it. |
| 7 | CONFORMS | `app.js:6695-6704` shows the status area with `pipelineMode: true` when a saved query exists and `PAUSED` yields 恢复发现 (`:967-972`), so no re-open path silently overwrites the saved condition; matches I-2 / T-3. |
| 8 | CONFORMS | `app.js:975-977` binds only `status.currentExecutionId`; the 202 `executionId` is never used for binding, the null case is covered by the JS test, and the run-list adoption helper cannot bind a historical run in continuous mode (`task-modal-runtime.js:90-104`, `terminalObserved` unreachable). |
| 9 | CONFORMS | `typeof discoveryPipelineMode !== "undefined"` (`app.js:1122-1124`) follows the existing I1-5 convention in this file; in production the variable is initialised at `app.js:819`, so the continuous branch is taken and the legacy watcher semantics are unchanged. |
| 10 | CONFORMS | The source preload (`app.js:6580-6585`) is now a shared pre-step for both modes with a 10 s timeout and `canRun:false` on failure, and the launch gate is `discoverySources.status === "ready"` (`:7086-7090`); I-7 does not restrict this to the new mode, the server-side legacy logic is untouched, and gate 1 keeps the legacy controller tests green. The related residual gap on the *mode/status* load path is O-1. |
| 11 | CONFORMS | `styles.css` has zero diff from this child (`git diff --stat` empty for that path), and `index.html` carries no inline-style or DOM change; the plan's `styles.css` line numbers were treated as stale, as the brief instructed. |

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1 (I-7 visible-error gap on the modal-open path): in `app.js:6688-6694` a failed or timed-out `GET /api/expert-discovery/pipeline` (10 s timeout) writes `加载失败：…` into `#taskLaunchDesc` and returns; `#taskLaunchDesc` lives inside `#taskProgressModal`, which is declared `hidden` (`index.html:1035`) and is only revealed later at `app.js:6770`, so the operator sees no dialog, no status message and no exit — the opposite of I-7's "visible error" and of T-3's "留在配置区域，显示加载失败：原因". The sibling source-load path does open the dialog because its preload returns `{desc, canRun:false}` instead of throwing. Not an AUTO_FIX because two corrections are equally supported by the plan (reveal the modal before the early return, or surface the failure through `showStatus`), and no test in `discoveryContinuousRun.test.js` exercises `openTaskLaunchModal`/`fetchDiscoveryPipelineInfo`; impact is limited to a misconfigured or unhealthy back end (503 from the new fail-loud path, 500, or a >10 s status call).
- O-2 (stale page-level task button after a refused continuous launch): `handleDiscoveryLaunchFailure` returns from the 409 branch (`app.js:7170-7180`) and the continuous branch (`:7181-7186`) without restoring the page button, while `openTaskModal` had already put `#discoverBtn` into the "执行中" state (`:1326` → `:1248`); the legacy branches restore it via `stopTaskWatcher(taskType, true)` (`:1182`, `:7190`, `:7196`), and `resumeProgressPollingIfNeeded` (`:756-777`) only ever sets buttons running, never restores them — so after a 409/503/timeout the 深度发现 button claims a task is running until a page reload, although the modal itself is correctly restored to the retryable configuration state. Judged P3/cosmetic (the button stays clickable and the modal state is correct), and the plan's "恢复配置/按钮" does not uniquely determine the fix location (`restoreLaunchConfigAfterFailure` vs. the two branches), hence a note rather than a repair demand.
- O-3 (test-invocation surface, outside the light gate): the two new-mode MVC classes in the authorized `ExpertDiscoveryControllerMvcTest.kt` file (`ExpertDiscoveryContinuousMvcTest`, `ExpertDiscoveryPipelineUnavailableMvcTest`, 6 + 1 cases) are not executed by the plan's required command (1), because surefire matches class names exactly without a wildcard; the implementer ran them through an extra command (reported green) and I inspected their assertions directly (202 field set and `RND_TARGET` equality, 409 `QUERY_CONFLICT`, 503 on unpersisted config, resume `NOT_CONFIGURED` → 202, status snapshot with formatted `DAILY_BUDGET` text and no `apiKey`, fail-loud 503 with `discover`/`tryStartWithToken` never called). Recording it because a future child/CI run that only uses the required command would silently skip these seven cases.

### Required Action
- COMPLETE_CHILD
