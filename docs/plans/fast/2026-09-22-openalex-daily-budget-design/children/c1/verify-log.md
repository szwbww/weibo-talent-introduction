## Light Verification: LIGHT_PASS_WITH_NOTES
Child: c1 (docs/plans/2026-09-22/01-openalex-account-budget.md)
Boundary: ee1dfcd5439de54475c12ff51c9c713e984a82ec..2dd074ad28ee9eeb4a350beba0be85a872ef2c12
Verifier: C1Verifier

### Four Gates
| Gate | Result | Evidence |
|---|---|---|
| Authorized scope | PASS | `git log --oneline ee1dfcd..2dd074a` = the single commit `2dd074a feat(fast-p): implement c1`; `git diff --name-only ee1dfcd..2dd074a` returns exactly the 10 authorized paths - V132__create_openalex_budget.sql, OpenAlexBudgetRepository.kt, config/OpenAlexRequestPolicy.kt, config/OpenAlexProperties.kt, config/RestTemplateConfig.kt, discovery/service/OpenAlexDataSource.kt, application.yml, OpenAlexRequestPolicyTest.kt, OpenAlexDataSourceTest.kt, OpenAlexBudgetRepositoryIT.kt; diffstat = 10 files, +2971/-276. No docs/plans/**, ES/mail/frontend/pom path appears in the boundary; V132 was free at base (baseline-java.txt, highest applied = V131). |
| Plan and invariants | PASS | I-1: Operation costs LIST 1/SEARCH 10/SINGLETON 0/CONTENT 100/RATE_LIMIT 0 at OpenAlexRequestPolicy.kt:38-53; explicit-operation entry :659, host/path validation :968-998 (deriveOperation :981), CONTENT refused before any ledger row :661-666; callers declare the operation explicitly (OpenAlexDataSource.kt:56 keywords=>SEARCH else LIST, :75-88 reserve->settle/recordTimeout, :279 /authors/{id}=>SINGLETON, :306/:352/:425 LIST, :159/:183 metered hosts never a fulltext candidate); tests OpenAlexRequestPolicyTest.kt:124,133,154,171 + OpenAlexDataSourceTest.kt:1180,1196,1062,1084,1096,1223. I-2: effective free limit = min(official, protection, legacy usd) OpenAlexRequestPolicy.kt:1025-1038, prepaid never read; stable scope + negative config fails startup OpenAlexProperties.kt:42,47,49,52-63; V132 has no API-key column; tests OpenAlexRequestPolicyTest.kt:194,205,214,232,243,251,268,281,358. I-3: available = min(free-confirmed, ceiling) - outstanding OpenAlexBudgetRepository.kt:56; reserve locks account->day->permit, applies the floor, inserts exactly one permit and outstanding += reserved :89-138; settle exactly once with actualCredits==null => reserved amount (no refund), evidenced-only refund, LEAST-only ceiling/limit :140-179; markUnknown keeps outstanding :180-190; tests OpenAlexRequestPolicyTest.kt:369,383,396,408,425,659, IT :168,:200. I-4: store failure => BUDGET_STORE_UNAVAILABLE and no trusted cycle => BUDGET_SYNC (OpenAlexRequestPolicy.kt:798-841), trusted-ledger gate :882-899, calibration lease+throttle :750-797, shared 429 notBefore written to the account row OpenAlexBudgetRepository.kt:192-203, reconcile accepts only unexpired cycles and closes old rows :229-274, lease CAS :282-303; tests OpenAlexRequestPolicyTest.kt:511,546,557,573,600,494, IT :242,:255,:273,:388. I-5: reserve = min(target, pending x estimate, effectiveRemaining) with 10-credit minimum and 60 s idle release OpenAlexRequestPolicy.kt:900-925; unfinished = PENDING/RUNNING/RETRY_WAIT and samples = settled NEW_ENRICHMENT costs, matching the real V131 CHECK, OpenAlexBudgetRepository.kt:316-345; only NEW_ENRICHMENT may borrow :19-28; tests OpenAlexRequestPolicyTest.kt:685,711,725,290, OpenAlexDataSourceTest.kt:1358, IT :409. I-6: V132 gives account PK scope, day UNIQUE(scope,reset_at), reservation PK permit_id + day_id link, status/kind/operation/amount CHECKs, DATETIME(3) UTC, account-level rate/cooldown/lease/last_synced; fixed lock order OpenAlexBudgetRepository.kt:402,:429,:464, UTC helpers :475-477, bounded cleanup of closed cycles only :348-374; tests IT :315,:368,:293, OpenAlexRequestPolicyTest.kt:742,775. |
| Required commands | PASS | (1) DOCKER_HOST=unix:///Users/lukai/.orbstack/run/docker.sock JAVA_HOME=/Library/Java/JavaVirtualMachines/zulu-11.jdk/Contents/Home mvn -B -Dtest=OpenAlexRequestPolicyTest,OpenAlexDataSourceTest,OpenAlexBudgetRepositoryIT -DmysqlIt=true -Dapi.version=1.40 test -> exit 0, Tests run: 119, Failures: 0, Errors: 0, Skipped: 0 (Policy 40 / DataSource 67 / IT 12); the IT really executed on Testcontainers MySQL (log: Creating container for image: mysql:8.0.36, Container mysql:8.0.36 started in PT10.651276S, JDBC URL: jdbc:mysql://localhost:32833/talent_introduction, Flyway Migrating schema talent_introduction to version "132 - create openalex budget"); the same test phase ran the node suite via exec-maven-plugin (tests 1037 / pass 1037). (2) JAVA_HOME=/Library/Java/JavaVirtualMachines/zulu-11.jdk/Contents/Home mvn -B -Dtest=ExpertDiscoveryServiceTest,RestTemplateConfigTest,ExpertDiscoveryControllerMvcTest,ExpertDiscoveryControllerTest,ExpertDiscoverySchedulerTest,TaskProgressControllerTest,TaskProgressControllerExecutionsTest,ExpertAcademicEnrichmentWorkerTest test -> exit 0, Tests run: 211, Failures: 0, Errors: 0, Skipped: 0 (18/17/3/8/121/10/26/8), BUILD SUCCESS. (3) node --test src/test/js/*.test.js -> exit 0, tests 1037 | suites 207 | pass 1037 | fail 0 | cancelled 0 | skipped 0, identical to baseline-java.txt section 1. |
| Downstream interfaces | PASS | Explicit-operation reserve path OpenAlexRequestPolicy.reserve(kind, operation, targetUrl): Permit at OpenAlexRequestPolicy.kt:659 returning Permit.Allowed(permitId) :78 or Permit.Deferred(reason, retryAt) :81; compatibility entries kept, beforeRequest :673, recordResponse(permitId, headers) :678, recordResponse(headers) :714, recordTimeout :740; the five reasons are exactly the brief's names and order, DeferredReason :58-79, and every Deferred carries retryAt :81; read-only snapshot OpenAlexBudgetSnapshot :545-557 has exactly accountScope, resetAt, officialLimitCredits, officialRemainingCredits, confirmedSpentCredits, reservedCredits, effectiveRemainingCredits, enrichmentReserveCredits, lastSyncedAt, deferredReason, retryAt, exposed by snapshot() :614; config keys account-scope/free-budget-credits/budget-sync-interval = application.yml:225,226,228 (defaults primary/10000/300s, all env-overridable) with the existing 5/s :216 and 0.2 ratio :217 preserved (OpenAlexProperties.kt:30,32,42,47,49). No credential exposure: V132 has no key column (read in full), the snapshot data class has no key field, the key is referenced only as properties.apiKey.isNotBlank() (OpenAlexRequestPolicy.kt:1026) and in the host-scoped interceptor (RestTemplateConfig.kt:469-494, isTrustedOrigin :498), the sync source logs status/message only, and OpenAlexProperties.toString() masks the key. |

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1 (code defect reported to the author; not a four-gate violation because the plan does not uniquely determine the fix): OpenAlexRequestPolicy.kt:859-864 computes waitMs = Duration.between(time.now(), result.retryAt).toMillis() and, because waitMs <= inlineWaitCapMs() is also true for non-positive values, calls time.sleep(waitMs) (PolicyTimeSource.SYSTEM = Thread.sleep(ms) at :141). A negative wait throws IllegalArgumentException out of reserve() instead of returning Permit.Deferred. result.retryAt is derived from the account rate slot or 429 cooldown relative to the now captured at :845 before the store round trip (OpenAlexBudgetRepository.kt:94,:96), so any elapsed time exceeding the remaining slot/cooldown inside that round trip - a lock wait, or the forced/stale calibration HTTP call on the ensureTrustedLedger path - makes the wait negative; the pre-change policy guarded this with if (sleepMs > 0).
- O-2 (coverage note): the JDBC IT always passes reservedFloorCredits = 0L (OpenAlexBudgetRepositoryIT.kt:459), so the MySQL store's floor rejection branch (OpenAlexBudgetRepository.kt:114-117, the ENRICHMENT_RESERVE reason) is never exercised on real MySQL; that semantics is covered only through the in-memory store (OpenAlexRequestPolicyTest.kt:290, OpenAlexDataSourceTest.kt:1358). Plan I-5 acceptance is satisfied by the unit tests, so this stays outside the light gate.
- O-3 (c2/c3 consumption caveat): snapshot().deferredReason/retryAt come from the process-local lastDeferral (OpenAlexRequestPolicy.kt:616,:650), which is only set when a reserve attempt was actually deferred. A freshly started process with no trusted official cycle reports deferredReason = null together with effectiveRemainingCredits = the configured protection (10000) and resetAt = null, even though metered requests are deferred as BUDGET_SYNC (:882-899, OpenAlexBudgetRepository.kt:110-112); the field list itself matches the brief exactly, so this is outside the light gate.
- O-4 (bounded blocking, deliberate per tests): waits within rate-limit-backoff-max-ms (default 60000) are served inline in the calling thread and the loop allows MAX_RATE_WAIT_RETRIES = 3 attempts (OpenAlexRequestPolicy.kt:859-864,:1006,:1069), i.e. up to about 180 s of sleeping in a scheduler/HTTP thread after a 429, whereas I-4 prefers expressing the wait as Deferred(reason, retryAt). It is bounded, happens outside any database-lock transaction, and is asserted by OpenAlexRequestPolicyTest.kt:494 (cap 0 => never waits) and :600, so it is outside the light gate.

### Required Action
- COMPLETE_CHILD

## Light Verification: LIGHT_PASS_WITH_NOTES
Child: c1 (docs/plans/2026-09-22/01-openalex-account-budget.md)
Boundary: ee1dfcd5439de54475c12ff51c9c713e984a82ec..b96470e4be86408b185c2fbdd2fb0037e8f4fc2c
Verifier: C1Reverifier

### Four Gates
| Gate | Result | Evidence |
|---|---|---|
| Authorized scope | PASS | `git log --oneline ee1dfcd..b96470e` = 2 commits: `2dd074a feat(fast-p): implement c1` and `b96470e fix(fast-p): repair c1 round 1` (parent 2dd074a). `git diff --name-only ee1dfcd..b96470e` = exactly the 10 authorized paths (V132__create_openalex_budget.sql, OpenAlexBudgetRepository.kt, config/OpenAlexRequestPolicy.kt, config/OpenAlexProperties.kt, config/RestTemplateConfig.kt, discovery/service/OpenAlexDataSource.kt, application.yml, OpenAlexRequestPolicyTest.kt, OpenAlexDataSourceTest.kt, OpenAlexBudgetRepositoryIT.kt); diffstat 10 files changed, 2999 insertions(+), 276 deletions(-). Round 1 touched only OpenAlexRequestPolicy.kt (+7) and OpenAlexRequestPolicyTest.kt (+21). No docs/plans/**, pom, migration renumber, ES, mail or frontend path appears in the boundary; `git status --porcelain` shows only the untracked fast-p evidence directory. V132 is still the only new migration (highest at base = V131, baseline-java.txt). |
| Plan and invariants | PASS | I-1: cost enum LIST 1/SEARCH 10/SINGLETON 0/CONTENT 100/RATE_LIMIT 0 at OpenAlexRequestPolicy.kt:38-53; explicit-operation entry reserve(kind, operation, targetUrl) :659 with CONTENT refused before any ledger row :661-666; host/path validation (never arbitrary substring) validateOperationTarget :975-985 + structural deriveOperation :988-1010; callers declare the operation explicitly OpenAlexDataSource.kt:56, :282, :309, :355, :425, and reserve→settle/recordTimeout is the single HTTP seam :74-89; metered hosts are never public fulltext candidates :183-184. I-2: effective free limit = min(official, freeBudgetCredits, legacy dailyBudgetUsd) at :1040-1045, prepaid balance never read; accountScope/freeBudgetCredits/budgetSyncInterval in OpenAlexProperties with negative-value startup failure; V132 has no API-key column and the snapshot has no key field. I-3: available = min(free_limit − confirmed_spent, provider_ceiling) − outstanding_reserved OpenAlexBudgetRepository.kt:49-57; reserve locks account→day in one transaction and inserts exactly one permit + outstanding increment :90-137; settle is exactly-once with null-actual=reserved amount, evidenced-only refund and LEAST-only ceiling/limit :140-179; markUnknown keeps outstanding :180-190. I-4: store failure → BUDGET_STORE_UNAVAILABLE and untrusted cycle → BUDGET_SYNC, never a local 10000 (OpenAlexRequestPolicy.kt:814-841, ensureTrustedLedger :889-906); shared 429 notBefore written to the account row OpenAlexBudgetRepository.kt:192-203; reconcile accepts only unexpired cycles and closes old rows :229-274; sync lease CAS :282-303. I-5: reserve = min(floor(freeLimit×ratio), pending×estimate, effectiveRemaining) with 10-credit minimum and 60 s idle release OpenAlexRequestPolicy.kt:907-932; unfinished = PENDING/RUNNING/RETRY_WAIT and samples = settled NEW_ENRICHMENT costs OpenAlexBudgetRepository.kt:316-345. I-6: V132 (read in full) gives account PK account_scope, day UNIQUE(account_scope, reset_at), reservation PK permit_id + day_id cycle link, status/kind/operation/amount CHECKs, DATETIME(3) UTC columns, account-level rate_next_at/cooldown_until/lease/last_synced_at; fixed lock order account→day→reservation OpenAlexBudgetRepository.kt:402,:429,:464; bounded cleanup of closed cycles only :348-374. Round 1 altered none of these seams — its diff is the rate-wait guard plus the test only. |
| Required commands | PASS | (1) `DOCKER_HOST=unix:///Users/lukai/.orbstack/run/docker.sock JAVA_HOME=/Library/Java/JavaVirtualMachines/zulu-11.jdk/Contents/Home mvn -B -Dtest=OpenAlexRequestPolicyTest,OpenAlexDataSourceTest,OpenAlexBudgetRepositoryIT -DmysqlIt=true -Dapi.version=1.40 test` → exit 0, BUILD SUCCESS, `Tests run: 120, Failures: 0, Errors: 0, Skipped: 0` (Policy 41, DataSource 67, IT 12); the IT really executed against Testcontainers MySQL (log: `Creating container for image: mysql:8.0.36` → `Container mysql:8.0.36 started in PT9.63S` → `JDBC URL: jdbc:mysql://localhost:32837/talent_introduction`, `Successfully applied 131 migrations ... now at version v132`, 23.4 s, not skipped); the same test phase ran the node suite via exec-maven-plugin (tests 1037 / pass 1037 / fail 0). (2) `JAVA_HOME=/Library/Java/JavaVirtualMachines/zulu-11.jdk/Contents/Home mvn -B -Dtest=ExpertDiscoveryServiceTest,RestTemplateConfigTest,ExpertDiscoveryControllerMvcTest,ExpertDiscoveryControllerTest,ExpertDiscoverySchedulerTest,TaskProgressControllerTest,TaskProgressControllerExecutionsTest,ExpertAcademicEnrichmentWorkerTest test` → exit 0, BUILD SUCCESS, `Tests run: 211, Failures: 0, Errors: 0, Skipped: 0` (RestTemplateConfigTest 18, ExpertDiscoveryControllerTest 17, ExpertDiscoveryControllerMvcTest 3, ExpertDiscoverySchedulerTest 8, ExpertDiscoveryServiceTest 121, ExpertAcademicEnrichmentWorkerTest 10, TaskProgressControllerExecutionsTest 26, TaskProgressControllerTest 8) plus node 1037/1037. (3) `node --test src/test/js/*.test.js` → exit 0, tests 1037 / suites 207 / pass 1037 / fail 0 / cancelled 0 / skipped 0 / todo 0, identical to baseline-java.txt section 1. All three freshly executed in this invocation at HEAD b96470e. |
| Downstream interfaces | PASS | Explicit-operation path `reserve(kind, operation, targetUrl): Permit` at OpenAlexRequestPolicy.kt:659 returning Permit.Allowed(permitId) :78 or Permit.Deferred(reason, retryAt) :81; compatibility entries kept: beforeRequest :673, recordResponse(permitId, headers) :678, recordResponse(headers) :714, recordTimeout :740; DeferredReason :58-73 carries exactly DAILY_BUDGET / RATE_LIMIT / BUDGET_SYNC / BUDGET_STORE_UNAVAILABLE / ENRICHMENT_RESERVE and every Deferred carries retryAt; read-only OpenAlexBudgetSnapshot :545-557 has exactly the 11 brief fields, exposed by snapshot() :614; syncOfficialBudget(force) :750 is the seam c3's scheduler tick calls; cleanupArchivedCycles :800. Consumer dispatch is explicit: OpenAlexDataSource.getJson maps `Permit.Deferred` → OpenAlexBudgetDeferredException(reason, retryAt) at OpenAlexDataSource.kt:75-77 before any HTTP call, so a deferred permit can never reach the external request. Config keys account-scope/free-budget-credits/budget-sync-interval = application.yml:225,226,228 (defaults primary/10000/300s, env-overridable) with the existing 5.0/s :216 and 0.2 ratio :217 preserved; production assembles the single bean sharedOpenAlexRequestPolicy with mandatory OpenAlexBudgetStore + OpenAlexBudgetSyncSource (RestTemplateConfig.kt:144-150) — the only other constructions are the non-bean compatibility factory :156 and the test-only default parameter, so there is no production in-memory fallback; V132 and the snapshot contain no credential. Line-pinned guard OperatorStatusWriteSeamGuardTest pins only files outside this child's 10 (ExpertSearchService.kt:498, MailRecordRepository.kt:612/669, etc.) and its ALLOWED_WRITE_SITES (ExpertOperatorStatusService.kt, ManualInitialOutreachService.kt) are untouched, so no pin moved; application.yml still has `placeholder-replacement: false` (:13) and V132 contains no `${...}`, so the known Flyway placeholder trap is not triggered. |

### Fix Round 1 Re-check
| Finding | Status | Evidence |
|---|---|---|
| F-1 | FIXED | OpenAlexRequestPolicy.kt:859-866: `waitMs = Duration.between(time.now(), result.retryAt).toMillis()` now passes `if (waitMs <= 0L && attempt < MAX_RATE_WAIT_RETRIES) { attempt++; continue }` before the sleep branch, so the only `time.sleep(waitMs)` call site (:868) can never receive a non-positive value; when the retry budget is exhausted the elapsed-wait condition is guarded by `attempt < MAX_RATE_WAIT_RETRIES` (MAX_RATE_WAIT_RETRIES = 3 at :1076), the guard does not fire, the inline-wait branch also fails on the same attempt bound, and control reaches `return defer(DeferredReason.RATE_LIMIT, result.retryAt)` — callers still get Permit.Deferred(RATE_LIMIT, retryAt), so bounded retry semantics and the exhausted-retry fallthrough are unchanged. Regression proof: `OpenAlexRequestPolicyTest.a rate-limit rejection whose retryAt already elapsed defers instead of sleeping (I-4)` (OpenAlexRequestPolicyTest.kt:618-636) advances the fake clock 5 s inside the store round trip while returning retryAt = now+200 ms, then asserts Permit.Deferred(RATE_LIMIT, slot) and no non-positive sleep; FakeTime.sleep now throws IllegalArgumentException for negative values (OpenAlexRequestPolicyTest.kt:34-35), matching Thread.sleep. Fresh run: OpenAlexRequestPolicyTest 41/41 pass within gate 1 (120 total, 0 failures). |

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-2 (coverage note): the JDBC IT always passes reservedFloorCredits = 0L (OpenAlexBudgetRepositoryIT.kt:459), so the MySQL store's floor rejection branch (OpenAlexBudgetRepository.kt:114-117, the ENRICHMENT_RESERVE reason) is never exercised on real MySQL; that semantics is covered only through the in-memory store (OpenAlexRequestPolicyTest.kt:290, OpenAlexDataSourceTest.kt:1358). Plan I-5 acceptance is satisfied by the unit tests, so this stays outside the light gate.
- O-3 (c2/c3 consumption caveat): snapshot().deferredReason/retryAt come from the process-local lastDeferral (OpenAlexRequestPolicy.kt:616,:650), which is only set when a reserve attempt was actually deferred. A freshly started process with no trusted official cycle reports deferredReason = null together with effectiveRemainingCredits = the configured protection (10000) and resetAt = null, even though metered requests are deferred as BUDGET_SYNC (:882-899, OpenAlexBudgetRepository.kt:110-112); the field list itself matches the brief exactly, so this is outside the light gate.
- O-4 (bounded blocking, deliberate per tests): waits within rate-limit-backoff-max-ms (default 60000) are served inline in the calling thread and the loop allows MAX_RATE_WAIT_RETRIES = 3 attempts (OpenAlexRequestPolicy.kt:859-864,:1006,:1069), i.e. up to about 180 s of sleeping in a scheduler/HTTP thread after a 429, whereas I-4 prefers expressing the wait as Deferred(reason, retryAt). It is bounded, happens outside any database-lock transaction, and is asserted by OpenAlexRequestPolicyTest.kt:494 (cap 0 => never waits) and :600, so it is outside the light gate.

### Required Action
- COMPLETE_CHILD
