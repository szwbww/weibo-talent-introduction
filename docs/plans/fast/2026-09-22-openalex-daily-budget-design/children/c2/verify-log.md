## Light Verification: LIGHT_FAIL
Child: c2 (docs/plans/2026-09-22/02-discovery-paper-queue.md)
Boundary: 6aa09e3de91b705dfb3e423bff71d24d37e4b080..58b96c7b2f66aee703eede6c283ce807ea9d16fe
Verifier: C2Verifier

### Four Gates
| Gate | Result | Evidence |
|---|---|---|
| Authorized scope | PASS | `git show --name-only 58b96c7` = exactly the 10 authorized files (V133 migration; DiscoveryPaperQueueRepository.kt; DiscoveryPipelineService.kt; ExpertDiscoveryService.kt; DiscoveryExecutorConfig.kt; ExpertDiscoveryProperties.kt; application.yml; DiscoveryPipelineServiceTest.kt; DiscoveryPaperQueueRepositoryIT.kt; RestTemplateConfig.kt); `git status --porcelain` shows only `docs/plans/fast/**` uncommitted, so plans are excluded from the commit; V133 was free at base (baseline-java.txt: highest applied = V132); `RestTemplateConfigTest.kt` / `ExpertDiscoveryServiceTest.kt` are NOT in the diff and both stay green (18 / 121 in gate 1). |
| Plan and invariants | FAIL | F-1: I-1 per-source stream identity is implemented but never wired (streams are keyed on the pipeline-level hash). Everything else verified in code + tests: I-1 page+cursor transaction (Repository.enqueuePage:775-855; IT :199/:221/:247/:264), I-2 identity/version (ExpertDiscoveryService.paperIdentity:1719 / envelopeForOrcid:1689; IT :298/:320/:334), I-3 lease/generation CAS (Repository.claimJob:965, saveExtraction:1009, completeJob:1052, scheduleRetry:1183, returnToPending:1214; IT :380/:412/:442/:482/:515), I-4 save-then-consume (Service.processJob:951; ExpertDiscoveryService.consumeOutcomeInternal:1223 / consumeQueuedItem:1616; tests :1699/:1717/:1742/:1760/:1780), I-5 capacity (Repository.enqueuePage:775, saveExtraction:1009, insertFailedItem:1252; IT :537/:559/:581/:601), I-6 fairness/per-host gate (Service.producePages:765 / claimNextJob:922; RestTemplateConfig.kt:266-440 FulltextRequestGate; tests :1898/:1937/:2061), I-7 control/window (Repository.launch:388 / pause:437 / claimOwner:493 / releaseStaleOwner:553; Service.launch:301 / pause:354 / resume:377 / tick:414; tests :1516/:1567/:1580/:1607/:1641/:1659), I-8 counters/cleanup (Repository.completeJobWithExperts:1119 / clearTerminalPayloads:1351 / deleteTerminalJobs:1391; tests :2088/:2106/:2131; IT :650/:801). MySQL lease/capacity/CAS semantics are evidenced by the IT really executing (31 tests, 25.577 s, `mysqlIt=true`, Docker required, schema built by Flyway), not by mocks. |
| Required commands | PASS | (1) `DOCKER_HOST=unix:///Users/lukai/.orbstack/run/docker.sock JAVA_HOME=…/zulu-11 mvn -B -Dtest=DiscoveryPipelineServiceTest,DiscoveryPaperQueueRepositoryIT,ExpertDiscoveryServiceTest,RestTemplateConfigTest -DmysqlIt=true -Dapi.version=1.40 test` → exit 0 / BUILD SUCCESS; surefire 42+31+121+18 = 212 tests, 0 failures, 0 errors, 0 skipped; IT ran 25.577 s on mysql:8.0.36 (not skipped). (2) `JAVA_HOME=…/zulu-11 mvn -B -Dtest=ExpertDiscoveryControllerMvcTest,ExpertDiscoveryControllerTest,ExpertDiscoverySchedulerTest,ExpertAcademicEnrichmentWorkerTest test` → exit 0 / BUILD SUCCESS; 3+17+8+10 = 38 tests, 0 failures/errors/skipped. (3) `node --test src/test/js/*.test.js` → exit 0; tests 1037 / suites 207 / pass 1037 / fail 0 / skipped 0 (identical to baseline-java.txt). (4) `JAVA_HOME=…/zulu-11 mvn -B -DskipTests clean package` → BUILD SUCCESS, war assembled + repackaged. |
| Downstream interfaces | PASS | Shapes match the brief: `launch(criteria,triggeredBy,includeRawScan)`:301 (pipelineId=1, phase, nullable executionId; 409 prepared as `PipelineRejection(QUERY_CONFLICT|NOT_CONFIGURED)` for c3’s controller), `pause()`:354 (persisted PAUSED, no running task needed, returns phase + in-flight counts), `resume()`:377 (saved query only, no new epoch), `tick()`:414 (short transaction, PAUSED/OWNED/RECOVERY/NOT_DUE/DRAINED/REJECTED, dispatch to `pipelineCoordinatorExecutor`, rejection keeps QUEUED), `status()`:517 (state derived PAUSED-first, phase, desiredState, currentExecutionId, queryHash, per-source rows, I-8 counters, queueDepth/bytes/oldest, waitReasons, nextWakeAt, budget snapshot). PipelineWaitReason (Repository.kt:27-37) = exactly the brief’s 7 reasons; the 6 termination literals live in a separate PipelineTerminationReason object (Service.kt:74-81) as the plan’s termination list. Window task record: one EXPERT_DISCOVERY row per window via the existing `TaskExecutionService.runAndRecordWithResult` + `onStarted → repository.bindExecutionId` (Service.kt:612-625), result implements `TaskExecutionSummaryProvider` with the ordered reason→status mapping (Service.kt:190-206), progress reuses the existing status vocabulary via `DiscoveryTerminalStatus.toProgressStatus`. Config keys application.yml:191-209 match the brief exactly (pipeline-enabled=false, 20000/10000, 1073741824, 65536, 32768, per-host 2, 30s, 4h window reused from time-budget, pipeline-fetch-concurrency=8, legacy discoveryFetchExecutor untouched) with startup validation (ExpertDiscoveryProperties.kt:66-101). |

### Declared Deviations
| # | Verdict | Evidence |
|---|---|---|
| 1 | CONFORMANT | `DiscoveryPaperQueueStore` (Repository.kt:217) and its JDBC implementation (:363) are both inside authorized file 2; no production class was added outside the 10; same seam style as c1’s `OpenAlexBudgetStore`; the plan’s "所有新写路径收口于DiscoveryPaperQueueRepository" still holds. |
| 2 | CONFORMANT | `priority TINYINT NOT NULL DEFAULT 0` V133:125 — additive column on a **new** table only, needed to satisfy I-6’s SQL-level "prefer publicly downloadable work within a source" (Repository.nextDueJob:920-940, nextDueOrdinaryJob:953); no shared/legacy table and no ES field touched; the plan says the three new tables’ columns are governed by I-1..I-8. |
| 3 | CONFORMANT | `guardAgainstNonAdvancingSource` (Service.kt:747, call site :867, MAX_NO_PROGRESS_ROUNDS=3 :1395) converts a same-cursor/non-exhausted page loop into a stream `SOURCE_ERROR` + 5-minute backoff through the existing `recordStreamError`; it neither moves/resets the cursor nor marks exhaustion nor consumes job attempts — consistent with I-1 (cursor advances only with the page transaction) and I-6 (a stuck source must not block others); test :1675. |
| 4 | CONFORMANT (required) | `Repository.pause:437-457` computes `generation = generation + CASE WHEN desired_state = …` **before** assigning `desired_state`, which is correct for MySQL’s left-to-right SET evaluation; I-3/I-7 require the pause to stop new claims, and the IT asserts generation 0→1 on RUNNING→PAUSED and 1 on a second pause (IT :693). |

### AUTO_FIX
- F-1 — governing requirement: I-1 ("每来源规范化有效条件生成query_hash；…不包含其他sources的排列/省略方式。stream唯一(query_hash,source,epoch)") and its acceptance criterion ("sources省略/排序不同但有效同源条件相同得到相同stream"). Evidence: stream rows are created with the **pipeline-level** hash — `DiscoveryPipelineService.kt:312` (`queryHash = queryHashOf(normalized)`), `:637` (`val queryHash = initial.queryHash`), `:646-652` (`repository.ensureStream(…, queryHash = queryHash, source = source, epoch = 1, …)`), where `queryHashOf` (:1188) hashes `DiscoveryCheckpointCodec.canonicalCriteria`, which includes `"src=" + normalizeSet(criteria.sources)` (DiscoveryCheckpointCodec.kt:73). The per-source hash `ExpertDiscoveryService.queueQueryHash` (:1415), documented as "与其他 sources 的排列/省略方式无关", has **no production caller** (`grep -rn queueQueryHash src/main` → definition only; the sole caller is DiscoveryPipelineServiceTest.kt:1160-1166, whose message is "sources 省略与显式列出必须同 stream"). Impact: launching the same effective query with the sources field written differently (`sources=[]` = all enabled vs an explicit list, or adding/removing one entry) produces a different pipeline query hash, so `ensureStream` inserts brand-new `(query_hash, source, epoch)` rows (V133:107) with `cursor_value = NULL` / `cursor_state = ACTIVE`; every source’s stored position — including `EXHAUSTED` — is abandoned and re-collected from page 0, and works already processed in the old stream are re-enqueued as new jobs (dedup is `UNIQUE(stream_id, item_key)`, V133:141), re-downloading pages and re-inflating the `queued_/processed_` counters (I-1’s own violation consequence "每天反复扫相同页"). Smallest authorized correction: at the stream creation site in `DiscoveryPipelineService.kt` (authorized file 3) use the per-source hash — `queryHash = expertDiscoveryService.queueQueryHash(source, criteria)` for the `:648` argument — while keeping the pipeline row’s `query_hash` as the query identity used by `launch` idempotency/conflict detection; nothing else reads `stream.query_hash`.

### RECORD_ONLY
- O-1: `releaseWindow` (Service.kt:1228-1236) always sets `next_wake_at = now + pipelineTick` instead of the earliest progressable time, and `hasProgressableWork` (:1140-1145) returns true when `streams.isEmpty()`; so while a launch is RUNNING with all sources deferred/backing off — or with a criteria whose enabled-source set resolves to nothing (`resolveEnabledSources` yields an empty list for an unknown/disabled source name) — `tick()` opens a window that ends immediately, producing one EXPERT_DISCOVERY task record per tick with 0 processed / 0 indexed. I-7’s "无操作需求不循环创建空task_execution" is implemented and tested only for PAUSED/DRAINED (DiscoveryPipelineServiceTest.kt:1659-1672). Outside the light gate: T-3 also prescribes "release the window, don’t sleep until reset", so the strict reading is arguable and the correction (nextWake = earliest progressable time, or rejecting an unresolvable source set at launch) is not uniquely determined by the plan.
- O-2: `status()` merges raw `stream.source_error` strings into `waitReasons` (Service.kt:538-540), so that list can carry values outside the brief’s 7-reason set (e.g. `SEARCH_FAILED`, `NO_PROGRESS`) alongside the documented constants (the constants themselves match). c3 must tolerate unknown values.
- O-3: `DiscoveryPaperQueueStore.forceFailForLeaseLoss` (Repository.kt:331, impl :1235) has no production caller and, unlike `completeJob`, leaves `active_count` / `processed_*` / `failed_items` and the result reservation untouched — a dead seam that would desync the capacity ledger if ever invoked.
- O-4: a window whose deadline expires exactly while the queue is already drained reports `WINDOW_END → PARTIAL_SUCCESS` although the pipeline phase becomes DRAINED (Service.kt:204-206 else-branch vs the `drained` computation at :712-718); the "drained ⇒ SUCCESS" behaviour of the child’s test holds only for the SOURCE_EXHAUSTED reason.

### Required Action
- AUTO_FIX

## Light Verification: LIGHT_PASS_WITH_NOTES
Child: c2 (docs/plans/2026-09-22/02-discovery-paper-queue.md)
Boundary: 6aa09e3de91b705dfb3e423bff71d24d37e4b080..ef1eb2b8c1e17aabaf04178f5f7931852872a42b
Verifier: C2Reverifier

### Four Gates
| Gate | Result | Evidence |
|---|---|---|
| Authorized scope | PASS | `git diff --name-only 6aa09e3..ef1eb2b` = exactly the 10 authorized paths (V133 migration; DiscoveryPaperQueueRepository.kt; DiscoveryPipelineService.kt; ExpertDiscoveryService.kt; DiscoveryExecutorConfig.kt; ExpertDiscoveryProperties.kt; application.yml; DiscoveryPipelineServiceTest.kt; DiscoveryPaperQueueRepositoryIT.kt; RestTemplateConfig.kt); fix commit `ef1eb2b --stat` touches only authorized files 3 and 8 (DiscoveryPipelineService.kt 4+/1-, DiscoveryPipelineServiceTest.kt 75+/13-), adds no new file; `git status --porcelain` shows only `docs/plans/fast/**` uncommitted (`ledger.md` M, `children/c2/` untracked) so plans stay out of both commits; `V133__create_discovery_paper_queue.sql` is unique in `db/migration` with `V132__create_openalex_budget.sql` from c1 present and baseline highest-at-base `V131` (baseline-java.txt:37); `RestTemplateConfigTest.kt` / `ExpertDiscoveryServiceTest.kt` are not in the boundary diff and stay green unmodified (18 / 121 in gate 1). |
| Plan and invariants | PASS | F-1 fixed (see re-check). I-1 production identity: `DiscoveryPipelineService.kt:651` passes `expertDiscoveryService.queueQueryHash(source, criteria)` (per-source normalized hash — ExpertDiscoveryService.kt:1415-1419 normalizes `sources=[source])`, so writing/omitting other sources cannot split or abandon a stream) while the pipeline row keeps `queryHashOf(normalized)` (`:312`) as the launch idempotency/conflict identity, exposed by `launch`(:341)/`resume`(:399)/`status`(:556); `findStream` has no caller other than repository `ensureStream` (`DiscoveryPaperQueueRepository.kt:647/:663`), so nothing else keys streams on the pipeline hash; a reused stream cannot collect under stale conditions because collection uses `state.criteria` normalized by `queueCriteria` (`DiscoveryPipelineService.kt:805` → `ExpertDiscoveryService.kt:1491`) and `seedLegacyCursor` (:1135-1141) only seeds pristine streams. I-2..I-8 and the round-1 deviation dispositions are untouched by the fix commit (single-argument diff) and re-executed green: DiscoveryPipelineServiceTest 42/42 (covers I-1..I-8 incl. `stream identity is the per-source hash, not the pipeline hash (I-1)` :1163, page+cursor transaction, capacity, lease/CAS, save-then-consume, fairness, pause/window, counters), DiscoveryPaperQueueRepositoryIT 31/31 on real MySQL. |
| Required commands | PASS | (1) `DOCKER_HOST=unix:///Users/lukai/.orbstack/run/docker.sock JAVA_HOME=…/zulu-11 mvn -B -Dtest=DiscoveryPipelineServiceTest,DiscoveryPaperQueueRepositoryIT,ExpertDiscoveryServiceTest,RestTemplateConfigTest -DmysqlIt=true -Dapi.version=1.40 test` → exit 0 / BUILD SUCCESS; `Tests run: 212, Failures: 0, Errors: 0, Skipped: 0` (surefire: Pipeline 42, IT 31, ExpertDiscoveryServiceTest 121, RestTemplateConfigTest 18); the IT really executed — `Creating container for image: mysql:8.0.36` (00:06:58) then `Successfully applied 132 migrations … now at version v133` (00:07:31), 37.987 s, not skipped. (2) `JAVA_HOME=…/zulu-11 mvn -B -Dtest=ExpertDiscoveryControllerMvcTest,ExpertDiscoveryControllerTest,ExpertDiscoverySchedulerTest,ExpertAcademicEnrichmentWorkerTest test` → exit 0 / BUILD SUCCESS; surefire 3+17+8+10 = 38 tests, 0 failures, 0 errors, 0 skipped (reports rewritten at 00:09 this invocation). (3) `node --test src/test/js/*.test.js` → exit 0; `tests 1037 / suites 207 / pass 1037 / fail 0 / cancelled 0 / skipped 0` (baseline-java.txt identical). |
| Downstream interfaces | PASS | Shapes for c3 unchanged by the fix, all read at HEAD: `launch(criteria,triggeredBy,includeRawScan)` (:301 — `pipelineId=1`, `phase`, nullable `executionId`, 409 prepared as `PipelineRejection(QUERY_CONFLICT|NOT_CONFIGURED)`), `pause()` (:354 — persisted PAUSED, no running task record needed, returns phase + in-flight jobs/collections), `resume()` (:377 — saved query only, no new epoch), `tick()` (:414 — short transaction then dispatch to `pipelineCoordinatorExecutor`, `RejectedExecutionException` keeps QUEUED for the next tick), `status()` (:517 — `state` derived PAUSED-first, `phase`, `desiredState`, `currentExecutionId`, `queryHash` = pipeline row (:556), per-source `DiscoverySourceStatus` (:131-143, no per-stream hash field), I-8 counters, queueDepth/bytes/oldest age, `waitReasons`, `nextWakeAt`, budget snapshot), `cleanup` (:586); `PipelineWindowResult.queryHash` stays the pipeline-level hash (:1309); the only per-stream key move is internal, so no exposed DTO/field changed; `pipeline-enabled` still defaults false (application.yml:201) and no controller/scheduler/endpoint file is in the boundary. |

### Fix Round 1 Re-check
| Finding | Status | Evidence |
|---|---|---|
| F-1 | FIXED | `DiscoveryPipelineService.kt:651` now creates streams with `queryHash = expertDiscoveryService.queueQueryHash(source, criteria)` (per-source normalized hash), while `:637-638` keeps the missing-criteria guard and `:312` keeps the pipeline-level hash as launch identity. Rewritten production-path test `stream identity is the per-source hash, not the pipeline hash (I-1)` (DiscoveryPipelineServiceTest.kt:1163-1228, green in gate 1): launch A `sources=["OPENALEX","PMC_OA"]` → the single stream's hash equals the real `queueQueryHash("OPENALEX", criteria())` and differs from the pipeline hash, and is EXHAUSTED; launch B `sources=[]` (same effective per-source conditions, different pipeline identity) → same stream id/hash, still EXHAUSTED, `cursor_value` null, `queuedPapers` unchanged (no page-0 re-collection); launch C `pageSize=50` → a second stream, original untouched. Falsification confirmed against the test's structure rather than the report: the expected value comes from a real `ExpertDiscoveryService` and the observed value comes straight from the argument the window passes (test `InMemoryQueueStore.ensureStream` :430-442 stores exactly `queryHash`), so with the old wiring both `assertEquals(perSource, first.queryHash)` and `assertNotEquals(pipelineHashA, first.queryHash)` fail; recomputing `sha256Hex(DiscoveryCheckpointCodec.canonicalCriteria(...))` independently reproduces the fixer's digests `a0a3887eceabb18d40df63f545642275c5978fde2fb8858ed739e6de5384f427` (per-source) and `60d86285024923732234e711aa962f92eef5df6017e0701a7f6b19e59f7e7519` (pipeline, PMC_OA included), matching the reported `expected a0a388… but was 60d862…`. No other production code reads a stream's `query_hash`, and `grep queueQueryHash` shows the window call site plus tests only. |

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1: `releaseWindow` (Service.kt:1228-1236) always sets `next_wake_at = now + pipelineTick` instead of the earliest progressable time, and `hasProgressableWork` (:1140-1145) returns true when `streams.isEmpty()`; so while a launch is RUNNING with all sources deferred/backing off — or with a criteria whose enabled-source set resolves to nothing (`resolveEnabledSources` yields an empty list for an unknown/disabled source name) — `tick()` opens a window that ends immediately, producing one EXPERT_DISCOVERY task record per tick with 0 processed / 0 indexed. I-7’s "无操作需求不循环创建空task_execution" is implemented and tested only for PAUSED/DRAINED (DiscoveryPipelineServiceTest.kt:1659-1672). Outside the light gate: T-3 also prescribes "release the window, don’t sleep until reset", so the strict reading is arguable and the correction (nextWake = earliest progressable time, or rejecting an unresolvable source set at launch) is not uniquely determined by the plan.
- O-2: `status()` merges raw `stream.source_error` strings into `waitReasons` (Service.kt:538-540), so that list can carry values outside the brief’s 7-reason set (e.g. `SEARCH_FAILED`, `NO_PROGRESS`) alongside the documented constants (the constants themselves match). c3 must tolerate unknown values.
- O-3: `DiscoveryPaperQueueStore.forceFailForLeaseLoss` (Repository.kt:331, impl :1235) has no production caller and, unlike `completeJob`, leaves `active_count` / `processed_*` / `failed_items` and the result reservation untouched — a dead seam that would desync the capacity ledger if ever invoked.
- O-4: a window whose deadline expires exactly while the queue is already drained reports `WINDOW_END → PARTIAL_SUCCESS` although the pipeline phase becomes DRAINED (Service.kt:204-206 else-branch vs the `drained` computation at :712-718); the "drained ⇒ SUCCESS" behaviour of the child’s test holds only for the SOURCE_EXHAUSTED reason.

### Required Action
- COMPLETE_CHILD
