## Light Verification: LIGHT_PASS_WITH_NOTES
Child: 10 and plan path docs/plans/2026-09-07/10-mailbox-chat-frontend.md
Boundary: 020e8392f89ebbf5ed38b95060605055a67167c5..51e08279cf4a66de598752275acd2a886c7b3c07
Verifier: Verify10

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
|Authorized scope|PASS|HEAD == 51e08279cf4a66de598752275acd2a886c7b3c07 (`feat(fast-p): implement 10`, parent 4675007 = child-09 verify-record docs). `git diff-tree --name-only 020e8392..51e08279` = 9 paths: 4 child-09 controller docs (recorded in interleaved doc commit 4675007), + the 5 authorized product/test files (app.js, mailbox-chat.js, mailbox-chat.css, mailboxChatBehavior.test.js, mailboxChatStyle.test.js). Child-10 commit alone (4675007..51e0827) touches exactly those 5 — a strict subset of the brief's 10-file list. The 5 plan-listed "修改" legacy tests are byte-identical base..head (sha256 b5783a68/15b02ab3/604f6d10/8f9f2c85/30ce1d25); justification verified: app.js change is purely additive (3 hunks, 221 added lines, **0 deleted**, diff `@@ -1672/-14311/-14330`), legacy fns (renderMailboxExpertGroups/renderMailboxPagination/loadMailbox legacy body/handleTaskDrilldown*/handleUnmatchedAction) intact; `mailboxChatEligible()` requires `MailboxChat` present + `taskExecutionId==null`, and none of the 5 legacy sandboxes defines MailboxChat (grep count 0/file), so they deterministically run the preserved compatibility branch; spot-checks confirm meaning: mailboxExpertGrouping asserts EXPERT+ALL by-expert loads, group/pagination renders, focus flows (mailboxExpertGrouping.test.js:129/182/219/258); taskDrilldown asserts taskExecutionId-filtered flat endpoint + params and drilldown handlers (taskDrilldown.test.js:165/244). Worktree: only controller evidence untracked/modified (ledger.md M; children/10, children/11 untracked; fix-logs "No fix rounds."). No index.html or trust-reply-workbench.js change. |
|Plan and invariants|PASS|I-1: (source,id) key render mailbox-chat.js:589 `\`${message.source}:${message.id}\``; safe text via escapeText/escapeHtml + translatableBody on raw body; mark-resolved only INBOUND+MANUAL_REVIEW footer (:683-689); PROCESSED read-only badge, no button; OUTBOUND only real sendStatus (SENT/FAILED, :616-634); attachments default-collapsed count + ≤3 names (:639-651); markResolvedByKey (:1044-1083) posts to existing `POST /api/mail/unmatched-inbound/{id}/mark-resolved` (controller UnmatchedInboundMailController.kt:165; server op-log MARK_INBOUND_RESOLVED via PendingMailOperationService.kt:1094) with openActionDialog("mark-unmatched-resolved") payload {resolvedBy,note} (app.js ACTION_DIALOG_SCHEMAS:13652), then local-only refresh (message state, expert pendingCount, refreshUnmatchedBadge), no editor rebuild/no downloads — test asserts editor values preserved (mailboxChatBehavior.test.js:911-938). I-2: right column order settings→timeline→workbench→manual→logs in renderConversationContent (:1005-1037); workbench `<details>` no `open` (collapsed default), manual always `open`, logs collapsed; workbench binds `summary.latestInbound.processingId` via ensureWorkbenchMounted (:864-887), not last visible message; mount deferred to expand (autoBootstrap:false via app.js mcHostMountWorkbench); expert switch tears down old workbench mount + materials hosts + seq-guards stale responses (teardownConversationSubViews :453-462, selectExpert :463-515). I-3: drafts Map keyed `contactId:processingId:accountCode` (:898/1156); edited-draft + new inbound → openActionDialog("confirm") target choice; no silent rewrite (:1322-1350, retarget :1351-1387); draft cleared only after successful send (:1271-1298, afterSuccessfulSend); adopt→manual send carries ragFactCodes/ragCorpusFingerprint/edited payload (:1208-1236, :1262-1270 — mirrors legacy RAG audit shape app.js:11118-11142/11920-11933) via mcHostSendRichReply (app.js mirror of submitManualRichReply incl. MANUAL_SEND_SAFETY_BLOCKED two-phase confirm, same endpoint app.js:11821). I-4: waitingReply row shows 收 0 · 发 N + 待专家回复 badge; failed-only has no badge (:255-284); outbound-only: workbench = mc-note no-mount (0 generate), manual = note + 选择模板发送跟进邮件 → mcHostOpenFollowUp → openContactInList (COMPOSE_TEMPLATE flow; no fake rich text/no processingId fabrication :957-965, app.js adapter); server predicate waitingReply = receivedCount==0 && sentCount>0 (MailboxConversationService.kt:126). I-5: chat guard app.js:14549-14567 `mailboxChatEligible()` (MailboxChat present && no taskExecutionId) → mount + hide old view controls/pagination only; legacy table/group intact (0 deletions); conversations summary API paged size=20 (:36/234-254, MailboxConversationController.kt GET list); follow PUT/DELETE `/api/mail/mailbox/conversations/{id}/follow` optimistic + rollback (:1123-1155; controller :158-176); 材料 N and 查看全部附件 → mcHostOpenMaterials → `ExpertMaterials.configure+mount({host,contactId,mode:"drawer"})` same contactId store (app.js adapter; expert-materials.js:1229-1268 mount signature). S-3: shipped mailbox-chat.css byte-identical to plan S-3 block and to ui-style-contract.md S-3 (independent extraction: 8454 bytes both, sha256 90a125bf45bcdbc23cfca5a48d410c044fe6a7e1d30b4e5555d0b0c299a28651; style tests mailboxChatStyle.test.js:37-58); no inline style/undeclared classes/namespaced rules (style tests :76-97; grep 0 `style=`/`.style` in mailbox-chat.js). S-3 DOM skeleton honored (skeletonHtml :194-226). Tests: mailboxChatBehavior 21 + mailboxChatStyle 5 = 26, all green (see Gate 3). |
|Required commands|PASS|Fresh runs in worktree at HEAD: (1) `node --check src/main/resources/static/mailbox-chat.js && node --check src/main/resources/static/app.js` → exit 0 (SYNTAX_OK). (2) `node --test src/test/js/mailboxChatBehavior.test.js src/test/js/mailboxChatStyle.test.js` → tests 26, pass 26, fail 0. (3) `node --test src/test/js/*.test.js` → tests 730, suites 136, pass 730, fail 0. (4) `JAVA_HOME=/Library/Java/JavaVirtualMachines/zulu-11.jdk/Contents/Home mvn test` → exit 0, BUILD SUCCESS; Surefire aggregate `Tests run: 3251, Failures: 0, Errors: 0, Skipped: 9` (target/surefire-reports); exec-plugin Node phase 730/730 in same run. |
|Downstream interfaces|PASS|mailbox-chat.js/css new namespaces only (.mail-chat/.mc-*) with inert guards — nothing assumes registration (mailboxChatAvailable/Eligible checks; script-absent test mailboxChatBehavior.test.js:1358 → full legacy path). No index.html/trust-reply-workbench.js change in boundary. Host adapter lives entirely in app.js: mcHostMountWorkbench mirrors existing mountLiveTrustReply options {mode:"LIVE", source:{sourceType:"LIVE_INBOUND", sourceId}, contextPath, autoBootstrap:false, onUnauthorized, onComplete} — trust-reply-workbench.js MODES LIVE exists (:15, mount :573, autoBootstrap :566); explicit host element, no `[data-trust-reply-live-host]` global querySelector reuse. ExpertMaterials (child 08) untouched; chat only calls public configure/mount via app.js adapter (expert-materials.js unchanged in boundary). All host fns referenced by hostFn() are top-level function declarations in app.js (api :1474, escapeHtml :1507, buildManualReplySubject :1521, translatableBody :1544, openActionDialog :13720, refreshUnmatchedBadge :10653, mcHost* adapters, etc.) → reachable as window props at runtime. |

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1 (mailbox-chat.js:765-766 latent production defect — empty 专家状态/专家层级 selects): `optionsFromCatalog(global.operatorStatusOptions, …)`/`global.indexLevelOptions` read app.js top-level **const** catalogs (app.js:656/665) through the window object, but top-level const/let in a classic script create *global-lexical* bindings, NOT window properties (vm simulation of exact load order: window-prop read `undefined` → options `""`; bare-identifier read resolves). All existing app.js consumers reference the catalogs as bare identifiers (app.js:8043/11268/13449), never via window. Result: in the real page the chat settings selects render zero options and save is inert — the S-3/A-3 "状态/层级 select 复用" is non-functional in-browser. Tests cannot catch it: sandbox mirrors the same emptiness and no test asserts select options. Why RECORD_ONLY: code-evidence-proven but only observable after child-11 browser registration; plan does not uniquely define the correction (bare-identifier reads vs host-exposed catalogs) → needs a human/child-11 decision, small follow-up fix in mailbox-chat.js (authorized file) or registration-time amendment.
- O-2 (test hygiene): leftover debug statement `console.error("NEWINBOUND DEBUG dialogs:", …)` inside mailboxChatBehavior.test.js (~:1106, 新来信+已编辑草稿 test) prints to stdout during suite runs; harmless but should be removed in a later cleanup.
- O-3 (documented deviation #2): plan 实现方案 step 2 said 查看全部附件 opens child-08 "按本信 source 筛选"; child-08's mount API (expert-materials.js:1251-1268) has no programmatic source/sourceId initial filter and expert-materials.js is not an authorized file, so delivery opens the same-contactId drawer unfiltered (store parity per I-5 preserved, test :1226). Requires child-08 extension or acceptance amendment; correctly recorded in execution.md.
- O-4 (coverage claim slightly overstated): execution.md states style/behavior tests assert "mc-scroll 内固定 设置→mc-timeline→workbench(默认折叠)→manual(默认 open)→logs(折叠)"; only workbench collapsed-default is pinned (mailboxChatBehavior.test.js:944) plus outer S-3 skeleton (:804-825). Full right-column DOM order and manual-open/logs-collapsed defaults are implemented correctly in code (renderConversationContent :1005-1037) but not machine-pinned. Not a plan violation; a future test could pin it.

### Required Action
- COMPLETE_CHILD
