# Child 06 Light Verification Log

## Light Verification: LIGHT_PASS_WITH_NOTES
Child: 06 and plan path docs/plans/2026-09-07/06-shared-material-api.md
Boundary: 21dad8bf0573c21eec52cd9783967ae97222ce3d..fb913c58f375f51eb6284d8c3b449134258811f4
Verifier: Verify06

### Four Gates

| Gate | Result | Evidence |
|---|---|---|
| Authorized scope | PASS | HEAD = fb913c58f375f51eb6284d8c3b449134258811f4 (`feat(fast-p): implement 06`); `git status --porcelain` shows zero tracked modifications (only untracked `docs/plans/fast/mailbox-materials/**` = controller evidence). `git show --name-only fb913c58` = EXACTLY the 10 brief-authorized files (5 main + 5 test). The boundary window additionally contains docs-only commit 8713ed1 (child-05 verification docs, 4 files under `docs/plans/fast/mailbox-materials/children/05/`) — no product/test file beyond the 10 in the whole window. Class `ExpertMaterialControllerHttpTest` physically lives in authorized file #6 `src/test/kotlin/com/weibo/talentintroduction/document/service/ExpertMaterialServiceTest.kt:970-1184` as a second top-level `@WebMvcTest` class (Kotlin allows multiple top-level classes per file); the file holds 33 service `@Test`s + 7 controller `@Test`s = 40. |
| Plan and invariants | PASS | I-1: single endpoint `GET /api/expert-contacts/{id}/materials` (`ExpertMaterialController.kt:34`); GET path is read-only parameterized SQL projection + filesystem verification, never enqueues/writes (test `GET list never touches transfer writes or the queue` verifies `enqueueMaterial`/`markRequested` never called and no repo interactions; code at `ExpertMaterialService.kt:250-280`); ownership chain document→attachment→unique owner→contact centralized (`resolveOwnerContact:72`, `resolveReadyFile:92`); reconcile dryRun returns candidates without DB writes (test verifies `save`/`findFirstByMailAttachmentId` never called), apply is `@Transactional` with `FOR UPDATE` row lock + NOT EXISTS + per-row re-check, inherits original attachment/file, default PENDING_REVIEW, type from filename classification, idempotent double-apply tested (`ExpertMaterialService.kt:605-666`). I-2: 3-way source resolution (transfer bridge → direct processing owner → strict-unique old mailRecord same account/expert/INBOUND/non-empty messageId); ≥2 candidates refuse with SOURCE_AMBIGUOUS and never attribute another account's file (`resolveSource:401`, tests `legacy attachment ambiguity refuses…` and `two accounts with the same message id never cross-wire…`); sourceInboundId never used as processing id (`resolveMessageAttachments` MAIL_RECORD branch returns only direct record attachments; test at test-file:944). I-3: POST transfers requires Session username (`ExpertMaterialController.kt:49-60`), validates every ID's doc ownership all-or-nothing → foreign ID rejects whole batch with 400 before any enqueue (test verifies `enqueueMaterial` never invoked), 1..500 distinct enforced, capacity exhaustion → `AttachmentTransferQueueFullException`; queue via child-02 `AttachmentTransferService.enqueueMaterial` (API matched: `AttachmentTransferService.kt:257`); AI extraction is two-phase — all selected resolved/readiness-checked before any content byte is read (`DocumentTextExtractor.kt` phase 1/2, test `extract validates all selected attachments are ready before reading any content`); AnalysisService test proves pre-read failure never deletes/saves existing results. 409 MATERIAL_NOT_READY + 429 mapped by `@Order(Ordered.HIGHEST_PRECEDENCE) @RestControllerAdvice(assignableTypes=[ExpertMaterialController, ExpertDocumentBrowseController, ExpertDocumentAnalysisController, MailboxAttachmentController])` (`ExpertMaterialController.kt:103-130`); no `ResponseStatusException` in new code (grep empty); GlobalExceptionHandler untouched (`common/controller/GlobalExceptionHandler.kt:65` catch(Exception) would 500 otherwise); 409/429 proven through the real advice chain by `@WebMvcTest` (loads all `@ControllerAdvice`). I-4: page≥0, size default 10 max 100 enforced at entry (`ExpertMaterialService.kt:250-279`, tests `page and size bounds are validated`); `total` filtered; summary over ALL expert materials independent of page/filter (`summarize:511` fed by unfiltered resolved rows; test `40 materials paginate 4 pages… summary covers all materials` asserts summary.total=40 while pages hold 10; search test asserts summary.total=40 with q filter); `defaultAnalysisAttachmentIds` = full set of METADATA_ONLY + {CV,PHD_DEGREE,MASTER_DEGREE,BACHELOR_DEGREE} + PDF/text-parseable rows, collected without truncation (no take/subList anywhere in `summarize`; 39/39 asserted on 40-row fixture); items fixed 19-field set never exposing storagePath/IMAP creds/workerToken (JSON test asserts `storagePath`/`workerToken` doesNotExist); unknown actualSize=null and real 0B stays 0 (test); sort receivedAt DESC then attachmentId DESC; FAILED/SOURCE_UNAVAILABLE transfer rows kept with error{code,message}; JdbcTemplate/NamedParameterJdbcTemplate parameterized only (`:contactId`/`:processingId` bind params, no user-SQL concatenation; search is in-memory substring — no SQL LIKE needed). Old 3 services delegate readiness to `ExpertMaterialService` (`ExpertDocumentBrowseService.resolveForDownload:89`→`resolveReadyFile`; `DocumentTextExtractor.extract`→`resolveReadyFile`; `MailboxAttachmentService.download:38`→`resolveReadyFileUnscoped`), private realpath copies deleted (diff shows −25/−49 lines), old URLs unchanged, file size stays `Long` (`DocumentFileResource.fileSize: Long = Files.size(path)`; `MailboxAttachmentDownload.fileSize: Long`). AI readable only for validly-owned docs (`resolveReadyFile` requires doc.expertContactId==contactId and owner contact match). Bean-name coexistence: pre-existing `campaign/service/ExpertMaterialService.kt:62` (plain `@Service`) and new `document/service/ExpertMaterialService.kt:49` (`@Service("expertMaterialQueryService")` — explicit name prevents default-name clobber/BeanDefinitionOverrideException); consumers inject by distinct types — campaign consumers `ExpertContactManagementController.kt:18`, `MailVariableService.kt:4` (unchanged) still bind the campaign service; new consumers (new controller + 3 modified services) bind the document type; full `mvn test` (3211 tests incl. full Spring context loads) proves both beans coexist. |
| Required commands | PASS | All run freshly at final HEAD (JAVA_HOME=zulu-11, worktree root, no Docker): (1) `mvn test -Dtest=ExpertMaterialServiceTest,ExpertDocumentBrowseServiceTest,ExpertDocumentAnalysisServiceTest,DocumentTextExtractorTest,MailboxAttachmentServiceTest` → **67 run / 0 fail / 0 err / 0 skip, BUILD SUCCESS, EXIT=0** (ExpertMaterialServiceTest 33, Browse 15, Analysis 6, Extractor 6, MailboxAttachment 7); (2) `mvn test -Dtest=ExpertMaterialControllerHttpTest` (the implementer-flagged extra class, wherever it lives — authorized file #6) → **7 run / 0 fail / 0 err, BUILD SUCCESS, EXIT=0** — 409 MATERIAL_NOT_READY and 429 TRANSFER_QUEUE_FULL assertions pass through the real advice chain (@WebMvcTest auto-loads scoped advice AND GlobalExceptionHandler; a preempting global handler would have yielded 500); (3) full `mvn test` → **3211 run / 0 fail / 0 err / 8 skipped, BUILD SUCCESS, EXIT=0**; the 8 skips are the design-gated mysqlIt/migrationIt ITs (incl. FlywayMigrationIntegrationTest) that skip by default — plain docker-free `mvn test` stays green, matching baseline facts and this child's no-migration/no-mysqlIt contract. |
| Downstream interfaces | PASS | No migration added by the child (diff-tree contains no `db/migration` path; tree max remains `V120__scope_inbound_uid_by_validity.sql`). `ExpertMaterialItem` (service file) has exactly the master API-contract item field set: attachmentId/documentId/source{type,id,subject,receivedAt,accountCode}/fileName/contentType/documentType/documentStatus/actualSize(nullable)/encodedSize(nullable)/storageState/bytesDownloaded/error{code,message}(nullable)/canFetch/canDownload/canPreview/analysisSupported/canAnalyze/downloadUrl/previewUrl(nullable) — no storagePath/credentials/workerToken. Capability semantics match the contract consumed by children 08/09/10: `analysisSupported` = PDF/text parseable regardless of fetch state (`isAnalysisSupportedContentType`), `canAnalyze` = analysisSupported AND verified STORED file AND ownerValid (service:317-400); child-09 plan (`docs/plans/2026-09-07/09-material-analysis-selection.md:62`) consumes `defaultAnalysisAttachmentIds` with the >500 non-truncation rule, implemented as full-list (sorted, no cap). Reconcile seam `{candidateAttachmentIds,createdCount}` with dryRun/apply matches master POST reconcile; child-07 plan (`docs/plans/2026-09-07/07-expert-conversations-follow.md:52`) consumes `resolveMessageAttachments` (bridge/direct-owner/strict-unique legacy semantics), provided at `ExpertMaterialService.kt:668` with the same parameterized strict-unique logic. |

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1 (query strategy deviation, I-4): `listMaterials` does NOT use a SQL LIMIT/OFFSET page window — it loads one parameterized metadata projection of ALL of the expert's material rows (`loadMaterialRows`, single query, `ExpertMaterialService.kt:230-248`) and filters/sorts/paginates in memory (`assemblePage:281`). Per-row file I/O is stat-level only (`Files.exists`/`toRealPath`, never file bytes) and is executed for every expert row (bounded by the expert's material count, ≤1000 per the master performance contract; page render remains 10). This was deliberately chosen because storage state of transfer-less historical rows is a filesystem fact that cannot be a SQL predicate, and the summary must aggregate all materials on the same snapshot (I-4). No N+1 full loads (single projection query + at most one bounded per-row PK re-read in the unique-candidate branch of `resolveSource`), no regression into full-file loads. Satisfies I-4 semantics and is covered by tests asserting summary.total=40 across pages/filters; recorded for the full machine review.
- O-2 (boundary window composition): the review window 21dad8bf..fb913c58 contains commit 8713ed1 `docs(fast-p): record 05 light verification` (4 docs files under children/05) in addition to the child-06 commit; product/test delta across the window is still exactly the 10 authorized files. Controller-owned docs evidence.
- O-3 (mailbox readiness scope): `MailboxAttachmentService.download` delegates to `resolveReadyFileUnscoped` (realpath/base-path readiness without doc/contact ownership check) rather than the contact-scoped `resolveReadyFile`, because mailbox attachments may lack an ExpertDocument and message-level ownership is enforced by the caller chain (MailboxService.resolveAttachments → child-07's `resolveMessageAttachments`). Same readiness core (`resolveFileReady:125`) with no path bypass; documented in execution.md decision #1/7.
- O-4 (no real-MySQL IT in this child): child-06's SQL (MySQL dialect, parameterized) was not executed against real MySQL — the authorized 10 files contain no mysqlIt-gated IT and the plan lists none; cross-account/ownership SQL proof is scheduled under master G-1 with child-07's MySQL IT, and this child covers the semantics with controlled-JDBC/unit tests. Observation only — not a gate failure under the child contract; master-level G-1 remains for the overall fast-p machine review/human acceptance.

### Required Action
- COMPLETE_CHILD
