## Light Verification: LIGHT_PASS_WITH_NOTES
Child: 07 and plan path docs/plans/2026-09-07/07-expert-conversations-follow.md
Boundary: fb913c58f375f51eb6284d8c3b449134258811f4..62d86310634e5cb1a9ae522f536cb2c91b0fc2b9
Verifier: Verify07

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
|Authorized scope|PASS|HEAD == 62d86310634e5cb1a9ae522f536cb2c91b0fc2b9 (git rev-parse). `git status --porcelain`: no product/test-code dirt — only tracked-modified docs/plans/fast/mailbox-materials/children/07/execution.md + ledger.md and untracked children/{08,09,10,11}/ stubs (controller evidence). `git diff-tree --name-only -r fb913c58..62d8631` = exactly the 11 A1-authorized files (V121__create_expert_follow.sql, ExpertFollowService.kt, MailboxConversationRepository.kt, MailboxConversationService.kt, MailboxConversationController.kt, MailboxConversationRepositoryIT.kt, MailboxConversationControllerTest.kt, FlywayMigrationIntegrationTest.kt, MailboxService.kt, MailboxServiceTest.kt, ExpertContactManagementController.kt) + docs only. Commit split: e0fa706 = epoch-1 10 files (git show --stat); 5891b1a = docs only (07 brief/execution/fix-log + ledger); 06bea85 = docs only (plan A1 amend, 1 file); 62d8631 = epoch-2 2 files (ExpertContactManagementController.kt +1/−1, MailboxConversationControllerTest.kt +62/−2); a2521a7 (also inside SHA window) = docs only (children/06 evidence ×4). Impl commits contain zero docs; docs commits touch only docs/plans/**. HTTP retirement real: grep across src/main/kotlin shows the only GET materials route is `document/controller/ExpertMaterialController.kt:26-27` class `@RequestMapping("/api/expert-contacts/{contactId}/materials")` + `:34 @GetMapping`; campaign `ExpertContactManagementController.kt:244` now a comment (1:1 annotation→comment swap), no `@GetMapping("/{contactId}/materials")` remains on either controller. PUT `/api/expert-contacts/{contactId}/materials/{materialCode}` updateMaterialStatus intact (:248), detail/other endpoints untouched. Full suite compiles the retained unrouted delegate: ExpertContactManagementControllerTest 7 run/0 fail in plain mvn test incl. the direct `controller.listMaterials(1L)` unit call (test file :31-49) — residue compilable, unrouted, production-effect-free (see O-1).|
|Plan and invariants|PASS|I-3 (ownership): V121__create_expert_follow.sql:9-18 `PRIMARY KEY (username, expert_contact_id)`, `expert_contact_id BIGINT NOT NULL` + `fk_expert_follow_contact → expert_contact(id)`, `created_at DATETIME NOT NULL` no default. ExpertFollowService.kt:36-66: `setFollowed` INSERT IGNORE (PUT) / DELETE (DELETE), never toggle, parameterized JDBC, contact-existence check → 404; username from controller Session only — MailboxConversationController.kt:107/138/151/161 GET/PUT/DELETE mappings; sessionUsername reads `AuthSessionKeys.USERNAME`, null → 401 (:172-177); PUT/DELETE endpoints take no body. Tests: ControllerTest `PUT follow ten times keeps one row and DELETE twice is idempotent` (:142-190, incl. 8-thread concurrent real-service PUT → 1 row, created_at unrefreshed), `username in request body is ignored…` (:192-213), `anonymous and invalid-session follow are rejected with 401` (:216-229, real AuthInterceptor), `follow ownership is per session user…` (:231-261). I-1 (authority): rangeUnionSql (MailboxConversationRepository.kt:415-479) = OUTBOUND mail_record (`FROM mail_record mr WHERE mr.direction='OUTBOUND' AND expert_contact_id IS NOT NULL`, :444-447) UNION ALL linked inbound_mail_processing (expert_contact_id IS NOT NULL, :467-470); INBOUND mail_record never enters; utf8mb4_unicode_ci collation on all text columns. DB group-before-pagination: pageConversations GROUP BY expert (:167) ORDER BY latest_event_at DESC, contact_id DESC + LIMIT/OFFSET (:169); countConversations COUNT(*) over GROUP BY subquery (:120-137). Event time = received_at / COALESCE(sent_at, created_at) (:437, :455); stable (time, source_rank, id) ordering. I-2 (waiting/pending): sent_flag only `send_status='SENT'`, failed_flag separate (:462-463); havingClause `SUM(received_flag)=0 AND SUM(sent_flag)>0` (:559-570); pending_flag = `process_status='MANUAL_REVIEW'` (:466) — verbatim same predicate as existing MailRecordRepository.kt:538/585/657; membership direction/date/subject/label via EXISTS only (membership() :481-537), label joins inbound side only, no dup rows; direction=OUTBOUND+label → impossible empty. I-4: summary projection has no body (includeBody=false; 7-key latestMessage; latestInboundByContacts uses real processing rows ROW_NUMBER (:270-305) with real `imp.id` as processing_id; timelineMessages (:363-412) only current expert with keyset (event_at, source_rank, id) + limit+1 hasMore; body/cleanedBody only in timeline. Service: listMessages resolves attachments via 06 `expertMaterialService.resolveMessageAttachments` (MailboxConversationService.kt:175) — same seam MailboxService.resolveAttachments now delegates to (MailboxService.kt:287-291); unqualified findFirstByMessageId fallback disabled (repo method has zero production callers; MailboxServiceTest verifies `never()` at :550/581/607/620). Cursor encode/decode bound to contact+accountCode scope, 400 on reuse/tamper (service :217-253; ControllerTest `timeline cursor is bound…` :362-399). Legacy task drill-down (MailboxController/MailRecordRepository) and unmatched-inbound entries untouched in boundary (zero files of theirs in diff-tree; MailboxTaskExecutionFilterTest / UnmatchedInboundMailServiceTest unchanged, green in full suite). A1 dual-controller regression: MailboxConversationControllerTest.kt:620-676 second top-level `@WebMvcTest(controllers=[ExpertMaterialController, ExpertContactManagementController])` class (all deps @MockBean, no mysqlIt) — context loads and both GET materials (06) and PUT materials/CV (campaign) resolve; pre-fix Ambiguous-mapping failure documented in epoch-2 execution.md. Evidence runs: RepositoryIT (real MySQL) `mixed record kinds…` (:79-133: 2 legacy INBOUND mail_record rows for A do NOT count; latestInbound real processing id), `same-timestamp pagination…` (:307-346: 64 experts same-instant, size=17, no dup/gap, strict contactId DESC), `multi-label…` (:211-238), `date filter only gates membership…` (:135-183), `waiting and pending filters use account-range full history` (:184-210), EXPLAIN sanity (:405-423).|
|Required commands|PASS|All run freshly 2026-09-08 at HEAD 62d8631, JAVA_HOME=zulu-11, worktree root, logs /tmp/v07-c{1,2,2b,2c,3,4}.log: (1) `mvn -Pmysql-it -Dtest=MailboxConversationRepositoryIT test` → EXIT=0 BUILD SUCCESS; MailboxConversationRepositoryIT **12 run / 0 F / 0 E / 0 S** (real MySQL 127.0.0.1:3306, Flyway→V121). (2a) `mvn test -Dtest=MailboxConversationControllerTest,MailboxServiceTest` → EXIT=0; MailboxServiceTest **20/0/0/0**, MailboxConversationControllerTest mysqlIt-gated skip (1 skipped). (2b) same + `-Pmysql-it` → EXIT=0; **30 run / 0 F / 0 E** (ControllerTest 10/0/0/0 incl. real service+JDBC follow/identity/cursor cases; MailboxServiceTest 20/0/0/0). (2c) A1 regression standalone `mvn test -Dtest=MailboxMaterialsDualControllerMappingTest` → EXIT=0; **1/0/0/0** (context loads; epoch-2 log documents pre-fix 1 ERROR on this class). (3) `DOCKER_HOST=unix:///Users/lukai/.orbstack/run/docker.sock mvn -Dtest=FlywayMigrationIntegrationTest -DmigrationIt=true -Dapi.version=1.40 test` → EXIT=0; **16 run / 0 F / 0 E** (incl. `V121 creates expert_follow…` :783+; bare invocation env-blocked at baseline by docker-java API 1.32 vs OrbStack 1.40 — known-good workaround used). (4) `mvn test` full → EXIT=0 BUILD SUCCESS; surefire **3215 run / 0 F / 0 E / 9 skipped** (9 skips = mysqlIt/migrationIt-gated ITs + pre-existing, by design; MailboxMaterialsDualControllerMappingTest 1/0 and OperatorStatusWriteSeamGuardTest 1/0 and ExpertContactManagementControllerTest 7/0 inside the run); node **671 pass / 0 fail** + node-check phases pass. See O-3 for the implementer-total delta.|
|Downstream interfaces|PASS|Migration numbering: epoch-1 base max V120; V121 next-free and now the max; **no V122 anywhere** (ls src/main/resources/db/migration). V121 = final schema migration of the master plan (master :62 `迁移顺序V118→V119→V120→V121`; children 01/02/04/07 hold V118/V119/V120/V121; plans 03/05/06/08/09/10/11 declare no migration — no V12x references in docs/plans/2026-09-07/{08,09,10,11}-*.md beyond the boilerplate IT-run line; child 11 only documents deployment). API shapes consumed by child 10: 10-mailbox-chat-frontend.md I-2 `workbench使用summary.latestInbound.processingId`, I-4 received=0/SENT>0 waiting semantics + `不得捏造processingId`, I-5 关注刷新保留 — all served by ConversationItemResponse (followed, waitingReply, receivedCount/sentCount/failedCount/pendingCount, latestInbound{processingId,…}|null, latestMessage, accountCodes, materialCount; ControllerTest pins the exact 7-key latestMessage projection and no-body items :264-300). Shared materials path: ExpertMaterialController is now the sole GET /api/expert-contacts/{id}/materials owner (single mapping verified by grep + dual-controller context load) that children 08/09/10 build on (08 shared panel mounting, 09 AI selection reuse, 10 same materials store — all documented against the 06 API). campaign `PUT /api/expert-contacts/{contactId}/materials/{materialCode}` updateMaterialStatus intact for audit-status operations (verified by route test + unit tests).|

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1: Retained unrouted delegate `ExpertContactManagementController.listMaterials` (:245-246, no mapping annotation — comment at :244) plus direct-call unit case `ExpertContactManagementControllerTest.kt:31-49` is a dead-code residue outside A1's authorized file list (deleting the method would break the unlisted test file; the method has no production caller other than that test, verified by grep across src). Routing retirement — the only production defect A1 targeted — is complete and independently proven (single GET mapping remains; dual-controller @WebMvcTest loads). Cleanup is properly sequenced: epoch-2 execution.md instructs a later child to add ExpertContactManagementControllerTest to its change list and delete method + case together. Not a light-gate violation (no AUTO_FIX possible without touching an unauthorized file).
- O-2: Controller-owned child brief (children/07/brief.md, committed at 5891b1a) still states the pre-A1 10-file list and "commit 10 authorized files"; epoch-2 scope lives in the amended plan + epoch-2 execution.md section. Controller evidence staleness only; no product impact.
- O-3: Implementer-reported full-suite aggregates (epoch-1 3247, epoch-2 3248 run / 9 skipped) are not byte-reproducible in my fresh clean run at the same HEAD: **3215 run / 0 F / 0 E / 9 skipped**, exit 0, node 671/671. Direction of the delta (their total higher) is consistent with their aggregates absorbing stale surefire XMLs from the immediately preceding -Pmysql-it / Flyway runs in target/ (their own docs warn about excluding stale reports, cf. child-04 verify-log). No failure/error signal exists in any run; the full suite is green under both accounting methods.
- O-4: `ConversationItemResponse.institution` is always null (expert_contact has no institution column; no authorized DB source) — implementer deviation #7 recorded in execution.md with rationale and a remaining-concern note; contract field preserved for child-10 frontend to fill from expert-profile (ES) data. Flagged for human awareness, not a gate violation (plan amendment would be required only if acceptance demands non-null institution).
- O-5: Plain full `mvn test` skips the mysqlIt-gated classes (RepositoryIT/ControllerTest) by design; their evidence comes from the explicit `-Pmysql-it` runs (commands 1/2b) per master-plan rule "数据库 IT 必须显式启用，不用默认跳过当证据".

### Required Action
- COMPLETE_CHILD
