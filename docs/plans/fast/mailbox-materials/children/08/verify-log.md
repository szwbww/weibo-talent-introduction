## Light Verification: LIGHT_PASS_WITH_NOTES
Child: 08 and plan path docs/plans/2026-09-07/08-shared-materials-frontend.md
Boundary: 62d86310634e5cb1a9ae522f536cb2c91b0fc2b9..47a72d885498ff473560d26901d2596ffdddb2cd
Verifier: Verify08

### Four Gates
| Gate | Result | Evidence |
|---|---|---|
| Authorized scope | PASS | `git rev-parse HEAD` = 47a72d885498ff473560d26901d2596ffdddb2cd (subject `feat(fast-p): implement 08`). `git diff-tree --name-status 62d8631 47a72d8` = M app.js, A expert-materials.css, A expert-materials.js, A expertMaterialsShared.test.js, A expertMaterialsStyle.test.js, plus M/A children/07 execution.md + verify-log.md from the intermediate controller evidence commit 7da63d3 `docs(fast-p): record 07 light verification` (the impl commit's direct parent). Impl commit `47a72d8^..47a72d8` = EXACTLY the 5 brief-authorized files. index.html / styles.css / trust-reply-workbench.js / backend / main-repo untouched. Working tree: only `M docs/plans/fast/mailbox-materials/ledger.md` (fast-p controller bookkeeping) + untracked `docs/plans/fast/mailbox-materials/children/{08,09,10,11}/` evidence dirs. |
| Plan and invariants | PASS | I-1: window.ExpertMaterials IIFE `global.ExpertMaterials = api` (expert-materials.js:1215), `mount({host,contactId,mode})/unmount({host})/unmountHostsIn(root)` + `configure` (api export :1206); contactId→store Map (:71, :138), store dies only when last view disposed (:166 `dropStore` aborts read + clears timers); mode is layout-only (normalizeMode; drawer=native `<dialog class="em-drawer">`, selectionOnly=no transfer footer, same buildPanel/mountView :418/:1067); two-host/one-GET & single-POST tests (shared:675, :724). I-2: GET-only `materialsUrl` (page/size/q/source/sourceId/state); no download/preview/transfers/reconcile in GET; GET-only test (:831); 300ms debounce default (:30) + search timer (:960s); request epoch `store.seq` drops stale responses in fetchPage (:303); AbortController only around read fetch — POST transfers (:377) passes no signal, never cancelled; row-local updates via emSnapshot diff (updateRowInPlace), inputs/checkboxes not rebuilt; stale-A-to-B tests (:745, :780, :804). I-3: PAGE_SIZE=10 (:27); header checkbox「选择本页」operates only `store.pageOrder` (:990s); selection = per-contact Set, survives page/filter (fetchPage never clears); POST distinct ids 1..500, >500 → em-error reason, no POST, selection kept (:377-396); QUEUED merged only from successful POST response then after-submit GET; failure path keeps selection + reason (syncView action-error); 1000-item/10-row window, 20-across-two-pages POST=20, cross-expert isolation, 500-cap tests (:846,:880,:926,:964); poll every 2s only while ≥1 view mounted & active task exists (schedulePoll :230), stops on unmount + zero leaked listeners (test :1006), drawer close/Esc frees UI only, other host keeps polling, reopen shows real server state (:1038). I-4: app.js strictly additive (+73 lines) with typeof guards; unmount-before-innerHTML + mount-after-write ordering in loadContactDetail (app.js:8070/8288); showExpertDetail unmounts but never mounts; ES no-contactId renders static `尚未建立联系，暂无资料。` S-1 panel with 0-count and zero materials/documents requests (renderNoContactMaterialsEmpty :8418; test :1089); mount guards require finite positive id (:8394); legacy renderExpertDocuments absent-component body is byte-identical to base 62d8631 version (independently re-extracted and diffed: signature + body after guard equal); legacy AI button preserved when component absent, inline S-1 header re-renders equivalent `data-action=open-ai-analysis` button which component delegation deliberately does not intercept (bindEvents comment; app.js #contactDetail delegation :12439 handles it at :9934 via openAiAnalysisModal — child-09 bridge seam). S-1 byte compare re-run independently: expert-materials.css SHA-256 `c5bd312cfe9a61efd786780bd3a5b3fa2028e0fcd40135e20cb027601b4d863e` == plan 08 S-1 block == ui-style-contract S-1 block (markdown-fence newline normalized; 60 lines/60 newlines identical); DOM whitelist test (:404) walks real mounted trees across inline/drawer/selectionOnly and asserts every class ∈ (expert-materials.css ∪ styles.css), zero ids, zero inline-style attrs, data-state ⊆ 6-state set, six fixed copy strings; component renders zero ids and zero `getElementById` uses (0 matches in expert-materials.js); backend summary DTO field names cross-checked (`ExpertMaterialService.kt:534-541` ExpertMaterialSummary stored/metadataOnly/active/failed/sourceUnavailable) match component reads. |
| Required commands | PASS | All fresh in worktree on 2026-09-08: (1) `node --check src/main/resources/static/expert-materials.js && node --check src/main/resources/static/app.js` exit 0; (2) `node --test src/test/js/expertMaterialsShared.test.js src/test/js/expertMaterialsStyle.test.js` — tests 21 / suites 7 / pass 21 / fail 0, exit 0; (3) `node --test src/test/js/*.test.js` — tests 692 / pass 692 / fail 0, exit 0; (4) `JAVA_HOME=…/zulu-11.jdk mvn test` — MVN_EXIT=0, BUILD SUCCESS; Java `Tests run: 3215, Failures: 0, Errors: 0, Skipped: 9`; exec-maven-plugin node-test phase re-ran JS suite: 692 pass / 0 fail. |
| Downstream interfaces | PASS | Contract surface consumed by child 09 (selectionOnly in AI picker) and child 10 (drawer host) is exported and stable: `ExpertMaterials.configure/mount({host,contactId,mode})/unmount({host})/unmountHostsIn(rootEl)` (expert-materials.js:1206-1215) with mode normalization inline/drawer/selectionOnly and a single store per contactId; drawer mounts as native `<dialog class="em-drawer">` and close/Esc only release UI. No index.html script/CSS registration yet (child 11; component inert until then — all app.js hooks typeof-guarded). CSS file and namespaces `.expert-materials` / `.em-drawer` / `.em-analysis` (S-2 picker rules included in the same S-1 CSS block) reserved as shipped. Legacy UI untouched while component unregistered (byte-identical legacy renderExpertDocuments + full JS suite incl. existing contract tests green). |

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1: Raw boundary range 62d8631..47a72d8 contains two extra docs files beyond the 5 authorized ones — `docs/plans/fast/mailbox-materials/children/07/{execution.md (M), verify-log.md (A)}` — attributable to intermediate commit 7da63d3 `docs(fast-p): record 07 light verification`, the impl commit's direct parent (fast-p controller evidence commit, same category as the untracked children/** evidence). The implementer commit 47a72d8 itself is exactly the 5 authorized files and excluded its own fast-p evidence; not an implementer deviation, no correction defined/authorized.
- O-2: Working tree carries a tracked modification to `docs/plans/fast/mailbox-materials/ledger.md` (fast-p controller state bookkeeping: Status RUNNING, child 07 LIGHT_PASS_WITH_NOTES, child 08 LIGHT_VERIFYING, amendment A1 rows) — controller evidence, not product code.
- O-3: em-stats 4-span interpretation — `失败` span = `failed + sourceUnavailable` (statDefs expert-materials.js:452-456, mapping :882, code comment :46-49). S-1 DOM contract (both plan docs) has exactly 4 stat spans (authoritative byte-verbatim blocks), and the 4 spans always sum to summary.total with no hidden gap; row-level `data-state` (SOURCE_UNAVAILABLE distinct) plus reason text still distinguish 获取失败 vs 来源不可用. Summary field usage is faithful to the backend DTO. Display-level fold only — no plan-contract violation.
- O-4: source-filter options accumulate from browsed pages (store.seenSources, merged in applyPage; rebuildSourceOptions on chrome render) rather than a full server-side source index. Display-level convenience: options are per-contact, current selection survives rebuild, and choosing a seen source refetches page 0 with server-side source/sourceId params; I-3 selection/store semantics unaffected. No contract text defines the option enumeration — no violation.
- O-5: `mvn test` Skipped 9 — pre-existing opt-in database/migration IT gates (@EnabledIfSystemProperty(migrationIt=true) / mysql-it profile) requiring Docker/test DB; not child-08 artifacts, unchanged from baseline.

### Required Action
- COMPLETE_CHILD
