## Light Verification: LIGHT_FAIL
Child: 04 and plan path docs/plans/2026-09-07/04-inbound-metadata-persistence.md
Boundary: 95d8661ed3d9e0557fe69cce6f6dde24dbea5e79..5f5623dc4df04b701aa37e0904ec07579d6ba8cb
Verifier: Verify04r2

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
|Authorized scope|PASS|`git diff-tree --name-only -r e0db1ea..5f5623dc` = exactly the 10 brief files (InboundMailProcessing.kt, InboundMailProcessingRepository.kt, AutoMailReplyService.kt, MailAttachmentService.kt, UnmatchedInboundMailService.kt, V120__scope_inbound_uid_by_validity.sql, AutoMailReplyServiceTest.kt, MailAttachmentServiceTest.kt, UnmatchedInboundMailServiceTest.kt, FlywayMigrationIntegrationTest.kt). The 4 extra paths in the full 95d8661..5f5623dc window are docs/plans/fast/mailbox-materials/children/03/*.md committed in the interleaved 03-evidence commit e0db1ea (controller evidence, no product code). HEAD == 5f5623dc (verified `git rev-parse HEAD`); tracked tree clean; only untracked controller evidence docs. |
|Plan and invariants|FAIL|AUTO_FIX finding A-1 below: legacy content-mode (metadataOnly=false) matched-branch messages with ≥1 attachment now fail at confirm with `IllegalStateException` from `bridgeInboundProcessing`, because child 03 unconditionally populates `ReceivedMailAttachment.source` in both fetch modes (ImapMailReceiveService.kt:362-394) while content-mode saves never create transfer rows (MailAttachmentService.kt:294-350; legacy unit test MailAttachmentServiceTest.kt:124 asserts `verifyNoInteractions(attachmentTransferRepository)`), yet every matched record-owner confirm runs bridge with default `bridgeMetadata=true` (AutoMailReplyService.kt:1119/1130/1181; call sites 222/285/294/328/405/437/469/500/523/561/624/647/732 → bridge at 1156/1209). Other I-1/I-3/I-4 checks pass with direct code/test evidence (see AUTO_FIX entry and command results). |
|Required commands|PASS|1) `mvn test -Dtest=AutoMailReplyServiceTest,MailAttachmentServiceTest,UnmatchedInboundMailServiceTest` → exit 0, fresh surefire: AutoMailReplyServiceTest 49/0/0, MailAttachmentServiceTest 11/0/0, UnmatchedInboundMailServiceTest 11/0/0 (71 total); exec-plugin Node 671/671. 2) `DOCKER_HOST=unix:///Users/lukai/.orbstack/run/docker.sock mvn -Dtest=FlywayMigrationIntegrationTest -DmigrationIt=true -Dapi.version=1.40 test` → exit 0, FlywayMigrationIntegrationTest 15/0/0 (142.5s), includes the new V120 contract test. 3) `mvn test` → exit 0, fresh-only (mtime≤10min) 3150 run / 0 fail / 0 err / 8 skipped (migrationIt/mysqlIt container-gated), Node 671/671. Bare FlywayMigrationIntegrationTest is environment-blocked at baseline (docker-java 1.32 vs OrbStack min 1.40 → "Docker is required"); the DOCKER_HOST + `-Dapi.version=1.40` workaround is the controller's known-good form and was used. |
|Downstream interfaces|PASS|V120 is next-free: pre-existing max V119 (V119__create_mail_attachment_transfer.sql), no V121+ present. V120 adds only `uid_validity BIGINT NOT NULL DEFAULT 0` and replaces the unique key with (sender_account_code, uid_validity, imap_uid) — exactly the master-plan 持久化契约 04 shape; no history backfill; no second business-state column (LEGACY_UID_UNVERIFIABLE/BODY_TRUNCATED via existing MANUAL_REVIEW+reason). V119 (transfer table) untouched; mail_attachment has no new columns (no migration/entity change for it in the window). Downstream 05/06/07/11 child briefs are not yet written in the worktree (only fix-logs), so consumption is checked against master contract text only. |

### AUTO_FIX
- **A-1 — bridgeInboundProcessing breaks legacy content-mode messages on matched branches (production reachable in the default metadataOnly=false configuration).**
  Governing requirement: plan 04 I-4 ("metadataOnly=false 保持旧路径，绑定新增关联随开关启用"), I-2 bridge completeness is scoped to **metadata 附件** ("任何 metadata 附件缺登记行都会使本信确认失败"), and master 持久化契约 04 (legacy content path unchanged; bridge = MATERIAL 终结登记 only). Exec report's own claim "content 路径保持旧写法…逐字段原样" covers only the save functions, not confirm-time bridge.
  Evidence chain (all HEAD code, base 95d8661 had no bridge — `git show base:AutoMailReplyService.kt | grep -c bridge` = 0):
  1. Child-03 fetch populates `source` unconditionally in BOTH modes (ImapMailReceiveService.kt:376-394: `content` conditional on `metadataOnly`, `source = ImapAttachmentSource(...)` unconditional; file unchanged base→head). Production default `metadataOnly=false` (MailAttachmentStorageProperties.kt:12; application.yml:162-163 sets only base-path) ⇒ content-mode attachments carry content AND source.
  2. Content-mode saves never write transfer rows: `saveLegacyRecordAttachment` (MailAttachmentService.kt:294) / `saveLegacyProcessingAttachment` (329) touch only mailAttachmentRepository + expertDocumentRepository; the legacy unit test asserts `verifyNoInteractions(attachmentTransferRepository)` (MailAttachmentServiceTest.kt:124). Transfer rows are written only by `registerMetadataAttachment` (MailAttachmentService.kt:198, content==null path) and AttachmentTransferService.register (no production caller in 04).
  3. Every matched record-owner confirm (lines 222/285/294/328/405/437/469/500/523/561/624/647/732) calls `confirmProcessed`/`confirmManualReview(WithBody)` with default `bridgeMetadata=true` (AutoMailReplyService.kt:1119/1130/1181; bridge call sites 1155-1156, 1208-1209). Only CONTACT_NOT_FOUND (163), INTRODUCTION_NOT_SENT (181), and reviewManualOnly (1235) pass `bridge=false`.
  4. `bridgeInboundProcessing` (MailAttachmentService.kt:155-193) skips only `source == null` attachments; for every source-bearing attachment it demands an existing transfer row (`?: throw IllegalStateException`, line ~170), i.e. it discriminates on `source != null` instead of on `content == null` (metadata registration).
  Consequence (default config, metadataOnly=false — the mode 03/11 keeps enabled until the program flips defaults): any matched expert reply with ≥1 attachment now throws IllegalStateException at confirm inside the TransactionTemplate (AutoMailReplyService.kt:82-99) → whole processSingle rolls back → no processing row, no markSeen, cursor never advances → the mail is re-fetched and re-processed every poll; in the QA auto-reply (send at ~682, confirm at 732) and meeting-invitation (send ~545, confirm 561) branches the SMTP side effect precedes the throwing confirm, so the failure repeats per poll with no durable record. This is a regression of the legacy content-mode path that the plan explicitly requires to keep old behavior, and it is not caught by the suite because AutoMailReplyServiceTest mocks MailAttachmentService entirely and MailAttachmentServiceTest exercises bridge only with metadata fixtures (the `contentAttachment` content+source fixture at MailAttachmentServiceTest.kt:83 is defined but never used; bridge tests at 320/389 use `metadataAttachment` only).
  Smallest authorized correction (plan uniquely defines it: bridge completeness applies only to metadata attachments — attachments registered with content==null; legacy content-mode attachments are already fully registered by the old content path and must not be required to have transfer rows): in `MailAttachmentService.bridgeInboundProcessing`, skip attachments whose `content != null` (i.e. only require/link transfer rows for metadata attachments), or equivalently make AutoMailReplyService matched confirms pass `bridgeMetadata = received.attachments.all { it.content == null }`. Files affected are all within the authorized 10-file list (MailAttachmentService.kt, AutoMailReplyService.kt, MailAttachmentServiceTest.kt, AutoMailReplyServiceTest.kt).

### RECORD_ONLY
- O-1: I-2 single-confirm-point/markSeen and skipImapAck logic verified in code (processSingle marks seen only after TransactionTemplate success and only when `!skipImapAck`; processByUids/receiveAndAutoReply honor it). Not a finding.
- O-2: V120 migration + entity/repo + Flyway contract test and legacy-0-validity corroboration logic match I-1 (new finder by account/uid_validity/uid; legacy finder only claims same-generation identity when non-null Message-ID + from + second-truncated receivedAt all match; unverifiable → MANUAL_REVIEW/LEGACY_UID_UNVERIFIABLE; `require(uidValidity > 0)` fail-closed). Not a finding.
- O-3: I-3 owner XOR and bindToContact idempotence hold (UnmatchedInboundMailService.kt:186-192 calls only `ensureDocumentsForProcessingAttachments` under metadata switch; UnmatchedInboundMailServiceTest asserts never saveUnmatched/saveInbound and same attachmentId; no owner move/re-download/review rewrite). Not a finding.

### Required Action
- AUTO_FIX

## Light Verification: LIGHT_PASS
Child: 04 and plan path docs/plans/2026-09-07/04-inbound-metadata-persistence.md
Boundary: 95d8661ed3d9e0557fe69cce6f6dde24dbea5e79..c9f80906c872b21d0f4887c7c3f4c95e961b1cef
Verifier: Verify04r3

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
|Authorized scope|PASS|HEAD == c9f80906c872b21d0f4887c7c3f4c95e961b1cef (`git rev-parse HEAD`); tracked tree clean (`git status --porcelain --untracked-files=no` empty; only untracked controller evidence docs/plans/fast/**). `git show --stat 5f5623dc` = exactly the brief's 10 authorized files (+1321/−133); `git show --stat c9f8090` = exactly 4 files, all subset of the authorized 10: MailAttachmentService.kt, AutoMailReplyService.kt, MailAttachmentServiceTest.kt, AutoMailReplyServiceTest.kt (+142/−6). Window 95d8661e..c9f8090 name-only = the 10 product/test files + docs/plans/fast/mailbox-materials/children/03/*.md (interleaved 03-evidence commit e0db1ea, docs only, no product code). |
|Plan and invariants|PASS|A-1 CLOSED (see below): bridge discriminator is now content==null-based — MailAttachmentService.kt:163-164 skips legacy content attachments (`// 旧 content 模式附件（child-03 无条件携带 source）：bridge 不适用.` + `if (received.content != null) return@forEach`) before any transfer-row lookup; the `?: throw IllegalStateException` strictness at :174-178 now only reachable for metadata (content==null, source!=null) attachments. Matched confirms still gate on bridge with default `bridgeMetadata=true` (AutoMailReplyService.kt:1121/1132/1183; bridge invoked at 1158/1211) so I-2 completeness for metadata attachments is unchanged; only bridge=false branches remain CONTACT_NOT_FOUND (:163), INTRODUCTION_NOT_SENT (:181), reviewManualOnly (:1237). I-1/I-2/I-3/I-4 re-confirmed unchanged on HEAD by the fix commit (fix touched only bridge + docs): I-1 require(uidValidity>0) fail-closed at AutoMailReplyService.kt:106; dedup via findBySenderAccountCodeAndUidValidityAndImapUid; legacy 0-validity same-uid claimed only via sameMessageGeneration else LEGACY_UID_UNVERIFIABLE (:143-153). I-2 TransactionTemplate wrapper + single post-commit markSeen honoring skipImapAck (:79-96). I-3 owner XOR + bindToContact calls only ensureDocumentsForProcessingAttachments (UnmatchedInboundMailService.kt:192), gated by metadataOnly switch (MailAttachmentService.kt:134-139). I-4 BODY_TRUNCATED→reviewManualOnly (:133-141); LEGACY_UID_UNVERIFIABLE/BODY_TRUNCATED in MANUAL_REVIEW_OUTCOMES (:1330-1341). New regression tests green in fresh runs (below). |
|Required commands|PASS|All three run freshly this session, exit 0 / BUILD SUCCESS each. 1) `JAVA_HOME=zulu-11 mvn test -Dtest=AutoMailReplyServiceTest,MailAttachmentServiceTest,UnmatchedInboundMailServiceTest` (10:50) → surefire 50+12+11=73 run / 0 F / 0 E (AutoMailReplyServiceTest 50 incl. new `matched content mode message with attachments confirms through the default bridge path`; MailAttachmentServiceTest 12 incl. new `bridge skips legacy content attachments with remote source and stays strict for metadata`; UnmatchedInboundMailServiceTest 11); exec-plugin Node 671/671. 2) `DOCKER_HOST=unix:///Users/lukai/.orbstack/run/docker.sock mvn -Dtest=FlywayMigrationIntegrationTest -DmigrationIt=true -Dapi.version=1.40 test` (11:02:55, re-run post-full-suite for durable artifact) → 15 run / 0 F / 0 E, incl. `V120 scopes inbound uid uniqueness by uid validity without backfilling history` + fresh/V23/V24 up-to-V120 cases. 3) `mvn test` (10:55-10:57) → fresh-window (230 report files, mtime 10:55-10:58) 3152 run / 0 fail / 0 err / 8 skipped (migrationIt/mysqlIt container-gated incl. FlywayMigrationIntegrationTest skipped=1 as designed), Node 671/671. |
|Downstream interfaces|PASS|V120 next-free: pre-existing max V119 (`V119__create_mail_attachment_transfer.sql`), no V121+ anywhere in db/migration; window migration diff = only new V120 file, V119 untouched (transfer table shape fixed). V120 shape exactly master 持久化契约 04: single `uid_validity BIGINT NOT NULL DEFAULT 0` on inbound_mail_processing, unique key replaced by (sender_account_code, uid_validity, imap_uid), no history backfill, no second business-state column (BODY_TRUNCATED/LEGACY_UID_UNVERIFIABLE ride existing MANUAL_REVIEW+reason). Entity/repo mirror (InboundMailProcessing.kt:13 `uidValidity: Long = 0`; repository new generation finder + old finder retained for legacy corroboration only, InboundMailProcessingRepository.kt:20-32). mail_attachment/mail_attachment_transfer schemas unchanged in window. Downstream 05/06/07/11 consumer surfaces unchanged (their briefs not yet present in worktree; consumption checked against master contract text as in prior round). |

### AUTO_FIX
- A-1: CLOSED. Evidence: (a) discriminator trace — MailAttachmentService.kt:164 now skips every attachment with `content != null` before the transfer-row existence lookup, so the previously-throwing path (`?: throw IllegalStateException` for source-bearing content-mode attachments, old code at :170, now only reachable for content==null metadata attachments at :174-178) is provably unreachable for legacy content-mode mail; bridge completeness is scoped to metadata attachments per plan I-2. (b) Fix commit c9f8090 production change is exactly this skip (+ doc-comment rewording in AutoMailReplyService.kt:1106-1112, no logic change there — matched confirms keep default bridgeMetadata=true and still invoke bridgeInboundProcessing at 1158/1211). (c) Regression tests exercise the previously-missed path and pass in the fresh targeted run: MailAttachmentServiceTest `bridge skips legacy content attachments with remote source and stays strict for metadata` — real legacy save writes file and asserts zero attachmentTransferRepository.save, then bridgeInboundProcessing(content attachment) makes zero findBy…/save transfer calls (no throw), then a mixed message (content + metadata attachment, metadata row absent) still throws IllegalStateException (strictness retained); AutoMailReplyServiceTest `matched content mode message with attachments confirms through the default bridge path` — content+source attachment through the default bridge path confirms with outcome AUTO_REPLY_DISABLED, asserts bridgeInboundProcessing called with the attachment, markSeen(account, 101), and verifyNoInteractions(groundedAutoReplyDecisionService, deliveryService). Old failure mode gone: prior fault was content-mode matched message w/ ≥1 attachment → ISE at confirm → full rollback → never markSeen/cursor stall; the exact former code path is eliminated by the :164 guard. No new F-ids. N/A.

### RECORD_ONLY
- N/A (no non-gate items; prior O-1..O-3 confirmations re-verified unchanged on HEAD — markSeen single-confirm point, V120/I-1 corroboration, I-3 owner XOR/bindToContact — none constitute findings; the full-suite run overwrites FlywayMigrationIntegrationTest surefire XML with skipped=1 by design, which is why command 2 is evidenced by its standalone run, re-executed post-full-suite at 11:02:55 with 15/0/0).

### Required Action
- COMPLETE_CHILD
