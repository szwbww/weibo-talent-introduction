# Verify Log — Child 01 附件元数据存储兼容

## Light Verification: LIGHT_PASS_WITH_NOTES
Child: 01 and plan path
docs/plans/2026-09-07/01-attachment-storage-compat.md
Boundary: 8a0c5360e25e875e52800d17797a7b1ea4bd452c..8779d71f00567625ebebe206801994aefcc5725b
Verifier: Verify01

Worktree: /Users/lukai/IdeaProjects/weibo-talent-introduction-fast-mailbox-materials (branch fast/mailbox-materials)
HEAD == 8779d71f00567625ebebe206801994aefcc5725b (`feat(fast-p): implement 01`) confirmed via `git rev-parse HEAD`; `git status --porcelain` shows only untracked `docs/plans/fast/mailbox-materials/` (controller-owned evidence, expected untracked per brief), no tracked modifications.

### Four Gates

|Gate|Result|Evidence|
|---|---|---|
|Authorized scope|PASS|`git diff-tree -r --name-only 8779d71` (impl commit vs parent a61ecb5) = exactly the 10 brief-authorized files: MailAttachment.kt, V118__allow_attachment_metadata_only.sql, MailboxAttachmentService.kt, ExpertDocumentBrowseService.kt, DocumentTextExtractor.kt, ExpertContactManagementController.kt, MailboxAttachmentServiceTest.kt, ExpertDocumentBrowseServiceTest.kt, DocumentTextExtractorTest.kt, FlywayMigrationIntegrationTest.kt. Full range 8a0c536..8779d71 src/ delta = same 10 files; the range also contains docs-seed commit a61ecb5 (docs/plans/2026-09-07/** + artifacts/**, controller-owned, not product code). No other src/ files changed (`git diff-tree -r --name-only 8a0c536..8779d71 -- src/` = 10 files; impl commit file list = same 10).|
|Plan and invariants|PASS|I-1: MailAttachment.kt:15-16 `fileSize: Long? = null` / `storagePath: String? = null`; V118 migration (13 lines) MODIFYs only file_size→BIGINT NULL DEFAULT NULL, storage_path→VARCHAR(1024) NULL, file_name→TEXT NOT NULL. DTO null passthrough: MailboxAttachmentService.kt AttachmentMetaResponse.fileSize:Long? (:16) & toMetaResponse (:63-71, entity value, no 0/"" coercion); ExpertDocumentBrowseService.kt ExpertDocumentFile.fileSize/downloadUrl/previewUrl nullable (:17-22, :57 `hasLocalFile = attachment.storagePath != null`, :63-68 URLs null when no local file, previewable = hasLocalFile && isPreviewable). Null path rejected before file read: requireNotNull in MailboxAttachmentService.validateStoragePath (:46-49), ExpertDocumentBrowseService.validateAndResolve (:131-133), DocumentTextExtractor.resolveAttachment (:58-61) — no silent skip; extraction test `extract rejects attachment with no storage path before reading any file` (DocumentTextExtractorTest.kt:142) asserts "no local file" message, would fail if code silently skipped or coerced. 0-byte legal: MailboxAttachmentServiceTest `download accepts a real zero-byte file as size zero` (:198, size=0 + Files.exists + empty read); Flyway IT row file_size=0 + storage_path IS NOT NULL legal. I-2: historical values not rewritten (V118 = MODIFY nullability only, verified vs V7 baseline: file_name VARCHAR(255) NOT NULL / file_size BIGINT NOT NULL DEFAULT 0 / storage_path VARCHAR(1024) NOT NULL, none of the three modified columns indexed or FK'd so MODIFY is constraint-safe); download length = actual verified file: MailboxAttachmentService.download fileSize=Files.size(resolvedPath) (:41), ExpertDocumentBrowseService.resolveForDownload fileSize=Files.size(validation.resolvedPath) (:94), consumers set `.contentLength(resource.fileSize)` (ExpertDocumentBrowseController.kt:40/:58, MailboxAttachmentController.kt:41 — both unchanged); mismatch tests: MailboxAttachmentServiceTest `download reports actual size…` (:168, stored 999 → actual 5), ExpertDocumentBrowseServiceTest `resolveForDownload reports actual size…` (:360, stored 999 → actual 4); cross-expert/boundary/symlink rejection tests retained (ExpertDocumentBrowseServiceTest :193/:209/:225/:264/:286; base-path realpath checks in both services + extractor unchanged). resolveForPreview reuses resolveForDownload (:98). I-3: file_name TEXT NOT NULL; Flyway IT 300-char Chinese name "研"×300 insert + CHAR_LENGTH=300 + verbatim round-trip; document_status untouched (V118 does not reference expert_document; IT asserts PENDING_REVIEW survives full V23→V118 chain); display fileName passed through verbatim in list DTOs. Controller: ExpertContactManagementController.kt diff = only MailAttachmentResponse.fileSize/storagePath → nullable (:487-488); toResponse keeps `requireNotNull(mailRecordId)` (:604) and old attachments array structure (:525); no processing-owner fields introduced. |
|Required commands|PASS|All run freshly at HEAD 8779d71, JAVA_HOME=zulu-11: (1) `mvn test -Dtest=MailboxAttachmentServiceTest,ExpertDocumentBrowseServiceTest,DocumentTextExtractorTest` → exit 0, BUILD SUCCESS; surefire MailboxAttachmentServiceTest 7/0/0/0, ExpertDocumentBrowseServiceTest 14/0/0/0, DocumentTextExtractorTest 5/0/0/0; same build exec-plugin node-test 671/671 pass. (2a) bare `mvn -Dtest=FlywayMigrationIntegrationTest -DmigrationIt=true test` → exit 1, `Tests run: 1, Errors: 1, IllegalStateException: Docker is required for Flyway migration tests` — identical to ledger baseline env-block (docker-java client API 1.32 vs OrbStack daemon MinAPIVersion 1.40, verified `docker version` → server 1.54/min 1.40); baseline-reproduced, NOT a child defect. (2b) documented workaround `DOCKER_HOST=unix:///Users/lukai/.orbstack/run/docker.sock … -Dapi.version=1.40` → exit 0, BUILD SUCCESS, FlywayMigrationIntegrationTest **Tests run: 13, Failures: 0, Errors: 0, Skipped: 0** on real mysql:8.0.36 container (~101 s). (3) full `mvn test` → exit 0, BUILD SUCCESS, **Tests run: 3101, Failures: 0, Errors: 0, Skipped: 8** (skips are pre-existing class-level gates: FlywayMigrationIntegrationTest migrationIt gate + 7 docker/IT/@Disabled gates in unchanged test classes, none among the changed 10-file set except the gated Flyway IT itself); exec-plugin node-test 671/671 pass. |
|Downstream interfaces|PASS|Migration numbering: at base 8a0c536 max = V117; at HEAD exactly one new migration V118 (117 files total), no V119+ exists — numbering free for children 02+ (`git ls-tree` evidence). mail_attachment contract per master 持久化契约: V118 file = `file_size BIGINT NULL DEFAULT NULL`, `storage_path VARCHAR(1024) NULL`, `file_name TEXT NOT NULL`; Flyway IT asserts IS_NULLABLE YES/YES/NO + DATA_TYPE 'text' via information_schema; no new columns/status fields (only 3 MODIFYs). V36 owner XOR + FKs preserved: IT asserts chk_mail_attachment_owner / fk_mail_attachment_record / fk_mail_attachment_inbound exist post-V118 and double-NULL owner insert still rejected (SQLException). Old URLs/file-read semantics preserved: download/preview URL strings unchanged in both services and their (unchanged) controllers; expert AI-analysis path (ExpertDocumentAnalysisService → extractor/validateAttachmentBelongsToContact) untouched and still file-gated; mailbox/browse download controllers unchanged; Content-Length now = Files.size of validated realpath — identical to stored value for consistent historical rows. |

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1: List DTOs (AttachmentMetaResponse.fileSize, ExpertDocumentFile.fileSize) pass the stored column value through (null when null) while download/preview file responses compute `Files.size(verified realpath)`. Asymmetry is observable only if a stored file_size ever goes stale; the plan requires actual size only on file responses ("下载 DTO 仍 Long…再 Files.size"; list DTO merely "允许 fileSize=null"), so this complies. Later child 06 defines a distinct actualSize contract for material items; note for cross-plan awareness only. Evidence: MailboxAttachmentService.kt:63-71, ExpertDocumentBrowseService.kt:63-70 (list) vs :41/:94 (file responses).
- O-2: execution.md I-2 phrasing "下载/浏览响应 fileSize = Files.size" is accurate for download/preview *resource* responses but the word "浏览" must not be read as the browse *list* DTO, which intentionally keeps the stored value (see O-1). Purely a wording nuance in the implementer report; no plan deviation.
- O-3: The workaround Flyway IT run prints an expected `ERROR … Migration of schema … to version "24 …" failed!` line from a pre-existing intentionally-broken/repair scenario inside the suite; final result 13/13 pass confirms it is scripted test noise, not a defect. Recorded to preempt misreading the log.
- O-4: Entity gained defaults (`fileSize: Long? = null`, `storagePath: String? = null`); both production write paths (MailAttachmentService.kt:41/:82, unchanged files) still pass explicit non-null fileSize/storagePath after Files.write, so metadata-only rows cannot yet be produced in production — consistent with the plan's "不启用元数据收信" boundary. No runtime null surface exists until children 03/04 add writers.

### Required Action
- COMPLETE_CHILD
