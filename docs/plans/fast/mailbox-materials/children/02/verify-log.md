## Light Verification: LIGHT_PASS
Child: 02 and plan path docs/plans/2026-09-07/02-attachment-transfer-core.md
Boundary: 8779d71f00567625ebebe206801994aefcc5725b..7c2420e80f98775c1fcf4970598617faca378c0b
Verifier: Verify02

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
| Authorized scope | PASS | `git diff-tree --name-only -r 8779d71..7c2420e8` shows commit 7c2420e `feat(fast-p): implement 02` (parent b8b6ae5, docs-only controller evidence for child 01) changing EXACTLY the 10 brief-authorized files: V119__create_mail_attachment_transfer.sql, MailAttachmentTransfer.kt, MailAttachmentTransferRepository.kt, AttachmentTransferService.kt, AttachmentTransferWorker.kt, ImapAttachmentContentFetcher.kt, MailAttachmentStorageProperties.kt, AttachmentTransferServiceTest.kt, AttachmentTransferWorkerIT.kt, FlywayMigrationIntegrationTest.kt (git show --stat 7c2420e8 = 10 files, 4089 insertions/11 deletions; no docs, no application.yml, no other migrations). The only other paths in the full range are docs/plans/fast/mailbox-materials/children/01/* committed by b8b6ae5 (controller evidence). HEAD == 7c2420e80f98775c1fcf4970598617faca378c0b; `git status --porcelain` clean apart from untracked docs/plans/fast/** (controller evidence, expected). |
| Plan and invariants | PASS | I-1 source identity: unique key (account_code,folder,uid_validity,imap_uid,part_path) + UNIQUE attachment_id + folder utf8mb4_bin (V119:40-42); register is idempotent by source w/ REQUIRES_NEW conflict fallback (AttachmentTransferService.kt:135-217); same-name-cross-UID distinct row tests (AttachmentTransferServiceTest.kt:135, AttachmentTransferWorkerIT.kt:502); I-2 state machine: only 6 states, DB CHECK (V119:66-70), requestable states METADATA_ONLY/FAILED/SOURCE_UNAVAILABLE→QUEUED (MailAttachmentTransfer.kt:64-70; repo markRequested:59-83), retry must be explicit, recovery touches only DOWNLOADING-expired (repo recoverExpiredLeases:152-168; IT restart test :704); I-3: fixed 2-driver + 1 renew/watchdog thread pool (AttachmentTransferWorker.kt:129-180), named lock talent-attachment-claim in same tx as CAS claim w/ finally release (:209-250), claim SQL enforces global<2 & per-account<1 (:101-132), 15s lease/2s renew/600s total deadline not extendable, abort hard-closes connection (:306-318, 641-660; IT :607 connection closed, :816 stale lease/token cannot commit), 100MiB/10min/10s/64KiB defaults in MailAttachmentStorageProperties.kt; I-4: .part at basePath/transfer/{id}/{workerToken}.part, ATOMIC_MOVE to fixed content.bin before STORED commit (:336-346, 520-526), crash-between-rename-and-commit converges via deterministic final-path check w/o re-download (worker:306-311; IT :750), failure deletes .part (multiple deleteQuietly paths), write-back updates only existing mail_attachment storage_path/file_size WHERE storage_path IS NULL in same tx as commitStored (:451-459); fetcher validates UIDVALIDITY→UID→Message-ID→partPath→contentType on READ_ONLY connection, partial fetch BODY.PEEK + fetchsize, forceClose → IMAPFolder.forceClose real socket close, no subject/from search (ImapAttachmentContentFetcher.kt:96-250, 345-365); all error messages desensitized (no credentials/body; e.g. "cannot connect to the source mailbox", MAX_ERROR_MESSAGE_LENGTH=500); no audit/expert_contact/conversation-state writes anywhere in the 3 production classes (sole DB write = mail_attachment row update at Worker:451; service's ExpertDocument/MailRecord repo use is read-only ownership lookup :391-395); unknown purpose rows stay QUEUED unexecuted and logged as config error (worker:191-206, 266-273; IT :946); concurrency caps hard-wired GLOBAL=2/ACCOUNT=1 (Worker companion), queue cap 5000 + 500/request (Service companion). V119 contract test asserts columns/CHECK/FK/collation/defaults/unique-key/combination rules (FlywayMigrationIntegrationTest.kt:534-680). |
| Required commands | PASS | All 4 run freshly at HEAD with JAVA_HOME=zulu-11: (1) `mvn test -Dtest=AttachmentTransferServiceTest` → Tests run: 20, Failures: 0, Errors: 0, exit 0; (2) `mvn -Pmysql-it -Dtest=AttachmentTransferWorkerIT test` (real MySQL 127.0.0.1:3306) → Tests run: 12, Failures: 0, Errors: 0, exit 0; (3) bare `mvn -Dtest=FlywayMigrationIntegrationTest -DmigrationIt=true test` → environment-block reproduced exactly as baseline: BadRequestException (Status 400: client version 1.32 too old, min 1.40) → `IllegalStateException: Docker is required`, Tests run: 1, Errors: 1, exit 1 — NOT a defect; workaround `DOCKER_HOST=unix:///Users/lukai/.orbstack/run/docker.sock mvn -Dtest=FlywayMigrationIntegrationTest -DmigrationIt=true -Dapi.version=1.40 test` → Tests run: 14, Failures: 0, Errors: 0, exit 0; (4) full `mvn test` → BUILD SUCCESS exit 0, 3121 tests run, 0 failures, 0 errors, 8 skipped (gated classes incl. Flyway IT correctly skipped in default run), 671 Node JS tests pass. |
| Downstream interfaces | PASS | Migration numbering: V118 (child 01) and V119 are the max; no V120+ exists and V119 is a new file never overwriting an applied migration (ls src/main/resources/db/migration). mail_attachment gained NO new columns from this child — the only SQL in the range is V119 (new table); write-back targets pre-existing storage_path/file_size columns (Worker:451-459; V7/V118 define them). Transfer rows reference real attachment ids: FK fk_mail_attachment_transfer_attachment → mail_attachment(id), register requires attachment exists (Service:165,197-203), Flyway IT seeds attachment 301 then inserts referencing MATERIAL row. States/columns match the master persistence contract (mail_attachment_transfer table spec) that children 04/06/07 consume: 26 columns field-for-field, unique source key, queue/account/inbound indexes, 6 states, purpose MATERAL/DMARC CHECK with attachment_id combination, lease/worker_token/attempt/queued_at/started_at/bytes_downloaded/error semantics, DATETIME(3) created/updated. |

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1: Global/per-account concurrency limits (2/1) and the 5000-row queue cap are compile-time constants (AttachmentTransferWorker.kt companion GLOBAL_DOWNLOAD_CONCURRENCY=2/ACCOUNT_DOWNLOAD_CONCURRENCY=1; AttachmentTransferService.kt companion MAX_QUEUED_ROWS=5000L) rather than MailAttachmentStorageProperties entries, although the brief's shorthand lists them among "properties defaults" and master states lease/renew/total-timeout as "可配置" (those are in properties). Invariant values themselves are exactly met; no plan requirement forces these three to be runtime-configurable. Outside the light gate; noting for the controller's records.
- O-2: Surefire does not write a report file for the class-level-gated AttachmentTransferWorkerIT when the default run skips it (container-level skip), leaving the earlier `-Pmysql-it` report (12 tests) in target/. Correcting for that stale file, the fresh full `mvn test` totals 3121 run / 0 fail / 0 err / 8 skipped — matching the execution.md claim exactly (a naive sum of all report files yields 3133 = 3121 + 12 stale). No test outcome affected.

### Required Action
- COMPLETE_CHILD
