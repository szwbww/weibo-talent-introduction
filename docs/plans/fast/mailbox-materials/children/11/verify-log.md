# Child 11 Light Verification Log

## Light Verification: LIGHT_PASS_WITH_NOTES
Child: 11 and plan path docs/plans/2026-09-07/11-release-and-cache-gate.md
Boundary: 51e08279cf4a66de598752275acd2a886c7b3c07..29ca7349ac1410934900fc0bbdaf181a88ba71d0
Verifier: Verify11

Preliminaries confirmed: `git rev-parse HEAD` = `29ca7349ac1410934900fc0bbdaf181a88ba71d0` (feat(fast-p): implement 11, parent 54262d4 ← a849605 ← 51e0827). `git status --porcelain` shows only controller evidence: ` M docs/plans/fast/mailbox-materials/ledger.md`, `?? docs/plans/fast/mailbox-materials/children/11/`. Plan file SHA-256 = `53b122a35ffb5db509fd2b55cb21c7106736e2b855ab5bd5971c13a9ff2917fa`, matching execution.md's recorded amended-plan identity.

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
|Authorized scope|PASS|`git diff-tree --name-only 51e08279..29ca7349` product/test set = EXACTLY the 11 brief-authorized files: `src/main/resources/static/index.html`, `src/main/kotlin/com/weibo/talentintroduction/config/MailAttachmentStorageProperties.kt`, 7× `src/test/js/{batchSendTaskConsoleVisualFix,checkRepliesRelocation,manualReplySubjectPrefill,overlayAndDialogContrast,ragKnowledgeBasePage,ragWorkbenchRender,trustReplyWorkbenchSharedMount}.test.js`, `src/test/kotlin/.../mail/service/ImapMailReceiveServiceTest.kt` (A2), `docs/runbooks/mailbox-material-chat-rollout.md` (new). Per-commit: 29ca734 = those 11 files only; `git diff-tree 54262d4` = `docs/plans/2026-09-07/11-release-and-cache-gate.md` only (+7/−1, A2 amendment block incl. row-11 file list, confirmed in file). Remaining in-range entries are docs-only controller evidence (plan 11 .md; children/10 brief/execution/fix-log/verify-log from a849605). No migration, no other main/test file touched in range.|
|Plan and invariants|PASS|I-1: `grep -o 'v=[0-9a-z-]*' index.html | sort | uniq -c` → `7 v=20260907-material-chat` (sole value); `grep -c 20260903-bounce-warning index.html` = 0. Registration lines: CSS styles.css:11 → expert-materials.css:12 → mailbox-chat.css:13; scripts trust-reply-workbench.js:2109 → expert-materials.js:2110 → mailbox-chat.js:2111 → app.js:2112 (workbench original position after task-modal-runtime.js preserved; git diff of index.html = key bump + 4 registration lines only, +7/−3, no DOM/CSS). All 7 pinned test files strengthened, not relaxed: 3→7 asset presence + `Set(keys).size==1`/per-key equality + monotonic `indexOf` order assertion over `[styles.css, expert-materials.css, mailbox-chat.css, trust-reply-workbench.js, expert-materials.js, mailbox-chat.js, app.js]` + old-key-zero-hits negative assertion; both spellings updated (CACHE_KEY const: checkRepliesRelocation:11, manualReplySubjectPrefill:13, overlayAndDialogContrast:22, ragWorkbenchRender:20, trustReplyWorkbenchSharedMount:22; inline includes: batchSendTaskConsoleVisualFix:50-63, ragKnowledgeBasePage:333-350). Repo-wide grep: old key `20260903-bounce-warning` exists ONLY as those 7 negative assertions. I-2: MailAttachmentStorageProperties.kt:13 `val metadataOnly: Boolean = true` (comment flip only, diff +1 line); explicit false preserved — ImapMailReceiveServiceTest.kt:28 `MailAttachmentStorageProperties(metadataOnly = false)` compiles and passes, runbook documents config fallback. A2 one-liner: git diff of ImapMailReceiveServiceTest.kt = single constructor line, no assertion/production change. I-3: runbook `docs/runbooks/mailbox-material-chat-rollout.md` covers release 2.1-2.9 (HEAD/worktree + max Flyway V121 record; test-DB backup/migration validation; compatible backend start; dryRun reconcile; 19+20 and 1000-attachment pressure, FETCH=0 expectation; metadataOnly enable; versioned frontend 7-key load; two-entry/check/analyze/send chain; real-version+FETCH+screenshot evidence) and rollback 3.0-3.5 (stop new submissions → worker idle/lease/close connections, no mid-DOWNLOADING kill; metadataOnly=false legacy-only receive with pause-check guidance; remove 4 new registrations to restore legacy hosts + triad re-bump; keep nullable/readiness compat, never pre-01 binary; no DROP/no file deletion; queue continue/pause explicit operator decision); header states no production execution in this plan phase; section 2/3 are documentation of future authorized steps only. No preview/mock page copied into index.html (diff evidence).|
|Required commands|PASS|All run freshly at worktree HEAD 29ca734 (this session, 2026-09-08 20:2x): (1) `node --test src/test/js/*.test.js` → exit 0, tests 730 / suites 136 / pass 730 / fail 0 / skipped 0. (2) `JAVA_HOME=/Library/Java/JavaVirtualMachines/zulu-11.jdk/Contents/Home mvn test` → BUILD SUCCESS (2:40 min), exec-plugin node stage green; fresh-window surefire aggregate (236 suites, mtime-filtered) Tests run 3218, Failures 0, Errors 0, Skipped 9 — matching execution.md; ImapMailReceiveServiceTest 13/13 PASS (0 failures). (3) `node --check src/main/resources/static/app.js && node --check .../expert-materials.js && node --check .../mailbox-chat.js` → exit 0 each.|
|Downstream interfaces|PASS|Release chain coherent: expert-materials.js (index.html:2110) and mailbox-chat.js (:2111) load BEFORE app.js (:2112), so `window.ExpertMaterials` (consumed app.js:8409 `window.ExpertMaterials.configure`) / chat host guards are defined before app code runs; CSS loads dependency order styles→expert-materials→mailbox-chat (:11-13). All 4 newly registered files exist under `src/main/resources/static/` (expert-materials.css/js, mailbox-chat.css/js) and nothing references unregistered resources. metadataOnly=true default matches the 01-10 verified chain (docker-free suite incl. legacy-representation test green under the flip). Rollback path documented and non-destructive (runbook 3.x). Human-acceptance A-1..A-8 (incl. A-8 7-key-same-value) are now reachable at code level; browser-level verification correctly remains for human review — not claimed here.|

### AUTO_FIX
- N/A

### RECORD_ONLY
- R1, full-range diff vs product set: `git diff-tree 51e08279..29ca7349` additionally lists `docs/plans/2026-09-07/11-release-and-cache-gate.md` (in-range 54262d4, docs amend A2) and 4× children/10 doc files (in-range a849605, docs record child 10) — all docs-only controller evidence inside the boundary, no product/test content; per-commit isolation leaves exactly the 11 authorized files. Not a gate violation; recorded for boundary precision.
- R2, stale surefire artifacts: `target/surefire-reports/` contains 3 older IT XMLs (ImapMetadataFetchIT, AttachmentTransferWorkerIT, MailboxConversationRepositoryIT) from earlier mysqlIt-gated runs; excluded from the fresh count via mtime window (fresh = 236 suites / 3218 tests). Child 11 adds no migration, so mysqlIt/Flyway ITs are out of scope per plan; baseline env-block note applies unchanged.
- R3, old-key literal in tests: each of the 7 synced test files keeps one `20260903-bounce-warning` occurrence — confirmed negative assertion (`assert.ok(!…includes("20260903-bounce-warning"))`), i.e. deliberate strengthening, not a stale pin.

### Required Action
- COMPLETE_CHILD
