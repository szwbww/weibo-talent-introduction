## Light Verification: LIGHT_PASS_WITH_NOTES
Child: 09 and plan path docs/plans/2026-09-07/09-material-analysis-selection.md
Boundary: 47a72d885498ff473560d26901d2596ffdddb2cd..020e8392f89ebbf5ed38b95060605055a67167c5
Verifier: Verify09

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
|Authorized scope|PASS|HEAD = 020e8392f89ebbf5ed38b95060605055a67167c5 (`git rev-parse`). Boundary contains exactly 2 commits: 19b01c0 `docs(fast-p): record 08 light verification` (4 controller docs: children/08/{brief,execution,fix-log,verify-log}.md) and 020e839 `feat(fast-p): implement 09`. `git show --stat 020e839` = exactly the 5 brief-authorized product/test files: app.js (+486/-14), expert-materials.js (+156/-16), expertMaterialAnalysisFlow.test.js (+1496 new), ExpertDocumentAnalysisService.kt (+18/-3), ExpertDocumentAnalysisServiceTest.kt (+96). `git diff-tree --name-only 47a72d88 020e839` shows no CSS / index.html / other static or backend changes beyond those 5 + the 4 controller docs. Working tree: sole tracked modification is docs/plans/fast/mailbox-materials/ledger.md (fast-p controller run ledger: child 09 row → LIGHT_VERIFYING with this review base SHA); untracked children/09,10,11 docs = controller evidence. |
|Plan and invariants|PASS|I-1 open-AI GET-only: openAiAnalysisModal (app.js:8499) teardown-first, component flow gated by aiAnalysisMaterialsCapable() (requires ExpertMaterials mount/subscribe/getState/setSelection/requestTransfers); only GET history (`/ai-analysis`) + picker mount issuing GET `/materials`; zero POST/DELETE asserted (test L1024-1043, `server.posts.length===0`). Carry-over: analysis candidates = shared selection ∩ server `analysisSupported` (aiAnalysisSelectionIds app.js); unsupported rows checkbox-disabled with visible reason — 当前不支持图片文字识别 for images / 仅支持 PDF/文本格式分析 (expert-materials.js:657-668 analysisBlockReason; rowSelectableForAnalysis; test L1072). No-selection default: `summary.defaultAnalysisAttachmentIds` (server, complete expert set) applied once per select entry (applyAiAnalysisEntrySelectionIfNeeded); >500 → status 默认材料超过500份，请分批选择, zero auto-check (defaultOverLimit; test L1152, summaryOverride 501 ids, selection.size 0). Freeze: startAiAnalysis freezes `{ids, needed, tokenAtFreeze, fired, phase}` synchronously; test L1179 proves uncheck/flip after submit leaves analysis body [1,2]. I-2: needed = non-STORED subset only → single `requestTransfers(needed)` via shared store submit path (no second download state; run observes store snapshot through subscribe → handleAiMaterialsSnapshot → stepAiAnalysisRunIfReady); exactly one `POST /ai-analysis` when all needed STORED and `intentToken === tokenAtFreeze` (run.fired guard; tests L1225, L1319 assert 1 call, no re-trigger); any FAILED/SOURCE_UNAVAILABLE → phase failed, 0 analysis, error lines `附件 id（fileName）：reason` (aiAnalysisFailureLines; test L1265); retry requests only run.failedIds, re-validates whole frozen run.needed, then one analysis with full frozen ids; duplicate-submit blocked (run existence + disabled CTA + store.submitting; `snapshot.submitting` skip prevents false-fail of in-flight retry); CTA labels M=0 开始分析 / M>0 获取所选文件并分析 (updateAiAnalysisSelectChrome; tests assert both texts + disabled states). I-3: close → teardownAiAnalysisSession destroys intentToken (token+=1), unsubscribes, unmounts picker, clears run (app.js closeAiAnalysisModal; test L1380: close mid-fetch → polls continue, zero later analysis, reopen shows 已存 state + 开始分析, no auto-submit); no server-cancel claims; results re-readable via history GET (reopen results mode; test L1426). Backend (ExpertDocumentAnalysisService.kt analyze L46-67): AFTER extract, BEFORE prompt/LLM/deleteAll — every selected item checked; `!supported` → original AnalysisFailedException `附件 {fileName}(attachmentId={id}) 不支持格式，无法分析`; blank text → `…无可读文字，无法分析`; silent filter removed (`supported = extracted.values.toList()` post-guard only); no silent partial success; deleteAll+LLM unreachable on mixed (throw precedes them). GlobalExceptionHandler.kt:29-30 ANALYSIS_FAILED → 500 mapping untouched (not in diff). extract() (DocumentTextExtractor.kt:31-45) returns every requested id (mapValues over readyFiles; resolveReadyFile throws when not ready) — no silent drop. Result edit/add/re-evaluate/save APIs and AnalysisFieldView schema untouched (only analyze() changed; addField/clearResults/updateField unchanged; test L190 asserts displayOrder continuation + plain delete). History never cleared by frontend before analysis (no DELETE path; tests assert deleteCalls 0). Tests 12/12 incl. app.js source-text S-2 marker assertions (K-dom-stub guard, L1058). S-2 DOM matches authoritative block: section.em-analysis > p.em-analysis-note (exact copy), div[data-material-picker], div.em-analysis-actions > span[role=status] + button.button.primary CTA, footer only 取消 (renderAiAnalysisMaterialsSelect app.js:8799); no new CSS classes, no inline styles; .em-analysis rules pre-exist in expert-materials.css shipped by child 08 (commit 47a72d8). |
|Required commands|PASS|All 5 run fresh from worktree root this session: (1) `node --check app.js && node --check expert-materials.js` → exit 0 (node v25.7.0); (2) `node --test src/test/js/expertMaterialAnalysisFlow.test.js` → tests 12, pass 12, fail 0; (3) `node --test src/test/js/*.test.js` → tests 704, pass 704, fail 0 (baseline 692 + 12 new); (4) `JAVA_HOME=…/zulu-11 mvn test -Dtest=ExpertDocumentAnalysisServiceTest` → BUILD SUCCESS, surefire tests 9, failures 0, errors 0, skipped 0 (+ node exec phase 704 pass); (5) `JAVA_HOME=…/zulu-11 mvn test` → BUILD SUCCESS (03:27), surefire aggregate tests 3251, failures 0, errors 0, skipped 9 (opt-in Flyway/MySQL IT gating; expected), node exec phase 704 pass. |
|Downstream interfaces|PASS|selectionOnly/snapshot/acquisition surface for child 10: ExpertMaterials exports configure/mount(unmountHostsIn)/subscribe/getState/setSelection/requestTransfers (expert-materials.js:1342-1351); normalizeMode accepts inline|drawer|selectionOnly; snapshotOf returns live store view {contactId, items, summary, selection, submitting, actionError, loading, loadedOnce, total, page}; no second download-state copy. index.html unchanged — registers only app.js/styles.css; expert-materials.js/.css still unregistered (child 11 scope); new app.js flow inert until then (capability guard; legacy fallback path verified, test L1469). ExpertDocumentAnalysisService failure contract → AI modal: AnalysisFailedException → GlobalExceptionHandler ANALYSIS_FAILED 500 with per-file message surfaced verbatim in `[data-role=ai-analysis-error]` (fireAiAnalysis catch, app.js). No new result-table fields / DTO changes. .em-analysis CSS rules untouched from 08 (available for child 10 drawer host). |

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1 Boundary includes controller commit 19b01c0 (children/08 verification docs) and a tracked ledger.md modification (fast-p run ledger updated by controller: child 09 row → LIGHT_VERIFYING with review base 47a72d88). Both are controller evidence per fast-p protocol, not product/test changes; the implementation commit 020e839 itself contains exactly the 5 authorized files.
- O-2 Execution report claims full-suite "Tests run: 3218, … Skipped 9"; fresh full `mvn test` aggregates 3251 tests (failures 0, errors 0, skipped 9) plus 704 node tests. Both green; the ~33-test delta is counting granularity (console vs surefire XML aggregation) and has no gate impact.
- O-3 Known boundary disclosed in execution.md deviations (not a gap per report): the shared store polls only rows of visited pages, so a frozen non-STORED item on an unvisited page refreshes to STORED only when its row is browsed or the modal is reopened; server-side ai-analysis is fail-closed for not-ready items (MATERIAL_NOT_READY 409, old results preserved), so no partial success can occur. Consistent with plan acceptance A-1 (same-page set) and A-2 (reopen shows stored) and the 06 API contract; recorded for downstream awareness, no plan conflict.
- O-4 Backend guard-loop `extracted[attachmentId] ?: return@forEach` branch is unreachable in practice (DocumentTextExtractor.extract returns an entry for every requested id by construction); inert, not a violation.

### Required Action
- COMPLETE_CHILD
