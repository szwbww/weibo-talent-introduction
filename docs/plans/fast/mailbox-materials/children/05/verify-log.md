# Child 05 Verify Log

## Light Verification: LIGHT_PASS_WITH_NOTES
Child: 05 and plan path docs/plans/2026-09-07/05-reply-check-progress-and-machine-mail.md
Boundary: c9f80906c872b21d0f4887c7c3f4c95e961b1cef..21dad8bf0573c21eec52cd9783967ae97222ce3d
Verifier: Verify05

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
|Authorized scope|PASS|`git diff-tree --name-only -r c9f80906..21dad8bf` = 4 child-04 docs (committed by 64efdad "docs(fast-p): record 04 light verification", controller evidence) + exactly the 10 brief-authorized product/test files: BatchAutoMailReplyService.kt, MailAutomationController.kt, AutoMailReplyService.kt, ImapMailReceiveService.kt, DmarcAttachmentTransferConsumer.kt (new), MailAttachmentStorageProperties.kt, BatchAutoMailReplyServiceTest.kt, MailAutomationControllerTest.kt, ImapMetadataFetchIT.kt (modify-extend per brief note), DmarcAttachmentTransferConsumerTest.kt (new). `git show --stat 21dad8bf` confirms commit `feat(fast-p): implement 05` touches only those 10 files. HEAD == 21dad8bf0573c21eec52cd9783967ae97222ce3d confirmed; `git status --porcelain` = only `?? docs/plans/fast/mailbox-materials/**` (untracked controller evidence); no tracked modifications. No probe/temp source left in tree. |
|Plan and invariants|PASS|I-1/I-4: BatchAutoMailReplyService.kt:17-21,39-44 (onAccountStarted/onStage optional, default null — scheduler/queue/legacy callers unaffected); :106-131 (CONNECTING published before account work, startedAt fixed per account); :132-179 (work then result; onProgress/accountsPolled only after account completes; per-account try/catch FAILED continues next account); AccountAutoMailReplyPhases (BatchAutoMailReplyService.kt:243-262) restricts phase to CONNECTING/READING_METADATA/PROCESSING_MAIL/COMPLETED/FAILED; MailAutomationController.kt:169-181 publishes only the single added `accountProgress` details-JSON key {accountCode,phase,startedAt,updatedAt} alongside unchanged existing keys (base details keys identical, cf. base file c9f80906:184-196); :199-216 message literal `当前账号：<code> · <activity>；已完成<N>/<M>个账号` (N = runningSuccess+runningFailed, never the just-finished account's position while a later account is active); :197-199 `require(AccountAutoMailReplyPhases.isValid(...))`; terminal update (:317-345) drops accountProgress and reuses unchanged final messages + task terminal enums (COMPLETED/PARTIAL_SUCCESS/FAILED/CANCELLED); token flow (tryStartWithToken/bindExecutionId/isCancelled with executionId-else-pendingToken/update with token) byte-identical to base. Tests: BatchAutoMailReplyServiceTest.kt:371-414 (started-before-work ordering per account), :418-454 (intra-account phase order, stable startedAt, totalAccounts), :457-480 (PARTIAL_SUCCESS retains counts, progress 1/2→2/2); MailAutomationControllerTest.kt:630-707 (LuKai stuck shows `当前账号：LuKai · 读取邮件信息；已完成1/2个账号`, accountsPolled=1, no 正在检查邮箱 wording, COMPLETED/FAILED completion rows, PARTIAL_SUCCESS terminal counts), :754-780+ (all running updates bound to executionId token 1). I-2: ImapMailReceiveService.kt:87-105/116-138 (fetchInboundSince & fetchByUids inside withAccountReceiveWindow); :153-262 (per-account deadline default 120s configurable, watchdog scheduled to hardCloseFolder at expiry, budget.check between candidate list/fetch/each conversion + post-conversion check so a truncated read cannot masquerade as success, finally cancels watchdog and cleans folder/store, timeout path hard-closes); :285-321 hardCloseFolder closes the JavaMail protocol socket via reflection first then public IMAPFolder.forceClose (documented JavaMail 1.6.x limitation: forceClose waits on in-flight literal read), all runCatching with graceful degradation; per-message 60s budget unchanged from child 03 (MailAttachmentStorageProperties.kt:29-30 metadataTotalTimeoutSeconds=60; :31-33 accountReceiveTimeoutSeconds=120; :35-38 dmarc 20MiB/10 members). Cancel only at safe boundaries: AutoMailReplyService.kt:786-792 (per-mail break before processing, never interrupting an in-flight business/SMTP transaction), Batch pollAccounts top-of-loop break (:111-115); controller maps cancel-during-finish to `正在结束当前处理` (:202-216) — no SMTP rollback/cancel path exists (grep of mail/ for smtp-cancel/rollback = only child-02 transfer comments). Protocol evidence: ImapMetadataFetchIT.kt:285-319 (200ms/byte drip > 1s account budget force-closed at ~1s with retryable error), :322-365 (after force-close the next account reads fully on a new connection), :368-396 (healthy account within budget). Receive-phase framing never claims a whole-task SLA (comment AutoMailReplyService.kt:750-756; activity copy is phase-only). I-3: AutoMailReplyService.kt:808-831 (metadata-mode DMARC branch only when attachments non-empty and all content==null; legacy inline ingest kept otherwise); :897-957 queueDmarcTransfers registers purpose=DMARC rows (attachmentId null, inboundProcessingId null, accountCode from server account, requestedBy=SYSTEM via enqueueTransferByIds SYSTEM_REQUESTER — enforced at AttachmentTransferService.kt:196-199,361-364) and throws on any failure so markSeen/cursor never confirm the UID (register is idempotent on source unique key, AttachmentTransferService.kt:87-140); markSeen is called strictly after queueDmarcTransfers returns (:830-832). No wait on the checking thread — worker consumes QUEUED rows asynchronously. DmarcAttachmentTransferConsumer.kt:41-78 (registers purpose=DMARC via 02 seam, bounded extract, parse-null check on EVERY extracted XML BEFORE ingestService — parse-null never silently skips), :84-170 bounded unzip (20MiB total / 10 members, path-traversal and over-limit rejection, streaming, never hands compressed bytes to legacy unbounded parser readBytes), :44-46 empty-archive DMARC_PARSE_FAILED; worker deletes .part after success and on error (AttachmentTransferWorker.kt:319-339), FAILED with errorCode visible/retryable (failAttempt STATE_FAILED + code), DMARC commit is ephemeral — no mail_attachment/expert document write (commitStoredEphemeral :433-449); detector rules untouched; bounce/DSN/self-check routing unchanged (branch order self-check → bounce → DMARC → processSingle at AutoMailReplyService.kt:793-838; fetchUnseenMessages/collectBounces untouched). Tests: DmarcAttachmentTransferConsumerTest.kt (gz/raw/zip multi-member ingest into original dmarc_report via real parser+ingest, report_id dedupe, member-limit/byte-cap LIMIT_EXCEEDED, traversal DMARC_ARCHIVE_INVALID, parse failure DMARC_PARSE_FAILED visible with no save + retryable after fix, corrupted stream visible not silent). |
|Required commands|PASS|Fresh runs (JAVA_HOME=zulu-11, worktree root; each ended in `BUILD SUCCESS`, which Maven prints only on exit 0; raw exit code masked by evidence pipeline, per-class surefire XML read instead): 1) `mvn test -Dtest=BatchAutoMailReplyServiceTest,MailAutomationControllerTest,DmarcAttachmentTransferConsumerTest` → BatchAutoMailReplyServiceTest 21 run/0 err/0 fail, MailAutomationControllerTest 34/0/0, DmarcAttachmentTransferConsumerTest 9/0/0 (target/surefire-reports XML), BUILD SUCCESS (01:16). 2) `mvn -Pmysql-it -Dtest=ImapMetadataFetchIT test` against real MySQL 127.0.0.1:3306 (reachable, 8.0.36; profile maps mysqlIt=true so the @EnabledIfSystemProperty gate is open — Skipped: 0) → 9 run / 0 fail / 0 err, elapsed 6.457s, BUILD SUCCESS; includes slow-drip force-close + next-account protocol tests. 3) `mvn test` full suite incl. exec-plugin Node → surefire aggregate 3168 run / 0 fail / 0 err / 8 skipped (gated migrationIt/mysqlIt IT classes, no gates set in this run), node tests 671 pass / 0 fail, BUILD SUCCESS (02:30). |
|Downstream interfaces|PASS|No migration added: db/migration max stays V120 (child-04 numbering); child-05 diff contains zero db/migration or task/* files (diff --name-only over task + migration dirs = empty); TaskProgressLog model/columns and TaskProgressStore API untouched (TaskProgressLog.kt:7-25, TaskProgressStore.kt update/clearExecutionContext/bindExecutionId signatures unchanged in boundary; TaskProgress.details is `Map<String, Any>?` serialized to detailsJson, so one added nested key is JSON-compatible — CHECK_REPLIES summary extraction reads only successAccountCount/failedAccountCount via objectMapper path, TaskExecutionSummaryExtractor.kt:161-164); frontend popup consumers read accountsPolled/totalAccountsToPoll/message only (app.js:7289) — accountProgress is additive and RUNNING-only, dropped in terminal update. Transfer table V119 untouched; DMARC rows keep master-02 contract fields (purpose=DMARC, attachment_id NULL, requested_by=SYSTEM, state METADATA_ONLY→QUEUED→STORED/FAILED, unique source key, CAS worker_token) — readable by later children 06+ per master contract. AutoMailReplyService constructor unchanged in this child (attachmentTransferService/transactionTemplate already present from child 04 seam; head == base constructor) — consistent with child-04 seams; receiveAndAutoReply gained only defaulted params, so MailQueueConsumer.kt:49, BounceCollectionScheduler.kt:37, MailAutomationScheduler.kt:36 and MailAutomationController legacy endpoints (:70-80) compile/behave unchanged (default null). |

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1, execution.md full-suite aggregate (3189) does not match the fresh verification run (3168 run / 0 fail / 0 err / 8 skipped, node 671 pass / 0 fail). Delta = 21, exactly the size of the removed diagnostic ProbeForceCloseTest.kt class, suggesting the implementer's full run predates probe deletion. Not a four-gate violation (final committed tree is green on a fresh run; count parity is not a plan invariant). Evidence: fresh run console aggregate + surefire XML; execution.md command table.
- O-2, AutoMailReplyServiceTest (not an authorized file) only exercises the legacy content-mode DMARC branch (`dmarc aggregate report is ingested and marked seen…`, AutoMailReplyServiceTest.kt:298-325, 330-360); the new metadata-mode queue branch (content==null → queueDmarcTransfers) has no direct unit test — covered by code evidence (AutoMailReplyService.kt:808-831, 897-957) plus consumer/02-seam tests. Plan's 10-file boundary made adding such a test out of scope; not a gate violation, worth noting for a later integration/manual gate (A-2).
- O-3, command evidence was captured as `BUILD SUCCESS` + per-class surefire XML rather than raw exit codes (output piped for size); Maven prints BUILD SUCCESS only on exit 0, so pass classification is sound, but exact exit-code digits were not recorded in this verification.
- O-4, watchdog force-close uses reflection on com.sun.mail.imap internals (protocol socket) because JavaMail 1.6.x forceClose/disconnect wait for in-flight literal reads (documented in code at ImapMailReceiveService.kt:160-165 and execution.md notes); fallback is the public forceClose and all closing is runCatching. Deviation from pure public-API usage is justified and protocol-IT-proven (1s budget closes in ~1s); reflection brittleness on JavaMail upgrades is a maintenance observation, not a plan violation.

### Required Action
- COMPLETE_CHILD
