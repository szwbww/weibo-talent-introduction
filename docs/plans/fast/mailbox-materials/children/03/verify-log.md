## Light Verification: LIGHT_PASS_WITH_NOTES
Child: 03 and plan path docs/plans/2026-09-07/03-mime-metadata-reader.md
Boundary: 7c2420e80f98775c1fcf4970598617faca378c0b..95d8661ed3d9e0557fe69cce6f6dde24dbea5e79
Verifier: Verify03

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
|Authorized scope|PASS|HEAD == 95d8661ed3d9e0557fe69cce6f6dde24dbea5e79; `git status --porcelain` tracked tree clean (only untracked `docs/plans/fast/mailbox-materials/children/{03..11}/` + `ledger.md` — controller evidence). Range 7c2420e8..95d8661e = 3 commits: dd236c5 docs-only (4 files under children/02, its parent IS the base SHA — child-02 evidence commit), 2b8c3d6 impl (exactly the 8 authorized files, +1465/-88), 95d8661 fix (2 of the same 8: ImapMailReceiveService.kt, ImapMetadataFetchIT.kt). `git diff-tree --name-only -r base head` product/test set == the 8 brief-authorized files exactly; no other product/test file anywhere in range.|
|Plan and invariants|PASS|I-1: metadata content strictly null + full source descriptor — `ImapMailReceiveService.kt:373-394` (registerAttachment: `content = if (metadataOnly) null`; source always populated with accountCode/folder/uidValidity/uid/partPath/messageId/encodedSize/disposition, `MailReceiveService.kt:39-49`); unit `ImapMailReceiveServiceTest.kt:86-116` and IT `ImapMetadataFetchIT.kt:107-125` assert null content + complete source; legacy content non-null and constructor-compatible (trailing defaults `MailReceiveService.kt:30-32,57`; test `ImapMailReceiveServiceTest.kt:118-128`; full-suite compile of all existing consumers green); encodedSize from `part.size` filtered `>=0` else null (`:377-378`), never written as file_size — MailAttachmentService writes `fileSize = content.size` only after null-content throw (`MailAttachmentService.kt:52,97`); ReceivedMail real uidValidity from `uidFolder.uidValidity` (`ImapMailReceiveService.kt:69,106,175-186`) + bodyTruncated default false; metadataOnly property default false (`MailAttachmentStorageProperties.kt:12`). I-2: FetchProfile = ENVELOPE/CONTENT_INFO/UID only, no MESSAGE content (`:62-66,98-102`); whitelist walk — filename/disposition exclusion before body read (`isAttachmentLeaf` `:344-358`; named text/* not body, unit `:156-175` + IT `:172-191`); alternative single / mixed ordered join (`:297,313-317`); DSN bounded read (`:327`); named nested message/rfc822 = one opaque attachment no recursion (unit `:214-241`); unnamed ATTACHMENT registered as 未命名附件-{partPath} (`:366`, unit `:194-213`); unnamed inline signature not material (unit `:176-192`). I-3: 1000-attachment case complete, fixture errors on any attachment section request in metadata mode (`ImapMetadataFetchIT.kt:745-754`) so completion proves zero stream access (`:144-170`); 10001-node cap raises MetadataStructureLimitException (`:284-286`, unit `:273-301`); 2MiB body cap enforced during byte reads with partial `<0.N>` FETCH log evidence + bodyTruncated=true (`readBoundedFromStream` `:425-451`; IT `:218-254`); 60s deadline raises MetadataTimeoutException, re-thrown not swallowed (`:265-268,408-418`, unit `:302-322`; IT `:255-309`); cursor never advances on failure — convertToReceivedMail throws before any InboundFetchResult is constructed (whole-batch abort, `:69-84`); no silent fake-complete for node/deadline limits; content=null consumers throw MetadataContentUnavailableException — no `?: byteArrayOf()` (`MailAttachmentService.kt:38-40,83-86`, `DmarcReportParser.kt:42`, unit `DmarcReportParserTest.kt:152+`); no migration added (V119 max).|
|Required commands|PASS|All run fresh 2026-09-08 on branch HEAD: (1) `JAVA_HOME=…/zulu-11.jdk mvn test -Dtest=ImapMailReceiveServiceTest,DmarcReportParserTest` → EXIT=0 BUILD SUCCESS; surefire 13+4 = 17 tests, 0 failures (reports `…ImapMailReceiveServiceTest.txt`/`…DmarcReportParserTest.txt`). (2) `mvn -Pmysql-it -Dtest=ImapMetadataFetchIT test` → EXIT=0 BUILD SUCCESS; 6/6, 0 failures (report `…ImapMetadataFetchIT.txt`: Tests run 6, 0 F/E, 4.4s). (3) `mvn test` full → EXIT=0 BUILD SUCCESS; aggregate surefire 3151 tests, 0 failures, 0 errors, 8 skipped. Protocol evidence: in-code runtime assertions — no attachment section in command log (`ImapMetadataFetchIT.kt:133-143`), fixture throws on attachment body request in metadata mode (zero-fetch enforced per test), 1000-attachment completeness + throwing stream, partial-range body fetch log in cap case. execution.md does not embed a raw transcript; evidence is executable IT assertions (see O-1).|
|Downstream interfaces|PASS|No migration added — `db/migration` max is V119 (child-02 table); zero migration files in the diff. ImapAttachmentSource (`MailReceiveService.kt:39-49`: accountCode/folder/uidValidity/uid/partPath/messageId/encodedSize/disposition) aligns field-for-field with master 持久化契约 transfer columns (account_code/folder/uid_validity/imap_uid/part_path/message_id/encoded_size/disposition) consumed by 04/05; ReceivedMail.uidValidity real (never guessed 0 in production — only constructor default for legacy callers; sole production constructions at `ImapMailReceiveService.kt:69,106` pass real values) and bodyTruncated field present for 04's BODY_TRUNCATED→MANUAL_REVIEW mapping (04 plan I-4). Child-02 transfer-registration files (AttachmentTransfer*) absent from diff — hooks unchanged. MailAttachmentService write seam legacy-compatible for 04's replacement: both save entry points keep the old write (content bytes → file_size/storagePath) when content non-null and raise the explicit metadata-content-unavailable error only in metadata mode (`MailAttachmentService.kt:33-53,80-99`); only ImapMailReceiveService constructs ReceivedMail in production.|

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1: Evidence-count misquotes in execution.md/fix-log.md: ImapMailReceiveServiceTest has 13 `@Test` (not "17"; "17+4" ⇒ actual 13+4 = 17 total, both classes 0 failures); full suite actual aggregate 3151 tests 0 failures 8 skipped (execution.md quotes 3133). All freshly re-run green, so substance unaffected; raw IMAP command transcript is not embedded in execution.md — protocol evidence lives as IT runtime log assertions + fixture-throw, which I re-executed green (ImapMetadataFetchIT 6/6).
- O-2: Un-guarded fall-through in walk (`ImapMailReceiveService.kt:329-334`): any non-text, non-attachment leaf (nameless `image/png` inline, `text/csv` without filename/disposition, etc.) is read via readBoundedText though the comment claims the branch is "其余 message/*" only and the plan body whitelist covers only text/plain|text/html + DSN + alternative/mixed structure. Base version returned "" without opening the stream (base `extractBody` non-text leaf → ""); new code appends bounded (metadata) or unbounded (legacy) binary to body. Unit "unnamed inline signature" uses a 0-byte part (`ImapMailReceiveServiceTest.kt:183`), so it cannot distinguish stream-excluded from read-empty; no fixture mail exercises a non-empty nameless inline part. "无名内联签名不当专家材料" acceptance is satisfied (nothing registered) and no test fails, so not AUTO_FIX — but flag for a plan decision before 04/05 wiring makes metadata mode operational (metadata mode would issue a bounded content FETCH of such a part; legacy body gains binary garbage vs base).
- O-3: `readBoundedText` catch-all (`:417-420`) swallows non-deadline/non-structure read exceptions into empty body with bodyTruncated=false — a hung body source that stalls past the 10s socket timeout surfaces as silent empty body rather than an explicit retryable error (deadline only enforced between successful reads). Legacy-consistent (base `readPartAsText` catch at base `ImapMailReceiveService.kt:186`) and the required timeout/truncation error paths are all re-thrown and tested; noted as residual silent-failure corner of I-3's 60s budget.
- O-4: IT fixture `attachmentContentRequests` is never appended anywhere (`ImapMetadataFetchIT.kt:317,420`) — `attachmentContentSections().isEmpty()` assertions (:133, :166) are vacuous. Actual zero-fetch enforcement is the fixture `error()` on attachment section requests in metadata mode (:745-754) plus the command-log BODY.PEEK[2]/BODY[2] absence assertion and body-content equality assertions — sound, but the collector list is dead code that could mislead future test authors.

### Required Action
- COMPLETE_CHILD
