## Light Verification: LIGHT_PASS_WITH_NOTES
Child: c1 docs/plans/2026-09-24/emailable-01-runtime-audit.md
Boundary: f64e9f6e9a1e5220d9267d9cde97127ae456df74..0965a037d94e198f6ce8b13900149a09d35683b4
Verifier: C1Verify

### Four Gates

|Gate|Result|Evidence|
|---|---|---|
|Authorized scope|PASS|`git diff --name-status f64e9f6..0965a03` = exactly the brief's 10 authorized files (5 M + 5 A): `campaign/domain/BatchExecutionModels.kt`, `campaign/service/ManualInitialOutreachService.kt`, `campaign/service/BatchEmailVerificationService.kt` (A), `campaign/repository/BatchEmailVerificationRepository.kt` (A), `db/migration/V138__create_batch_email_verification.sql` (A), 5 test files (3 M + 2 A). No extra path, no new file outside the table, no V139. `git show --stat 0965a03` = one commit, only those 10 files, no `docs/plans/**`. Applied migrations V1..V137 untouched (`V137` is the only pre-existing highest; V138 added new). `git status --porcelain` adds only the untracked controller report `children/c1/execution.md` and the controller-owned `ledger.md` modification.|
|Plan and invariants|PASS (notes: O-1/O-2/O-3)|I-1 `BatchExecutionModels.kt:42` (`emailVerificationEnabled: Boolean = false`, trailing default → old JSON = false) + `ManualInitialOutreachService.kt:156-161` (INTRODUCTION require + `requireConfiguredApiKey()` before dispatch) + `:536-543` (context built only when enabled); tests `ManualInitialOutreachServiceTest:5015` `verification disabled never touches the verification service (I-1)`, `:5026` `material reminder with verification enabled is rejected before any business write (I-1)`, `:5048` key gate before any read/write. I-2 `BatchEmailVerificationService.kt:364` normalize (trim+lowercase ROOT, keeps `+tag`/dots), `:234-262` (200 + echoed email match + state), `:479-490` `verifyUri`/`percentEncode`; `ManualInitialOutreachService.kt:843-852` SMTP-pre `normalizeVerificationEmail(mail.to)` assertion → stop; tests `BatchEmailVerificationServiceTest:68,86,129,150,163,508` (state matrix, no-echo=ERROR, `name%2Blab%40example.test` encoding, key only in Bearer). I-3 `ManualInitialOutreachService.kt:683-748` (verification after type/suppression/bound gates, before `selectSendAccount`); test `:5065` `a rejected address is skipped without contact attempt smtp or account selection` (never save contact/attempt, never selectAccount, never send, `verifyNoInteractions(txHelper)`). I-4 `BatchEmailVerificationService.kt:192-232` (249 retried once) / `:214-232` classification / `:352-361` codes; `ManualInitialOutreachService.kt:727-736` ERROR → stop + PARTIAL_SUCCESS/FAILED; `OutcomeAccumulator.annotateTerminalRemaining` (`BatchExecutionModels.kt:261-274`) has no new code → remaining preserved; tests `:179,202,219,258`, `ManualInitialOutreachServiceTest:5093,5122`. I-5 `BatchEmailVerificationService.kt:269-299` (`_mget` via existing `ExpertSearchService.findByDocumentIds` `:850`, real-id/ORCID/current-email triple match, existing `ExpertIndexWriterService.addTag` `:633` which appends without removing tags, MISSING_DOC_ID/FAILED); tests `:358,373,402,419,436`. I-6 repo PENDING insert/conditional `recordDecision`/`markSending` (`BatchEmailVerificationRepository.kt:29,57,79,83,91`) + `EmailVerificationAuditException` boundary `ManualInitialOutreachService.kt:997-1003` ordered before the broad catch `:1005`; tests `:452,467,480` and `ManualInitialOutreachServiceTest:5137,5152,5174,5189,5210`. I-7 `BatchEmailVerificationService.kt:186-192` reuse(requestCount=0), `:417-424` ≥100ms spacing, `:447`; `ManualInitialOutreachService.kt:712-726` skipped+processed+roundRejected+roundSent; `remaining = target-success-failure-skipped` (`BatchExecutionModels.kt:259`); tests `:271,297,309,324,341` and `:5234` (roundSize=5 → 2 sent/3 skipped, exactly 5 `verify` calls, no 6th). I-8 `BatchEmailVerificationService.kt:63-70` (empty/`test_` rejected), `:479-538` (fixed host, Bearer, no redirect, 3s/12s); tests `:497,508`. I-9 `V138__create_batch_email_verification.sql` (18 columns in brief order, unique `(task_execution_id,orcid_id,email)`, index `(task_execution_id,id)`, FK CASCADE) + `BatchEmailVerificationRepository.kt:95-121` (cursor `listAfter`, `aggregate`, `readPage` in one `@Transactional(readOnly=true)`, 50/100, hasMore=limit+1); tests `BatchEmailVerificationRepositoryIT:105,132,163,183,202,218,235,277`, `FlywayMigrationIntegrationTest:632` `V138 creates the batch email verification table with its keys and cascade`.|
|Required commands|PASS|Fresh in this worktree, JDK zulu-11. (1) `mvn test -Dtest=BatchEmailVerificationServiceTest,ManualInitialOutreachServiceTest,BatchSendTaskRuntimeIntegrationTest` → exit 0, `Tests run: 178, Failures: 0, Errors: 0` (25+131+22), BUILD SUCCESS; the test-phase-bound JS suite in the same run reports `tests 1152 / pass 1152 / fail 0` = JS baseline 1152 pass; the brief's targeted Java baseline (279 tests / 0 failures, superset of these classes) is not contradicted. (2) `DOCKER_HOST=unix:///Users/lukai/.orbstack/run/docker.sock mvn test -Dtest=BatchEmailVerificationRepositoryIT,FlywayMigrationIntegrationTest -DmysqlIt=true -DmigrationIt=true -Dapi.version=1.40` → exit 0, `Tests run: 37, Failures: 0, Errors: 0` (Flyway 29 + IT 8), BUILD SUCCESS, log shows `Successfully applied ... now at version v138`. Baseline comparison: `FlywayMigrationIntegrationTest` baseline was 28 tests / 19 failures, every one `expected: <136> but was: <137>` (baseline/mvn-migrationit.txt) — all 19 latest assertions now assert 138 and are green, and the 9 explicit historical targets (116/121/130/131/133/135/23/24/36) are preserved verbatim (`git diff` = 20 × `assertEquals("138"`, 19 × removed `assertEquals("136"`, one new V138 test). No unclassifiable command.|
|Downstream interfaces|PASS|`BatchExecutionSnapshot.emailVerificationEnabled: Boolean = false` `BatchExecutionModels.kt:42` (trailing param, default false). `batch_email_verification` columns = brief list/order exactly 18: id, task_execution_id, expert_doc_id, orcid_id, expert_name, email, decision, provider_state, provider_reason, error_code, request_count, checked_at, send_status, send_reason, tag_status, tag_error, created_at, updated_at (V138:38-56). Repository contract `listAfter(executionId, afterId, limit)` `:95`, `aggregate(executionId)` `:101`, `readPage(executionId, afterId=0, limit=50)` `:110` (readOnly tx, MAX 100, hasMore from limit+1). Value sets exact: decision PENDING/PASS/SKIP/ERROR, send_status NOT_SENT/SENDING/SENT/FAILED/SKIPPED, tag_status NOT_REQUIRED/PENDING/APPLIED/FAILED (`BatchEmailVerificationRepository.kt:324-349`). `EMAIL_VERIFICATION_REJECTED` + label「邮箱验证未通过」`BatchExecutionModels.kt:211,225`; the 7 literal `EMAIL_VERIFY_AUTH_ERROR|NO_CREDITS|RATE_LIMITED|TIMEOUT|INCOMPLETE|BAD_RESPONSE|SERVICE_ERROR` `BatchEmailVerificationRepository.kt:352-360` (no rewrite, no aliasing).|

### AUTO_FIX
- N/A — no four-gate violation was proven; gates 1, 3, 4 are clean and gate 2's deviations (O-1..O-3) are disclosed, additive or test-coverage-shaped, with no plan-unique repair.

### RECORD_ONLY
- O-1: the plan's I-2 address-change termination is implemented (`ManualInitialOutreachService.kt:69,843-852`) but no test asserts it, although acceptance I-2 lists a "compose.to变动测试"; the closest engine test deliberately stubs `compose` to echo the expert address to avoid the branch (`ManualInitialOutreachServiceTest.kt:5246-5249`). Test-coverage gap only, single authorized file — the branch would fail-open to a stop, not to a wrong send.
- O-2: the two verification-gated mid-loop cancel checks added for I-3/I-6 (`ManualInitialOutreachService.kt:687-694` before verification, `:859-866` before SMTP) have no engine-level test with `emailVerificationEnabled=true`; the only coverage is service-level (`BatchEmailVerificationServiceTest:341` cancellation during verification) plus the pre-existing engine cancel test `ManualInitialOutreachServiceTest:501` which runs with verification disabled. Plan acceptance I-3 asks for one cancel test on each side.
- O-3: disclosed deviations in `execution.md` §4, verified as additive/ambiguous: (a) three extra controlled codes `EMAIL_CHANGED`, `EMAIL_VERIFY_AUDIT_FAILED`, `EMAIL_VERIFY_SEND_STATE_CONFLICT` (`ManualInitialOutreachService.kt:63,66,69`) beyond the brief's enumerated send_reason/stopReason sets — additive, all brief-required codes remain literal, `error_code` is still written only from the 7 `EMAIL_VERIFY_*` codes; (b) a thrown `mailDeliveryService.send` leaves the row `SENDING` and the loop continues (`:1005-1013`) instead of stopping the run, so plan T3's「结果不明保持SENDING，停止本次执行」is implemented for the audit-failure case (`:997-1003`) but not for an unknown SMTP delivery; plan text is ambiguous and the implementer's reading is defensible, hence not a proven gate-2 violation — flagged for the human whole-plan review. Also non-behavioral: `FlywayMigrationIntegrationTest` test method renamed to `fresh database migrates through the latest version` (assertion 136→138), and V138 adds no CHECK constraints (DDL follows the plan's column list exactly; value sets are held by the Kotlin constants + tests).

### Required Action
- COMPLETE_CHILD
