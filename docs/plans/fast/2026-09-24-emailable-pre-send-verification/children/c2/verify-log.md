## Light Verification: LIGHT_PASS
Child: c2 docs/plans/2026-09-24/emailable-02-task-config.md
Boundary: fa4db333fc89f6cb22b67e9e251e5d95148b8942..1968f01d0d07afda1f0d2de571cf3dc3bf72e776
Verifier: C2Verify

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
|Authorized scope|PASS|`git diff --name-status fa4db33..1968f01` = exactly 8 files (7 M + 1 A), 1:1 with brief Authorized Files: `campaign/domain/BatchSendTaskConfig.kt`, `campaign/service/BatchSendTaskConfigService.kt`, `campaign/domain/BatchExecutionModels.kt`, `campaign/service/BatchSendControlService.kt`, `A db/migration/V139__add_batch_email_verification_enabled.sql`, `test/.../BatchSendTaskConfigServiceTest.kt`, `test/.../BatchSendControlServiceTest.kt`, `test/.../FlywayMigrationIntegrationTest.kt`. Only migration touched is the added V139; `git diff --name-only ... -- src/main/resources/db/migration` = V139 only (V1..V138 untouched). Boundary is exactly 1 commit (`1968f01 feat(fast-p): implement c2`); no `docs/plans/**`, `static/**`, or `BatchSendScheduler` changes. Highest migration in tree = V139 (`ls \| sort -V \| tail` = V135..V139).|
|Plan and invariants|PASS|I-1: `V139:3-4` `ADD COLUMN email_verification_enabled BOOLEAN NOT NULL DEFAULT FALSE` (no backfill-to-true); entity `BatchSendTaskConfig.kt:40`, View `:74`, Create `:109` all `Boolean = false`; Update `:138` `Boolean? = null`; merge `BatchSendTaskConfigService.kt:107-112` (`cmd.emailVerificationEnabled ?: existing.emailVerificationEnabled`), persisted at `:91`/`:136`. I-2: create `:66/91`, update `:102/136`, setEnabled `:146` (existing.copy at `:160`), softDelete `:170` (existing.copy at `:175`), updateLegacyConfig `:196/226` (explicit `existing.emailVerificationEnabled`), ConfigFields `~:648`, NormalizedConfig `~:673`, three `toFields` (Create `~:696`, Update `~:725` w/ `mergedEmailVerificationEnabled`, entity `~:748`), toView `~:551`, saveConfig passes entity to `repository.save`. I-3: config guard `BatchSendTaskConfigService.kt:369` `require(mailType == INTRODUCTION \|\| !fields.emailVerificationEnabled)` (→ `GlobalExceptionHandler` IllegalArgumentException = 400), direct-manual guard `BatchSendControlService.kt:421` returns explicit 400; INTRODUCTION-only on both save and manual snapshot. I-4: `BatchExecutionModels.kt:373` `emailVerificationEnabled = emailVerificationEnabled` inside `toExecutionSnapshot`; all entry points (`BatchSendControlService.kt:65,106,123,153,257`) go through it; `startManual(request)` uses `request.snapshot` with zero `repository.save`; `BatchSendScheduler` unchanged. Test names: config `create persists emailVerificationEnabled true into entity view and launch snapshot (I-1)`, `create without emailVerificationEnabled defaults to false ... (I-1)`, `update without the field keeps the stored switch on (I-1)`, `update with explicit false turns the stored switch off (I-1)`, `setEnabled false keeps the stored switch and setEnabled true revalidates it (I-2 I-3)`, `setEnabled true rejects a MATERIAL_REMINDER row ... (I-3)`, `softDelete keeps the stored switch value (I-2)`, `updateLegacyConfig preserves existing emailVerificationEnabled entity value (I-1 I-2)`, `create/update rejects emailVerificationEnabled true ... MATERIAL_REMINDER ... (I-3)`, `legacy row without the column reads switch false in view and snapshot (I-1)`; control `startScheduled copies the switch into snapshot and execution request payload (I-1 I-4)`, `startManualFromConfig derives the switch from the persisted config entity (I-1)`, `startManual(request) temporary override never rewrites the source config (I-4)`, `startManual(request) rejects a MATERIAL_REMINDER snapshot with the switch on with 400 (I-3)`, `startManual(request) accepts an INTRODUCTION snapshot with the switch on (I-3)`, `legacy request payload without the flag still reads as false (I-1)`; IT `V139 adds the email verification switch backfilling existing configs with false` (asserts DATA_TYPE=tinyint, IS_NULLABLE=NO, COLUMN_DEFAULT=0, all rows `<> FALSE` count 0, and pre-upgrade fields unchanged).|
|Required commands|PASS|Cmd1 fresh: `mvn test -Dtest=BatchSendTaskConfigServiceTest,BatchSendControlServiceTest,BatchSendTaskRuntimeIntegrationTest,BatchSendSchedulerTest` → BUILD SUCCESS; surefire 85+40+22+5 = **152 tests / 0 failures / 0 errors** (baseline 135/0; +17 new cases). Bundled node suite: **1152 pass / 0 fail** (= baseline). Cmd2 fresh: `DOCKER_HOST=unix:///Users/lukai/.orbstack/run/docker.sock ... -DmigrationIt=true -Dapi.version=1.40` → BUILD SUCCESS; **30 tests / 0 failures / 0 errors** (baseline 28 tests / 19 failures `expected: <136> but was: <137>`; pre-existing red now cleared). V139 IT case executed (time 10.265s, no failure/error node).|
|Downstream interfaces|PASS|`BatchSendTaskConfigView.emailVerificationEnabled: Boolean` at `BatchSendTaskConfig.kt:74`; Create `:109` `Boolean = false`, Update `:138` `Boolean? = null`; no `@JsonProperty`/`@JsonAlias` in file, so JSON wire name is `emailVerificationEnabled`; snapshot propagation via `toExecutionSnapshot` (`BatchExecutionModels.kt:373`) into `BatchExecutionSnapshot.emailVerificationEnabled` (`:42`); manual-snapshot guard in `validateSnapshotFields` (`BatchSendControlService.kt:421`). Matches c3 brief lines 52-60 exactly.|

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1: `validateSnapshotFields` returns HTTP 400 for the new type guard while sibling invalid-field checks in the same method keep 422. This is plan/brief-sanctioned (I-3 and acceptance A-4 mandate 400; consistent with config-path `require` → `GlobalExceptionHandler` 400), and is documented as deviation #1 in the execution report. No action; mixed status codes within one validator is intentional per spec.
- O-2: Execution report cites control-service entry-point line numbers (`:827/:841/:868`) that are actually test-file lines; product entry points are `BatchSendControlService.kt:65,106,123,153,257`. Citation drift only; code and behaviour verified correct.
- O-3: Rejection messages are duplicated literals in service (`:369`) and control (`:421`) with tests asserting substrings; future copy changes must update both sites. Outside the four light gates.

### Required Action
- COMPLETE_CHILD
