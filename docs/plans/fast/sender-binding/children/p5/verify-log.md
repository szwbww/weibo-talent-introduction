# p5 light verification log


## Light Verification: LIGHT_PASS_WITH_NOTES
Child: p5 and plan path — docs/plans/2026-08-10/sender-binding-05-frontend-visibility.md (authoritative, incl. A8/A10 amendments; A11 master M-2 matrix amended in 00-main-plan-sender-binding.md)
Boundary: 4330726e29bb71b438e2b611437e447e7dc223f2..60e8e3c04400643dbd27abc6a826cf20df250d19
Verifier: P5Verifier (fresh, epoch 2 round 1 re-verification; run 2026-08-11)

### Four Gates
| Gate | Result | Evidence |
|---|---|---|
| Authorized scope | PASS | Boundary non-doc files = exactly the 10 authorized files (1-10): ExpertIndexController.kt, ExpertContactManagementController.kt, MailSenderAccountController.kt, MailSenderAccountService.kt, app.js, index.html, styles.css, senderBindingDisplay.test.js (new), MailSenderAccountServiceTest.kt, MailSenderAccountContextTest.kt — `git diff --name-only base..head \| grep -v '^docs/'` lists exactly these. Impl commit ce353c8 touches 9 files (1-9) + knowledge file; fix commit 60e8e3c0 touches ONLY MailSenderAccountContextTest.kt (+4: import + `.withBean(ExpertContactRepository, Supplier { Mockito.mock })`, `git show --stat`). A10/A11 amendments present in plan docs. Docs in boundary (p4 docs from e84aed5, ledger, plan amendments) are plan-process artifacts, not product/test code. |
| Plan and invariants | PASS | I-1: both DTOs carry boundSenderAccountCode/senderAccountChanged (ExpertContactManagementController.kt:442-443, ExpertIndexController.kt:369-370); app.js both maps take values (app.js:4607-4608 DB `c.`, :4658-4659 ES `e.`). I-2: call site uses `contact?.boundSenderAccountCode` / `contact?.senderAccountChanged ?: false` (ExpertIndexController.kt:83-84, contact from contactMap join at :73-78); 0 hits of `expert.boundSenderAccountCode` in src diff; no ExpertProfile change. I-3: listAccounts() calls `service.bindingCountsByAccount()` once, outside `.map {}` (:28-31); toResponse body repo-free (only `effectiveDailyLimitFor`); 7 single-account endpoints (getAccount/createAccount/updateAccount/enableAccount/disableAccount/resetTodaySentCount/resumeAutoSend) each pass `service.bindingCountsByAccount()[code] ?: 0L`. I-4: `const contact = detail.contact` (app.js:6958); card uses contact.boundSenderAccountCode/contact.senderAccountChanged (app.js:7037-7051); both action branches end `await loadContactDetail(id)` then `await loadContacts()` (app.js:8415-8416, 8427-8428); no state.contacts.find in new card/handlers (only pre-existing name-fallback :6960, outside diff). I-5: predicate verbatim `a => a.enabled && a.accountCode !== "SIMULATOR_NOOP"` (app.js:7176). I-6: no try/catch in the two branches; no hardcoded failure text (only plan-mandated empty-selection validation + success msgs); errors ride pre-existing delegation `.catch((error) => showStatus(error.message, "error"))` (app.js:10862/10871/10875/10888/11117). S-1: `.expert-tag.tag-sender-changed` 3-property block verbatim directly after `.expert-tag.tag-discovered` (styles.css:4528-4531); base `.expert-tag`/:4485-4495 and `.tag-discovered` blocks zero diff. S-2: bare `<span>账号：…</span>` no class/style; expert-row-sub condition expanded `\|\| bindingText` (app.js:4755). S-3: two `.metadata-card-value .sender-binding-editor` blocks verbatim after `.metadata-card-value a:hover` (styles.css:1516-1528); `.metadata-card*` existing blocks zero diff; card SVG attrs match app.js:7011-7014 spec. S-4: accounts table 7 th with 绑定专家数 between 当前状态 and 管理操作 (index.html:300); loadAccounts template 7 td incl. `<td>${account.boundExpertCount ?? 0}</td>` (app.js:1691). M-4: `cmp` byte-identical for both tests (base :34-60 vs head :36-62 span). M-6: no migration files in diff. M-8: no ES mapping writes (ExpertProfile untouched), DB tags bug untouched (`val tags` absent from ExpertContactResponse), no 已变更 filter (only display texts app.js:4732/7039/8413). Global: no added `style="` lines (the 管理操作 th is a diff context line); new classes tag-sender-changed/sender-binding-editor both declared in CSS. M-7 knowledge file docs/knowledge/frontend/K-contact-list-dual-path-field-parity.md present with dual-path parity lesson. |
| Required commands | PASS | `node --test src/test/js/senderBindingDisplay.test.js` exit 0, 6 pass/0 fail; `node --test src/test/js/*.test.js` exit 0, 485 pass/0 fail; `node --check src/main/resources/static/app.js` exit 0 no output; `JAVA_HOME=zulu-11 mvn test` exit 0, surefire summed Tests run: 2276 Failures: 0 Errors: 0 Skipped: 4, BUILD SUCCESS; `JAVA_HOME=zulu-11 mvn clean package` exit 0, WAR built (46,051,720 B), 2276/0/0/4; `git diff --check` on full boundary exits 2 with 2 warnings in p4-epoch doc files (docs/plans/fast/sender-binding/children/p4/fix-log.md:2 and verify-log.md:25 "new blank line at EOF", added by e84aed5 inside boundary) — scoped to p5 files (`git diff --check base..head -- src/ docs/knowledge/`) exit 0. |
| Downstream interfaces | N/A | p5 is the last child; no later children consume p5 output. P4 endpoints consumed match the brief: rebind `POST /api/expert-contacts/{id}/sender-account` (app.js:8409) and clear-change-mark `POST /api/expert-contacts/{id}/sender-account/clear-change-mark` (app.js:8421) wired in the two action branches; migrate endpoint intentionally not wired in UI (plan out-of-scope: 批量迁移入口 UI 另做). |

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1: T3.2 dropdown fill uses `(Array.isArray(accounts) ? accounts : []).filter(a => a.enabled && a.accountCode !== "SIMULATOR_NOOP")` (app.js:7176) instead of plan's bare `accounts.filter(...)`. Declared deviation verified as necessary: pre-existing (unauthorized) src/test/js/expertProfileAbsence.test.js stubs `api()` with fallback `return {}` (lines 248-283) and calls `await sandbox.loadContactDetail(1)` (:362); plan's exact snippet would throw `TypeError: accounts.filter is not a function`. I-5 predicate string is byte-verbatim; guard is additive and runtime-neutral for real arrays. Not AUTO_FIX: the plan does not provably require the removal (removing it would break an unauthorized test that may not be edited), and the invariant text is preserved. RECORD_ONLY.
- O-2: `git diff --check` on the full review boundary exits 2 (2× "new blank line at EOF" in docs/plans/fast/sender-binding/children/p4/{fix-log,verify-log}.md, committed by e84aed5 "docs(fast-p): record p4 light verification" — p4-epoch artifacts inside the boundary, not p5-authorized files, not product/test code). p5's own files (src/ + docs/knowledge/) pass `git diff --check` exit 0. This also means the round-1 fix-log's "git diff --check -> exit 0" claim applies only to p5-scoped files. RECORD_ONLY.

### Required Action
- COMPLETE_CHILD
