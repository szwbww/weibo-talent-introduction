# p3 light verification log

## Light Verification: LIGHT_PASS_WITH_NOTES
Child: p3 (sender-binding-03-assignment-stock-balance), plan `docs/plans/2026-08-10/sender-binding-03-assignment-stock-balance.md`
Boundary: 5dc9f95cb4782c68b53dd0ecdbaa89853ecb9a3b..66e19ecf43a5bb44487adea2b9ce687612938d6e
Verifier: P3Verifier

### Four Gates
| Gate | Result | Evidence |
|---|---|---|
| Authorized scope | PASS | p3 commit (f9bfb6f..66e19ec) touches exactly the 7 authorized files (ExpertContactRepository.kt, SenderAccountAssignmentService.kt, InitialOutreachService.kt, ManualInitialOutreachService.kt, SenderAccountAssignmentServiceTest.kt, InitialOutreachServiceTest.kt, ManualInitialOutreachServiceTest.kt) + new knowledge file docs/knowledge/campaign/K-sender-binding-stock-balance.md (git diff --name-status f9bfb6f..66e19ec: 8 files, all in scope). Declared stub-arity deviation verified matcher-count-only: every `selectAccount` Mockito stub in InitialOutreachServiceTest (7 sites) and ManualInitialOutreachServiceTest (27 sites) gained exactly one 4th matcher `, anyValue(SenderBindingStock.EMPTY)`; reconstruction (strip import + 4th matcher + new tests + SAAS constructor wiring) yields byte-identical files vs base for all 3 test files; no assertion/semantics line changed. Boundary also contains p2 docs files (brief/execution/fix-log/verify-log, ledger.md) from the f9bfb6f docs commit — plans/docs, not product/test files. |
| Plan and invariants | PASS | I-1: `loadBindingStock()` at InitialOutreachService.kt:33 (outside forEachIndexed :36) and ManualInitialOutreachService.kt:201/491 (outside while loops); `expertContactRepository.` appears only inside loadBindingStock (SenderAccountAssignmentService.kt:61,63) — not in assignmentScore/selectAccount bodies. I-2: companion consts STOCK_TOTAL_WEIGHT=0.5 / STOCK_SEGMENT_WEIGHT=0.3 (:80-81); share fns return [0,1] ratios (:103-118). I-3: return expression exactly 5 terms matching the I-3 formula (:62-66). I-4: `stock: SenderBindingStock = SenderBindingStock.EMPTY` default (:24, :45); EMPTY → grandTotal=0 → both shares 0.0; existing SenderAccountAssignmentServiceTest cases byte-identical vs base (only 4 constructor-wiring sites + import + 6 new tests; reconstruction IDENTICAL). I-5: both @Query have `IS NOT NULL AND <> '' AND <> 'SIMULATOR_NOOP'` (ExpertContactRepository.kt:81-98); single normalizeKey (:69), distributionKey body = `normalizeKey(expert.country)` (:70). I-6: diff contains no writes to bound_sender_account_code/today_sent_count/auto_send_paused — only read-only SELECT in the two @Query (diff grep: 7 hits, all inside new SELECTs). IP-1: assignments accumulation sites unchanged (IOS base :80/:109 → head :81/:110; MIOS base :354/:777 → head :355/:779 — pure shift from inserted stock lines). M-6: no migration files in diff; no index/`@Table` annotations added. |
| Required commands | PASS | Full `mvn test`: exit 0, BUILD SUCCESS, Tests run: 2264, Failures: 0, Errors: 0, Skipped: 4 (surefire aggregate). Targeted `-Dtest=SenderAccountAssignmentServiceTest,InitialOutreachServiceTest,ManualInitialOutreachServiceTest` (comma form): exit 0, 65/0/0/0. Single method `-Dtest='SenderAccountAssignmentServiceTest#empty stock keeps score identical to legacy behavior'`: exit 0, 1/0/0/0. `mvn clean package`: exit 0, BUILD SUCCESS, 2264/0/0/4. `git diff --check` p3-only (f9bfb6f..66e19ec): exit 0, clean. Boundary-wide `git diff --check`: exit 2 — one finding in p2 docs file (see O-1). All runs fresh under JAVA_HOME=/Library/Java/JavaVirtualMachines/zulu-11.jdk/Contents/Home (OpenJDK 11.0.15 Zulu). |
| Downstream interfaces | PASS | `SenderBindingStock` data class with `totalShare`/`segmentShare`/`grandTotal`/`EMPTY` (SenderAccountAssignmentService.kt:99-122). `selectAccount(expert, currentBatchAssignments = emptyList(), ignoreWarmup = false, stock = SenderBindingStock.EMPTY)` (:18-25). `countBindingsByAccount(): List<AccountBindingCount>` — single GROUP BY on bound_sender_account_code, excludes NULL/''/SIMULATOR_NOOP (ExpertContactRepository.kt:79-90); P5-consumable. `loadBindingStock()` batch-start semantics: called once per batch outside round loops, snapshot of pre-batch bindings, in-batch covered by assignments (IP-1/IP-2). |

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1: Boundary-wide `git diff --check` exits 2: `docs/plans/fast/sender-binding/children/p2/fix-log.md:2: new blank line at EOF`. Finding lives in a p2 docs file added by the f9bfb6f docs commit (inside boundary, not p3's work); the p3 implementation commit alone is clean (exit 0). Docs hygiene only, outside light-gate definition.
- O-2: Brief commit rule says p3 implementation commit = implementation files + execution.md; commit 66e19ec contains the 7 product/test files + knowledge file but NOT execution.md. `docs/plans/fast/sender-binding/children/p3/execution.md` exists untracked in the worktree (created after the commit). Docs-process note; no product/test scope impact.

### Required Action
- COMPLETE_CHILD
