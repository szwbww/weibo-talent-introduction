# p4 light verification log

## Light Verification: LIGHT_PASS_WITH_NOTES
Child: p4 (sender-binding-04-rebind-api-and-audit) — plan docs/plans/2026-08-10/sender-binding-04-rebind-api-and-audit.md (incl. A6), brief docs/plans/fast/sender-binding/children/p4/brief.md
Boundary: 66e19ecf43a5bb44487adea2b9ce687612938d6e..4330726e29bb71b438e2b611437e447e7dc223f2
Verifier: P4Verifier (fresh, read-only; JDK 11 zulu-11)

### Four Gates
| Gate | Result | Evidence |
|---|---|---|
| Authorized scope | PASS | `git diff --name-status 66e19ec..4330726` = 8 authorized files (V86 SQL, ExpertContact.kt, ExpertContactRepository.kt, OperatorActionType.kt, SenderAccountBindingService.kt, ExpertContactManagementController.kt, SenderAccountBindingServiceTest.kt, ExpertContactManagementControllerTest.kt) + 6 harness docs (A6/A7 plan amendments, p3 verification records, ledger) — no product/test file outside the 8. I-7 red line: grep for PendingMailOperationService/AutoMailReplyService/MailRecordRepository/MailboxService over the boundary diff = no matches (exit 1). Controller test diff = exactly the A6 compile fix (import + `senderAccountBindingService = Mockito.mock(...)` at :17, no assertion changes). |
| Plan and invariants | PASS | I-1: ExpertContactRepository.kt rebindSenderAccountById SET includes `sender_account_changed = true` + `sender_account_changed_at`; migrateBindingByAccount SET has NO `sender_account_changed`; clearSenderChangeMarkById SET has NO `bound_sender_account_code`. OperatorActionType.kt:17-19 appends CHANGE_SENDER_ACCOUNT/MIGRATE_SENDER_ACCOUNT/CLEAR_SENDER_CHANGE_MARK at tail, existing 15 untouched. I-2: three `@Modifying @Query` column-level UPDATEs (rebind/migrate/clear); `grep -n "save("` in SenderAccountBindingService.kt = 0 matches. I-3: `requireEnabledTarget` uses `require(...)` for blank/simulator/enabled (400 via GlobalExceptionHandler.kt:14-16 IllegalArgumentException→BAD_REQUEST); tests `rebind rejects disabled target`/`rebind rejects simulator target`. I-4: migrateAccount writes one audit row per contact (`affected.forEach { record(..., expertContactId = c.id, ...) }`); before/after maps contain only `boundSenderAccountCode` (clear uses only `senderAccountChanged`); `boundedNote` truncates at NOTE_MAX=500 + `…(truncated)`; tests `migrate writes one audit row per contact` (3 distinct ids) + `rebind note is truncated at 500 chars`. I-5: rebind `if (old == target.accountCode) return contact` (no write/audit); migrateAccount `require(fromAccountCode != target.accountCode)`; tests present. I-6: migrateBindingByAccount WHERE exactly `bound_sender_account_code = :fromAccountCode`; scope = findAllByBoundSenderAccountCode only; test `migrate scope is source account only`. I-7: activeThreadHint returns note-only hint (terminal set {NEW, MANUAL_HANDOFF} matches ConversationStatus.kt:4,27), no blocking/mutation. M-6: base migration max = V85 (git show base dir), only V86 added, head max = V86, no other migrations. 12/12 planned tests present (SenderAccountBindingServiceTest 22 @Test = 10 baseline + 12 new). |
| Required commands | PASS | All run freshly under JAVA_HOME=zulu-11: (1) `mvn test` exit 0, BUILD SUCCESS, Kotlin surefire aggregate Tests run: 2276, Failures: 0, Errors: 0, Skipped: 4 (2264+12 new, matches expectation), node --test 479 pass/0 fail. (2) `mvn test -Dtest=SenderAccountBindingServiceTest,ExpertContactManagementControllerTest` (comma syntax) exit 0, 27/0/0/0, BUILD SUCCESS. (3) `mvn test -Dtest='SenderAccountBindingServiceTest#migrate does not touch change mark'` exit 0, 1/0/0/0, BUILD SUCCESS. (4) `mvn clean package` exit 0, BUILD SUCCESS, target/weibo-talent-introduction-1.0.0-SNAPSHOT.war built. (5) `git diff --check` full boundary exit 2 with exactly one finding: docs/plans/fast/sender-binding/children/p3/fix-log.md:2 new blank line at EOF — introduced by p3 docs commit 02f2a0f (predates p4 implementation); restricted to src/main+src/test: exit 0; `git diff --check 02f2a0f..4330726` (p4 commit): exit 0 → p4 changes whitespace-clean. |
| Downstream interfaces | PASS | Endpoints: ExpertContactManagementController.kt:169 `POST /{contactId}/sender-account`, :176 `POST /{contactId}/sender-account/clear-change-mark`, :200 `POST /sender-account/migrate`. Types: SenderAccountBindingService.kt tail — `RebindCommand(senderAccountCode, operatorName?, note?)`, `MigrateCommand(fromAccountCode, toAccountCode, operatorName?, reason?)`, `MigrateResult(migrated, fromAccountCode, toAccountCode)`. Columns: V86__add_expert_contact_sender_change_mark.sql (sender_account_changed TINYINT(1) NOT NULL DEFAULT 0, sender_account_changed_at DATETIME NULL) + ExpertContact.kt:27-28 fields. Action types: OperatorActionType.kt:17-19. rebind/clearChangeMark return re-fetched ExpertContact (`findById` after write) mapped via existing `toResponse` (no DTO field additions). |

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1: full-boundary `git diff --check` exits 2 solely on `docs/plans/fast/sender-binding/children/p3/fix-log.md:2` (new blank line at EOF, introduced by p3 docs commit 02f2a0f before p4's implementation). Not an authorized p4 file, not product/test code; p4's own commit and all src/main+src/test pass `git diff --check` (exit 0) — outside the light gate.
- O-2: boundary also contains harness doc changes (A6/A7 plan amendments, p3 verification records, ledger.md) — fast-p bookkeeping, not product/test files, so not part of the 8-file scope gate.

### Required Action
- COMPLETE_CHILD

