## Light Verification: LIGHT_PASS_WITH_NOTES
Child: 03 — docs/plans/2026-08-28/03-expert-types-required.md (amended by HUMAN-approved A1, ledger row A1)
Boundary: 658b60c25370bd8dd974e6a98d6eacc48315943b..bc8a93762cca39c2542d79d1f3801589b6e4e155
Verifier: Verify03Light

### Four Gates

| Gate | Result | Evidence |
|---|---|---|
| 1. Authorized scope | PASS (1 note) | Implementation commit `bc8a937` (`git show --stat`) touches exactly the 12 authorized files (7 plan + 5 A1). Boundary range 658b60c..bc8a937 additionally contains 6 fast-p docs files (`docs/plans/fast/single-gate/children/02/{brief,execution,fix-log,verify-log}.md`, `children/03/brief.md`, `ledger.md`) from orchestration commit `229feeb` "record 02 light verification" — pure fast-p bookkeeping (base-SHA fills, ledger status/evidence rows), zero product code; see RECORD_ONLY O-1. `styles.css`, `index.html`, `ExpertClassificationService.kt` zero-diff (absent from `git diff --name-only`). No gates deleted: `expertSendableFilter` (ExpertSearchService.kt:55, applied ManualInitialOutreachService.kt:1328), in-memory check `BatchExecutionModels.kt:85` (`mailType == INTRODUCTION && expertTypes.isNotEmpty()`), `expertTypesFilter` empty→null contract (ExpertSearchService.kt:124) all present, files untouched. Read-side empty=unrestricted unchanged (`BatchExecutionModels.kt` not in diff; replaced test preserves read-side assertion: stored `'[]'` row still views as empty list). |
| 2. Plan and invariants | PASS | I3-1: both new checks gated on `mailType == BatchSendType.INTRODUCTION.name`; MATERIAL_REMINDER empty still saves (config test 2) and launches (control test 5). I3-2: both write entrances — `BatchSendTaskConfigService.normalizeAndValidate` (after `resolveMailType`, before return) and `BatchSendControlService.validateSnapshotFields` (after regions loop, same `IllegalArgumentException`→422 channel); `grep -rln "研发类型至少选择一个" src/main/kotlin` → exactly the 2 intended files. I3-3: V109 `SET expert_types_json = '["PRODUCTION_RND","ACADEMIC_RND","HYBRID_RND"]'` verbatim; `ExpertClassification.SENDABLE_TYPES` (expert/domain/ExpertClassification.kt:41-45) = enum first three, manually cross-checked match. I3-4: `WHERE expert_types_json IS NULL OR expert_types_json = '' OR expert_types_json = '[]'` exact. I3-5: V109 contains zero `${` (grep count 0; test asserts). I3-6: frontend defaults exactly the three literals, matching `batchExpertTypeOptions()` (app.js:14534) first three values verbatim (PRODUCTION_RND/ACADEMIC_RND/HYBRID_RND); no six-value list anywhere in diff. S3-1: message verbatim `请至少选择一个研发类型` via `showStatus(…, "error")`, added in the same numeric-check paragraph of `saveBatchConfigEditor`; app.js diff has no `style=`, no new class, no new DOM node; styles.css/index.html zero-diff. Fixture edits mechanical: all additions are post-V109 defaults (`expertTypesJson`/`expertTypes` three types, picker stubs `readBatchMultiPickerValue: () => [three types]`); only assertion value updated is `batchExpertTypeFilter.test.js` captured payload `[]` → three types (exact-equality retained, reflects new production default — not weakened/removed); one config-service test replaced (I2-3 empty-persists → I3-1 rejection; write-side assertion superseded by new contract, read-side assertion preserved); no production behavior change outside plan scope. |
| 3. Required commands | PASS | Freshly run, JDK 11 (zulu-11.0.15): (a) `mvn test -Dtest='BatchSendTaskConfigServiceTest,BatchSendControlServiceTest,V109ExpertTypesMigrationTest'` → BUILD SUCCESS, surefire 64+34+3 = 101 tests, 0 failures/0 errors; (b) `node --check src/main/resources/static/app.js` → OK, exit 0; (c) `node --test src/test/js/*.test.js` → 755 tests, 755 pass, 0 fail, exit 0; (d) full `mvn test` → `Tests run: 2974, Failures: 0, Errors: 0, Skipped: 5`, BUILD SUCCESS (baseline de228e1 = 2952; after 01+02 = 2967; +7 net new = 2974 — one test replaced, not added); (e) `git diff --check` (worktree + cached) → clean, no output. No pre-existing failures; all negative-path ERROR log lines are expected test-induced logs. |
| 4. Downstream interfaces (child 04) | PASS | V109 is the next free migration version (dir max before it = V108; V109 unique, no V110). After V109 + new write gates, every stored INTRODUCTION config is non-empty (V109 fills all NULL/''/'[]' rows; saves reject empty; snapshots don't persist). Read-side unchanged: `RecipientScope.expertTypes` empty = unrestricted still holds (BatchExecutionModels.kt untouched, comment intact, test asserts stored `'[]'` views as empty). `expertTypesFilter`/`expertTypePredicate` untouched (ExpertSearchService.kt not in diff). No sendable/version gate touched (`expertSendableFilter`, in-memory checks, version gate all present). |

### AUTO_FIX
N/A — no four-gate violation proven; nothing to auto-fix.

### RECORD_ONLY
- O-1: Review boundary 658b60c..bc8a937 spans two commits: orchestration docs commit `229feeb` ("docs(fast-p): record 02 light verification", 6 fast-p files) + implementation commit `bc8a937`. `git diff --name-only` on the full range therefore yields 18 files (12 authorized product files + 6 fast-p docs), not 12. The implementation commit itself touches exactly the 12 authorized files; the 6 extra are fast-p orchestration records (child-02 verification docs, brief base-SHA fills, ledger status/evidence rows), no product/test/git-binary impact. Controller boundary bookkeeping only.

### Required Action
- COMPLETE_CHILD
