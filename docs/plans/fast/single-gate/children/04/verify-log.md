## Light Verification: LIGHT_PASS_WITH_NOTES
Child: 04 — `docs/plans/2026-08-28/04-single-gate-remove-sendable.md` (amended by HUMAN-approved A2: authorized list = 8 original + ExpertClassificationServiceTest.kt I5a2-10 deletion + OperatorStatusWriteSeamGuardTest.kt NoiseSite 545->498)
Boundary: bc8a93762cca39c2542d79d1f3801589b6e4e155..960fbe48e0b1ad7edd3f2ca68eccd29adafa654b (implementation 742d1a2 + fix round 1 960fbe4)
Verifier: Verify04Light

### Four Gates

| Gate | Result | Evidence |
|---|---|---|
| 1. Authorized scope | PASS | `git diff --name-only bc8a937..960fbe4` filtered to `src/` = exactly the 10 authorized files (ExpertSearchService.kt, ManualInitialOutreachService.kt, BatchExecutionModels.kt, ExpertClassificationService.kt, ExpertSearchServiceTest.kt, ManualInitialOutreachServiceTest.kt, ExpertClassificationVersionGateGuardTest.kt, BatchSendTaskRuntimeIntegrationTest.kt, ExpertClassificationServiceTest.kt, OperatorStatusWriteSeamGuardTest.kt). Child-05 files (ExpertClassification.kt, ExpertIndexWriterService.kt, ExpertClassificationBackfillService.kt, ExpertIndexController.kt) zero-diff. Zero frontend files. `expertTypesFilter`/`expertTypePredicate` bodies verbatim (no hunks touch them; current state matches plan contract: empty→null, should+minimum_should_match:1; UNCLASSIFIED→must_not exists). Fix round 742d1a2..960fbe4 touches only the 2 A2 files (+ docs/plan amendment + ledger, controller bookkeeping). |
| 2. Plan and invariants | PASS | **I4-1**: `expertSendableFilter()` and `searchSendableExpertsWithEmail` deleted from ExpertSearchService.kt (diff); `buildEsFiltersForLevel` INTRODUCTION branch = `ExpertSearchService.expertTypesFilter(scope.expertTypes) ?: ExpertSearchService.MATCH_NONE_FILTER`; send-before gate (`ManualInitialOutreachService.kt:601-`) = `if (!scope.matchesExpertType(expert))` recording EXPERT_NOT_SENDABLE; `matchesExpert` = `if (mailType == INTRODUCTION && !matchesExpertType(profile)) return false`. **I4-2**: `MATCH_NONE_FILTER` present with exact shape `mapOf("bool" to mapOf("must_not" to listOf(mapOf("match_all" to emptyMap<String, Any>()))))` (production + literal-structure test); empty expertTypes → ES appends MATCH_NONE_FILTER (test `empty expertTypes appends MATCH_NONE_FILTER on CANDIDATE (I4-2)`), memory returns false for every profile (test `matchesExpertType semantics per profile including UNCLASSIFIED and empty (I4-2 I4-5)`); legacy `runScheduledBatch` entry (config-derived snapshot, no expertTypes) proven fail-closed by new test `runScheduledBatch legacy config without expertTypes sends nobody (I4-2)`. **I4-3**: `expertTypesFilter`/`expertTypePredicate` bodies unchanged; fail-closed expressed only at the call site; list-page caller (`ExpertSearchService.kt:1093` `expertTypesFilter(expertTypes)?.let { filters.add(it) }`) keeps empty=unrestricted. **I4-4**: single Kotlin implementation `RecipientScope.matchesExpertType(profile)` (`BatchExecutionModels.kt:122-131`) shared by `matchesExpert` and ManualInitialOutreachService's send-before gate; no second copy added. **I4-5**: `BatchSendTaskRuntimeIntegrationTest` `matchesExpertType agrees with expertTypesFilter predicate semantics per profile (I4-5)` evaluates ES `bool.should` structure per profile incl. `expertClassification = null` (UNCLASSIFIED ↔ field absent). **I4-6**: `ExpertClassificationService.kt` diff = only `ACCEPTED_CLASSIFICATION_VERSIONS` + doc comment deleted (7 lines); `const val VERSION = "rnd-v2-2026"` preserved verbatim. Guard test has TWO cases (line 57 version-comparison whitelist empty; line 98 `sendable reads appear only in the child-05 whitelist (I4-1)` = exactly ExpertClassification.kt / ExpertIndexWriterService.kt / ExpertClassificationBackfillService.kt / ExpertIndexController.kt). EXPERT_NOT_SENDABLE: `const val EXPERT_NOT_SENDABLE = "EXPERT_NOT_SENDABLE"` unchanged (line 167); label `:179` = 研发类型不在本次选择范围内; test assertion synced (`ManualInitialOutreachServiceTest` label assert = 研发类型不在本次选择范围内). |
| 3. Required commands | PASS | All run fresh with JAVA_HOME=zulu-11: (a) `mvn test -Dtest='ExpertSearchServiceTest,ManualInitialOutreachServiceTest,BatchSendTaskRuntimeIntegrationTest,ExpertClassificationVersionGateGuardTest'` → BUILD SUCCESS, 193 Kotlin tests (69+100+22+2), 0 fail; (b) `mvn test -Dtest=ExpertClassificationServiceTest` → BUILD SUCCESS, 29/29, 0 fail; (c) full `mvn test` → BUILD SUCCESS, 2972 Kotlin run, 0 failures, 0 errors, 5 skipped (all in unrelated environmental classes: OperatorActionLogRepositoryTest, AuthFlowIntegrationTest, FlywayMigrationIntegrationTest, EuropePmcDataSourceTest, InboundMailProcessingRepositoryTest — untouched by children 01-04), 755 JS pass; net -2 vs post-01-03 baseline 2974 exactly matches child-04 mandated deletions (ExpertSearchServiceTest -2, ExpertClassificationServiceTest -1, ManualInitialOutreachServiceTest net -1, BatchSendTaskRuntimeIntegrationTest +1, Guard +1); (d) `git diff --check` clean on range and worktree; (e) `grep -rn "expertSendableFilter\|ACCEPTED_CLASSIFICATION_VERSIONS" src/main/kotlin` → zero output; (f) `grep -rn "searchSendableExpertsWithEmail" src/main src/test` → zero output. |
| 4. Downstream interfaces (child 05 contract) | PASS | `grep -rn "sendable" src/main/kotlin` filtered to `classification.sendable` reads: exactly ExpertClassification.kt:36 (derived property `val sendable: Boolean`), ExpertIndexWriterService.kt:355 (serialization `put("sendable", classification.sendable)`), ExpertClassificationBackfillService.kt:274 (stats `if (classification.sendable) sendable++`), ExpertIndexController.kt:454 (API DTO `expertSendable = expert.expertClassification?.sendable`). ExpertSearchService.kt / BatchExecutionModels.kt / ManualInitialOutreachService.kt absent from classification.sendable reads (their `sendable` hits are account-sendability locals `runRoundGate`/`sendableTargets` and comments, not classification reads). List-page query filter unchanged (empty = unrestricted via unchanged `expertTypesFilter` returning null; caller :1093 untouched). |

### AUTO_FIX
N/A — no four-gate violation proven; no correction required.

### RECORD_ONLY
1. Plan I4-4 acceptance grep `UNCLASSIFIED") typeName == null` hits 2 places, not 1: BatchExecutionModels.kt:129 (child-04 `matchesExpertType`) plus InitialOutreachService.kt:54 — a pre-existing child-02 inline predicate in the legacy first-launch path (`searchExpertsByTypesWithEmail`, I2-5), zero-diff in this range and explicitly out of child-04 scope per plan 现状审计 #4. I4-4's actual rule (single impl shared by `matchesExpert` + ManualInitialOutreachService send-before gate) holds; the plan's "exactly 1" grep criterion was imprecise. No action.
2. Review range includes fast-p bookkeeping docs commits (plan amendment A2 44c3d65, pause record 89ae836, brief/ledger updates) — non-`src/`, excluded by the gate filter; not product changes. No action.
3. ManualInitialOutreachServiceTest.kt diff (494 lines) exceeds the plan's Task-5 items 4-8 in volume because ~30 legacy-entry tests were mechanically migrated from `runScheduledBatch`/`runBulkOutreach`/`countPending` to the modern `service.run(runScheduledSnapshot(), …)`/`countBySnapshot` entry, with fixtures carrying `expertTypes` — a direct consequence of the mandated M-2 semantic flip (config-derived snapshots without expertTypes are now fail-closed and would vacuously pass). All within the authorized file; no test intent weakened (assertions on sent/skipped counts preserved; a new test explicitly pins the legacy entry's fail-closed behavior). No action.

### Required Action
- COMPLETE_CHILD
