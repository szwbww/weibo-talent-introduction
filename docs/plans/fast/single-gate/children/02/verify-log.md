## Light Verification: LIGHT_PASS_WITH_NOTES
Child: 02 — docs/plans/2026-08-28/02-legacy-outreach-explicit-types.md (plan SHA-256 fc4a622d874924bcc1f1d118dfcea237923aa55eda5fed3cf0cbedd747c8f858, matches execution.md)
Boundary: cec6ce15ba3b41a6bf76e70eae503cdc5a925560..658b60c25370bd8dd974e6a98d6eacc48315943b
Verifier: Verify02Light

### Four Gates

| Gate | Result | Evidence |
|---|---|---|
| 1. Authorized scope | PASS | Child 02 impl commit (427222f..658b60c) = exactly the 7 authorized files (MailSchedulingProperties.kt, application.yml x2, ExpertSearchService.kt, InitialOutreachService.kt, ExpertSearchServiceTest.kt, InitialOutreachServiceTest.kt; +384/−32). No frontend files, no ManualInitialOutreachService, expertTypesFilter/expertTypePredicate untouched (ExpertSearchService diff is 42 insertions, 0 deletions). Boundary cec6ce1..658b60c additionally contains 8 fast-p harness docs (docs/plans/fast/single-gate/**) committed by the preceding 01-verification record commit 427222f — not child 02 work (see RECORD_ONLY). |
| 2. Plan and invariants | PASS | I2-1: `MailSchedulingProperties.kt:22` → `val initialOutreachExpertTypes: List<String> = emptyList()`; both yml files → `initial-outreach-expert-types: ${MAIL_SCHEDULING_INITIAL_OUTREACH_EXPERT_TYPES:}` (nothing after `:`); no hardcoded type fallback in any layer; no multi-value type literal arrays in diff. I2-2: `sendInitialBatch` head `require(types.isNotEmpty())` message `未配置 talent-introduction.scheduling.initial-outreach-expert-types，旧首发链路拒绝执行` before target fetch; test asserts IAE + message contains `initial-outreach-expert-types` + search never called. I2-3: `types.forEach { require(it in ExpertSearchService.ALLOWED_EXPERT_TYPES) }`; ALLOWED_EXPERT_TYPES = ExpertType.values() + "UNCLASSIFIED" (line 115-116) — no hand-written six-name list. I2-4: new method filter exactly `[exists email, expertTypesFilter(types)]` (ExpertSearchService.kt:1192-1217); no sendable/version/discipline conditions; test asserts exactly 2 filter items and absence of `sendable`/`version`. I2-5: gate `typeName = expert.expertClassification?.type?.name; types.any { if (it == "UNCLASSIFIED") typeName == null else typeName == it }` — UNCLASSIFIED = null classification/type, same set as query; tests cover OUT_OF_SCOPE skip/send and null-classification UNCLASSIFIED-only send. New method signature `(Int, ExpertIndexLevel, List<String>)` confirmed at 1192. |
| 3. Required commands | PASS | Fresh run: `mvn test -Dtest='ExpertSearchServiceTest,InitialOutreachServiceTest'` → ExpertSearchServiceTest 71/0/0, InitialOutreachServiceTest 18/0/0, JS 755 pass/0 fail, BUILD SUCCESS (exit 0). `node --check src/main/resources/static/app.js` → OK. `git diff --check` (working tree + boundary) → clean. Full `JAVA_HOME=…zulu-11… mvn test` → `Tests run: 2967, Failures: 0, Errors: 0, Skipped: 5`, BUILD SUCCESS (exit 0). Counts consistent: baseline de228e1 2952 → +8 net child 01 (2960) → +7 new tests (3 ExpertSearchServiceTest + 4 InitialOutreachServiceTest) = 2967; no pre-existing failures. |
| 4. Downstream interfaces | PASS | `grep -rn "searchSendableExpertsWithEmail" src/main` → only KDoc mention (line 52) + own definition (line 409), zero production call sites. `grep -rn "expertSendableFilter" …/InitialOutreachService.kt` → zero hits. `expertSendableFilter()` still exists (line 55); remaining callers only ManualInitialOutreachService (console) + its own method. MailAutomationController/MailQueueConsumer/MailAutomationScheduler unchanged (empty diff base..head); `sendInitialBatch(campaignId, size, taskExecutionId=null)` signature stable. |

### AUTO_FIX (or N/A)
N/A — no four-gate violation proven.

### RECORD_ONLY (or N/A)
1. Review boundary cec6ce1..658b60c includes 8 fast-p harness docs (children/01 brief/execution/fix-log/verify-log, children/02 brief, children/03 brief, human-review-handoff, ledger) added by commit 427222f "docs(fast-p): record 01 light verification", which sits inside the boundary but predates child 02 execution. Child 02's own commit is exactly the 7 authorized files. Brief working-note "fast-p docs should stay untracked" was not honored for 427222f; flag for ledger/process awareness, not a child 02 scope violation.
2. Execution deviations (all documented in execution.md, all within authorized files): new method placed at end of class (after aggregateEmailDomains, line 1192) instead of adjacent to searchSendableExpertsWithEmail — verified necessary: OperatorStatusWriteSeamGuardTest line-pin at ExpertSearchService.kt:545 is byte-identical base vs head; guard test green in full run. Gate test I5a2-9 refixtured from stale-version to OUT_OF_SCOPE (I2-5 removes version criterion; old fixture would now send, contradicting new contract). Stub count 14 not plan's 15 (grep-verified 14 pre-existing stubs + 5 new). Unused ExpertClassificationService import removed cleanly (no remaining references; compiles).
3. Unstaged working-tree edits to brief.md + ledger.md and untracked execution.md — fast-p bookkeeping outside the boundary, expected.

### Required Action
- COMPLETE_CHILD
