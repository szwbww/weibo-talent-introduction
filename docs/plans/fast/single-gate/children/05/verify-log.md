## Light Verification: LIGHT_PASS
Child: 05 — docs/plans/2026-08-28/05-sendable-vocabulary-cleanup.md (amended A3+A4+A5, 13 authorized files)
Boundary: 960fbe48e0b1ad7edd3f2ca68eccd29adafa654b..4636727749202052c6affd2550e5353139fcb4a1
Verifier: Verify05Light

### Four Gates

| Gate | Result | Evidence |
|---|---|---|
| 1. Authorized scope | PASS | `git diff --name-only base..head` filtered to `src/` = exactly the 13 authorized files (4 main: ExpertIndexController.kt, ExpertClassification.kt, ExpertClassificationBackfillService.kt, ExpertIndexWriterService.kt; 9 test: ExpertClassificationAdminControllerTest.kt, ExpertClassificationBackfillServiceTest.kt, ExpertClassificationSchedulerTest.kt, ExpertClassificationServiceTest.kt, ExpertClassificationVersionGateGuardTest.kt, ExpertIndexWriterServiceTest.kt, ExpertSearchServiceTest.kt, ManualInitialOutreachServiceTest.kt, OperatorStatusWriteSeamGuardTest.kt). Non-src boundary files = 6 docs only (A4/A5 amendment + pause-epoch process commits). I5-4: ExpertSearchService.kt zero-diff. I5-3: `src/main/resources/es/*.json` zero-diff. No frontend/static files. ExpertType.kt zero-diff (enum unchanged). Working tree has no src modifications (only fast-p docs). |
| 2. Plan and invariants | PASS | Task 1: ExpertClassification.kt — `sendable` derived property, `SENDABLE_TYPES` constant, and doc-comment paragraphs deleted; ExpertType enum untouched. Task 2: ExpertIndexWriterService.kt diff = single line `put("sendable", classification.sendable)` deleted; other classificationNode keys untouched. Task 3 (I5-1/I5-2): BackfillCounters `byType: MutableMap<String, Long> = linkedMapOf()`; `record()` uses `byType.merge(classification.type.name, 1L)` (keys from `classification.type.name`, no hardcoded type-name list); progress message `"已扫描 …，<TYPE> <n>，…"` per-type via joinToString (no 可发信/不可发信); statsMap `"byType" to LinkedHashMap(byType)`; result field `byType: Map<String, Long>`; `sendable`/`notSendable`/`classifiedByType` gone. Task 4: ExpertIndexController.kt — exactly 2 lines deleted (`val expertSendable: Boolean? = null` + `expertSendable = expert.expertClassification?.sendable`). Task 5: ServiceTest — sendable assertions replaced with direct `type` assertions, I1-1 rewritten as type-retention + serialization-no-sendable tests; no classify() inputs changed (M-3); WriterTest asserts no `sendable` key, sizes 9→8; BackfillServiceTest/AdminControllerTest/SchedulerTest assert `byType`. A3: ExpertSearchServiceTest — exactly the two I1-5 tests deleted in one contiguous hunk (~:1870), nothing else. A4: ManualInitialOutreachServiceTest — exactly 3 `ExpertClassification.SENDABLE_TYPES` refs in `classification(type)` fixture helper replaced with `setOf(ExpertType.PRODUCTION_RND, ExpertType.ACADEMIC_RND, ExpertType.HYBRID_RND)` (6 lines, nothing else). A5: OperatorStatusWriteSeamGuardTest — NoiseSite pin 436→435, same anchor text, nothing else. M-1: `ALLOWED_SENDABLE_SITES = emptySet()`. |
| 3. Required commands | PASS | Fresh runs on JDK 11 (zulu-11): (a) targeted 8-class set `mvn test -Dtest='ExpertClassificationServiceTest,ExpertIndexWriterServiceTest,ExpertClassificationBackfillServiceTest,ExpertClassificationAdminControllerTest,ExpertClassificationSchedulerTest,ExpertClassificationVersionGateGuardTest,ManualInitialOutreachServiceTest,ExpertSearchServiceTest'` — all 8 classes green (28+30+15+8+6+2+100+67 = 256 tests, Failures 0, Errors 0, Skipped 0); (b) full `mvn test` — `Tests run: 2969, Failures: 0, Errors: 0, Skipped: 5`, BUILD SUCCESS, exit 0 (2969 = baseline 2972 Kotlin − 2 deleted I1-5 tests − 1 net I1-1 rewrite 3→2 tests; matches implementer report exactly); JS suite 755/755 pass in same lifecycle; (c) `node --check src/main/resources/static/app.js` OK; (d) `git diff --check` clean (boundary and working tree). Machine criteria: `grep -rn "notSendable\|可发信\|不可发信" src/main/kotlin` → zero output; `grep -rn "SENDABLE_TYPES" src/main/kotlin src/test/kotlin` → 1 hit, comment only (V109ExpertTypesMigrationTest.kt:18, I5-5 exclusion); `[Cc]lassification(\?)?\.sendable|sendable\s*:\s*Boolean` in src/main/kotlin → zero; guard test with empty whitelist passed in both targeted and full runs. |
| 4. Downstream interfaces | PASS | Final child — no downstream. M-1 terminal criterion proven: `ALLOWED_SENDABLE_SITES = emptySet()` and ExpertClassificationVersionGateGuardTest passes (2/2) in targeted and full suite — whole-repo machine proof no `classification.sendable` reads remain. Remaining `sendable` mentions in src/main/kotlin are all I5-5 fixed exclusions (sender-account semantics in ManualInitialOutreachService.kt, KDoc in ExpertClassificationService.kt/ExpertSearchService.kt, mapping-error string in BackfillService.kt:93 describing the retained orphan field). |

### AUTO_FIX
N/A

### RECORD_ONLY
N/A

### Required Action
- COMPLETE_CHILD
