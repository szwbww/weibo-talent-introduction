## Light Verification: LIGHT_FAIL
Child: c1 docs/plans/2026-09-23/01-batch-sender-filter-backend.md
Boundary: a58ce98..c3f694f
Verifier: C1Verifier

### Four Gates
| Gate | Result | Evidence |
|---|---|---|
| Authorized scope | PASS | `git diff --name-status a58ce98..c3f694f` = exactly 10 files (6 main Kotlin, 3 test Kotlin, `A src/main/resources/db/migration/V135__add_batch_sender_account_codes.sql`); 1:1 with the plan `## 变更文件清单`; single new file is the authorized migration; `git ls-tree c3f694f` shows no `docs/plans/**` and no other new path. Brief item-1 text still reads `V134__...` but the same brief's header, amended plan A5 (`ledger.md ## Amendments`) and the committed filename all read `V135`, so the committed migration is the authorized one. |
| Plan and invariants | PASS | I-1: V135 = ADD TEXT NULL → `UPDATE ... '[]'` → MODIFY NOT NULL; single strict parse `parseSenderAccountCodes` (readTree + isArray + isTextual, throws `IllegalStateException`, empty/absent text → `[]`) used by `toExecutionSnapshot` and `toView`; no `inbound_mailbox_code`/owner reference anywhere in the diff; tests `create persists senderAccountCodes and get returns them in order (I-1)`, `invalid senderAccountCodesJson is rejected on read and snapshot instead of degrading to unrestricted (I-1)`, Flyway `V135 adds the sender account codes column backfilling existing rows with an empty array`. I-2: `normalizeAndValidate` trims/dedupes/rejects comma+`SIMULATOR_NOOP`/requires existence (BatchSendTaskConfigService.kt:327-350); `validateSnapshotFields` same rule → 422 (BatchSendControlService.kt:434-445 inside try, mapped at :452-455); every per-round gate filtered by `allowedAccountCodes` (`listSendableWithin` :1019-1021, both `runRoundGate` overloads, `classifyNoSendableOutcome` :909-912, `hasWarmupLimitedAccounts` :964-966) and round quota sums only the filtered `sendable` (:237-238, :579-580); `selectSendAccount` :1030-1037 calls the 4-arg overload for `[]` and the 5-arg overload otherwise, and the 5-arg `selectAccount` adds `allowedAccountCodes.isEmpty() \|\| it.accountCode in allowedAccountCodes` above the existing enabled/warmup/quota/paused predicates; tests `run sends only from the selected sender account and never falls back to an unselected one (I-2)`, `run stops without falling back when the selected account has no capacity (I-2)`, `run with empty senderAccountCodes keeps the legacy selection path (I-2)`, `material reminder sends only from the selected sender account (I-2)` (asserts the real `ManualMailSendCommand.senderAccountCode`). I-3: construction-side exclusion via `boundOrcidsOf` in `buildRetryableTargets` (:1078-1091) and `boundOrcids` in `buildMaterialReminderSnapshotFromScope` (:1307-1316) over `findByOrcidIdIn` (all campaigns); pre-send re-read in both loops (`existingContact?.boundSenderAccountCode != null \|\| hasBoundContact(normOrcid)` :637-646, `contact.boundSenderAccountCode != null \|\| hasBoundContact(normOrcid)` :279-287) before `selectSendAccount`/`bindIfAbsent`/`save`/SMTP; skip records `BOUND_SENDER_ALREADY_SET` and advances processed/roundProcessed/roundRejected without `roundSent`; tests verify 0 SMTP, no `updateBindingById`, no `save`, and cross-campaign binding. I-4: `countBySnapshot` (:450-467) reuses both target constructors; tests assert preview==execution (1/1 and 0/0) and ES-side `skippedReasons["BOUND_SENDER_ALREADY_SET"]={label:"专家已绑定发件账号",count:1}`, `failed==0`; `countExperts` estimate口径 unchanged. I-5: only logical `account_code` flows through config/snapshot/selection/binding/log; `SIMULATOR_NOOP` rejected in both write paths; LuKai/LuKai_QF asserted as two independent codes. MAIN M-2/M-3/M-4 map onto I-1/I-3/I-2+I-4 and hold; M-5 satisfied (V135 used, no V134 in this branch); M-1 is a release-ordering rule (c2 still PENDING), nothing for c1 to violate. |
| Required commands | FAIL | Fresh, sequential, zulu-11: (1) `mvn -B -Dtest=BatchSendTaskConfigServiceTest,BatchSendControlServiceTest,ManualInitialOutreachServiceTest test` → exit 0, `Tests run: 227` (74+34+119), 0 failures/errors (baseline `baseline/mvn.txt`: exit 0, 208) — reproduces the implementer's numbers. (2) `DOCKER_HOST=unix:///Users/lukai/.orbstack/run/docker.sock mvn -B -Dtest=FlywayMigrationIntegrationTest -DmigrationIt=true -Dapi.version=1.40 test` → exit 1, `Tests run: 27, Failures: 1, Errors: 1` (baseline: exit 1, 26 tests, 17 failures, all `expected: <131> but was: <133>`); the 17 baseline reds are gone and the new V135 test passes. Both remaining reds adjudicated in O-1 (pre-existing latent expectations, no plan-determined repair). **Gate fails at the reviewed boundary**: the commit under review is not the state that was run — `git show c3f694f:src/test/kotlin/com/weibo/talentintroduction/campaign/repository/FlywayMigrationIntegrationTest.kt` still contains 18× `assertEquals("134", ...targetSchemaVersion)` while the commit's own migration set max is `V135__add_batch_sender_account_codes.sql`; Flyway's `targetSchemaVersion` is the highest applied version (proved by the baseline `expected: <131> but was: <133>` when max was V133, and by my run logging `now at version v135`), so the committed tree fails ≥18 of its 27 Flyway assertions (`expected: <134> but was: <135>`). The green-for-baseline evidence exists only as an unstaged worktree edit: `git status` shows ` M ...FlywayMigrationIntegrationTest.kt`, `git diff` = 18× `"134"`→`"135"` plus the V134→V135 name/comment, file mtime 20:03:49 vs commit time 20:27:52 (partial staging; `git reflog` has no amend). |
| Downstream interfaces | PASS | `senderAccountCodes` present on `BatchSendTaskConfigCreateCommand` (:90), `BatchSendTaskConfigUpdateCommand` (:114) and `BatchSendTaskConfigView` (:60); persisted by `normalizeAndValidate` and echoed by `list`/`get` via `toView` (BatchSendTaskConfigService.kt:526), with `BatchSendConfigController` `GET /configs`, `POST /configs`, `GET /configs/{id}`, `PUT /configs/{id}` binding those DTOs unchanged. `BatchExecutionSnapshot.senderAccountCodes` (BatchExecutionModels.kt:25-29) filled by `toExecutionSnapshot` (:339,:354) for the config-driven start path; `POST /api/mail/batch-send/recipients/preview` binds the snapshot directly (BatchSendConfigController.kt:97-99) and `POST /api/mail/batch-send/manual-executions` binds `ManualBatchExecutionRequest.snapshot` (:105-107). Unknown manual code → 422 via `validateSnapshotFields` catch (BatchSendControlService.kt:452-455), asserted by `startManual rejects an unknown senderAccountCode with 422 before launching (I-2)` (also asserts no progressStore/executor interaction); a valid code passes to the capacity gate (409), asserted by `startManual accepts a snapshot whose senderAccountCodes exist (I-2)`. `updateLegacyConfig` passes `parseSenderAccountCodes(existing.senderAccountCodesJson)` (BatchSendTaskConfigService.kt:207-211) with test `updateLegacyConfig preserves existing senderAccountCodesJson entity value (I-1)`. `BatchOutcomeReasonCodes.BOUND_SENDER_ALREADY_SET` + label `专家已绑定发件账号` (BatchExecutionModels.kt:192-212) visible in `skippedReasons` per the ES-skip test. |

### AUTO_FIX
- F-1 (governing requirement: child plan 阶段 1 "Flyway 测试中普通最新版本断言…改到 `135`" + brief "Flyway「最新版本」断言目标 = `135`", ledger A5): commit `c3f694f` adds `V135__add_batch_sender_account_codes.sql` yet the same commit's `FlywayMigrationIntegrationTest.kt` still asserts `"134"` in 18 places (`git show c3f694f:<file> | grep -c 'assertEquals("134"'` = 18; migration set max = V135), so the required Flyway command fails at the reviewed commit with ~18 `expected: <134> but was: <135>` while the implementer's verified run is reproducible only from the unstaged worktree edit (`git diff` = those 18 values plus the `V134`→`V135` test name/comment). Smallest authorized correction: include that already-present worktree change to `src/test/kotlin/com/weibo/talentintroduction/campaign/repository/FlywayMigrationIntegrationTest.kt` (18× `"134"` → `"135"`, test name/comment `V134`→`V135`) in the child's implementation commit — no other file, no product change, no assertion target other than the latest-version ones.

### RECORD_ONLY
- O-1: the two remaining Flyway reds are pre-existing latent expectations exposed by the plan-mandated 131→135 assertion bump, not caused by this child's product change, and A5 limits the child's test-expectation change to the latest-version target, so no repair is plan-determined. V124 `:654`→`execute:1637` `SQLIntegrityConstraintViolationException` FK `fk_eap_contact`: the test `flyway.clean()`s then inserts `expert_application_promotion(expert_contact_id=1,…)` and `grep -rn "INSERT INTO expert_contact" src/main/resources/db/migration/` has no hits (nothing seeds that row); baseline failed this test earlier at the version assertion, so the INSERT never ran. V131 `:447` `assertEquals(historyBefore + 1, …)` → `expected: <130> but was: <133>`: a V130→latest upgrade now adds V131/V132/V133/V135 (4 history rows), and this is not a "latest version" assertion, so A5 keeps it. Both need human authority (seed the fixture / relax the count), and any such edit is outside the child's unique repair.
- O-2: within I-3 the construction-side filter uses `!isNullOrBlank()` while the pre-send guard uses `!= null`, so a dirty whitespace-only `bound_sender_account_code` would be counted by `countBySnapshot` and then skipped at send time (I-4 preview/execution parity). V85 forbids blank binding values and the plan declares such dirty values out of scope; no legitimate-data path is affected.
- O-3: the legacy KV start paths do not carry the whitelist — `BatchSendControlService.launchLegacyKv`→`toLegacySnapshot` and `ManualInitialOutreachService.toSnapshot` are built from `BatchSendSettingService.getConfig()` (`batch_send_setting`), so a whitelist written through the legacy typed API (`updateLegacyConfig` → `batch_send_task_config`) does not constrain a legacy KV-driven start. Outside the plan's declared scope (config row + manual snapshot paths only).
- O-4: `BatchSendTaskConfigService`'s new `mailSenderAccountService: MailSenderAccountService? = null` skips existence validation when null (`if (known != null)`); production is safe (both classes are `@Service`, the only main-source construction site is the declaration, so Spring injects the bean), but the shape is fail-open for any future non-Spring wiring.

### Required Action
- AUTO_FIX

## Light Verification: LIGHT_PASS_WITH_NOTES
Child: c1 docs/plans/2026-09-23/01-batch-sender-filter-backend.md
Boundary: a58ce98..248c30a
Verifier: C1ReVerifier (epoch 1, attempt 2, re-verification after fix round 1)

### Four Gates
| Gate | Result | Evidence |
|---|---|---|
| Authorized scope | PASS | `git diff --name-status a58ce98..248c30a` = exactly 10 paths, 9 M + 1 A: main Kotlin BatchExecutionModels.kt, BatchSendTaskConfig.kt, BatchSendControlService.kt, BatchSendTaskConfigService.kt, ManualInitialOutreachService.kt, SenderAccountAssignmentService.kt; `A src/main/resources/db/migration/V135__add_batch_sender_account_codes.sql`; test Kotlin FlywayMigrationIntegrationTest.kt, BatchSendTaskConfigServiceTest.kt, ManualInitialOutreachServiceTest.kt — 1:1 with the plan `## 变更文件清单`, the only new path is the authorized migration. Fix commit 248c30a changes exactly 1 file (`git show --name-only --format= 248c30a` = FlywayMigrationIntegrationTest.kt, 21 insertions / 21 deletions). `git status --porcelain` = only `?? docs/plans/fast/2026-09-23-batch-sender-filter-main/`; no modified/untracked product or test path outside `docs/plans/fast/**`; HEAD = 248c30add5c01958c2f85dfe11692f02635c7a22 on `fast/2026-09-23-batch-sender-filter-main`. Migration numbering: worktree holds V130/V131/V132/V133/V135 (V134 gap explicitly allowed by ledger A4–A6 and plan A5); brief item-1's literal `V134__...` string is the pre-amendment text, while the brief header, amended plan and committed filename all read V135, so the committed migration is the authorized one. |
| Plan and invariants | PASS | I-1: V135 (read in full) = `ADD COLUMN sender_account_codes_json TEXT NULL` → `UPDATE ... SET '[]' WHERE ... IS NULL` → `MODIFY ... TEXT NOT NULL`; the single strict parse point `parseSenderAccountCodes` (BatchExecutionModels.kt:370-389) throws `IllegalStateException` on malformed JSON / non-array / non-string and yields `[]` only for empty-or-absent text, used by `toExecutionSnapshot` (:339) and `toView` (BatchSendTaskConfigService.kt:529); the only `inbound_mailbox_code` hits in the boundary diff are two explanatory comments (no alias merge). F-1 target: `git show 248c30a:<file> \| grep -c '"135"'` = 18, `grep -c '"134"'` = 0, `grep -c '"131"'` = 0; the fix diff's only non-`assertEquals` changes are `fresh database migrates through V134`→`V135`, `V134 adds the sender account codes column...`→`V135 ...` and one comment line; fixed setup targets are untouched (the `fromVersion("...")` multiset is identical between c3f694f and 248c30a, e.g. `fromVersion("133")` and `fromVersion("130")`). I-2: `normalizeAndValidate` trims/dedupes/rejects comma + `SIMULATOR_NOOP` + unknown code (BatchSendTaskConfigService.kt:330-348); runtime restriction holds in every round gate/self-check/quota/selection site (ManualInitialOutreachService.kt:178, :226-228, :511, :568-570, :909-912, :964-966, :990-1015, :1019-1021, :1033-1037) and `SenderAccountAssignmentService.selectAccount(..., allowedAccountCodes)` :33-46 adds `allowedAccountCodes.isEmpty() \|\| it.accountCode in allowedAccountCodes` above the existing enabled/warmup/quota/paused predicates, with the old 4-arg overload (:22) preserved so `[]` keeps the legacy path. I-3: construction-side `boundOrcidsOf` (:1055-1060) filters NEW retryables (:1082) and material targets (material builder `if (normOrcid in boundOrcids) continue`); pre-send re-read `hasBoundContact` (:1047-1049) guards both loops (:279 material, :637 ES page) before selection/bind/SMTP and records `BOUND_SENDER_ALREADY_SET` (:281, :639) without incrementing sent. I-4: `countBySnapshot` (:450-467) calls the same `buildRetryableTargets` / `buildMaterialReminderSnapshot` constructors used by execution (:512, :180), so preview and execution share the binding filter; the ES estimate stays `countExperts`. I-5: every candidate, binding and log key is the logical `accountCode` (selection filter, `bindIfAbsent(picked.accountCode, ...)`), never `inbound_mailbox_code`/owner. Committed tests carrying the invariant names include `create persists senderAccountCodes and get returns them in order (I-1)`, `invalid senderAccountCodesJson is rejected on read and snapshot instead of degrading to unrestricted (I-1)`, `create rejects an unknown senderAccountCode (I-2)`, `create rejects the simulator account in senderAccountCodes (I-1 I-5)`, `updateLegacyConfig preserves existing senderAccountCodesJson entity value (I-1)`, `V135 adds the sender account codes column backfilling existing rows with an empty array`, `startManual rejects an unknown senderAccountCode with 422 before launching (I-2)`, `run sends only from the selected sender account and never falls back to an unselected one (I-2)`, `run with empty senderAccountCodes keeps the legacy selection path (I-2)`, `run excludes a bound NEW retryable contact from preview and execution (I-3 I-4)`, `run skips an ES target bound in another campaign with the bound reason (I-3 I-4)`, `material reminder with every target bound previews and sends zero (I-3 I-4)`. |
| Required commands | PASS | Fresh, sequential, zulu-11, at HEAD 248c30a. (1) `mvn -B -Dtest=BatchSendTaskConfigServiceTest,BatchSendControlServiceTest,ManualInitialOutreachServiceTest test` → exit 0, `Tests run: 227, Failures: 0, Errors: 0, Skipped: 0` (74 + 34 + 119), BUILD SUCCESS; baseline `baseline/mvn.txt` = exit 0 / 208 (64+34+110), so +19 new passing tests and no new failure; the same phase ran the JS suite (tests 1121 / pass 1121 / fail 0, identical to baseline/js.txt). (2) `DOCKER_HOST=unix:///Users/lukai/.orbstack/run/docker.sock mvn -B -Dtest=FlywayMigrationIntegrationTest -DmigrationIt=true -Dapi.version=1.40 test` → exit 1, `Tests run: 27, Failures: 1, Errors: 1, Skipped: 0` (baseline: exit 1 / 26 tests / 17 failures, all `expected: <131> but was: <133>`). F-1 is closed: `expected: <134>` occurs 0 times in the fresh output, and the surefire XML (`target/surefire-reports/TEST-...FlywayMigrationIntegrationTest.xml`, tests=27 errors=1 failures=1) shows `<testcase name="V135 adds the sender account codes column backfilling existing rows with an empty array" .../>` and `<testcase name="fresh database migrates through V135" .../>` as self-closing (passed), i.e. 25/27 pass and every scenario migrates to the new max (`Successfully applied 110 migrations ... now at version v135`). The 2 remaining reds are exactly the pre-existing latent ones adjudicated in O-1: V124 → ERROR `java.sql.SQLIntegrityConstraintViolationException` FK `fk_eap_contact` at FlywayMigrationIntegrationTest.kt:654→execute:1637 (the test `flyway.clean()`s then inserts `expert_application_promotion(expert_contact_id=1, ...)` and `grep -rln "INSERT INTO expert_contact" src/main/resources/db/migration/` = no hits), and V131 → FAILURE `expected: <130> but was: <133>` at :447 (`assertEquals(historyBefore + 1, ...)` vs the V131/V132/V133/V135 history rows). Both are migration-history/fixture expectations unrelated to this child's product change (in baseline both tests failed earlier at the version assertion, so these lines never ran), and A5 limits the child's expectation change to the latest-version target → RECORD_ONLY, not repaired. |
| Downstream interfaces | PASS | `senderAccountCodes` is present and identically named on `BatchSendTaskConfigCreateCommand` (BatchSendTaskConfig.kt:91), `BatchSendTaskConfigUpdateCommand` (:115) and `BatchSendTaskConfigView` (:61); persisted through `normalizeAndValidate` (`senderAccountCodesJson` :87, :124, :381) and echoed by `list`/`get` via `toView` (:529), with `BatchSendConfigController` binding those DTOs on `GET /configs` (:52), `POST /configs` (:56), `GET /configs/{id}` (:60), `PUT /configs/{id}` (:64). Launch snapshot: `BatchExecutionSnapshot.senderAccountCodes` (BatchExecutionModels.kt:28) filled by `toExecutionSnapshot` (:339, :354); `POST /api/mail/batch-send/manual-executions` (:105-107) binds `ManualBatchExecutionRequest.snapshot` (:39-42) → `startManual` → `validateSnapshotFields` (BatchSendControlService.kt:83-84, :418-455), which trims/dedupes and requires each code to exist and not be the simulator, mapping an unknown code to HTTP 422 `UNPROCESSABLE_ENTITY` (:437-455) instead of treating it as `[]`; the estimate endpoint `POST /api/mail/batch-send/recipients/preview` (:97-99) binds the snapshot directly, so it carries `senderAccountCodes`. Legacy typed API: `updateLegacyConfig` passes `parseSenderAccountCodes(objectMapper, existing.senderAccountCodesJson)` explicitly (BatchSendTaskConfigService.kt:210-211), so an existing whitelist survives. Skip reason: `BatchOutcomeReasonCodes.BOUND_SENDER_ALREADY_SET = "BOUND_SENDER_ALREADY_SET"` (BatchExecutionModels.kt:199) with label `专家已绑定发件账号` (:212), recorded into skip statistics (ManualInitialOutreachService.kt:281, :639). |

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1: the two remaining Flyway reds are pre-existing latent expectations exposed by the plan-mandated 131→135 assertion bump, not caused by this child's product change, and A5 limits the child's test-expectation change to the latest-version target, so no repair is plan-determined. V124 `:654`→`execute:1637` `SQLIntegrityConstraintViolationException` FK `fk_eap_contact`: the test `flyway.clean()`s then inserts `expert_application_promotion(expert_contact_id=1,…)` and `grep -rn "INSERT INTO expert_contact" src/main/resources/db/migration/` has no hits (nothing seeds that row); baseline failed this test earlier at the version assertion, so the INSERT never ran. V131 `:447` `assertEquals(historyBefore + 1, …)` → `expected: <130> but was: <133>`: a V130→latest upgrade now adds V131/V132/V133/V135 (4 history rows), and this is not a "latest version" assertion, so A5 keeps it. Both need human authority (seed the fixture / relax the count), and any such edit is outside the child's unique repair.
- O-2: within I-3 the construction-side filter uses `!isNullOrBlank()` while the pre-send guard uses `!= null`, so a dirty whitespace-only `bound_sender_account_code` would be counted by `countBySnapshot` and then skipped at send time (I-4 preview/execution parity). V85 forbids blank binding values and the plan declares such dirty values out of scope; no legitimate-data path is affected.
- O-3: the legacy KV start paths do not carry the whitelist — `BatchSendControlService.launchLegacyKv`→`toLegacySnapshot` and `ManualInitialOutreachService.toSnapshot` are built from `BatchSendSettingService.getConfig()` (`batch_send_setting`), so a whitelist written through the legacy typed API (`updateLegacyConfig` → `batch_send_task_config`) does not constrain a legacy KV-driven start. Outside the plan's declared scope (config row + manual snapshot paths only).
- O-4: `BatchSendTaskConfigService`'s new `mailSenderAccountService: MailSenderAccountService? = null` skips existence validation when null (`if (known != null)`); production is safe (both classes are `@Service`, the only main-source construction site is the declaration, so Spring injects the bean), but the shape is fail-open for any future non-Spring wiring.

### Required Action
- COMPLETE_CHILD
