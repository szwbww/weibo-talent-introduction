## Light Verification: LIGHT_PASS_WITH_NOTES
Child: c2 docs/plans/2026-09-23/02-batch-sender-filter-frontend.md
Boundary: 248c30a..75cc1714
Verifier: C2Verifier

### Four Gates
| Gate | Result | Evidence |
|---|---|---|
| Authorized scope | PASS | Implementation commit `75cc1714` (`git diff --name-status 47505d3..75cc1714`) is exactly `M src/main/resources/static/app.js`, `M src/main/resources/static/index.html`, `A src/test/js/batchSenderFilter.test.js`. `git diff --stat 248c30a..75cc1714 -- src/main/resources/static/styles.css` → 0 lines (zero diff). The literal `git diff --name-status 248c30a..75cc1714` additionally lists 8 `docs/plans/fast/2026-09-23-batch-sender-filter-main/**` evidence files, all added by the separate orchestrator commit `47505d3 docs(fast-p): record c1 light verification` (no product code); the product delta inside the boundary is exactly the 3 authorized files. New DOM: `git diff … -- index.html | grep -c '^+.*style='` → 0 inline styles; added `class=` values are only the 10 pre-existing picker/diff classes (`batch-config-field`, `batch-config-field-label`, `batch-tag-picker{,-control,-chips,-search,-chevron,-dropdown}`, `batch-config-diff-badge`, `batch-config-diff-original`). No other file created/modified; `docs/plans/fast/**` untouched by `75cc1714` (`children/c2/execution.md` and `ledger.md` remain uncommitted working-tree changes). |
| Plan and invariants | PASS | I-1: `batchSenderAccountOptions()` reads only `accountCode`/`senderEmail`/`enabled` (no `inboundMailboxCode`/`imapUsername`), `value = accountCode`, `label = senderEmail · accountCode`, `enabled === false` → appends `（已停用，本次不会发信）`; unknown/historical codes survive via the untouched `renderBatchMultiPicker` fallback `{value,label:value}` and the preload failure branch never wipes `preloadedSenderAccounts`. I-2: `senderAccountCodes` present in `buildConfigEditorRecipientSnapshot`, `saveBatchConfigEditor` (POST/PUT payload), `deepCloneConfig` (`.slice()` copy), `readManualFormValues`, `buildManualExecutionSnapshot`; `fillManualFormDefaults` = `[]`; `computeManualDiffs`/`computeAndRenderDiffs`/`clearAllDiffMarkers` map to `manualFieldSenderAccounts` with the existing badge/original nodes. I-3/S-1/S-2: both plan DOM blocks are byte-verbatim (whitespace-normalized) substrings of the real `index.html`; both old sub-texts removed and replaced with the plan text under unchanged parent+class; `data-tag-picker` appears exactly once each; registry has both keys and `bindBatchSendTaskEvents` binds both; `index.html` holds exactly 11 `?v=` keys, all `20260923-batch-sender-filter` (old key 0 hits in static and 0 hits in `src/test`); `batchSenderFilter.test.js` reads the real `index.html` via `fs.readFileSync` and asserts on that source, not only on DOM stubs. |
| Required commands | PASS | Fresh runs: `node --check src/main/resources/static/app.js` → exit 0; `node --test src/test/js/batchSenderFilter.test.js src/test/js/batchSendTaskConsoleInteraction.test.js` → exit 0, 97 tests / 97 pass / 0 fail (new file alone: 16 tests / 5 suites / 16 pass); `node --test src/test/js/*.test.js` → exit 0, 1137 tests / 227 suites / 1137 pass / 0 fail. Baseline `baseline/js.txt`: `node --check` exit 0, 1121 tests / 222 suites / 1121 pass / 0 fail. Delta +16 tests / +5 suites = exactly the new file; 0 new failures, 0 pre-existing failures. |
| Downstream interfaces | PASS | c1 Kotlin DTOs: `BatchSendTaskConfigCreateCommand.senderAccountCodes: List<String>`, `BatchSendTaskConfigUpdateCommand.senderAccountCodes: List<String>` (`BatchSendTaskConfig.kt:76-95`), `BatchExecutionSnapshot.senderAccountCodes: List<String> = emptyList()` (`BatchExecutionModels.kt:28`), consumed by `BatchSendTaskConfigService` (`fields.senderAccountCodes…trim/filter/distinct`, `:330-348`) and `ManualInitialOutreachService`/`BatchSendControlService`. `BatchSendConfigController` accepts `POST/PUT /configs` as Create/UpdateCommand, `POST /recipients/preview` as the raw `BatchExecutionSnapshot`, `POST /manual-executions` as `ManualBatchExecutionRequest{snapshot}`. `app.js` sends the identically named ordered array in all five paths: config POST/PUT body, editor estimate snapshot (posted verbatim to `/recipients/preview`), and `{sourceConfigId, sourceUpdatedAt, snapshot}` for `/manual-executions`; empty selection sends `[]` (verified by test). Field name and array shape match c1 exactly. |

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1: `notifyBatchMultiPickerChanged` historically hard-coded the manual-draft key `emailDomains` for every `previewKind:"manual"` picker, so `batchManualExpertTypes`/`batchManualOperatorStatuses` also write `manualDraft.emailDomains`; c2 changed the expression to `meta.draftKey || "emailDomains"`, keeping all 6 pre-existing pickers byte-identical, so the latent pre-existing mis-keying is unchanged and out of this child's four gates.
- O-2: `readBatchMultiPickerValue` does not itself `distinct()`; ordered/deduped semantics are by construction (toggle-based UI cannot produce duplicates) plus the backend's `.trim().filter{}.distinct()`. No reachable duplicate path found, so this is a note on the plan's "有序去重" wording rather than a proven four-gate defect.
- O-3: the boundary command `git diff --name-status 248c30a..75cc1714` literally includes 8 docs-only fast-p evidence files (baseline/*.txt, children/c1/*, children/c2/brief.md, ledger.md) added by the orchestrator evidence commit `47505d3`, not by c2's `75cc1714`; recorded only because a literal reading of gate 1 could look like extra scope.

### Required Action
- COMPLETE_CHILD
