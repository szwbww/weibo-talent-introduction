## Light Verification: LIGHT_PASS_WITH_NOTES
Child: c2 — docs/plans/2026-08-26/02-unrecognized-asks-and-orphan-keys.md (identity commit:ee0749d3beedea7e26f4bf4e097b3d33a1684b7d, SHA-256 0f81dd9033236fca5f616e80bed9597faea9b67fbc10c39f638847804cd3f1e2)
Boundary: de5e130a84fba33296ea906734a1c7f071e3383a..f6dc048359b0d7f46b335f640d78033fa7747a27
Verifier: C2Verifier

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
|Authorized scope|PASS|Commit f6dc048 (single `feat(fast-p): implement c2`) changes exactly the 5 authorized files (`git show --name-only f6dc048`): QaFactSelectionService.kt, QaCoverageKeyCatalog.kt, AiReplyIntentCatalog.kt, QaCoverageKeyIntentParityTest.kt, QaFactSelectionUnrecognizedStatusTest.kt. No Flyway migration, no other product file. The interleaved docs commits 46232e4/beae04c in the range are controller evidence (ledger/c1 verify-log), not product. Worktree dirty state = controller-owned only (`M ledger.md`, untracked `children/c2/`, `children/c3/`); no product files dirty.|
|Plan and invariants|PASS|T1.3/I-1/I-3: `QaFactSelectionService.kt:609-620` — ONE composed status expression (single hunk, 15+/2-): 01's lift first (`naturalStatus==UNSUPPORTED && factRuleIds.isNotEmpty() -> PARTIAL`), then 02's cap (`askEnumeration.available && unrecognizedAsks.isNotEmpty() && naturalStatus==GROUNDED -> PARTIAL`), else `naturalStatus`; comment `:607-614` carries both plan numbers (计划 01 (I-6) + 计划 02 (I-1/I-3)). available=false short-circuits the cap (I-3). I-1: `QaFactSelectionUnrecognizedStatusTest` — `unrecognized ask caps grounded status to partial…` asserts PARTIAL + 1 unrecognizedAsk; `cap never downgrades partial status` asserts PARTIAL unchanged. I-3: `unavailable enumerator never caps status` asserts GROUNDED with available=false (verbatim pre-plan). I-2: `intents = intentCoverages` at `:626` byte-identical to base de5e130:621; `AiReplyIntentCatalog.kt` diff = 1 hunk adding only `alternativeCoverageKeys = listOf("application.required_materials")` to `application.next_stages` (:282-283; key/title/requestAliases untouched); `matchIntentsWithSpans` reads only `requestAliases` (:386) so matching set/requestKey unchanged. I-4: `QaCoverageKeyCatalog.kt:112-117` — `work.time_commitment` + `work.advisory_duration` appended AFTER `governance.sponsor_level` at list END with the plan-specified 计划 02 (I-4) comment (T2.1 literal). I-5: `QaCoverageKeyIntentParityTest` has the two parity tests ((a) every intent key in catalog, (b) every catalog key referenced) + `knownUnreferencedKeys` = {general.answer, work.relocation} each with rationale comment + `known unreferenced exceptions are exactly the truly unreferenced keys` (the negative test proving the exception set is live; clearing it fails (b)) + I-4 order assertion + IP-5 reachability assertion (`rule carrying application required materials is assigned to next stages intent`); failure message prints unreferenced names.|
|Required commands|PASS|All 8 commands run freshly on final state, JDK11 zulu-11: `mvn test -Dtest=QaCoverageKeyIntentParityTest` exit 0, Tests run: 5, F:0, E:0; `-Dtest=QaFactSelectionUnrecognizedStatusTest` exit 0, 3, F:0, E:0; `-Dtest=QaFactSelectionServiceTest` exit 0, 69, F:0, E:0; `-Dtest=AiReplyIntentCatalogTest` exit 0, 35, F:0, E:0; `-Dtest=QaRuleManagementServiceTest` exit 0, 60, F:0, E:0; `-Dtest=TrustReplyWorkbenchItemFlowTest` exit 0, 52, F:0, E:0; full `mvn test` exit 0, Tests run: 2868, Failures: 0, Errors: 0, Skipped: 4, BUILD SUCCESS; `git diff --check` exit 0 (worktree and commit). Baseline math exact: 2830 base + 30 (c1) = 2860 at c1 head; + 5 parity + 3 unrecognized-status = 2868 at c2 head — matches implementer report. No test-class substitution needed (both `AiReplyIntentCatalogTest` and `QaRuleManagementServiceTest` exist).|
|Downstream interfaces|PASS|c3 sees final statuses: `buildRequestFact` returns `status` from the single composed expression (service :609-620) — the ONE expression is the final form (01 lift THEN 02 cap), no second derivation. requestKey-relevant fields unchanged: `intents = intentCoverages` (:626) identical to base; no RequestIntentDefinition key/title/aliases changed; `alternativeCoverageKeys` not read by `matchIntentsWithSpans` (:386) → `matchIntents` output, `intentKeys`, and `requestKey` hashes byte-identical. `normalizeAndValidate` ordering unchanged for existing keys: entries appended at END, return order = catalog declaration order (`all().map{it.key}.filter{it in trimmed}` :134); parity test asserts `normalizeAndValidate(listOf("programme.purpose","fees.policy"))` returns the same order as pre-plan (plan I-4 acceptance verbatim).|

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1: `scripts/worktree_identity.py` crashes on stale deleted worktree entries (`Path.resolve(strict=True)` → FileNotFoundError for `/sessions/rcw-01pxobmo3wj1bdm5hjgawb7s/…`). Not a plan-required command and not a product change; implementer replicated the Target Worktree Gate with the script's exact git commands (show-toplevel/branch/absolute-git-dir/git-common-dir/HEAD) before/after commit. Independently confirmed: HEAD = f6dc048 on `fast/2026-08-26-execution-order` in the correct worktree; boundary contains exactly the 5 authorized files. Same class as c1's O-1. Outside the four gates.
- O-2: parity test models plan C-1's whole-file-literal methodology: `application.timeline` has no static `definitions` entry; it is counted via the runtime `next_stages` timing composition (`AiReplyIntentCatalog.kt:424`) and `intentReferencedKeys()` includes it with a documented comment. `knownUnreferencedKeys` remains exactly {general.answer, work.relocation} as the plan specifies. Test-methodology note only.
- O-3: `AiReplyIntentCatalog.kt` diff includes the required trailing comma on the preceding `requiredCoverageKeys = listOf("application.steps")` line — syntactic necessity to append the new parameter, matching plan T2.2's literal exactly; no semantic change.

### Required Action
- COMPLETE_CHILD
