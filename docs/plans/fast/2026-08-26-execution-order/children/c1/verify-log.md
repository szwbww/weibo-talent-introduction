## Light Verification: LIGHT_PASS_WITH_NOTES
Child: c1 — docs/plans/2026-08-26/01-llm-fact-retrieval.md (identity commit:ee0749d3beedea7e26f4bf4e097b3d33a1684b7d, SHA-256 7cf649022bc8bc0018b8223bc1d5073954bca16d6ffb0e51511642fd2e5ec264 — re-verified on disk, unchanged)
Boundary: f2935072c819a9167e75220a6a959b0769462fde..de5e130a84fba33296ea906734a1c7f071e3383a
Verifier: C1Verifier

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
|Authorized scope|PASS|Implementation commit de5e130 (`git show --stat`) touches EXACTLY the 7 authorized files: FactRetrieverProperties.kt (new), QaFactRetriever.kt (new), QaFactSelectionService.kt (mod), AiReplyDraftService.kt (mod), application.yml (mod), QaFactRetrieverTest.kt (new), QaFactSelectionRetrievalTest.kt (new). No docs/plans/fast/**, no other product/test file. The range f293507..de5e130 additionally contains 4 docs/plans/2026-08-26/*.md added by the controller's plan-seed commit ee0749d (pre-execution HEAD, not c1 work). `git diff --check f293507..de5e130` exit 0. Worktree clean except untracked docs/plans/fast/** (controller-owned evidence); branch `fast/2026-08-26-execution-order`, HEAD de5e130 = single commit `feat(fast-p): implement c1`. |
|Plan and invariants|PASS|I-1: `assignRulesToIntents(strictCandidateRules, matchedIntents)` (QaFactSelectionService.kt:546) — first arg still strictCandidateRules; `intents = intentCoverages` in RequestFactItem unchanged; retrieval only reaches factRuleIds + diagnostic retrievedFactRuleIds (QaFactSelectionService.kt:630). I-2: all 5 buildRequestFact call sites explicitly set retrievedRuleIds — select real (:74), matrix emptyList (:301), legacy-empty emptyList (:345), legacy-main emptyList (:373), auto real (:409). I-3: union `strictCandidateRules` + `retrievedRules` (filtered `it !in strictCandidateRules`, :542-544), keyword hits first. I-4: four checks in rejectionReason (QaFactRetriever.kt:211-223: not_in_pool / disabled / policy_never / blank_answer_body) with per-id log.warn (I-4 no-silent-drop); rejected count in test `rejects ids failing any of the four checks and counts them` (rejected=4). I-5: orderEvidenceRuleIds appends item.factRuleIds after intent evidence (:652); workbenchResult untouched. I-6: status lift `naturalStatus == UNSUPPORTED && factRuleIds.isNotEmpty() -> PARTIAL` appended after unchanged when (:609-612). I-7: explicit `temperature = 0.0` (QaFactRetriever.kt:88, not LlmProperties.temperature); cache key `sha256(inboundText) + ":" + poolFingerprint(pool)` (:68); poolFingerprint = id-sorted `id|updatedAt|sha256(answerBody)` then SHA-256 (:286-295); capacity overflow → clear() (:cachePut). I-8: every failure path returns `FactRetrieval(false, emptyMap(), outcome=...)` — DISABLED (:58), CLIENT_ABSENT (:61), TRANSPORT_ERROR (:93/:100), EMPTY_RESPONSE (:96/:104), PARSE_ERROR (:108/:112), ALL_REJECTED (:189); never throws (chat in try/catch :81-94, parse in try/catch). I-9: per-request cap `ids.take(cap)` (:170) with log.warn count+ids (:172-179); prompt cap maxRulesInPrompt with warn (:71-78). Fixed system prompt verbatim (QaFactRetriever.kt:306-318 == plan 定稿, line-by-line, pinned by test `system prompt is verbatim from the plan`). What-must-NOT-change: resolveMatrixSelection factRuleIds=boundRuleIds=explicitIds untouched (only added retrievedRuleIds=emptyList); requestKey/intents hashes untouched (I-1 test asserts intentKey sequences equal with/without retrieval); LLM-unavailable byte-identical (test `disabled retrieval returns the exact same result as an absent retriever` — full ResolvedQaRules equality); AiReplyDraftService diff = only the retrievedFactRuleIds field append (6+/1-); mail_record_qa_rule write paths untouched. |
|Required commands|PASS|All 8 run FRESH on final state with JAVA_HOME=/Library/Java/JavaVirtualMachines/zulu-11.jdk/Contents/Home (JDK 11, path verified): 1) `mvn test -Dtest=QaFactRetrieverTest` exit 0, Tests run: 17, Failures: 0, Errors: 0, Skipped: 0. 2) `mvn test -Dtest=QaFactSelectionRetrievalTest` exit 0, Tests run: 13, Failures: 0, Errors: 0, Skipped: 0. 3) `mvn test -Dtest=QaFactSelectionServiceTest` exit 0, Tests run: 69, Failures: 0, Errors: 0, Skipped: 0. 4) `mvn test -Dtest=AiReplyDraftServiceTest` exit 0, Tests run: 183, Failures: 0, Errors: 0, Skipped: 0. 5) `mvn test -Dtest=TrustReplyWorkbenchItemFlowTest` exit 0, Tests run: 52, Failures: 0, Errors: 0, Skipped: 0. 6) `mvn test -Dtest=GroundedAutoReplyDecisionServiceTest` exit 0, Tests run: 17, Failures: 0, Errors: 0, Skipped: 0. 7) `mvn test` (full) exit 0, Tests run: 2860, Failures: 0, Errors: 0, Skipped: 4, BUILD SUCCESS. 8) `git diff --check f293507..de5e130` exit 0, silent. Every count matches the implementer's execution report exactly. Baseline comparison: ledger baseline (f293507) claims 2847; a fresh full-suite run on a materialized f293507 tree (git archive into scratch dir, no repo modification) yields Tests run: 2830, Failures: 0, Errors: 0, Skipped: 4, exit 0 — and 2830 + 30 new tests (17+13) = 2860 exactly, so the implementer's post-count is arithmetically exact and the ledger's 2847 is off by +17 vs the true base tree (see O-5). No failures/errors anywhere; pre-existing 4 skips unchanged. |
|Downstream interfaces|PASS|QaFactSelectionService constructor gains `qaFactRetriever: QaFactRetriever? = null` + `factRetrieverProperties: FactRetrieverProperties = FactRetrieverProperties()` (QaFactSelectionService.kt:22-23) — nullable-with-default; the 1740-line QaFactSelectionServiceTest still compiles and passes (69/69 green, single-arg constructor). RequestFactItem gains `retrievedFactRuleIds: List<Long> = emptyList()` (AiReplyDraftService.kt:378, appended with default, diagnostic comment; only change in that file). Status rule `UNSUPPORTED && factRuleIds.isNotEmpty() -> PARTIAL` (QaFactSelectionService.kt:609-612) — exact expression c2 composes its cap on. `select()` sendQaRuleIds includes retrieved facts via orderEvidenceRuleIds appending item.factRuleIds (:652) — verified by test `select sendQaRuleIds includes retrieved facts` and `workbench auto path keeps sendQaRuleIds equal to the fact union`. |

### AUTO_FIX
- N/A — no four-gate violation is proven; all 5 recorded deviations classify RECORD_ONLY with evidence below, none requires an authorized correction.

### RECORD_ONLY
- O-1 (deviation 1 — worktree identity gate run manually): `scripts/worktree_identity.py` crashes iterating a stale deleted worktree entry in the shared repo (unrelated to this worktree); gate replicated with the script's exact git commands, checked before/after/at commit. Verified independently: worktree root/branch/HEAD match the brief; de5e130 is HEAD of `fast/2026-08-26-execution-order` with parent ee0749d = pre-execution HEAD. Process-level, zero product impact — outside the light gate.
- O-2 (deviation 2 — FactRetrieval carries extra outcome/stats fields): the plan's own acceptance criteria demand rejected/truncated counts and outcome in the `[FACT_RETRIEVAL]` log line (I-4 rejected==4, I-9 truncated==2, T2.8/A-3 outcome), which the plan's minimal `(available, byRequestIndex)` shape cannot carry. All failure paths still return exactly `FactRetrieval(available = false, byRequestIndex = emptyMap())` with defaulted extras — the plan's literal construction compiles unchanged. Additive only.
- O-3 (deviation 3 — `candidateRules` intermediate omitted): the plan's own T2.4 factRuleIds expression uses `strictCandidateRules` + `retrievedRules` directly and never references T2.3's `candidateRules` (dead local in the plan itself). Implementation computes the union exactly as T2.4 specifies (QaFactSelectionService.kt:542-544, factRuleIds expr); `assignRulesToIntents(strictCandidateRules, …)` preserved (I-1). Test `keyword hits and retrieved facts both enter factRuleIds with keyword hits first` asserts A-first union. No behavior change; T2.3/I-1 intact.
- O-4 (deviation 4 — workbench always calls retrieve, gate delegated to retriever's internal DISABLED guard): T2.7's literal `factRetrieverProperties.enabled` call-site gate would emit NO `[FACT_RETRIEVAL] source=WORKBENCH … outcome=DISABLED` line when the flag is off, directly contradicting the plan's own manual acceptance A-3 which mandates that exact line under `LLM_FACT_RETRIEVER_ENABLED=false`. The implementation resolves the plan-internal tension in favor of A-3: retrieve() is invoked whenever requests/pool are non-empty (QaFactSelectionService.kt:220-221); the retriever's internal `!enabled → FactRetrieval(false, emptyMap(), outcome="DISABLED")` guard (QaFactRetriever.kt:57-58, I-8) yields an empty byRequestIndex so downstream facts are identical to the gated behavior (byRequestIndex empty → emptyList at :409), and the WORKBENCH line is emitted (test `workbench logs the fixed disabled line when the retriever flag is off` asserts the exact line). T2.7's gating intent is preserved: workbench is governed by fact-retriever.enabled, never by enabledForAutoReply; the AUTO path `select()` gates on enabledForAutoReply exactly per T2.7 (:54-60) and emits no line when off.
- O-5 (deviation 5 — valid empty JSON array `[]` is `available = true`): I-8's failure enumeration (LLM 关闭 / bean 缺席 / chat 抛异常 / 空响应 / JSON 不可解析 / 全部 id 校验失败) does not cover a successfully parsed empty array — the model ran and explicitly said "no facts". I-8's stated consequence requires distinguishing "模型说没有" from "模型没跑"; mapping `[]` to EMPTY_RESPONSE/available=false would erase that distinction. Blank/empty content still maps to EMPTY_RESPONSE/available=false (QaFactRetriever.kt:95-96, 103-104). Downstream identical (empty map → emptyList), log records outcome=OK. Test `empty json array means the model says no facts and is available` pins it. I-8 contract intact.
- O-6 (ledger baseline count): ledger baseline "Tests run: 2847" at f293507 could not be reproduced — a fresh full-suite run on the materialized f293507 tree yields 2830 (exit 0, 0 failures, 0 errors, 4 skipped). Current head = 2860 = 2830 + 30 exactly (the 30 new tests). Implementer's reported counts are all verified accurate; the ledger's 2847 is +17 above the true base-tree count. No regression implication (0 failures/errors at both ends), ledger-record quality note only.

### Required Action
- COMPLETE_CHILD
