# Verify Log — trust-reply-configurable-workbench-01

- Verdict: **LIGHT_PASS_WITH_NOTES**
- Boundary: `931e724..ed944d1`
- Verifier: `VerifyChild01b` (fresh verifier, no connection to ImplChild01; previous died-mid-run log empty — all findings re-derived)
- Plan SHA-256 (from execution.md): `86a68e2b9576b0b47cc876cd7c8bdc1d19d33a280ff039f4331f4b7781606a22`

## Gate results

| Gate | Result | Evidence |
|---|---|---|
| 1 Authorized scope | PASS | `git log` shows boundary = exactly one commit `ed944d1 feat(fast-p): implement trust-reply-configurable-workbench-01` (subject matches brief). `git diff 931e724 ed944d1 --stat` = exactly the 9 files of the plan 变更文件清单 (4 main + 5 test), +1613/-160. No `docs/plans/fast/**` inside the commit (worktree has untracked `?? docs/plans/fast/` only). Note: `TrustReplyWorkbenchStateStoreTest.kt` was absent at base (`git ls-tree 931e724` empty) and was created at the exact authorized path — plan labeled it 修改; path is in the authorized list and Task 5 coverage #6 requires it. Not a scope violation. |
| 2 Plan and invariants | PASS | I-1: `resolveCanonicalSelection` (TrustReplyWorkbenchService.kt:1243) + `validateMatrixKeys` requires matrix to fully express every canonical request; `canonicalMatrix` derives from `selection.requestFacts`; `workbenchResult` (QaFactSelectionService.kt) builds sendQaRuleIds as ordered union never fed back; canonical `requestFactSelections` on bootstrap response/saved state/assemble response; `canonicalFactIds`/`suggestedFactIds` = union only. I-2: `checkWorkbenchUniqueness` + resolver post-canonicalization guard → 422 `TRUST_REPLY_FACT_ALREADY_ASSIGNED`; resolver precedes stateStore.save (L380<410), composeLockedItems (L807<873), draft service (adjustItem L715); tests `matrix mode rejects the same rule assigned to two requests`, `legacy flat rejects duplicate ids as already assigned`, `assemble rejects the same source rule bound to two requests`, controller `ambiguous and duplicate assignment map to stable 422 codes`. I-3: per-request pool = explicit rules only, `item.factRuleIds != explicitIds` → `TRUST_REPLY_FACT_SELECTION_INVALID`; `validateExplicitSelection` requires exists/enabled/non-NEVER/non-blank answerBody; tests `matrix mode rejects a fact that matches another request only`, `matrix mode rejects disabled never and blank facts`. I-4: `resolveLegacySelection` first-accepting-request consumption, remaining must empty, both fields → `TRUST_REPLY_FACT_SELECTION_AMBIGUOUS` (L1250, QaFactSelectionService L86); tests `legacy flat assigns each id exactly once to the first accepting request`, `legacy flat rejects ids not consumed by any request`, `bootstrap rejects ambiguous legacy and matrix input`. I-5: `evidenceSetVersionWithMapping` = `sha256Hex(base + "\u0000" + mappingCanonical)` with mapping = `requestKey\u0000orderedIds` joined `\u0001`; inputs ruleId:available:updatedAt:answerBodyHash only, no observed time; OMIT `localEvidenceSetVersion` mirrors base + same mapping; tests `same fact union bound to different requests changes evidence version` (+repeatability), `adjust item materializes OMIT version...`. I-6: `SCHEMA_VERSION=v2`, `LEGACY_SCHEMA_VERSION=v1`; `decodePayload` returns v2 matrix or v1 flat union, null for unknown/corrupt → restore `INVALID`; restore compares sourceVersion + mapping-sensitive evidence + v2 matrix equality or v1 normalized union → `STALE`; saveState accepts v1/v2 request schema, writes v2 only; tests `v1 payload decodes for migration with legacy flat union`, `v2 payload decodes with canonical matrix`, `unknown and corrupt payloads decode to null`, `bootstrap restores v1 payload after flat normalization`, `bootstrap marks stale when stored v2 matrix drifts from current`, `bootstrap marks invalid payload and expired rows as non-restorable`, `bootstrap marks stale when source or evidence version drifted`. I-7: `generate()` fails closed `operation != "ADJUST_ITEM" && requestFactSelections != null` → 422 `TRUST_REPLY_OPERATION_INVALID` (L551; controller L58 stable code); tests `full draft with matrix fails closed and adjust item forwards matrix`, `invalid operation maps to stable 422 code`. answerBody metadata: `TrustReplyRuleMetadata.answerBody` (default null) display-only via `availableFactMetadata()`; not in identity/claims (I-5 input uses rule answerBody content hash per plan). |
| 3 Required commands | PASS | Fresh runs in worktree: `node --test src/test/js/*.test.js` → exit 0, 413 pass / 0 fail (matches baseline); `git diff --check 931e724 ed944d1` → exit 0 clean; `JAVA_HOME=/Library/Java/JavaVirtualMachines/zulu-11.jdk/Contents/Home PATH=.../bin:$PATH mvn test` → exit 0, Tests run: 2092, Failures: 0, Errors: 0, Skipped: 4 (pre-existing), BUILD SUCCESS (matches baseline). |
| 4 Downstream interfaces | PASS | New fields all defaulted: `TrustReplyBootstrapRequest.requestFactSelections = null`, `TrustReplyGenerationRequest.requestFactSelections = null`, `TrustReplyAssembleRequest.requestFactSelections = null`, `TrustReplySavedStatePayload.requestFactSelections = emptyList()` (also required for v1 JSON decode), `TrustReplyRuleMetadata.answerBody = null`. PendingMailOperationService / UnmatchedInboundMailController / AiTrainingController / app.js NOT in the diff; full `mvn test` compile+run passes proves those construction points unchanged. Legacy `requestedFactIds` retained on all requests/payloads. Plan-02 `frameSnapshot` slot correctly untouched (not needed yet). Old-frontend node suite (app.js path) passes untouched; `QaFactSelectionServiceTest` diff is purely additive (only `-` line is the diff header) — existing `select` non-workbench tests unchanged. |

## AUTO_FIX items

N/A — no four-gate violation found.

## RECORD_ONLY items

1. `TrustReplyWorkbenchStateStoreTest.kt` was created (not modified): plan's 变更文件清单 lists it as 修改 but the file did not exist at base 931e724 (`git ls-tree` empty). Created at the exact authorized path to satisfy Task 5 coverage #6 (v1/v2 codec, concurrency, payload limit). No functional impact; scope gate unaffected because the path is authorized. Implementer disclosed this in execution.md.

## Required Action

COMPLETE_CHILD
