## Light Verification: LIGHT_PASS_WITH_NOTES
Child: 04 — docs/plans/2026-08-16/expert-reachability-04-list-badge.md (commit 9e92424a44025b65b4c9091e139c28c596901205, amendment A2)
Boundary: 111aea17aa434bc5836a9409b451dc72954d62be..8530af46bdf0b6575a607645392e12a2bfbdc3e6
Verifier: Reachability04Verifier

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
|Authorized scope|PASS|`git show --name-only 8530af4` = exactly the 5 authorized files: ExpertIndexController.kt (T1), app.js (T2/T3/T4), styles.css (T5), ExpertIndexControllerTest.kt (test), OperatorStatusWriteSeamGuardTest.kt (A2 5th file). Boundary additionally contains 2 docs-only commits (9e92424 plan amendment A2, 9ced588 ledger/execution evidence) — controller-side evidence, no product/test code outside the 5 files. A2 check: guard diff is exactly 1 line 483→484, context `operatorStatus = operatorStatus ?: expert.operatorStatus` unchanged; :94 pin untouched (still `contact?.operatorStatus`); actual line 484 at head = `operatorStatus = operatorStatus ?: expert.operatorStatus ?: "NOT_CONTACTED"` — pin valid.|
|Plan and invariants|PASS|I-4-1: tier decision only `reachabilityMeta[contact.reachability]` (app.js:4785); `emailSource` appears only in `emailSourceLabel` helper (app.js:4761-4764) + title assembly (app.js:4792), never in a tier decision; `enrichedAt` only in unchanged `enrichedBadge` (app.js:4782). T1: `val reachability: String? = null` + `reachability = expert.reachability` in from() (ExpertIndexController.kt:453,498). I-4-2: default `\|\| { label: "可达 未知", cls: "reach-unknown" }` (app.js:4785); grep `reachability || "LOW"` / `reachability ?? "LOW"` = 0 hits. I-4-3: `已退订 · 停发` and `邮箱失效 · 停发` distinct (app.js:4758-4759); grep count = 2 ≥ 2. S-1: styles.css added block byte-verbatim vs plan S-1 fence (40 lines = 40 lines, VERBATIM True via python diff); inserted after `.academic-enriched`, before `/* === 详情 sub-tab === */`; `.academic-badge`/`.academic-hindex`/`.academic-enriched`/`.expert-row-sub` rule blocks zero changed lines (diff = 41 insertions only); badge inserted BEFORE hIndexBadge (app.js:4823 `${reachBadge}${hIndexBadge}${enrichedBadge}`); title texts per plan. S-2: checkbox `(!contact.contactId \|\| isBlockedReach(contact.reachability)) ? 'disabled' : ''` union (app.js:4805); no row-level dimming/strikethrough; no `style=`/`opacity`/`line-through` in renderContactListItems body. N-2: `hIndexBadge`/`enrichedBadge`/`tagsHtml`/`bindingText`/`senderChangedTag` definition lines unchanged (diff +/- lines contain only the S-1 DOM insertion line). T1 test: existing mysql-vs-ES test asserts `"HIGH"` pass-through + new `listExperts passes through null reachability` test. T2: MySQL path `reachability: null` (app.js:4654), ES path `reachability: e.reachability ?? null` (app.js:4706).|
|Required commands|PASS|All run freshly with JDK 11 (JAVA_HOME=/Library/Java/JavaVirtualMachines/zulu-11.jdk/Contents/Home): `node --check src/main/resources/static/app.js` exit 0; `mvn test -Dtest=ExpertIndexControllerTest` exit 0 BUILD SUCCESS, Tests run: 19, Failures: 0, Errors: 0, Skipped: 0 (surefire report); `mvn test` full regression exit 0 BUILD SUCCESS, Tests run: 2484, Failures: 0, Errors: 0, Skipped: 4 (= 2456 base + 13 child-02 + 14 child-03 + 1 new); `git diff --check` exit 0 clean.|
|Downstream interfaces|PASS|`ExpertIndexResponse.reachability: String?` (ExpertIndexController.kt:453) consumed by frontend ES path (app.js:4706); dual-path keys present (app.js:4654/4706); badge DOM/skeleton per S-1 (`reach-badge` + `reach-high/low/unknown/blocked`, dot via `::before`, `title` tooltip); child 05 depends on 03 not 04 — no hard interface from this child; 05 filter seams `listExperts`/`loadContacts` unchanged in behavior (ExpertSearchService.kt / ExpertIndexService.kt zero diff in boundary); controller test additions (19 tests) do not conflict — full suite green. `ExpertProfile.reachability: String?` (domain, line 33) exists, so `from()` compiles and passes through.|

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1: T3 mapping/helpers placed INSIDE `renderContactListItems` body (app.js:4755-4767), not before the function as plan prose said. Documented, evidence-backed deviation (Node `extractFn` vm sandbox breaks on top-level helper refs; JS test files not authorized) — explicitly accepted by amended brief (epoch 2); all acceptance criteria are location-agnostic greps/diffs, all pass.
- O-2: Worktree contains uncommitted epoch-2 record additions to docs/plans/fast/expert-reachability/children/04/execution.md (docs/plans/fast evidence, excluded from implementation commit per brief constraint 5; controller commits evidence separately). Not product code; no gate impact.
- O-3: HIGH/LOW badge title reads `contact.emailSource` (app.js:4792) but ExpertIndexResponse does not carry emailSource (plan I-6/T1: no new backend field) — runtime label may render empty for that segment. Faithful to plan; out of 5-file scope; would need separate authorization.

### Required Action
- COMPLETE_CHILD
