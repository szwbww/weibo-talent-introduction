## Light Verification: LIGHT_PASS
Child: 06 — docs/plans/2026-08-16/expert-reachability-06-batch-config.md (amended A4, commit fb184c4510964449e15928e432ccecc07c794c77)
Boundary: a591cb7972bd7838cead435a496f90e095817bc1..6d0479382b181be33cb8de3d6f12115c7f17eabf
Verifier: Reachability06Verifier

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
|Authorized scope|PASS|`git diff --name-only a591cb7..6d04793`: 8 product/test files changed — BatchExecutionModels.kt, BatchSendTaskConfig.kt, BatchSendTaskConfigService.kt, V100__add_reachability_filter_to_batch_send_task_config.sql, app.js, index.html, BatchSendTaskConfigReachabilityTest.kt, BatchSendTaskConfigServiceTest.kt — all inside the 9 authorized files; ManualInitialOutreachService.kt (authorized T6) zero-diff (A4 carrier, confirmed `git diff ... -- ManualInitialOutreachService.kt` empty). Interleaved docs commits (76fe246: 05 logs, fb184c4: plan amendment, 9c84ee6: brief+ledger) are docs-only evidence, no product changes. A4 3-line check: BatchExecutionModels.kt diff = 6 insertions/3 deletions = exactly 3 additive lines (snapshot `val reachabilityFilter: String? = null` defaulted @:27; `toExecutionSnapshot()` passthrough @:308-309; `RecipientScope.fromSnapshot()` passthrough @:148-149) + comma-only adjustments on the 3 preceding lines; no other change to that file.|
|Plan and invariants|PASS|I-6-1: BatchSendTaskConfigService.kt:199-200 `reachabilityFilter = existing.reachabilityFilter` inside updateLegacyConfig body, immediately after `gateFilterEnabled = existing.gateFilterEnabled` (T4). I-6-2: `toView()` includes `reachabilityFilter = row.reachabilityFilter` (:461); `toLegacyConfig()` (:229-243) does NOT; updateLegacyConfig return BatchSendConfig construction does NOT; all three `*Fields()` (CreateCommand:594-597, UpdateCommand:614-617, BatchSendTaskConfig:634-637) include it. I-6-3: `${` count in V100 = 0. I-6-4: `ALLOWED_REACHABILITY_MODES` defined 1× (ExpertSearchService.kt:242 `setOf("HIGH_ONLY","EXCLUDE_BLOCKED","UNKNOWN_ONLY","BLOCKED_ONLY")`), referenced ≥2 (ExpertSearchService.kt:262; BatchSendTaskConfigService.kt:302-303 `require`); no self-held string set in BatchSendTaskConfigService. I-6-5: migration `ADD COLUMN reachability_filter VARCHAR(32) NULL` no DEFAULT; entity/CreateCommand/UpdateCommand all `reachabilityFilter: String? = null`; test asserts create default null. T6/IP-1 (A4) end-to-end: config → `toExecutionSnapshot()` (BatchExecutionModels.kt:308-309; 5 call sites in BatchSendControlService.kt:64/105/122/152/256 all config-driven) → `BatchExecutionSnapshot.reachabilityFilter` (:27) → `RecipientScope.fromSnapshot()` (:148-149) → `resolveScope(snapshot)` (ManualInitialOutreachService.kt:427-428; 4 callers :176/:453/:458/:509) → RecipientScope.reachabilityFilter; wiring is real, not a no-op; O-2: the only other RecipientScope constructions (:1023 legacy-typed retry, :1166 material-reminder KV) do NOT pass it. T7: app.js 14 reachabilityFilter hits cover all 12 gateFilterEnabled roles (list pill :13455/:13457, editor backfill :13600, editor snapshot collect :14332, manual snapshot :14355, save payload :14474, deepClone :14559, manual defaults :14581, draft backfill :14605, readManualFormValues :14690, normalizeManualSnapshot :14709, change-log format branch :14720 `if (key === "reachabilityFilter") return batchReachabilityFilterLabel(value)`, diff key :14760, diff-map :14808, clearAllDiffMarkers :14838); select with 3 options (不过滤/EXCLUDE_BLOCKED/HIGH_ONLY) in BOTH editors (index.html editor `batchConfigEditorReachabilityFilter`, manual `batchManualReachabilityFilter`); pill only when non-empty via `.batch-gate-pill` reuse. N-3: BatchSendSettingService.kt (BatchSendConfig data class) zero diff. S-6-1: index.html additions use only existing classes (batch-config-field/-label, bsc-input bsc-select, batch-gate-hint, batch-config-diff-badge/-original), no new class, no inline style. S-6-2: styles.css zero diff (`.batch-gate-pill` rules @:9167/:9180/:9186 untouched); pill reuses class in place.|
|Required commands|PASS|`node --check src/main/resources/static/app.js` exit 0. `mvn test -Dtest=BatchSendTaskConfigReachabilityTest` (JDK 11) BUILD SUCCESS, Tests run: 6, Failures: 0, Errors: 0, Skipped: 0. `mvn test -Dtest=BatchSendTaskConfigServiceTest` (JDK 11) BUILD SUCCESS, Tests run: 54, Failures: 0, Errors: 0, Skipped: 0. `mvn test -Dtest=FlywayMigrationIntegrationTest -DmigrationIt=true` (JDK 11) BUILD FAILURE, Tests run: 1, Errors: 1 — `IllegalStateException: Docker is required for Flyway migration tests` (Docker socket `unix:///Users/lukai/.docker/run/docker.sock` not listening); test is `@EnabledIfSystemProperty(named="migrationIt", matches="true")` (FlywayMigrationIntegrationTest.kt:21) and self-detects the environment (:36); NOT silently passing/faked — recorded and classified as environmental per the plan's own gating (plain full suite skips it; Skipped count unchanged). Full `mvn test` (JDK 11) BUILD SUCCESS, Tests run: 2515, Failures: 0, Errors: 0, Skipped: 4 — exactly the expected 2456+13+14+1+25+6=2515 vs recorded baseline. `git diff --check a591cb7..6d04793` clean.|
|Downstream interfaces|PASS|V100 unique (migration tail 98/99/100); `reachability_filter VARCHAR(32) NULL` no DEFAULT (NULL = 不过滤); RecipientScope.reachabilityFilter receives config value on config-driven paths only (O-2 manual/independent paths omit it); frontend select ids `batchConfigEditorReachabilityFilter`/`batchManualReachabilityFilter` with options 不过滤/EXCLUDE_BLOCKED/HIGH_ONLY; pill `可达性 · <label>` rendered only when non-empty, immediately after gate pill, reusing `.batch-gate-pill`; change-log formatting branch for key `reachabilityFilter` (app.js:14720 via `batchReachabilityFilterLabel`, falls back to "不过滤"). No other child depends on 06 (final child).|

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1: FlywayMigrationIntegrationTest errors under `-DmigrationIt=true` because Docker is unavailable on this machine (testcontainers: DOCKER_HOST socket not listening; explicit "Docker is required" failure). Environmental, per the plan's gating rule (test skipped in plain full suite via `@EnabledIfSystemProperty(migrationIt=true)`); recorded, not a defect, not a PAUSE.
- O-2: `BatchSendTaskConfigCreateCommand` also gained `reachabilityFilter: String? = null` (plan T2 names entity + UpdateCommand only). Necessitated by I-6-2's "three `*Fields()` include the column" (CreateCommand.toFields() must carry it) and by the create/save path (:77); within the authorized file, defaulted, consistent with plan intent — no behavioral surprise.
- O-3: Frontend label map `BATCH_REACHABILITY_LABELS` (app.js:13416-13423) includes display labels for `UNKNOWN_ONLY`/`BLOCKED_ONLY` — modes in the shared `ALLOWED_REACHABILITY_MODES` but not offered by the UI select. Display-only map aligned to the single validation source (I-6-4 unaffected); mirrors the plan's "API 可写但 UI 未列出" note.

### Required Action
- COMPLETE_CHILD
