## Light Verification: LIGHT_PASS
Child: 05 (docs/plans/2026-08-16/expert-reachability-05-filter-seams.md, plan commit e9badbbd347dd12fdb6a65c6c3a9191763ecaefd incl. amendment A3)
Boundary: 8530af46bdf0b6575a607645392e12a2bfbdc3e6..a591cb7972bd7838cead435a496f90e095817bc1
Verifier: Reachability05Verifier

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
|Authorized scope|PASS|`git diff --name-only 8530af4..a591cb7` (excl. docs/plans/fast/) = exactly the brief's 8 authorized files: BatchExecutionModels.kt, ManualInitialOutreachService.kt, ExpertIndexController.kt, ExpertSearchService.kt, index.html, app.js, ReachabilityFilterSeamTest.kt (NEW), OperatorStatusWriteSeamGuardTest.kt. Linear history: 8530af4 → f0168a1 (docs only: 04 execution.md/verify-log.md) → f5025fc (impl, 7 files, message `feat(fast-p): implement 05`) → e9badbb (docs: plan A3 amendment) → 9d75ce7 (docs: ledger) → a591cb7 (fix, 1 file, `fix(fast-p): repair 05 round 1`). docs/plans/2026-08-16/expert-reachability-05-filter-seams.md also changed in boundary = plan evidence of human-approved A3, not product/test code. A3 check: fix commit a591cb7 diff on OperatorStatusWriteSeamGuardTest.kt is EXACTLY 3 insertions/3 deletions = pin syncs only — ExpertIndexController.kt 94→95 (context `operatorStatus = contact?.operatorStatus`), 484→485 (context `operatorStatus = operatorStatus ?: expert.operatorStatus`), ExpertSearchService.kt 431→476 (context `operatorStatus = source.nullableText`); contexts byte-unchanged, no assertion changes, no other file in that commit.|
|Plan and invariants|PASS|I-5-1: all `"reachability"` hits in src/main/kotlin reviewed — only ES-query construction is ExpertSearchService.reachabilityFilter() (ExpertSearchService.kt:263-277); other hits are mapping check (ExpertIndexService.kt:221/223), writer doc field (ExpertIndexWriterService.kt:246), mapToProfile read (:483), sourceFields whitelist (:506), DTO/domain fields (ExpertIndexController.kt:455/502, ExpertProfile.kt:33), logs/messages. Four construction points delegate: buildExpertFilters (ExpertSearchService.kt:1005 `reachabilityFilter(reachability)?.let { filters.add(it) }`), buildEsFiltersForLevel (ManualInitialOutreachService.kt:1300 `ExpertSearchService.reachabilityFilter(scope.reachabilityFilter)?.let { ... }`), buildMaterialReminderEsFilters (ManualInitialOutreachService.kt:1149, same pattern), matchesExpert (BatchExecutionModels.kt:75-88, in-memory, no ES query). No self-held term/terms/must_not reachability expressions elsewhere. I-5-2: UNKNOWN_ONLY = `bool.must_not.exists` (ExpertSearchService.kt:271-275); matchesExpert UNKNOWN_ONLY = `value.isNullOrBlank()` (BatchExecutionModels.kt:82); exact acceptance grep `reachability" to "UNKNOWN"\|term.*reachability.*UNKNOWN` → 0 hits. I-5-3: ReachabilityFilterSeamTest has 20-case 4×5 @CsvSource parity test (NULL/HIGH/LOW/BLOCKED_UNSUBSCRIBED × 5 modes) asserting matchesExpert vs hand-written expected per reachabilityFilter semantics, plus expression-shape test (must_not+exists for UNKNOWN_ONLY, no "HIGH"), plus null/""/"  "→null test. I-5-4: `mode.isNullOrBlank() → null` (ExpertSearchService.kt:261); test asserts searchExperts without reachability appends nothing (filter size stays 5 = tag+operatorStatus+region+hasField+discipline, none contains "reachability"). T2: RecipientScope.reachabilityFilter: String? = null appended as last field (BatchExecutionModels.kt:61); matchesExpert segment placed directly after operatorStatuses segment (:73-88). T3: buildExpertFilters(reachability: String? = null, ...) (:962), searchExperts param + passthrough (:295/:302), listExperts `@RequestParam(required = false) reachability: String? = null` (ExpertIndexController.kt:72) + passthrough (:76). T4: index.html select id=expertReachabilityFilter inserted immediately after expertRecentYearsFilter block (index.html:551-557) with exactly 5 options ""/HIGH_ONLY/EXCLUDE_BLOCKED/UNKNOWN_ONLY/BLOCKED_ONLY (labels 全部/仅高可达/高+低（排除已失效）/仅未知/仅已失效); app.js has 4 sync points (>=4 required): read+params at :4671 and :11718, active-check at :11451, reset/forEach at :11466. N-3: `git diff` of boundary on ExpertSearchService.kt contains zero ALLOWED_HAS_FIELDS lines. N-1: full impl diffs reviewed — existing filter items and order untouched in all four construction points (reachability appended last).|
|Required commands|PASS|All run freshly with JAVA_HOME=/Library/Java/JavaVirtualMachines/zulu-11.jdk/Contents/Home (JDK 11): `node --check src/main/resources/static/app.js` exit 0; `mvn test -Dtest=ReachabilityFilterSeamTest` BUILD SUCCESS, 25/0/0/0 (surefire: tests="25" failures="0" errors="0"); `mvn test -Dtest=ExpertSearchServiceTest` BUILD SUCCESS, 43/0/0/0; `mvn test -Dtest=ManualInitialOutreachServiceTest` BUILD SUCCESS, 86/0/0/0; full `mvn test` BUILD SUCCESS, `Tests run: 2509, Failures: 0, Errors: 0, Skipped: 4` — exactly the expected 2456 base + 13 + 14 + 1 + 25 = 2509; `git diff --check` clean (exit 0). OperatorStatusWriteSeamGuardTest passed in full run (surefire 1/0/0/0); pinned lines confirmed current: ExpertIndexController.kt:95 = `operatorStatus = contact?.operatorStatus ...`, :485 = `operatorStatus = operatorStatus ?: expert.operatorStatus ...`, ExpertSearchService.kt:476 = `operatorStatus = source.nullableText("operatorStatus")`.|
|Downstream interfaces|PASS|`ExpertSearchService.reachabilityFilter(mode: String?): Map<String, Any>?` (ExpertSearchService.kt:260-278) with companion constant `ALLOWED_REACHABILITY_MODES = setOf("HIGH_ONLY","EXCLUDE_BLOCKED","UNKNOWN_ONLY","BLOCKED_ONLY")` (:242); null/blank → null; illegal value → IllegalArgumentException via `require(mode in ...)` (:262) — child 06 validation can reuse the constant. `RecipientScope.reachabilityFilter: String? = null` default (BatchExecutionModels.kt:61). `buildExpertFilters(reachability: String? = null, ...)` (:962), `searchExperts(..., reachability: String? = null)` (:295), `listExperts` `@RequestParam(required = false) reachability: String? = null` (ExpertIndexController.kt:72). Frontend select id `expertReachabilityFilter` wired at 4 app.js sync points (4671 read, 11451 active-check, 11466 reset, 11718 read+params). buildEsFiltersForLevel (:1300) and buildMaterialReminderEsFilters (:1149) delegate to the authority. Child 06 wires config → RecipientScope.reachabilityFilter at resolveScope; it does NOT reimplement the expression — no reimplementation present anywhere (I-5-1).|

### AUTO_FIX
- N/A

### RECORD_ONLY
- N/A

### Required Action
- COMPLETE_CHILD
