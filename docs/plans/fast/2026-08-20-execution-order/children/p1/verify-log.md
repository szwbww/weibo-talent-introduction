## Light Verification: LIGHT_PASS_WITH_NOTES
Child: P1 and plan path docs/plans/2026-08-20/P1-fact-binding-drop-not-fatal.md (brief: docs/plans/fast/2026-08-20-execution-order/children/P1/brief.md)
Boundary: 8ea1e241b5703e967da9861847663e67e5eb3bdc..a356ea4f97d2dbc31dfc07e745fffb1ae5813dc0
Verifier: P1Verifier
Attempt: 1

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
|Authorized scope|PASS|`git diff-tree --root --no-commit-id --name-only -r a356ea4` = exactly the 7 authorized files (AiReplyDraftService.kt, QaFactSelectionService.kt, TrustReplyWorkbenchService.kt, trust-reply-workbench.js, trustReplyWorkbench.test.js, QaFactSelectionServiceTest.kt, TrustReplyWorkbenchServiceTest.kt). `git diff --name-only 8ea1e24..a356ea4 | grep -E "styles\.css\|AiReplyGroundedContentPlanner\|PendingMailOperationService\|AutoReplyConfidenceScorer"` → NO_FORBIDDEN_FILES. Range extra files are docs evidence commits (62f99f1/a76e1c9 P0 evidence + P1/P2 child docs) only.|
|Plan and invariants|PASS|I-1: sole un-throw site QaFactSelectionService.kt:207-208 — `val accepted = item.factRuleIds.toSet(); val dropped = explicitIds.filter { it !in accepted }; if (dropped.isEmpty()) item else item.copy(droppedBindingRuleIds = dropped)`; keeps explicitIds order, continues with item.factRuleIds; TRUST_REPLY_FACT_SELECTION_INVALID in src/main *.kt = 6 (7→6; gone site is the old :199-204): QaFactSelectionService.kt:171,185,243,266 + TrustReplyWorkbenchService.kt:1750,1754. I-2: exactly 4 dropped-field hits in src/main *.kt — AiReplyDraftService.kt:362 `droppedBindingRuleIds: List<Long> = emptyList()`; QaFactSelectionService.kt:208 assignment; TrustReplyWorkbenchService.kt:151 `droppedFactRuleIds: List<Long> = emptyList()`; :1949 toCoverage transfer `droppedFactRuleIds = item.droppedBindingRuleIds`. I-3: commit diff touches TrustReplyWorkbenchService.kt only at :145-152 (declaration) and :1941-1950 (toCoverage); zero change in requestEvidenceVersion/canonicalMatrix/versionId/TrustReplySavedStatePayload regions; no `dropped*` hit inside those bodies; C-2 test 2 (evidenceSetVersion identical with vs without dropped bindings) green. I-4: canonicalMatrix :1766 `factRuleIds = item.factRuleIds`, toCoverage :1929 `factRuleIds = item.factRuleIds`; new field only as parallel third projection (:1949); frontend applyBootstrap guard (trust-reply-workbench.js:610-624) zero-diff; C-2 test 1 asserts requestFactSelections == [TrustReplyRequestFactSelection(key, emptyList())]. I-5: serializeRequestFactSelections zero-diff (js diff hunks only @488 requestFromCoverage shadow carry, @1842 droppedFactLabels, @1899 droppedMarkup); C-3 test 3 asserts outbound requestFactSelections contain ONLY requestKey+factRuleIds. S-1: styles.css diff base..head = 0 lines; droppedMarkup (js:1919-1921) verbatim per plan, class="muted" data-role="item-facts-dropped", appended as `${staleMarkup}${droppedMarkup}` (:1923); `style="` count 1→1 unchanged; C-3 test 1 asserts no inline style / no new class.|
|Required commands|PASS|All 13 commands exit 0 (echo EXIT_1..13=$? = 0). C-1 four singles: 4× mvn exit 0, each `Tests run: 1, Failures: 0, Errors: 0, Skipped: 0`, BUILD SUCCESS. C-2 two singles: 2× mvn exit 0, same 1/0/0/0 each. Combined `mvn test -Dtest=QaFactSelectionServiceTest,TrustReplyWorkbenchServiceTest,TrustReplyWorkbenchItemFlowTest`: exit 0, `Tests run: 145, Failures: 0, Errors: 0, Skipped: 0` (48+59+38). Full `mvn test`: exit 0, `[WARNING] Tests run: 2642, Failures: 0, Errors: 0, Skipped: 4` = expected 2642/0/0/4 (2630 baseline + 6 P0 + 6 P1), BUILD SUCCESS. `node --test src/test/js/trustReplyWorkbench.test.js`: exit 0, tests 22 / pass 22 / fail 0. `node --test src/test/js/*.test.js`: exit 0, tests 676 / pass 676 / fail 0 = expected 676 (670 baseline + 3 P0 + 3 P1). `node --check` app.js and trust-reply-workbench.js: exit 0, silent. `git diff --check`: exit 0, no output.|
|Downstream interfaces|PASS|P2a contract: field names `droppedBindingRuleIds`/`droppedFactRuleIds` (both `List<Long>`, defaulted) + chain stable (declaration → resolveMatrixSelection filter → toCoverage transfer → requestFromCoverage shadow → renderFactSection), `explicitIds - item.factRuleIds` computation preserved (order-keeping filter); markup `<span class="muted" data-role="item-facts-dropped">` present at trust-reply-workbench.js:1920 with same class/data-role so P2a can rewrite copy. 422 + TRUST_REPLY_FACT_SELECTION_INVALID still thrown for genuinely illegal input: QaFactSelectionService.kt:170-171 (matrix size mismatch), :184-185 (validateExplicitSelection), TrustReplyWorkbenchService.kt:1750/:1754, legacy :243/:266; C-1 tests 3-4 green; code readable by P0 UI (frontend map trust-reply-workbench.js:42). Accepted bindings: dropped empty → droppedMarkup = "" (:1921), C-3 test 2 asserts no `data-role="item-facts-dropped"`; chips rendering unchanged. Dropped never in AI context/outbound: workbenchResult/sendQaRuleIds unchanged (built from item.factRuleIds); C-1 tests assert `sendQaRuleIds` empty; C-3 test 3 asserts shadow never serialized back.|

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1 (forced deviation, judged WITHIN plan authority): implementer updated pre-existing test `matrix mode rejects a fact that matches another request only` → `matrix mode keeps the request working when a bound fact matches another request only`, asserting no-throw + `droppedBindingRuleIds=[2L]` + `factRuleIds=[]` + `sendQaRuleIds=[]`. The old assertion tested the exact pre-P1 throw at I-1's sole un-throw site (operator-bound visa fact on the Salary request gets server-filtered) — exactly the input P1 is designed to stop throwing on; keeping it would fail the plan's own 回归 acceptance (full suite green, achieved 2642/0/0/4). New assertions match I-1/I-2/must-NOT-change #4. Plan said "只加用例" (add cases only), so this modification goes beyond the letter but is forced by and consistent with the plan's I-1 semantics. Not an AUTO_FIX item.
- O-2 (context correction): shared context listed head as a356ea4e95cd4fad71a61d4e512c6d6e3cf1f390 (bad object — mistyped). Actual P1 implementation commit is a356ea4f97d2dbc31dfc07e745fffb1ae5813dc0 ("feat(fast-p): implement P1"), which is HEAD of branch fast/2026-08-20-execution-order. All scope/invariant/command evidence gathered against this real commit; base 8ea1e241b5703e967da9861847663e67e5eb3bdc correct.

### Required Action
- COMPLETE_CHILD
