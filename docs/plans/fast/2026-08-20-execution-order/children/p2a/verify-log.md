## Light Verification: LIGHT_PASS_WITH_NOTES
Child: P2a and plan path `docs/plans/2026-08-20/P2a-bound-vs-evidence-split.md` (brief: `docs/plans/fast/2026-08-20-execution-order/children/P2a/brief.md`)
Boundary: a356ea4f97d2dbc31dfc07e745fffb1ae5813dc0..14f88ad08b3b35caf8d27e6e2eb0704b030c0c6f
Verifier: P2aVerifier
Attempt: 1

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
|Authorized scope|PASS|`git diff-tree --root --no-commit-id --name-only -r 14f88ad08b3b35caf8d27e6e2eb0704b030c0c6f` outputs exactly the 8 authorized files: `AiReplyDraftService.kt`, `QaFactSelectionService.kt`, `TrustReplyWorkbenchService.kt`, `trust-reply-workbench.js`, `trustReplyWorkbench.test.js`, `QaFactSelectionServiceTest.kt`, `TrustReplyWorkbenchItemFlowTest.kt`, `TrustReplyWorkbenchServiceTest.kt`. Forbidden files absent: `AiReplyGroundedContentPlanner.kt`, `AutoReplyConfidenceScorer.kt`, `AiReplyReviewAuditService.kt`, `PendingMailOperationService.kt`, `AiTrainingController.kt`, `UnmatchedInboundMailController.kt`, `styles.css` — none in commit. Range log = 14f88ad (implementation) + d0721ee/37c7d2c (P1 docs evidence, expected). Working tree clean of product-code changes (only plan docs execution.md/ledger.md modified, see O-3).|
|Plan and invariants|PASS|I-1: `val boundRuleIds: List<Long> = emptyList()` verbatim at end of `RequestFactItem` (AiReplyDraftService.kt:374) with plan-verbatim comment; exactly 3 production assignments: QaFactSelectionService.kt:204 `resolveMatrixSelection` → `boundRuleIds = explicitIds` (verbatim, ordered), :267 `resolveLegacySelection` → `item.factRuleIds`, :300 `resolveAutoSelection` → `item.factRuleIds`; all at `item.copy(...)` call sites, none inside `buildRequestFact`. Acceptance grep `boundRuleIds = ` in src/main = 4 lines. I-2: `item.boundRuleIds` = exactly 4 lines in TrustReplyWorkbenchService.kt — :1714 (resolveCanonicalSelection `requestEvidenceVersion(key, item.boundRuleIds, …)`), :1766 (canonicalMatrix `factRuleIds = item.boundRuleIds`), :1844 (buildInitialItemVersions `requestEvidenceVersion(key, item.boundRuleIds, …)`), :1929 (toCoverage `factRuleIds = item.boundRuleIds`). `item.factRuleIds` count in file dropped 8 (base a356ea4f) → 4 (head) = exactly −4; remaining 4 are B-class untouched (:1057 whole-letter requestEvidenceVersion, :1440-1441 canonicalizeClaims general.answer fallback, :1916 adjacent `filterKeys`). I-3: canonicalMatrix (:1766) and toCoverage (:1929) both assign `factRuleIds = item.boundRuleIds` byte-equal, switched in same commit; JS `applyBootstrap` guard zero-diff (JS diff = one string only); D-2 #1 green. I-4: `boundRuleIds != explicitIds` = 1 hit (:207, constant-true defensive, throws `TRUST_REPLY_FACT_SELECTION_INVALID` :209-210); `factRuleIds != explicitIds` = 0 hits. I-5: both `requestEvidenceVersion` call shapes unchanged (:1714, :1844); D-2 #2 (`evidence version is unchanged when every binding is accepted`) and D-3 (`locked items survive the bound-vs-evidence split`) green — no-drop items keep evidenceSetVersion/versionId. I-6: `droppedBindingRuleIds = explicitIds.filter { it !in accepted }` (:205, `accepted = item.factRuleIds.toSet()` → value = explicitIds − factRuleIds); name/chain preserved to `droppedFactRuleIds = item.droppedBindingRuleIds` (TrustReplyWorkbenchService.kt:1949); front-end wording verbatim S-1; `未被采纳` in JS = 0. S-1: `class="muted" data-role="item-facts-dropped"` unchanged (trust-reply-workbench.js:1920); JS diff = exactly one Chinese string: `以下事实未被采纳：…` → `以下事实已绑定但不会作为本条回答的依据：…绑定会保留，但 AI 不会引用它们的正文。`. S-2: `boundRuleIds` in JS = 0 hits; `styles.css` diff = 0 lines; no new CSS classes, no `style=` increments (D-4 #2 also asserts `!/style=/`).|
|Required commands|PASS|ONE foreground chained bash call (timeout 1800, ~868 s, log /tmp/p2a-gate3.log, `;`-chained, exit marker after every command). All 15 exit markers = 0: D-1 ×4 single tests (`QaFactSelectionServiceTest#…`, each `Tests run: 1, Failures: 0, Errors: 0`); D-2 ×3 single tests (`TrustReplyWorkbenchServiceTest#…`, each 1/0/0); D-3 ×1 single test (`TrustReplyWorkbenchItemFlowTest#…`, 1/0/0); combined 4-class `mvn test -Dtest=QaFactSelectionServiceTest,TrustReplyWorkbenchServiceTest,TrustReplyWorkbenchItemFlowTest,AiReplyDraftServiceTest` → `Tests run: 321, Failures: 0, Errors: 0, Skipped: 0` (52+62+39+168); full `JAVA_HOME=…zulu-11… mvn test` → `Tests run: 2650, Failures: 0, Errors: 0, Skipped: 4`, `BUILD SUCCESS` (matches expected 2650/0/0/4); `node --test src/test/js/trustReplyWorkbench.test.js` → 24 tests, 24 pass, 0 fail; `node --test src/test/js/*.test.js` → `tests 678, pass 678, fail 0` (matches expected 678/0); `node --check` on app.js and trust-reply-workbench.js both silent exit 0; `git diff --check` exit 0, no output. 10× `BUILD SUCCESS`, 0 `BUILD FAILURE`.|
|Downstream interfaces|PASS|P2b contract fully present: `RequestFactItem.boundRuleIds` field (:374); three `resolve*Selection` assignments (:204/:267/:300); four projections on `boundRuleIds` (:1714/:1766/:1844/:1929); assertion comparing `boundRuleIds` (:207). Outbound audit path untouched: `workbenchResult` `sendIds = requestFacts.sortedBy { it.index }.flatMap { it.factRuleIds }.distinct()` (QaFactSelectionService.kt:311) → `sendQaRuleIds`/`promptRuleIds` still evidence-only; AiReplyDraftService.kt diff is field-only (ResolvedQaRules :475-476 send/prompt reads untouched). `TrustReplyRequestCoverage.factRuleIds: List<Long>` name/type unchanged (TrustReplyWorkbenchService.kt:136), value now the bound set. Front-end chips render bound facts via existing `request.factRuleIds` path — zero front-end schema change (S-2). `TRUST_REPLY_FACT_SELECTION_INVALID` still thrown for genuinely illegal input — matrix size mismatch (:168-171), disabled rule via `validateExplicitSelection` catch (:185), legacy remaining non-empty (:271-272) — plus the constant-true defensive assertion (:207-210).|

### AUTO_FIX
- N/A — no four-gate violation.

### RECORD_ONLY
- O-1 (plan-audit composition note): acceptance grep `boundRuleIds = ` in src/main = 4 lines exactly as required, but the 4th hit is the field's doc-comment line (AiReplyDraftService.kt:365 `//   boundRuleIds = 运营主张…`), not the declaration — the declaration `val boundRuleIds: List<Long> = emptyList()` (:374) cannot match the literal pattern (`: List<Long> =` between). Count criterion met exactly; declaration verified verbatim. No action needed.
- O-2 (forced deviations, all documented in brief, all benign): (a) test fixture helper `item(...)` at TrustReplyWorkbenchServiceTest.kt:454-456 sets `boundRuleIds = facts` mirroring production I-1 assignment; divergence cases override via `.copy(boundRuleIds = …)` (:1195, :1262); (b) legacy empty-selection early return (QaFactSelectionService.kt:254-268) left plain — no `.copy`, keeps grep count at 4, semantically consistent (nothing bound on that path); (c) D-2 #3 fixture stubs `qaRules.findAllById` echo (TrustReplyWorkbenchServiceTest.kt:1264-1270) plus an extra `draftService.buildEvidenceSnapshotForSelection(listOf(10L))` stub (:1273-1274) because the bound-but-not-evidence id now enters the per-request version identity (I-2).
- O-3 (worktree bookkeeping, outside review boundary): unstaged modifications to `docs/plans/fast/2026-08-20-execution-order/children/P2a/execution.md` and `ledger.md` (plan/ledger docs, not product code or tests); `git diff --check` silent on them. Left untouched by verifier.

### Required Action
- COMPLETE_CHILD
