## Light Verification: LIGHT_PASS_WITH_NOTES
Child: P2b (plan: docs/plans/2026-08-20/P2b-bound-facts-into-prompt.md; brief: docs/plans/fast/2026-08-20-execution-order/children/P2b/brief.md)
Boundary: 14f88ad08b3b35caf8d27e6e2eb0704b030c0c6f..a3ef1cd3fbeafdb5c05ed03cca97996b1b328fe6
Verifier: P2bVerifier
Attempt: 1

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
|Authorized scope|PASS|`git diff-tree --root --no-commit-id --name-only -r a3ef1cd` outputs exactly the 5 authorized files: QaFactSelectionService.kt, AiReplyDraftService.kt, QaFactSelectionServiceTest.kt, AiReplyDraftServiceTest.kt, TrustReplyWorkbenchItemFlowTest.kt (all under src/main|test/kotlin/com/weibo/talentintroduction/llm/service/). Forbidden files absent: TrustReplyWorkbenchService.kt, AiReplyGroundedContentPlanner.kt, AutoReplyConfidenceScorer.kt, AiReplyReviewAuditService.kt, PendingMailOperationService.kt, src/main/resources/static/ (grep of the file list; zero hits). Branch fast/2026-08-20-execution-order at HEAD a3ef1cd.|
|Plan and invariants|PASS|I-1: QaFactSelectionService.kt:315-320 — `sendIds = ordered.flatMap { it.factRuleIds }.distinct()`; `promptIds = (sendIds + ordered.flatMap { it.boundRuleIds }).distinct()`; `sendQaRuleIds = sendIds, promptRuleIds = promptIds`. AiReplyDraftService.kt:488-490 — `sendQaRuleIds = requestFact.factRuleIds.distinct()`, `promptRuleIds = (requestFact.factRuleIds + requestFact.boundRuleIds).distinct()`. Grep: no `boundRuleIds` on any sendQaRuleIds/sendIds RHS (0 hits); select() legacy path :69 `promptRuleIds = sendQaRuleIds` untouched. Evidence-first union at both sites. I-2: system message :693 contains `neither the answer basis nor the attached reference facts` (count 1); :697 contains `Do not introduce any outbound action that the answer basis does not state.` verbatim (count 1, unchanged context line in diff); user-message fact channel `Facts the operator attached to this question (reference material, not the answer basis):` appended after answer basis; action sentence references only the answer basis (never reference facts). I-3: boundFactsBlock AiReplyDraftService.kt:678-685 isomorphic to knowledge block :2319-2328 — `qaRuleRepository.findById(ruleId).orElse(null)`, `if (body.isBlank()) return@mapNotNull null`, title fallback `"Fact $ruleId"`, `.joinToString("\n\n").take(12000)`; current values from repository, no cache/snapshot. I-4: `if (boundFactsBlock.isNotBlank())` guard at :717 (no empty paragraph); union is identity when boundRuleIds empty/equal (distinct preserves sendIds order); C-1 case 2 + C-2 case 2 green. I-5: output-validation section (`invalid` = blank || INTERNAL_RESPONSE_MARKER || `AiReplyActionPolicy.findViolations(candidate, allowedActions)`; `rejectNonEnglishItemAnswer`) zero-diff vs base — no diff hunks in that region; C-2 case 3 green. Acceptance greps: sendQaRuleIds RHS free of boundRuleIds ✓; promptRuleIds/promptIds RHS contain boundRuleIds evidence-first ✓; Line A assertion test updated (C-3) with `without attached facts` suffix, assertions preserved.|
|Required commands|PASS|Single chained bash call (timeout 1800, all foreground), JAVA_HOME=zulu-11. C-1 x2 (EXIT_1=0: 1/0/0/0; EXIT_2=0: 1/0/0/0), C-2 x3 (EXIT_3=0, EXIT_4=0, EXIT_5=0: 1/0/0/0 each), C-4 x1 (EXIT_6=0: 1/0/0/0), 3-class run (EXIT_7=0: 265 tests, Failures: 0, Errors: 0, Skipped: 0, BUILD SUCCESS), full `mvn test` (EXIT_8=0: **Tests run: 2656, Failures: 0, Errors: 0, Skipped: 4**, BUILD SUCCESS — matches expected 2656/0/0/4), `node --test src/test/js/*.test.js` (EXIT_9=0: **pass 678, fail 0**, cancelled 0, skipped 0 — matches expected 678/0), `node --check app.js` (EXIT_10=0, silent), `node --check trust-reply-workbench.js` (EXIT_11=0, silent), `git diff --check 14f88ad..a3ef1cd` committed range (EXIT_13=0, clean). Worktree `git diff --check` EXIT_12=2 due to trailing whitespace in unstaged harness doc execution.md:62 — see O-1; not in the implementation commit.|
|Downstream interfaces|PASS|sendQaRuleIds/sendIds evidence-only (grep: all RHS from factRuleIds only; outbound audit mail_record_qa_rule records evidence only — C-4 test asserts requestedFactIds/canonicalFactIds/qaRuleIds all exclude bound id 99L). canonicalizeClaims TrustReplyWorkbenchService.kt:1440-1441 and AiReplyGroundedContentPlanner.kt:73-80 still read factRuleIds (claims stay evidence-grounded). Line A operator-directed assertions still hold (`prompt.contains("operator-provided answer basis")` etc. — updated C-3 test green in full suite). status/allowedHandlings/factRuleIds semantics unchanged from P2a (no changes to those files in commit). No HTTP/storage contract change (no controller/storage/static files in the commit).|

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1: Worktree `git diff --check` exits 2: trailing whitespace at `docs/plans/fast/2026-08-20-execution-order/children/P2b/execution.md:62` — an unstaged, harness-owned tracking file (modified outside the implementation commit; committed-range `git diff --check 14f88ad..a3ef1cd` is clean). Outside the light gate; P2b's committed diff is whitespace-clean.
- O-2: Documented deviation in QaFactSelectionServiceTest.kt: P2a-era test `send and prompt rule ids still come from factRuleIds` renamed to `send rule ids stay evidence only while bound facts join prompt ids`; send-side assertions preserved (`assertEquals(listOf(1L), sendQaRuleIds)`, `assertFalse(2L in sendQaRuleIds)`), prompt-side assertion updated from `assertFalse(2L in promptRuleIds)` to `assertEquals(listOf(1L, 2L), promptRuleIds)`. Judged WITHIN plan authority: I-1 deliberately changes promptRuleIds semantics at workbenchResult (the exact code under test), so the old assertion asserted pre-P2b behavior and contradicts the new mandated contract; plan C-3/IP-5 establishes the lockstep test-update precedent for the identical situation on the Line A case, and a full-suite pass requires the stale assertion update. No behavioral code change; no gate violation.

### Required Action
- COMPLETE_CHILD
