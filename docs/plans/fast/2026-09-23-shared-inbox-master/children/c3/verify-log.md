# c3 verify-log.md

## Light Verification: LIGHT_PASS_WITH_NOTES
Child: c3 and docs/plans/2026-09-23/03-shared-inbox-bounce.md
Boundary: e28da46..e77cb06
Verifier: C3Verifier

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
|Authorized scope|PASS|`git show --name-status --format="" e77cb06` = exactly the 6 Authorized Files, all `M` (product: `BounceCollectionService.kt`, `AutoMailReplyService.kt`, `SelfCheckProbeDetector.kt`; tests: the 3 matching test files), 768 insertions / 55 deletions. `git show --stat --diff-filter=AD e77cb06` is empty → no new/deleted files, no `create mode`/`delete mode`. `git diff --name-only e28da46..e77cb06` product/test paths are the same 6 (the other range entries are `docs/plans/fast/**`: brief/ledger/c2 logs carried by the intervening controller doc commits between base and `e77cb06`, not by the implementation commit). `BatchAutoMailReplyServiceTest.kt` and `SelfCheckProbeSender`/`SenderAccountSelfCheckService` (probe send format) are absent from the commit; the bounce read paths `BounceController.kt`, `BounceRateMonitorService.kt`, `MailMonitoringService.kt` are absent too. Single commit `feat(fast-p): implement c3` = `HEAD` of `fast/2026-09-23-shared-inbox-master`; only untracked/modified path in `git status` is the (uncommitted) `children/c3/execution.md`.|
|Plan and invariants|PASS|**I-1**: dedup precedes every lookup (`BounceCollectionService.kt:93-96`); `:100-102` `findOutboundCandidates` (`:159-170` = `MessageIdNormalizer.candidatesFor` × c2's `MailRecordRepository.findOutboundCandidatesByMessageId`, deduped by record id, deterministic order) → `resolveSenderAccountCode` (`:176-208`) rewrites only when there is exactly one candidate, non-blank `senderAccountCode`, and `candidateCode in groupMemberCodes(...)`; all four negative shapes (`NO_OUTBOUND_RECORD`/`AMBIGUOUS_OUTBOUND_RECORD`/`OUTBOUND_WITHOUT_SENDER_ACCOUNT`/`OUTBOUND_OUTSIDE_GROUP`) → `keepPassedInAttribution` (`:211-224`) keeping the passed-in code and emitting the greppable `log.warn("Bounce attribution unresolved: reason={} passedInAccountCode={} originalMessageId={} outboundCandidates={}")`; group = `listAccounts()` + `(inboundMailboxCode ?: accountCode)` + passed-in (`:233-243`); `failedRecipient` only ever resolves an **expert** (`resolveOriginalContact` `:247-252`), never the sender account. Tested: `BounceCollectionServiceTest` alias-attribution / owner-attribution / no-candidate / multiple-candidates / other-group / failedRecipient-is-never-the-sender / backfilled-logical-account / dedup-before-any-lookup (all green, see Required commands). **I-2**: `collectBounces` still uses `fetchUnseenMessages(account)` (`:44`) and its only production caller passes the resolved owner (`AutoMailReplyService.kt:936`, `account` = `getAutoReceiveAccount(...)` = `resolveInboundOwner`); `bounce_message_id` uniqueness/dedup and the expert `EMAIL_INVALID` side effect are untouched (`:94`, `:120-129`; existing prefixed-Message-ID / HARD-marks-EMAIL_INVALID / SOFT-no-mark tests pass); one shared-mailbox collection now checks every logical member: `AutoMailReplyService.kt:946-947` `groupMembersOf(account).map { it.accountCode }.distinct().forEach { bounceRateMonitorService.checkAndWarn(it) }`, asserted once for owner and once for alias by `shared mailbox check runs the hard bounce monitor for every logical group member once`. **I-3**: `SelfCheckProbeDetector.kt:18-28` requires From (`trim().lowercase()`) == some group member's `senderEmail` **and** the subject to be that member's complete generated form — `PROBE_SUBJECT` (`:41-44`) `^\[\s*self\s*-\s*check\s*\]\s*(\S+)\s+(\d+)\s*$` with `IGNORE_CASE`, code token compared verbatim to `member.accountCode`; 11/0 detector tests cover owner, alias-via-owner-mailbox, `[ self - check ]` spacing, case-insensitive From, other physical group, other member's code, `Re:`, incomplete subject, non-decimal and extra-token tails, missing from/subject, empty group. **Single entry**: probe check inside `processSingle` (`AutoMailReplyService.kt:123-135`) after `resolveInboundOwner` (`:100`) and the positive-UIDVALIDITY `require` (`:113`), and before `physicalInboundRowExists` (`:138`), the transaction (`:144`) and routing/`RECIPIENT_UNRESOLVED`; returns `SELF_CHECK_IGNORED` with `recorded=false` (`:129-134`), which is absent from `MANUAL_REVIEW_OUTCOMES` (`:1579-1592`). The batch path's own current-account-only branch is deleted, not duplicated (diff hunk `@@ -841,12 +862,6 @@`). **I-4**: `markSeen(owner, uid)` only under `!skipImapAck` (`:124`); zero business writes asserted with `verifyNoInteractions(inboundMailProcessingRepository, mailRecordRepository, inboundIntentRepository, inboundMailTagService, mailAttachmentService, contactRepository, deliveryService, groundedAutoReplyDecisionService, expertEmailAliasService, expertOperatorStatusService)` for both the batch path and the UID-backfill path; cursor continuity via `handledUids.add` executed only after `processSingle` returns (`:900`) → `advance("owner", 1L, listOf(302L), setOf(302L), 0L)` while `cursorService.get("alias")` is never called (asserted); no-positive-UIDVALIDITY probe throws at `:113` before the filter → no ack, no handled set (asserted); a throwing `markSeen` propagates out of `processSingle` into the batch `catch` (`:918`) so `handledUids.add` (`:900`) and therefore the cursor advance cannot include that UID. External same-subject mail still takes the business path (`MANUAL_REVIEW/CONTACT_NOT_FOUND`, asserted). **Deviation (a) judged within authorization**: `BounceCollectionService.kt:27-36` adds a tail-defaulted nullable `MailSenderAccountService? = null` — same convention as the pre-existing `expertOperatorStatusService`, inside an already-authorized file, no new file/field/interface, no non-Spring caller (`grep "BounceCollectionService("` → only `BounceCollectionServiceTest.kt:39` and `BounceBackfillServiceTest.kt:16`, the latter relying on the default), production Spring injects the real `@Service` bean (see O-1). **Deviation (b) judged within authorization**: `SelfCheckProbeDetector.kt:41-44` anchored full-form regex replaces the base space-stripped `startsWith` prefix + From-only test; it is strictly narrower, keeps the tag-spacing tolerance, and is what I-3 requires ("完整生成格式", "仅主题前缀不识别"); the only dropped legacy acceptance is a subject with no whitespace between code and timestamp, which `DefaultSelfCheckProbeSender` never emits (`message.subject = "[self-check] ${account.accountCode} ${System.currentTimeMillis()}"`, `SenderAccountSelfCheckService.kt:29`).|
|Required commands|PASS|Fresh on HEAD `e77cb06`, JDK 11 (`JAVA_HOME=/Library/Java/JavaVirtualMachines/zulu-11.jdk/Contents/Home`): `mvn -B -Dtest=BounceCollectionServiceTest,AutoMailReplyServiceTest,SelfCheckProbeDetectorTest,BatchAutoMailReplyServiceTest,ImapMailReceiveServiceTest,OperatorStatusWriteSeamGuardTest test` → `BUILD SUCCESS`; fresh surefire reports (20:21) give `Tests run: 152, Failures: 0, Errors: 0, Skipped: 0` — BounceCollectionServiceTest **15**, AutoMailReplyServiceTest **76**, SelfCheckProbeDetectorTest **11**, BatchAutoMailReplyServiceTest **21**, ImapMailReceiveServiceTest **28**, OperatorStatusWriteSeamGuardTest **1**. Against the brief/plan baselines: Bounce 6→15, Auto 70 (after c2)→76, Probe 4→11, Batch 21→21, Imap 28→28, guard 1→1, zero failures — the claimed 152/0 reproduces exactly, and the delta is exactly the new I-1/I-2/I-3/I-4 cases. `node --test src/test/js/*.test.js` → `tests 1121 / pass 1121 / fail 0 / suites 222` (= baseline; the same suite also ran green inside the mvn build via `exec-maven-plugin`). No Docker, no formatter/linter, no full-suite run.|
|Downstream interfaces|PASS|`BounceController.kt`, `BounceRateMonitorService.kt`, `MailMonitoringService.kt` are not in the commit → bounce read paths unchanged; no exhaustive `when`/switch over `SinglePipelineOutcome` exists outside `AutoMailReplyService` (`grep "SinglePipelineOutcome\."` outside that file = empty; `MailAutomationController.backfillUids` only serialises `result.outcome.name`/`reason`), so the new enum constant cannot break a consumer. `V29__create_bounce_record.sql` untouched and `bounce_message_id` dedup still precedes every lookup (`BounceCollectionService.kt:93-96`, asserted), so row identity/uniqueness is unchanged; expert association for OUTBOUND shapes unchanged (`ingest resolves prefixed originalMessageId to original contact`, `ingest marks EMAIL_INVALID ... for HARD bounce`, `ingest does not mark EMAIL_INVALID for SOFT bounce` all green). c2's `processSingle` contract still holds with the probe filter inserted: owner resolution (`:100`), physical-key dedup, legacy-fingerprint claim, routing and `RECIPIENT_UNRESOLVED` are all unchanged, and c2's own cases inside the 76 stay green (`replaying the same physical owner uid validity and uid adds no row`, `concurrent unique key violation is a duplicate only when the physical key exists`, `unique key violation without the physical key propagates`, `legacy ownerless row ...`, `unresolved recipient lands once as manual review under the owner without any send`). The only observable ordering delta is that a probe UID which already has a new physical row now returns `SELF_CHECK_IGNORED` instead of `DUPLICATE_IMAP_UID` — identical `recorded=false` + `markSeen` + handled-set semantics, and plan-mandated by I-4.|

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1 (implementation-technique caveat, not a four-gate violation): the new `mailSenderAccountService: MailSenderAccountService? = null` on `BounceCollectionService` is the only source of physical-group membership for I-1, so any *future* non-Spring construction that omits it silently degrades alias attribution to the passed-in account and leaves the hard-bounce check owner-only, with no assertion catching it (today `BounceBackfillServiceTest.kt:16` exercises exactly that degraded path). Production wiring injects the real `@Service` bean, and adding the param was the scope-minimal alternative to editing the unauthorized `BounceBackfillServiceTest.kt`, so the gate stays PASS; recorded for visibility before c4/merge.
- O-2 (plan-authorized semantic narrowing, not a four-gate violation): the original-contact read now uses the same OUTBOUND-only candidate list as the attribution (`BounceCollectionService.kt:247-252`; base used the direction-unrestricted `findByMessageId`). A bounce whose `originalMessageId` matches only an INBOUND `mail_record` therefore leaves `original_expert_contact_id` NULL and falls back to `failedRecipient`→alias instead of attaching that record's contact. Plan 阶段 2 explicitly authorises this read change ("改原始专家关联读取为 OUTBOUND-only 候选"), no acceptance criterion covers that shape, so gate 2 stays PASS; flagged only because I-2's wording is "解析专家逻辑不变".

### Required Action
- COMPLETE_CHILD
