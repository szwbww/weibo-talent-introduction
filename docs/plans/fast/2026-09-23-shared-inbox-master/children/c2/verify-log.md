# c2 verify-log.md

## Light Verification: LIGHT_PASS_WITH_NOTES
Child: c2 and docs/plans/2026-09-23/02-shared-inbox-routing.md
Boundary: a15cb52..e28da46
Verifier: C2Verifier

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
|Authorized scope|PASS|`git show --stat 16efaae` = exactly the 10 authorized non-A1 files (`MailReceiveService.kt`, `ImapMailReceiveService.kt`, `AutoMailReplyService.kt`, `BatchAutoMailReplyService.kt`, `InboundMailProcessingRepository.kt`, `MailRecordRepository.kt`, `MailSenderAccountService.kt`, `ImapMailReceiveServiceTest.kt`, `AutoMailReplyServiceTest.kt`, `OperatorStatusWriteSeamGuardTest.kt`; 862 insertions/89 deletions). `git show --stat e28da46` = `MailSenderAccountServiceTest.kt` only (1 file, 17 deletions, 0 insertions). Intermediate commits `a617dad`/`327bbbf`/`8de18f6`/`4a726f9` touch only `docs/plans/**`. Range `a15cb52..e28da46` product/test paths = exactly 11 files; no new files; `BatchAutoMailReplyServiceTest.kt` untouched. Working tree clean except the implementer-owned `children/c2/execution.md`.|
|Plan and invariants|PASS|I-1: `MailSenderAccountService.kt:48-50` owner-only list (`filter { it.inboundMailboxCode == null }`, `enabled` still unfiltered); `:61-62` `getAutoReceiveAccount` → `resolveInboundOwner` (`:67-70`), `getReceiveAccount` (`:53-58`) unchanged; all three poll entry points use the owner-only list (`BatchAutoMailReplyService.kt:24`, `MailQueueConsumer.kt:63`, `BounceCollectionScheduler.kt:33`); `AutoMailReplyService.kt:818-820,904-919` put fetch/`markSeen`/`cursor.get`/`advance`/bounce-monitor on `account.accountCode` (owner) and `:946` `processByUids` fetches as owner; attachment source is stamped by the fetch account (`ImapMailReceiveService.kt:301-306`). I-2: `AutoMailReplyService.kt:1394-1412` case-insensitive `senderEmail` match over `recipientAddresses`, unique hit wins, `size<=1` group keeps legacy routing (`:1390-1392`), else unique OUTBOUND `In-Reply-To` (`:1403-1412`); `MailRecordRepository.kt:146-160` `findOutboundCandidatesByMessageId` is `direction='OUTBOUND'`-only and list-returning; `:1420-1441` unresolved → one `MANUAL_REVIEW/RECIPIENT_UNRESOLVED` under the owner; routing runs before contact lookup (`:178-186`). I-3: `:117-121` physical-key check pre-transaction, `:123-131` `DataIntegrityViolationException` duplicate only when the exact physical key exists else rethrow, `require(uidValidity > 0)` fail-closed (`:112-116`), `findByMailboxOwnerCodeAndUidValidityAndImapUid` (`InboundMailProcessingRepository.kt:24-28`), group-wide legacy finder (`:39-50`), strict 3-field fingerprint with `uidValidity==0` gate and no backfill (`AutoMailReplyService.kt:162-174`, `:1489-1500`), both processing writers fill `mailboxOwnerCode` (`:1285`, `:1340`; repo-wide only these two `InboundMailProcessing(...)` constructions). I-4: `markSeen` only after the transaction returns (`:132-134`), failures leave UID unconfirmed and out of `handledUids` (`:899-911`, `:914-920`), alias cursors never read/advanced. A1 honoured exactly: the sole deletion is `MailSenderAccountServiceTest.kt:1059-1072` method `listAutoReceiveAccounts still returns disabled and shared inbox accounts`; siblings `:452`/`:465` intact, no other assertion in the file touched (file's whole-range diff = 17 deletions). Executed assertions: `AutoMailReplyServiceTest` shared-group suite `:2231,2266,2282,2296,2333,2351,2373,2392` (routing/ambiguity/BCC/two-member To+Cc/dup OUTBOUND), `:2422,2438,2454,2468,2494` (dedup/legacy fingerprint/unique-conflict propagation), `:2523` (alias backfill owner login, alias cursor never touched), `:2549,2568` (one poll per physical owner incl. independent account); `ImapMailReceiveServiceTest:569,580,604,618,650` (missing To/Cc, order+display-name strip, case preserved, nested `message/rfc822` To excluded, malformed header non-blocking); `OperatorStatusWriteSeamGuardTest.kt:100,104` pins mechanically shifted 612→628 / 669→685 (verified: `MailRecordRepository.kt:628` = `ec.operator_status AS operator_status`, `:685` = `ec.operator_status, ec.current_index_level`), exclusion text/rules unchanged.|
|Required commands|PASS|Fresh on HEAD `e28da46`, JDK 11: `mvn -B -Dtest=ImapMailReceiveServiceTest,AutoMailReplyServiceTest,BatchAutoMailReplyServiceTest,OperatorStatusWriteSeamGuardTest,MailSenderAccountServiceTest test` → `BUILD SUCCESS`, `Tests run: 181, Failures: 0, Errors: 0, Skipped: 0` (fresh surefire reports 19:06-19:07: Imap 28, Auto 70, Batch 21, MailSenderAccountService 61, guard 1); stale reports for other classes (Flyway 18:06, MVC 18:00, Bounce/SelfCheck 17:16) are prior-run leftovers and were not selected. `node --test src/test/js/*.test.js` → `tests 1121 / pass 1121 / fail 0 / suites 222` (also re-run inside `mvn test`'s exec plugin in the same build). Both match the recorded baselines and the report's counts exactly.|
|Downstream interfaces|PASS|`AutoMailReplyService.processSingle(account, received, skipImapAck)` is the single inbound entry point (`:97-134`): resolves the owner internally, physical-key dedup, then one transaction calling `processSingleCore`; every non-bounce/non-DMARC message in `receiveAndAutoReply` (`:895`) and `processByUids` (`:957`) goes through it, and manual rows are created only inside it (`unresolvedRecipientReview` `:1420`, `reviewManualOnly`, `CONTACT_NOT_FOUND`), so c3's group-wide `SELF_CHECK` filter can be inserted before that routing/business logic. `MailRecordRepository.findOutboundCandidatesByMessageId(messageId): List<MailRecord>` (`MailRecordRepository.kt:146-160`) is OUTBOUND-only and list-returning. `ReceivedMail.recipientAddresses: List<String> = emptyList()` (`MailReceiveService.kt:24-32`). `MailSenderAccountService.listAutoReceiveAccounts()` owner-only (`:48-50`), `getAutoReceiveAccount` (`:61-62`) / `getAutoReceiveAccountOrNull` (`:74-79`) resolve alias→owner, `getReceiveAccount` (`:53-58`) still returns the raw logical account, `resolveInboundOwner` (`:67-70`). Both processing-row writers fill `mailboxOwnerCode` (`:1285`, `:1340`). Names match c3's brief verbatim.|

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1 (c3 hand-off nuance, not a c2 violation): in the batch entry a `[self-check]` probe is still short-circuited *before* `processSingle` (`AutoMailReplyService.kt:847-853`, byte-identical to base `a15cb52` — `isSelfCheckProbe` at base line 799), so the batch path's probes never reach the single entry point even though the requirement's literal wording is "every non-bounce/non-DMARC inbound message". No business row is written on that path, and M-3/plan 03 owns the group-wide probe filter (`processByUids` probes *do* currently reach `processSingle`), so gate 2 stays PASS; c3 must not assume batch-path probes arrive at `processSingle` today.

### Required Action
- COMPLETE_CHILD
