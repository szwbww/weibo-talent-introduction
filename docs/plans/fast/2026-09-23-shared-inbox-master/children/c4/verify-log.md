# c4 verify-log.md

## Light Verification: LIGHT_PASS_WITH_NOTES
Child: c4 and docs/plans/2026-09-23/04-lukai-production-migration.md
Boundary: e77cb06..1cff8f6
Verifier: C4Verifier

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
|Authorized scope|PASS|`git show --stat 1cff8f6` → only `docs/runbooks/repair-lukai-shared-inbox.md` (+197). `git diff --name-status e77cb06..1cff8f6` → 8 files, all under `docs/` (c3 evidence, c4 `execution.md`/`window-checklist.md`, ledger, handoff, runbook); zero product/test file. Deployed `webapps/talent.war` sha256 `cbcca08c1cc9e8878e7b460f81ad77f5edacdb8b4b187c7bf16a32450c301d26` = expected, mtime Sep 23 21:34. `webapps/talent/WEB-INF/classes/db/migration/V134__shared_inbox_owner.sql` present, sha256 `a54c78f0fd731b8aa55462c12511a00cc27a82267718ea6e88e7cd3b7533835d` identical to repo `src/main/resources/db/migration/V134__shared_inbox_owner.sql`.|
|Plan and invariants|PASS|I-1: `mail_inbox_cursor` `LuKai`=1782107786/435 and `LuKai_QF`=1782107786/326 (same generation); `mail_sender_account` `LuKai_QF.inbound_mailbox_code`='LuKai' (was NULL), `enabled=0`, `daily_send_limit=100`, `today_sent_count=0`; MD5 over all non-target columns identical now vs `_bk_c4_20260923_acc` (`LuKai` 635f3c1c…, `LuKai_QF` decdcb7c…) → SMTP/IMAP/limits/enabled unchanged. I-2/I-3: group rows `LuKai MANUAL_REVIEW 27 / PROCESSED 109`, `LuKai_QF PROCESSED 2`; `[self-check]` MANUAL_REVIEW in group = **0**; id 383 absent; 366/369 present `PROCESSED` with `process_reason=reason_type='MANUAL_RESOLVED'`, `resolved_by='lk'`. 13 FK/soft-ref families scanned: only non-zero is `inbound_mail_tag.inbound_processing_id`=6 — rows 19–24 pointing at processing 78/79 created 2026-07-06, none in `_bk_c4_20260923_imp` (pre-existing, unchanged); 0 duplicates on `(mailbox_owner_code, uid_validity, imap_uid)`; `uid_validity=0 AND mailbox_owner_code IS NOT NULL` = 0. Snapshot cross-check: `_imp` 157 = 95 probes + 62 (35 dup deleted + 27 reassigned); of the 95 probes 0 still exist; of the 62 non-probes 27 still exist and all are `sender_account_code='LuKai'`; `_mr` 47 rows → 0 still `LuKai_QF`; `_probe3` 3 rows → 0 still exist. I-4: cursors as above (`LuKai_QF` last written 21:41:27, frozen); MAX(created_at) processing 18:40:06, mail_record 20:31:33; rows created ≥21:49:39 = 0/0. DDL: both columns `varchar(64) NULL`, `uk_inbound_mail_processing_owner_uid` on all 3 columns, 0 backfill. Runbook counts all reproduce exactly (imp 157 / mr 47 / att 6 / tag 31 / trf_all 57 / probe3 3 / grp 204 / acc 2; post-state `mail_attachment` 135, `mail_attachment_transfer` 72, `inbound_mail_tag` 397, `expert_document` 126, `inbound_intent` 233, `mail_record` 6963); backup `/root/db-backups/20260923-211757/talent_introduction.sql` 111,376,225 B sha256 `78fb471af3fed0da78d3f42dcb1dc94e1c9cbee257de66584fa8dc1d4063e910` exactly as recorded, restore DB `talent_introduction_restorecheck` present, no `flyway_schema_history`. Runbook documents commands (§2/§3/§8/§11), per-ID classification (§6 A 95 probes / B 35 dups / C 27 reassigned / D 47 mail_record), backup path+sha256 (§4) and rollback (§9).|
|Required commands|PASS|c4 has no build/test command of its own (docs-only change; zero product/test files in boundary). `curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1/talent/` → **200**. `task_execution`: 21:50:00 cycle has exactly **1** `AUTO_REPLY_ALL` (id 19543 SUCCESS 21:50:00→21:51:12); 21:40:00 has 2 (19541/19542) and every cycle 20:20→21:40 has 2 (the parallel stray context); no cycle after 21:50 as of server time 21:57:27. `webapps/` contains no `talent.dir.bak-20260922-manual-scope`; the directory is at `/opt/talent/backups/talent.dir.bak-20260922-manual-scope` and its `AutoMailReplyService.class` has 0 occurrences of `SELF_CHECK_IGNORED` (old code, corroborating runbook §8b).|
|Downstream interfaces|PASS (N/A for later children)|c1–c3 contracts still deployed: V134 byte-identical to repo and live in schema (columns + unique key), `SELF_CHECK_IGNORED` present in deployed `WEB-INF/classes/com/weibo/talentintroduction/mail/service/AutoMailReplyService.class` (also in `SinglePipelineOutcome.class`), owner-aware query `findByMailboxOwnerCodeAndUidValidityAndImapUid` present in the same class.|

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1: `docs/plans/fast/2026-09-23-shared-inbox-master/children/c4/execution.md` is modified in the working tree but uncommitted; the version committed at `1cff8f6` still reads `PAUSED_FOR_HUMAN`. The child's execution report (READY_FOR_VERIFICATION) therefore lives only in the worktree — fast-p bookkeeping, not a product/test file, so outside the four gates; the batch commit must include it.
- O-2: I-1's "same physical mailbox" evidence was verified only through DB state (`mail_inbox_cursor.uid_validity` = 1782107786 for both logins); no live IMAP login was performed because the read-only authorization list does not cover IMAP. The `_bk_c4_20260923_acc` column-level comparison independently confirms the account switch touched exactly the one target column.
- O-3: 6 orphan soft-reference rows (`inbound_mail_tag` 19–24 → `inbound_processing_id` 78/79, created 2026-07-06) pre-date c4 and are unchanged (runbook records 6→6); they are the sole non-zero family of the 13 scanned. No c4-caused orphan.
- O-4: Pre-existing production hygiene items already recorded in runbook §10 remain open (weak MySQL root password, plaintext credentials in `setenv.sh`, 1.05 GB `catalina.out` mixing three applications' output). Out of c4's scope and outside the light gate.

### Required Action
- COMPLETE_CHILD
