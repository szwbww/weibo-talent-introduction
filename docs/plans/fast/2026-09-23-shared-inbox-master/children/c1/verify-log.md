# c1 verify-log.md

## Light Verification: LIGHT_PASS_WITH_NOTES
Child: c1 and docs/plans/2026-09-23/01-shared-inbox-configuration.md
Boundary: aa47dde..a15cb52
Verifier: C1Verifier

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
|Authorized scope|PASS|`git diff --name-status aa47dde..a15cb52` = exactly the 10 authorized files (5 main Kotlin incl. V134 new, 2 static, 3 tests) and nothing else; `MailSenderAccountRepository.kt`, `styles.css`, `mailbox-chat.css` untouched. `docs/plans/fast/**` absent from the range (execution.md only dirty in the worktree, `children/c2/brief.md` untracked).|
|Plan and invariants|PASS|I-1: service `requireValidInboundMailbox` (`MailSenderAccountService.kt:273-303`) rejects self (`:279`), existing children-becoming-child (`:281-285`), missing principal (`:286-287`), simulator (`:288-290`), non-NULL owner i.e. chains/cycles (`:291-293`); `deleteAccount` guard `:177-183`; non-cascading self-FK in V134 (`:25-27`). Tests `MailSenderAccountServiceTest.kt:860,873,885,898,1009,1028,1045` (+`:942` asserts sender_email/SMTP/IMAP/limits/todaySentCount/enabled unchanged across a relation save); REST 400 proven by `MailSenderAccountControllerMvcTest.kt:188-204` (status 400, code `BAD_REQUEST`). Plan-required case-insensitive per-group e-mail check present (`MailSenderAccountService.kt:294-301`, group = `inboundMailboxCode ?: accountCode`) and proven discriminatorily by `MailSenderAccountServiceTest.kt:911` (`Alias@qftechtalent.com` vs `alias@qftechtalent.com` → rejected). I-2: V134 has only `ADD COLUMN`/`ADD CONSTRAINT`/`ADD UNIQUE KEY` (lines 21,25,29,33) — zero DML, no DEFAULT/backfill; IT `FlywayMigrationIntegrationTest.kt:1561` asserts both columns' name/type/length/nullability, FK, the new unique key, that `uk_inbound_mail_processing_uid_validity` survives, 0 non-NULL `inbound_mailbox_code`, `uid_validity=0` legacy row stays NULL, FK rejects dangling owner, two owners may share one UID, same owner+(uid_validity,imap_uid) rejected. I-3: `enabled` untouched on all write paths (no hunk before `service:74`); `listAutoReceiveAccounts` still `findAllByAccountCodeNot("SIMULATOR_NOOP")` returning disabled+shared-inbox accounts (`MailSenderAccountServiceTest.kt:1060`); copy exactly `启用此账号发信` (`index.html:1815`). S-1/S-2: fragment byte-identical to the plan at `index.html:1799-1803`, placed after `.form-section-pair` and before `<!-- 发送策略 -->` (`:1798` comment, `:1806` next fieldset), existing classes only, no new CSS/inline style, no CSS file edit; rendered in real Chromium: selector count 1, prev sibling class `form-section-pair`, next legend `发送策略`, computed fieldset `1px solid rgba(15,23,42,0.055)`/radius `10px`/padding `12px 14px 14px`, `.form-grid-1` `display:grid; gap:10px`, select `34px`/`13px`/`6px 10px`/radius `7px`, `style` attribute absent, enabled row class `checkbox-row`; `fillInboundMailboxOptions` (`app.js:3194-3227`) builds options via `createElement`/`textContent` only and a live call with a 4-account fixture excluded `SIMULATOR_NOOP`, the current account and an already-child account, echoed a stored owner, defaulted new mode to `独立收件箱（本账号）`. 11 cache keys: exactly 11 occurrences of `?v=20260923-shared-inbox-owner` (`index.html:11-15,2176-2181`), 0 remaining `20260922-discovery-continuous`, and no test pins either literal.|
|Required commands|PASS|Fresh on HEAD `a15cb52`, JDK 11: (1) `mvn -B -Dtest=FlywayMigrationIntegrationTest,MailSenderAccountServiceTest,MailSenderAccountControllerMvcTest test` → exit 0, `Tests run: 71, Failures: 0, Errors: 0, Skipped: 1` (MvcTest 8/0/0, ServiceTest 62/0/0, Flyway container skipped without `-DmigrationIt`); baseline equivalent subset was MvcTest 4 + ServiceTest 48 + 1 skipped = 53, so +18 tests and 0 regressions. (2) `DOCKER_HOST=unix:///Users/lukai/.orbstack/run/docker.sock mvn -B -Dtest=FlywayMigrationIntegrationTest -DmigrationIt=true -Dapi.version=1.40 test` → exit 0, `Tests run: 27, Failures: 0, Errors: 0, Skipped: 0` (baseline `mvn-flyway-it.txt`: 26 run / 17 failures, all `expected: <131> but was: <133>`; that log contains exactly 17 such failing assertions = the 17 line anchors in the brief). (3) `node --check src/main/resources/static/app.js` → exit 0 (baseline exit 0). (4) `node --test src/test/js/*.test.js` → exit 0, `tests 1121 / pass 1121 / fail 0 / suites 222`, identical to baseline. Current counts independently confirm the report's 27/0 claim.|
|Downstream interfaces|PASS|`MailSenderAccount.kt:22` `val inboundMailboxCode: String? = null` (after `countryName`); `InboundMailProcessing.kt:16` `val mailboxOwnerCode: String? = null` (after `senderAccountCode`); REST JSON property exactly `inboundMailboxCode` on response (`MailSenderAccountController.kt:106,238`) and on create/update request→command (`:139,161,184,209`), asserted end-to-end by `MailSenderAccountControllerMvcTest.kt:114-136,163-185`; migration file/version exactly `V134__shared_inbox_owner.sql` (V133 was the base maximum — `git ls-tree aa47dde` shows no V134); DDL exactly `mail_sender_account.inbound_mailbox_code VARCHAR(64) NULL` + FK to `account_code` (unique at `V1:3`) and `inbound_mail_processing.mailbox_owner_code VARCHAR(64) NULL` + unique key `(mailbox_owner_code, uid_validity, imap_uid)`; service entry points unchanged in shape — `createAccount`/`updateAccount`/`deleteAccount` (`service:74,99,175`) and the read side `listAccounts:22`, `getAccount:36`, `listAutoReceiveAccounts:43` all sit before the first diff hunk (`@@ -84,7 +84,16 @@`), so no signature or read-path change; whole project + tests compiled under JDK 11 in command (1).|

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1 Two Flyway IT repairs beyond the literal pin update, judged **not** a four-gate violation (`FlywayMigrationIntegrationTest.kt:439-449` and `:603-609`): the V131 test's `historyBefore + 1` delta is now bound to the single V130→V131 step (`flyway(MigrationVersion.fromVersion("131"))`) instead of "V131 is latest", and the V124 test now starts from the file's existing `migrateToV23AndSeedBase()` so `expert_application_promotion.expert_contact_id=1` satisfies the pre-existing FK. Both files are authorized, both repairs are uniquely determined by the plan's "the three commands must exit 0", and neither test's behavior target or assertion set was weakened (17/17 latest-version pins moved `131→134`, `count134=18` = 17 bumped pins + the new V134 test; all other targets unchanged), so the plan's "历史定点测试保持原目标" clause holds — fresh 27/0 confirms.
- O-2 The report's browser evidence could not be captured as a PNG (`page.screenshot` protocol timeout). Gate evidence is nevertheless complete: I re-rendered the shipped `index.html` in managed Chromium and independently confirmed the S-1 fragment, placement (after the SMTP/IMAP pair, before `<!-- 发送策略 -->`), computed geometry, absence of inline style/new classes, the exact S-2 copy and the candidate-list behavior; only the human A-3 visual sign-off remains.
- O-3 `app.js:3203` hardcodes the literal `"SIMULATOR_NOOP"` (duplicating `MailSenderAccountService.SIMULATOR_ACCOUNT_CODE`); this matches the file's existing convention (`app.js:3278` does the same for the delete button) and is style/maintainability only, outside the four gates.

### Required Action
- COMPLETE_CHILD
