## Light Verification: LIGHT_PASS_WITH_NOTES
Child: 02 — docs/plans/2026-09-09/02-mailbox-refinement-frontend.md (child plan, contract; brief: docs/plans/fast/mailbox-refinement/children/02/brief.md)
Boundary: cf257779ae420ab4c745b20aa4de6e6942a66b18..1dd2e53f33d5817c9340c44fe733c308db8d5bea
Verifier: Verifier02

### Four Gates (table)
| Gate | Result | Evidence |
|---|---|---|
| 1. Authorized files | PASS | `git diff-tree --name-only -r cf257779..1dd2e53` — product/test files are exactly the 7 authorized: `src/main/resources/static/{index.html, app.js, mailbox-chat.js, mailbox-chat.css}`, `src/test/js/{mailboxChatBehavior.test.js, mailboxChatStyle.test.js, mailboxInboundTags.test.js}`. Implementer commit `1dd2e53 feat(fast-p): implement 02` itself touches exactly those 7 files (`git show --stat`); remaining boundary deltas are `docs/plans/fast/**` evidence from controller commit b6a1061 (excluded from product scope). Worktree dirty only under `docs/plans/fast/mailbox-refinement/**` (brief/execution/ledger); no product file dirty. |
| 2. Requirements & invariants | PASS | Direct code/test evidence for I-1..I-8, T1..T5, S-1..S-7, acceptance matrix — citations below. `mailbox-chat.css` byte-identical to S-6 contract `docs/plans/2026-09-09/mailbox-refinement-evidence/mailbox-chat.target.css` (cmp identical; 17894/17894 bytes). |
| 3. Commands run freshly | PASS | (a) `node --check mailbox-chat.js` exit 0; (b) `node --check app.js` exit 0; (c) trio tests 65 pass / 0 fail; (d) full JS gate 765 pass / 0 fail (baseline 733 → 765, matches child report); (e) `JAVA_HOME=…zulu-11 mvn test` BUILD SUCCESS 02:39, surefire XML totals tests 3255 / failures 0 / errors 0 / skipped 9 (child report 3255/0/9 — exact match), exec-plugin node 765 pass. |
| 4. Child-03 interfaces | PASS | index.html:11-13 and :2109-2112 — styles.css / expert-materials.css / mailbox-chat.css / trust-reply-workbench.js / expert-materials.js / mailbox-chat.js / app.js all still registered with `v=20260907-material-chat` (unchanged; child 03 owns the flip). The 7 fixed-value JS tests depending on the literal pass inside the 765-test full run. `mailbox-chat.css` content = S-6 evidence verbatim (the file child 03 will not modify). |

Invariant evidence (file:line; mailbox-chat.js = mcb, mailbox-chat.css = mcc, mailboxChatBehavior.test.js = beh, mailboxChatStyle.test.js = sty, mailboxInboundTags.test.js = tags):

- **I-1 tabs/server order**: `chipParams` mcb:195-199 — all→`{}`, followed→`{followed:true}`, pending→`{pendingOnly:true}`; URL assembly mcb:985-987 sends only those; zero `waitingReply` sends in static tree (only comment mcb:11). External onlyPending initializes once, tab authoritative after user touch (mcb:303-304, 3302-3310); full snapshot replace, no merge (mcb:3317-3319). mark-resolved re-queries server list with empty-page fallback mcb:1925-1972; editor/draft preserved. No client sort (0 `.sort(`), no per-message thread GET (0 matches). Tests: beh:1190 (three-tab params, never waitingReply), beh:1219 (onlyPending init once), beh:1610/1650 (mark re-query + empty-page fallback).
- **I-2 single filter source**: app.js:14384-14400 snapshot maps recipientEmail + keyword (no keyword→subject), full snapshot replace mcb:3317-3319; draft semantics — field change no request, ×/Escape restores effective values, Enter applies, reset clears advanced filters and keeps tab+q (beh:1241, 1296, 1316, 1332); 7 unique `mailboxFilter*` ids + `mailboxSearchBtn` exactly once in index.html source and retained inside `#mailboxLegacyToolbar` for task drill-down (sty source-text suite); legacy toolbar non-chat behavior restored on unmount (beh:1369/1399; app.js early-return guards for chat mode).
- **I-3 tags identity**: tags read inline from timeline items — `renderTagRow` mcb:1592-1603 uses `message.tags`; no per-message `/thread` call; DELETE by tagId with local window update + quiet re-verify + list refresh, failure keeps chips with inline error (mcb:2004-2029; beh:1466 same-numeric-id different-source, beh:1514 delete-failure). New-chat tag modal via host adapter `{inboundId, source:"INBOUND_PROCESSING", contactId, onTagsChanged}` (mcb:1972-1997), old 来信汇总/详情 branch unchanged when no adapter, adapter cleared on success/close/unmount (app.js:14359-14362; tags adapter suite incl. POST-return tags direct display; beh:1527).
- **I-4 translate**: click-only POST `/api/translate` (mcb:2051-2086), idle/loading/ok/error state machine; collapse+reopen zero requests (ok branch); output `escapeText` mcb:1612; failure label 翻译失败，重试; empty body → no translate button (mcb:1529-1531); `cleanedBody`-else-`body` display (mcb:1490-1494), body escaped (mcb:1510-1511); translations map reset per contact load (mcb:1278-1279). Tests beh:1546 (one click one request / reopen zero / empty-body), beh:1573 (retry; manual DOM not rebuilt).
- **I-5 bounded cache**: constants mcb:39-41 (`MESSAGE_LIMIT=50`, `MESSAGE_CACHE_LIMIT=500`, `SESSION_CACHE_LIMIT=10`); module Map keyed `user|accountScope|contactId` mcb:218-222; LRU eviction >10 mcb:269-280; >500 loaded messages → drop record, next entry locates latest mcb:1847-1851; no localStorage bodies (only operatorName read mcb:96); saves on scroll (180ms debounce)/selectExpert/unmount/loadOlder/quiet; anchor + fallback scrollTop with 0-valid semantics (rec fields mcb:247-262). Async guards: convEpoch/seq captured per select (mcb:1258-1261), all callbacks guard epoch (mcb:1294, 1322, 2822, 2845, 3271); loadOlder busy flag + merge dedupe. Tests: beh:2031 (late A after B select never writes into B), beh:2063 (quiet merge source:id server-wins), beh:2100 (loadOlder busy, no jump), beh:2135 (scrollTop 0 valid, accountScope/user isolation), beh:2178 (latest button no mark/send), beh:2196 (60-msg first visit in valid scroll range).
- **I-6 manage overlay**: portal div `.mail-chat.mc-overlay-root` appended to `document.body` (mcb:2281-2302), overlay `role=dialog` z-index 990 (S-6 `mcc .mc-manage-overlay{position:fixed;inset:0;z-index:990…}` on its own body child — never inside `.mc-scroll` or backdrop-filter `.panel`); options from `window.operatorStatusOptions/indexLevelOptions` (mcb:163-187); cancel/close zero requests, save only changed fields through the two existing endpoints with per-endpoint result collection + re-read, partial failure surfaced honestly, button restored (beh:1720, 1741, 1761); expert tags via shared `fetchExpertTagsFromEs/renderMailboxExpertTagEditor/mutateExpertTag` immediate-save with visible hint; missing-profile unavailable message (beh:1785, 1818); overlay closes on expert switch (beh:1836); portal root removed on unmount (mcb:3335+ restore/remove).
- **I-7 production preserved**: workbench host `mcHostMountWorkbench` fixed mode `"LIVE"`, source `{sourceType:"LIVE_INBOUND", sourceId: processingId}`, autoBootstrap false (app.js:14467-14476); mounted lazily on expand with unmount of previous instance on expert switch (mcb:2104-2124; beh:1858); adopt → `mcHostSendRichReply` with QA payload intact (mcb:2786-2789; beh:1918); drafts preserved across expert switch/unmount, new inbound prompts target confirm and never silently replaces edited draft (beh:1891, 1945); materials still `mcHostOpenMaterials` (mcb:2651-2654; beh:2017/2334 drawer contract); task drill-down keeps legacy list (beh:2319), script-missing full legacy fallback (beh:2326).
- **I-8/S-7 expert-tag row**: `renderPerson` mcb:1005-1036 — heading strong + counts + tags; reads only `summary.expertTags` via `personTagNames` with `expertTagLabels` display mapping and raw-escape fallback (mcb:149-154); `null` → single grey line 标签暂不可用 with same title (mcb:1017-1018), `[]` → no placeholder (mcb:1019); per-tag spans escaped, `title="专家标签：…"` full untruncated list, aria-label carries full names (mcb:1019-1022); no waiting/pending badge anywhere in the row markup; tags refresh after manage add/delete via silent list/header refresh guarded by contactId. Tests beh:1150 ([]/null, no waiting|pending badge), beh:1176 (special-char raw values escaped, title complete); style S-7 rules (single-line flex/ellipsis, counts flex:none, 20px line, 10px chips, unavailable class) asserted in sty S-7 suite; CSS rules present verbatim at the S-7 tail of mcc.
- **S-6 CSS**: byte-identical (cmp; wc 17894 both).
- **Cache keys untouched**: index.html:11-13, 2109-2112 still `v=20260907-material-chat`.

### AUTO_FIX (F-id list or N/A)
N/A — no proven four-gate violation.

### RECORD_ONLY (O-id list or N/A)
- O-1 (I-4, informational): translation cache is keyed per message by `${source}:${id}` with the body text stored in state and equality enforced at response-write time (mcb:1496-1503, 2058-2080), not as an open-time cache key including the body. All observable I-4 contracts hold and are tested (one click one request, reopen zero requests, retry, escape, empty-body no button, editor identity preserved); a stale display would require a server-side body mutation for an identical source:id, which this system cannot produce (mail content immutable; only tags/status mutate). No action.
- O-2 (T4/A-10/A-11): real-browser screenshot acceptance (1440×900/1920×1080/760/390) is deferred to human acceptance per master plan/brief; this verification used machine proofs only (byte CSS compare + DOM/source-text assertions). No fabricated browser evidence. No action.
- O-3 (T5 edge rows): S-7 0/1/10-tag rows, >60-char single labels, narrow-275px pane line-height and full hover text are human A-11 acceptance items; mini-DOM behavior tests cover the code-path semantics ([]/null, escape, full title/aria, no badge) at beh:1150/1176. No action.

### Required Action
- COMPLETE_CHILD
