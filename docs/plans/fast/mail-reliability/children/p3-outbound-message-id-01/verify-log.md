# Light Verification Log — p3-outbound-message-id-01 (fast-p)

## Light Verification: LIGHT_PASS_WITH_NOTES
Child: p3-outbound-message-id-01 — `docs/plans/2026-08-06/outbound-message-id-01-fill-missing.md`
Boundary: 33e1ffb..025b875
Verifier: VerP3
Date: 2026-08-06

### Four Gates

| Gate | Result | Evidence |
|---|---|---|
| 1. Authorized scope | PASS | Implementer commit `025b875` (`git show --stat 025b875`) = exactly 10 files, +200/-8, matching brief authorized list 1-10: `OutboundMessageIdFactory.kt` (new, 23), `MeetingInvitationMailComposer.kt` (+2/-1), `AutoMailReplyService.kt` (+4/-2), `MeetingScheduleService.kt` (+3/-1), `OutboundMessageIdFactoryTest.kt` (new, 64), `MeetingInvitationMailComposerTest.kt` (+9), `AutoMailReplyServiceTest.kt` (+52/-1), `MeetingScheduleServiceTest.kt` (+17/-1), `K-message-id-fingerprint.md` (+8/-2), `K-outbound-message-id-single-factory.md` (new, 18). `git diff --name-only 33e1ffb..025b875 | grep -E 'IntroductionMailComposer|ManualExpertMailService|ManualInitialOutreachService|ManualReplySendAttemptService|SmtpMailDeliveryService|PendingMailOperationService|migration|Migration'` → **NONE of the forbidden files changed anywhere in the boundary**. Boundary includes 4 extra doc-only files from intermediate commit `92b13aa` (p1 verification record: p1-expert-profile-absence/execution.md, fix-log.md, verify-log.md, ledger.md) — controller bookkeeping, not this child's implementation (child commit itself is exactly the 10 authorized files). |
| 2. Plan and invariants | PASS | I-1: all 4 construction points explicitly set `messageId = OutboundMessageIdFactory.newId(...)` — MeetingInvitationMailComposer.kt:26, AutoMailReplyService.kt:573, AutoMailReplyService.kt:963, MeetingScheduleService.kt:130. I-2: factory (`OutboundMessageIdFactory.kt` full text) uses `senderEmail.substringAfter("@", "")` + `require(domain.isNotBlank())`; `grep -iE 'weibo\.com|talents\.szwebotech\.cn|hostname|InetAddress'` over factory → NONE; `require(kind.isNotBlank())` + `require(KIND_PATTERN.matches(kind))` (`^[a-z-]+$`) + `require(discriminator.isNotBlank())` — all four fail-fast paths present; all 4 construction points pass `account.senderEmail` of the actual sending account (each construction immediately precedes `mailDeliveryService.send(account, ...)`; MeetingInvitationMailComposer's `account = mailSenderAccountService.getEnabledAccount(accountCode)`). I-3: format `<{kind}-{discriminator}-{UUID}@{domain}>` matches `^<...-[0-9a-f-]{36}@...>$`; 1000-uniqueness test present (`newId is unique across 1000 calls`); factory exposes only `newId` (no reverse-parse method); no `startsWith`/prefix-parse of kind literals anywhere in main or test code (`grep 'startsWith\("(meeting-invitation|auto-reply|meeting-confirmation|intro-|reminder-|manual-)'` → none; kind literals appear only as generator args in the 4 construction points + pre-existing Intro/Reminder/manual-* literals outside this diff). I-4: `ManualReplySendAttemptService.kt` not in boundary (zero diff, `git diff --name-only` NONE). I-5: `IntroductionMailComposer.kt` + `ManualExpertMailService.kt` zero diff in boundary (NONE). IP-4: MeetingInvitationMailComposerTest asserts `^<meeting-invitation-0000-0001-...>`, AutoMailReplyServiceTest meeting branch asserts extracted kind `== "meeting-invitation"` AND full regex `^<meeting-invitation-ORCID-11-...>` — same kind literal on both paths, discriminator = ORCID both (`expert.orcidId` / `contact.orcidId`). M-2: factory `grep 'Normalizer|normalize'` → none; no shared constants (only private KIND_PATTERN inside factory). M-3: no migration files in boundary. M-4: AutoMailReplyService.kt diff = exactly 2 hunks (the :567/:958 ComposedMail constructions only; `mailTemplateVariables()` body untouched). M-5: `K-message-id-fingerprint.md` diff = front-matter `created`/`last_used` bump (2026-07-06→2026-08-06) + appended 「2026-08-06 二次复验修正」 section only (5→4, PendingMailOperationService 域名问题, ManualExpertMailService 3bff469 已修复); appended not overwritten; 末段 P2-correction (库内值≠实际投递值) NOT present (`grep '投递值\|不一致'` → none in the new section; original body preserved); `K-outbound-message-id-single-factory.md` exists with the 4 rules (唯一入口/域名取自本次投递账号/唯一性只依赖 UUID/同类邮件同 kind) + `[[K-vendor-message-id-prefix.md]]` link. |
| 3. Required commands (fresh, cwd=worktree, JDK 11) | PASS | All run fresh 2026-08-06, `JAVA_HOME=/Library/Java/JavaVirtualMachines/zulu-11.jdk/Contents/Home` (zulu 11.0.15 verified): ① `mvn test` exit 0 — Kotlin surefire aggregate `Tests run: 2169, Failures: 0, Errors: 0, Skipped: 4`, JS `tests 466, pass 466, fail 0` (baseline d911bd6 JS 459 → +7; matches implementer claim exactly). ② `mvn test -Dtest=OutboundMessageIdFactoryTest` exit 0 — `Tests run: 7, Failures: 0, Errors: 0` (implementer claimed 7 ✓). ③ `mvn test -Dtest=MeetingInvitationMailComposerTest` exit 0 — `Tests run: 1, Failures: 0, Errors: 0` ✓. ④ `mvn test -Dtest=AutoMailReplyServiceTest` exit 0 — `Tests run: 40, Failures: 0, Errors: 0` ✓. ⑤ `mvn test -Dtest=MeetingScheduleServiceTest` exit 0 — `Tests run: 5, Failures: 0, Errors: 0` ✓. ⑥ `mvn clean package` exit 0 — `Tests run: 2169, Failures: 0, Errors: 0, Skipped: 4`, BUILD SUCCESS. ⑦ `git diff --check 33e1ffb..025b875` exit 0 silent; `git diff --check` (worktree) exit 0 silent. All targeted-test counts match implementer claims exactly (7/1/40/5); full-suite counts match (Kotlin 2169/0/0/4, JS 466/0). |
| 4. Downstream interfaces | PASS | P2's MessageIdNormalizer must stay format-agnostic: this child adds NO kind-string parsing/matching into shared code — kind literals appear only as `OutboundMessageIdFactory.newId(...)` generator args at the 4 construction points (and in test assertion regexes, which are child-test-local). Factory is write-side-only, has no reverse-parse, references no normalizer (grep none). No shared constants introduced (M-2). Pre-existing format-agnostic `MailMessageIdNormalizer` (angle-bracket/whitespace strip only) untouched by this diff. `K-outbound-message-id-single-factory.md` carries forward link `[[K-vendor-message-id-prefix.md]]` for P2 to create and link back; `K-message-id-fingerprint.md` 末段 P2-correction correctly NOT written by this child. Plan's IP-1 (DeliveredMail.messageId equals composed value via SmtpMailDeliveryService override branch) is not a brief gate; that mechanism has pre-existing coverage in `SmtpMailDeliveryServiceTest` (messageId="msg-1" → override branch captured), and `SmtpMailDeliveryService.kt` is zero-diff — no regression surface. |

### AUTO_FIX
N/A — no gate violations found; no correction needed.

### RECORD_ONLY
1. Implementer deviation (1) — `AutoMailReplyServiceTest` IP-4 kind-extraction regex (`Regex("^<([a-z-]+)-").find(...).groupValues[1]` then `assertEquals("meeting-invitation", kind)`): still proves the IP-4 same-kind assertion (kind == "meeting-invitation" asserted explicitly, plus full-format regex `^<meeting-invitation-ORCID-11-[0-9a-f-]{36}@qftechtalent\.com>$` and `MeetingInvitationMailComposerTest` pins the same kind literal). Test-local parse of a freshly generated id, not production logic; I-3 production no-parse rule intact. No action.
2. Implementer deviation (2) — `MeetingScheduleServiceTest` Mockito captor replaced with `thenAnswer` capture into `sentMails` list: still proves the I-1/I-2 domain-from-account assertion (`accountDomain = account.senderEmail.substringAfter("@")`, regex `^<meeting-confirmation-orcid-1-[0-9a-f-]{36}@${Regex.escape(accountDomain)}>$` — domain derived from stub account, not hardcoded). Original assertions (mailType MEETING_CONFIRMATION, status) retained. No action.
3. Boundary 33e1ffb..025b875 includes 4 doc-only files from intermediate commit `92b13aa` (p1-expert-profile-absence verification record + ledger.md) — controller bookkeeping; the child implementation commit 025b875 itself is exactly the 10 authorized files. `ledger.md` additionally has an unstaged controller update in the worktree (p1→LIGHT_PASS_WITH_NOTES, p3 row LIGHT_VERIFYING) — not part of this child's commit. No action.

### Required Action: COMPLETE_CHILD
