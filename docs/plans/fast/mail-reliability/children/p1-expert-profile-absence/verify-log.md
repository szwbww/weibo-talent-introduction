# Light Verification Log — p1-expert-profile-absence (fast-p)

## Light Verification: LIGHT_PASS_WITH_NOTES
Child: p1-expert-profile-absence — `docs/plans/2026-08-06/expert-profile-absence-not-error.md`
Boundary: f2916674..33e1ffb
Verifier: VerP1
Date: 2026-08-06

### Four Gates

| Gate | Result | Evidence |
|---|---|---|
| 1. Authorized scope | PASS | `git diff f2916674..33e1ffb` product code (src/) = exactly 6 files: `ExpertIndexController.kt` (+4/-6), `app.js` (+39/-18), `expertProfileAbsence.test.js` (new, 148), `expertTagBatchFix.test.js` (+7), `mailboxInboundTags.test.js` (+1/-1), `ExpertIndexControllerTest.kt` (+23). Matches brief authorized list 1-6 and implementer claim. No styles.css, no db/migration, no mail-side file, no `ExpertSearchService.kt`/`AiReplyContextService.kt` (`grep -iE "styles\.css\|migration\|db/\|mail/"` over boundary names → NONE). Boundary also contains 11 doc files (briefs/ledger/plan updates) = controller bookkeeping (e704a58/9bbb046), excluded from gate 1 per instructions. `mailboxInboundTags.test.js` :84 stub changed bare array → `async () => ({ found: true, tags: ["自动晋升"] })`, assertions untouched — exactly the uniquely-determined arbitration repair. Arbitration trail verified in full: master plan M-1 matrix row (00-main-plan-mail-reliability.md:57), child plan 变更文件清单 row 6 + M-1 arbitration note, brief item 6, arbitration commit 9bbb046 (touches master plan, child plan, brief, execution.md, ledger). |
| 2. Plan and invariants | PASS | I-1: `getExpertProfile` 404 throw removed; returns `ExpertProfileTagsResponse(orcidId = orcidId, found = profile != null, tags = profile?.tags.orEmpty())`; no try/catch; `require(orcidId.isNotBlank())` retained; `fetchExpertTagsFromEs` no try/catch. I-2: no APPLICATION→CANDIDATE fallback added anywhere in diff; S-1 DOM preserves `data-level` (sole level source for add/remove). I-3: S-1 branch contains no `data-action="expert-add-tag-open"`/`expert-remove-tag`; `handleContactAction` guard `if (existing.found === false) { showStatus("该专家在 ES 中无画像文档，标签功能不可用", "warn"); return; }` before `openExpertTagAddDialog`. I-4: all comparisons `found === false` (app.js:6616, 6980, 8359, 8787, 9351); `profile?.found !== false` maps undefined/null → has-profile; zero `if (!found)` (grep NONE). I-5: both listeners end with `.catch((error) => showStatus(error.message, "error"))` — app.js:10525 (`await showUnmatchedDetail(...).catch(...)`) and 11510 (`handleUnmatchedAction(target).catch(...)`), verbatim shape of reference :11034; profile-missing now returns 200 found=false so the panel renders with the degraded tag section (fetch failure surfaces via the .catch status bar, matching plan task 2.6 prescription and requirement 3). S-1: rendered DOM matches plan block verbatim incl. `data-profile-missing="true"`, `class="muted"`, exact notice text, no actions div (only diff vs plan block = `escapeHtml()` wrappers, consistent with S-2 baseline). S-2: has-profile branch untouched (diff inserts only the `profileMissing === true` block before `const chips`). All 5 consumption points take `.tags`; `mutateExpertTag` uses `refreshed.tags` (:4056-4057) per IP-3; `ExpertProfileTagsResponse.found` declared with no default value. Task 1.3 tests: existing test gains `assertEquals(true, response.found)`; new absent-profile test (found=false, empty tags, orcidId=input, no throw); new exception-propagation test (assertThrows). Task 3.1: `createTagFetchSandbox` api stub → `{ found: true, tags: [] }`, assertions adapted to `result.tags` + `result.found`. Task 3.2 new test file covers S-1 verbatim (7 assertions), S-2 verbatim, undefined-found → present-profile, api throw → rethrow (never `{found:false}`), styles.css class existence (.muted/.inbound-tag-editor-chips/.expert-tag-editor/.inbound-tag-editor-head). |
| 3. Required commands (fresh, cwd=worktree, JDK 11) | PASS | All run fresh 2026-08-06, `JAVA_HOME=/Library/Java/JavaVirtualMachines/zulu-11.jdk/Contents/Home`: ① `mvn test` exit 0 — Kotlin `Tests run: 2161, Failures: 0, Errors: 0, Skipped: 4` (surefire aggregate), JS `tests 466, pass 466, fail 0` (baseline d911bd6 was 459 pass → +7 = new test file exactly). ② `mvn test -Dtest=ExpertIndexControllerTest` exit 0 — `Tests run: 18, Failures: 0, Errors: 0, Skipped: 0`. ③ `node --test src/test/js/expertProfileAbsence.test.js` exit 0 — `pass 7, fail 0`. ④ `node --test src/test/js/expertTagBatchFix.test.js` exit 0 — `pass 33, fail 0`. ⑤ `node --check src/main/resources/static/app.js` exit 0, no output. ⑥ `mvn clean package` exit 0 — JS 466 pass/0 fail. ⑦ `git diff --check` exit 0, silent (working tree incl. unstaged doc edits, `--cached`, and `git show 33e1ffb --check` on committed product code all silent). All counts match the implementer's claims exactly. |
| 4. Downstream interfaces | PASS (N/A with note) | M-1 ownership matrix (00-main-plan-mail-reliability.md:32-63): `ExpertIndexController.kt` → P1 only; `static/app.js` → P1 only ("四份中唯一的前端文件"); `src/test/js/mailboxInboundTags.test.js` → P1 (arbitrated); each plan owns its own test files — zero cross-child ownership of any P1 file; P2/P3/P4 consume none of these. Knowledge write-back: N/A — child plan has no Phase 6 section (last section 关联缺陷移交 at :403); brief confirms "P1 无 Phase 6 节". |

### AUTO_FIX
N/A — no gate violations found; no correction needed.

### RECORD_ONLY
1. `src/test/js/mailboxInboundTags.test.js` :84 stub change (`async () => ["自动晋升"]` → `async () => ({ found: true, tags: ["自动晋升"] })`) — outside the child plan's original 变更文件清单 but authorized by M-1 arbitration 2026-08-06 (master plan matrix row + child plan row 6 + brief item 6 + commit 9bbb046); verified consistent with the uniquely-determined repair (IP-3), test assertions unchanged, and the file is in the commit 33e1ffb authorized set. No action.
2. Boundary f2916674..33e1ffb includes 11 doc-only files (child briefs/execution.md/ledger.md/plan updates from commits 92a678b/e704a58/9bbb046) — controller bookkeeping, excluded from gate 1 product-scope check by instruction. No action.

### Required Action: COMPLETE_CHILD
