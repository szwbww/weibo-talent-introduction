## Light Verification: LIGHT_PASS
Child: p2-inbound-message-id-prefix — docs/plans/2026-08-06/inbound-message-id-vendor-prefix.md
Boundary: 025b875..ef7e471
Verifier: VerP2
Date: 2026-08-06

### Four Gates (table: Gate | Result | Evidence)

| Gate | Result | Evidence |
|---|---|---|
| 1. Authorized scope | PASS | Commit `ef7e471` (p2 implementation) touches EXACTLY the 8 authorized files: `MessageIdNormalizer.kt` (new), `UnmatchedInboundMailService.kt`, `BounceCollectionService.kt`, `MessageIdNormalizerTest.kt` (new), `UnmatchedInboundMailServiceTest.kt`, `BounceCollectionServiceTest.kt`, `K-message-id-fingerprint.md`, `K-vendor-message-id-prefix.md` (new). `git show --name-only ef7e471` = 8 files, +366 -4 (matches implementer claim). No SmtpMailDeliveryService.kt / ImapMailReceiveService.kt / ComposedMail construction point / MailRecordRepository.kt / OutboundMessageIdFactory.kt / migration (`V*__`/flyway) / frontend file present in the commit. Boundary 025b875..ef7e471 additionally contains 4 files from the sibling docs-only commit `68536d8` (p3 verification record: p3 execution.md/fix-log.md/verify-log.md, ledger.md) — not part of p2's commit; expected boundary composition, not a p2 scope change. |
| 2. Plan and invariants | PASS | I-1: `VENDOR_PREFIX = Regex("^[0-9A-F]{16}\\+")` exact; `stripVendorPrefix` strips only that from local-part (first `@` split; empty-local-part guard returns input); no format assumption after strip; grep of MessageIdNormalizer.kt for `intro-|reminder-|manual-outreach-|manual-rich-` = no matches. I-2: diff contains no LIKE/Containing/EndingWith/StartingWith on mail records, no new repository method, no repository file in diff; both call sites use `MessageIdNormalizer.candidatesFor(...).firstNotNullOfOrNull { mailRecordRepository.findByMessageId(it) }` (exact per candidate). I-3: repo-wide grep — symbol `MessageIdNormalizer` appears only in `UnmatchedInboundMailService.kt:78`, `BounceCollectionService.kt:137`, `MessageIdNormalizerTest.kt`; pre-existing distinct symbol `MailMessageIdNormalizer` untouched. I-4: candidatesFor = [raw.trim, canonicalize, stripVendorPrefix(canonicalize)], blank-filtered, `distinct()` in order (source verified); test asserts order+dedup (e.g. prefixed reminder → `[raw, <reminder-…>]`, unprefixed → single element); call-site test verifies raw candidate queried first (mock verify `findByMessageId(prefixed)` then `findByMessageId(stored)`); pre-existing exact-hit test (`out-msg-1`) proves raw-priority behavior unchanged. I-5: `IN_REPLY_TO` block `reason="IN_REPLY_TO", confidence=90` verbatim, NAME_OR_EMAIL_MATCH/EMAIL_SIMILARITY sections byte-identical (diff = 1 hunk, 1+1 line), `?: signal.failedRecipient?.let { … }` fallback verbatim (diff = first branch only); NOID: falls through (test asserts `NOID:deadbeef` → no original contact, failedRecipient path). M-2: zero reference between MessageIdNormalizer and OutboundMessageIdFactory, no kind recognition, no shared constants. M-5: K-message-id-fingerprint.md contains P3's 二次复验修正 (缺失数 5→4, PendingMailOperationService 域名问题, ManualExpertMailService 已修复 by 3bff469) AND the new 末段 correction (三次复验修正, 落库值证伪) — both coexist; K-vendor-message-id-prefix.md (new) has the two samples, the two read paths, read-side bounded-strip + exact-equality rule, and `[[K-outbound-message-id-single-factory.md]]` link. Note: `created: 2026-08-06` unchanged baseline→head — bump is a date no-op (file created same day); substance (correction appended) satisfied. |
| 3. Required commands | PASS | Freshly run, cwd=worktree, JAVA_HOME=zulu-11 (java 11.0.15): full `mvn test` exit 0, `Tests run: 2187, Failures: 0, Errors: 0, Skipped: 4` (matches implementer claim exactly; baseline recorded as PASS at d911bd6 — see RECORD_ONLY note on count granularity); `-Dtest=MessageIdNormalizerTest` exit 0, 13/0/0/0; `-Dtest=UnmatchedInboundMailServiceTest` exit 0, 10/0/0/0; `-Dtest=BounceCollectionServiceTest` exit 0, 4/0/0/0; `mvn clean package` exit 0, BUILD SUCCESS, 2187/0/0/4; `git diff --check 025b875..ef7e471` and worktree `git diff --check` both silent (exit 0, 0 lines). All counts match implementer claims. |
| 4. Downstream interfaces | PASS | No downstream (last child). J-1 cross-plan scenario explicitly out of scope (human review item). The 11-case test matrix fully present: all 11 rows covered in MessageIdNormalizerTest (reminder+strip, javamail-default suffix+strip, unprefixed single candidate, lowercase hex no-strip, len-6 no-strip, len-17 no-strip, local-part `+` no-strip, unbracketed raw+bracketed, empty-local-part no-strip, null/blank empty, multi-msgid first segment), plus 2 extra tests (canonicalize trims, stripVendorPrefix unbracketed) = 13 tests as run. |

### AUTO_FIX
N/A — no gate violation found; no AUTO_FIX needed.

### RECORD_ONLY
1. Baseline test-count granularity: recorded baseline at d911bd6 is "JS 459/0"; fresh full run (and implementer claim) shows 2187/0/0/4. Both PASS exit 0; 459 vs 2187 is presumed a different measurement scope at baseline, not caused by p2. Not a failure; controller may reconcile baseline wording.
2. `created` on K-message-id-fingerprint.md was not literally bumped (remains 2026-08-06); since that equals today's date, the re-validated bump is a no-op. Substance of M-5 (correction appended, P3 corrections preserved) satisfied.
3. Boundary includes sibling commit 68536d8 (p3 verification record, docs-only, 4 files) — outside p2's authorized list but not part of p2's commit; flagged for transparency only.
4. Worktree contains unstaged `docs/plans/fast/mail-reliability/ledger.md` modification and untracked p2 `execution.md` (controller/verifier artifacts); not part of commit ef7e471, untouched by this verifier.

### Required Action: COMPLETE_CHILD
