# 04 Light Verification Log（Verifier04，2026-08-13）

## Light Verification: LIGHT_PASS_WITH_NOTES
Child: 04（P-C operator_status 对账作业）— plan: `docs/plans/2026-08-13/04-operator-status-reconciler.md`（含 Amendments A4；brief `children/04/brief.md` 含 A4）
Boundary: bdf853ceb2536772f9b1fcd4f0283877536e4376..9df711aa2f0017450dfb531a3aa03376c94c4f5d
Verifier: Verifier04

### Four Gates

|Gate|Result|Evidence|
|---|---|---|
|Authorized scope|PASS|`git show --name-status 9df711a~1..9df711a`：恰好 8 文件 = 7 计划授权（OperatorStatusReconcileService.kt 新增 / MailAutomationScheduler.kt / MailSchedulingProperties.kt / ExpertIndexController.kt / OperatorActionLogRepository.kt / OperatorStatusReconcileServiceTest.kt 新增 / docs/knowledge/campaign/K-operator-status-reconcile.md 新增）+ A4 授权守卫测试 OperatorStatusWriteSeamGuardTest.kt。A4 变更 = 恰好 2 行排除项行号 85→90、410→431（`git diff 9df711a~1 9df711a -- …/OperatorStatusWriteSeamGuardTest.kt`：2 insertions/2 deletions，context 子串 `operatorStatus = contact?.operatorStatus` / `operatorStatus = operatorStatus ?: expert.operatorStatus` 原样未动；ALLOWED_WRITE_SITES 闭包与断言未动）。行号实测命中：当前 ExpertIndexController.kt:90 与 :431 逐字包含上述 context（grep 确认），守卫自检 `lines[line-1].contains(context)` 通过（guard 1/1 fresh）。T-3 授权文件 #5 由 ExpertContactRepository 换为 OperatorActionLogRepository —— 计划 T-3 明示「或在 OperatorActionLogRepository 侧新增，取决于既有归属；执行前 grep 确认该 repository 是否存在」，该 repository 为既有 audit repository，属计划内二选一，非越权。提交不含 `docs/plans/fast/*`。|
|Plan and invariants|PASS|I-1 只读：服务构造依赖 = 5 个只读 repository + RestTemplate + ElasticsearchProperties，无任何 writer；服务内 grep `save|saveAll|delete|update|index|_bulk|_update|_doc|_create` 零命中；ES 仅 `"${properties.baseUrl}/$index/_search"`（OperatorStatusReconcileService.kt:249-268）。测试 `reconcile performs zero writes`：5 个 repository 全部 CrudRepository 写方法 verify(never)（save/saveAll×2/deleteById/delete/deleteAll/deleteAllById）+ ExpertContactRepository 5 个 @Modifying 更新 verify(never) + verifyNoMoreInteractions 总闭包 + ES URL captor 断言 `allValues.all { it.endsWith("/_search") }`（Test:337-389）。I-2：`OperatorActionLogRepository.findContactIdsWithChangeOperatorStatusLogs`（@Query `WHERE action_type = 'CHANGE_OPERATOR_STATUS' AND expert_contact_id IN (:contactIds)`，V19:36,38 列核实）；判别器成立——`ExpertOperatorStatusService.changeStatus:31-55` 写审计、`updateAutomatically:57-75` 不写；OperatorActionType.kt:4 枚举存在。服务将 humanOverrideIds 单列 CATEGORY_HUMAN_OVERRIDE 且 `continue` 跳过异常计数；测试断言 humanOverride=1 / dbVsExpected=0。I-3：`if (dbStatus != "COMPLETED" && expectedStatus != dbStatus)`（Service:119）——COMPLETED 不参与期望值差异判定；`OperatorStatus.COMPLETED` 全仓仅 ExpertOperatorStatusService:57 读取判断一处，deriveExpectedStatus 不可能返回 COMPLETED；测试 `COMPLETED is not anomalous even when events suggest otherwise` 通过。I-4：映射逐条比对既有实现——CONTACTED 逐字 = ManualInitialOutreachService.hasSentIntroduction():895（OUTBOUND+INTRODUCTION+SENT）；INVITED = AutoMailReplyService:484（updateAutomatically INVITED on MEETING_INVITATION_SENT）+ :816；REPLIED = AutoMailReplyService:802（inbound → updateAutomatically REPLIED）；MATERIALS_RECEIVED = AutomaticApplicationPromotionService:50,57（promoteByMaterialIfNeeded，INBOUND 材料附件）；EMAIL_INVALID = BounceCollectionService:104-108（HARD bounce）+ ManualInitialOutreachService:697-706（PERMANENT 失败，errorSummary 前缀由 buildSmtpErrorSummary:885-888 生成）；12 个单测逐条覆盖含 TRANSIENT 反例与最高里程碑。T-1：findAll 全表 + 内存比对；ES 三层 500/批 terms（ES_BATCH_SIZE=500，先例 ExpertIndexWriterService:121 chunked(500)）；ReconcileReport 含 total/consistent/dbVsExpected/esVsDb/humanOverride + SAMPLE_LIMIT=20 样本，实现 TaskExecutionSummaryProvider。T-2：MailSchedulingProperties.kt `operatorStatusReconcileCron: String = "-"`（默认关闭）；`@Scheduled(cron="${talent-introduction.scheduling.operator-status-reconcile-cron:-}")` taskType=OPERATOR_STATUS_RECONCILE 走 runAndRecordWithResult（Scheduler:79-91）；`POST /api/experts/reconcile-operator-status` 照抄 backfill :197-216 模式（Controller:223-238）。|
|Required commands|PASS|全部 fresh 运行（JAVA_HOME=zulu-11，worktree 根）：① `mvn test` exit 0，surefire `Tests run: 2404, Failures: 0, Errors: 0, Skipped: 4`，JS 496 pass / 0 fail（guard 1/1、reconcile 12/12）——与实现记录基线 2404/0/0/4 完全一致（child 03 基线 2392 + 12 新增）；② `mvn test -Dtest=OperatorStatusReconcileServiceTest` exit 0，`Tests run: 12, Failures: 0, Errors: 0, Skipped: 0`；③ `mvn clean package` exit 0，surefire `Tests run: 2404, Failures: 0, Errors: 0, Skipped: 4`，JS 496 pass / 0 fail，BUILD SUCCESS，产出 target/weibo-talent-introduction-1.0.0-SNAPSHOT.war；④ `git diff --check` exit 0（工作树与 HEAD~1..HEAD 均无输出）。|
|Downstream interfaces|PASS|无下游子计划消费本计划 API：README（docs/plans/2026-08-13/README.md）依赖图 P-E(05) 前置 = P-A+P-B、P-F(06) 前置 = P-E，P-C(04) 无消费者。本计划全部为增量改动：新 cron 属性默认 "-"（关闭）、新 @Scheduled 默认不触发、新 POST 端点、控制器尾部默认构造参数——既有行为不变（ExpertIndexControllerTest:47-50 仍以 9 位置参数构造，编译通过且全量测试绿）。|

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1（I-3 范围解读）：COMPLETED 仅豁免「期望值差异」（dbVsExpected）异常；ES 与 DB 的**事实**比对（esVsDb）对 COMPLETED 仍生效（Service KDoc + K-operator-status-reconcile.md 均有明示）。计划原文「不判异常」未限定范围，T-4 验收用例仅覆盖 ES 与 DB 一致场景；实现解读为「不参与期望值计算」= 不产生期望值类异常，事实漂移仍报（符合 A-2 漂移发现意图）。属合理且有文档的解读，提请 human 知悉。
- O-2（实现偏差，任务指定记录）：ExpertIndexController 新构造参数 `operatorStatusReconcileService: OperatorStatusReconcileService? = null` 为尾部可空默认参数（端点内 requireNotNull 兜底），以避开修改未授权文件 ExpertIndexControllerTest（:47-50 以 9 位置参数构造）。已核实：仓库既有先例 ManualExpertMailService:35-36 同款尾部默认注入；生产由 Spring 按类型注入 @Service bean；现有端点均不引用该参数，端点行为与计划「照抄 :197-216」一致（无行为偏差）；全量测试绿证明兼容。最小且正当的偏差。

### Required Action
- COMPLETE_CHILD
