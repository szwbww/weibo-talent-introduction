# 03 Light Verification

## Light Verification: LIGHT_PASS_WITH_NOTES
Child: 03 (P-B: ES mapping 契约收敛)
Plan: docs/plans/2026-08-13/03-es-mapping-contract-convergence.md (amended per A3, commit c703137)
Boundary: e36cf27e6580ce3f5b178b518fe4e490f779ea68..bdf853ceb2536772f9b1fcd4f0283877536e4376
Verifier: Verifier03

### Four Gates

| Gate | Result | Evidence |
|---|---|---|
| Authorized scope | FAIL | Implementation commit bdf853c changes 19 files. 18 are authorized (brief T-1..T-6 + A3): 3 es JSON (`orcid_info_application/candidate/raw.json`), `ExpertIndexService.kt`, `ExpertIndexWriterService.kt`, `EsMappingContractTest.kt` (new), `docs/runbooks/es-mapping-reindex.md` (new), `K-es-dynamic-false.md` (corrected), `K-es-mapping-single-declaration-source.md` (new), `CandidateOperatorStatusSyncService.kt` (call-site, brief allows), A3 rename callers `BounceCollectionService.kt` / `ManualInitialOutreachService.kt` / `ExpertOperatorStatusService.kt`, A3 tests `ExpertIndexServiceTest.kt` / `ExpertIndexWriterServiceTest.kt` / `CandidateOperatorStatusSyncServiceTest.kt` / `ExpertOperatorStatusServiceTest.kt` / `ManualInitialOutreachServiceTest.kt`. ONE file is NOT in any authorized list: `src/test/kotlin/com/weibo/talentintroduction/campaign/OperatorStatusWriteSeamGuardTest.kt` (child 02's file; 7 lines: one stale NoiseSite exclusion at old `ExpertIndexWriterService.kt:84` removed + comment; `ALLOWED_WRITE_SITES` untouched). A3-authorized `ManualOutreachTxHelperTest.kt` correctly zero-change (grep: no residual `syncCandidateOperatorStatus` callers anywhere). |
| Plan and invariants | PASS | I-1: `grep -n phase5NewFields src/main/kotlin` → 0 hits (only test comments); `loadMappingProperties` returns all JSON properties, no whitelist filter (ExpertIndexService.kt:135-163); `EsMappingContractTest` 5/5 green. I-2: batch PUT 4xx → `pushFieldsIndividually` per-field with per-field success/conflict logging + summary `index=X 推送 N 字段：成功 M，冲突 K（字段列表）` (ExpertIndexService.kt:100-104, 117-146). I-3: `K-es-dynamic-false.md` corrected with measured commands+output (`filter_path=**.dynamic` evidence), created bumped to 2026-08-13, last_source re-validated. I-4: `docs/runbooks/es-mapping-reindex.md` exists with copyable `_update_by_query` commands. T-1: application JSON `operatorStatus: keyword` (:48), `enrichedAt` → keyword (:51); candidate/raw `givenNames/familyNames/employment/keyword` text→keyword. T-3: `syncOperatorStatus` (ExpertIndexWriterService.kt:66-110) and `syncOperatorStatusBatch` (:113-208) loop RAW/CANDIDATE/APPLICATION with `documentExistsInIndex` HEAD skip (pattern matches `ExpertDiscoveryService.updateExpertAcademicFields:1098`); `checkOperatorStatusMapping` loops 3 layers (ExpertIndexService.kt:170-198); `CandidateOperatorStatusSyncService.kt:16-19` updated; 3 rename callers updated. |
| Required commands | PASS | Fresh runs, JAVA_HOME=zulu-11: (1) `mvn test` exit 0, surefire 2392/0/0/4, JS 496 (baseline after 02: 2387/0/0/4 → +5); (2) `mvn test -Dtest=EsMappingContractTest` exit 0, 5/0/0/0; (3) `mvn clean package` exit 0, surefire 2392/0/0/4, JS 496 pass; (4) `git diff --check` (boundary e36cf27..bdf853c) exit 0, plain worktree `git diff --check` exit 0. |
| Downstream interfaces | PASS | Child 05 needs: APPLICATION `operatorStatus` mapping — `orcid_info_application.json:48` `{"type":"keyword"}` + pushed via bootstrap; three-layer sync — `syncOperatorStatus`/`syncOperatorStatusBatch` cover RAW/CANDIDATE/APPLICATION; three-layer `checkOperatorStatusMapping` — ExpertIndexService.kt:170-198; `operatorStatusFilter` NOT_CONTACTED semantics — NOT_CONTACTED branch removes the field per layer (script `ctx._source.remove('operatorStatus')`), same semantics as pre-change, now on all 3 layers. |

### AUTO_FIX
- N/A

### RECORD_ONLY
- **O-1**: `src/test/kotlin/com/weibo/talentintroduction/campaign/OperatorStatusWriteSeamGuardTest.kt` (child 02's file, NOT in A3 list) changed in bdf853c — 7 lines, removes the now-stale NoiseSite exclusion `ExpertIndexWriterService.kt:84 "ctx._source.operatorStatus = params.status"`. Verified necessary: T-3 rewrote `syncCandidateOperatorStatus` (script-based `_update_by_query`) into `syncOperatorStatus` (doc `_update`); the referenced script line no longer exists anywhere in the file (line 84 is now `} else {`), and the guard's own stale-exclusion self-check (`assertTrue(staleExclusions.isEmpty())`, guard test source "排除名单失效请同步更新 EXCLUDED_NOISE_SITES") would FAIL if the entry were kept → `mvn test` would fail gate 3. Verified NOT weakening child 02's I-1 closure: `ALLOWED_WRITE_SITES` and the `assertEquals(ALLOWED_WRITE_SITES, hitFiles)` assertion are unchanged; independent re-scan of `src/main/kotlin` reproduces hitFiles = {ExpertOperatorStatusService.kt, ManualInitialOutreachService.kt} exactly and zero stale exclusions. So the change is necessary and contract-preserving, but the file is outside the A3 authorized list and the plan does not uniquely authorize it → cannot be AUTO_FIX (unauthorized file), does not break child 02's guard contract → RECORD_ONLY, not PAUSE.
- **O-2**: T-1 fidelity: CANDIDATE/RAW `enrichedAt` remains `date` in JSON (`orcid_info_candidate.json:41`, `orcid_info_raw.json:38`) while live indexes have `keyword` (plan audit) and APPLICATION was aligned to `keyword` (application JSON:51). Plan A-2's premise ("临时把 orcid_info_candidate.json 的 enrichedAt 改回 date") and 未决决策 方案甲 ("JSON 迁就线上") imply candidate/raw alignment to keyword was intended; T-1's literal enumeration for CANDIDATE/RAW lists only the 4 text fields, so implementation is defensible. Impact: batch `PUT _mapping` for CANDIDATE/RAW will 400 on `enrichedAt` at every startup and per-field degradation permanently engages (33 fields pushed, 1 conflict logged) — functionally safe via I-2, no gate impact. Also, plan's "在 JSON 旁加注释" for the `enrichedAt` tech debt is not present in JSON (strict JSON cannot hold comments); the debt note is documented in the runbook §1 and EsMappingContractTest comment instead.

### Required Action
- COMPLETE_CHILD
