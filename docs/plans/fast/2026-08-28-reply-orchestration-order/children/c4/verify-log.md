## Light Verification: LIGHT_PASS_WITH_NOTES
Child: c4 (13-letter-orchestrator) and plan docs/plans/2026-08-28/13-letter-orchestrator.md
Boundary: 41aae5af28e81e31e0469937c96bd532f13dc784..889210e339c3c5dd2533777d35076bdfc5c65793
Verifier: C4Ver

### Four Gates

| Gate | Result | Evidence |
|---|---|---|
| 1. Authorized files | PASS | Product commit e669b37 touches exactly the 5 plan-authorized files: AiReplyLetterOrchestrator.kt (new), AiReplyLetterCloser.kt (modify), AiReplyValidationDiagnostic.kt (modify), AiReplyLetterOrchestratorTest.kt (new), AiReplyLetterCloserTest.kt (modify). `git diff --name-only 763e83f..e669b37` shows no other product file; AiReplyDraftService.kt absent from boundary (0 matches; unchanged). Closer diff reviewed in full: steps 1/2/4/5 untouched — only doc comments, import (QaCoverageKeyCatalog), constant MAX_FACTS_PER_PLAN_TOPIC, `orchestration` seam param, and step 3 replaced per T-1/T-4. Diagnostic diff = exactly 5 new ORCH_* constants (ORCH_FACT_ID_UNKNOWN, ORCH_REQUIRED_FACT_COUNT_INVALID, ORCH_VERBATIM_BODY_MISSING, ORCH_ACTION_IN_PARAGRAPH, ORCH_PLAN_MISMATCH) + comment; G5 reuses existing ACTION_* codes per T-3. CloserTest diff = T-5.4 only (2 tests + helpers + imports). Docs-only commit 62edb0b ignored (fast-p evidence). |
| 2. Requirements & invariants I-1..I-8 | PASS | I-1/I-6: `validatePlanConsistency` (G6) enforces paragraphs.size == plan.size, topic sequence == topicOrder, per-group factIds set equality -> ORCH_PLAN_MISMATCH; tests `paragraph topics not matching topic order fails plan consistency`, `paragraph count not matching plan fails plan consistency`. Grouping/order are server inputs (closer `buildPlan`; prompt instructs but validation is the gate, I-7). I-2: `validateSourceClosure` -> ORCH_FACT_ID_UNKNOWN (ids must be in plan union AND known fact ids); test `unknown fact id fails source closure`; c6 channel-A premise. I-3: `validateRequiredFactCount` -> ORCH_REQUIRED_FACT_COUNT_INVALID; tests for 0 and 2 occurrences. I-4: `validateVerbatimBodies` -> ORCH_VERBATIM_BODY_MISSING; normalization is `normalizeWhitespace` (trim + collapse `\s+` to single space) — never touches `--`, `–` (U+2013), `${...}`; tests: placeholder rewritten fails, `--`->`—` fails, `15–20`->`15-20` fails, frozen meeting body passes verbatim; expectations sourced from `PlanFact.body` and `QaCoverageKeyCatalog.controlledGroups()` (test uses `controlledBody("G2")` from catalog; validation side has 0 canonical literals). Frozen set {1,3,21,24} in closer FROZEN_RULE_IDS; frozen fixtures = demand-side verbatim baselines (id 1 placeholders, id 3 `--`, id 21 en dash + CTA). I-5: `validateParagraphActions` (G4, ORCH_ACTION_IN_PARAGRAPH) — actionText non-blank => any paragraph action invalid; actionText null => paragraph actions must be subset of that paragraph's frozen-fact built-in actions (c2 I-5 exemption); `validateActionReconciliation` (G5) reuses ACTION_TEXT_INVALID/ACTION_NOT_ALLOWED/ACTION_BODY_MISMATCH; tests: action-in-paragraph, multi-action actionText, unauthorized actionText, body-action mismatch, frozen CTA exemption pass. I-6: gaps attached via gapCondition on plan entry (closer buildPlan, unanswered.request.N); test `gap condition entry is honored with matching paragraph count`; paragraphs.size == paragraphPlan.size asserted. I-7: all six validations are server-side pure functions in the parser (validatePlanConsistency / validateSourceClosure / validateRequiredFactCount / validateVerbatimBodies / validateParagraphActions / validateActionReconciliation), failures reuse AiReplyValidationIssue + initial/repair retry. I-8: every orchestrator failure path returns null; closer `orchestratedGroupsOrFallback` falls back to deterministic groupByTopic with warning; test `orchestration null falls back to deterministic closure with warning` asserts output == pure deterministic result; `llm disabled returns null`, `client unavailable returns null`; assemble never fails. G-1 frozen ids {1,3,21,24}; G-4 controlled exact-set via catalog; G-7 requestKey untouched (closer hash inputs unchanged; no grouping data enters any hash). |
| 3. Required commands (fresh, zulu-11) | PASS | Focused: `mvn test -Dtest=AiReplyLetterOrchestratorTest,AiReplyLetterCloserTest` -> Tests run: 19+9=28, Failures: 0, Errors: 0, BUILD SUCCESS. Full: `mvn test` -> Tests run: 2991, Failures: 0, Errors: 0, Skipped: 5, BUILD SUCCESS (baseline c3 head 763e83f: 2970/0/0/5; delta +21 = 19 orchestrator + 2 closer tests; skipped 5 unchanged; node 760 pass). Build: `mvn clean package` -> Tests run: 2991, Failures: 0, Errors: 0, Skipped: 5, BUILD SUCCESS. Hygiene: `git diff --check` exit 0. All with JAVA_HOME=/Library/Java/JavaVirtualMachines/zulu-11.jdk/Contents/Home. |
| 4. Downstream interfaces | PASS | c5 protocol matches brief: `ParagraphPlanEntry(topic, factIds, gapCondition?)` (closer buildPlan; gapCondition = requestText), facts `f<ruleId>`/`x<n>` (factIdOf; multi-rule ids encode the dedup identity set as `f1+3`, decoded by ruleIdsOf — single-rule facts are exactly f<ruleId>), `topicOrder` produced alongside plan, orchestration response parsed as `{"paragraphs":[{"topic","factIds","text"}],"actionText"}` (parseParagraph/parseActionText enforce exact field sets). c6 channel-A safety premise holds: G1 source closure (ORCH_FACT_ID_UNKNOWN) implemented and tested — sample propositions without a legal factId are rejected. |

### AUTO_FIX
N/A (no four-gate violation proven)

### RECORD_ONLY
- O-1: AiReplyLetterOrchestratorTest.kt:210 contains the string "We never charge any fees." — a G2-canonical sentence prefix used as a deliberately truncated/rewritten negative fixture in `rewritten controlled body fails verbatim` (the full canonical body "…throughout the entire process." is absent; the expectation side uses `controlledBody("G2")` from the catalog). The plan's concrete acceptance grep (G1 phrase "Your materials are kept strictly" == 0) passes; validation-side grep = 0 matches. Not a failure, but the brief's broader "no canonical text in test file" wording technically matches this fragment. Non-blocking; no plan-unique repair (AUTO_FIX not applicable).

### Required Action
- COMPLETE_CHILD
