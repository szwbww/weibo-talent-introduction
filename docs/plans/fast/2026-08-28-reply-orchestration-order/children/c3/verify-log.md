## Light Verification: LIGHT_PASS_WITH_NOTES
Child: c3 · docs/plans/2026-08-28/14-workbench-concurrency.md (brief: docs/plans/fast/2026-08-28-reply-orchestration-order/children/c3/brief.md)
Boundary: e3217e5079616839e514fe54d45b42a073035219..41aae5af28e81e31e0469937c96bd532f13dc784
Verifier: C3Ver

### Four Gates

| Gate | Result | Evidence |
|---|---|---|
| 1. Authorized files only | PASS | `git show --stat 763e83f` touches exactly the 8 plan-authorized files (Controller +45, Service +139, app.js +2, styles.css +21, trust-reply-workbench.js +89/-13, autoRunOrchestration.test.js +136, trustReplyWorkbenchSharedMount.test.js +80/-1, TrustReplyWorkbenchServiceTest.kt +157). The docs files in the full boundary diff (c2 brief/execution/verify-log, ledger) belong to the docs-only intermediate 2d1245e (fast-p evidence, ignored). app.js diff = exactly two `autoBootstrap: false` additions at `mountAiTrainingTrustReply` (~:3787) and `mountLiveTrustReply` (~:9964), nothing else. styles.css diff = only the S-2 block (+21) after `.trust-reply-autorun-hint`. Commit message matches brief: `feat(fast-p): implement c3`. |
| 2. Requirements & invariants I-1..I-5, S-1/S-2/S-3 | PASS (notes O-2, O-3) | I-1: persistEach branch (:988-1023) sets only `request.stateSavePending` (never `state.stateSavePending`); single-item generation `adjustItem`→`requestItemVersion` sets only `request.pending` (never `state.generation.pending`); `busyOverlayState()` (:2100) 4 global branches unchanged; `itemBusyState(request)` (:2118) per-item with short-circuit kept. New tests: "masks only the pending item…", "keeps the global mask hidden while a single-item durable save is pending" (both pass). I-2: `persistResolvedItem(request)` (:859, PATCH /state/item with requestKey + lockedItem + expectedStateVersion) used at :997; `persistResolvedSnapshot` absent from persistEach branch (remaining sites: regenerateContextStale :1171, assemble :1272, autoRun :1465, persistDecisionUnlock :1594, frameSave :1989, toggleResolve :2616 — letter-level/pre-existing, unchanged by c3); backend `saveStateItem` merges only the requestKey-matched item (replace-or-append), keeps other lockedItems and letter fields verbatim (Kotlin test asserts stored2/frameSnapshot/selectedModel/requestFactSelections preserved). I-3: `renderItemActions` disabled = `action.disabled \|\| request.factChangePending \|\| request.stateSavePending` (global term removed); `hasRequestMutationPending()` refs = 3 call sites (regenerateContextStale :1157, autoRun :1416, autoReset :1499) + def :1241, unchanged from base (base 45602d7 also had 3; plan's "4 处" audit predates c2 — see O-2); `canStartAssembly` guard byte-identical base vs head; per-item handling select disabled is per-item only (:2332). I-4: `persistResolvedItem` updates `state.savedStateVersion` from response.stateVersion (same as :834-836 pattern); backend stale → existing 409 `TRUST_REPLY_STATE_CONFLICT` via the same `stateStore.save` optimistic-lock call as saveState; conflict test passes (T-5.3). I-5: mount guard `options.autoBootstrap !== false \|\| options.mode === MODES.AUTO_PREVIEW` (AUTO_PREVIEW = readOnly per :265) with the mandated 方案 A comment ("默认取舍，非疏漏"); `validateMount` rejects non-boolean autoBootstrap; `state.analyzed` set in bootstrap() success path (:736) so mount-auto and start-analysis converge; both hosts pass `autoBootstrap: false`; `startAnalysis()` reuses `bootstrap()` (exactly-once click test passes); read-only keeps auto analysis. S-1: autoRunBar template = start-analysis + auto-run + auto-reset + hint, class names one-to-one with contract skeleton; after analysis button becomes `button secondary` "重新分析", auto-run/auto-reset un-disabled; unanalyzed hint text matches contract verbatim. S-2: styles.css block verbatim (diff == contract char-for-char), inserted after `.trust-reply-autorun-hint`; preanalysisZone DOM verbatim; zero `style=` additions in c3 static diff. S-3: styles.css diff contains only the S-2 block — `.trust-reply-autorun` / `.button` / `.trust-reply-item-busy-overlay` / `.trust-reply-busy-overlay` rule blocks untouched. What must NOT change: `persistResolvedSnapshot` (PUT /state) semantics untouched; readOnly requestJson whitelist intact (:345); SSE/cancel/TTL untouched; requestKey hash inputs untouched (G-7). |
| 3. Required commands (fresh, zulu-11) | PASS | `mvn test -Dtest=TrustReplyWorkbenchServiceTest`: BUILD SUCCESS, 66/0/0/0. `node --test src/test/js/trustReplyWorkbenchSharedMount.test.js`: 60 pass / 0 fail, exit 0. `node --test src/test/js/autoRunOrchestration.test.js`: 18 pass / 0 fail, exit 0. `node --test src/test/js/*.test.js`: 760 pass / 0 fail, exit 0 (baseline 755 → +5 = 2 sharedMount + 3 autoRun new tests). `mvn test`: BUILD SUCCESS, `Tests run: 2970, Failures: 0, Errors: 0, Skipped: 5` (baseline seed 2952 → c2 head 2968 → c3 +2 Kotlin tests = 2970, consistent); `node --test` exec record present (`exec:3.1.0:exec (node-check-app)`; full 760-test node output observed inside the focused mvn run). `mvn clean package`: BUILD SUCCESS, 2970/0/0/5. `git diff --check`: exit 0. |
| 4. Downstream interfaces (c5) | PASS | PATCH `/api/trust-reply/workbench/state/item` (class @RequestMapping + @PatchMapping) request body exactly `{source, expectedStateVersion, schemaVersion, sourceVersion, evidenceSetVersion, requestKey, lockedItem}` (TrustReplySaveStateItemHttpRequest) → returns TrustReplySavedState with new `stateVersion` (from stateStore.save, updated on frontend via `state.savedStateVersion`); `persistResolvedItem(request)` exported function present (:859); `itemBusyState(request)` / `busyOverlayState()` scope split (per-item stateSavePending; global mask only completePending/frameSavePending/generation.pending); `autoBootstrap` mount option (default true, validated boolean); `data-action="start-analysis"` toolbar contract (becomes `重新分析` secondary after analysis). |

### AUTO_FIX
N/A

### RECORD_ONLY
- O-1: Pre-existing 1-Hz timing flake in `UnmatchedInboundAiReplyTurnKnowledgeTest` (`src/test/kotlin/com/weibo/talentintroduction/mail/controller/`) — unrelated to c3 (outside the 8 authorized files, zero boundary diff) and NOT reproduced in this fresh run (2970/0/0/5). [INFERENCE: flake intermittent by nature; no failure observed to confirm]
- O-2: Plan I-3 acceptance states `hasRequestMutationPending` refs "仍恰为 4 处", but the c2-head base 45602d7 already had 3 call sites (canStartAssembly uses an equivalent per-item `state.requests.some(...)` guard, byte-identical base vs head). Plan audit line numbers predate the c2 refactor; c3 left the ref set unchanged, so the invariant (global-entry-guards only) holds.
- O-3: Pre-existing per-item operator saves `toggleResolve` (:2616) and `persistDecisionUnlock` (:1594) still persist via full `PUT /state` (with `request.stateSavePending` → local mask only, no global mask; observable A-2 acceptance holds). Present at base (45602d7:2546); outside c3's T-2 scope (persistEach branch) and not touched by the c3 diff. Strictest I-2 wording ("只在整合路径") would cover these, but the plan's Applies-to/acceptance and tests target only the persistEach branch.

### Required Action
- COMPLETE_CHILD
