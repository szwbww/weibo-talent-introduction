# Epoch 2 — light verification (post-A1, fresh commands)

## Light Verification: LIGHT_PASS
Child: c2 · plan docs/plans/2026-08-28/12-letter-closer.md (A1-amended, plan identity commit:5a90e3e53e5fe8b40059b3090f086d6b36a09a01)
Boundary: 97e414658b1fe9196271f607cf763853c04d5098..e3217e5079616839e514fe54d45b42a073035219
Verifier: C2Ver

### Four Gates

| Gate | Result | Evidence |
|---|---|---|
| 1. Authorized files only | PASS | `git diff --name-only 97e4146..45602d7 | grep -v '^docs/'` → exactly the 5 authorized files (nothing else product/test changed at the boundary; intermediate commits e795769/ca8e25f/444d43c/b6e3d45 are docs-only fast-p evidence/plans and ignored): `src/main/kotlin/.../llm/service/AiReplyLetterCloser.kt` (new, +291), `src/main/kotlin/.../llm/service/TrustReplyWorkbenchService.kt` (+8/−3, single hunk `@@ -1463,9 +1463,14 @@` touching ONLY the `orderedAnswers` construction at :1466-1468 — the closer call replaces `versions.mapNotNull { answerText.takeIf { handling != OMIT } }`; nothing else in the file), `src/test/kotlin/.../llm/service/AiReplyLetterCloserTest.kt` (new, T-4.1..T-4.7), `src/test/kotlin/.../llm/service/TrustReplyWorkbenchServiceTest.kt` (+52, T-4.8 only), `src/test/kotlin/.../llm/service/TrustReplyWorkbenchItemFlowTest.kt` (A1: exactly the 5 obsolete assertions updated + fixture helper extended with contact/defaultFrame). |
| 2. Requirements & invariants (T-1..T-4, I-1..I-7, G-1/G-3) | PASS | I-1: `close(versions: List<TrustReplyItemVersion>, allowedActions)` — `expandUnits` builds units from `version.claims` (intentKey/text/sourceRuleIds), never string-level dedup; acceptance grep `linkedSetOf<String>` = 0 (dedup uses `linkedSetOf<Set<Long>>`). I-2: `dedupBySourceRuleIds` — set equality keeps first survivor (`seen.add`), empty `sourceRuleIds` always kept; T-4.1, T-4.2 pass. I-3: `groupByTopic` — `intentKey.substringBefore('.')`, `linkedMapOf` first-survivor topic order, canonical order inside; T-4.3 pass. I-4: `reconcileCta` reuses `AiReplyActionPolicy.detectActions` (`detectAction` delegates; file defines zero `Regex` — no second action regex); keeps the letter's last action sentence, removes the rest; kept action must be in `allowedActions` else removed + warning; T-4.4 pass (incl. unauthorized → whole CTA removed). I-5: `FROZEN_RULE_IDS={1,3,21,24}`, `${`-placeholder and verbatim bodies are never split/rewritten (skip removal + warning when I-4 would remove); fixtures are the demand-side verbatim baselines — id 21 body byte-checked with en dash U+2013 in `15–20` (codepoint 0x2013), id 1 body with `${researchFields|your field}`, `${recentWorkTitle|your recent research}` and the action sentence; T-4.5, T-4.6 pass. I-6: escape hatch — `surviving.all { generationKind != AI_GENERATED }` → returns original orderedAnswers verbatim (no trim/dedupe/reorder); T-4.7 pass. I-7: closer runs inside the orderedAnswers construction — after `validateGroundedTrustBoundary` (:1461), before `resolveFrameForAssemble` (:1471) and `composeLockedItems` (:1472); `canonicalFactIds = selection.sendQaRuleIds` and `draftHash = sha256Hex(raw)` on the final closed raw are untouched; T-4.8 asserts canonicalFactIds identical pre/post closing. Five-step structure explicit: class doc + `close()` label Steps 1-5, step 3 isolated in `groupByTopic` (c4 swap point). Zero new LLM calls: closer is a pure object, only slf4j import, no services/HTTP. A1: all 5 listed ItemFlowTest assertions now assert closed output (dedup {9}/{9}→"raw Claim A"; topic-merged "Same  claim Same CLAIM"; canonical order "Claim A Claim B"; paragraph/space variance merged; IP-4 per-item "Claim A\n\nClaim B" vs raw "raw Claim A"). |
| 3. Required commands fresh vs baseline | PASS | All with `JAVA_HOME=/Library/Java/JavaVirtualMachines/zulu-11.jdk/Contents/Home`. Focused (3-class): `mvn test -Dtest=AiReplyLetterCloserTest,TrustReplyWorkbenchServiceTest,TrustReplyWorkbenchItemFlowTest` exit 0 — kotlin-test aggregate `tests 755 / pass 755 / fail 0 / cancelled 0`. Full: `mvn test` → BUILD SUCCESS, exit 0, Surefire `Tests run: 2968, Failures: 0, Errors: 0, Skipped: 5` (baseline seed 2952/0/0/5; c1 head 97e4146: 2960/0/0/5; delta +8 = 7 new closer unit tests + T-4.8; Skipped 5 unchanged). Build: `mvn clean package` → BUILD SUCCESS, exit 0, `Tests run: 2968, Failures: 0, Errors: 0, Skipped: 5`. Hygiene: `git diff --check` exit 0. |
| 4. Downstream interfaces | PASS | c4 (13-letter-orchestrator) swaps closer step 3 only: five-step structure explicit in `AiReplyLetterCloser` (doc + `close()` labels; step 3 = `groupByTopic`, the only step to be replaced); `verifyAssembly` callers still receive plain `List<String>` — `composeLockedItems(orderedAnswers, resolvedFrame)` contract unchanged (:1472). c6 (16-unsupported-index) archives per-item `itemVersions` (pre-closing answerText) distinct from final raw (IP-4): `verifyAssembly` returns `itemVersions = versions` untouched while `rawDraftText` is the closed composition; ItemFlowTest `multi claim item answerText keeps each claim as its own paragraph` proves it (itemVersions "Claim A\n\nClaim B" vs rawDraftText "raw Claim A"). |

### AUTO_FIX (F-ids or N/A)
N/A — no four-gate violation found.

### RECORD_ONLY (O-ids or N/A)
- O-1 (informational): working tree has uncommitted modifications to `docs/plans/fast/2026-08-28-reply-orchestration-order/children/c2/brief.md` and `execution.md` (fast-p evidence owned by the controller, outside the product boundary); no product/test file is dirty. No action needed.

### Required Action
- COMPLETE_CHILD
