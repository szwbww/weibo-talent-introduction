## Light Verification: LIGHT_PASS_WITH_NOTES
Epoch: 2 (A2-amended plan; plan SHA-256 03967d14608ac297da2448776543b8ce814b18b497d6c1b291b3c5f22a07e45e)
Child: c5 (15-workbench-three-step) and plan docs/plans/2026-08-28/15-workbench-three-step.md
Boundary: 889210e339c3c5dd2533777d35076bdfc5c65793..0a2a8f58acf93da9b2b903082af938be92d2783e (implementation commit 870a54f; intermediate docs-only commits 9739c4c/7040859/7043464/7bea6e3 ignored as fast-p evidence/plans)
Verifier: C5Ver

### Four Gates

| Gate | Result | Evidence |
|---|---|---|
| 1. Authorized files (9) | PASS | `git diff --name-only 9739c4c 870a54f` = exactly the 9 A2-authorized files: trust-reply-workbench.js, styles.css, TrustReplyWorkbenchController.kt, TrustReplyWorkbenchService.kt, AiReplyGroundedContentPlanner.kt, TrustReplyWorkbenchServiceTest.kt, trustReplyWorkbenchThreeStep.test.js (new), trustReplyWorkbench.test.js, trustReplyWorkbenchSharedMount.test.js. 1740 insertions / 39 deletions, no other product file in the implementation commit. HEAD = 870a54f; worktree clean except fast-p evidence docs (brief.md/execution.md) owned by the controller — untouched by this verifier. |
| 2. Requirements & invariants I-1..I-6 + S-1/S-2/S-3 + What must NOT change | PASS | I-1/G-7: `TrustReplyWorkbenchService.requestKey(sourceVersion, index, requestText, intentKeys)` byte-unchanged (TrustReplyWorkbenchService.kt:2789-2802) — exactly the 4 inputs, sha256(...).take(32); op facts flow only through `operatorFacts`/`paragraphPlanDraft` of the new rearrange endpoint, never into any requestKey input; frontend never computes requestKey (uses server-provided keys; op facts kept in separate `state.operatorFacts` with sequential `op${++state.operatorFactSeq}` ids). JS test asserts `rearrangeBody.operatorFacts.every((fact) => /^op\d+$/.test(fact.id))` — "op ids must use the op<n> space, never hashes (I-1)" (threeStep.test.js:407). I-2: `TrustReplyOperatorFactHttpRequest.frozen=true, required=true` DTO defaults force verbatim slots server-side; `validateRearrangement` runs the same verbatim check as 13's G3 (`fact.controlled != null || fact.frozen` → ORCH_VERBATIM_BODY_MISSING); T-6.5 passes in both paths — stubbed rearrange response with one changed word (`"operator fixed wording"` vs `"operator fixed text"`) yields ORCH_VERBATIM_BODY_MISSING, and the real `AiReplyLetterOrchestrator` G3 rejects the changed text (orchestrate → null) while the verbatim text passes. I-3: pinned validity keyed on entry-level evidence — backend compares `requestEvidenceVersions[requestKey(...)]` (per-request, from `TrustReplyRequestCoverage.evidenceSetVersion` seam) against `TrustReplyPinnedParagraphRequest.evidenceSetVersion`; frontend `paragraphEvidenceVersion()` resolves the owning request's per-request value, never `state.evidenceSetVersion` (whole-letter scalar used only in pre-existing assembly/save paths); JS test asserts pinned never carries `"letter-e1"` and carries `"per-r1-e1"`; backend T-6.4 asserts pinned paragraph re-filled verbatim (`LOCKED REVIEW TEXT`), excluded from the orchestration plan/facts. I-4: adopt/unadopt, topic change, pin/unpin, edit toggle, merge-up, move up/down are pure local-draft mutations (`setFactAdopted`/`setFactTopic`/`toggleParagraphPin`/`toggleParagraphEdit`/`mergeParagraphUp`/`moveParagraph` — render only, no fetch); T-6.2 fetch-stub test asserts zero network calls for all six interaction classes (baseline call count unchanged); only `rearrange` (new /rearrange endpoint) and `assemble` (existing) talk to the server; backend `rearrangeInternal` performs no persistence (I-4, no repository writes). I-5: fact set is a derived view — `factSetRows()` builds from server 13 protocol `state.plan.facts` ∪ local op facts, with adopt/usage/topic from the local `paragraphDraft` and trigger questions reverse-looked-up from the requestFacts matrix (`state.requests` factRuleIds); bootstrap now carries server-authoritative `paragraphPlan`/`facts`/`topicOrder` (TrustReplyBootstrapResponse); no hardcoded fact bodies in JS (only pre-existing fact-picker search UI references answerBody); T-6.1 asserts 3 deduped rows for f7/f9/f10 with f9 triggered by `R1 · R3` (one row). I-6: step-01 panel keeps `data-role="items"` summary cards with coverage badges (`GROUNDED · 依据充分`), handling selects, 500-char instruction; renderItem/renderRequestHeader untouched by the diff; T-6.3 regression asserts cards + badge + handling select survive after tab switching, tab switch does not re-bootstrap. What must NOT change: HANDLING_LABELS (7 options) and OPERATOR_INSTRUCTION_HANDLINGS (lines 15-50 diff-identical to base); allowedHandlings enum full set untouched; composeLockedItems (AiReplyPointByPointComposer.kt) absent from diff; 14's PATCH /state/item and local overlay untouched; step-03 keeps the integration summary preview + frame selects (only new paragraph-card area added below). S-1: `git diff` shows styles.css 79 insertions / 0 deletions — no existing `.trust-reply-page-*`/`.trust-reply-item`/`.trust-reply-item-list`/`.button`/`.compose-panel` rule block modified; tabs reuse `trust-reply-page-tab` (no new tab class). S-2/S-3: added blocks verified **verbatim** against the plan's contract code blocks by scripted comparison (S2 verbatim: True; S3 verbatim: True); placement per contract (S-2 before `.trust-reply-item-list`, S-3 immediately after `.trust-reply-item`); zero `style="` additions in the JS or CSS diff (grep: no matches). |
| 3. Required commands (fresh, zulu-11) | PASS | All 7 run freshly at HEAD 870a54f with JAVA_HOME=/Library/Java/JavaVirtualMachines/zulu-11.jdk/Contents/Home: (1) `mvn test -Dtest=TrustReplyWorkbenchServiceTest,AiReplyGroundedContentPlannerTest` → exit 0, `Tests run: 68, Failures: 0, Errors: 0, Skipped: 0`, BUILD SUCCESS (surefire runs only the existing class; see O-1); (2) `node --test src/test/js/trustReplyWorkbench.test.js` → tests 33, pass 33, fail 0; (3) `node --test src/test/js/trustReplyWorkbenchThreeStep.test.js` → tests 3, pass 3, fail 0; (4) `node --test src/test/js/*.test.js` → tests 764, suites 121, pass 764, fail 0 (baseline 760 + 4 new); (5) `mvn test` → `Tests run: 2993, Failures: 0, Errors: 0, Skipped: 5`, BUILD SUCCESS (baseline c4 head e669b37: 2991/0/0/5; delta +2 = T-6.4/T-6.5; seed 2952/0/0/5); (6) `mvn clean package` → `Tests run: 2993, Failures: 0, Errors: 0, Skipped: 5`, BUILD SUCCESS; (7) `git diff --check` → exit 0. |
| 4. Downstream interfaces (c6 / c4) | PASS | c6 op* archiving subject: op facts are real `PlanFact(id="op<n>", body=<verbatim operator text>, frozen=true, required=true)` carried in the rearrange request `operatorFacts` and present in the rearrange response `facts` list — c6 can archive them as the operator-fact subject with body intact. c4 protocol stable at the archive seam: rearrange response reuses the 13 shapes — `paragraphPlan: List<ParagraphPlanEntry>(topic, factIds, gapCondition?)`, `facts: List<PlanFact>`, `paragraphs: List<OrchestratedParagraph>(topic, factIds, text)`; per-paragraph post-close `text` is separate from per-item versions (`answerText`/`versionId` untouched). `initialLetterPlan` derives the bootstrap 13 protocol from the same matrix + QA library (topicOrder/plan/facts). |

### AUTO_FIX
N/A (no four-gate violation proven; no plan-unique repair needed)

### RECORD_ONLY
- O-1: `AiReplyGroundedContentPlannerTest` referenced by the plan's focused command does not exist in the repo (confirmed absent at base 9739c4c too — only `AiReplyGroundedDraftMaterializerTest.kt` exists); surefire runs the matching class only (`TrustReplyWorkbenchServiceTest`, 68 tests) and exits 0, identical to baseline behavior. Not a c5 regression; T-6.4/T-6.5 live in the plan-designated `TrustReplyWorkbenchServiceTest.kt`.
- O-2: plan I-1's literal acceptance "测试断言加入 op* 事实前后同一封信的 requestKey 完全相同" is not implemented as a standalone with/without-op test. The invariant holds by direct evidence instead: (a) the `requestKey` hash construction is byte-unchanged with exactly the 4 inputs (code read, TrustReplyWorkbenchService.kt:2789-2802) and is pinned by existing canonical-key tests (locked-item restore, stale-state, bootstrap selections), and (b) the JS test asserts op ids are `op<n>` never hashes. No code path feeds op facts into any hash; not a violation, but the plan's literal test wording is unimplemented.

### Required Action
- COMPLETE_CHILD
