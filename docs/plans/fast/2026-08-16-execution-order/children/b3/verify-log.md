# Light Verification Log — b3 (first attempt)

## Light Verification: LIGHT_PASS
Child: b3 (B3：mail_record.task_execution_id 写入链路（后端）) — plan docs/plans/2026-08-16/b3-mail-record-execution-link-backend.md (plan identity commit 65b8de831a5f0edeafeae5683a2f15b79f7000a3), brief docs/plans/fast/2026-08-16-execution-order/children/b3/brief.md
Boundary: 7ca26a1129399fa5f0431fb7830dcecbaf4f9f3f..eb27b8d84a4286ce3ef92ca40acf98d761168121
Verifier: VerifyB3

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
|Authorized scope|PASS|Implementation commit `eb27b8d feat(fast-p): implement b3` touches EXACTLY the 10 authorized files (diff --name-only eb27b8d^..eb27b8d = 10/10, no frontend files, only migration V101). Boundary base..head additionally spans 3 prior docs commits (1f09fbb, 816cd31, 4e2c76f) carrying 5 docs/plans/fast/ process artifacts (b2 execution/fix-log/verify-log, b3 brief, ledger) — fast-p process records, not product code; constraint 5 "Exclude docs/plans/fast/ from the commit" satisfied for the implementation commit. No index.html/app.js/styles.css/batchSendTaskConsoleVisualFix.test.js changes anywhere in boundary.|
|Plan and invariants|PASS|V101 (file, 7 lines): `ADD COLUMN task_execution_id BIGINT NULL` + `CREATE INDEX idx_mail_record_task_execution ON mail_record (task_execution_id, id)`; no `FOREIGN KEY`, no `${`. MailRecord.kt:30 `val taskExecutionId: Long? = null` (default null). Exactly 2 MailRecord( construction sites pass taskExecutionId, both ManualOutreachTxHelper.kt:60/:111 (assignments :77/:129); recordSuccess (:36-92) and recordFailure (:107-143) keep trailing `taskExecutionId: Long? = null` param, bodies otherwise untouched (recordSuccess transition→updateAutomatically order preserved, N2a-3). ManualInitialOutreachService.kt: `grep -c "taskExecutionId = executionId"` = 5 (:745/:759/:774/:799/:816); NO new fields (no added private val/var); executionId in scope at all 5 (adjacent same-scope uses :710 updateProgressWithAccumulator, :750 updateProgressCounts; no findRecentByTaskType anywhere — grep exit 1; I2a-3/I2a-5). InitialOutreachService.kt:32 `sendInitialBatch(campaignId, size, taskExecutionId: Long? = null)`; 2 internal txHelper calls pass it (:97 recordSuccess, :108 recordFailure). MailAutomationScheduler.kt:52-73: onStarted-only wiring, `var executionId: Long? = null` + `onStarted = { executionId = it }`; queue branch (publisher != null) does NOT pass executionId; comment lines 52-55 document 队列分支 behavior. runAndRecord/onStarted signatures unchanged (TaskExecutionService.kt:107/:185 `onStarted: ((executionId: Long) -> Unit)? = null`, `onStarted?.invoke(running.id!!)` :126/:204; file not in diff; N2a-4). Tests: ManualOutreachTxHelperTest 4 call sites adapted + 2 new cases asserting taskExecutionId 4242L written (success + failure, I2a-2); InitialOutreachServiceTest 5 verify sites adapted + new case asserting 42L passthrough (I2a-3); ManualInitialOutreachServiceTest 3 verify sites adapted; new MailRecordTaskExecutionLinkTest asserts default null + V101 text (ADD COLUMN/index present, no FOREIGN KEY, no `${`). X-4 re-grep: 10 main construction sites (plan's 14 = +2 MailboxService read-side +data-class decl per X-4 receipt), 7 txHelper production calls (2 success: IOS:90, MIOS:741; 5 failure: IOS:100, MIOS:755/770/795/812), 12 listed test-dep sites all adapted.|
|Required commands|PASS|All run FRESH at head eb27b8d, JAVA_HOME=zulu-11: `mvn test -Dtest=ManualOutreachTxHelperTest` exit 0, Tests run: 6, Failures: 0, Errors: 0, Skipped: 0; `mvn test -Dtest=InitialOutreachServiceTest` exit 0, Tests run: 11, Failures: 0, Errors: 0, Skipped: 0; `mvn test -Dtest=ManualInitialOutreachServiceTest` exit 0, Tests run: 86, Failures: 0, Errors: 0, Skipped: 0; `mvn test -Dtest=MailRecordTaskExecutionLinkTest` exit 0, Tests run: 2, Failures: 0, Errors: 0, Skipped: 0 (counts from fresh surefire report files). `mvn test` (full) exit 0, BUILD SUCCESS, Tests run: 2493, Failures: 0, Errors: 0, Skipped: 4 — b2 terminal head (b3 base) was 2488/0/0/4 (b2 verify-log), +5 = exactly b3's new tests (TxHelperTest +2, InitialOutreachServiceTest +1, LinkTest +2); node --test exec inside mvn: 616 pass / 0 fail; all-green preserved vs recorded baseline 2456/0/0/4. `git diff --check` on base..head: exit 0, clean. FlywayMigrationIntegrationTest -DmigrationIt=true: NOT executed — no Docker daemon (docker info exit 1, no server version); plan acceptance explicitly allows 'on machines with Docker'; recorded unexecuted-with-rationale (O-1).|
|Downstream interfaces|PASS|b4 query `WHERE task_execution_id = ? ORDER BY id` served by V101 index second column `id` (ON mail_record (task_execution_id, id)). Queue-path rows stay null: MailAutomationScheduler.kt:62-65 queue branch passes no executionId; MailQueueConsumer.kt:32 calls `sendInitialBatch(message.campaignId, message.size)` → default null; comment records the behavior. N-7/M-5: other construction sites keep default null — AutoMailReplyService (4), ManualReplySendAttemptService (2), ManualExpertMailService (1), MeetingScheduleService (1) all unchanged (not in diff); grep of `taskExecutionId = ` in src/main shows only the 10 planned sites + pre-existing TaskProgressStore.kt:190 (TaskProgressLog, unrelated); the only MailRecord( sites receiving the value are the 2 in ManualOutreachTxHelper.|

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1: FlywayMigrationIntegrationTest (-DmigrationIt=true) unexecuted — Docker daemon not available on this machine (docker info exit 1); plan/brief acceptance permits this ('on machines with Docker'). No defect.
- O-2: Boundary base..head includes 5 docs/plans/fast/ process artifacts (b2 execution.md/fix-log.md/verify-log.md, b3 brief.md, ledger.md) committed in 3 separate docs commits before the implementation commit; they are outside the 10-file authorized list but are the fast-p process records themselves (constraint 5 excludes docs/plans/fast/ from the *implementation* commit, which is satisfied — eb27b8d contains exactly the 10 authorized files). Same pattern as b2's O-1/O-2. No product-code impact.
- O-3: Family main plan X-4 labels the test dependency set "11 处" but its own site list totals 12 (InitialOutreachServiceTest 5 + ManualOutreachTxHelperTest 4 + ManualInitialOutreachServiceTest 3); the implementation adapted all 12 listed sites (line-shifted per added lines). Plan arithmetic nit only — no implementation gap, no scope violation.

### Required Action
- COMPLETE_CHILD
