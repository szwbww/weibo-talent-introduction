## Light Verification: LIGHT_PASS_WITH_NOTES

- Child: 03 人工回复接入与历史日历下载 — docs/plans/2026-09-09/03-meeting-confirmation-send-download.md (amended by A1, plan commit 42c149c; 11 authorized files)
- Boundary: `6a1b54697778bdea64002516d5c63fbabedd0f13..a275366c1d29951814443d34bdab8b49da634232`
- Verifier: Child03Verifier
- Epoch: 2 (A1 resume), Attempt: 1
- Timestamp: 2026-09-09 21:30 +08:00
- Worktree: /Users/lukai/IdeaProjects/weibo-talent-introduction-fast-meeting-confirmation (branch fast/meeting-confirmation, HEAD a275366)
- Scope note: docs-only commits inside the range (31e058a 02-verify docs, 114af5a pause, 42c149c A1 plan amendment) touch only `docs/plans/**`; reviewed product/test diff only.

### Four Gates

| Gate | Result | Evidence |
|---|---|---|
| Authorized scope | PASS | `git diff --stat 6a1b546..a275366` — product/test changes = exactly the 11 authorized files (10 epoch-1 files + A1 11th `UnmatchedInboundTrustWorkbenchTest.kt`); all inside commit a275366 (`git show --stat a275366`: 11 files, +1067 −71). Intermediate commits 31e058a/114af5a/42c149c touch only `docs/plans/**`. A1 change minimal: 4 matcher-recorded `sendManualRichReply` stubs gain `+Mockito.isNull(), Mockito.isNull()` trailing matchers + comment each (diff 42c149c..a275366 on the 11th file: 12 insertions/4 deletions, all matcher/comment lines); `.thenThrow/.thenReturn` and no assertion or production code changed. |
| Plan and invariants | PASS | Direct code evidence below (I-1..I-4, T1..T4, A1). |
| Required commands | PASS | All 3 required commands run freshly on 2026-09-09 (details below), all exit 0; counts match expected head baseline. |
| Downstream interfaces | PASS | See Gate 4 below. |

**Gate 2 — plan/invariant evidence (worktree files at HEAD a275366):**

- I-1/T1 send-time re-validation: `PendingMailOperationService.kt` gains non-default-null `meetingConfirmationService` (:85); trailing `meeting: MeetingInput? = null, previewAttachmentSha256: String? = null` on `sendManualRichReply` (:171-172) and on `PendingManualRichReplyRequest` (:1418-1419); pair rule → 400 "会议附件配置不完整，请重新预览" (:190-191); `UnmatchedInboundMailController.manualRichReply` passthrough (:272-273, no second send API). After final subject/text/html render and placeholder validation (:315-328), `validateAndBuild(processingId, contact, account, meeting)` with server-resolved contact/account (:337-344) — before operatorAction/safety findings (:378+), suppression check (:429-433), `prepareAndClaim` (:447), SMTP (:455+). sha mismatch → 400 "会议配置已变化，请重新预览" (:348-349); IllegalArgumentException (01 template-disabled etc., MSG_TEMPLATE_UNAVAILABLE MeetingConfirmationService.kt:640) → 400 with 01 message (:343-347); full-text containment (not keyword search) via `normalizeBodyText` (CRLF→LF, NBSP→space, `\s+`→single space, trim; :936-942) applied to expected text, finalText, `htmlToPlainText(finalHtml)` — both must contain the FULL expected text else 400 "会议正文与附件不一致，请编辑会议后重新生成，或移除日历附件" (:351-358). Snapshot constructed once (:337/:360) and the same instance goes into `SendPayload.calendarAttachment` (:418) and `ComposedMail.calendarAttachment` (:454). Test evidence: `PendingMailOperationServiceTest` (base 2 → 11 tests, +9) uses the real 01 `MeetingConfirmationService` (only `listEnabled` mocked) and asserts `assertSame(snapshot, mail.calendarAttachment)`, sha/semanticSha/icsText/filename == preview, thread headers `in-1`/`in-1` for meeting sends; 400s for missing digest, digest-only, changed config, edited meeting time in body, disabled template (01 message), all with `prepareAndClaim`/`send` never invoked; outside-block edit still SENT; no-meeting old path leaves calendar + inReplyTo/references null; controller-shaped DTO (KotlinModule mapper) round-trips meeting + nullable digest defaults.
- I-2 gates unchanged: safety findings/`safetyWarningConfirmed`/`strongConfirmationText` branches untouched (blocked test asserts hallucinated-fact warning still throws `ManualSendSafetyBlockedException` unconfirmed, same meeting/digest succeeds after confirm with `assertSame` snapshot); suppression check (`emailSuppressionService.isSuppressed`) still precedes `prepareAndClaim` (:429-433); DEDUP/SAFE_RETRY/SENT/UNKNOWN/safe-failure branches untouched; QA/RAG exclusivity untouched (RagSendBridgeTest 10/0 and PendingMailOperationServiceTrustWorkbenchTest 59/0 green with injected mock `MeetingConfirmationService`). Thread headers: only the meeting branch sets `inReplyTo`/`references` = real processing `record.messageId` (:452-453, existing max255 422 check retained :421-425); no-meeting construction unchanged (defaults null) — covered by `no meeting old path` test.
- I-3 download: new `CalendarAttachmentController.kt` (84 lines) injects MailRecordRepository + MailSenderAccountRepository; GET `/api/mail/conversations/{contactId}/messages/{mailRecordId}/calendar-attachment` (:45). Explicit checks: `direction==OUTBOUND`, `mailType==MANUAL_RICH_REPLY`, `sendStatus==SENT`, `expertContactId==contactId` (:52-55); snapshot parsed via 01 `CalendarAttachmentCodec.parseOrNull` (:59; same parse at the timeline read site — both read sites use parseOrNull); active-account scope uses the same expression as `MailboxConversationService.activeAccountCodes` (:471-473) — `findAllByAccountCodeNot(MailSenderAccountService.SIMULATOR_ACCOUNT_CODE)` (:63), no extra enabled condition, no new user-level permission; uniform 404 "日历附件不可用" via `NoSuchElementException` (:69) → GlobalExceptionHandler NOT_FOUND channel (GlobalExceptionHandler.kt:24-26 keeps ex.message); response 200 with real stored UTF-8 bytes (never regenerated), Content-Type from snapshot, `ContentDisposition.attachment().filename(UTF-8)` (:74-76), real Content-Length (:77), `Cache-Control: private,no-store` (:79), `X-Content-Type-Options: nosniff` (:81).
- I-4 timeline: `MailboxConversationService` gains MailRecordRepository; per page filters `page.rows` MAIL_RECORD+OUTBOUND+SENT id set (:233-237) and calls `findAllById` exactly once (:241); `calendarAttachmentOf` (:374-391) requires row.source==MAIL_RECORD, record ownership `expertContactId == row.expertContactId` && `senderAccountCode == row.accountCode`, parse-valid snapshot → `ConversationCalendarAttachment(filename, byteLength, downloadUrl)`; union/keyset/count/current expert-material parsing untouched (diff adds only the snapshot block + trailing DTO arg :277); DTO trailing `calendarAttachment: ConversationCalendarAttachment? = null` keeps existing construction sites (MailboxConversationController.kt:97-98; DTO :112-115; downloadUrl built at MailboxConversationService.kt:68 + :395-397). INBOUND rows with equal numeric ids never read the outbound snapshot (guard :381 + INBOUND excluded from the batch set; test `inbound rows with the same numeric id never read the outbound snapshot` seeds real rows sharing numeric id). attachmentCount/firstAttachmentNames path untouched (expertMaterialService calls unchanged; both mysqlIt classes stub `resolveMessageAttachments` → empty and pre-existing assertions untouched).
- T3 tests: `CalendarAttachmentIntegrationTest` (5 tests, same authorized file) — real MySQL timeline batch read with one `findAllById` (`verify times(1)`), real HTTP bytes (`assertArrayEquals` with archived bytes), headers (Content-Type text/calendar…UTF-8, attachment+filename, real Content-Length, private,no-store, nosniff), uniform 404s (wrong contact / FAILED / corrupt JSON / old-row null / out-of-scope account acc-zz / nonexistent record) incl. message "日历附件不可用", corrupt snapshot rows null, no `findAllById` for inbound-only windows. `MailboxConversationControllerTest` adds `@MockBean MailRecordRepository` default empty (`MailboxConversationControllerTest.kt:52-63`); both mysqlIt contexts complete (21 + 5 tests green).
- T4: `OperatorStatusWriteSeamGuardTest` NoiseSite for UnmatchedInboundMailController.kt line 1118 → 1121 (diff: path/context unchanged, whitelist not widened). Fresh `rg -n 'operatorStatus = '` on the controller returns exactly 1121 (`operatorStatus = operatorStatus,`) and 215 (`request.operatorStatus`) — measured, not guessed.
- A1 (11th file): matcher-only change as described in Gate 1; test file runs 12/12 green.

**Gate 3 — required commands (all fresh 2026-09-09):**

| # | Command | Exit | Counts | Baseline comparison |
|---|---|---|---|---|
| 1 | `mvn test -Dtest=PendingMailOperationServiceTest,PendingMailOperationServiceTrustWorkbenchTest,RagSendBridgeTest,UnmatchedInboundTrustWorkbenchTest` (JAVA_HOME=zulu-11) | 0 | 92/0/0 (11+59+10+12); node JS 766/766/0 | 80/0/0 (3 classes) + 12 for A1 class → PASS |
| 2 | mysql-it gate: `env DB_URL='jdbc:mysql://localhost:3306/talent_introduction_mc03?...' DB_USERNAME=root DB_PASSWORD=root mvn -Pmysql-it -Dtest=MailboxConversationControllerTest test` after listener check | 0 | MailboxConversationControllerTest 21/0/0 | matches epoch-1 evidence 21/0/0 → PASS |
| 2b | Same env, `-Dtest=CalendarAttachmentIntegrationTest` (same-file sibling class; not matched by the mandated filter — see RECORD_ONLY O-2) | 0 | 5/0/0 | matches epoch-1 evidence 5/0/0 → PASS |
| 3 | `mvn test` full suite (JAVA_HOME=zulu-11) | 0 | 3310/0/10; node JS 766/766/0; BUILD SUCCESS | seed 3234/0/9; child-02 head 3300/0/9; this head expected 3310/0/10 + node 766 → PASS |

Listener check before gate 2 (fresh): `docker ps` shows `mailbox-refinement-mysql mysql:8.0.36 … 0.0.0.0:3306->3306/tcp Up 12 hours`; `lsof -nP -iTCP:3306 -sTCP:LISTEN` → OrbStack PID 2291 on `*:3306` (container forwarder); `SHOW DATABASES` lists `talent_introduction` (prior run — NOT used) and `talent_introduction_mc03` (dedicated gate DB). DB_URL env pointed the gate at mc03, never talent_introduction; container untouched.

**Gate 4 — downstream interfaces for child 04 (match brief exactly):**

- Timeline `calendarAttachment: { filename, byteLength, downloadUrl } | null` trailing nullable on `ConversationMessageItemResponse` (MailboxConversationController.kt:97-98/112-115), present only on MAIL_RECORD+OUTBOUND+SENT rows (service filter + ownership/parse guard); `downloadUrl` = `/api/mail/conversations/{contactId}/messages/{id}/calendar-attachment` (service :68/:395-397) which is exactly the controller `@GetMapping` route (CalendarAttachmentController.kt:45) — child 04 renders link/card from downloadUrl, bytes only via GET.
- Send request: optional `meeting` + `previewAttachmentSha256` flow through the EXISTING manual-rich-reply endpoint (`PendingManualRichReplyRequest` trailing nullable fields + controller passthrough; no new send API; `app.js mcHostSendRichReply` untouched). 400 texts verbatim: "会议附件配置不完整，请重新预览" / "会议配置已变化，请重新预览" / "会议正文与附件不一致，请编辑会议后重新生成，或移除日历附件" (PendingMailOperationService.kt:191/349/357) — byte-for-byte equal to plan strings.
- GET calendar-attachment returns exactly the archived original bytes (sha == preview == SMTP chain via stored snapshot JSON `calendarAttachmentJson` written by base ManualReplySendAttemptService finalize; download reads stored UTF-8 bytes only — no regeneration) — integration test asserts archived bytes equality and header contract.

### AUTO_FIX
- N/A — no four-gate violation found.

### RECORD_ONLY
- O-1: execution.md epoch-1 notes say PendingMailOperationServiceTest has "11 tests total incl. 7 new"; measured at base `git show 6a1b546` = 2 @Test, at head = 11 @Test → 9 new tests were added (the note undercounts by 2). Docs-internal miscount only; the 11-run total and all gate evidence are correct. Outside the four gates.
- O-2: surefire `-Dtest=MailboxConversationControllerTest` runs only that class (21 tests); the same-file `CalendarAttachmentIntegrationTest` (5 tests, plan T3) requires its own `-Dtest` entry (epoch-1 execution added it explicitly; the plan-mandated command name alone does not match it). Both classes were run freshly on the mc03 DB (26/0/0 combined) so no gate impact; worth knowing for future epochs re-running the IT.
- O-3: timeline metadata does not apply the active-account scope check inside `calendarAttachmentOf` (only row-level ownership); the controller download does. Not a violation — plan T2 scopes metadata to ownership/parse only and the page is already account-scoped by the SQL query; recorded as an observation.

### Required Action
- COMPLETE_CHILD
