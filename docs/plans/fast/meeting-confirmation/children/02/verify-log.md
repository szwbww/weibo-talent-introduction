## Light Verification: LIGHT_PASS
Child: 02 — docs/plans/2026-09-09/02-meeting-confirmation-delivery.md (MIME附件、防重和邮件存档)
Boundary: 73c53fe6689f14ddbab48d9f8724b651c036a469..6a1b54697778bdea64002516d5c63fbabedd0f13
Verifier: Child02Verifier
Epoch: 1 | Attempt: 1 | Timestamp: 2026-09-09 16:45 +0800
Plan SHA-256 (unchanged): 6f87732ccdbeb8f4b0b3199987a3e32f15fa7d062ec63b7f085a5c1a43c54109
Worktree: /Users/lukai/IdeaProjects/weibo-talent-introduction-fast-meeting-confirmation @ fast/meeting-confirmation, HEAD = 6a1b546

### Four Gates
| Gate | Result | Evidence |
|---|---|---|
| Authorized scope | PASS | `git diff --name-status 73c53fe..6a1b546` = the 8 authorized files only in code (`src/main/...` MailRecord.kt, IntroductionMailComposer.kt, SmtpMailDeliveryService.kt, ManualReplySendAttemptService.kt; `V123__add_mail_record_calendar_attachment.sql` new; 3 test files) + 3 docs under `docs/plans/fast/meeting-confirmation/children/01/` (execution/fix/verify logs) added by the in-range evidence commit 2ec6f48 (`docs(fast-p): record 01 light verification`, docs-only, per brief "controller commits evidence separately"). Implementation commit 6a1b546 alone (`git show --stat`, parent 2ec6f48): exactly 8 files, +863/−30. No product/test change outside the whitelist; helpers used by the new Flyway test (`Connection.execute/queryLong/queryString/columnExists`) pre-existed at base (FlywayMigrationIntegrationTest.kt:1036-1056 @73c53fe). |
| Plan and invariants | PASS | T1/I-1: V123 SQL = single `ALTER TABLE mail_record ADD COLUMN calendar_attachment_json LONGTEXT NULL` (no backfill/CHECK/`${}`); MailRecord.kt trailing `calendarAttachmentJson: String? = null` (data class, default null, MailRecord.kt:30-37). Flyway IT `V123 adds nullable calendar attachment json column leaving history null` (FlywayMigrationIntegrationTest.kt:60-105 @head): seeds 2 old rows at V23, migrates to 123, asserts column exists / DATA_TYPE=longtext / IS_NULLABLE=YES / both history rows NULL / zero non-NULL rows in table / column writable; 11 base `"122", flyway` latest-version assertions all flipped to `"123"` (plan said "10处"; base actually had 11 — every site updated, no semantic gap; executor flagged as count precision), V23 checksum assert (e0ba4f…), V23/V24/V116 targeted asserts and old checksums untouched (diff grep clean). T2/I-2: SendPayload trailing `calendarAttachment: CalendarAttachmentSnapshot? = null` (ManualReplySendAttemptService.kt:60-66); computeFingerprint appends the 2 length-prefixed segments (`meeting-calendar-v1`, `semanticSha256`) only when non-null after all 11 original fields (:93-96); shortKey unchanged `MANUAL_RICH:`+32=43 chars ≤ mail_send_attempt.mail_type VARCHAR(50) (V23:4). Golden test `fingerprint without calendar stays byte identical to pre-02 golden` asserts fa838dbceec46871ec1eae26e9ba4666d6f1ac0fda1bd36f872110efd38becda — independently recomputed by verifier from the exact 11 pre-change segments + payload → hash matches, so it is the genuine pre-02 byte stream, not circular. `same calendar semantics identical even when ics bytes change` (generatedAt differs → ICS differs → semanticSha256 equal → fp equal); `fingerprint changes when calendar semantics change` (zoomUrl → semanticSha256 differ → fp differ); child-01 `semanticSha256` (MeetingConfirmationService.kt:592-618) covers processingId/contactId/accountCode/recipient/salutation/zoneId/startInstant/endInstant/zoomUrl/signature and excludes generatedAt — time/URL/salutation/signature/account/target semantics all flow into the identity. I-1/I-3/I-4 four branches: `finalizeSuccess`/`finalizeFailure` each compute `snapshotJson = payload.calendarAttachment?.let { CalendarAttachmentCodec.serialize(it) }` and write it in both new and copy branches (:226, :259, :309, :344); copy branch `.copy(calendarAttachmentJson = snapshotJson)` overwrites, null explicitly clears (tests `finalizeSuccess copy branch without calendar clears prior failed record snapshot`, `finalizeFailure copy branch clears snapshot when retry has no calendar`); QA/RAG saves and `sourceInboundId = null` untouched. State machine single-path: `calendar attempt claims safe retry and finalize success on the same attempt id` (SAFE_RETRY_CLAIMED → same mail_record id 703 converges to SENT); `calendar attempt dedup sent returns old record without resend` (DEDUP_SENT, no claim); `calendar attempt unknown fails closed` (UNKNOWN + `verify(claimStatus, never())`); failures assert `sentAt == null`. T3/I-3: ComposedMail trailing default `calendarAttachment` (IntroductionMailComposer.kt:83-87); SMTP null branch preserved verbatim; calendar branch builds `MimeMultipart("mixed")` via javax.mail API only — part 1 wraps original alternative(plain,html) or text/plain, part 2 = `ByteArrayDataSource(icsText.toByteArray(UTF-8), contentType)` + filename + ATTACHMENT (SmtpMailDeliveryService.kt:63-98); no hand-concatenated MIME, no METHOD, no visible-body substitute, no duplicated html/plain; Message-ID/From/In-Reply-To/References/List-Unsubscribe and error classification stay in the same pre/post positions. Tests: Mockito-captured real MimeMessage → `writeTo` → reparse as `MimeMessage`; mixed exactly 2 parts, part-1 alternative exactly plain+html, ICS decoded bytes `assertArrayEquals(snapshot.icsText UTF-8 bytes)`, filename/disposition/contentType asserted, plain-body+ics case, no-calendar legacy shapes, header preservation; snapshot bytes fed from real child-01 generator `preview` output (IP-3/4/5 — no hand-written twin strings); A-1 `.eml` written to `target/meeting-confirmation.eml` (fixture decoded by verifier: single `meeting-2026-09-11-Professor-Basdogan.ics` part, DTSTART 20260911T070000Z = 15:00 China, no METHOD, quoted-printable text/calendar bytes decode to the ICS). |
| Required commands | PASS | 1) `JAVA_HOME=…/zulu-11.jdk/Contents/Home mvn test -Dtest=ManualReplySendAttemptServiceTest,SmtpMailDeliveryServiceTest` — exit 0; Tests run: 62 (33+29), Failures 0, Errors 0, Skipped 0 (surefire .txt/.xml); node 766 pass/0 fail. 2) `DOCKER_HOST=unix:///Users/lukai/.orbstack/run/docker.sock JAVA_HOME=… mvn test -Dtest=FlywayMigrationIntegrationTest -DmigrationIt=true -Dapi.version=1.40` — exit 0; Tests run: 19, Failures 0, Errors 0, Skipped 0 (incl. new V123 test); node 766/0. 3) `JAVA_HOME=… mvn test` (fresh, no migrationIt) — exit 0; Java run=3300, failures=0, errors=0, skipped=9 (Flyway IT gated), node 766 pass/0 fail. Baseline: seed 3234/0/9 + node 766/0; child-01 head 3283/0/9 + node 766/0 → child 02 adds +17 Java tests (3283→3300), node unchanged — matches execution.md exactly. |
| Downstream interfaces | PASS | `mail_record.calendar_attachment_json` (V123, LONGTEXT NULL) + `MailRecord.calendarAttachmentJson: String?` trailing (MailRecord.kt:36) — child 03 reads archives for download; null = no calendar (V123 IT proves historical rows stay NULL). Archive value = `CalendarAttachmentCodec.serialize` output (child-01 codec, MeetingConfirmationModels.kt:141-194, strict validate incl. 64KiB/hash/contentType/filename); round-trip `parseOrNull` equality asserted in all four snapshot tests → 03 parses via codec as required. Fingerprint: no-calendar byte-identical golden (verified above; 03 dedup semantics preserved); calendar semantics join identity via marker+semanticSha256 after all original fields, shortKey shape `MANUAL_RICH:`+32 unchanged. `ComposedMail.calendarAttachment` trailing default null (03 passes previewed snapshot through existing send path); SMTP multipart delivery exists with zero hand-written MIME. No public send entry added — diff contains no controller/endpoint; service-level tests drive `SmtpMailDeliveryService.send`/`prepareAndClaim` only. No new tables/columns beyond V123. |

### AUTO_FIX
- N/A

### RECORD_ONLY
- N/A (plan states “Flyway 10处最新断言 122→123”; the base file actually contained 11 `"122"` latest-version assertions and all 11 were flipped to 123 — a plan-count imprecision only, documented in execution.md, no acceptance semantics affected.)

### Required Action
- COMPLETE_CHILD
