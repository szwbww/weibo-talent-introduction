## Light Verification: LIGHT_PASS_WITH_NOTES
Child: p4b-template-gate-filter-frontend / docs/plans/2026-08-15/p4b-template-gate-filter-frontend.md
Boundary: 9cde7473..2250a666
Verifier: VerifyP4bGateFilter

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
|Authorized scope|PASS|Commit 2250a66 touches exactly the 4 authorized files: `src/main/resources/static/styles.css`, `src/main/resources/static/index.html`, `src/main/resources/static/app.js`, `src/test/js/batchSendTaskConsoleInteraction.test.js` (git show --name-status). No `.kt` / `.sql` anywhere in the commit. styles.css diff = 98 insertions / **0 deletions** (N4b-5: additions only; verified `git diff 9cde7473..2250a666 -- styles.css` shows only `+` lines). Range 9cde7473..2250a666 also contains docs-only changes from separate commit f35cef7 (p4a verify records + ledger.md) — not part of the p4b implementation commit; fast-p artifacts (brief.md/execution.md/ledger.md) remain unstaged in the worktree (git status: M brief.md, M execution.md, M ledger.md only).|
|Plan and invariants|PASS|I4b-1: two POSTs built from ONE `snapshot` via `Object.assign({}, snapshot, { gateFilterEnabled: gateOn })` (app.js:14337); `excluded = Math.max(0, offTotal - onTotal)` (:14357). Exactly ONE URL literal `"/api/mail/batch-send/recipients/preview"` at app.js:14335; `grep -c` == 2 only because of a PRE-EXISTING comment at :14841 (present in base 9cde7473 at :14641; base grep -c == 2) — no second endpoint. I4b-2: `seq = ++recipientPreviewRequestSeq[kind]` incremented exactly once per round at function start (:14330), shared by both requests; seq checked before render on success (:14345) AND on failure (:14370). I4b-3: `refreshBatchGateState(kind)` (app.js:14158) implements all 8 steps — unavailable when templateId empty (no request, G2), request fails (console.error once, no dialog, G3), esFields empty (warn text 「该模板未配置门禁字段…」, G1), all-dropped (warn 「该模板的必填字段均无法预筛（<dropped>）」, G6); checkbox disabled+unchecked in every unavailable path; label sync 不可用/已开启/已关闭 (:14139-14146); every path ends with `scheduleRecipientPreview(kind)`. I4b-4: badges only for the 6 filterable fields via `BATCH_GATE_FILTERABLE_FIELDS` (app.js:14122) — 6 keys byte-match `ExpertSearchService.ALLOWED_HAS_FIELDS` (ExpertSearchService.kt:24: setOf("employment","degree","institution","researchFields","patentTitles","recentWorkTitles"); identical key set & order) — plus `.batch-gate-keys-dropped` note for the difference (G4/G5). I4b-5: all 5 diff registration points present — #1 normalizeManualSnapshot `Boolean(v.gateFilterEnabled)` (:14649), #2 formatManualDiffValue 开启/关闭 (:14659), #3 computeManualDiffs fieldDefs (:14698), #4 computeAndRenderDiffs fieldMap `gateFilterEnabled: "manualFieldGateFilter"` (:14745), #5 clearAllDiffMarkers fields array appends `"manualFieldGateFilter"` (:14775). I4b-6: unavailable state sends `[post(false)]` only — `requests = gateAvailable ? [post(false), post(true)] : [post(false)]` (:14339-14340). S4b-1: CSS block appended after `.batch-tag-picker-empty` (styles.css:9043-9139) is byte-identical to plan (diff of extracted plan block vs impl block: empty); both DOM blocks byte-identical to plan (diff empty); editor block is last child of editor grid, manual block last child of manual grid (sed :1231-1247 / :1428-1446); `git diff index.html | grep -c 'style='` == 0 (no inline style). S4b-2: `batchGatePillHtml` three states (app.js:13387-13391): `!c.templateId` → is-na 「模板无门禁字段」; gateFilterEnabled → blue pill 「门禁过滤 · 开」 (INTENTIONAL plan deviation — per-row esFields unavailable without per-row requests — recorded in execution.md and asserted by G13); otherwise is-off 「门禁过滤 · 关」. N4b-1: all app.js hunks start ≥ :13381 > :11760; `populateExpertGateTemplateFilter`/`initExpertGateFilter` (:11589/:11612) byte-untouched. N4b-3: `scheduleRecipientPreview` body zero changed lines (hunk @@ -14160,27 only carries context `}, 500);`/`}`; changed lines all within refreshRecipientPreview + new helpers); `recipientPreviewRequestSeq` structure unchanged (:13257). `baseHintHtml` (:14319) reproduces the pre-change hint sentence byte-verbatim ("当前条件命中 <strong>…</strong> 位专家（其中未联系 …、可重试 …）").|
|Required commands|PASS|Re-run freshly this session: (1) `node --test src/test/js/batchSendTaskConsoleInteraction.test.js` → exit 0, tests 72, pass 72, fail 0 (G1-G14 all ✔); (2) `node --test src/test/js/*.test.js` → exit 0, tests 583, pass 583, fail 0 (baseline 569 + 14 new = 583, matches expectation); (3) `git diff --check` → clean, exit 0 (working tree and 9cde7473..2250a666 range both clean). All three match execution.md exactly (72/72, 583/583, no output).|
|Downstream interfaces|PASS|Last child — nothing downstream consumes p4b. `refreshRecipientPreview` exported behavior preserved for `scheduleRecipientPreview` callers: same 500ms debounce, same seq structure, same URL, same response-shape handling, failure text `预估失败：<msg>`; `recipientPreviewHintId`/`scheduleRecipientPreview` untouched. `buildConfigEditorRecipientSnapshot` (:14281) and `buildManualExecutionSnapshot` (:14301) both gain `gateFilterEnabled` (editor: `gateToggleChecked("editor")`; manual: `values.gateFilterEnabled` from readManualFormValues). saveBatchConfigEditor payload also carries it (:14419). G14 regression (P2b V1/V9 + P3b W1/W9) green.|

### AUTO_FIX
- N/A — no four-gate violation found; all checks pass with patch-anchored evidence.

### RECORD_ONLY
- O-1: `grep -c "recipients/preview" app.js` == 2, not 1 — the second hit is a pre-existing comment at app.js:14841 ("与预估（recipients/preview）同源（I-2）"), already present in base 9cde7473 (:14641, base count also 2). Operative requirement (exactly ONE URL literal, no second endpoint) satisfied; no action.
- O-2: Plan snippet `scopeParts.push(batchGatePillHtml(c))` implemented as appending a separate `<span class="batch-task-scope-line">` pill row after scopeHtml (app.js:13411-13412) instead of pushing into scopeParts. Behavior matches S4b-2's contract text ("在全部 scopeParts 之后追加一行（始终输出，三态之一）") and preserves the 「无限制」 branch for the empty-scope row (V9/W9 regression). Documented as implementation note in execution.md; not a behavioral deviation.
- O-3: `readManualFormValues` reads `#batchManualGateFilter.checked` inline (app.js:14629-14631) rather than calling `gateToggleChecked`, to keep the non-authorized `expertTagBatchFix.test.js` sandbox intact. Behavior equivalent (default false). Documented in execution.md.
- O-4: `fields/dropped` classification uses `Object.prototype.hasOwnProperty.call(BATCH_GATE_FILTERABLE_FIELDS, f)` instead of `f in …` (app.js:14192). Equivalent for the 6 real keys; guards against prototype-chain keys. Documented in execution.md.
- O-5: Review range includes docs changes (p4a brief/execution/fix/verify-log, ledger.md) committed in f35cef7 before p4b dispatch. These are the p4a verification records, not p4b scope; the p4b implementation commit 2250a66 contains exactly the 4 authorized files. No action.
- O-6: Existing tests adapted minimally (I-3/V5/W5/U-series/V9/W9 sandboxes gain `batchGateState`, `gateToggleId`, `gateToggleChecked`, `batchGatePillHtml` stubs; V5/W5 additionally assert payload `gateFilterEnabled === true`). Assertions strengthened, none weakened; all still green.

### Required Action
- COMPLETE_CHILD
