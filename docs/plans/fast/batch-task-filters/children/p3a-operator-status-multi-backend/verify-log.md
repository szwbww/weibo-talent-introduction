## Light Verification: LIGHT_PASS_WITH_NOTES
Child: p3a-operator-status-multi-backend / docs/plans/2026-08-15/p3a-operator-status-multi-backend.md
Boundary: f3ca1abe..1ba04713
Verifier: VerifyP3aOperatorStatus

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
|Authorized scope|PASS|`git diff --name-only f3ca1abe..1ba04713` = 14 files: 9 product/test files + 5 docs. Product files: V98 SQL (new), BatchSendTaskConfig.kt, BatchExecutionModels.kt, BatchSendTaskConfigService.kt, ManualInitialOutreachService.kt, ExpertSearchService.kt, OperatorStatusWriteSeamGuardTest.kt, BatchSendTaskConfigServiceTest.kt, ManualInitialOutreachServiceTest.kt — all ⊆ 11 A6-authorized files. BatchSendControlService.kt (#6) zero-diff (`grep operatorStatus` → 0 hits). BatchSendTaskRuntimeIntegrationTest.kt (#10) struck per brief pre-execution grep gate (`grep -c operatorStatus` → 0). No app.js / index.html / styles.css / BatchSendSettingService.kt / BatchSendConfigController.kt / applied-migration changes. |
|Plan and invariants|PASS|V98 verbatim per T3a-1/I3a-7: `ADD COLUMN operator_statuses_json TEXT NOT NULL AFTER discipline` + backfill CASE (NULL/''→'[]', else `CONCAT('["', operator_status, '"]')`) + `DROP COLUMN operator_status`; zero `${` literals. I3a-1: `grep -rn '"NOT_CONTACTED"'` campaign+expert = 16 hits in 8 files, every one must_not-exists / isNullOrBlank / expert_contact write-path / default-merge — zero `term` queries; serialized-filter test asserts no literal NOT_CONTACTED. I3a-2: `operatorStatusPredicate` (:201-211) pure `{"bool":{"must_not":[{"exists":{"field":"operatorStatus"}}]}}`; pure-predicate test (no email / EMAIL_INVALID) green. I3a-3: `operatorStatusesFilter` (:214-223) null on empty; callers append only via `?.let`; matchesExpert skips on empty. I3a-4: `onlyNotContacted = statuses.isEmpty() || statuses.all { it == "NOT_CONTACTED" }` → notContactedWithEmailDomainsFilters base without status filter; else status-agnostic base + operatorStatusesFilter — verbatim per plan T3a-3; base-switch + mixed tests green. I3a-5: matchesExpert any-OR verbatim; 4×5 ES/DB parity test green. I3a-6: `ALLOWED_OPERATOR_STATUSES` (:625) still `OperatorStatus.entries.map { it.name }.toSet()` (single authority, no literal set); whitelist require :282 + comma require :284; 4 validation tests green. M-2: updateLegacyConfig (:191) `operatorStatuses = parseOperatorStatuses(existing.operatorStatusesJson)`; M-2 preservation test green. M-3: `grep -rn "operatorStatuses" src/main/kotlin | wc -l` = **29** (matches execution.md); legacy returns (BatchSendConfig) carry no field; remaining singular `.operatorStatus` refs are only expert_contact / ExpertProfile paths (untouched). N3a-1: ExpertSearchService diff = 2 KDoc lines (三处→两处活体旁路) + 2 new companion functions; operatorStatusFilter / notContactedWithEmailFilters / buildExpertFilters / searchExperts bodies zero diff. N3a-2: two hardcoded-baseline equivalence tests (empty and ["NOT_CONTACTED"] on CANDIDATE) assert the verbatim pre-change filter list. A6 guard fix: `git show 1ba04713` touches ONLY OperatorStatusWriteSeamGuardTest.kt — ExpertSearchService pin 386→419 refresh + 10 dead noise-site removals (BatchExecutionModels 110/255; BatchSendTaskConfigService 77/110/190/304/423/551/569/587), 2 insertions/25 deletions entirely inside EXCLUDED_NOISE_SITES; zero judgment-logic change. |
|Required commands|PASS|All freshly re-run with JAVA_HOME=/Library/Java/JavaVirtualMachines/zulu-11.jdk/Contents/Home: 1) `mvn test -Dtest=ManualInitialOutreachServiceTest` → exit 0, surefire 78/0/0 (fresh report 17:17). 2) `mvn test -Dtest=BatchSendTaskConfigServiceTest` → exit 0, 51/0/0. 3) `mvn test -Dtest=OperatorStatusWriteSeamGuardTest` → exit 0, 1/0/0 **PASS after fix round** (matches execution.md expectation). 4) `mvn test -Dtest=FlywayMigrationIntegrationTest -DmigrationIt=true` → exit 1, recorded **SKIPPED: no Docker** with identical evidence to execution.md (`IllegalStateException: Docker is required for Flyway migration tests`; `DOCKER_HOST unix:///Users/lukai/.docker/run/docker.sock is not listening`; NoSuchFileException /var/run/docker.sock) — accepted as PASS-equivalent per brief; not faked. 5) `node --test src/test/js/*.test.js` → 559 tests, 559 pass, 0 fail, exit 0 (matches baseline 559/0). 6) `git diff --check` → exit 0 (worktree) and `git diff --check f3ca1abe..1ba04713` → exit 0 (range). All match execution.md counts. |
|Downstream interfaces|PASS|RecipientScope.operatorStatuses: List<String> = emptyList() (BatchExecutionModels.kt:55) + fromSnapshot trim/filter/distinct (:110) + matchesExpert any-OR. ExpertSearchService.operatorStatusesFilter(List<String>): Map<String,Any>? (null on empty, :214) + operatorStatusPredicate(String): Map<String,Any> (:201). BatchSendTaskConfigView.operatorStatuses: List<String> = emptyList() (BatchSendTaskConfig.kt:49). Entity column operator_statuses_json (entity `operatorStatusesJson: String = "[]"` :25, after discipline); `operator_status` GONE via V98 DROP. buildEsFiltersForLevel stable shape for P4a: mutable list — status-agnostic base (exists email + emailDomainsFilter + discipline + operatorStatusesFilter) or notContacted base; tags appended; regionsFilter appended last before return; P4a plan (:274) appends gate filters at "regionsFilter 之后、return filters 之前" → compatible. |

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1: BatchSendControlService.kt (authorized #6) carries zero diff — grep shows no operatorStatus reference; its snapshot construction flows through toExecutionSnapshot. Documented in execution.md; outside the light gate (no change to verify).
- O-2: BatchSendTaskRuntimeIntegrationTest.kt (authorized #10) struck from change list per brief's pre-execution grep gate — 0 operatorStatus refs; compiles unchanged against new shapes (defaults). Documented in execution.md.
- O-3: Docs-only commits inside the review range (611c901 p2b evidence, 2642b08 A6 plan amendment adding authorized file #11, ledger.md p3a row, p4b brief) are fast-p bookkeeping, not child code; the A6 plan amendment itself is the authority for the guard test file change.

### Required Action
- COMPLETE_CHILD
