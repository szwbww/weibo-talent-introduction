## Light Verification: LIGHT_PASS_WITH_NOTES
Child: p2b-email-domain-multi-frontend / docs/plans/2026-08-15/p2b-email-domain-multi-frontend.md
Boundary: e84229e0..f3ca1abe
Verifier: VerifyP2bEmailDomain

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
|Authorized scope|PASS|Implementation commit f3ca1ab touches exactly 3 files: `src/main/resources/static/app.js` (211±), `src/main/resources/static/index.html` (32±), `src/test/js/batchSendTaskConsoleInteraction.test.js` (335±). No styles.css / .kt / .sql anywhere in the range (`git show --stat f3ca1ab`). Note: range e84229e0..f3ca1abe also spans docs-only commit 0589bea (p2a verification records, p3b/p4a briefs, ledger update) — fast-p bookkeeping by the main flow, not the p2b implementation; the implementation commit itself contains zero `docs/plans/fast/` artifacts, satisfying the brief's commit rule.|
|Plan and invariants|PASS|I2b-1: `BATCH_MULTI_PICKER_REGISTRY` (app.js:13890) with exactly 2 initial entries `{options: batchProviderOptions, emptyText: "没有匹配服务商", previewKind}`; 7 functions read/set/render/toggle/open/close/bind (app.js:13927-14072) + `notifyBatchMultiPickerChanged` (13961) + `batchProviderOptions()` (13906, compat logic lifted verbatim from deleted `fillBatchConfigEditorProviderSelect`: string-vs-{domain} via `typeof providers[0]`). The 3 stated functional differences vs region family confirmed by side-by-side read: (1) options via `BATCH_MULTI_PICKER_REGISTRY[valueId].options()`, (2) empty text via `meta.emptyText`, (3) toggle/notify dispatch on `meta.previewKind`. grep counts: registry=4 (≥3), renderBatchMultiPicker=1. I2b-2: app.js hunks all ≥13386; insertion hunk `@@ -13882,6 +13882,151 @@` begins after `bindBatchRegionPicker` closing brace; no hunk covers :13627-13870; tag/region family read in full, zero changes; V10 green. I2b-3: both DOM blocks carry hidden input id=`<valueId>`, `<valueId>Chips`/`Search`/`Dropdown`, `data-tag-picker="<valueId>"`; V1 green. I2b-4: grep receipts — normalizeManualSnapshot :14426, formatManualDiffValue :14446 (`emailDomains` join("、")/"全部服务商"), computeManualDiffs fieldDefs :14477, computeAndRenderDiffs fieldMap :14523 (`emailDomains → manualFieldEmailDomain`), clearAllDiffMarkers :14554 (`manualFieldEmailDomain` still in fields array); V7/V8 green. I2b-5: normalize sorts (`slice().sort()` at :14426); V6 green. S2b-1/S2b-2: verbatim blocks; outer `<div>` not `<label>`; `.batch-config-diff-badge` + `.batch-config-diff-original` retained as direct children of `#manualFieldEmailDomain`; no `style=`; added classes all from master X-3/S2b reuse lists. S2b-3: app.js:13389 verbatim scope-line change; V9 green. N2b-2: expert-page lines (:3863/:3913/:3940/:4088/:4513/:4588-4623/:4662/:11415/:11433/:11673-11674) untouched — remaining `emailDomain` refs are all in that forbidden zone or old KV panel (:5820-6288), none in diff. N2b-3: no styles.css. N2b-5: `scheduleRecipientPreview` 500ms debounce + `refreshRecipientPreview` stale-seq discard intact (read app.js:14131-14162, not in diff). T2b-3/T2b-4 wiring: all plan table rows match (showBatchConfigEditor :13563, buildConfigEditorRecipientSnapshot :14101, saveBatchConfigEditor :14205, buildManualExecutionSnapshot :14120, deepCloneConfig :14288, fillManualFormDefaults :14308, fillManualFormFromDraft :14331, readManualFormValues :14409; both provider-select functions deleted — only comment mention remains at :13905; `"batchConfigEditorEmailDomain"` removed from change-listener array :15171-15175; `bindBatchMultiPicker` x2 at :15156-15157 adjacent to `bindBatchRegionPicker`). 3 recorded deviations assessed: all in authorized files, behavior-preserving, plan-consistent (see RECORD_ONLY D1-D3).|
|Required commands|PASS|Run freshly in this verification (final state, HEAD f3ca1ab): `node --test src/test/js/batchSendTaskConsoleInteraction.test.js` → exit 0, tests 48 / pass 48 / fail 0; `node --test src/test/js/*.test.js` → exit 0, tests 559 / pass 559 / fail 0 (baseline 549 + 10 new V1-V10); `git diff --check` → no output, exit 0 (both worktree and range e84229e0..f3ca1abe). All match execution.md (48 / 559 / clean). Maven deferred to merge gate per brief — not run.|
|Downstream interfaces|PASS|Registry + 7-function family present and generic (no per-valueId branching except registry lookup); registry currently holds exactly 2 entries — P3b's "register 2 entries ONLY" contract uncluttered. `readBatchMultiPickerValue` returns `string[]` (split/trim/filter(Boolean)); `setBatchMultiPickerValue` writes comma-joined hidden input (join(",")). Manual diff keys stay `emailDomains` (normalizeManualSnapshot, formatManualDiffValue, fieldDefs, fieldMap). DOM id `manualFieldEmailDomain` unchanged. `batchProviderOptions()` shared by both registry entries.|

### AUTO_FIX
- N/A — no proven four-gate violation; no plan-determined repair needed.

### RECORD_ONLY
- D1 (aria-label genericization): `renderBatchMultiPicker` chip remove-button writes `aria-label="移除 <label>"` vs region family's `"移除地区 <label>"`. Registry contract I2b-1 pins exactly `{options, emptyText, previewKind}` — no per-field remove-label is available, and keeping the region literal would mis-announce email-domain chips. A11y copy only; no behavior/DOM/class delta; tests and downstream interfaces unaffected. Consistent with the plan's own pattern of parameterizing field-specific copy (emptyText).
- D2 (typeof guard): `readManualFormValues` guards `readBatchMultiPickerValue` with `typeof === "function"` at :14409, mirroring the adjacent pre-existing `regions` line in the same function. Production behavior unchanged (function always defined); protects a non-authorized test harness (`expertTagBatchFix.test.js`) from the extracted-function pattern. Behavior-preserving.
- D3 (if/else collapse): `fillManualFormFromDraft` — after the plan-mandated deletion of both `fillBatchManualProviderSelect(...)` calls, the two branches were identical (`fillBatchManualTemplateSelector(d.templateId)`), so collapsing to a single call is the minimal clean result; `batchTaskState.manualSource` still used elsewhere (updateManualSourceInfo). Behavior-preserving.
- O1 (docs commit in range): review range includes docs-only commit 0589bea (p2a verification bookkeeping, p3b/p4a briefs, ledger). Not p2b implementation content; the implementation commit f3ca1ab contains exactly the 3 authorized files with no `docs/plans/fast/` artifacts. Informational only.

### Required Action
- COMPLETE_CHILD
