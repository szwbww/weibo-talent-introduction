## Light Verification: LIGHT_PASS
Child: p2a-email-domain-multi-backend / docs/plans/2026-08-15/p2a-email-domain-multi-backend.md
Boundary: 8d8dccb2..e84229e0
Verifier: VerifyP2aEmailDomain

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
|Authorized scope|PASS|`git diff --name-status 8d8dccb2..e84229e0 -- src/` = exactly 11 files, all authorized (plan list #1-#10 + `OperatorStatusWriteSeamGuardTest.kt` per A5): V97__add_email_domains_to_batch_send_task_config.sql (A), BatchSendTaskConfig.kt, BatchExecutionModels.kt, BatchSendTaskConfigService.kt, ManualInitialOutreachService.kt, BatchSendControlService.kt, ExpertSearchService.kt, OperatorStatusWriteSeamGuardTest.kt, BatchSendTaskConfigServiceTest.kt, ManualInitialOutreachServiceTest.kt, BatchSendTaskRuntimeIntegrationTest.kt. Forbidden-file grep (`app.js\|index.html\|styles.css\|BatchSendSettingService\|BatchSendConfigController\|V7x-V96__` over full range name-only list) = 0 hits. Docs commits inside the range (a7833d0/d698076/401ab4d/cd03169) touch plans/ledger/briefs only, no src/. Fix commit e84229e0 = only the guard test. |
|Plan and invariants|PASS|T2a-1 V97 (I2a-1/X-2): file read verbatim — `ADD COLUMN email_domains_json TEXT NOT NULL AFTER regions_json` (no DEFAULT on TEXT, V93 two-step) + backfill `CASE WHEN email_domain IS NULL OR email_domain = '' THEN '[]' ELSE CONCAT('["', email_domain, '"]') END` + `DROP COLUMN email_domain` (V92 precedent); `grep -c '\${'` = 0. I2a-2: `emailDomainsFilter` returns null on empty (ExpertSearchService.kt:120-133), call sites use `?.let { base.add(it) }` (:1254/:1261, no append on null); `matchesExpert` gates on `emailDomains.isNotEmpty()`; `normalizeAndValidate` empty → `"[]"`; `fromSnapshot` empty → `emptyList()`. I2a-3: single `bool.should` + `minimum_should_match: 1` verbatim per plan; wiring grep = `emailDomainsFilter` at :1254/:1261 (exactly 2) + `notContactedWithEmailDomainsFilters` at :1249 (exactly 1). I2a-4: `matchesExpert` (BatchExecutionModels.kt:79-83) `emailDomains.none { email.endsWith("@$it") }` any-OR with empty-skip; ES/DB parity test present. I2a-5: `normalizeAndValidate` trim→filter-notEmpty→distinct + `require(!it.contains(","))` (:265); same trim/filter/distinct in `fromSnapshot` (:107-108) and `parseEmailDomains` (:393-405, verbatim). I2a-6/M-2: `updateLegacyConfig` `emailDomains = parseEmailDomains(existing.emailDomainsJson)` (:188) — never rebuilt from `request.emailDomain`; return `emailDomain = view.emailDomains.firstOrNull().orEmpty()` (:203); `toLegacyConfig` `parseEmailDomains(row.emailDomainsJson).firstOrNull().orEmpty()` (:230). N2a-1/N2a-2: ExpertSearchService range numstat = `41 0` (additions only); old `notContactedWithEmailFilters` (:157) untouched and still used by `countPending` (:110). M-3: `grep -rn "emailDomains" src/main/kotlin | wc -l` = 42 (≥20); per-file counts BatchSendTaskConfigService 15 / ManualInitialOutreachService 9 / BatchExecutionModels 9 / BatchSendTaskConfig 4 / BatchSendControlService 1 / ExpertSearchService 4; every mapping point verified by direct read: entity :23, view :47, create cmd :71, update cmd :89, create :75, update :108, updateLegacyConfig :188/:203, toLegacyConfig :230, normalizeAndValidate :259-267, NormalizedConfig :531-area, ConfigFields :512-area, toView :421-area, 3× toFields, snapshot :18, RecipientScope :53, fromSnapshot :107-108, matchesExpert :79-83, toExecutionSnapshot parse block, buildEsFiltersForLevel :1249/:1254/:1261, log strings :1130/:1146, KV bridges :1281/:1297, BatchSendControlService :575. Residual single-value `emailDomain` refs are all KV-layer/pre-existing/dead-code (`BatchSendSettingService` :74/:96/:249/:262 untouched; `buildMaterialReminderEsFilters` :1110-1111 untouched per X-1; `countPending` KV reads :102/:111). Guard fix (A5): `git show e84229e0` = 11 insertions/11 deletions, all `NoiseSite` line-number-only refreshes with identical context strings — ExpertSearchService 345→386, BatchExecutionModels 107→110, 243→255, BatchSendTaskConfigService 74→77, 107→110, 187→190, 292→304, 399→423, 527→551, 545→569, 563→587 — matching the A5 table exactly; spot-checked :110/:386/:587 contexts at head; guard logic untouched (only the 22 line-number lines in range diff). |
|Required commands|PASS|All re-run freshly by this verifier with JDK zulu-11 (JAVA_HOME=/Library/Java/JavaVirtualMachines/zulu-11.jdk/Contents/Home): (1) `mvn test -Dtest=BatchSendTaskConfigServiceTest` → EXIT 0, surefire `tests=45 failures=0 errors=0 skipped=0`, BUILD SUCCESS (JS 549/549 pass in same build). (2) `mvn test -Dtest=ManualInitialOutreachServiceTest` → EXIT 0, `Tests run: 70, Failures: 0, Errors: 0, Skipped: 0`, BUILD SUCCESS. (3) `mvn test -Dtest=OperatorStatusWriteSeamGuardTest` → EXIT 0, `Tests run: 1, Failures: 0, Errors: 0`, BUILD SUCCESS (was FAIL pre-fix; round-1 fix verified effective). (4) `mvn test -Dtest=FlywayMigrationIntegrationTest -DmigrationIt=true` → EXIT 1, error `IllegalStateException: Docker is required for Flyway migration tests` (testcontainers `NoSuchFileException /var/run/docker.sock`; `docker version` → `failed to connect to the docker API at unix:///Users/lukai/.orbstack/run/docker.sock ... no such file or directory`; OrbStack daemon not running) — recorded **SKIPPED: no Docker** with fresh evidence, pre-agreed PASS-equivalent; the V97 migration itself never executed, so no migration verdict claim. (5) `node --test src/test/js/batchSendTaskConsoleInteraction.test.js` → EXIT 0, `tests 38, pass 38, fail 0`. (6) `git diff --check` (working tree) → EXIT 0; `git diff --check 8d8dccb2..e84229e0` → EXIT 0. |
|Downstream interfaces|PASS|`BatchSendTaskConfigView.emailDomains: List<String> = emptyList()` (BatchSendTaskConfig.kt:47); `BatchSendTaskConfigCreateCommand.emailDomains: List<String> = emptyList()` (:71); `BatchSendTaskConfigUpdateCommand.emailDomains: List<String> = emptyList()` (:89); `RecipientScope.emailDomains: List<String>` (BatchExecutionModels.kt:53) with any-OR `matchesExpert`; `ExpertSearchService.emailDomainsFilter(List<String>): Map<String, Any>?` (:120) + `notContactedWithEmailDomainsFilters(List<String>, String?)` (:137); entity column `email_domains_json` (entity :23, V97 ADD) with old `email_domain` column GONE (V97 DROP COLUMN); `buildEsFiltersForLevel` wired exactly at :1249 (`notContactedWithEmailDomainsFilters`) / :1254 / :1261 (`emailDomainsFilter`) — grep count: `emailDomainsFilter` 2 + `notContactedWithEmailDomainsFilters` 1 in ManualInitialOutreachService.kt. |

### AUTO_FIX
- N/A — no four-gate violation found; no repair needed.

### RECORD_ONLY
- O-1 (informational, not a gate issue): `OperatorStatusWriteSeamGuardTest.kt` comment at the ExpertSearchService NoiseSite still narrates the historical shift `:332 → :345`; the live entry is :386 per A5. Comment-only, guard logic untouched, guard passes. Left as-is.

### Required Action
- COMPLETE_CHILD
