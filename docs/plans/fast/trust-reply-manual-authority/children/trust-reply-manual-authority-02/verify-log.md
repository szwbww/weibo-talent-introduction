# Light Verification Log — trust-reply-manual-authority-02

Append-only, one report per attempt.

## Light Verification: LIGHT_PASS_WITH_NOTES

- Child: trust-reply-manual-authority-02
- Boundary: `7989af65e5c62d414d5a4557d79e3f06007bc4f9..ce2bc4a4d86e0da6df4151cc26b2da8778a73826` (commits `78e17225` impl 10 authorized files; `9705852` A1 plan amendment; `d1ede90` pause docs; `ce2bc4a` fix round 1 = SharedMount test rewrite)
- Verifier: Verify02 (2026-08-24, worktree `.worktrees/trust-reply-manual-authority`, branch `fast/trust-reply-manual-authority`, HEAD `ce2bc4a`)
- Plan identity: `docs/plans/2026-08-24/02-manual-fact-authority-workbench.md` @ `commit:9705852` (A1 amendment present at plan line 147)

### Four Gates

| Gate | Result | Evidence |
|---|---|---|
| 1. Authorized scope | PASS | Boundary non-docs changes = exactly 11 authorized files: QaFactSelectionService.kt, AiReplyDraftService.kt, AiReplyGroundedContentPlanner.kt, TrustReplyWorkbenchService.kt, trust-reply-workbench.js, QaFactSelectionServiceTest.kt, AiReplyDraftServiceTest.kt, TrustReplyWorkbenchItemFlowTest.kt, TrustReplyWorkbenchServiceTest.kt, trustReplyWorkbench.test.js (10 from plan) + trustReplyWorkbenchSharedMount.test.js (A1). `git diff --name-only 7989af6 ce2bc4a` shows 29 files: 18 docs (plans/children artifacts, ledger, plan amendment) + these 11; no other product/test/store/CSS/mail file. ce2bc4a touches ONLY the SharedMount test (14 lines, `git show ce2bc4a --stat`). Child-01 regression fixtures preserved: `asks claim against absolute spans when the request starts at nonzero offset` (QaFactSelectionServiceTest.kt:1557), `old saved matrix with signature request keys falls back to stale after extractor tightening` (TrustReplyWorkbenchServiceTest.kt:1003) and `inbound text change flips request keys and marks saved state stale` (:297) all present and unchanged; child-01-only files (QaRequestExtractor.kt, QaRequestExtractorTest.kt, AiReplyIntentCatalog.kt) untouched. |
| 2. Plan and invariants | PASS WITH NOTES | Stages 1-5 + I-1..I-8 + S-1 verified by code+test (details below); two RECORD_ONLY notes (per-item duplicate I-6 clause; I-5 same-request residual test gap). I-1/I-2: resolveMatrixSelection copies `factRuleIds=boundRuleIds=explicitIds` with `intentMatchedFactRuleIds=item.factRuleIds`, `intentMismatchFactRuleIds=explicitIds-filtered` (QaFactSelectionService.kt:209-253); sendQaRuleIds = ordered first-occurrence union of factRuleIds (:358); diagnostics never feed authorization/handling/version/send. I-3: `allowedHandlings = TrustReplyItemHandling.values().toList()` (:2081-2082); tests assert all 7 for GROUNDED/PARTIAL/UNSUPPORTED (ItemFlowTest:264-308, JS test `renders all seven backend handling options for every coverage`). I-4: single `requireHandlingPrerequisites` (TrustReplyWorkbenchService.kt:2097-2117) shared by generateItem (AiReplyDraftService.kt:429), adjustItem (:1135 area), validateLockedItem (:1353 area); 4 fact modes → `TRUST_REPLY_FACT_REQUIRED`, 2 instruction modes → `TRUST_REPLY_OPERATOR_INSTRUCTION_INVALID`; options never hidden (allowedHandlings constant + JS renders all, no disabled select). I-5: planner UNSUPPORTED early-return only when facts empty; residual = factRuleIds − usedSourceIds merged/appended to general.answer (AiReplyGroundedContentPlanner.kt:46-121); canonicalizeClaims mirrors projection (TrustReplyWorkbenchService.kt:1443-1466). I-6: matrix uniqueness gate removed (QaFactSelectionService.kt:207-208, legacy keeps checkWorkbenchUniqueness :282); validateNoDuplicateClaims deleted + both call sites (restore :918, assemble :1275) removed; canonicalMatrix ALREADY_ASSIGNED gate removed (:1723-1724); cross-request reuse tests pass (`matrix mode allows the same rule bound to two requests`, `assemble accepts the same source rule bound to two requests`). I-7: `canonicalFactIds = requestedFactIds = selection.sendQaRuleIds` (TrustReplyWorkbenchService.kt:1298-1304). I-8: auto/null/legacy call strict-only buildRequestFact (operatorBound deleted; strictCandidateRules keyword-gated :452-457); `auto path never bypasses keyword matching even with bound facts present` passes. S-1: mismatch hint = fixed text `人工选择已生效；系统未匹配到对应意图，已记录供后续优化。` in existing `.muted` span, same `item-facts-dropped` location, no inline style/new class (trust-reply-workbench.js:1892-1894); `used` state branch + factOwnerById + `已用于摘要 N` + addFact owner block removed (:588-590, 1620-1625, 1856-1866); styles.css untouched (unused `data-state="used"` CSS retained per S-1). A1 SharedMount rewrite: test 2344-2399 now asserts cross-request fact available/no-used/no-`已用于摘要 1`/no-disabled and keeps remove→re-bootstrap→matrix-release flow. |
| 3. Required commands | PASS | Ran fresh in worktree: (1) `JAVA_HOME=/Library/Java/JavaVirtualMachines/zulu-11.jdk/Contents/Home mvn -q -Dtest=QaFactSelectionServiceTest,AiReplyDraftServiceTest,TrustReplyWorkbenchItemFlowTest,TrustReplyWorkbenchServiceTest test` → exit 0; Kotlin 366 tests 0 failures (QaFactSelectionServiceTest 68, AiReplyDraftServiceTest 183, ItemFlowTest 52, WorkbenchServiceTest 63 per surefire XML); exec-plugin node-test phase globbed `src/test/js/*.test.js` → 731 tests / 112 suites / 0 fail, includes rewritten SharedMount `keeps a cross-request fact selectable in the picker and releases facts on remove`. (2) `node --test src/test/js/trustReplyWorkbench.test.js` → exit 0, 31 tests 31 pass. (3) `git diff --check` → exit 0. |
| 4. Downstream interfaces | PASS | `assemble(request)` signature/semantics stable; canonical ids from `selection.sendQaRuleIds` (I-7, child 03). Coverage DTO carries `intentMatchedFactRuleIds`/`intentMismatchFactRuleIds` + natural status + `unrecognizedAsks` for child 04 (TrustReplyWorkbenchService.kt:1974-1982; toCoverage :1949-1976). DTO rename `droppedFactRuleIds`→matched/mismatch complete (no `droppedFactRuleIds` reference remains in main/ or static/, JS test asserts absence). Matrix serialization = `requestKey + factRuleIds` only (serializeRequestFactSelections trust-reply-workbench.js:547-549; test asserts Object.keys == [factRuleIds, requestKey]). No mail-send (`PendingMailOperationService`, `ManualReplySendAttemptService`), schema, or store files changed. |

### AUTO_FIX

(none)

### RECORD_ONLY

1. **I-6 per-item duplicate ids no longer hard-blocked (gate-2 note, non-blocking).** Plan I-6: 「单条 request 内重复 id … 仍是脏输入并硬拦」. Base `7989af6` rejected duplicates anywhere in the flattened matrix (incl. within one request) via `checkWorkbenchUniqueness(selectionsByRequest.flatten())` (base QaFactSelectionService.kt:204). The implementation removed that call and no per-item replacement exists: `validateExplicitSelection` checks only not-found/disabled/NEVER/blank-body (QaFactSelectionService.kt:392-408), `resolveMatrixSelection` copies `explicitIds` verbatim (:235-241), `validateMatrixKeys` checks only key uniqueness + `<=0` ids (TrustReplyWorkbenchService.kt:1769-1785), canonicalMatrix gate removed (:1723-1724). Direct API call with `[1,1]` for one request is silently accepted (matrix stores the duplicate; `sendQaRuleIds`/`canonicalFactIds` dedupe via `.distinct()`). Product UI cannot produce it (addFact dedupes, trust-reply-workbench.js:1625). Not AUTO_FIX: the plan's operative stages (1.1 lists exactly 4 validation checks; 4.1 deletes the only duplicate gate) never specify a per-item replacement or error code, so the repair (reject per-request duplicates, e.g. TRUST_REPLY_FACT_SELECTION_INVALID at the existing per-request validation wrap, vs per-request checkWorkbenchUniqueness) is not uniquely determined by the plan. Suggested repair for human/fixer consideration: per-request distinct check in `resolveMatrixSelection` throwing `TRUST_REPLY_FACT_SELECTION_INVALID`.
2. **I-5 same-request supported+residual claim path has no direct unit test.** Planner/canonicalizeClaims code for "supported intent claim + residual general.answer in one request" is verified by reading (AiReplyGroundedContentPlanner.kt:73-121, TrustReplyWorkbenchService.kt:1443-1466), and the no-supported-intent residual case is tested (`bound fact adopted on partial item supports exactly one general answer claim`, ItemFlowTest:310-328), but no test asserts a single item emitting BOTH a named supported claim and a residual `general.answer` claim together (the plan acceptance item 「supported intent fact 与 mismatch fact 同时存在时，planner/claims 包含 supported claim 和 residual general.answer」). Coverage observation only — code is correct per reading.
3. **Stale-but-harmless fixtures referencing removed fields/premises.** (a) `src/test/js/overlayAndDialogContrast.test.js:69,90` still emits `droppedFactRuleIds: []` in fake coverage items — inert (workbench JS ignores unknown fields; file not in boundary and passes in the 731). (b) ItemFlowTest:1092 `bound facts never enter the send audit rule ids` asserts a `boundRuleIds≠factRuleIds` split that matrix/auto/legacy paths can no longer produce; it passes and only pins mechanical workbenchResult behavior, no plan-02 invariant contradicted. No action required; documented for awareness.

### Required Action

- COMPLETE_CHILD

## Light Verification: LIGHT_PASS_WITH_NOTES

- Child: trust-reply-manual-authority-02
- Boundary: `7989af65e5c62d414d5a4557d79e3f06007bc4f9..f6f577f` (commits `78e17225` impl; `ce2bc4a` fix round 1 = A1 SharedMount rewrite; `f6f577f` fix round 2 = O1 per-item duplicate hard block)
- Verifier: Verify02b (2026-08-24, worktree `.worktrees/trust-reply-manual-authority`, branch `fast/trust-reply-manual-authority`, HEAD `f6f577f`)
- Plan identity: `docs/plans/2026-08-24/02-manual-fact-authority-workbench.md` (amended; A1 authorized file `src/test/js/trustReplyWorkbenchSharedMount.test.js` at plan line ~147)
- Round: 2 (re-verification after controller reclassified O1 as AUTO_FIX; fix round 2 = `f6f577f`)

### Four Gates

| Gate | Result | Evidence |
|---|---|---|
| 1. Authorized scope | PASS | Boundary `7989af65..f6f577f` non-docs changes = exactly the 11 authorized files (10 plan files + `src/test/js/trustReplyWorkbenchSharedMount.test.js` per A1 amendment); 29 changed paths total = 18 docs/plans artifacts + these 11; no mail/schema/store/CSS/other product file. `git show f6f577f --stat` = 2 files only (`QaFactSelectionService.kt` +11, `QaFactSelectionServiceTest.kt` +31). Child-01 fixtures preserved: SharedMount rewrite (ce2bc4a) keeps remove/release assertions (trustReplyWorkbenchSharedMount.test.js:2393-2399), only used-owner gating assertions updated (no `data-state="used"` / `已用于摘要 1` at :2388-2389). |
| 2. Plan and invariants | PASS | Re-confirmed stages 1-5 + I-1..I-8 + S-1; O1 AUTO_FIX verified in code and by regression test. I-1: `resolveMatrixSelection` copies `factRuleIds=boundRuleIds=explicitIds` (QaFactSelectionService.kt:243-248); `sendQaRuleIds` = per-request ordered first-occurrence union (:361-364). I-2: `intentMatchedFactRuleIds=item.factRuleIds`, `intentMismatchFactRuleIds=explicitIds.filter { it !in matchedIds }` (:249-252); diagnostics never enter authorization/allowedHandlings/version/send (DTO comment :156). I-3: `allowedHandlings = TrustReplyItemHandling.values().toList()` (TrustReplyWorkbenchService.kt:2084-2085). I-4: shared `requireHandlingPrerequisites` → `TRUST_REPLY_FACT_REQUIRED` on empty facts for 4 fact handlings; instruction non-empty ≤500 chars (:2095-2115). I-5: planner residual channel — UNSUPPORTED+facts skips missing-only early return; `residual = factRuleIds - usedSourceIds` merged/appended as `r{index}:general.answer` (AiReplyGroundedContentPlanner.kt:46-113). I-6: no backend `TRUST_REPLY_FACT_ALREADY_ASSIGNED`/`TRUST_REPLY_DUPLICATE_CLAIM`; `validateNoDuplicateClaims` removed; NEW per-request duplicate → 422 `TRUST_REPLY_FACT_SELECTION_INVALID` inside `resolveMatrixSelection`'s `validateExplicitSelection` block only (QaFactSelectionService.kt:218-222); cross-request reuse accepted. I-7: assemble `canonicalFactIds = requestedFactIds = selection.sendQaRuleIds` (:1303/:1306). I-8: `select()` dispatch unchanged — matrix→`resolveMatrixSelection`, legacy→`resolveLegacySelection` (retains `checkWorkbenchUniqueness` :291), auto/null→`resolveAutoSelection`; the round-2 fix is in the matrix path only. S-1: no `used` state produced (JS :1861), fixed mismatch hint text `人工选择已生效；系统未匹配到对应意图，已记录供后续优化。` (:1897), 7 options rendered from `availableHandlings` (:2254), serialization `requestKey + factRuleIds` only (:1294); no new class/inline style. |
| 3. Required commands | PASS | Ran fresh in worktree. (1) `JAVA_HOME=/Library/Java/JavaVirtualMachines/zulu-11.jdk/Contents/Home mvn -q -Dtest=QaFactSelectionServiceTest,AiReplyDraftServiceTest,TrustReplyWorkbenchItemFlowTest,TrustReplyWorkbenchServiceTest test` → exit 0; surefire: 367 Kotlin tests, 0 failures (QaFactSelectionServiceTest 69, AiReplyDraftServiceTest 183, ItemFlowTest 52, WorkbenchServiceTest 63); exec-plugin node-test phase → 731 tests / 112 suites / 0 fail. (2) `node --test src/test/js/trustReplyWorkbench.test.js` → exit 0, 31 tests 31 pass. (3) `git diff --check` → exit 0 (round-1 verify-log.md trailing blank line fixed controller-side; current worktree clean — `git status` shows only uncommitted docs/plans/fast artifacts with no whitespace errors). |
| 4. Downstream interfaces | PASS | `assemble(request: TrustReplyAssembleRequest)` signature stable (TrustReplyWorkbenchService.kt:1212); canonical ids from `selection.sendQaRuleIds` for child 03 (I-7). Coverage DTO carries `intentMatchedFactRuleIds`/`intentMismatchFactRuleIds` + natural status + `unrecognizedAsks` for child 04 (:151/:158-162/:1973-1982). DTO rename complete — no `droppedFactRuleIds` remains in main/ or static/. Matrix serialization = `requestKey + factRuleIds` only (JS :1294; JS test asserts the key set). No mail-send (`PendingMailOperationService`, `ManualReplySendAttemptService`), schema, or store files in boundary. |

### AUTO_FIX

(none outstanding — O1, reclassified AUTO_FIX by controller, was applied in `f6f577f` and verified: per-item duplicate `[1,1]` in one request's matrix selection → 422 `TRUST_REPLY_FACT_SELECTION_INVALID`; regression test `matrix mode hard rejects a duplicated fact id within one request` covers both clauses, including same id across two requests accepted with `sendQaRuleIds=[1]`.)

### RECORD_ONLY

1. **I-5 same-request supported+residual claim path still has no direct unit test (O2, persists).** Planner/canonicalizeClaims code verified by reading (AiReplyGroundedContentPlanner.kt:73-121, TrustReplyWorkbenchService.kt:1443-1466); no test asserts one item emitting BOTH a named supported claim and a residual `general.answer` together (ItemFlowTest:310-328 covers the residual-only case). Coverage observation only.
2. **Stale-but-harmless fixtures (O3, persists).** (a) `src/test/js/overlayAndDialogContrast.test.js:69,90` still emits `droppedFactRuleIds: []` — inert (workbench JS ignores unknown fields; file not in boundary, passes in the 731). (b) ItemFlowTest:1092 `bound facts never enter the send audit rule ids` pins a boundRuleIds≠factRuleIds split no plan-02 path produces; passes and contradicts no invariant.
3. **New: inert stale error-map entries in the authorized JS file.** `trust-reply-workbench.js:54,64` still list `TRUST_REPLY_FACT_ALREADY_ASSIGNED` / `TRUST_REPLY_DUPLICATE_CLAIM` display strings in `WORKBENCH_ERROR_TEXT` — backend can no longer throw those codes (I-6), so the entries are dead lookups; no logic references them. SharedMount test:975 similarly asserts the retained-but-unused `[data-state="used"]` CSS rule (S-1 permits keeping unused legacy CSS).

### Required Action

- COMPLETE_CHILD
