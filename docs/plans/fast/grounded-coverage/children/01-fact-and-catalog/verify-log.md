# Verify Log — 01-fact-and-catalog (P1)

## Light Verification: LIGHT_PASS_WITH_NOTES
Child: 01-fact-and-catalog — `docs/plans/2026-08-19/01-fact-and-catalog.md` (identity `commit:af1723f`)
Boundary: `af1723f37021328f8ffa61261504727e514fbb4b`..`f5c09382744c0da8a537610af6145974ee1fcaf4`
Verifier: `Verify01FactCatalog`
Date: 2026-08-19 (fresh audit; JDK11 zulu-11)

### Four Gates

| Gate | Result | Evidence |
|---|---|---|
| Authorized scope | PASS | `git diff --name-status af1723f..f5c0938` — product files are exactly the 6 authorized files (A: `V105__add_programme_identity_facts.sql`, `ProgrammeIdentityFactsMigrationTest.kt`; M: `QaCoverageKeyCatalog.kt`, `AiReplyIntentCatalog.kt`, `AiReplyIntentCatalogTest.kt`, `QaFactSelectionServiceTest.kt`). `TrustReplyWorkbenchService.kt` absent from diff (I-8 STALE path untouched). 13 docs files in the boundary come from seed commit `841c633` (controller evidence, not product). N6: `git status --porcelain src/main/resources/db/migration` clean; boundary adds only V105 in migration dir. |
| Plan and invariants | PASS | I-1: `verification_evidence` in AiReplyIntentCatalog.kt: head=1 (base=0); A-4 bidirectional guards present with explicit exemption constants + comments. I-2: V105 keyword lines contain 0× `programme` (all 19 occurrences are comments/reply_subject/body/coverage literal, per plan-mandated text); mechanical parity test runs production `scoreRuleIntentAlignment` for `official name` / `government body` / `form of collaboration` and passes. I-3/N2: exactly one `requiresProfile = true` (line 116, `expertise.programme_fit`) unchanged; new intents carry no `requiresProfile` arg (default false). I-4: V105 INSERT column list carries `coverage_keys` literals `programme.name` / `governance.sponsor_level`; asserted by migration test. I-5: 2× `WHERE NOT EXISTS (SELECT 1 FROM qa_rule WHERE reply_subject =`; id=6 CONCAT+3 NOT LIKE guards, id=18 CONCAT+2 NOT LIKE guards, both `updated_at = updated_at`; asserted by migration test. I-6: id=6 appends only `form of collaboration`/`forms of collaboration`/`how the collaboration works` (no `partner companies`/`collaboration with`); negative test green. I-7: letter fixture byte-identical (813 chars) in both test files vs plan line 5 (verified char-for-char, ends `...continue the conversation by email.`). I-8: file not in diff. N1: existing 20 definitions untouched (diff appends only); 3 existing-fixture regression tests. N3: evidence assertions `finance.arrangements→[8]`, `ip.arrangements→[34]` pass. N4: only id=6/id=18 keyword appends; no other rule columns touched. N5: existing 29 catalog keys untouched (diff appends 2 + trailing comma). |
| Required commands | PASS | `mvn test -Dtest='AiReplyIntentCatalogTest,QaFactSelectionServiceTest,ProgrammeIdentityFactsMigrationTest'` (fresh, JDK11): exit 0, BUILD SUCCESS; surefire AiReplyIntentCatalogTest tests=30 err=0 fail=0, QaFactSelectionServiceTest tests=35 err=0 fail=0, ProgrammeIdentityFactsMigrationTest tests=6 err=0 fail=0. Full regression: not rerun; implementer claim (2563/0/0/4 skipped, exit 0) verified via surefire XML aggregate (198 files: tests 2563, failures 0, errors 0, skipped 4) matching execution.md exactly. `git diff --check` af1723f..f5c0938: exit 0. |
| Downstream interfaces | PASS | After P1: `AiReplyIntentCatalog.definitions` gains `programme.name`/`governance.sponsor`/`collaboration.form` (plan A-2 exact keys/titles/aliases/coverage); `QaCoverageKeyCatalog` gains `programme.name`/`governance.sponsor_level` appended at list end (declaration-order note respected); V105 exists with exactly 4 statements (2 INSERT + 2 UPDATE); C-1 test asserts orthopaedic letter matches exactly 5 intents (P2 dependency). S1/S2 bodies byte-identical to plan (327/433 chars); test-pool keywords for S1/S2/id=6/id=34 match post-V105 production values. |

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1 (implementer D-1): A-4 catalog-side exemption constant additionally lists `application.timeline`. Verified factual: key exists in catalog (QaCoverageKeyCatalog.kt:83) and is referenced only by a runtime `def.copy(requiredCoverageKeys = ...)` in `matchIntents` (AiReplyIntentCatalog.kt:342), never by any definition's required/alternative list — hence an O4-class orphan, so the plan's literal exemption set (`general.answer`, `application.required_materials`, `work.relocation`) would make the plan-mandated guard fail on current facts. Resolution uses the plan's own mechanism (explicit constant + comment; removal turns test red). Plan internally inconsistent; no unique plan-mandated correction; guard still enforced. Not a four-gate violation.
- O-2 (implementer D-2): C-2 test's Funding support fixture (id=8) adds keyword `remuneration` beyond production keywords. Verified: production id=8 keywords (V3: `salary,subsidy,funding,compensation`; V81 appends `advisory role compensated,is the advisory role compensated`) contain NO substring of the verbatim letter (checked each: false), while the letter's finance phrase is `remuneration` (a `finance.arrangements` alias, AiReplyIntentCatalog.kt:202). The plan mandates on this letter 5/5 SUPPORTED with Funding support in `factRuleIds` and N3 evidence `finance.arrangements→[Funding support]` — impossible with production keywords; plan internally inconsistent. Fixture-tailored keywords match the established convention in this test file (e.g. existing `rule(...)` fixtures at lines 42/47/126 use scenario keywords, not production values). N3 assertions still enforced. Note for downstream (not plan scope): with real rule keywords a letter phrased exactly like this would not bind Funding support in production — pre-existing behavior, not introduced by P1.
- O-3 (implementer D-3): plan acceptance "`grep -c 'programme' V105__*.sql` = 0" is unsatisfiable against the plan's own mandated text — `reply_subject 'Programme name and public visibility'`, both bodies, and the coverage literal contain `programme` (raw count 19, none in keyword lines). The operative I-2 invariant (no rule keyword contains `programme`) holds and is enforced by the mechanical parity test. Plan acceptance text internally inconsistent; implementer's reading is the faithful semantic one.
- O-4 (implementer D-4): C-2 pool reading of "三条新规则 + id=6" as 2 new facts + id=6 (+ Funding support + Pre-contract IP boundary = 5 rules). Plan's own `factRuleIds` enumeration (新事实①②、id=6、Funding support、Pre-contract IP boundary) matches this pool exactly; id=18 binding is exercised by plan's A-2 acceptance, not this letter scenario. Faithful reading of internally inconsistent plan wording.
- O-5 (implementer D-5): `<正文见下>`/`<同 reply_body>` plan placeholders substituted with the plan's verbatim bodies; verified byte-identical to plan text (S1 327 chars, S2 433 chars, `reply_body` = `answer_body`). Faithful reading; the placeholders are plan-internal directives.

### Required Action
- COMPLETE_CHILD

Verdict rules applied: no gate PAUSE, no AUTO_FIX, RECORD_ONLY present → LIGHT_PASS_WITH_NOTES.
