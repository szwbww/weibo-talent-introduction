# Verification Log — 02-unrecognized-request-detection (P2a)

## Light Verification: PAUSE
Child: 02-unrecognized-request-detection — docs/plans/2026-08-19/02-unrecognized-request-detection.md (plan identity commit:af1723f37021328f8ffa61261504727e514fbb4b; sha256 440b5b5536b1ae1fdef28e3c5541e3fd7660ba39ffcf920c6105d5177a9ebea6)
Boundary: f5c09382744c0da8a537610af6145974ee1fcaf4..1df49e8e37f24d4040e27e1d78052a8645253e2b
Verifier: Verify02UnrecognizedAsk (fresh agent, no writer conversation; read-only except this report)

Boundary contains 11 files: 9 product/test (8 authorized + AiReplyDraftService.kt) and 2 docs (child-01 evidence committed at 481db67, not product). Implementation commit 1df49e8; prior 841c633/481db67 are controller/evidence commits.

### Four Gates
| Gate | Result | Evidence |
|---|---|---|
| Authorized scope | PAUSE | 9 product/test files changed; 8 are the plan's authorized list. `AiReplyDraftService.kt` (12 insertions / 2 deletions) is NOT authorized and no `## Amendments` row exists in the ledger (ledger.md `## Amendments` table empty; header `Amendments: N/A`). The edit is confined to the two data classes `RequestFactItem` (AiReplyDraftService.kt:349) and `ResolvedQaRules` (:361), whose physical home is this file — the plan's C-1 field mandates (`RequestFactItem.unrecognizedAsks`, `ResolvedQaRules.unrecognizedAskCount`/`enumeratorAvailable`) force the content of the edit, and the plan's own audit section ("ResolvedQaRules 的下游") cites AiReplyDraftService.kt:362, i.e. the plan's 变更文件清单 attribution of "两个新字段" to QaFactSelectionService.kt is stale. HOWEVER the change does not stay inside the plan's named types/fields: (1) `ResolvedQaRules` gains two extra defaulted fields `enumeratorEnumerated`/`enumeratorClaimed` beyond the plan's stated field set; (2) the plan's C-2 wiring (workbench calls enumerate() and passes the result through a new `selectForWorkbench` parameter) was replaced by internal enumeration inside `QaFactSelectionService.selectForWorkbench` — signature preserved because exact-argument Mockito stubs/verifies in unauthorized tests (TrustReplyWorkbenchServiceTest.kt:221/251/355-357/812/925/937/1198-1200/1378, TrustReplyWorkbenchItemFlowTest.kt:266/593/614/983/1186) would break; (3) the plan's C-3 auto-path log is emitted from `QaFactSelectionService.select()` (source=AUTO contactId=0), firing for every select() caller — PendingMailOperationService.kt (10 sites) and AiReplyDraftService.resolveQaRules (training/unmatched generate) — beyond the plan's two stated decision points (workbench bootstrap + auto-reply decision). The plan's literal C-3 location (auto-reply services) is itself unimplementable within the authorized list (no auto-reply service file is authorized), so the plan is internally inconsistent on the file list; resolving that contradiction is a contract decision requiring an amendment, not a verifier self-authorization. Unauthorized file cannot be auto-fixed into authorization → PAUSE (missing authority / scope expansion). |
| Plan and invariants | PASS | I-1: `InboundAskEnumerator.parse()` (InboundAskEnumerator.kt:96-128) — whitespace-folded `contains` (no lowercase), folded quote length >= 8, dedupe, 12-item cap with explicit truncation log; 5 I-1 cases + dedupe + fenced-JSON + cap green (13 tests). I-2: `git diff -U0 f5c0938..1df49e8 -- TrustReplyWorkbenchService.kt` has zero +/- lines containing requestKey/versionId/evidenceSetVersion/canonicalRequests/intentKeys (grep exit 1 = no hits); awk-region grep `unrecognized\|askEnumer` over requestKey/versionId/evidenceSetVersionWithMapping bodies = 0 hits; `matchIntents` is a thin wrapper (AiReplyIntentCatalog.kt:422-425) keeping hash inputs byte-identical (N4/IP-1). I-3: QaFactSelectionService diff has zero +/- lines containing RequestGroundingStatus (status `when` block at :449-457 character-identical) and zero +/- lines for groundedRequestCount/unsupportedRequests/requestCount expressions; shadow fields are defaulted-only slots; shadow-equivalence test (3 vs 0 unrecognized → identical status/counts/factRuleIds/sendQaRuleIds) green. I-4: `enumerate()` (InboundAskEnumerator.kt:57-93) — llm disabled / provider null / chat exception / non-JSON / empty array all return `AskEnumeration(false, emptyList())`, never throws; five fail-open tests green. I-5: `grep -rn "unrecognizedAsks\|EnumeratedAsk" src/main/kotlin` → only AiReplyDraftService.kt / InboundAskEnumerator.kt / QaFactSelectionService.kt / TrustReplyWorkbenchService.kt; zero hits in AiReplyPointByPointComposer / AiReplyGroundedContentPlanner / AiReplyGroundedDraftMaterializer. I-6: `AskEnumeratorProperties.enabledForAutoReply = false` default (AskEnumeratorProperties.kt:23); `select()` gates enumeration on the flag (QaFactSelectionService.kt:39-49; off → no `enumerate()` call — Mockito `never()` test green); workbench path always enumerates (test green). I-7: `claimed()` (InboundAskEnumerator.kt:41-46) implements the plan's overlap formula verbatim; `buildRequestFact` (:407-413) uses it with per-request region attribution; double-intent overlap test (`remuneration and intellectual property` overlapping finance+ip spans) claimed once, no negatives. N1-N6: covered above; `allowedHandlings`/`recommendedHandling` bodies untouched (diff has no such lines; region grep 0 hits); full suite 0 failures. Prompt: `ASK_ENUMERATION_SYSTEM_PROMPT` programmatically compared — identical to plan B-1 block, 8/8 lines. [ASK_ENUM] format: `buildAskEnumLogLine` (InboundAskEnumerator.kt:224-231) fixed field names/order; exact-string assertions green (D-4). |
| Required commands | PASS | FRESH run (JDK11 zulu-11, 2026-08-19 16:39): `mvn test -Dtest='InboundAskEnumeratorTest,QaFactSelectionServiceTest,AiReplyIntentCatalogTest' -o` → exit 0, BUILD SUCCESS; surefire XML 13/43/35 = 91 tests, 0 failures, 0 errors, 0 skipped. `git diff --check` → exit 0, no output. Full suite not re-run (optional per dispatch; verified via surefire XML aggregate): 199 test classes, aggregate 2589 tests / 0 errors / 0 failures / 4 skipped — matches implementer's clean-state claim (incremental run's 2591 explained by an orphaned ScratchSpanDebugTest.class; no Scratch class present in target/test-classes now). |
| Downstream interfaces | PASS | P3 (03-fact-order-drag, frontend-only) consumes trust-reply-workbench.js state; the diff contains no .js/.html/.css changes; `TrustReplyRequestCoverage.unrecognizedAsks` added with default empty (TrustReplyWorkbenchService.kt:132-143) so frontend is unaffected (O2). `RequestFactItem.unrecognizedAsks` / `ResolvedQaRules.unrecognizedAskCount` (+ enumerator shadow fields) all defaulted → existing constructors compile unchanged (full suite compiles; brief's Downstream contract satisfied). |

### AUTO_FIX
- N/A — the only gate violation (authorized scope) cannot be auto-fixed: the correction is not a code change but a plan amendment widening the authorized file list to include `AiReplyDraftService.kt` (and ratifying the two extra shadow fields / select()-internal wiring / AUTO-path log emission). Per the light-verifier rubric, AUTO_FIX requires every changed file to be authorized; an unauthorized file cannot be auto-fixed into authorization. This is missing authority / scope expansion → PAUSE.

### RECORD_ONLY
- O-1: `canonicalizeWithMap` (AiReplyIntentCatalog.kt) lowercases per char (`ch.toString().lowercase()`) while the preserved `canonicalize` lowercases the whole string — theoretically divergent for context-sensitive Unicode lowercasing (Greek sigma final form, non-BMP chars). No impact on any fixture or realistic ASCII mail; N4/IP-1 wrapper equivalence verified on all P1 fixtures (AiReplyIntentCatalogTest `matchIntents stays a thin wrapper…`). Speculative edge, outside the light gate.
- O-2: `select()` emits an `[ASK_ENUM]` line for every caller, not only the auto-reply decision points — PendingMailOperationService suggest/preflight and training/unmatched generate also log (deviation 4 consequence). Fixed format preserved, behavior-neutral log noise; each auto-reply decision (decide() → generate() → resolveQaRules → select(), verified single call) still yields exactly one line.
- O-3: an ask whose `originalRange` starts outside every request region is excluded from the unrecognized counts (region attribution inside `buildRequestFact`), whereas the plan's B-2 wording is a flat `asks.filterNot { claimed }`. Behavior-neutral for the A-1/A-2 acceptance letters (all their asks fall inside request regions); an unclaimed ask between request regions is silently uncounted.

### Required Action
- PAUSE

Verdict rationale: Gates 2/3/4 PASS with direct evidence (all seven invariants, six must-not-change constraints, verbatim prompt, fixed log format, focused suite 91/0/0, diff-check clean, full-suite aggregate 2589/0/0/4). Gate 1 PAUSE: a product file outside the authorized list was changed without an approved amendment, and the implementation adds API surface beyond the plan's stated field set (enumeratorEnumerated/enumeratorClaimed), replaces the plan's C-2 wiring, and extends log emission beyond the plan's stated decision points. The content of the AiReplyDraftService edit is forced by the plan's C-1 mandates (the data classes physically live there — the plan's file attribution is stale), but the deviations are plan-internal-inconsistency resolutions that only a human can ratify as an amendment. Per the rubric, any PAUSE gate → PAUSE.

---

## Light Verification: LIGHT_PASS_WITH_NOTES
Child: 02-unrecognized-request-detection — docs/plans/2026-08-19/02-unrecognized-request-detection.md (plan identity commit:ec68a4d2f601307b6806fa815f238d7b76ca6e1b; 修正 A1 appended; epoch-1 plan identity af1723f superseded by A1)
Boundary: f5c09382744c0da8a537610af6145974ee1fcaf4..1df49e8e37f24d4040e27e1d78052a8645253e2b
Verifier: ReVerify02A1 (fresh agent, no writer conversation; read-only except this report)

Epoch context: epoch-2 re-verification of the SAME implementation commit under Amendment A1 authority (ledger.md `## Amendments` row A1; Before commit:af1723f → After commit:ec68a4d; Approval: HUMAN:user approved A1 via ask 2026-08-19; header `Amendments: A1`). Epoch-1 verdict PAUSE; its single gate-1 PAUSE reason (unauthorized AiReplyDraftService.kt + extra shadow fields + C-2 wiring replacement + select()-based log emission) is re-adjudicated below under A1 items 1-4. Implementation commit 1df49e8; prior 841c633/481db67/7955162/ec68a4d are controller/evidence/amendment commits, not product. Boundary unchanged from epoch 1 (11 files: 9 product/test + 2 docs).

### Four Gates
| Gate | Result | Evidence |
|---|---|---|
| Authorized scope | PASS | `git diff --name-status f5c0938..1df49e8` = 9 product/test files (8 plan 变更文件清单 + AiReplyDraftService.kt, now A1-authorized) + 2 docs (child-01 evidence @481db67). A1 item 1 ratified the file; `git diff -U0 f5c0938..1df49e8 -- AiReplyDraftService.kt` = 12 insertions / 2 deletions, exactly two hunks confined to the two data-class definitions: `RequestFactItem` (class at :349) adds only `unrecognizedAsks: List<EnumeratedAsk> = emptyList()`; `ResolvedQaRules` (class at :361) adds only `unrecognizedAskCount: Int = 0` / `enumeratorAvailable: Boolean = false` / `enumeratorEnumerated: Int = 0` / `enumeratorClaimed: Int = 0` (all defaulted). No other lines in the file; no other unauthorized file in the boundary. All four epoch-1 gate-1 deviations now explicitly ratified (A1 items 1-4). |
| Plan and invariants | PASS | Spot-checked against epoch-1 evidence; same commit, nothing changed. I-1 `parse()` (InboundAskEnumerator.kt:96-128): whitespace-folded `contains` (no lowercase), folded length >= 8, dedupe, 12-item cap with explicit truncation log. I-2: `git diff -U0` TrustReplyWorkbenchService.kt → 0 +/- lines with requestKey/versionId/evidenceSetVersion/canonicalRequests/intentKeys; awk-region grep `unrecognized\|askEnumer` over evidenceSetVersionWithMapping (:1576-1584), requestKey (:1658, :1875), versionId (:1890) bodies = 0 hits; `matchIntents` thin wrapper (AiReplyIntentCatalog.kt:436-437) keeps hash inputs byte-identical (N4/IP-1). I-3: QaFactSelectionService diff → 0 +/- lines with RequestGroundingStatus/groundedRequestCount/unsupportedRequests/requestCount; status `when` (:426-433) char-identical (N1); N2/N3 bodies untouched. I-4: `enumerate()` (InboundAskEnumerator.kt:57-93) fail-open on llm disabled / provider null / chat exception / non-JSON / empty — returns `AskEnumeration(false, emptyList())`, never throws (N6). I-5: `grep -rln "unrecognizedAsks\|EnumeratedAsk" src/main/kotlin` → AiReplyDraftService/TrustReplyWorkbenchService/QaFactSelectionService/InboundAskEnumerator only; zero hits in AiReplyPointByPointComposer/AiReplyGroundedContentPlanner/AiReplyGroundedDraftMaterializer (N5). I-6: `AskEnumeratorProperties.enabledForAutoReply = false` default (AskEnumeratorProperties.kt:23); `select()` gates enumeration on flag (:42-46), workbench path always enumerates (:134-135). I-7: `claimed()` (:41-46) implements the plan overlap formula verbatim (positional, never count subtraction); per-request attribution in `buildRequestFact` (:414-417). Prompt: `ASK_ENUMERATION_SYSTEM_PROMPT` compared line-by-line to plan B-1 block — 8/8 lines verbatim. D-4: `buildAskEnumLogLine` (:224-231) fixed field names/order `[ASK_ENUM] source={} contactId={} available={} enumerated={} claimed={} unrecognized={} kind={}`. |
| Required commands | PASS | FRESH run (JDK11 zulu-11, 2026-08-19 16:50 +08): `mvn test -Dtest='InboundAskEnumeratorTest,QaFactSelectionServiceTest,AiReplyIntentCatalogTest'` → exit 0, BUILD SUCCESS; surefire XML InboundAskEnumeratorTest 13 / QaFactSelectionServiceTest 43 / AiReplyIntentCatalogTest 35 = 91 tests, 0 failures, 0 errors, 0 skipped. `git diff --check` → exit 0, no output. Full suite via surefire XML aggregate (199 test classes; 3 refreshed by the focused run): 2589 tests / 0 errors / 0 failures / 4 skipped — matches implementer claim and epoch-1 aggregate. |
| Downstream interfaces | PASS | P3 (03-fact-order-drag) frontend-only consumes trust-reply-workbench.js state; boundary contains no .js/.html/.css. `TrustReplyRequestCoverage.unrecognizedAsks` default empty (TrustReplyWorkbenchService.kt:126-139) → frontend unaffected (O2). `RequestFactItem.unrecognizedAsks` / `ResolvedQaRules.unrecognizedAskCount` (+ enumerator shadow fields) all defaulted → existing constructors compile unchanged (full suite compiles). A1 item 3: in-service enumeration wiring equivalent via ResolvedQaRules shadow-field pass-through; brief's Downstream contract satisfied. |

### AUTO_FIX
- N/A — no four-gate violation remains under A1.

### RECORD_ONLY
- O-1 (carried, not ratified by A1): `canonicalizeWithMap`/`lowercaseWithMap` (AiReplyIntentCatalog.kt:477-527) lowercases per char (`ch.toString().lowercase()`, length-expansion mapped) while preserved `canonicalize` (:357-360) lowercases the whole string — theoretically divergent only for context-sensitive Unicode lowercasing (Greek sigma final form, non-BMP); no fixture or realistic ASCII-mail impact; N4/IP-1 wrapper equivalence verified on all P1 fixtures. Speculative edge, outside the light gate.
- O-2 (re-adjudicated under A1 — RATIFIED, resolved): `select()` emits an `[ASK_ENUM]` line for every caller (PendingMailOperationService suggest/preflight, training/unmatched) beyond the plan's two stated decision points. A1 item 4 explicitly authorizes auto-path emission from `select()` (source=AUTO, contactId=0) and classifies the additional caller lines as 行为中性的日志行 RECORD_ONLY 不算缺陷. No action.
- O-3 (carried, plan-wording tension): an ask whose `originalRange` starts outside every request region is excluded from unrecognized counts (region attribution in `buildRequestFact`, QaFactSelectionService.kt:414-417) whereas plan B-2 words a flat `asks.filterNot { claimed }`. Structurally forced by C-1's per-request `RequestFactItem.unrecognizedAsks` placement; behavior-neutral for the A-1/A-2 acceptance letters (their asks fall inside request regions). Outside the light gate.

### Required Action
- COMPLETE_CHILD

Verdict rationale: Gate 1 converts PAUSE → PASS under A1: the human-approved amendment (ledger row A1, plan Before af1723f → After ec68a4d) ratifies all four epoch-1 deviations, and the fresh `-U0` diff proves the AiReplyDraftService.kt change touches ONLY the two data-class definitions at :349/:361 with no other unauthorized file in the boundary. Gates 2/3/4 re-confirmed with fresh evidence: focused suite 91/0/0/0 exit 0, `git diff --check` exit 0, full-suite surefire XML aggregate 2589/0/0/4, invariants I-1..I-7/N1..N6 spot-checked unchanged from epoch 1. No AUTO_FIX; O-1 and O-3 remain RECORD_ONLY, O-2 ratified under A1 → LIGHT_PASS_WITH_NOTES.

### Finalization note (2026-08-19, controller)

Evidence re-recorded in the terminal evidence commit; verifier report content unchanged since signature.
