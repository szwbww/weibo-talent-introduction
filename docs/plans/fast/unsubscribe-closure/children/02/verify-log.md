# Child 02 Verification Log — unsubscribe-closure fast-p run

## Light Verification: LIGHT_PASS_WITH_NOTES
Child: 02 and plan path docs/plans/2026-08-11/unsubscribe-02-suppression-gate.md
Boundary: 6a822c6a6ee0f3a94dd31c2660cfac922333e535..f09f8c314951279aaabd025d31d4e045d2928aa6
Verifier: Verify02

### Four Gates

|Gate|Result|Evidence|
|---|---|---|
|Authorized scope|PASS|`git diff --name-only 6a822c6..f09f8c3` lists 13 paths: 4 fast-p evidence docs under `docs/plans/fast/unsubscribe-closure/children/01/` (controller evidence commit `bf0cb02 docs(fast-p): record 01 light verification`, pre-execution HEAD per execution.md — fast-p infrastructure, not implementer scope) plus 9 product files. The implementation commit `f09f8c3 feat(fast-p): implement 02` itself (9 files, +189 −22) touches exactly the plan's 变更文件清单 9 files: `IntroductionMailComposer.kt`, `EmailSuppressionService.kt`, `SmtpMailDeliveryService.kt`, `PendingMailOperationService.kt`, `ManualExpertMailService.kt`, `ExpertContactManagementController.kt`, `SmtpMailDeliveryServiceTest.kt`, `PendingMailOperationServiceTrustWorkbenchTest.kt`, `InitialOutreachServiceTest.kt`. No file outside the authorized list.|
|Plan and invariants|PASS|I-1: `SmtpMailDeliveryService.kt:17-20` — suppression check is the first statement of `send()`, before `smtpSenderFactory.getSender(account)` (`:22`); throws `RecipientSuppressedException`, no `DeliveredMail` return. New test `send throws RecipientSuppressedException before touching smtp when recipient suppressed` asserts exception + `Mockito.verify(factory, never()).getSender(anyValue(testAccount()))`. I-2: `grep -n "SUPPRESSED" SmtpMailDeliveryService.kt` → zero hits; `git diff 6a822c6..f09f8c3 -- SmtpErrorCategory.kt MailDeliveryService.kt` → empty. I-3: `EmailSuppressionService.kt:105-106` `class RecipientSuppressedException(email: String) : IllegalStateException("收件人已退订，禁止外发：$email")`; test `RecipientSuppressedException is an IllegalStateException` passes. I-4: `allowSuppressedRecipient` default `false` (`IntroductionMailComposer.kt:79`); only `ManualExpertMailService.composeComposeTemplate()` sets it (`ManualExpertMailService.kt:255`); `BatchMailSendRequest.toCommand()` (`ExpertContactManagementController.kt:332-338`) has no field; `ManualMailSendCommand.allowSuppressed` default `false` (`:320`). I-5/IP-3: `PendingMailOperationService.kt:255-260` check throws `ResponseStatusException(BAD_REQUEST, "收件人已退订，禁止外发：…")` at `:255`, `prepareAndClaim` at `:262` — check precedes claim; both new workbench tests assert 400 + `prepareAndClaim` never + `send` never + `finalizeFailure` never. IP-2: new `InitialOutreachServiceTest` case `sendInitialBatch skips only suppressed emails with zero sends and no FAILED records` asserts skipped=2, sent=0, failed=0, no `status == "FAILED"`, `mailDeliveryService.send` never. IP-4: `ManualInitialOutreachService.kt` zero diff; `ManualMailSendCommand(` construction at `:317` inherits default false. Test adaptations: `ex.status`/`ex.reason` on `ResponseStatusException` is the Spring 5 API (`getStatus()`), correct for this JDK 11/Spring 5 project (execution.md deviation 1); `anyValue()` is the file-local helper already established in these test files (`SmtpMailDeliveryServiceTest.kt:486`, `PendingMailOperationServiceTrustWorkbenchTest.kt:1175`, `InitialOutreachServiceTest.kt:406`) replacing `Mockito.any()` for non-null Kotlin params (deviation 2). Both stay inside the 9 authorized files and preserve every plan T-6 behavior. One RECORD_ONLY item (O-1, I-4 grep-count tension) — see below.|
|Required commands|PASS|All run fresh with `JAVA_HOME=/Library/Java/JavaVirtualMachines/zulu-11.jdk/Contents/Home` in this verification: `mvn test -Dtest=SmtpMailDeliveryServiceTest` → exit 0, surefire Tests run: 24, Failures: 0, Errors: 0, Skipped: 0; `mvn test -Dtest=PendingMailOperationServiceTrustWorkbenchTest` → exit 0, Tests run: 28, Failures: 0, Errors: 0, Skipped: 0; `mvn test -Dtest=InitialOutreachServiceTest` → exit 0, Tests run: 10, Failures: 0, Errors: 0, Skipped: 0; `mvn test -Dtest='ManualExpertMailService*Test,ManualInitialOutreachServiceTest,MeetingScheduleServiceTest,AutoMailReplyServiceTest,BatchSendTaskRuntimeIntegrationTest'` → exit 0, Tests run: 140 (Gate 5 + AutoMailReply 40 + ManualExpert 24 + ManualInitialOutreach 43 + MeetingSchedule 7 + BatchSendTask 21), Failures: 0, Errors: 0, Skipped: 0; `mvn test` (full) → exit 0, BUILD SUCCESS, Tests run: 2290, Failures: 0, Errors: 0, Skipped: 4; `mvn clean package` → exit 0, BUILD SUCCESS, Tests run: 2290, Failures: 0, Errors: 0, Skipped: 4; `git diff --check` → exit 0, clean. Baseline (master 8e8ddfc, 2284 tests) had zero failures; 2290 = 2284 + 6 new tests, no baseline failures in any run. All counts match execution.md exactly.|
|Downstream interfaces|N/A|Child 02b declares no dependency on 02 (controller brief for this verification; `docs/plans/fast/unsubscribe-closure/children/` contains only 01 and 02 — 02b not yet materialized). No downstream interface to verify.|

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1 (plan-internal tension, not an implementation defect): Plan I-4 acceptance says `grep allowSuppressedRecipient src/main/kotlin` must hit 恰好 2 处 (definition + `ManualExpertMailService.kt:244` explicit set), but plan T-3 itself requires reading the field in `SmtpMailDeliveryService.send()` — so the actual count is 3: definition `IntroductionMailComposer.kt:79`, explicit set `ManualExpertMailService.kt:255` (same code point as plan's `:244`, shifted by T-5 edits), read `SmtpMailDeliveryService.kt:20`. The plan does not uniquely determine a correction (removing the read breaks T-3), so per classification rules this is RECORD_ONLY, not AUTO_FIX. The behavioral contract of I-4 is fully met: field defined once, default `false`, only the manual single-send path ever sets it, batch paths fixed false. Implementation matches the plan's operative rule, not its acceptance-count text.

### Required Action
- COMPLETE_CHILD
