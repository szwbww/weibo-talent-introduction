# Verification Log — p1-snippet-name

- Epoch: 1
- Attempt: 1
- Timestamp: 2026-08-14 (local)
- Reviewed SHA range: `7a5dbdb..15633f7a` (implementation commit `15633f7a` — `feat(fast-p): implement p1-snippet-name`)
- Verifier: P1Verifier (independent; no writer conversation)
- Worktree: `/Users/lukai/IdeaProjects/weibo-talent-introduction/.worktrees/fast/expert-mail-preview` (branch `fast/expert-mail-preview`, HEAD = `15633f7a`)

## Light Verification: LIGHT_PASS_WITH_NOTES
Child: p1-snippet-name (docs/plans/2026-08-14/expert-mail-preview-p1-snippet-name.md)
Boundary: 7a5dbdb..15633f7a
Verifier: P1Verifier

### Four Gates
| Gate | Result | Evidence |
|---|---|---|
| Authorized scope | PASS | `git diff --name-only 7a5dbdb..15633f7a` = 11 files, all within the 10 authorized files ∪ {`docs/knowledge/mail/K-spring-data-jdbc-null-default.md`} (Phase 6 writeback): V96 migration, ReplySnippet.kt, ReplySnippetController.kt, ReplySnippetService.kt, MailComposeTemplateService.kt, index.html, app.js, ReplySnippetServiceTest.kt, replySnippetLabel.test.js, batchSendTaskConsoleVisualFix.test.js, K-spring-data-jdbc-null-default.md. Forbidden files absent from diff: styles.css, trust-reply-workbench.js, trustReplyWorkbenchSharedMount.test.js, MailComposeTemplateController.kt. Working tree clean except untracked `docs/plans/fast/` (fast-p artifacts, expected untracked). |
| Plan and invariants | PASS | I-1: `replySnippetDisplayLabel` (app.js:2903) and Kotlin 3-tier fallback (MailComposeTemplateService.kt:383-386 resolveRefDisplayName, :504-507 resolveBlocks) are verbatim-equivalent (name→first non-empty trimmed line→40 chars+`…` (U+2026)→`<snippetType> #<id>`); `EXCERPT_MAX_CHARS = 40` in companion object (:616); remaining `snippetType} #` only at the 2 tier-3 defensive branches. I-2: `frameSlotIdentity` (:166-173) reads only slot.name (enum)/id/snippetType/enabled/updatedAt/sha256(content) — no snippet `name`; frame functions untouched in diff; test `name does not affect frame version` present (ReplySnippetServiceTest.kt) and passing. I-3: 7 layers — controller 6 `name` hits (Request×2 :65/:87, toCommand×2 :76/:97, Response :110, toResponse :126), service 4 hits (CreateCommand :388, UpdateCommand :398, create constructor :201, update copy :238 — explicit normalized). I-4: `command.name?.trim()?.takeIf { it.isNotBlank() }` at create :201 and update :238 (identical to variantGroup pattern). I-5: `\${` count in V96 = 0; application.yml untouched, `placeholder-replacement: false` at :13. S-1: index.html:1741 `<label>片段名称<input name="name" maxlength="120" placeholder="留空则显示内容摘要"></label>` verbatim, placed after type select / before 片段 ID, no `style=`/new class. S-2: app.js:3573 `<th>名称</th>`, :3541 `<td>${snippet.name ? escapeHtml(snippet.name) : '<span class="muted">未命名</span>'}</td>` verbatim, :3563 colspan `showDefault ? 7 : 6`. S-3: app.js:8072 — only `const label = replySnippetDisplayLabel(snippet);` changed; option structure/selected logic untouched. M-1: `grep -o 'v=[^"]*' index.html | sort -u` = single value `v=20260814-v9-snippet-name-01`; batchSendTaskConsoleVisualFix.test.js:36-39 asserts the same 3 keys. M-3: `name="name"` count in index.html = 1. M-4: grep receipts for I-1/I-2/I-3 captured (see above and verify command log). N-1..N-6: frame functions, workbench JS, preview endpoint, content-variant functions, `requireValidPlaceholders` (:187/:225) all unchanged in diff. Phase 6: K-spring-data-jdbc-null-default.md appended (`updated_at` explicit write-back implication); K-variant-pool-dto-chain conditional not triggered (7-layer checklist complete). |
| Required commands | PASS | All run freshly with `JAVA_HOME=...zulu-11...` / node v25.7.0: (1) `mvn test -Dtest=ReplySnippetServiceTest` — Kotlin `Tests run: 24, Failures: 0, Errors: 0, Skipped: 0`; mvn exit 1 ONLY on exec node-test step (2 pre-existing JS failures, see baseline note). (2) `mvn test -Dtest=MailComposeTemplateServiceTest` — `Tests run: 37, Failures: 0, Errors: 0, Skipped: 0`; same exit-1 cause. (3) `node --test src/test/js/replySnippetLabel.test.js` — exit 0, tests 6, pass 6, fail 0. (4) `node --test src/test/js/batchSendTaskConsoleVisualFix.test.js src/test/js/trustReplyWorkbenchSharedMount.test.js` — exit 0, tests 63, pass 63, fail 0. (5) `mvn test` (full) — Kotlin aggregate `Tests run: 2418, Failures: 0, Errors: 0, Skipped: 4` (187 classes; FlywayMigrationIntegrationTest `Skipped: 1` → 未执行（无 Docker）, confirmed in surefire report); mvn exit 1 solely on the SAME 2 baseline failures (`ReferenceError: buildManualExecutionSnapshot is not defined` at batchManualExecutionLog.test.js:83 and :117; JS suite 508 pass / 2 fail). (6) `mvn clean package` — exit 1 at same exec node-test step, no WAR (matches recorded baseline; do NOT claim exit 0). (7) `git diff --check` on boundary and working tree — exit 0, no output. Supplementary (not required): `mvn clean package -DskipNodeTests=true` — exit 0, BUILD SUCCESS, WAR produced `target/weibo-talent-introduction-1.0.0-SNAPSHOT.war` (46,123,011 bytes). |
| Downstream interfaces | PASS | `MailComposeTemplateService.resolveRefDisplayName` (:370) now yields meaningful snippet labels → `MailComposeTemplateBlockDetail.refDisplayName` (:667) and `ComposeTemplatePreviewBlock.refDisplayName` (:690, default arg :581); resolveBlocks preview path sets `refDisplayName = displayName` with the identical 3-tier algorithm (:488, :536). Frontend consumption sites (template-list pill app.js:8017, preview block description :8261; plan line refs 8004-8006/8248 shifted +13 by the 9-line function insertion at :2900) are unchanged — 0 hits for `refDisplayName` in the app.js diff. No `snippetType} #` remains except the 2 tier-3 defensive branches. P2 can consume without rework. |

### AUTO_FIX
- N/A — no four-gate violation proven; no changed file outside the authorized set; the 2 baseline JS failures are NOT AUTO_FIX candidates (file `batchManualExecutionLog.test.js` not authorized; plan does not uniquely determine a repair).

### RECORD_ONLY
- R-1 (plan-audit gap completed by implementer, D-1): the plan's I-1 audit claimed "共 2 处实现" of the display-label algorithm but the M-4 grep found a third site the plan missed — `resolveBlocks()` built `ComposeTemplatePreviewBlock.refDisplayName` inline as `"${snippet.snippetType} #${snippet.id}"` (former :491, now :507). Implementer fixed it with the identical 3-tier fallback (:504-507) inside the authorized file. This is REQUIRED by the master-plan cross-child check (preview panel block description app.js:8248 must show the same name) and the brief's downstream interface ("no rework allowed" for P2), so it is a correct completion, not a violation. The plan's acceptance grep ("除第 3 级防御分支外无其他命中") remains satisfied — both remaining sites are tier-3 defensive branches. Recorded because the binding plan text did not enumerate this third site.
- R-2 (tier-3 defensive divergence, pre-existing JS behavior preserved): JS `replySnippetDisplayLabel` tier-3 maps `snippetType` through `replySnippetTypeLabels` (e.g. `尊语 #10`), while Kotlin tier-3 uses raw `snippetType` (`SALUTATION #10`). Tier-3 is unreachable in product semantics (content is TEXT NOT NULL; create/update enforce `isNotBlank`), the plan declares it defensive-only ("不参与产品语义"), and I-1 spec's tier-3 wording ("snippetType 原样") supports the Kotlin raw form. Not a gate violation.
- R-3 (baseline): `src/test/js/batchManualExecutionLog.test.js` 2 failures (`ReferenceError: buildManualExecutionSnapshot is not defined`) reproduce at base 7a5dbdb (confirmed identical failure identity at both the boundary base and HEAD runs); the file is not authorized and the plan does not uniquely determine a repair → classified as recorded baseline, consistent with controller-verified baseline.

### Required Action
- COMPLETE_CHILD
