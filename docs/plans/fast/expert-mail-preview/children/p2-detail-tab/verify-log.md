## Light Verification: LIGHT_PASS_WITH_NOTES
- Epoch: 1
- Attempt: 1
- Timestamp: 2026-08-14T14:05:00+08:00
- Reviewed SHA range: 6ba9a66..c2acd4f (implementation commit c2acd4f `feat(fast-p): implement p2-detail-tab`, single parent 6ba9a66; c2acd4f descends from ledger product base 15633f7a — `git merge-base --is-ancestor 15633f7a c2acd4f` OK)
- Child: p2-detail-tab — docs/plans/2026-08-14/expert-mail-preview-p2-detail-tab.md (plan SHA-256 `1e6257ca205734d7dc5f0329427b6712c56c4f6088ea98a17351a8035fdb2089` verified in worktree)
- Verifier: P2Verifier
- Worktree: /Users/lukai/IdeaProjects/weibo-talent-introduction/.worktrees/fast/expert-mail-preview (branch fast/expert-mail-preview, HEAD c2acd4f)

### Four Gates
| Gate | Result | Evidence |
|---|---|---|
| Authorized scope | PASS | `git diff --name-status 6ba9a66..c2acd4f` = exactly {CLAUDE.md(M), docs/knowledge/mail/K-mail-body-display-sites.md(M), src/main/resources/static/app.js(M), index.html(M), styles.css(M), src/test/js/batchSendTaskConsoleVisualFix.test.js(M), src/test/js/expertMailPreviewTab.test.js(A)} — 5 authorized files + 2 Phase-6 writeback; NO docs/plans/fast/**; `git diff --stat 6ba9a66..c2acd4f -- src/main/kotlin` = empty (0 lines); `git diff --check 6ba9a66..c2acd4f` = no output |
| Plan and invariants | PASS | I-1: `preview-draft` POST at app.js:8336 (new renderExpertMailPreview; :8088 = existing editor fn untouched); no `compose-templates/${…}/preview` / `/preview"` GET in app.js (grep 0 hits); body via textContent, no client-side `${}` replacement. I-2: ensureComposeTemplatesLoaded def :8025, called exactly once each from loadExpertMailPreview (:8040) and openTemplateEditorForExpert (:8116), idempotent early-return (grep receipt). I-3: strict order ensure→find→setView→switchMailTemplatesSubTab→openComposeTemplateEditor→await loadComposeTemplatePreviewOptions→openComposeTemplatePreview; missing template → showStatus+return, NO setView (test group 5). I-4: `$("#previewComposeExpertInput").value` = composeTemplatePreviewExpertLabel({displayName,expertEmail}) (`姓名 <邮箱>` :7708-7713) AND state.previewDrawer.orcidId/contactId/expertEmail; mirrors randomComposeTemplatePreviewExpert (test group 6 asserts "Ada Smith <ada@mit.edu>" + 3 state fields). I-5: `.detail-tab-panel[data-panel="mail-preview"]` divs in showExpertDetail (:6734) AND loadContactDetail (:7194); `grep -c` mail-preview=3 == template=3 (equality holds; D1 literal, RECORD_ONLY O-1); test group 7 asserts panelDivs==2 && totals equal. I-6: `strictPlaceholders: false` literal :8079 in new fn (no checkbox read; :8324/:8328 = existing editor fn untouched); fallbackKeys → `<span class="badge warn">兜底: KEY</span>` textContent (test group 2). I-7: new-fn body global access = only `$("#previewComposeExpertInput")` (allowed) + `#contactDetail` delegation; all panel elements via panel.querySelector('[data-role=…]'); no getElementById. S-1: tabs array first 3 byte-identical to base + `{ key: "mail-preview", label: "邮件预览" }`, default activeTab "academic" unchanged. S-2: both panel blocks verbatim incl. indentation; loading `加载邮件预览中...` / error `加载失败: ${escapeHtml(e.message)}` / no-ORCID `无 ORCID，无法预览邮件。` verbatim. S-3/S-4: styles.css blocks verbatim vs plan (property count/order/values), placed between `.tpl-var-empty,.tpl-var-loading` and `.preview-drawer-shell` (contiguous in diff); DOM verbatim; no inline `style="`, no `.toolbar`, no `.field-label`, no undeclared classes. N-4: base-vs-head diff by symbol — first 3 tab entries, default activeTab, and activateDetailSubTab template lazy-load branch byte-identical (only additions; 146 insertions / 1 deletion). M-1: `grep -o 'v=[^"]*' index.html | sort -u` = single `20260814-v10-expert-mail-preview-01`; batchSendTaskConsoleVisualFix.test.js has 3 assertions on it; differs from P1 v9. M-3: panel host = app.js template strings (:6734/:7194 in the two render functions); no new index.html selector. M-4: all counts above carry grep receipts |
| Required commands | PASS | All freshly run in worktree (pwd-asserted). `node --test src/test/js/expertMailPreviewTab.test.js` → exit 0, tests 8 / pass 8 / fail 0. `node --test batchSendTaskConsoleVisualFix.test.js trustReplyWorkbenchSharedMount.test.js` → exit 0, 63/63. `node --test composeTemplatePreview.test.js contactsLayoutDefault.test.js expertProfileAbsence.test.js expertTagBatchFix.test.js` → exit 0, 53/53. `node --check src/main/resources/static/app.js` → exit 0, no output. `node --test src/test/js/batchManualExecutionLog.test.js` (baseline repro at head) → exit 1, fail 2, both `ReferenceError: buildManualExecutionSnapshot is not defined` — identical to recorded baseline. `mvn test` (full) → exit 1; Kotlin aggregate `Tests run: 2418, Failures: 0, Errors: 0, Skipped: 4` (FlywayMigrationIntegrationTest Skipped: 1 未执行（无 Docker）); sole failure cause = exec-maven-plugin node-test goal (node suite 91 suites / 516 pass / 2 fail, both the baseline ReferenceError) — matches recorded baseline. `mvn clean package` (full) → exit 1, same sole cause; no WAR (fails at node-test before package). `mvn clean package -DskipNodeTests=true` → exit 0, BUILD SUCCESS, WAR 46,124,369 bytes `target/weibo-talent-introduction-1.0.0-SNAPSHOT.war`; WAR contents verified: index.html v10 key ×3, app.js `data-panel="mail-preview"` ×3, styles.css `expert-mail-preview-toolbar` ×2. Recorded honestly: full mvn runs exit 1 ONLY on the 2 baseline JS failures; WAR builds only with -DskipNodeTests=true. `git diff --check` → clean |
| Downstream interfaces | PASS | No later children (p2 is last). Cross-child final checks: block dropdown uses replySnippetDisplayLabel (app.js:8199, P1 name-first label), template-list pill consumes block.refDisplayName (:8144), preview drawer blockNotes consume refDisplayName (:8270 renderComposeTemplatePreviewInDrawer / :8388 renderComposeTemplatePreviewHtml — the preview the P2 jump flow surfaces) — all show the same snippet name for named snippets. index.html triad single value consistent with the 3 test assertions; trustReplyWorkbenchSharedMount 3-key equality test passes (in the 63-test run). See O-3 for the plan-internal wording note on "块说明" in the new panel |

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1 (implementer D1): I-5 acceptance literal `grep -c 'data-panel="mail-preview"'` = 2 vs actual 3 (activateDetailSubTab lazy-load querySelector :6578 + 2 panel divs :6734/:7194); plan equality invariant HOLDS (3 == 3 with `data-panel="template"`); test group 7 asserts panelDivs==2 && totals equal. No plan-unique repair: removing the selector would break the mandatory A-4 lazy-load branch; rewording the acceptance is a plan-text change → not AUTO_FIX.
- O-2 (implementer D2): test group 1 counts `data-sub-tab="` (=4) instead of `class="detail-sub-tab` (which would also match the `.detail-sub-tabs` container, =5); N-4 regression intent (4 buttons, first 3 key/label unchanged) satisfied.
- O-3: master-plan cross-child final check item 1 ("P2 邮件预览面板的块说明（app.js:8248 消费 refDisplayName）") and child A-11 ("块说明区域") reference a block-description area in the new mail-preview panel, but the binding child plan's own implementation spec (B-3 render list, S-4 DOM, D-3 test groups) never defines one — the panel renders subject/body/`收件人:`/fallback badges exactly as specified. The three snippet-name surfaces that exist (block dropdown :8199, template-list pill :8144, drawer blockNotes :8270/:8388) are mutually consistent. Plan-internal wording inconsistency (master vs child), no gate violation, no authorized correction path → RECORD_ONLY; controller to note at joint close-out.
- O-4: duplicate `function scrollBackToContactsList` in app.js (2 definitions) — pre-existing at base 6ba9a66 (verified via `git show 6ba9a66:... | grep -c` = 2), not introduced by this commit; incidentally visible, outside light-gate scope.

### Required Action
- COMPLETE_CHILD
