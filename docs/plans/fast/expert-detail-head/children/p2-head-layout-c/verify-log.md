# P2 Verification Log — p2-head-layout-c

## Light Verification: LIGHT_PASS_WITH_NOTES
Child: p2-head-layout-c — `docs/plans/2026-08-14/expert-detail-head-p2-head-layout-c.md` (AMENDED A2; master `docs/plans/2026-08-14/expert-detail-head-main.md`)
Boundary: `111180741ec46bea796e81a60e513769d2de534c..708dd28bb394204605826c13be883c51465d27e5` (P2 implementation commit `708dd28`; docs-only commits `c51b2d6`/`9839ed0`/`209f184` between are plan/ledger records, not product code)
Verifier: P2Verifier

### Four Gates
| Gate | Result | Evidence |
|---|---|---|
| Authorized scope | PASS | `git diff --name-status 209f184..708dd28` = exactly 4 files: `M src/main/resources/static/app.js`, `M src/main/resources/static/styles.css`, `A src/test/js/contactHeadLayout.test.js`, `M src/test/js/expertProfileAbsence.test.js`. No other product/test file changed in the boundary; docs commits touch only `docs/plans/` (c51b2d6: 2 plan files; 9839ed0/209f184: ledger + P1 records). |
| Plan and invariants | PASS | I-1: `app.js:8604` `senderAccountCode: null` in `send-manual-mail` branch; zero diff touch on that line; contactHeadLayout test #4 asserts body null + branch source has no `senderBindingSelect`. I-2: `app.js:8843-8854` `updateSenderBindingDirtyState()` sets all three (note.hidden / sendBtn.disabled / pill.dataset.dirty) from `select.value !== (select.dataset.original \|\| "")`; init call `app.js:7062` after select fill; change-delegate branch added `app.js:11228-11230` (`senderBindingSelect` only; existing 3-id branch untouched); test #2 both states. I-3: `app.js:7017` `data-original="${contact.boundSenderAccountCode \|\| ""}"` sole source; `selectedIndex` 0 hits in whole file (base 0 too); test #3. I-4: `app.js:26` `contactHeadExpanded: false`; render reads `app.js:7030/7032`; toggle branch `app.js:8564-8571` flips state + syncs `#contactHeadMoreRow.hidden` + `aria-expanded`; 6 hits ≥ 3; test #5. I-5: `app.js:3965` 6th param `layout = "section"` (no `)` in default list); inline early-return `app.js:3966-3992`; existing body byte-untouched; expertProfileAbsence 15/15 incl. new test #4 (undefined/"section"/"whatever" → S2_EXPECTED strictEqual) and existing S1/S2 cases. I-6: inline roots `app.js:3967` / `app.js:3982` carry `class="expert-tag-editor is-inline"`, `id`, `data-orcid`, `data-level`, `data-layout="inline"` (+`data-profile-missing="true"` for nodoc); new test #3. I-7: `app.js:4103-4108` `updateExpertTagEditor` reads `editor.dataset.layout` and passes back (inline↔section fallback); test #6. I-8: `styles.css:9487-9488` `.expert-tag-editor.is-inline.tag-editor-loading { min-height: 0; }` exactly once (grep count 1; test #8). I-9: `app.js:7008-7009` gray dot `is-unbound` conditional + label `未绑定`; `styles.css:9397` `.sender-binding-dot.is-unbound`; test #8. I-10: `data-panel="mail-preview"`=3, `data-panel="template"`=3, `<div class="detail-tab-panel" data-panel="mail-preview"`=2 — identical to base `209f184` (3/3/2); `renderDetailSubTabs` zero modified lines; test #7. Style contract S-1..S-9: all new CSS appended at end under single `/* === 专家详情头部 C 布局 === */` marker (styles.css diff = 2 hunks: dead-rule deletion at old :1553-1567 + append at :9280+); S-1..S-6 rule blocks script-verified verbatim vs plan (6/6); S-4 conditional `.contact-head-actions .button[disabled] { opacity: 0.5; cursor: not-allowed; }` appended (styles.css:9406-9410) with note comment — correct, styles.css had no `.button[disabled]`; `.dropdown`/`.dropdown-menu`/`.expert-tag*`/`.inbound-tag-editor-chips`/`.mail-expert-overview .expert-tag-editor`/`.contact-head-status-row`/`.contact-head-mail-row` existing blocks zero-diff; `contact-head-mail-row` 0 / `sender-binding-editor` 0 in app.js and 0 in styles.css; `class="expert-profile-header"` = 2; app.js `style="` hit set identical to base (182 = 182, same content, line numbers only shifted). |
| Required commands | PASS | Fresh (node v25.7.0, worktree): `node --test src/test/js/contactHeadLayout.test.js` exit 0 — `tests 9 / pass 9 / fail 0`; `node --test src/test/js/expertProfileAbsence.test.js` exit 0 — `tests 15 / pass 15 / fail 0` (11 existing + 4 new); `node --check src/main/resources/static/app.js` exit 0, no output; 4-file gate `node --test contactHeadLayout expertProfileAbsence senderBindingDisplay expertMailPreviewTab` exit 0 — `tests 43 / pass 43 / fail 0` (9+15+6+13), matches implementer 43/43; `git diff --check 209f184..708dd28` exit 0, no output. Recorded (implementer, not re-run per brief): full JS 537/537, `mvn test` BUILD SUCCESS `Tests run: 2421, Failures: 0, Errors: 0, Skipped: 4` with node --test record present in build output. |
| Downstream interfaces | PASS | P1 preview payload intact: `renderExpertMailPreview` (`app.js:8112`) zero diff hits; payload `app.js:8133-8135` keeps `contactId: previewContactId` (from contact lookup, null when absent), `expertEmail: null`, `senderAccountCode: null`. M-3 mailbox tag editor: `renderMailboxExpertTagEditor` (`app.js:4497`) and both call sites (`app.js:9087`, `app.js:9656`) zero diff; default section output byte-identical via S1/S2_EXPECTED cases (15/15). Four sub-tab panels: `renderDetailSubTabs` + panels zero modified lines; data-panel counts unchanged (see I-10). `index.html` zero diff (0 lines); `senderBindingDisplay.test.js` zero diff; `updateSaveButtonState` body zero diff (new function inserted before it only); list-item tag chips region zero diff. |

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1: Plan T11 case-1 / S-8 acceptance wording "loadContactDetail 源码不含 `style=`" conflicts with 5 pre-existing inline styles in the metadata-grid region of `loadContactDetail` (base app.js). Implementer scoped the no-inline-style assertion to the new action-bar template region (contactHeadLayout test #1 slices `$("#contactHeadActions").innerHTML` → `const banner`) and satisfied the plan's own "全局" criterion: app.js `style="` hit set identical to base (182 = 182, content-identical). Plan-consistent interpretation; no violation.
- O-2: S-4 conditional `.contact-head-actions .button[disabled]` was appended (styles.css:9406-9410) because `styles.css` has no `.button[disabled]` rule; the plan requires this exact conditional addition with a PR note, and a `/* S-4 … */` comment documents it in-file. Plan-sanctioned; recorded for visibility.

### Required Action
- COMPLETE_CHILD
