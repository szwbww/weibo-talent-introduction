## Light Verification: LIGHT_PASS_WITH_NOTES
Child: 03-promotion-classification-gate (docs/plans/2026-08-25/03-promotion-classification-gate.md)
Boundary: 05ad78be88861136400b0ad4b42033fe50812295..b2fdf028d16b1669c9c3f481fb5b94abd77d4e60
Verifier: Verify03Light

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
|Authorized scope|PASS|`git diff --name-only 05ad78b..b2fdf02` product/test files = exactly the 6 authorized: config/ExpertClassificationProperties.kt, application.yml, ExpertIndexWriterService.kt, ExpertRevalidationService.kt, ExpertRevalidationServiceTest.kt, ExpertIndexWriterServiceTest.kt. Other changed files are docs-only evidence (master plan amendment A3, ledger, child-02 execution/fix/verify logs). `git diff -- CandidateEligibilityService.kt` = 0 lines; ExpertClassificationService.kt absent from diff (untouched).|
|Plan and invariants|PASS|I3-1: ExpertRevalidationService.kt:171-173 explicit `classification.type == ExpertType.SERVICE_ONLY || ... == ExpertType.OUT_OF_SCOPE`; zero `!classification.sendable` repo-wide (only pre-existing `classification.sendable` writes at ExpertIndexWriterService.kt:355 / backfill stats :274); tests `gate on passes UNKNOWN` and `Robert Bosch GmbH plus 2024 profile as UNKNOWN` (real classifier) pass. I3-2: gate comment :168-169 verbatim cites enrichExistingExperts (ExpertDiscoveryService.kt:845-877) scanning only CANDIDATE (:850, :877). I3-3: zero code reads of `profile.expertClassification`/`.expertClassification?.type` (only explanatory comment :166); classify(profile) fresh at :165; test `gate off still writes expertClassification` asserts written node == classify() output passed through classificationNode. I3-4: classificationNode single definition in src/main/kotlin (ExpertIndexWriterService.kt:352), diff = modifier-only change private->fun, body verbatim (9 keys, classifiedAt `yyyy-MM-dd HH:mm:ss` via dateFormatter :40/:362); called at ExpertRevalidationService.kt:276. I3-5: promotionGateEnabled default false (ExpertClassificationProperties.kt:20; application.yml:34 `promotion-gate-enabled: ${EXPERT_CLASSIFICATION_PROMOTION_GATE_ENABLED:false}` under expert-classification); write unconditional (put :276 outside gate); tests 5/6 pass. I3-6: gate after evaluateEligibility block (:163) before documentExistsInIndex (:176); test `eligibility failure never calls classify` verifyNoInteractions passes. promoteRawToCandidate(profile, classification) :257 + call site :201; 3 overlay keys candidateValidatedAt/updatedAt/tags + rawDoc copy untouched (test `promoted doc preserves raw fields` asserts exact key set); revalidateCandidates untouched; `CLASSIFICATION:${type.name}` filterReasons + stats.filtered++ (:174-175).|
|Required commands|PASS|JAVA_HOME=zulu-11. `mvn test`: exit 0, Tests run 2863, Failures 0, Errors 0, Skipped 4 (child-02 baseline 2853 → +10 new tests). `-Dtest=ExpertRevalidationServiceTest`: exit 0, 19/19. `-Dtest=ExpertIndexWriterServiceTest`: exit 0, 30/30. `-Dtest=ExpertClassificationServiceTest`: exit 0, 29/29. `-Dtest=ExpertRevalidationServiceBehaviorTest`: exit 0, 14/14. `git diff --check`: exit 0, no output. CandidateEligibilityService.kt diff: empty. All run FRESH on b2fdf02.|
|Downstream interfaces|PASS|Write path uses classificationNode exclusively (ExpertRevalidationService.kt:276; sole other writer = pre-existing backfill via same node ExpertIndexWriterService.kt:248). Written object is classificationNode-shaped: 9 keys, classifiedAt via dateFormatter (yyyy-MM-dd HH:mm:ss). Test `classificationNode output matches backfill node shape (I3-4)` asserts all 9 keys + format. Child 01 consumes via unchanged seam toExpertProfile→parseExpertClassification (ExpertSearchService.kt:549). No later child consumes child 03 API: child 04 (04-discovery-subject-scope.md:299) only references its own EXPERT_CLASSIFICATION_INCREMENTAL_ENABLED note.|

### AUTO_FIX
- N/A

### RECORD_ONLY
- D-1 (constructor deps with defaults): `expertClassificationService = ExpertClassificationService()` / `expertClassificationProperties = ExpertClassificationProperties()` appended at END of the parameter list (ExpertRevalidationService.kt:24-25), satisfying the plan's placement rule; defaults exist solely to keep the pre-existing positional 6-arg construction in the unlisted ExpertRevalidationServiceBehaviorTest.kt:25,231 compiling (it passes 14/14). Spring wiring is correct: single constructor (8 params, only 2 defaulted → no synthetic no-arg constructor) → implicit constructor autowiring resolves all 8 params; both types are registered beans (ExpertClassificationService `@Service` component-scanned; ExpertClassificationProperties via `@EnableConfigurationProperties` RestTemplateConfig.kt:36). Kotlin constructor defaults are only a Spring fallback when no bean exists — never here — so production injects the real beans, the gate honors EXPERT_CLASSIFICATION_PROMOTION_GATE_ENABLED, and classify uses the real service. No proven gate violated; no AUTO_FIX. Theoretical nit (outside light gate): if a bean were ever removed, production would silently fall back to a default-constructed classifier instead of failing fast.

### Required Action
- COMPLETE_CHILD
