## Light Verification: LIGHT_PASS_WITH_NOTES
Child: 02-batch-send-type-filter (docs/plans/2026-08-25/02-batch-send-type-filter.md)
Boundary: 7c703e3d5e51c165ee6c75f316de0f018c44e8df..05ad78be88861136400b0ad4b42033fe50812295
Verifier: Verify02Light

### Four Gates
|Gate|Result|Evidence|
|---|---|---|
|Authorized scope|PASS|`git diff --stat 7c703e3..05ad78b`: product/test files = exactly the 10 authorized files (V108__add_expert_types_to_batch_send_task_config.sql new, BatchSendTaskConfig.kt +4, BatchExecutionModels.kt +26, BatchSendTaskConfigService.kt +38, ManualInitialOutreachService.kt +2, index.html +26, app.js +42, ManualInitialOutreachServiceTest.kt +160, BatchSendTaskConfigServiceTest.kt +168, batchExpertTypeFilter.test.js new +156). Remaining changes in range are docs-only (brief.md, execution.md, ledger.md, plan .md, child-01 verify/fix/execution docs). styles.css NOT in diff (untouched). PASS. |
|Plan and invariants|PASS|I2-1: ManualInitialOutreachService.kt:1325 `ExpertSearchService.expertTypesFilter(scope.expertTypes)?.let { filters.add(it) }` inside INTRODUCTION if-block (:1322-1327) directly above :1326 `filters.add(ExpertSearchService.expertSendableFilter())` — sendable line verbatim (file diff = 2 additions only); BatchExecutionModels.kt:84-90 type block placed after operatorStatuses (:72-79) and before discipline (:91), i.e. after hard gate (:69-72, additions-only diff = verbatim); tests `non-empty expertTypes adds type filter and keeps sendable hard gate (I2-1)` + `matchesExpert hard gate wins over expert type match (I2-1)`. I2-2: grep — type judgment only at BatchExecutionModels.kt:84-90 + ManualInitialOutreachService.kt:1325; ExpertSearchService.kt:1137 (child-01 list seam) and ExpertIndexController.kt:453 (display mapping) are pre-existing, outside this range diff; countEsTargets/fetchEsPage share the single buildEsFiltersForLevel seam; test `preview and execution use identical filter arrays for same scope with expertTypes (I2-2)` (3× countExperts with identical expectedFilters). I2-3: `?.let` skip on null, matchesExpert guard `expertTypes.isNotEmpty()` (:84), BatchSendTaskConfig.kt:26 default `expertTypesJson = "[]"`, V108 UPDATE `'[]'`; test `empty expertTypes keeps pre-change baseline filters verbatim on CANDIDATE (I2-3)`. I2-4: V108 adds only `expert_types_json TEXT NOT NULL AFTER operator_statuses_json`; grep over src/main/resources/db/migration — zero single-value expert_type columns; parse-failure → emptyList at BatchExecutionModels.kt:296-303 and BatchSendTaskConfigService.kt:448-455; tests `invalid expertTypesJson parses as empty list without throwing (I2-4)` + `entity toFields roundtrip preserves expertTypes and setEnabled revalidation passes (I2-4)`. I2-5: BatchSendTaskConfigService.kt:197 `expertTypes = parseExpertTypes(existing.expertTypesJson)` explicit, right after operatorStatuses precedent (:193-195); test `updateLegacyConfig preserves existing expertTypesJson entity value (I2-5)`. I2-6: type filter only inside `if (scope.mailType == BatchSendType.INTRODUCTION.name)` block; matchesExpert type block gated on INTRODUCTION; test `MATERIAL_REMINDER with expertTypes adds no type filter (I2-6)`. M-1: I2-1 evidence. M-2: BatchSendTaskConfigService.kt:304 requires `it in ExpertSearchService.ALLOWED_EXPERT_TYPES` — references child-01 constant, no new whitelist. M-3: I2-3 evidence. M-6: baseline-verbatim test + MATERIAL_REMINDER unchanged test. Frontend S2-1/S2-2: index.html:1278-1289 (editor picker after #batchConfigEditorOperatorStatuses block) and index.html:1497-1511 (manual picker after #manualFieldOperatorStatus, outer `<div class="batch-config-field" id="manualFieldExpertTypes">`, trailing `.batch-config-diff-badge`/`.batch-config-diff-original`); four-piece ids complete; app.js:14519/14524 registry entries with previewKind "editor"/"manual"; options :14548-14554 labels equal child-01 chips (app.js:5134-5140); payloads carry expertTypes (app.js:14896, 15039, 15259); backfill restores (:14131, :15174); binds (:16053-16054); no `change` listeners added for the new ids. PASS. |
|Required commands|PASS|All run FRESH in worktree with JAVA_HOME=zulu-11: (1) `mvn test` EXIT=0, BUILD SUCCESS, Tests run: 2853, Failures: 0, Errors: 0, Skipped: 4 (baseline: child 01 = 2839; implementer 05ad78b = 2853 — matches). (2) `mvn test -Dtest=ManualInitialOutreachServiceTest` EXIT=0, Tests run: 99, Failures: 0, Errors: 0. (3) `mvn test -Dtest=BatchSendTaskConfigServiceTest` EXIT=0, Tests run: 62, Failures: 0, Errors: 0. (4) `mvn test -Dtest=FlywayMigrationIntegrationTest -DmigrationIt=true` attempted fresh — Docker unavailable on machine (no /var/run/docker.sock; docker-machine not found), test errors IllegalStateException "Docker is required for Flyway migration tests" (1 test, 1 error); recorded as planned skip (brief permits when Docker unavailable), NOT a code failure. (5) `node --test src/test/js/batchExpertTypeFilter.test.js` EXIT=0, tests 6, pass 6, fail 0. (6) `node --test src/test/js/*.test.js` EXIT=0, tests 743, pass 743, fail 0. (7) `node --check src/main/resources/static/app.js` OK. (8) `git diff --check` EXIT=0, no output. PASS. |
|Downstream interfaces|PASS|No later child consumes child-02 deliverables: children 03/04 briefs contain zero references to expertTypes/expert_types_json/V108/BatchSendTaskConfig changes; dependency direction is 01→02 (child-01 brief declares downstream interface child 02 consumes). Child-02 own deliverables present: V108 column surfaced in toView (BatchSendTaskConfigService.kt:479 `expertTypes = parseExpertTypes(row.expertTypesJson)`) and in execution snapshot (BatchExecutionModels.kt:317 `expertTypes = expertTypes` in toExecutionSnapshot). PASS. |

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1: app.js manual-panel diff wiring beyond the literal Task-6 bullets (formatManualDiffValue app.js:15310-15315, computeManualDiffs :15335, computeAndRenderDiffs :15384, clearAllDiffMarkers :15416, normalizeManualSnapshot :15279, deepCloneConfig :15125, fillManualFormDefaults :15148, fillManualFormFromDraft :15174). Authorized file; consistent with S2-2 DOM contract (manualFieldExpertTypes carries `.batch-config-diff-badge`/`.batch-config-diff-original`); makes the '已修改' badge functional (A2-7). No filter logic touched and no change listeners added → I2-* invariants intact. Not a gate violation; noted for downstream acceptance.
- O-2: FlywayMigrationIntegrationTest -DmigrationIt=true could not execute: Docker daemon unavailable in this environment (no /var/run/docker.sock), test aborts with IllegalStateException "Docker is required for Flyway migration tests" before any migration logic. Brief explicitly permits skip when Docker is unavailable; V108 syntax is exercised indirectly by the full suite (Flyway parses/validates migrations during test startup, full `mvn test` exit 0). Not a code failure.

### Required Action
- COMPLETE_CHILD
