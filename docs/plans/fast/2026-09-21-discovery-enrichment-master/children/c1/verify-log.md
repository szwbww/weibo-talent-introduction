## Light Verification: LIGHT_PASS_WITH_NOTES
Child: c1 — docs/plans/2026-09-21/01-openalex-auth-budget.md
Boundary: f0c41271fc56d7455e14d28a71d563a5341dfdeb..147dc953a194a27ea73b7934a8e2bc334beca655
Verifier: C1Verifier

### Four Gates
| Gate | Result | Evidence |
|---|---|---|
| Authorized scope | PASS | `git diff --name-only <base>..<head> \| grep -v '^docs/'` returns exactly the 8 authorized files: OpenAlexProperties.kt, OpenAlexRequestPolicy.kt, RestTemplateConfig.kt, OpenAlexDataSource.kt, application.yml, RestTemplateConfigTest.kt, OpenAlexRequestPolicyTest.kt, OpenAlexDataSourceTest.kt. Commit 147dc95 `feat(fast-p): implement c1` (`git show --name-only`) contains only those 8; `docs/plans/fast/**` is untracked (`git status --porcelain` -> `?? docs/plans/fast/2026-09-21-discovery-enrichment-master/`); no file under `src/main/resources/db/migration` in the boundary |
| Plan and invariants | PASS | V-1/I-1: `OpenAlexAuthInterceptor` RestTemplateConfig.kt:122-152 — bearer only for the trusted HTTPS origin (:146-147), `.remove(AUTHORIZATION)` on every other origin (:149), `isTrustedOrigin` (:154-159) — attached solely to `openAlexRestTemplate` (:96-110); key never placed in a URL; `OpenAlexProperties.toString()` redacts the key (OpenAlexProperties.kt:27-33, test `configuration never prints a live key (I-1)`); `application.yml:206` ships an empty key default. V-2/I-2 + V-3/I-3: `Permit` (:25-30), `beforeRequest` (:105-137), `recordResponse` (:140-195), reserve floor (:115-117) + `reservedCredits` (:225-226), provider header parsing/reconciliation (:145-190), UTC-day rollover (:228-241), free-budget defaults keyless $0.10 / keyed $1 (:217-221, :259-260), no automatic paid-balance use (test `paid prepaid balance is never consumed automatically (I-2)`). Policy seam: `getJson(kind,url)` OpenAlexDataSource.kt:64-78, `RequestKind.DISCOVERY` at :51, kind-threaded enrich overloads :143-258. V-3 no-header/restart path: `keyless budget is the 0_10 dollar free budget and is never unlimited (I-2)` and `budget is rebuilt after the UTC reset (V-3)` |
| Required commands | PASS | Fresh run in this worktree with `JAVA_HOME=/Library/Java/JavaVirtualMachines/zulu-11.jdk/Contents/Home`: `mvn test -Dtest=RestTemplateConfigTest,OpenAlexRequestPolicyTest,OpenAlexDataSourceTest` -> exit 0, BUILD SUCCESS; surefire reports: RestTemplateConfigTest 10, OpenAlexRequestPolicyTest 13, OpenAlexDataSourceTest 37 = Tests run 60, Failures 0, Errors 0, Skipped 0; the same run's exec-plugin JS regression reports `tests 1035, pass 1035, fail 0`, matching the recorded baseline (the full `mvn test` suite was deliberately not re-run: it is the integration-stage gate) |
| Downstream interfaces | PASS | Required shapes exist verbatim: `sealed class Permit { object Allowed; data class Deferred(val resetAt: Instant) }` (OpenAlexRequestPolicy.kt:25-30), `enum class RequestKind` with DISCOVERY/NEW_ENRICHMENT/HISTORY_ENRICHMENT (:14-22), `fun beforeRequest(kind: RequestKind): Permit` (:105), `fun recordResponse(headers: HttpHeaders): Unit` (:140); `searchPapers` marks DISCOVERY (OpenAlexDataSource.kt:51, proven by the reserve test); the policy is usable off-Spring (`OpenAlexRequestPolicy(properties)`, OpenAlexRequestPolicy.kt:66-69 + default `time`) and is also a bean (RestTemplateConfig.kt:112-114); existing public signatures unchanged — base/head signature lists identical for `searchPapers`, `extractAuthorEmails`, 1-arg `enrichAuthor`, `enrichAuthorByOrcid`, `enrichAuthorByOrcidWithReason`, `batchEnrichByOrcids`, only additive `kind` overloads; the sole existing production enrich caller (`ExpertDiscoveryService.enrichExistingExperts`, reached from `POST /api/expert-discovery/enrich`) keeps the legacy 1-arg path |

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1: I-3's "list calls and fulltext downloads are counted separately" exists only at the policy boundary (`fulltextDownloadCount()` OpenAlexRequestPolicy.kt:99, incremented when `X-RateLimit-Credits-Used >= 100` at :155-157, covered by `list calls and fulltext downloads are counted and charged separately (I-3)`), but it has no production writer today: the only `recordResponse` caller is `OpenAlexDataSource.getJson` (all list calls), and OpenAlex fulltext PDFs are downloaded by `PdfEmailExtractor` over `pdfDownloadRestTemplate`, so the counter stays 0 until a later child wires an OpenAlex fulltext call. Informational, not a four-gate violation: wiring a call site needs files outside c1's authorized list (the fulltext path belongs to child c10).

### Required Action
- COMPLETE_CHILD
