## Light Verification: LIGHT_PASS_WITH_NOTES
Child: c6 (docs/plans/2026-09-21/06-targeted-enrichment.md)
Boundary: 1ba685217a166628968a798450ff203191ead79c..16647117f7f7f7e7d1a66f35524900f0ea431e0d
Verifier: C6Verifier

### Four Gates
| Gate | Result | Evidence |
|---|---|---|
| Authorized scope | PASS | `git show --stat 1664711` = exactly the 8 authorized product/test files (ExpertDiscoveryService.kt, OpenAlexDataSource.kt, ExpertSearchService.kt, ExpertRevalidationService.kt + their 4 tests); no 9th file, no Flyway/ES-mapping/config/new-table change, no `docs/plans/fast/**` in the commit. `OperatorStatusWriteSeamGuardTest.kt` is byte-identical to the base (absent from the diff): its only `ExpertSearchService.kt` pin (line 498, context `operatorStatus = source.nullableText`, test file lines 76-80/158-166) still asserts the same snippet because the new `findByDocumentIds` was added at ExpertSearchService.kt:850, below the pinned region - no assertion relaxed, deleted, or re-scoped. Worktree HEAD == 1664711 and `git status --porcelain` shows only `docs/plans/fast/.../ledger.md` (modified) and `children/c6/execution.md` (untracked), i.e. no un-committed product delta outside the boundary. Legacy test edits are signature-only (`batchEnrichByOrcids(anyList())` -> `(anyList(), eqValue(RequestKind.HISTORY_ENRICHMENT))`, 20 stub/verify lines); the diff of ExpertDiscoveryServiceTest.kt deletes no assertion. |
| Plan and invariants | PASS | I-1: identity precedence authorId -> ORCID -> `NoId` (ExpertDiscoveryService.kt:1573-1583), `trustedOpenAlexAuthorId` only accepts `A`+digits via `normalizeOpenAlexAuthorId` (:1535-1542), `trustedOrcid` rejects `EMAIL-*` (:1546-1547), batching `coerceIn(1, MAX_ENRICHMENT_IDENTITIES_PER_BATCH=100)` (:1528-1529, constant at :1872-1875) with per-identity chunks (:1614), results keyed by real `_id` (`enrichmentDocId` = esDocId ?: orcidId, :1525). Tests: `enrichProfiles prefers the trusted author id and keys results by the real esDocId (I-1)` (ExpertDiscoveryServiceTest.kt:2928 - asserts keys {DOC-A,DOC-B,DOC-C}, `NoId` for `EMAIL-noid`, and `never()` on the EMAIL-/wrong-identity queries), `enrichProfiles splits more than 100 identities into bounded mixed batches (V-1)` (:2967 - 201 experts -> authorId [100] + orcid [100,1]), `batchEnrichByAuthorIds queries by author id and only maps canonical ids (I-1)` / `never attributes a non-canonical response id (I-1)` (OpenAlexDataSourceTest.kt:904/937). V-1 satisfied. I-2: `updateExpertAcademicFields` stays the sole academic write point and now returns `LayerUpdateResult` (:1448-1491, contract type :1891-1912); per-layer HEAD-then-`_update` by real `_id` with 404 -> `ABSENT` and non-404 / `_update` failure -> `FAILED` (:1493-1521); null facts omitted from the body (:1450-1468); body carries only academic keys + `updatedAt`/`enrichedAt`/`enrichmentSource`/`expertClassification` (no name/email/institution/operatorStatus) and no layer is ever created. Tests: `enrichProfiles reports per-layer results without creating a missing layer (V-2, I-2)` (:3006 - `ABSENT` for CANDIDATE/APPLICATION plus `never()` PUT), `never writes null facts over existing values (I-2)` (:3035), `reports Partial when an existing layer write fails (V-2, I-2)` (:3058), `enrichExistingExperts counts a RAW-only update as success (I-2)` (:3196) paired with `counts a partially written expert as failed (I-2)` (:3146). V-2 satisfied. I-3: base facts parsed independently of the switches (`parseAuthorBase`, OpenAlexDataSource.kt:406-449), titles gated by `fetchWorksEnabled`/`fetchPatentsEnabled` with `TitlesFetch.NOT_REQUESTED/failed/ok` keeping "request failed" apart from "no works" (:287-345, :499-513), single and batch paths share `parseAuthorEnrichment` (:159-171 vs :448) - patents default off (OpenAlexProperties.kt:20-21, application.yml:204-205); `Partial.recentWorksFailed` is the only new completion signal and the write body is built only on Success/Partial (:1625-1633). Tests: `enrichAuthor uses the same titles switches as the batch path (V-3)` (OpenAlexDataSourceTest.kt:950 - exactly 1 GET with switches off), `fetches recent works only when the switch is on (V-3)` (:969), `batchEnrichByOrcids keeps base facts usable when the titles call fails (I-3)` (:996), `treats an empty titles result as success not failure (I-3)` (:1035), `enrichProfiles reports Partial when the optional recent-titles fetch fails and Success after recovery (I-3)` (ExpertDiscoveryServiceTest.kt:3087), `enrichAuthorByOrcidWithReason keeps a 404 not-found apart from a retryable failure (V-3)` (:1073). V-3 satisfied. I-4: one gate implementation `evaluateRawPromotionGate` (ExpertRevalidationService.kt:267-292) shared by `promoteEligibleRawExperts` (:156) and `revalidateEnrichedRaw` (:308-346), which returns `AlreadyPresent` for an existing APPLICATION then CANDIDATE before any read (:311-329), reads the newest RAW by real `_id` through `findByDocumentIds` (ExpertSearchService.kt:850-869) and re-reads RAW at write time inside `promoteRawToCandidate` (:250-251); no demotion path added. Tests: ExpertRevalidationServiceTest.kt:364, 377 (AlreadyPresent), 388 (candidate body from newest RAW, hIndex 42), 418 (ineligible -> Rejected, classify never called), 433, 445, 455 (fail-closed), 466. |
| Required commands | PASS | Fresh run this invocation in the worktree, `JAVA_HOME=/Library/Java/JavaVirtualMachines/zulu-11.jdk/Contents/Home mvn test -Dtest=ExpertDiscoveryServiceTest,OpenAlexDataSourceTest,ExpertRevalidationServiceTest,ExpertSearchServiceTest,OperatorStatusWriteSeamGuardTest` -> exit 0, `BUILD SUCCESS`, `Total time: 02:38 min`, finished 2026-09-21T13:29:10+08:00. Surefire (reports dated 13:29:06): ExpertDiscoveryServiceTest 98, OpenAlexDataSourceTest 48, ExpertRevalidationServiceTest 27, ExpertSearchServiceTest 69, OperatorStatusWriteSeamGuardTest 1 = 243 tests, Failures 0, Errors 0, Skipped 0 - matches the implementer claim. The same invocation ran the test-phase JS suite: `tests 1035, pass 1035, fail 0`, identical to the controller baseline. Full `mvn test` (3504 tests) intentionally not run, per the child brief. |
| Downstream interfaces | PASS | `ProfileEnrichmentOutcome` variants are exactly `Success, Partial, Deferred, NotFound, NoId, RetryableError` with those names (ExpertDiscoveryService.kt:1913-1939); payloads `Success.layers`, `Partial.layers/recentWorksFailed`, `Deferred.resetAt`, `RetryableError.retryAfterMs/rateLimited` are additive. `enrichProfiles(profiles, requestKind = HISTORY_ENRICHMENT)` at :1559-1563 and `findByDocumentIds(level, ids)` at ExpertSearchService.kt:850 keep the plan names/shapes (`_mget` with per-doc `_index`/`_id`/`_source`, `found=false` dropped, reusing `toExpertProfile` so readers see the same fields). `LayerUpdateResult(raw, candidate, application)` is a 3-field data class whose only extra members are functions, so `result_json` round-trips on exactly those three stable names (:1891-1912). `revalidateEnrichedRaw(docId): PromotionOutcome` with `AlreadyPresent` on existing CANDIDATE or APPLICATION (ExpertRevalidationService.kt:308-329) and the remaining variants at :347-365. Manual entry and its three scopes still route through the same core: ExpertDiscoveryController.kt:230-243 passes the scope, ExpertDiscoveryService.kt:1165-1173 keeps DEFAULT / INSTITUTION_TYPE_BACKFILL / LAST_PUBLICATION_YEAR_BACKFILL filters, progress/token usage unchanged, and `enrichExistingExperts` delegates each chunk to `enrichProfiles(..., RequestKind.HISTORY_ENRICHMENT)` (:1227); the three scope tests and the progress/token tests pass in the fresh run. Legacy `enrichAuthor` / `enrichAuthorByOrcid(WithReason)` / `batchEnrichByOrcids(List)` / `batchEnrichByAuthorIds(List)` entry points are all retained (OpenAlexDataSource.kt:167-171, :237-249, :281-282, :294-295). |

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1: the I-4-mandated single-gate extraction reorders the pre-existing `promoteEligibleRawExperts` sequence from eligibility -> classification -> CANDIDATE existence -> email to eligibility -> classification -> email -> CANDIDATE existence (ExpertRevalidationService.kt:267-292 vs the removed block in the base), so a RAW doc that is already a CANDIDATE and also fails the email gate now lands in `stats.emailRejected` (counted in `PromotionScanStats.taskFailureCount`, ExpertRevalidationServiceTest.kt:37-45) instead of `stats.alreadyPromoted` (counted in neither), and such docs no longer reach the HEAD check that fed `stats.existenceCheckFailed`; promotion decisions are identical (nothing created, nothing demoted) and no c6 invariant fixes the counter attribution, so this is operator-visible task-status attribution on a pre-existing scan rather than a four-gate failure - flagged for human awareness, not as a repair.

### Required Action
- COMPLETE_CHILD
