## Light Verification: LIGHT_PASS
Child: c5 — docs/plans/2026-09-21/05-author-identity.md (brief: docs/plans/fast/2026-09-21-discovery-enrichment-master/children/c5/brief.md)
Boundary: 985f1ddf5891bdf534fbfeb2e5140ce6fd3f254b..1ba685217a166628968a798450ff203191ead79c
Verifier: C5Verifier

### Four Gates
| Gate | Result | Evidence |
|---|---|---|
| Authorized scope | PASS | `git diff --name-only 985f1dd..1ba6852` contains exactly the 10 authorized product/test paths (6 main + 4 test) and `git show --stat 1ba6852` = those same 10 files under message `feat(fast-p): implement c5`; no migration, ES mapping (`src/main/resources/es/*.json`) or config file is touched. The boundary additionally carries 4 `docs/plans/fast/**` bookkeeping files (children/c4/{execution,fix-log,verify-log}.md, ledger.md) that arrived via c4 evidence commits + controller ledger edits — not product/test files and deliberately not committed by the implementer (`git status`: only ` M ledger.md`, `?? children/c5/execution.md`). |
| Plan and invariants | PASS | **I-1**: `PaperAuthor.kt:16` / `AuthorEmail.kt:16` add nullable default `openAlexAuthorId: String? = null` (`@JvmOverloads` on both constructors, `PaperAuthor.kt:3` / `AuthorEmail.kt:3`); `OpenAlexDataSource.kt:154` keeps `authorships[].author.id` via `normalizeOpenAlexAuthorId` (`OpenAlexDataSource.kt:419-422`, `A\d+` only, `https://openalex.org/` prefix stripped); `ExpertDiscoveryService.kt:1482-1491` writes it as the `externalIds` sub-key and leaves `_id`/`orcidId` semantics untouched (`ExpertDiscoveryService.kt:1035` still uses the unchanged id generator). Tests: `OpenAlexDataSourceTest` "searchPapers keeps the OpenAlex author id of every authorship (I-1)" (file lines 731-758 — `A5023888391`/`A5086928770` kept, `https://openalex.org/W9` rejected), `ExpertDiscoveryServiceTest` "discover never stores a non-canonical author id in externalIds (I-1)" (555-581), "discover keeps the EMAIL-* primary key and promotes the author id to CANDIDATE (V-1)" (511-552: `EMAIL-` prefix + `promoted=1` + sub-key present in the CANDIDATE PUT body). **I-2**: `PdfEmailExtractor.kt:209-223` — `hasStrongEmailNameEvidence` requires the local part to contain the full family *and* full given name (tokens ≥2 chars); `verifiedAuthorFor` = `singleOrNull()` strong match else sole-author-and-sole-email; `CoreDataSource.kt:179-194` consumes the same seam (one rule for PDF and CORE full text); `OpenAlexDataSource.kt:81-108` attaches a PMC author id only when the email ORCID is exactly equal to a uniquely corresponding author id (`distinct().singleOrNull()`), leaving EuropePMC itself untouched. Tests: `PdfEmailExtractorTest` 267-355 (shared-initial → null, identical full name → null, surname substring `li ⊂ lian` → null; unique full-name combination and sole-author carry identity+ORCID+id), `CoreDataSourceTest` 223-274 (same three shapes + sole-author), `OpenAlexDataSourceTest` 760-825 (exact ORCID aligned → id; two authors sharing one ORCID → null; missing/mismatching ORCID → null). **I-3**: mappings unchanged and still `"externalIds": {"type":"object","enabled":false}` (`src/main/resources/es/orcid_info_raw.json:31`, `orcid_info_candidate.json:32`, `orcid_info_application.json:43`); the write merges `pmcId`/`doi`/`pmid`/`orcid` plus the new sub-key into the existing map (`ExpertDiscoveryService.kt:1483-1490`), asserted by exact map equality at `ExpertDiscoveryServiceTest:497-505`; reads stay `_source`-only (`ExpertSearchService.kt:492`, `ExpertDiscoveryService.kt:1052`) and a worktree-wide grep for `externalIds` finds no `exists`/`term` on any sub-field. |
| Required commands | PASS | Fresh run in the worktree at commit 1ba6852 (working tree clean except docs): `JAVA_HOME=/Library/Java/JavaVirtualMachines/zulu-11.jdk/Contents/Home mvn test -Dtest=OpenAlexDataSourceTest,PdfEmailExtractorTest,CoreDataSourceTest,ExpertDiscoveryServiceTest` → exit 0, `BUILD SUCCESS`, `Tests run: 167, Failures: 0, Errors: 0, Skipped: 0` (OpenAlexDataSourceTest 41, PdfEmailExtractorTest 22, CoreDataSourceTest 16, ExpertDiscoveryServiceTest 88; the four surefire reports were rewritten by this run at 12:50). The same invocation's exec-bound JS suite reported `pass 1035 / fail 0`, matching the recorded 1035/0 seed baseline; no counter regressed against the recorded baseline. |
| Downstream interfaces | PASS | c6's first trusted identity (`externalIds.openAlexAuthorId`, see `docs/plans/2026-09-21/06-targeted-enrichment.md:20`) is written under exactly that key and only in canonical `A`+digits form (`ExpertDiscoveryService.kt:1489-1490` after `normalizeOpenAlexAuthorId`, `OpenAlexDataSource.kt:419-422`), and never as a document key (V-1 test asserts the `EMAIL-` primary key; the id generator at `ExpertDiscoveryService.kt:1035` is unchanged). Constructor call sites still compile: the fields are additive nullable defaults with `@JvmOverloads`, keeping the unauthorised Java site `src/test/java/com/weibo/talentintroduction/discovery/service/DiscoveryMockHelper.java:190` and every Kotlin site valid — proven by the exit-0 compilation of main+test in the fresh run. `buildExternalIds` (`ExpertDiscoveryService.kt:1482`) is the only `externalIds` writer in this boundary and merges rather than replaces; three-layer promotion still copies the whole `_source`, asserted by the CANDIDATE PUT assertion in the V-1 test (511-552). |

### AUTO_FIX
- N/A

### RECORD_ONLY
- N/A

### Required Action
- COMPLETE_CHILD
