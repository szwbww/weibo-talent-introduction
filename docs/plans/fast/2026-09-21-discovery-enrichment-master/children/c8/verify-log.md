## Light Verification: LIGHT_PASS_WITH_NOTES
Child: c8 — docs/plans/2026-09-21/08-auto-enrichment.md
Boundary: 449fb48f264872402b40f14ef4b22f98a521e480..8873dc96cd2799a49ee1bd055d5366c9e3a78ae0
Verifier: C8Verifier

### Four Gates
| Gate | Result | Evidence |
|---|---|---|
| Authorized scope | PASS | `git show --stat 8873dc9` = exactly 8 files, all inside the child's 9-file list: `ExpertDiscoveryService.kt`, `ExpertAcademicEnrichmentWorker.kt` (new), `DiscoveryExecutorConfig.kt`, `ExpertDiscoveryProperties.kt`, `application.yml`, and the 3 named tests; the production controller is authorized but deliberately unchanged. The 4 `docs/plans/fast/**` files inside the boundary range come from c7's docs commit 72a7bfd (`git show --stat 72a7bfd`), not from the c8 commit — plan artifacts, not product/test files. `git status --porcelain` shows only the untracked `children/c8/execution.md` and the pre-existing `ledger.md` edit. |
| Plan and invariants | PASS | **I-1**: enqueue only after a successful RAW write on both entry paths — `ExpertDiscoveryService.kt:1075-1077` (paper: `indexToRaw` → `indexed++` → `enqueueEnrichmentJob(esDocId, source.sourceName, executionId, …)`) and `:857-859` (ORCID loop, same order); a failed enqueue is counted as `ENRICHMENT_ENQUEUE_FAILED` (`:1111-1117`, code `:2239`) and the entering page is retained: paper `:614`/`:623`/`:668-671` (`stopReason = ENQUEUE_INCOMPLETE` before break; run end re-persists `resumeCursor`), ORCID `:899`/`:908`/`:923-925`. Dedup results carry the matched document's real `_id` (`:1994` ORCID HEAD doc id; `:2017` `hits.hits[0]._id`); replay/duplicate only calls `ensureEnrichmentJob` (`:1124-1126`) and never rewrites the expert doc; other flows' imports are never re-identified (no identity write on the duplicate branch). Tests: `论文路径在 RAW 落库成功后按真实 _id 入队并带上发现执行 id（I-1 I-4）`, `ORCID 路径在 RAW 落库成功后按真实 _id 入队（I-1）`, `补全入队失败保留当前页，重放按匹配 _id 补建任务且只新增 1 人（I-1）` (V-1: run 1 indexed=1, cursor stays `C1`, stopReason `ENQUEUE_INCOMPLETE`, pendingSources=1; run 2 duplicates=1, indexed=0, cursor advances to `C2`). **I-2**: `@Scheduled(fixedDelay = 30000)` `ExpertAcademicEnrichmentWorker.kt:46`; bounded claim `limit.coerceIn(1, MAX_ENRICHMENT_IDENTITIES_PER_BATCH)` `ExpertDiscoveryService.kt:1593-1601` (partial batches still run — `领取上限使用配置的批次大小（尾批不足 100 也照常执行）`, clamp test `补全批次领取上限钳制到 100 条（I-2）`); batches run on the dedicated single-thread `autoEnrichmentExecutor` (`Worker.kt:61`, `DiscoveryExecutorConfig.kt:41-52`), not on the shared scheduler thread; unfinished state kept on cancel/time-slice end (`Worker.kt:106-115`, per-job cancel check + break `:1641-1643`; tests `取消发生在领取之前时不领取新任务`, `批次失败时如实记 FAILED 并释放任务锁，未完成状态留给租约恢复`); quota exhaustion only defers (`:1650-1652`, classify `:1739-1741`, test `额度延期只把任务记为待补而不算失败（I-2 I-5）`: pending=1, budgetDeferred, 0 failed) while other source discovery is untouched by this worker; switch defaults to false (`ExpertDiscoveryProperties.kt:19-22`, `application.yml:183`). **I-3**: automatic and manual both go through the same core `processClaimedEnrichmentJobBatch` → `enrichProfiles` (`:1616-1632`; worker `NEW_ENRICHMENT` `Worker.kt:94`, manual `DISCOVERY_PENDING` → `HISTORY_ENRICHMENT`); RAW-only targeted re-evaluation only (`isRawOnly` `:1694-1697`, `revalidateRawOnlySuccess` `:1703-1708`; test `DISCOVERY_PENDING 领取到期任务，只对成功且 RAW-only 的专家定向复评（I-3 I-4）` verifies `revalidateEnrichedRaw` once for the RAW-only doc and never for the candidate doc); no new academic gate (eligibility path untouched) and no auto-demotion (`revalidateEnrichedRaw` only promotes — `ExpertRevalidationService.kt:308-339`). **I-4**: `stats.indexed` is only incremented right after a successful `indexToRaw` (`:857`, `:1075`) and is never touched by enrichment; the job row carries the discovery execution id end to end (`enqueue(docId, source, executionId)` `:1113` → `discovery_execution_id` column, `ExpertAcademicEnrichmentJobRepository.kt:41-44`); the automatic run writes its own `EXPERT_ENRICHMENT` record (`Worker.kt:87-95`, triggerType `SCHEDULED`) whose per-source enqueued/succeeded/pending/unmatched counters go into the existing details JSON (`:1711-1730`, `AutoEnrichmentBatchResult.toDetails()` `:2360-2378`; worker test asserts taskType/triggerType/status/successCount/`bySource`). |
| Required commands | PASS | Fresh run in the child worktree after the final implementation state: `JAVA_HOME=/Library/Java/JavaVirtualMachines/zulu-11.jdk/Contents/Home mvn test -Dtest=ExpertDiscoveryServiceTest,ExpertAcademicEnrichmentWorkerTest,ExpertDiscoveryControllerTest` → exit 0, `BUILD SUCCESS`, finished 2026-09-21T15:20:33, `Tests run: 132, Failures: 0, Errors: 0, Skipped: 0` = ExpertDiscoveryServiceTest 106 + ExpertAcademicEnrichmentWorkerTest 9 + ExpertDiscoveryControllerTest 17 (surefire reports re-written 15:20:05–15:20:28). The test-phase JS suite executed by the same invocation reports `tests 1035 / pass 1035 / fail 0`, identical to the controller's recorded seed baseline. Full `mvn test` was not run (integration-stage gate, out of scope per the brief). |
| Downstream interfaces | PASS | Same mutex as the manual entry: `Worker.kt:50` `tryStartWithToken` with `TASK_TYPE` (`Worker.kt:120`) = `EXPERT_ENRICHMENT_TASK_TYPE` = `"EXPERT_ENRICHMENT"` (`ExpertDiscoveryService.kt:2242`), the literal also used by the manual endpoint (`ExpertDiscoveryController.kt:221`); tests `人工补全持锁时自动检查直接跳过（自动与手动互斥）` and `自动批次在 EXPERT_ENRICHMENT 任务记录下按 NEW_ENRICHMENT 处理并释放任务锁`; discovery keeps its own `EXPERT_DISCOVERY` lock and is unaffected. Manual scope: `DISCOVERY_PENDING` added (`:2230`) with the three legacy scopes unchanged and still callable (`:1268-1281`; controller test `enrichExperts forwards DISCOVERY_PENDING scope to service`, legacy scope tests green in the same run). Config: `autoEnrichmentEnabled` (false) + `autoEnrichmentBatchSize` (100) added additively (`ExpertDiscoveryProperties.kt:19-22`, `application.yml:183-184`) with env overrides, so c9 can read the same object unchanged. Worker bean: `@Service` + `@ConditionalOnProperty(prefix = "talent-introduction.scheduling", name = ["enabled"])` (`Worker.kt:33-34`), parity with `MailAutomationScheduler.kt:14-15`, and a no-op returning at `Worker.kt:48` when the switch is off (test `开关关闭时一次检查什么都不做（不领取、不建任务记录）`). Task UI: no frontend file appears in the commit; counters ride the existing `TaskProgress.details` JSON and the additive `EnrichmentStats.autoEnrichment` response field (`:2382-2391`). |

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1: The automatic batch's `task_execution.result_summary` is `AutoEnrichmentBatchResult`, whose JSON keys are `claimed/succeeded/pending/unmatched/failed/...`, but the shared `EXPERT_ENRICHMENT` summary rule reads `enriched` (`TaskExecutionSummaryExtractor.kt:96-104` and `:160-168`), so an automatic batch that succeeded reports `passed=0` in the existing task totals view (the manual path returns `EnrichmentResult`, which does have `enriched`, so it renders correctly); likewise `details.bySource` now carries enrichment keys (`enqueued/succeeded/pending/unmatched/failed`) while the shared renderer `app.js:1414-1440` renders the discovery columns (`papersSearched`, `indexed`, …) and therefore shows zeros. Display-only; no frontend/shared file is in c8's authorized set, and the plan's contract (counters in `details_json`/`message`) is met — outside the four gates, no four-gate violation proven and the repair is not uniquely determined by the plan.
- O-2: Idle ticks still persist one progress-log row each: `tryStartWithToken` writes the log on every successful start (`TaskProgressStore.kt:145-155`) at `Worker.kt:50`, before `runBatch` returns for an empty claim (`Worker.kt:82-85`), so the claim at `Worker.kt:19-20` / execution.md deviation 5 that an idle check “不写进度日志” does not hold (~2 880 rows/day at the 30 s cadence); after the store entry is cleared (`Worker.kt:106-115`) `GET /api/task-progress/EXPERT_ENRICHMENT` restores that RUNNING orphan row as `INTERRUPTED` (`TaskProgressStore.kt:196-203`), which the page paints red (`app.js:1236-1242`). Orphan rows with negative execution ids are an existing tolerated pattern (created_at-based retention cleanup), the switch is off by default, and a fix would need an unauthorized file — noise/display note only, outside the four gates.

### Required Action
- COMPLETE_CHILD

