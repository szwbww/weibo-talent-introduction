## Light Verification: LIGHT_PASS_WITH_NOTES
Child: c7 (docs/plans/2026-09-21/07-enrichment-job-store.md)
Boundary: 16647117f7f7f7e7d1a66f35524900f0ea431e0d..449fb48f264872402b40f14ef4b22f98a521e480
Verifier: C7Verifier

### Four Gates
| Gate | Result | Evidence |
|---|---|---|
| Authorized scope | PASS | `git diff --name-status 1664711..449fb48 -- src` = exactly the 7 authorized files, 6 new + 1 modified: `A src/main/resources/db/migration/V131__create_expert_academic_enrichment_job.sql`, `A src/main/kotlin/com/weibo/talentintroduction/discovery/domain/ExpertAcademicEnrichmentJob.kt`, `A .../discovery/repository/ExpertAcademicEnrichmentJobRepository.kt`, `A .../discovery/service/ExpertAcademicEnrichmentJobService.kt`, `A src/test/kotlin/.../discovery/repository/ExpertAcademicEnrichmentJobRepositoryIT.kt`, `A src/test/kotlin/.../discovery/service/ExpertAcademicEnrichmentJobServiceTest.kt`, `M src/test/kotlin/.../campaign/repository/FlywayMigrationIntegrationTest.kt`; `git show --stat 449fb48` = the same 7 (1550 insertions / 17 deletions). The boundary's only other commit 65429bf is docs-only (c6 verify-log/fix-log/execution + ledger = the pre-execution HEAD). No applied migration edited: `git diff 1664711..449fb48 -- src/main/resources/db/migration` = only `A V131...`; the prior max was V130 (`ls src/main/resources/db/migration`), so V131 is the next version in release order with no collision, no out-of-order and no `${}` in the new file. DDL `V131__create_expert_academic_enrichment_job.sql:31-49` declares all 13 planned columns at the planned widths, `UNIQUE(expert_doc_id)` (`uk_expert_academic_enrichment_job_doc`), the queue index `(status, next_attempt_at, id)` (`idx_expert_academic_enrichment_job_due`) and a named CHECK restricting `status` to the six I-1 values; `DATETIME(3)`/column-level `utf8mb4_bin` follow the repo's V119/V130 convention and keep the declared column set/index/UNIQUE (no extra business column, no FK on `discovery_execution_id`, asserted in the Flyway test). |
| Plan and invariants | PASS | **I-1**: the V131 table is the only lifecycle store (no second queue/status table, no ES mapping/config file touched) and the three new main files contain no ES/HTTP symbol (`grep 'RestTemplate\|_update\|elasticsearch\|apiKey\|http'` over them -> 0 hits), so no root-level ES status field exists; `UNIQUE(expert_doc_id)` at V131:45 and the six statuses (ExpertAcademicEnrichmentJob.kt:48-63 + CHECK V131:47-48); idempotent merge = `INSERT ... ON DUPLICATE KEY UPDATE id = id` (Repository.kt:41-49) plus conditional reopen `status='UNMATCHED' OR (status='SUCCEEDED' AND updated_at <= now-30d)` (Repository.kt:61-76), `SUCCESS_FRESHNESS_DAYS=30` (Service.kt:205), composed in one tx by `enqueue` (Service.kt:41-48). Persistence-boundary evidence (IT): `enqueue merges repeated enqueues for one expert into a single pending row {I-1}` (3 enqueues -> 1 row, attempts 0, first `discovery_execution_id` kept), `enqueue keeps a success younger than thirty days and reopens a stale success {I-1}` (<30d stays SUCCEEDED; 40d reopens PENDING and is immediately claimable), `enqueue merges FAILED until an explicit manual reopen and reopens UNMATCHED {I-1, I-3}`. **I-2**: candidate query (Repository.kt:80-90) + per-row CAS `claimById` writing `lease_token`/`lease_until` and re-evaluating the predicate (Repository.kt:92-118; accepts due PENDING/RETRY_WAIT and RUNNING with expired lease; no `SKIP LOCKED`); `@Transactional` enqueue/claimDue/renewLease/complete (Service.kt:41,54,71,79); `LEASE_MINUTES=10` (Service.kt:202); per-batch `renewLease` (Service.kt:71-73, Repository.kt:120-133); token-matched completion (Service.kt:79-98 + Repository.kt:135-166); the service contains no external HTTP call at all, so no DB transaction spans a request. IT: `two concurrent transactions claim the same due row only once {I-2}` (2 threads, own connections/tx; exactly one wins, persisted token = winner's, still 1 row), `an expired lease is reclaimable and the previous token can neither renew nor complete {I-2}` (stale-token completion changes no column), `a lease becomes claimable exactly when it expires {I-2}`, `claimDue never claims more than the requested limit {I-2}`, `renewLease extends only the current holder's lease {I-2}`. **I-3**: `classify` (Service.kt:114-149) maps exactly the c6 variants - Success->SUCCEEDED (`last_error` null), Partial->RETRY_WAIT with attempts+1 and `LAYER_UPDATE_FAILED`/`RECENT_WORKS_FAILED` (never SUCCEEDED), Deferred->RETRY_WAIT with attempts unchanged and `next_attempt_at = ofInstant(resetAt, systemDefault)` (Service.kt:127-130), RetryableError(rateLimited)->RETRY_WAIT from `retryAfterMs` (min 1m), attempts unchanged (Service.kt:134-141,169-172), RetryableError->`transientFailure` ladder `BACKOFF_MINUTES = 1/5/30/120` and FAILED at `MAX_FAILURE_ATTEMPTS=5` (Service.kt:154-163,208-212), NotFound/NoId->UNMATCHED (Service.kt:147-152); manual reopen exists only for FAILED (Service.kt:101, Repository.kt:168-183). IT: `transient failures back off one five thirty and one hundred twenty minutes then fail {I-3}` (every rung asserted on the persisted row; 5th failure -> FAILED, not claimable 30d later), `rate limit and daily budget deferral wait on the reset without consuming attempts {I-3}` (attempts stay 3, not due 1s before reset, due exactly at reset; 429 with attempts=7 stays RETRY_WAIT), `a partially written enrichment waits for retry instead of reporting success {I-3}`, `NoId and NotFound persist as UNMATCHED and never as SUCCEEDED {I-3}`, `complete persists SUCCEEDED with the layer detail and clears the lease {I-1, I-2}`. V-1/V-2/V-3 are covered by those real-MySQL tests plus the two V131 migration tests below. |
| Required commands | PASS | Fresh runs, worktree root, JDK 11. (a) **Bare** required command (14:19->14:21, EXIT=1): `Tests run: 6, Failures: 0, Errors: 2` - `ExpertAcademicEnrichmentJobRepositoryIT` and `FlywayMigrationIntegrationTest` both die in `startMysql` with `IllegalStateException: Docker is required ...` after `o.t.d.DockerClientProviderStrategy - Could not find a valid Docker environment ... BadRequestException (Status 400: {"message":"client version 1.32 is too old. Minimum supported API version is 1.40"}...)`; only `ExpertAcademicEnrichmentJobServiceTest` ran (4/0/0/0). (b) **Base-commit reproduction** of that block: scratch worktree `/tmp/c7v-base` at the child base 1664711 (c7 absent), bare `mvn test -Dtest=FlywayMigrationIntegrationTest -DmigrationIt=true` -> EXIT=1, `Tests run: 1, Failures: 0, Errors: 1`, identical 1.32-vs-1.40 message and identical `IllegalStateException` at `FlywayMigrationIntegrationTest.startMysql` => the bare block is environmental and predates c7. (c) **Workaround** `DOCKER_HOST=unix:///Users/lukai/.orbstack/run/docker.sock ... -Dapi.version=1.40` (14:23->14:31, EXIT=1): `Tests run: 44, Failures: 0, Errors: 1`; `ExpertAcademicEnrichmentJobRepositoryIT` 14 run / 0 F / 0 E / 0 skipped (surefire XML `tests="14" errors="0" failures="0"`, 26.1 s, real `mysql:8.0.36` container, table built by the real V131 migration) and `ExpertAcademicEnrichmentJobServiceTest` 4 run / 0 F / 0 E, so the child's whole contract is green against real MySQL. `FlywayMigrationIntegrationTest` 26 run / 0 F / 1 E: the single error is the pre-existing `V124 allows material attached promotion audit trigger` (`SQLIntegrityConstraintViolationException ... CONSTRAINT fk_eap_contact FOREIGN KEY (expert_contact_id) REFERENCES expert_contact (id)`), while both V131 tests passed inside that class (`fresh database migrates through V131` 11.8 s; `V131 creates the enrichment job table on a V130 database leaving existing contacts untouched` 12.4 s, no failure/error element; log shows `Migrating ... to version "131 - create expert academic enrichment job"` -> `now at version v131`). (d) **Base-commit reproduction of that error**: the same workaround run at 1664711 -> EXIT=1, `Tests run: 25, Failures: 0, Errors: 1`, one and the same `fk_eap_contact` violation in `V124 allows material attached promotion audit trigger` => pre-existing, not caused by c7 (the method was introduced by 6ab8eb3, an ancestor of the child base, and at base is byte-identical to HEAD apart from the `130`->`131` literal). |
| Downstream interfaces | PASS | Plan names/shapes kept exactly: `fun enqueue(docId: String, source: String, executionId: Long?)` (Service.kt:42, Unit), `fun claimDue(limit: Int, now: LocalDateTime): List<ExpertAcademicEnrichmentJob>` (Service.kt:55), `fun complete(id: Long, leaseToken: String, outcome: ProfileEnrichmentOutcome): Boolean` (Service.kt:80); the two additive helpers `renewLease` (Service.kt:72) and `reopenFailed` (Service.kt:101) leave those three untouched. Persisted field set is exact and complete: `id, expertDocId, source, discoveryExecutionId, status, attempts, nextAttemptAt, leaseToken, leaseUntil, lastError, resultJson, createdAt, updatedAt` (ExpertAcademicEnrichmentJob.kt:29-44) = the 13 declared columns. The c6 mapping is exhaustive and compiler-forced (sealed `when` with no `else`, Service.kt:114-149) over `Success/Partial/Deferred/NotFound/NoId/RetryableError`, with `result_json` carrying the base/recent-titles/layer detail (Service.kt:178-196) and `last_error` holding reason codes only (no key/email/body symbol anywhere in the file). `discoveryExecutionId: Long? = null` (ExpertAcademicEnrichmentJob.kt:35) with no FK, asserted `IS_NULLABLE=YES` in the Flyway test. c8 readiness: a plain `@Service class ExpertAcademicEnrichmentJobService(repository: ExpertAcademicEnrichmentJobRepository)` (Service.kt:34-36) - injectable, no scheduler, no config switch, no in-memory queue, so c8 can wire the whole path behind its default-false flag. |

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1: pre-existing red inside the required command - `FlywayMigrationIntegrationTest.V124 allows material attached promotion audit trigger` (FlywayMigrationIntegrationTest.kt:603-614) inserts `expert_contact_id = 1` after `clean()+migrate()` with no seeded `expert_contact` row, so it hits `CONSTRAINT fk_eap_contact`; reproduced at base 1664711 with the identical command and error (25 run / 1 E), and the method is unchanged by this commit except its version literal, so it is neither introduced by c7 nor repairable inside c7's authorized file set (choosing new seed data is a behavioural decision the brief's stop conditions exclude). Recorded, not fixed; the consequence is limited to this environment-blocked command because the class is `migrationIt`-gated and skipped in the default suite.
- O-2: the schedule columns are naive `DATETIME(3)` and `Deferred.resetAt` is persisted as `LocalDateTime.ofInstant(resetAt, ZoneId.systemDefault())` (Service.kt:127-130, mirrored by the IT assertion), so the store's clock convention is the JVM default zone: c8 must call `claimDue`/`renewLease` with `LocalDateTime.now()` on that same zone - a UTC-based `now` would mis-schedule quota deferrals by the zone offset (about 8 h early on Asia/Shanghai, i.e. a retry before the real reset). Self-consistent within this child (write and read use the same clock), hence a note for c8 rather than a defect.

### Required Action
- COMPLETE_CHILD
