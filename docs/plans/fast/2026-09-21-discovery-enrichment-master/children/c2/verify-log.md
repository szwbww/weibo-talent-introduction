## Light Verification: LIGHT_PASS_WITH_NOTES
Child: c2 — docs/plans/2026-09-21/02-discovery-checkpoint.md
Boundary: 147dc953a194a27ea73b7934a8e2bc334beca655..468df56bf69b4b2f9afc7b4f38ad4791d8331240
Verifier: C2Verifier

### Four Gates
| Gate | Result | Evidence |
|---|---|---|
| Authorized scope | PASS | `git log --oneline --name-status 147dc95..468df56` shows commit 468df56 `feat(fast-p): implement c2` touching exactly the 8 authorized files (M ExpertDiscoveryService.kt, DiscoveryStats.kt, SourceStats.kt, DiscoveryResult.kt, ExpertDiscoveryServiceTest.kt; A DiscoveryCheckpointCodec.kt, DiscoveryCheckpointCodecTest.kt, DiscoveryResultTest.kt) and nothing else; the 6 added `docs/plans/fast/**` files (c1 brief/execution/fix-log/verify-log, ledger.md, c2/brief.md) come from the controller commit a8a7be6, not from c2; `git status --short src/` is empty, so the reviewed run is exactly commit state 468df56; no migration or application.yml change is in the boundary (V32 `discovery_source_cursor` untouched) |
| Plan and invariants | PASS | I-1/Task 1: page-boundary persist only after a fully consumed page with no RAW write failure (ExpertDiscoveryService.kt:578-585 paper, :829-830 ORCID), entering cursor re-persisted at run end (:636-637 / :848-849), and no failure/defer/limit branch can set `exhausted` (budget :502-508, SEARCH_FAILED :523-531, RAW_WRITE_INCOMPLETE/PAGE_PARTIAL :626-635, cancel :478-481 / :706-708). The base defect is real: at 147dc95 `lastNextCursor` is null-initialised (:352), returned (:479) and unconditionally saved (:282), so a first-request TLS failure wiped the stored cursor. V-1 tests: `first page TLS failure keeps the entering cursor` (:2026, stored C7 + `papersProcessedTotal=0` + `sourceFailures=1` + FAILED), `second page failure resumes at the second page entry and persists at the page boundary` (:2047, ≥2 saves, page-1 entry C2 persisted before the page-2 request, ends at C2), `partial batch does not advance cursor to nextCursor` (:1986), `partial batch due to sourceLimit does not advance cursor` (:2006), `budget deferred stop keeps the entering cursor and is not a search failure` (:2225), `page with failed RAW persistence keeps the entering cursor for replay` (:2250). V-2/I-1 empty page: :533-544 continues when `nextCursor != null`; `empty page with next cursor keeps paging` (:2075), `empty result without cursor is exhausted not a failure` (:2096). I-2/Task 2: DiscoveryCheckpointCodec.kt:54-60 `SOURCE:v2:<24 hex>` with `require(length <= 50)`, :65-76 canonical form covering all 9 non-cursor fields of PaperSearchCriteria.kt:3-12, :77-81 encode, :86-96 decode rejects every non-v2 shape; service side :95-113 treats legacy/undecodable rows as no-checkpoint and never rewrites them, and `papersProcessedTotal` is written only (:136,:144) with zero readers in src/main. Tests: codec 10 incl. `every production source name stays within the V32 column width`, `key ignores the transient cursor so pages of one query share a key`, `decode rejects legacy and malformed values so they are never adopted`; service `different keywords persist to different checkpoint keys` (:2113), `legacy plain source name rows stay untouched and are never adopted` (:2135), `v2 key holding a legacy raw cursor value is not adopted` (:2156). I-3/Task 2: one decision function (DiscoveryResult.kt:9-24) drives both the record (:41-47) and progress via `toProgressStatus` (ExpertDiscoveryService.kt:341-346, :375-383); `V-3 all attempted sources failing yields FAILED with non-zero source failures` (:2172), `V-3 one failed source plus one healthy source yields PARTIAL_SUCCESS` (:2192), `empty result without cursor is exhausted not a failure` (:2096), DiscoveryResultTest 7 cases. I-4: `recordTerminalSourceFailure` (:160-164) is called only on terminal paths (:515 CIRCUIT_BREAKER, :523/:527 SEARCH_FAILED, :726 ORCID) while 429/503 retries only merge RATE_LIMITED (:509-517), so retries never inflate the count; `taskFailureCount` adds `sourceFailures` (DiscoveryResult.kt:38-40, DiscoveryStats.kt:46-49); a 400/non-429/503 `HttpStatusCodeException` ends the source on the first request with no retry; cancellation exits at the next loop check. TLS/IO ≤ 2 retries sit in the pre-existing HTTP layer (`RetryingClientHttpRequestInterceptor` maxRetries=2 wired at RestTemplateConfig.kt:60,:77; `FetchRetry` for fulltext), so the page path adds no retry beyond the capped 429/503 breaker. The V-3 base defect (source failures absent from the record's failure count) is confirmed from the base bytes via `git show 147dc95:src/main/kotlin/com/weibo/talentintroduction/discovery/domain/DiscoveryResult.kt`. Cancellation keeps the entering cursor by the same code path (:478-481 / :636) but has no dedicated checkpoint test (existing cancel tests assert ES writes only) |
| Required commands | PASS | Fresh run in this worktree at 468df56 with the tree verified clean: `JAVA_HOME=/Library/Java/JavaVirtualMachines/zulu-11.jdk/Contents/Home mvn test -Dtest=ExpertDiscoveryServiceTest,DiscoveryCheckpointCodecTest,DiscoveryResultTest` → MVN_EXIT=0, BUILD SUCCESS; surefire reports: ExpertDiscoveryServiceTest 79, DiscoveryCheckpointCodecTest 10, DiscoveryResultTest 7 = Tests run 96, Failures 0, Errors 0, Skipped 0 (matches the implementer's claimed 96/96); the same command's exec-plugin pass reports `tests 1035 / suites 207 / pass 1035 / fail 0`, identical to the controller's recorded baseline (1035/1035, fail 0); 96 = 68 pre-existing tests kept (no test method removed: base/head `fun \`` name sets differ only by 11 additions in this class) + 11 new in this class + 17 in the two new classes; the full `mvn test` suite was deliberately not run (integration-stage gate per the child contract) |
| Downstream interfaces | PASS | `data class SourceRunOutcome(val resumeCursor: String?, val exhausted: Boolean, val stopReason: String)` verbatim and top-level (ExpertDiscoveryService.kt:1605-1609), returned by both runners (:655, :861), and every catch branch produces a non-exhausted outcome; `DiscoveryCheckpointCodec` is the only encoder of the key/envelope (`grep -rn 'QUERY_VERSION\|":v2:"' src/main/kotlin` hits only DiscoveryCheckpointCodec.kt plus one log reference at ExpertDiscoveryService.kt:109); every source name stays ≤ 50 (guarded at DiscoveryCheckpointCodec.kt:56, asserted for all seven names); `nonPersistableCursorSources` is now `emptySet()` with the explicit CORE seam comment (ExpertDiscoveryService.kt:89) so c4 can finish the offset envelope — per docs/investigations/2026-09-21-deep-discovery.md:65 production CORE responses contain no `scrollId`, so `nextCursor` stays null today and neither keeping nor removing CORE changes live coverage; fields are additive-only (SourceStats.kt:26-31, DiscoveryStats.kt:19-26, DiscoveryResult keeps its constructor and gains DiscoveryTerminalStatus), `taskFinalStatus` is now non-null and is consumed by TaskExecutionService.kt:135-143 exactly as designed, progress status `PARTIAL_SUCCESS` fits `task_progress_log.status VARCHAR(32)` (V22:6) and is already rendered by the UI (app.js:586,:1238,:1308; task-modal-runtime.js:41,:48,:235) so no frontend change was needed; entry points are untouched (`discover(criteria, triggeredBy, includeRawScan)` has no diff hunk; callers ExpertDiscoveryController.kt:93/:171 and ExpertDiscoveryScheduler.kt:52 unchanged and green); quota deferral is a preserving stop reason — `catch (e: OpenAlexBudgetDeferredException)` precedes the generic catches (:502), yields BUDGET_DEFERRED with the entering cursor kept, `exhausted=false` and no source failure (type extends IllegalStateException, OpenAlexRequestPolicy.kt:36-37), locked by the test at :2225 |

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1: because `TaskExecutionService` now adopts the always-non-null `DiscoveryResult.taskFinalStatus` (TaskExecutionService.kt:135-143) instead of deriving from counts, expert-level fault counters no longer influence the recorded terminal status: dedup lookups failing for every candidate leave `sourceFailureCount`/`pendingWork` at 0 (the run still pages to exhaustion) and `decide` returns SUCCESS (DiscoveryResult.kt:19-24), so record and progress show SUCCESS/COMPLETED with `failureCount>0, successCount=0` where the base derivation gave FAILED — the existing `dedup search error counts dedupErrors and skips` test (ExpertDiscoveryServiceTest.kt:496, source stubbed with `nextCursor=null`) exercises exactly that input. Outside the light gate: I-3's enumerated clauses and V-3 ("全源 SEARCH_FAILED → FAILED", "故障 failure_count 非 0") are all satisfied for source-level signals, and any repair (e.g. treating zero-success-with-failures as FAILED, or promoting dedup errors to source failures) is a new behavioral decision the plan does not uniquely determine, so it needs human sign-off rather than an AUTO_FIX; the same change also removes the base false-FAILED for legitimate filter-only empty results, which is intended

### Required Action
- COMPLETE_CHILD
