## Light Verification: LIGHT_PASS_WITH_NOTES
Child: c10 docs/plans/2026-09-21/10-fulltext-yield.md
Boundary: f81f71f30ee6a9749aeba3556a0527b8ccf05901..e12c3471f89a9bc333ef8e7777703214abe29d5e
Verifier: C10Verifier

### Four Gates
| Gate | Result | Evidence |
|---|---|---|
| Authorized scope | PASS | `git show --name-status e12c347` = exactly the 10 authorized files (6 main + 4 test), no new file, helper constants/types inside them; `git diff 9127427..e12c347 -- src/main/kotlin/config src/main/resources pom.xml` = empty (no mapping/config/migration/pom change); the range's only extra entry is docs-only commit 9127427 (c9 record) plus the untracked controller-owned ledger/report; commit subject is exactly `feat(fast-p): implement c10` |
| Plan and invariants | PASS | I-1: `OpenAlexDataSource.kt:85-146` (order PMC XML → `downloadUrl` → `candidateDownloadUrls` → Unpaywall once via `unpaywallConsulted`, `attemptedUrls.size < MAX_FULLTEXT_ADDRESSES`=3, one `deadline` shared), `publicFulltextUrl` rejects non-http(s); tests `the fallback chain stops after three addresses and shares one deadline (V-1, I-1)`, `a repeated url is attempted only once (V-1)`, `unpaywall is consulted only when the open locations yielded nothing (I-1)`, `an expired shared deadline prevents any download (I-1)`. I-2: `PdfEmailExtractor.kt:345-352` (`hasStrongEmailNameEvidence`/`verifiedAuthorFor`) untouched, every fallback attempt uses the same `paper.authors`; test `a fallback pdf never attaches an identity when the authors are ambiguous (V-1, I-2)` (real extractor, two same-name authors → orcidId/openAlexAuthorId all null). I-3: `EmailExtractionOutcome.kt:13-37` (nullable `fulltextObtained`, `downloadFailureCategory`, null→pre-change derivation), `PdfEmailExtractor.kt:135-150` (HTML-no-email = obtained + `NO_EMAIL_IN_HTML`), `:175-197` bucket classifier, `:302-313` vocabulary; tests `HTML without an email counts as content obtained and stays NO_EMAIL_IN_HTML (I-3)`, `classifies download failures into stable low-cardinality buckets (I-3, V-2)`, `NO_EMAIL_IN_HTML counts as content obtained and keeps its own reason (I-3)`. I-4: `PdfExtractionProperties` untouched (`maxPdfSizeBytes`=10_485_760, `maxPages`=2), all new fields defaulted, `findPdfUrl` = `findPdfUrls().firstOrNull()` (UnpaywallClient.kt:18); tests `an adapter without the declaration keeps the previous derivation (I-4, V-3)`, `rejects oversized PDF`, `findPdfUrl returns best_oa_location url_for_pdf`. V-1/V-2/V-3 all mapped |
| Required commands | PASS | Fresh run in this worktree: `JAVA_HOME=/Library/Java/JavaVirtualMachines/zulu-11.jdk/Contents/Home mvn test -Dtest=OpenAlexDataSourceTest,PdfEmailExtractorTest,ExpertDiscoveryServiceTest,UnpaywallClientTest` → exit code 0, `BUILD SUCCESS`, `Tests run: 210, Failures: 0, Errors: 0, Skipped: 0` (surefire: OpenAlexDataSourceTest 58, PdfEmailExtractorTest 27, ExpertDiscoveryServiceTest 119, UnpaywallClientTest 6); the same invocation's exec-maven JS suite reports `tests 1035, pass 1035, fail 0` = recorded baseline; full `mvn test` NOT run (integration-stage gate per brief) |
| Downstream interfaces | PASS | `PaperMetadata.candidateDownloadUrls: List<String> = emptyList()` (PaperMetadata.kt:18) — the five other sources' constructions are unchanged and the whole main+test source set compiles; `EmailExtractionOutcome` keeps its current fields, additions defaulted (lines 13/18), `resolvedFulltextObtained()` derives null→pre-change; per-paper accounting counts one paper once (`papersSearched++`/`fulltextAttempted++`/`fulltextObtained++` per paper at `ExpertDiscoveryService.kt:737` and `:1156-1180`, attempts carried as `apiRequests`) proven by `one paper with two download attempts counts one paper and two requests (V-1, I-3)` (papersSearched 1, fulltextAttempted 1, fulltextObtained 1, apiRequests 3) and `a dead primary address falls back to the next open pdf and counts one paper once (V-1, I-1, I-3)` (httpRequests 2); c1 O-1's dead counter now has a production writer at `OpenAlexDataSource.kt:161-163` → `OpenAlexRequestPolicy.recordResponse` (`creditsUsed >= FULLTEXT_DOWNLOAD_CREDITS` → `fulltextDownloadsToday`), tests `a metered fulltext download reports its quota headers to the shared policy (c1 O-1)` (1L) and `an external host without openalex quota headers leaves the shared policy untouched (c1 O-1)` (0L); categories are stable low-cardinality strings that reach the task details via `failureReasons` — test `download failure categories reach the per-source details summary (I-3)` asserts `bySource.EUROPE_PMC.failureReasons.HTTP_403 == 1` |

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1, I-1 "总计每篇最多3个全文地址" is implemented as at-most-3 **URLs**; the PMC XML step is an extra pre-chain fetch, so a PMC paper can make 1 XML + up to 3 URL attempts (OpenAlexDataSource.kt:86-87 and :119) — the plan's own sketch (`take(3)` over the URL list, XML handled before the loop) supports this reading, so it is not an AUTO_FIX, but the literal invariant wording leaves room for the human to read the cap as covering all fulltext fetches.
- O-2, the brief's `downloadAttempts += 2` has no own `details_json` key: attempts are folded into the existing `apiRequests` counter, which also carries the page-search request and the Unpaywall lookup (`OpenAlexDataSource.kt:129-131`, `ExpertDiscoveryService.kt:1157-1159`; the test asserts apiRequests 3 = 1 search + 2 attempts). `SourceStats.kt` is outside this child's 10-file authorization, so a distinct key cannot be added inside this child — outside the light gate, flagged for the human/integration stage.
- O-3, the plan Task checklists' "write the failing test first and run it red" was not performed (execution.md Deviations): the new tests reference fields/parameters that do not exist at base, so they cannot compile against pre-change code; the gaps they pin are the ones the plan's own 现状审计 records and all acceptance evidence is green on the frozen head.
- O-4, the shared 90-second deadline bounds the URL download attempts only: the PMC XML fetch (`OpenAlexDataSource.kt:100`; `EuropePmcDataSource` is not an authorized file) and the Unpaywall lookup (`:129-131`, no deadline argument) run outside it, bounded by their own HTTP timeouts; matches the plan sketch, recorded so I-1's "total per-paper deadline" is not read as covering every fetch.

### Required Action
- COMPLETE_CHILD
