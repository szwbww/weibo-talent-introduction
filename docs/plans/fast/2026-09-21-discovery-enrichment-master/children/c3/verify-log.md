## Light Verification: LIGHT_PASS_WITH_NOTES
Child: c3 — docs/plans/2026-09-21/03-crossref-arxiv.md
Boundary: 468df56bf69b4b2f9afc7b4f38ad4791d8331240..fba6173efd061aef73ebbce1854f83decb52a84f
Verifier: C3Verifier

### Four Gates
| Gate | Result | Evidence |
|---|---|---|
| Authorized scope | PASS | Every non-docs path in the boundary is one of the brief's 8 authorized files, and they arrive in the exact 8 file set: `git diff --name-only 468df56..fba6173 \| grep -v '^docs/plans/fast/'` → ArxivProperties.kt, SubjectScopeCatalog.kt, ArxivDataSource.kt, CrossrefDataSource.kt, application.yml, SubjectScopeCatalogTest.kt, ArxivDataSourceTest.kt, CrossrefDataSourceTest.kt (8, no more, no less); `git show --name-only fba6173` = those same 8 (+491/-107) and 0 hits for `docs/plans/fast`; the other 12 boundary files are `docs/plans/fast/**` (c2/c3 logs + c4..c10 briefs + ledger) introduced by the controller commit 69bdb7d `docs(fast-p): record c2 light verification`, not by c3. No migration, no new file, no new dependency; `git status --porcelain` in the worktree shows only ` M docs/plans/.../ledger.md` and untracked `children/c3/execution.md` (both plan artifacts; brief forbids `git add docs/plans/fast/**`) |
| Plan and invariants | PASS | I-1 Crossref: one encode point per value (`CrossrefDataSource.kt:63-70`, `encodeComponent` at `:84`) and the final `URI` object handed to `RestTemplate` (`:74` `getForObject(uri, JsonNode::class.java)`, never the String overload); base defect is real, not invented — at 468df56 the class pre-encoded `filter`/`query`/`mailto` into a query string and passed it to `getForObject(String)`, which re-encodes via the template handler. Boundary test `searchPapers encodes each component exactly once at the request boundary` (`CrossrefDataSourceTest.kt:72-90`, real `RestTemplate` + `MockRestServiceServer`, asserts the decoded `ClientHttpRequest.uri.rawQuery`) proves filter=`from-pub-date:2020-01-01,until-pub-date:2026-12-31,has-full-text:true`, cursor `MTA1JTJGMTA1Ny0w+AbC/def==` round-trips, `材料 科学` decodes to itself, and the double-encoding fingerprints `%253A`/`%252C`/`%25E6` are absent. I-2 arXiv: HTTPS default in both places (`ArxivProperties.kt:11`, `application.yml:224`) plus legacy-`http` normalization (`ArxivDataSource.kt:33`), and every failure mode is explicit — 3xx/blank (`:69-71` `ARXIV_EMPTY_OR_REDIRECT`), blank body in the parser (`:88`), parse error (`:95-97` `ARXIV_INVALID_XML`), non-Atom root (`:100-102` `ARXIV_NON_ATOM_PAYLOAD`), Atom error entry (`:107-112` `ARXIV_ERROR_ENTRY`); base returned `PaperSearchResult(emptyList(), null, 0)` for blank/error payloads, so the defect is real; tests `ArxivDataSourceTest.kt:74-87` (HTTPS for default *and* legacy http, asserted on the emitted URI), `:158-171` (301 empty), `:173-180` (blank 200), `:182-188` (non-Atom), `:190-196` (malformed), `:204-212` (error entry). I-3: arXiv keyword/category/`all:*` branches byte-identical to 468df56 (`ArxivDataSource.kt:40-50` vs base `:33-42`) with tests `:126-131`, `:118-124`, `:133-139`, `:141-146`, `:150-156`; Crossref operator keywords win (`:43-44`, test `searchPapers lets the operator keyword win over the catalogue topic queries`) while the catalogue supplies R&D topics when no keyword is given (`:46-48`, test `searchPapers uses the catalogue topic queries when no keyword is given`), and null/unknown scope adds no `query` param (test `searchPapers keeps the pre-change filter-only query when scope is unknown`); catalogue literal anchored by `SubjectScopeCatalogTest.crossrefQueries RND_TARGET is the locked topic list` against `SubjectScopeCatalog.kt:75-78`. Acceptance V-1 and V-2 are exactly the boundary tests above and all are green in my run |
| Required commands | PASS | Fresh run in this worktree at fba6173 (tree state = commit + plan artifacts only, no product-code edits by me): `JAVA_HOME=/Library/Java/JavaVirtualMachines/zulu-11.jdk/Contents/Home mvn test -Dtest=CrossrefDataSourceTest,ArxivDataSourceTest,SubjectScopeCatalogTest` → `MVN_EXIT=0`, `BUILD SUCCESS`, Total time 01:28; surefire reports written 12:00 — CrossrefDataSourceTest 12, ArxivDataSourceTest 18, SubjectScopeCatalogTest 5 = **Tests run: 35, Failures: 0, Errors: 0, Skipped: 0**; the same `mvn test` JS stage (exec-maven-plugin) reports `tests 1035 / suites 207 / pass 1035 / fail 0`, identical to the controller's recorded baseline (1035 pass / 0 fail). The implementer's RED claim (35 tests, 9 failures, 5 errors) was not replayed (it needs a revert); `plan_identity.py`-style freshness is out of my scope, and the base-code defect it describes was instead confirmed directly from 468df56 source (see gate 2) |
| Downstream interfaces | PASS | `SubjectScopeCatalog.crossrefQueries(scope)` is additive and remains the single catalogue source (`SubjectScopeCatalog.kt:49-51,75-78`; sole consumer `CrossrefDataSource.kt:46`; no shard/rotation state in either adapter, so c4 can extend the same object). Public surfaces unchanged: CrossrefDataSource constructor (restTemplate, properties, unpaywallClient, pdfEmailExtractor) and `searchPapers`/`extractAuthorEmails`/`sourceName`/`emailExtractionMethod`/`maxPapersPerSource`; ArxivDataSource constructor and public `parseAtomResponse(xml: String?, criteria: PaperSearchCriteria)` (only internal call site `ArxivDataSource.kt:73`; `grep -rn 'parseAtomResponse\|searchPapers' src/main` shows the only external consumer is the interface call `ExpertDiscoveryService.kt:501`); `PaperSearchResult` is not in the boundary diff. c2 contract: a year-filtered empty arXiv page still yields `nextCursor` from the **raw** entry count (`ArxivDataSource.kt:121` `rawEntryCount`, `:167-169` `start + rawEntryCount < totalResults`), test `parseAtomResponse keeps the nextCursor of a year-filtered page` (papers empty, `nextCursor == "2"`, `totalResults == 200`) and the distinguishability case `parseAtomResponse filters by publication year` (`nextCursor == null` only at true page end); the c2 loop consumes it at `ExpertDiscoveryService.kt:533-544` (`EMPTY_PAGE` + `continue`), while the new `IllegalStateException`s land in `catch (e: Exception)` at `ExpertDiscoveryService.kt:526-531` → `SEARCH_FAILED` with `resumeCursor` preserved (not exhaustion). `excludedSources`/EuropePMC/PMC OA exclusion untouched; no OpenAlex bearer token path touched (c1 contract) |

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1: the arXiv request URL is still a pre-encoded `String` handed to RestTemplate's template overload (`ArxivDataSource.kt:41` `URLEncoder.encode(it)` into `:53-62` `exchange(url, ...)`), so for any non-ASCII keyword the wire carries a second encoding: a jshell probe of RestTemplate's default handler (`DefaultUriBuilderFactory.expand` of the exact template) yields `search_query=all:%2522%25E6%259D%2590%25E6%2596%2599%2522`, and live arXiv distinguishes the two forms (`all:%22K%C3%A4hler%22` → 6341 results vs `all:%2522K%25C3%25A4hler%2522` → 0, curl 2026-09-21). This is byte-identical at 468df56, I-1 is scoped to Crossref only, and I-3 requires arXiv's query string to stay verbatim for this child, so it is outside the light gate — but it is a real silent-zero-result risk for operator keywords with non-ASCII characters and is worth naming at human acceptance A-2 and to c4.
- O-2: `src/test/resources/application.yml:120` still defaults `arxiv.base-url` to `${ARXIV_BASE_URL:http://export.arxiv.org/api}` while production `application.yml:224` and `ArxivProperties.kt:11` are https; that file is outside the brief's 8-file list, so it cannot be repaired under this child, and I-2 stays satisfied because `ArxivDataSource.kt:33` normalizes `^http://` at construction. No test asserts the yml default (`grep` for `base-url` in `src/test` shows only explicit `ArxivProperties(...)` construction at `ArxivDataSourceTest.kt:79,163`), so nothing is red — recorded for the controller to sync in the closing commit.
- O-3: Crossref encoding departs from the literal Task-1 snippet in the approved plan — `CrossrefDataSource.kt:63-70,84` encode each value once with `URLEncoder` and declare it encoded via `build(true)`, instead of `.build().encode().toUri()`. Spring's `encode()` leaves a literal `+` in a query value while form decoding maps `+` to space, so the snippet cannot satisfy V-1's cursor `+/=` requirement; the deviation satisfies I-1 and is already documented in `children/c3/execution.md` (Deviations), so it is not a gate failure — recorded because it changes an artifact the approved plan states literally.

### Required Action
- COMPLETE_CHILD
