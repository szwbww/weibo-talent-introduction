## Light Verification: LIGHT_PASS_WITH_NOTES
Child: c9 — docs/plans/2026-09-21/09-discovery-throughput.md (brief: docs/plans/fast/2026-09-21-discovery-enrichment-master/children/c9/brief.md)
Boundary: 8873dc96cd2799a49ee1bd055d5366c9e3a78ae0..f81f71f30ee6a9749aeba3556a0527b8ccf05901
Verifier: C9Verifier

### Four Gates
| Gate | Result | Evidence |
|---|---|---|
| Authorized scope | PASS | Implementation commit f81f71f touches exactly the authorized paths and nothing else: `docs/plans/2026-09-21/discovery-enrichment-rollout.md` (new runbook, deliberately outside `docs/plans/fast/`), `config/ExpertDiscoveryProperties.kt`, `config/OpenAlexProperties.kt`, `discovery/domain/SourceStats.kt`, `discovery/service/ExpertDiscoveryService.kt`, `src/main/resources/application.yml`, `test/.../ExpertDiscoveryServiceTest.kt` (7 files; the 8th authorized file `ExpertDiscoverySchedulerTest.kt` is unmodified — 8 `@Test` at base and head). The boundary's other 4 paths (`children/c8/execution.md`, `c8/fix-log.md`, `c8/verify-log.md`, `fast/.../ledger.md`) belong to e946fee, the controller's c8 verification-record commit; f81f71f stages no `docs/plans/fast/**`. No migration/SQL file, no `src/main/resources/static/**`, no property class outside the list. Runbook key hygiene: grep for `api-key|apiKey|API_KEY|password|secret|token|Bearer|ZoMR|sk-` over `discovery-enrichment-rollout.md` returns only env-var *names* (`OPENALEX_API_KEY` at :4, :56) — no credential value. |
| Plan and invariants | PASS | I-1: run target decoupled from page/batch — `ExpertDiscoveryProperties.kt:17` (15000), `:19` (20000), `:21` (fetchConcurrency 4), `:26` (`timeBudget` 4h); `OpenAlexProperties.kt:17` (10000); `application.yml:182/206/226/237/262` (15000 / OpenAlex 10000 / Crossref 1000 / arXiv 2000 / CORE 1000); `validateRunQuota` (`ExpertDiscoveryService.kt:478-506`, called at `:330` before any request) rejects non-positive global/author/time budget and `globalCap < Σ min(pageSize, cap)` with a knob-naming `IllegalArgumentException` (→ 400 via GlobalExceptionHandler.kt:18). Tests: `全局上限不足各来源基础份额时启动校验失败且不发任何请求（I-1）` (0 requests, 0 checkpoint writes), `0 的配置不会被当成无限量（I-1）`, `公平额度：全局上限恰好覆盖各来源一页时每源各发一次请求（I-1 I-2）` (4×100=400 papers ≠ 100 page, 1 request each), `本源上限为 0 的来源不发请求也不计论文（I-1）`. I-2: `allocateSourceQuota` (`:514-527`) = `min(cap, globalRemaining − Σ_later min(page, cap))`, called at `:376`; the reservation is subtracted from every earlier source's quota, so a later source's one-page share can never be consumed by OpenAlex, and an exhausted/dead source's remainder flows forward because `globalRemaining` is recomputed from real `papersSearched` (`papersUsedForGlobalCap`, `:535-536`); ORCID is a separate unit/limit (`SourceStats.kt:8/42/44`; `:871-873` sets `unit=RECORD`, `runBudget=orcid.maxRecordsPerRun`=1000, yml:255) still bounded by `maxAuthorsPerRun`. Tests: `穷尽来源释放的剩余额度由后来源在各自上限内用尽（I-2）` (EPMC 40 → OpenAlex runBudget 160 → CROSSREF/ARXIV 100, total exactly 400, ARXIV `GLOBAL_PAPER_LIMIT`), `手动只选 arXiv 时全额使用自身额度且不碰 OpenAlex（I-2 V-2）` (`bySource` == {ARXIV}, OpenAlex 0 requests), `ORCID 记录与论文数在汇总里分列且限额独立（I-2）` (runBudget 1000 ≠ 400, `unit=RECORD`, `论文 2`/`ORCID 记录 3`). I-3: run deadline at `:332`, `timeBudgetReached` at `:539`, checked before every HTTP request (`:630` paper, `:908` ORCID) and per consumed item (`:719`, `:951`); `TIME_BUDGET` const `:2372` named in both loops (`:802-807`, `:1062-1072`); checkpoint preserved by the `!timeBudgetExpired` guards (`:753`, `:1051`) plus the run-end `persistCheckpoint(resumeCursor, stopReason, exhausted)` (`:818`, `:1081`) — never `exhausted`; no duplicate-rate/skip-unknown-page branch exists in the service (grep `重复率|duplicateRate|skipPage|跳过` finds only unrelated comments); cron unchanged `application.yml:178` (`0 0 2 * * ?`), UTC-day quota path untouched. Test: `时间预算到点停在进入页并记录 TIME_BUDGET（I-3）` (2 requests only, checkpoint stays `C2` not `C3`, `pendingWork=true`). I-4: runbook §3 (small end-to-end then 2500→5000→10000 with the per-stage knob values), §4 metric table (unique new experts / duplicates / enrichment 成功-待补-未匹配-失败 / elapsed / per-source `papersSearched`+`runBudget` / stopReason distribution), §5 rollback switches, §6 cursor+rescan rules, §7 UTC reset / 02:00 Asia/Shanghai; the numeric cells are reserved for the operator per plan A-2 (measurement needs a test-environment run and cannot exist pre-deployment). |
| Required commands | PASS | Fresh run in this worktree at f81f71f, `JAVA_HOME=/Library/Java/JavaVirtualMachines/zulu-11.jdk/Contents/Home mvn test -Dtest=ExpertDiscoveryServiceTest,ExpertDiscoverySchedulerTest` → `EXIT_PIPE=0`, `BUILD SUCCESS`; surefire: ExpertDiscoveryServiceTest `Tests run: 114, Failures: 0, Errors: 0, Skipped: 0`, ExpertDiscoverySchedulerTest `Tests run: 8, Failures: 0, Errors: 0, Skipped: 0` = 122/0/0 (matches the implementer's claim; base @Test counts 106+8=114 → +8 new, none deleted). The JS suite bound to the `test` phase ran inside the same invocation: 1035 pass / 0 fail, identical to the controller's recorded baseline. Config-binding sanity check: `mvn test -Dtest=ExpertDiscoveryControllerMvcTest` → exit 0, `Tests run: 3, Failures: 0` — the slice declares `@EnableConfigurationProperties(ExpertDiscoveryProperties::class)` and loads `application.yml`, so the new `time-budget: 4h` → `Duration` and `max-papers-per-run: 15000` bind with no conversion/context failure. Full `mvn test` intentionally not run (integration-stage gate). |
| Downstream interfaces | PASS | Stop-reason vocabulary stays explicit and distinct, each constraint naming itself: `BUDGET_DEFERRED` (API budget, `:2345`), `SOURCE_LIMIT` (`:2350`), `GLOBAL_PAPER_LIMIT` (`:2348`) / `GLOBAL_AUTHOR_LIMIT` (`:2349`), new `TIME_BUDGET` (`:2372`), `CANCELLED` (`:2347`), `WINDOW_LIMIT` (`:2366`) — no existing literal renamed (c1/c2/c4/c10 consumers unaffected; nothing outside the authorized test file asserts `PAGE_PARTIAL` for the quota path), and every value reaches the task record through `details.bySource[*].stopReason` (`:254`) plus the per-source progress message (`:785`). `SourceStats` reporting stays additive (`runBudget`, `unit` appended to the existing details JSON, `SourceStats.kt:42/44`) with no new column and no schema/migration change; `papersSearched` keeps its ORCID-=-records semantics, and paper vs ORCID counts are separated only in the summary text (`processedCountsSegment`, `:280`; text is byte-identical when no ORCID records exist). New knob `time-budget` lives in `ExpertDiscoveryProperties` (`:26`) with the yml env override `application.yml:187`; the OpenAlex/Crossref/CORE/arXiv caps are yml env defaults (`:206/226/237/262`) because those Properties classes are outside the authorized list (documented deviation, same pattern as c3) — all four remain env-overridable in production. `fetchConcurrency=4` unchanged (`ExpertDiscoveryProperties.kt:21`, `application.yml:185`); no frontend file and no applied migration touched. |

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1: `src/main/resources/application.yml:248` (`PMC_OA_API_KEY:6c4864…`) and `:259` (`CORE_API_KEY:ZoMR12…`) still hold literal API-key *values* as placeholder defaults, and the c9 hunk edits the same CORE/PMC-OA blocks. Byte-identical at base 8873dc9, so this is pre-existing and outside this child's authorized change; gate 1's "no credential" property is verified for the runbook itself, not for application.yml, and master M-5 does not authorize c9 to rotate these defaults.

### Required Action
- COMPLETE_CHILD

