## Light Verification: LIGHT_PASS_WITH_NOTES
Child: c4 — docs/plans/2026-09-21/04-core-orcid-scope.md
Boundary: fba6173efd061aef73ebbce1854f83decb52a84f..985f1ddf5891bdf534fbfeb2e5140ce6fd3f254b
Verifier: C4Verifier

### Four Gates
| Gate | Result | Evidence |
|---|---|---|
| Authorized scope | PASS | `git show --name-only 985f1dd` = exactly the brief's 10 authorized files (CoreDataSource.kt, OrcidDataSource.kt, CrossrefDataSource.kt, ExpertDiscoveryService.kt, SubjectScopeCatalog.kt + the 5 named tests; +893/-165) and 0 hits for `docs/plans/fast`. The other 4 paths in the boundary are `docs/plans/fast/**` (c3 execution/fix/verify logs + ledger) introduced by the controller's docs commit 915e542 `docs(fast-p): record c3 light verification` (109 insertions, 0 product code), not by c4. The c4 commit adds no file, no migration, no `application.yml`/config class, no dependency — helper types `CoreCursor`/`CoreSearchPage` (CoreDataSource.kt:49,77), `OrcidSearchPage`/`OrcidCursor` (OrcidDataSource.kt:46,53) sit inside the authorized files as the brief requires. Working tree carries no product-code drift (`git status --porcelain` = only `docs/plans/fast/.../ledger.md` modified + `children/c4/execution.md` untracked), HEAD = 985f1dd = boundary head. |
| Plan and invariants | PASS | **I-1**: cursor is `topic\|year\|offset`, no scroll — `CoreDataSource.kt:49-70` (`CoreCursor.parse` rejects anything that is not 3 integer fields, so a legacy `scroll:…` value is treated as "no cursor" and restarts at shard 0), `:91-121` POST `/search/works` body `{q,limit,offset}` (`:96-100`), advance = `shard.offset + parsed.rawCount` (`:110`), page-shard end = `rawCount < pageSize` (`:111`), rotation `nextShard` (`:149-156`); `grep` over CoreDataSource.kt finds no `scroll`/`scrollId` in any request. Request-boundary proof (records real URL+body): `CoreDataSourceTest` `pages by offset and advances by the raw returned count` (asserts no scroll param, offset 0, limit, `nextCursor=0\|2020\|2`), `resumes from the persisted shard cursor instead of restarting at page one` (offset=2 entered, 4 out), `rotates to the next yearly shard on the real last page`, `ends the source after the last yearly shard`, `keeps a legacy scroll cursor out of the paging state`. Cross-run persistence: `ExpertDiscoveryServiceTest` `CORE resumes from the persisted shard offset instead of restarting at page one` (stored v2 envelope `0\|2020\|100` → first request offset=100 → saved cursor `0\|2020\|200`, state ACTIVE) and `CORE query shards per year use independent checkpoints` (two keys, requests yearPublished=2020 then 2021). Advance only after a fully consumed page: `ExpertDiscoveryService.kt:591-598`. **I-2**: wrapper `OrcidSearchPage(records,nextCursor,rawCount)` (`OrcidDataSource.kt:46`, built at `:83-121`: `rawCount` = raw `expanded-result` size `:107`, advance `:108-116`, window `:111`); the loop moves the source cursor instead of counting email-filtered records (`ExpertDiscoveryService.kt:740-762`, `:852-861`); tests `OrcidDataSourceTest` `advances by the raw count when a whole page has no public email` (100 no-email records → `rawCount=100`, `nextCursor=0\|100`), `ExpertDiscoveryServiceTest` `ORCID pages past a whole page without public emails and still acquires the next page expert` (requests start=0 then 100, 1 expert indexed, then shards `keyword:"materials"…"physics"` each at start=0, checkpoint EXHAUSTED). Scope topic seeds when keyword empty, operator keyword wins, and explicit skip without a request: `SubjectScopeCatalog.kt:77-86`, `OrcidDataSource.kt:85-89`, `:137-147`; tests `OrcidDataSourceTest` `lets operator keywords win over the scope seeds` (`"machine learning" AND "robotics"`, no `keyword:`), `skips without a request when no keyword and no scope seed exist`, `ExpertDiscoveryServiceTest` `ORCID skips without a request when no keyword and no scope seed exist` (`Mockito.never()` on `getForObject`, 0 papers, 0 source failures). **I-3**: the catalogue keeps the six classes as the single SSOT (`SubjectScopeCatalog.kt:39-56`; API additive only — see gate 4), locked verbatim by `SubjectScopeCatalogTest` `coreKeywords RND_TARGET is the locked six R&D topics` / `orcidSeedKeywords RND_TARGET is the locked six R&D topics` plus the branch-coverage loop; CORE emits explicit parenthesised OR (`CoreDataSource.kt:130-136`, literal string asserted in `CoreDataSourceTest` `pages by offset…` and `ExpertDiscoveryServiceTest` `CORE sends the catalogue topics with explicit parentheses and keeps operator keywords`); ORCID seeds query the public `keyword` field (`OrcidDataSource.kt:73`, `:140-147`) and `parseOrcidRecords` still sets `country = null`, so no company/affiliation value becomes nationality; Crossref seeds go to `query.bibliographic` with the operator keyword untouched (`CrossrefDataSource.kt:44-52`, `:65-66`; `CrossrefDataSourceTest.kt:92-110`) — see O-1 for the one literal sub-clause without evidence; EuropePMC/PMC_OA stay excluded (`SubjectScopeCatalog.kt:97-98`, untouched by the diff; c3's exclusion tests unchanged). **I-4**: `CoreDataSource.MAX_OFFSET = 9000` (`:84`) with window stop + `uncoveredTail` + shard rotation (`:112-121`), `OrcidDataSource.MAX_OFFSET = 9999` (`:80`, `:111-120`); `DiscoveryStopReason.WINDOW_LIMIT` (`ExpertDiscoveryService.kt:1662`) is recorded into the source's `failureReasons` (`:543`, surfaced by `buildProgressDetails` `:222`) and never conflated with `SEARCH_FAILED`/`EXHAUSTED`; tests `CoreDataSourceTest` `stops the shard at the vendor window and reports the uncovered tail` (exactly 1 request, offset=9000, tail 190900, next shard `0\|2021\|0`), `ExpertDiscoveryServiceTest` `CORE records WINDOW_LIMIT and rotates the shard at the vendor offset window` (saved `0\|2021\|0` ACTIVE, `failureReasons[WINDOW_LIMIT]=1`, `stopReason=SOURCE_LIMIT`, `pendingWork=true`), `OrcidDataSourceTest` `stops the shard at the public search window` (→ `1\|0`, never offset 10000). Offsets are bounded: the highest offset ever requested is 9000 (CORE) / 9900 (ORCID), both inside their window. Timeout/cancel/quota handling is retained per request — `CoreDataSource.kt:96-104`, `OrcidDataSource.kt:93-105` (throw preserved), loop-head cancel checks + 429/503 circuit breaker + `OpenAlexBudgetDeferredException` deferral at `ExpertDiscoveryService.kt:470-474`, `:509-530`, `:720-724`. **V-1/V-2/V-3**: each acceptance item has request-boundary or request-URL assertions — V-1 `CORE resumes…` / `…independent checkpoints` / `stops the shard at the vendor window…`; V-2 `ORCID pages past a whole page without public emails…` + `ORCID skips without a request…`; V-3 `CORE sends the catalogue topics…`, `CrossrefDataSourceTest` `uses the catalogue topic seeds as query bibliographic when no keyword is given` + `lets the operator keyword win over the catalogue topic seeds`, and unchanged EuropePMC/PMC_OA exclusion coverage. |
| Required commands | PASS | Fresh run of the brief's exact command in this worktree at HEAD 985f1dd with no product-code edit by me: `JAVA_HOME=/Library/Java/JavaVirtualMachines/zulu-11.jdk/Contents/Home mvn test -Dtest=CoreDataSourceTest,OrcidDataSourceTest,CrossrefDataSourceTest,SubjectScopeCatalogTest,ExpertDiscoveryServiceTest` → `BUILD SUCCESS`, `Tests run: 126, Failures: 0, Errors: 0, Skipped: 0` (ExpertDiscoveryServiceTest 85, CoreDataSourceTest 13, CrossrefDataSourceTest 12, OrcidDataSourceTest 9, SubjectScopeCatalogTest 7; surefire reports timestamped 12:24); an immediate offline rerun with a captured exit code gave `MVN_EXIT=0`, `BUILD SUCCESS`, `Tests run: 126, Failures: 0, Errors: 0, Skipped: 0` — identical to the implementer's recorded 126/0/0/0. The same `mvn test` JS stage (exec-maven-plugin) reports `tests 1035 / suites 207 / pass 1035 / fail 0`, identical to the controller's recorded baseline (1035 pass / 0 fail). The recorded full-suite baseline (3504 tests, 13 skipped) is an integration-stage gate the brief forbids running here, so no full-suite delta is claimed; the directed subset has no separate recorded baseline and matched the implementer's counts exactly. |
| Downstream interfaces | PASS | (a) c2 codec reused, no parallel encoding, no new column: both CORE and ORCID cursors go through `persistSourceCheckpoint` → `DiscoveryCheckpointCodec.encode` (`ExpertDiscoveryService.kt:127-129`) and `loadSourceCheckpoint` → `decode` (`:95-112`); `decode` splits with `limit = 3` (`DiscoveryCheckpointCodec.kt:88`) so the embedded `\|` of `0\|2020\|100` / `0\|100` round-trips — exercised by the test helper `decodedCheckpoint` (`ExpertDiscoveryServiceTest.kt:240-243`) inside the new CORE/ORCID service tests; the c4 commit contains no migration/repository/entity change, so `discovery_source_cursor` stays at V32 (`cursor_value TEXT`). (b) CORE is out of `nonPersistableCursorSources` (`ExpertDiscoveryService.kt:89` = `emptySet()`, with a doc comment naming CORE), so `:96`/`:127` no longer short-circuit CORE persistence — the end state the plan requires; c4 itself needed no edit there, matching the implementer's recorded deviation. (c) The stop reason reaches the shared surfaces explicitly: `DiscoveryStopReason.WINDOW_LIMIT` (`:1662`) → source `failureReasons` (`:543`) → shared progress payload (`:222`), while `pendingWork`/`stopReason` (`:651-653`, `:880-882`) feed `DiscoveryTerminalStatus.decide` (`DiscoveryResult.kt:19-25`, unchanged by c4). (d) `SubjectScopeCatalog` stays additive for c9: only `orcidSeedKeywords` + two private seed lists were added; `openAlexFilterParts`/`arxivCategories`/`coreKeywords`/`crossrefQueries`/`excludedSources` signatures and values are byte-identical (`SubjectScopeCatalog.kt:60-100`) and the branch-coverage loop now also covers the new function (`SubjectScopeCatalogTest.kt:77-84`); the sole consumer of `crossrefQueries` remains `CrossrefDataSource.kt:46`. (e) `PaperSearchResult` semantics unchanged: `CoreDataSource.kt:121` still returns `(papers, nextCursor, totalResults)`, with `nextCursor` now the shard cursor and never a vendor `searchId`; `OrcidDataSource.OrcidRecord`'s field set and `orcidRecordToAuthorEmails` are untouched, and the record-only view `searchOrcidRecords` (`OrcidDataSource.kt:130`) is kept for `tryGetEmailFromOrcid` (`ExpertDiscoveryService.kt:1504`, which passes `keywords = ["orcid:<id>"]` and therefore keeps the pre-change single-shard quoted-AND query) and for the out-of-list Java helper `DiscoveryMockHelper.java:180-184`. |

### AUTO_FIX
- N/A

### RECORD_ONLY
- O-1: the Crossref clause of I-3 is unmet in its literal form and has no code or test evidence. Plan 04 I-3 states "Crossref 针对目录主题轮换检索并保留来源/主题审计", `docs/plans/2026-09-21/03-crossref-arxiv.md:32` defers "04 再统一分片轮换", and the brief's I-3 states "Crossref rotates over catalogue topics and keeps per-source/topic audit"; the implementation instead joins all six catalogue topics into one `query.bibliographic` value and streams a single vendor-cursor page sequence (`CrossrefDataSource.kt:44-52`; `grep -rn crossrefQueries src/` → sole consumer `CrossrefDataSource.kt:46`), with no per-topic request rotation and no topic audit surface (the class logs only errors, `:79`/`:151`; the progress details expose no resolved seed). Plan 04's Task-2 line and acceptance V-3 determine only "catalogue topic seeds via query.bibliographic + operator keyword not overridden", which is implemented and asserted at the request boundary (`CrossrefDataSourceTest.kt:92-110`). No uniquely determined repair exists in the approved bytes (a rotation would need new Crossref cursor/checkpoint encoding and would rewrite c3's verified request contract), so this is recorded instead of raised as AUTO_FIX; the controller should reconcile the 03/04 wording before c9's per-source quotas depend on it.
- O-2: CORE operator keywords keep AND semantics while the plan's Task-1 snippet shows `terms.joinToString(" OR ")`: `CoreDataSource.kt:131-136` applies OR only to the catalogue topic group and `AND` to `criteria.keywords` (asserted by `CoreDataSourceTest` `lets operator keywords win and keeps their AND semantics` → `(graph neural network AND recommendation) AND yearPublished=2020` and `ExpertDiscoveryServiceTest` → `(perovskite solar cell) AND yearPublished=2020`). V-3's "人工指定关键词不被覆盖" and M-2's manual-entry regression favour the pre-change reading, and the deviation is recorded in `children/c4/execution.md` (Deviations), so it is a plan-snippet deviation rather than a gate failure.
- O-3: ORCID now has two entry points — `searchOrcidPage` (paging) and `searchOrcidRecords` (record view, `OrcidDataSource.kt:130` delegating to the page call) — because `ExpertDiscoveryService.tryGetEmailFromOrcid` (`:1504`) and the out-of-list `DiscoveryMockHelper.java:180-184` consume the record view; the plan asked only for the additive wrapper, so the older signature was deliberately retained rather than deleted.
- O-4: `sourceStats.apiRequests` is incremented before the call (`ExpertDiscoveryService.kt:497` generic loop, `:738` ORCID loop), so the explicit skip path (`OrcidDataSource.kt:85-89`) reports `apiRequests=1` with zero HTTP requests; the pre-patch blank-query skip incremented the counter at the same place, so behaviour is byte-identical to the old state — outside the light gate, but c9/c10 funnel reporting may care.

### Required Action
- COMPLETE_CHILD
