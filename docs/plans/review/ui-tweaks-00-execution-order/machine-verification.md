# Review-Fast-P Machine Verification — master: docs/plans/2026-08-21/ui-tweaks-00-execution-order.md

## Epoch 1 — 2026-08-21

- Master plan: docs/plans/2026-08-21/ui-tweaks-00-execution-order.md
- Governing master identity: worktree sha256 87848b296a47161fcba1bd85ac04029c22be46e264c50242486f3b0d45ec55d2; recorded identity commit 737b4b1a974a5ef40e5f46f2aa8b8b3c09e8c4fa
- Master identity state: AMENDMENT_RECORDED (A3: master plan 2cbf6d3 → 737b4b1a; master rule "master 计划「为什么必须按序」缓存键同步约定 + K-frontend-cache-key-triad"; reason "本 run 每个后续子计划都需同步此前子计划新增的三键断言测试，避免逐计划重复暂停"; approval HUMAN:user selected "Approve A2 + A3 (recommended)" (ask p2_amendment, 2026-08-21)). Invoked identity sha256 136dee5c1746395a72a73a21e9e1a908afa23deb192141ab7e950d9b6931c09e == pre-amendment content at 2cbf6d3; divergence fully covered by A3. Retroactively authorized files per amendments: src/test/js/unmatchedQaReplySource.test.js (A1), src/test/js/checkRepliesRelocation.test.js (A2), src/test/js/expertProfileAbsence.test.js (A4), src/test/js/qaFactCardEditor.test.js (A5) — all judged PASS against their stated master rules (see matrix M-5..M-8).
- Boundary: bb34ca2001d0abeac3bd7a8fc13995769e14143e..c13b12d8c25652b5047889c4075aba6c9c4a5bbf (product/test diff: 20 files, +2008/-452; docs/plans excluded from product scope)
- Reviewer: AggregateReviewerEpoch1 (fresh subagent, no inherited implementation/verification conversation; differs from every fast-p writer/verifier: P1Implementer-2/P1Verifier-2, P2Implementer/P2Verifier, P3Implementer/P3Verifier, P4Implementer/P4Verifier2)
- Result: PASS
- Convergence: INITIAL
- Repair artifact/result: N/A (no repair planned; verify-p returned PASS)

## Verification Result: PASS

Plan: docs/plans/2026-08-21/ui-tweaks-00-execution-order.md (master; governing worktree sha256 87848b296a47161fcba1bd85ac04029c22be46e264c50242486f3b0d45ec55d2, recorded identity commit 737b4b1a974a5ef40e5f46f2aa8b8b3c09e8c4fa; invoked identity sha256 136dee5c1746395a72a73a21e9e1a908afa23deb192141ab7e950d9b6931c09e, divergence fully covered by recorded amendment A3)
Implementation boundary: bb34ca2001d0abeac3bd7a8fc13995769e14143e..c13b12d8c25652b5047889c4075aba6c9c4a5bbf (product/test diff: 20 files, +2008/-452 — confirmed via git diff --numstat; docs/plans excluded from product scope)
Convergence: INITIAL (review epoch 1; no prior aggregate report exists for this slug)
Manual acceptance: PENDING (handoff outcome READY_FOR_HUMAN_REVIEW; no human manual-acceptance checks were performed or simulated)

### Commands
| Command | Result | Evidence |
|---|---|---|
| `node --test src/test/js/*.test.js` | PASS | exit 0; tests 722, pass 722, fail 0, cancelled 0, skipped 0 (fresh run, node v25.7.0) |
| `node --check src/main/resources/static/app.js src/main/resources/static/trust-reply-workbench.js` | PASS | exit 0, no output (task-modal-runtime.js not in this boundary) |
| `git diff --check bb34ca2 c13b12d` | PASS | exit 0, no output |
| `JAVA_HOME=/Library/Java/JavaVirtualMachines/zulu-11.jdk/Contents/Home mvn test` | PASS | exit 0, BUILD SUCCESS; `Tests run: 2695, Failures: 0, Errors: 0, Skipped: 4` (baseline 2693 +2 new P4 backend tests; Skipped 4 = pre-existing @Disabled integration tests); exec:3.1.0 node-test / node-check-app / node-check-task-modal-runtime ran in test phase (skipNodeTests not active; JS suite output visible in log) |
| `mvn test -Dtest=FlywayMigrationIntegrationTest -DmigrationIt=true` (p4 plan-required) | BLOCKED (environment) | docker unavailable: `docker info` exit 1, `docker version` exit 1. Human-accepted substitute (ledger RECORD_ONLY O-1) = text-level V107 assertions in QaRuleManagementServiceTest.kt:1116-1135; substitute verified present and passing (QaRuleManagementServiceTest: 60 tests, Failures 0, Errors 0) |

### Contract Matrix (master plan + amendments A1-A5 + child plans; every mandatory item)
| ID | Requirement | Mandatory | Verdict | Evidence |
|---|---|---|---|:---:|---|
| M-1 | Serial execution ordering: children p1→p2→p3→p4 depend on previous; final head is cumulative state, not just last child | Yes | PASS | git log bb34ca2..c13b12d shows strictly sequential feat commits 9b90e41→cc9037d→34acb52→42dae13 (+fixes 53e12b9, c13b12d); triad values v9→v10→v11→v12 accumulate correctly at each boundary |
| M-2 | K-frontend-cache-key-triad: index.html styles.css?v= / trust-reply-workbench.js?v= / app.js?v= all equal, one value per child boundary, cumulative at final head | Yes | PASS | Boundary keys: bb34ca2=v8-trust-fact-actions, 9b90e41/53e12b9=v9-check-replies-move, cc9037d=v10-overlay-contrast, 34acb52=v11-reply-subject-prefill, 42dae13/c13b12d=v12-qa-coverage-gate; each commit's index.html has exactly one unique value (grep -o 'v=[0-9a-z-]*' \| sort -u) |
| M-3 | batchSendTaskConsoleVisualFix.test.js:49-51 verbatim triad assertions synced per child | Yes | PASS | Diff per child shows only the 3 `includes` strings changed v8→v9→v10→v11→v12; final head lines 49-51 assert all three v12 assets |
| M-4 | A3 master rule: each child's own cache-key-asserting tests (checkRepliesRelocation, overlayAndDialogContrast, manualReplySubjectPrefill) synced to that child's key values; other assertions verbatim unchanged | Yes | PASS | Per-child diffs show ONLY CACHE_KEY constant + test-title literal changed (2 lines/file/child), all other assertions untouched; p2: checkRepliesRelocation v9→v10; p3: both files v10→v11; p4: all three files v11→v12 |
| M-5 | Retroactively authorized file A1: src/test/js/unmatchedQaReplySource.test.js — judge against A1 rule (P1 I-4 + K-ui-removal-retires-obsolete-contract-tests) | Yes | PASS | 4th case flipped to retirement guard: asserts mountAutoPreviewTrustReply / data-trust-reply-auto-preview-host / data-auto-preview-status ABSENT + inherits loadAutoReplyPreview / preview-auto-reply absence asserts; other 7 cases untouched; change follows from rule as written; test green in full suite |
| M-6 | Retroactively authorized file A2: src/test/js/checkRepliesRelocation.test.js — judge against A2 rule (P2 I-8 + K-frontend-cache-key-triad) | Yes | PASS | p2 diff: CACHE_KEY + I-3 title v9→v10 only; follows from rule; test green |
| M-7 | Retroactively authorized file A4: src/test/js/expertProfileAbsence.test.js — judge against A4 rule (P3 I-1/S-1 + K-dom-stub-tests-hide-dangling-refs) | Yes | PASS | p3 diff: single added line `vm.runInContext(extractFunction("buildManualReplySubject"), sandbox);` before showUnmatchedDetail registration (expertProfileAbsence.test.js:348); follows from rule (sandbox must register new top-level function); test green |
| M-8 | Retroactively authorized file A5: src/test/js/qaFactCardEditor.test.js — judge against A5 rule (P4 I-3/T6 + K-ui-removal-retires-obsolete-contract-tests) | Yes | PASS | Deletes `loadQa does not request coverage-keys endpoint` (7 lines, :99-104 old numbering) which directly contradicted I-3/T6 (loadQa must request /api/qa/coverage-keys); contract now asserted by qaCoverageKeyEditor.test.js (`loadQa fetches coverage-keys endpoint`); fix commit c13b12d touches ONLY this file; follows from rule as written (citation inaccuracy in A5's reason is an Observation, not a rule violation) |
| M-9 | Must-not-change 1: trust-reply-workbench.js AUTO_PREVIEW mode + readOnly branches (MODES/MODE_SOURCE/renderReadOnlyZone/requestJson write-gate/autoRunBar suppression, 11 sites) NOT deleted | Yes | PASS | P1 diff on trust-reply-workbench.js = 0 lines (byte-identical); final head has 23 matches for AUTO_PREVIEW/MODES/MODE_SOURCE/renderReadOnlyZone/autoRunBar; trustReplyWorkbenchSharedMount.test.js:420-mode guard intact |
| M-10 | Must-not-change 2: backend AutoReplyPreviewService + GET /api/mail/unmatched-inbound/{id}/auto-reply-preview NOT deleted | Yes | PASS | UnmatchedInboundMailController.kt:259 @GetMapping("/unmatched-inbound/{id}/auto-reply-preview") → autoReplyPreviewService.preview(id).toResponse(); import at :13; no Kotlin file for it deleted in boundary |
| M-11 | Must-not-change 3: mailbox view does NOT add resumeProgressPollingIfNeeded() | Yes | PASS | app.js:1669 call is inside `if (view === "contacts")` branch of setView; second call at :12683 (startup); view-mailbox path has no new call |
| M-12 | Must-not-change 4: app.js 未填写 label in manual-reply collapse header unchanged | Yes | PASS | app.js:10173 `<span class="reply-workflow-status">未填写</span>` inside 人工富文本回复 summary (line shifted from plan-printed 9952 by earlier children); not in any child diff |
| M-13 | P1 contract (I-1..I-6, S-1..S-3): checkReplies move + auto-preview removal | Yes | PASS | I-1: id="checkRepliesBtn" exactly 1× in index.html (mailbox view, after view-mailbox), 5× app.js verbatim (689/700/5202/5224/5525), P1 app.js diff contains 0 checkRepliesBtn lines; I-2: P1 diff has 0 setView/resumeProgressPollingIfNeeded/expert-select-cb hunks; I-3: v9 at p1 boundary; I-4: grep auto-preview/autoPreview/AUTO_PREVIEW/auto-reply-preview over app.js+index.html+styles.css exit 1 (0 hits), degraded copy text gone, reply-workflow-detail count 8→6 (delta 2); I-5: unmountMailboxTrustReplyHosts() count = 9 (1 def + 8 call sites), body single-line unmountLiveTrustReply(); I-6: requireTrustReplyWorkbenchRuntime(host) = 3 occurrences (186/3783/9938) and sharedMount.test.js:351 asserts 3; S-1: .panel-head-actions block verbatim in styles.css (after :830), used exactly once in index.html, batchEntryRelocation I3-2 rewritten to new skeleton; S-2: view-contacts fragment contains no checkRepliesBtn, keeps bulkAutoReplyBtn before backfillOperatorStatusBtn |
| M-14 | P2 contract (I-1..I-8, S-1..S-3): overlay + confirm-dialog contrast | Yes | PASS | I-1: renderBusyOverlay() 2 hits (renderMarkup interpolation :2057 + def :2088), createElement count 0 in workbench; I-2: `</section>${renderBusyOverlay()}</div>` = overlay is last child of .reply-workflow-content; I-3: position:absolute count 28(base)→29(final), delta exactly 1; I-4: data-action="cancel-generation" count = 2 (toolbar+overlay), onClick function not in diff, onClick single cancel-generation branch per overlay test; I-5: busyOverlayState() six branches in order requests.pending→factChangePending→stateSavePending→generation.pending→frameSavePending→completePending mirroring factActionBlockReason priority (factActionBlockReason body untouched); I-6: trustReplyWorkbench.test.js:573 no-style assertion passes; I-7: .action-dialog background rgba(255,255,255,0.97)+blur, no var(--panel-bg) in .action-dialog block, dark-pair overrides for .trust-reply-busy-overlay/.trust-reply-busy-card/.action-dialog inside prefers-color-scheme dark block; I-8: v10 at p2 boundary; S-1: .reply-workflow-content rule block verbatim; S-2/S-3 rule blocks verbatim (diff matched) |
| M-15 | P3 contract (I-1..I-5, S-1): manualReplySubject prefill (new top-level buildManualReplySubject, sandbox-registered per A4) | Yes | PASS | I-1: buildManualReplySubject (app.js:1491-1501) mirrors GroundedAutoReplyDecisionService.buildReplySubject (Kotlin :93-103) exactly (trim, blank→"Re:", Re:-prefix case-insensitive, else "Re: "+s); I-2: `.slice(0, 255)` inside function; I-3: S-1 line verbatim `value="${escapeHtml(buildManualReplySubject(record.subject))}"` inserted between placeholder and style; manualReplySubject appears exactly 2× in app.js (9865 render / 10332 read), no `.value =` assignment, no addEventListener on the id; I-4: no placeholder rewriting (no replace in function); I-5: v11 at p3 boundary; styles.css/trust-reply-workbench.js/Kotlin zero-change in p3 |
| M-16 | P4 contract (I-1..I-8, T1-T11): backend QA catalog/controller/service + V107 migration + frontend qaCoverageKeyEditor changes + qaFactCardEditor stale-case retirement (A5) | Yes | PASS | I-1: QaCoverageKeyCatalog.kt validateControlledBody uses `firstOrNull { it.keys == parsed } ?: return` (exact-group trigger), 4 groups get id G1..G4/name; I-2: frontend evaluateQaCoverageGate mirrors, only drift disables save, no skipCoverageGate/forceSave anywhere in app.js (grep 0), backend error retention intact (`saveQaRule(event).catch((error) => showStatus(...))` at app.js:11284); I-3: saveQaRule payload `coverageKeys: collectQaCoverageKeys()` (app.js:3212), collect returns [] when empty, backend updateRule null→parseStored(existing) branch intact (QaRuleManagementService.kt:101-103); I-4: revoke-do unchecks EVERY key of group together (G3 pair), controller adds only @GetMapping read endpoints, no write endpoints for controlled groups; I-5: authorities endpoint advisory-only, revoke card filters current rule (renderQaGateRevokeCard), impact-ok/impact-bad wording, save state unchanged; I-6: V107 SQL verbatim = plan T3 (9-key write string, updated_at=updated_at, WHERE id=24 + 11-key baseline guard matching V76 backfill exactly, no answer_body/reply_body); I-7: collectQaCoverageKeys walks all .qa-cov-input, 31-key resident-DOM test; I-8: v12 triad at final head; T8: qaCoverageKeyEditor absence→presence reversals + 4-state gate + partial-not-blocking + I-5/I-4 tests; T9: real rule-24 11-key fixture create/update preserved verbatim, mixed keys accepted, unpaired contract.party accepted; T10: batchSend v12; T11: qaFactCardEditor deletion; QaRuleManagementServiceTest 60/60 pass |
| M-17 | Authorized cumulative file scope: exactly the amended authorized lists (P1 9, P2 6, P3 7, P4 11 + A3 triad tests); no out-of-scope files | Yes | PASS | Per-child `git diff --name-only` matches each authorized list exactly (p1: 9 files; p2: 6; p3: 7; p4: 14 = 11 + 3 A3 triad files); union = 20 files, matching boundary diff (20 files, +2008/-452); no file outside the union changed |
| M-18 | Required commands pass freshly (node suite, node --check, git diff --check, mvn test) | Yes | PASS | See Commands table (4/4 executed commands PASS; flyway docker item BLOCKED with verified accepted substitute) |
| M-19 | Manual acceptance | Yes (human) | PENDING | Not performed or simulated; children A-1..A-11 checklists + handoff READY_FOR_HUMAN_REVIEW |

### Finding Lineage
| Finding | State | Evidence |
|---|---|---|
| (no prior findings — epoch 1) | N/A | No prior aggregate report exists for this slug; fast-p ledger shows all 4 children terminal LIGHT_PASS (p1 WITH_NOTES) |

### Findings
#### P1
- None.
#### P2
- None.
#### Observations
- OBS-1 [A5 rationale mis-citation]: Amendment A5's reason cites a "positive twin case at :102" in qaFactCardEditor.test.js; no such case exists — the deleted `loadQa does not request coverage-keys endpoint` was the file's ONLY coverage-keys-asserting case (confirmed by c13b12d diff and grep of the current file). The rule and the change are correct and the contract is asserted by qaCoverageKeyEditor.test.js; the error is confined to amendment rationale and p4 brief (matches ledger RECORD_ONLY O-2).
- OBS-2 [P4 T9 sub-item covered by pre-existing tests]: T9 says "新增 disable→改错正文→enable 拒绝", but `invalid controlled rule can be disabled but stays blocked on enable` (QaRuleManagementServiceTest.kt:903) and `enable re-verifies controlled stored rule before saving` (:871) already existed at base and still enforce the I-1 enable-revalidation invariant (disable passes, enable with wrong body throws "canonical body", save not called). Mandatory acceptance criterion satisfied by passing pre-existing tests; no new test added for this sub-item.
- OBS-3 [fast-p evidence SHA streams]: child verify-logs/fix-logs reference backup-stream SHAs (0ad6b3b/b9c8e1d/0d0923f/12b3b7d, branch fast-p-finalization-backup) while the final branch uses cc9037d/34acb52/42dae13/c13b12d; product trees are identical across streams (git diff --stat src/ empty except 0d0923f-vs-42dae13 qaFactCardEditor 7-line direction), so no content discrepancy; the final branch head c13b12d is authoritative and matches the handoff.

### Evidence Boundaries
- Flyway Docker integration run (`mvn test -Dtest=FlywayMigrationIntegrationTest -DmigrationIt=true`) is plan-required for p4 but unexecutable (docker info exit 1, no socket). The human-accepted text-level substitute (V107 assertions at QaRuleManagementServiceTest.kt:1116-1135) is verified present and passing (60/60). What the docker run would additionally prove, recorded as boundary: V107 applies cleanly against the Flyway baseline — real-schema SQL validity, schema_history insertion, and actual id=24 row targeting on a live DB.
- Rule id=24 ↔ Program overview identity cannot be machine-verified without a DB. V107's WHERE guard (id=24 AND the 11-key V76 baseline string, verified verbatim against V76) plus plan T3's documented deployment pre-check (`SELECT ... WHERE id = 24`) mitigate; live-row state is a release-checklist item.
- Manual acceptance items (P1 A-1..A-11, P2 A-1..A-10, P3 A-1..A-9, P4 A-1..A-13) are PENDING; none performed or simulated.
- Visual/UI behavior (button placement, overlay/dialog appearance) verified via source-contract tests and DOM-structure assertions only; no browser-driven visual check was performed in this machine review (covered by the human acceptance checklist).

### Fast-P RECORD_ONLY Re-evaluation
| Source item | Child | Master requirement | Result | Evidence |
|---|---|---|---|---|
| O-1: A1 retirement guard asserts 2 extra absence tokens (loadAutoReplyPreview, preview-auto-reply) beyond A1's enumerated 3 | p1 | A1 rule (P1 I-4 retirement guard) | SATISFIES — not a violation | All five tokens are retired AUTO_PREVIEW identifiers absent from app.js (grep exit 1); extra tokens inherited from the original case, consistent with guard spirit; test green |
| O-1: uncommitted doc-only edits (brief/execution/ledger amendment records) at verify time | p2 | Product scope (docs excluded) | NON-ISSUE — docs-only, product files matched reviewed commit | Not product code; no gate impact |
| O-1: S-1 anchor line drift (app.js:9865 vs plan-printed 9956/10424) | p3 | Master constraint: symbol/DOM anchors authoritative, line numbers cross-check only | NON-ISSUE — content verbatim | Actual implementation matches S-1 verbatim (input line at 9862 with value attr, read at 10332); line drift from earlier children's deletions |
| O-2: mvn test records node gate as exec-maven-plugin node-test execution, not literal "node --test" text | p3 | Required commands incl. node suite in test phase | SATISFIES — confirmed | Fresh mvn log shows exec:3.1.0:exec (node-test) / (node-check-app) / (node-check-task-modal-runtime) in test phase with JS TAP output; standalone node --test run independently verified (722 pass) |
| O-1: Flyway Docker run unexecutable; accepted substitute = text-level V107 assertions | p4 | P4 I-6 (V107 invariants) + required command 5 | SATISFIES via accepted substitute — docker run still an evidence boundary | Substitute assertions exist at QaRuleManagementServiceTest.kt:1116-1135 (updated_at=updated_at, WHERE id=24, 9-key write, 11-key baseline, no answer_body/reply_body) and pass (60/60); docker unavailable confirmed (exit 1); what docker would additionally prove recorded in Evidence Boundaries |
| O-2: brief A5 mis-cited "positive twin case at :102" | p4 | A5 rule (retire stale absence case) | SATISFIES — deletion correct; citation error non-blocking | Deletion is the file's only coverage-keys case; contract asserted by qaCoverageKeyEditor.test.js:138,143 per T8 (passing); see OBS-1 |

### Next Action
- PASS + INITIAL convergence, manual acceptance PENDING → proceed to human acceptance gate (fast-p handoff READY_FOR_HUMAN_REVIEW) or finish the branch. No repair planning: Repair planning: N/A (repair.md not created; verify-p returned PASS).

No product code was modified.
