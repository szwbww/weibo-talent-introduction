# 单封真实邮箱打开实验

状态：已发送，已捕获 1 次图片加载事件；尚待收件人确认人工打开时间。此记录是临时实验，不代表计划功能已实现。

## 授权及范围

用户指定先向 `lukai18014905480@gmail.com` 发送测试邮件，验证打开时是否可获取事件。只发送一封，不改业务发信代码、不修改数据库、不重启应用。

## 已完成证据

- 使用现有启用且未暂停的 `LuKai` 发件账号：`lukai@updates.szwebotech.cn`。
- 独立随机目录位于服务器 Tomcat ROOT，包含探针 GIF 和邮件专用 GIF，各 42 字节。使用项目既有 HTTPS 域名 `qingfei.szwbww.com`。
- 公网探针返回 HTTP 200、`Content-Type: image/gif`，正文 GIF89a。探针与邮件像素使用不同文件名，探针访问不会算进邮件事件。
- 邮件主题：`邮件打开跟踪测试 | 2026-09-25 17:09:48`。
- SMTP 接受时间：2026-09-25 17:09:50 +08:00。
- Message-ID：`<179032738828.9216.8955903862822127840.open-tracking-test@updates.szwebotech.cn>`。
- 2026-09-25 17:10:23 +08:00 第一次检查：邮件专用图片请求 0 条。
- 2026-09-25 17:11:54 +08:00 再次检查：1 条 GET，事件时间 `2026-09-25 17:11:26 +08:00`，HTTP 200，返回 42 字节。记录与邮件独有路径一致；未主动访问该路径。来源 IP 不用于判断收件人真实位置或身份。
- 首次执行因服务器 Python 命令行 Unicode 解码失败，脚本未执行、未连接 SMTP；修正命令编码后单次成功投递，没有重复发送。

## 核对方式与限制

按邮件随机图片路径精确筛选 Tomcat access log，核对 GET 时间、状态与返回字节数；不会主动访问邮件像素制造信号。现有日志格式不包含 User-Agent。

SMTP 接受不等于已进入收件箱；图片请求只证明图片被加载，不能直接证明本人阅读。此实验使用静态 GIF 和既有访问日志，不具备正式计划中的聚合存储、关闭开关或防缓存响应。

临时工具：`/tmp/mail_open_test.py`；私有测试回执：`/tmp/mail-open-test-receipt.json`。回执包含精确测试路径，便于收尾清理；未保存 SMTP 密码。实验结束后已逐文件核验42字节内容，删除该回执指向的两个测试 GIF 与空目录；ROOT其它内容未改。之后再次打开这封旧测试邮件不会继续记录有效GIF加载。
