# 计划自检记录

2026-09-08。仅为计划文档检查，不是代码实施验收，不是生产修复证明。

- 11个子计划，文件数依次10/10/8/10/10/10/10/5/5/10/10，均≤10；测试/迁移/注册文件计入。
- 必需章节齐全且顺序正确；每个I项都有机器验证描述与人工覆盖。
- 两份实际新增CSS全文在样式总契约与08/10逐字相同；CSS花括号配对；新host骨架无inline style。
- 所有Markdown本地链接指向存在文件；总计划包含真实页面预览、逐字基线、代码审计和全部子计划入口。
- 已纠正sourceInboundId错误知识；核实旧专家手动发件仅模板；补齐空PDF被静默跳过和全局异常拦截导致409不生效的真实代码交互。
- 新存储字段/状态默认值、写入者、读取者、所有权、幂等、迁移/恢复边界均有明确契约；新表完整schema单列，既有processing只新增uid_validity一列。
- 分阶段默认关闭新接收模式，前端最后统一注册；保持旧任务明细/未匹配入口，11才全链启用。
- 本阶段未运行业务测试、迁移、SMTP、IMAP下载或生产部署；执行命令写于总计划，不能把此自检当测试通过。
- 人工验收派生文件尚未生成；开始人工验收后再从各A项导出。

已知限制：当前源码与线上部署commit没有重新做版本一致性证明；页面依据来自本会话的真实只读查看和截图，下载状态来自明确标示的mock。上线验收必须记录实际版本与网络请求证据。
