# 删除未确认专家并撤除额外身份限制

2026-09-26。**数据删除已完成；代码已提交本地 main；线上撤除发布被自动审批拒绝，尚未执行。** 没有开启发送，也没有创建自动验证任务。

## 授权与范围

用户要求：“这批错误数据你删掉吧 我不想添加额外逻辑 你这部分逻辑也去掉”。本次按上次隔离清单精确删除 938 名未确认专家，保留来源核实正确的 4 条。938 条是未确认记录，不宣称均已证明姓名错误。

本次指令覆盖旧 02-storage / 04-admission / 05-outreach 计划中的静态删除名单、首发身份门禁及存量晋升身份门禁要求。原计划未改，哈希见 plan-hashes.json；范围和保留行为见 scope.json。目标工作树 `/Users/lukai/IdeaProjects/weibo-talent-introduction`，分支 main，基线 `d3986f2462b396c6ec07155dcb37919baadfee10`。

## 数据结果

- 删除 938 名专家的 938 份 RAW 文档；候选副本此前已删除，本轮不重复计数。此前删除的 68 名不包含在这 938 名中。
- 删除前核对上一轮完整内容、当前版本、三层同邮箱副本，确认清单无变化；在服务器内完整备份后，逐 ID 使用 seq_no/primary_term 条件删除。
- 回读确认目标邮箱在 RAW/CANDIDATE/APPLICATION 三层均为 0；凌晨批次剩 RAW 4、CANDIDATE 4、APPLICATION 0。
- 4 条来源核实记录、1 个既有联系人、1 封历史邮件保持一致；MySQL 业务数据未写入。发送配置 1/3/4/5 的 auto_enabled 均为 0。
- 备份：`/opt/talent/backups/identity-delete-unconfirmed-20260926-103638/`。包含删除前快照、精确操作、执行日志、受保护记录和回读结果；完整专家数据未导出到本机。

## 代码结果

本地 main：`2bcfdf8faeefaa2c167bb8a74714eec6d617d3f3`，16 个代码/测试/资源文件。未推送。其他邮件功能及知识库未提交改动未夹带。

已撤除：

- 自动首发和手动批量首发的额外身份判断，包括查询、计数、分页、重试联系人过滤以及实际发送前重读拦截。
- 存量候选资格、通用候选/有效层晋升、存量补邮箱晋升的额外身份条件。
- 1,919 个邮箱哈希静态名单、加载类和发现入口名单命中拦截。存在可信新来源的邮箱不再因历史名单禁止导入。

保留：原有任务配置、邮箱抑制/重复发送等基础规则；JATS 姓名邮箱解析、来源归属验证、新发现入库校验、原子创建、旧抽取缓存版本和学术补全防错绑。identityVerification 审计字段保留，但不再作为首发资格条件。新的自动发现只有明确归属结果才入库；本次没有重新运行发现批次。

## 验证

对照旧生产代码执行新契约测试：269 项中 39 项失败，确认旧隐藏限制与本次要求冲突。最终代码完成修正后，在与提交逐文件一致的隔离源码快照执行 JDK 11 `mvn clean package`：

- JVM 4,057 项，4,044 通过、13 跳过，失败/错误 0。
- JavaScript 1,193 项全部通过，语法检查通过。
- 新测试覆盖自动/手动首发不要求身份凭证、重试仍服从已配置研发类型、候选资格仍服从已配置条件、历史名单不阻止有来源的导入、无明确归属仍不入库。
- 编译后的首发、重试范围与候选资格类均无 DiscoveryIdentity 调用。源码和发布资源无静态名单加载入口。
- 首次全量回归发现一条旧测试仍要求黑名单拒绝，按新契约改为“无来源拒绝、有来源可导入”后重跑全量通过；未通过跳过测试发布。

日志和摘要见 test-summary.json。完整日志 `/tmp/identity-remove-gates/final-build.log`。

## 发布阻塞

准备的补丁替换 89 项、删除 2 项（黑名单资源及其加载类），所有被替换项均与上次已核验的生产基线匹配。补丁及清单仅上传到服务器 `/tmp/identity-remove-gates-*`；没有运行安装脚本、替换 WAR 或重启。

自动审批拒绝安装：认为移除首发/晋升门禁及黑名单会扩大可发送、可晋升范围，用户授权了代码修改，但需要明确批准该变更线上发布。不能绕过拒绝；等待用户明确授权。线上当前仍为旧包 `dcca6ce9996a35972f5c2d7891c6240c089c0509eb236d0d7777c05b0f1e840f`，原额外门禁仍生效。

## 执行交接

Outcome: BLOCKED（仅生产发布授权阻塞；数据删除及本地代码完成）。Executor: /root。Code SHA: 2bcfdf8faeefaa2c167bb8a74714eec6d617d3f3。Worktree ID: /Users/lukai/IdeaProjects/weibo-talent-introduction@main@/Users/lukai/IdeaProjects/weibo-talent-introduction/.git。

原计划哈希已复核；代码提交属于目标 main；相关源码无未提交差异，其他任务改动保留。上述为执行自检和自动测试证据，不冒充独立代理审核。等待确认的具体动作是将已测补丁安装到 150.158.92.103，保持发送暂停，随后检查应用健康、89 项文件哈希、2 项删除及数据状态。
