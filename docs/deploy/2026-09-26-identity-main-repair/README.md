# 身份修复纳入 main 与凌晨批次修复结果

2026-09-26 10:24 完成。授权：用户要求“先把代码合并到本地 main 分支，然后修复这批数据给我一个结果”。本次没有推送，没有恢复发送，没有创建自动核验任务。

## 本地 main

- `ab4d218b8062a9c553510f2c8503638483149ac2`：身份防错实现、三层存储、队列版本、晋升/首发/补全门禁与测试，共 29 个源码/资源/测试文件。
- `463607372b848388805e3794cf03485cf815dc7a`：四条经原文复核的名单解除及回归测试。
- 两个提交均在本地 main；身份修复文件不再有未提交差异。其他邮件功能、releases.json 等已有未提交改动未夹带进这两个提交。
- 原计划“暂不提交”被本次用户明确提交授权取代。原计划哈希保持，见 `plan-hashes.json`；本轮必要补充为存量档案原文复核凭证、四条定向解除及可回退的数据处理。

## 正确记录与拦截名单交集

预检发现已逐条核对正确的四条记录全部属于此前的 72 条删除名单回流。原方案“删 72、留 4、隔离 934”存在交集，预检在写入前中止；未按错误数量执行。

本次按明确原文复核解除这四个邮箱哈希，删除拦截集合由 1,923 降为 1,919。保留历史原始删除记录。四条来源与哈希见 `reviewed-source-releases.json`：

- Edward Raff 的两个邮箱由[同一论文](https://arxiv.org/html/2609.21306v1)明确归属同一作者。
- 两条 Hang Zhao 各自有署名、机构和邮箱来源：[港科大广州论文](https://arxiv.org/html/2609.22795v1)、[清华相关论文](https://arxiv.org/html/2609.21983v1)。不因重名合并。

新增 `REVIEWED_SOURCE_SHA256` 仅供显式复核后的存量凭证使用，绑定当前邮箱、姓名、版本和原文哈希。自动发现的 `validEvidence` 仍只接受 JATS/ORCID 来源，不能用“已人工复核”标记绕过自动准入；测试覆盖该区别及剩余名单继续拦截。未给未核实的外部作者 ID 授予信任。

## 构建与上线

- 从 `4636073` 的干净 Git 归档构建，不包含工作区其他未提交改动。
- 命令：JDK 11 `mvn clean package`。后端共 4,056 项，4,043 通过、13 跳过，0 失败/错误；前端 1,193 项通过，语法检查通过。完整日志 `/tmp/identity-main-repair/final-build.log`，哈希见 `test-summary.json`。
- 当前 main 原始基线编译类与线上被替换文件逐项匹配，无不明基线差异。基于当前生产 WAR 仅替换 152 项、删除两个失效编译类，所有其他包内文件哈希不变。
- 生产备份 `/opt/talent/backups/identity-main-repair-20260926-102310/`。
- 旧包 `bee031087e6a268bc42d50628165ec384503096b82b4592032bd7f65f3898d95`。
- 新包 `dcca6ce9996a35972f5c2d7891c6240c089c0509eb236d0d7777c05b0f1e840f`。
- 发布前活跃任务为 0；原子替换后健康 HTTP 200，152 项展开文件哈希一致、旧类消失，三层 identityVerification mapping 已核对。
- 这是执行与机器检查证据，不声称独立代理审核，也不代表已经执行下一批发现验收。

## 数据结果

范围严格限定 2026-09-26 00:00—06:00 的本次 1,010 条专家、2,020 份 RAW/CANDIDATE 文档。数据备份在服务器内，未导出全量明文到本机。

| 处理 | 专家数 | 结果 |
|---|---:|---|
| 删除此前删除名单的回流记录 | 68 | RAW 与 CANDIDATE 各删 68 份，APPLICATION 原本为 0 |
| 证据未确认，隔离 | 938 | 仅保留 RAW，标记 UNRESOLVED、identity_review_pending、IDENTITY_UNVERIFIED；移除候选副本 |
| 原文确认，保留 | 4 | RAW/CANDIDATE 均补 VERIFIED 绑定凭证；原姓名、邮箱、业务键保持 |
| 合计 | 1,010 | 全部归入明确处理分组 |

938 条待核验中，681 条原本为空姓名，257 条虽有姓名但未逐条核实。703 条原空姓名中的其余 22 条属于此次删除的 68 条。隔离不等于姓名已修正，不能将 938 条报告为“验证正确”。

执行删除文档 1,074 份、局部更新 946 份。最终本批 RAW 942 份、CANDIDATE 4 份。1,010 是当前独立档案数；原任务报告 1,015 次 indexed/promoted，其相差 5 次的原因本轮没有查实。

## 回读与保护

- 服务器备份 `/opt/talent/backups/identity-data-repair-20260926-101735/`，包含完整 `_source`、`_seq_no`、`_primary_term`、精确操作清单、三篇公开原文及逐批执行日志。目录 0700，专家快照和日志 0600。
- 写前重读全部目标；内容或版本改变即中止。每个删除/更新均带 ES seq_no/primary_term CAS，不使用 delete-by-query，不做 upsert。
- 写后逐条回读全部 2,020 个目标：删除项不存在，更新项与“原始文档＋限定补丁”完全一致，其他字段未变化；回读 PASS。
- 本批关联的 1 个既有联系人、1 封历史邮件的状态、时间与正文哈希保持一致；没有修改 MySQL 业务数据或历史审计。
- 发送配置 1/3/4/5 的 auto_enabled 均保持 0。
- 昨日四条之外的 1,919 个删除拦截哈希继续有效；未自动解除其他历史记录。

## 剩余事项与回退

938 条目前安全隔离，尚需来源复核才能恢复候选资格；这不影响本轮确认的四条。新发现批次的验收仍由用户另行通知。

回退只使用服务器备份，先核对当前 WAR/文档版本。文档恢复不得覆盖后来修改：删除项仅在仍不存在时 create，更新项仅在仍等于本次结果时恢复受影响字段。旧代码缺少身份门禁，不可回退代码后恢复发送。原联系人、邮件与发送配置不随回退改变。
